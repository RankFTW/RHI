diff --git a/.github/workflows/build-installer-ru.yml b/.github/workflows/build-installer-ru.yml
new file mode 100644
index 0000000..e4b46fb
--- /dev/null
+++ b/.github/workflows/build-installer-ru.yml
@@ -0,0 +1,85 @@
+name: Сборка инсталлятора (RU)
+
+# Собирает русифицированный RHI-Setup.exe и выкладывает его в артефакты задачи.
+# Запуск: вкладка Actions → «Сборка инсталлятора (RU)» → Run workflow
+# (или автоматически при пуше в main).
+
+on:
+  workflow_dispatch:
+  push:
+    branches: [ main ]
+    paths-ignore:
+      - '**.md'
+      - 'docs/**'
+      - 'todo/**'
+      - 'screenshots/**'
+
+jobs:
+  build-installer:
+    runs-on: windows-latest
+    defaults:
+      run:
+        shell: pwsh
+
+    steps:
+      - name: Checkout
+        uses: actions/checkout@v4
+
+      - name: Установка .NET 8
+        uses: actions/setup-dotnet@v4
+        with:
+          dotnet-version: 8.0.x
+
+      - name: Публикация RHI (WinUI 3)
+        run: >
+          dotnet publish RenoDXCommander/RenoDXCommander.csproj
+          -c Release -r win-x64 -p:PublishSingleFile=true -p:Platform=x64
+          --self-contained false -o publish/RHI
+
+      - name: Сборка DropHelper
+        run: |
+          dotnet build RHI.DropHelper/RHI.DropHelper.csproj -c Release -v quiet
+          $bin = "RHI.DropHelper/bin/x64/Release/net8.0-windows"
+          $out = "publish/RHI"
+          foreach ($f in @("RHI.DropHelper.exe", "RHI.DropHelper.runtimeconfig.json")) {
+            if (Test-Path "$bin/$f") { Copy-Item "$bin/$f" $out -Force }
+          }
+          if (Test-Path "$bin/RHI.DropHelper.dll") { Copy-Item "$bin/RHI.DropHelper.dll" $out -Force }
+
+      - name: Копирование сопутствующих файлов
+        run: |
+          $src = "RenoDXCommander"
+          $out = "publish/RHI"
+          $files = @(
+            "icon.ico", "7z.exe", "7z.dll",
+            "ReShade.ini", "ReShade.Vulkan.ini", "ReShade64.json",
+            "RHI_PatchNotes.md", "relimiter.ini", "reshade.rdr2.ini",
+            "OptiScaler_nightly.nvidia.ini", "OptiScaler_nightly.amd-dlss.ini", "OptiScaler_nightly.amd-nodlss.ini",
+            "OptiScaler_dlssnr.nvidia.ini", "OptiScaler_dlssnr.amd-dlss.ini", "OptiScaler_dlssnr.amd-nodlss.ini",
+            "OptiScaler.nvidia.ini", "OptiScaler.amd-dlss.ini", "OptiScaler.amd-nodlss.ini"
+          )
+          foreach ($f in $files) {
+            if (Test-Path "$src/$f") { Copy-Item "$src/$f" $out -Force }
+          }
+          New-Item -ItemType Directory -Force -Path "$out/Assets/icons" | Out-Null
+          if (Test-Path "$src/Assets/icons") { Copy-Item "$src/Assets/icons/*" "$out/Assets/icons/" -Force }
+
+      - name: Установка Inno Setup 6
+        run: choco install innosetup -y --no-progress
+
+      - name: Компиляция инсталлятора (русский)
+        run: |
+          $iscc = "C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
+          $pub = (Resolve-Path "publish/RHI").Path
+          $icon = (Resolve-Path "RenoDXCommander/icon.ico").Path
+          $outDir = Join-Path (Get-Location).Path "Installers"
+          New-Item -ItemType Directory -Force -Path $outDir | Out-Null
+          & $iscc "/DPublishDir=$pub" "/DIconFile=$icon" "/DInstallerOutDir=$outDir" "RHI Setup.iss"
+          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
+
+      - name: Загрузка артефакта
+        uses: actions/upload-artifact@v4
+        with:
+          name: RHI-Setup-RU
+          path: Installers/RHI-Setup.exe
+          if-no-files-found: error
diff --git a/README.md b/README.md
index 45472bc..5cf1ff2 100644
--- a/README.md
+++ b/README.md
@@ -1,167 +1,167 @@
 # RHI — ReShade HDR Installer
 
-**One tool. Every game. Full HDR, DLSS, and NVIDIA driver management from a single window.**
+**Один инструмент. Любая игра. Полное управление HDR, DLSS и драйвером NVIDIA из одного окна.**
 
-RHI detects your entire game library across every major store and handles ReShade, HDR mods, frame limiters, DLSS management, OptiScaler, shader packs, and NVIDIA driver profiles — all in one click.
+RHI находит всю вашу игровую библиотеку во всех популярных магазинах и берёт на себя ReShade, HDR-моды, ограничители FPS, управление DLSS, OptiScaler, наборы шейдеров и профили драйвера NVIDIA — всё в один клик.
 
 ---
 
-## Download
+## Загрузка
 
-**[Latest release](https://github.com/RankFTW/RenoDXChecker/releases/latest)** · **[Discord](https://discord.gg/ultraplace)**
+**[Последний релиз](https://github.com/RankFTW/RenoDXChecker/releases/latest)** · **[Discord](https://discord.gg/ultraplace)**
 
 ---
 
-## What It Does
+## Что умеет
 
-### One-Click HDR for Every Game
+### HDR в один клик для каждой игры
 
-- **[RenoDX](https://github.com/clshortfuse/renodx)** — auto-detects and installs the right HDR mod for each game. Hundreds of bespoke game-specific mods, plus generic HDR support for every Unreal Engine and Unity game without one.
-- **UE-Extended** — the default for all Unreal Engine games without a named mod. Automatically configures `reshade.ini` and `Engine.ini` with the correct HDR settings. No manual ini editing.
-- **[Luma Framework](https://github.com/Filoppi/Luma-Framework)** — every DX11 Unreal Engine game in your library gets a Luma row automatically. Named mods for supported titles, generic support for everything else. Engine.ini keys, launch args, and TAA settings applied from the wiki automatically on install.
-- **[DXVK + Lilium HDR](https://github.com/EndlesslyFlowering/dxvk)** — brings scRGB HDR output to older DX9 games that normally have no HDR path at all.
-- **[RenoFX HDR Toolkit](https://github.com/clshortfuse/renofx)** — SDR-to-HDR conversion for any game that doesn't have a dedicated RenoDX mod yet.
+- **[RenoDX](https://github.com/clshortfuse/renodx)** — сам определяет и устанавливает подходящий HDR-мод для каждой игры. Сотни именных модов под конкретные игры плюс универсальная поддержка HDR для любой игры на Unreal Engine и Unity без именного мода.
+- **UE-Extended** — вариант по умолчанию для всех игр на Unreal Engine без именного мода. Сам настраивает `reshade.ini` и `Engine.ini` с правильными HDR-параметрами. Ручное редактирование ini не требуется.
+- **[Luma Framework](https://github.com/Filoppi/Luma-Framework)** — каждая игра на DX11 Unreal Engine в вашей библиотеке автоматически получает строку Luma. Именные моды для поддерживаемых игр, универсальная поддержка для остальных. Ключи Engine.ini, аргументы запуска и настройки TAA подгружаются из вики автоматически при установке.
+- **[DXVK + Lilium HDR](https://github.com/EndlesslyFlowering/dxvk)** — добавляет вывод scRGB HDR в старые игры на DX9, у которых HDR-пути не было вовсе.
+- **[RenoFX HDR Toolkit](https://github.com/clshortfuse/renofx)** — преобразование SDR в HDR для любой игры, под которую ещё нет отдельного мода RenoDX.
 
-### ReShade — Fully Managed
+### ReShade — полностью под управлением
 
-RHI replaces the ReShade installer entirely.
+RHI полностью заменяет собой установщик ReShade.
 
-- Detects each game's graphics API via PE binary scanning and names the DLL correctly — `dxgi.dll`, `d3d9.dll`, `opengl32.dll` — automatically.
-- Latest stable and nightly builds staged on every launch.
-- Per-game channels: Stable, Nightly, Custom, Legacy, or No Addons. Mix freely across your library.
-- Vulkan games use the global implicit layer — one-click install, no manual steps.
-- Foreign DLL detection: if another tool (DXVK, Special K, ENB) is already in the game folder, you're warned before anything is overwritten.
-- `reshade.ini` seeded and merged automatically — hotkeys, screenshot path, peak brightness, and DLSS paths all set without any manual configuration.
+- Определяет графический API каждой игры по PE-заголовку и автоматически даёт DLL правильное имя — `dxgi.dll`, `d3d9.dll`, `opengl32.dll`.
+- Свежие стабильные и ночные сборки загружаются при каждом запуске.
+- Каналы для каждой игры: Stable, Nightly, Custom, Legacy или No Addons. Свободно комбинируются по библиотеке.
+- Vulkan-игры используют глобальный implicit-слой — установка в один клик, без ручных шагов.
+- Обнаружение чужих DLL: если в папке игры уже лежит другой инструмент (DXVK, Special K, ENB), вы получите предупреждение до перезаписи.
+- `reshade.ini` создаётся и объединяется автоматически — горячие клавиши, путь к скриншотам, пиковая яркость и пути DLSS настраиваются сами.
 
-### DLSS 5 and Neural Rendering
+### DLSS 5 и нейронный рендеринг
 
-- **DLSS5 Feeder** — full pipeline install in one click: deploys feeder addon, DLSS5 consumer, `nvngx_dlss.dll`, `nvngx_dlssnr.dll`, pre-configured `ReShadePreset.ini`, and required shader packs. Auto-selects the right method for your game (32-bit, DX11, DX12).
-- **ShortFuse DLSS Tool** — alternative neural rendering implementation for DX12 games.
-- **DX11 Bridge variant** — DLSS5 Tool + bridge for games that need DX11 compatibility.
-- **NR Cost Scaler** — wraps `nvngx_dlssnr.dll` to run neural rendering at reduced cost (default 75%). Toggle before installing a method to deploy it automatically.
+- **DLSS5 Feeder** — установка всего конвейера в один клик: разворачивает аддон Feeder, DLSS5-потребитель, `nvngx_dlss.dll`, `nvngx_dlssnr.dll`, преднастроенный `ReShadePreset.ini` и необходимые наборы шейдеров. Сам выбирает подходящий метод для вашей игры (32 бита, DX11, DX12).
+- **ShortFuse DLSS Tool** — альтернативная реализация нейронного рендеринга для игр на DX12.
+- **Вариант с DX11 Bridge** — DLSS5 Tool + мост для игр, которым нужна совместимость с DX11.
+- **NR Cost Scaler** — оборачивает `nvngx_dlssnr.dll`, чтобы запускать нейронный рендеринг с меньшими затратами (по умолчанию 75%). Включите перед установкой метода — развернётся автоматически.
 
-### DLSS and Streamline Version Manager
+### Менеджер версий DLSS и Streamline
 
-- Swap SR, RR, FG, and Streamline versions per game from a dropdown. Every version from 2.x to current, plus Custom.
-- Original DLLs backed up automatically. Restore the originals any time.
-- Set global defaults once — Quick Apply pushes them to any game in one click.
-- Batch Deploy: select multiple games from a checklist and push versions, presets, and render scales to all of them at once.
-- DLSS presets (J/K/L/M for SR, D/E for RR, A/B for FG) written directly to NVIDIA driver profiles. No Profile Inspector needed.
-- **DLSS SR Render Scale Override** — force 33–100% render resolution per game.
-- **NVIDIA Override** — set the driver's "Latest DLL" flag per component directly from the version dropdown, equivalent to enabling it in NVIDIA App or Profile Inspector.
-- Auto-update for DLSS SR, RR, and FG from the RHI manifest. Opt-in per-game.
+- Подмена версий SR, RR, FG и Streamline для каждой игры из выпадающего списка. Все версии с 2.x до актуальной, плюс Custom.
+- Оригинальные DLL резервируются автоматически. Вернуть оригиналы можно в любой момент.
+- Задайте значения по умолчанию один раз — «Быстрое применение» разом применит их к любой игре.
+- Массовое развертывание: отметьте несколько игр в чек-листе и разом примените к ним версии, пресеты и масштабы рендеринга.
+- Пресеты DLSS (J/K/L/M для SR, D/E для RR, A/B для FG) записываются прямо в профили драйвера NVIDIA. Profile Inspector не нужен.
+- **Переопределение масштаба рендеринга DLSS SR** — принудительный масштаб рендеринга 33–100% для каждой игры.
+- **Переопределение NVIDIA** — включайте флажок драйвера «Latest DLL» для каждого компонента прямо из списка версий, как в NVIDIA App или Profile Inspector.
+- Автообновление DLSS SR, RR и FG из манифеста RHI. По желанию, для каждой игры.
 
-### Multi Frame Generation
+### Мульти-генерация кадров
 
-- **RTX 40 MFG Unlock** — enables DLSS MFG multipliers beyond 2x (up to 6x) on RTX 40 Series via Ultimate ASI Loader. One-click install from the Extras section.
-- **MFG Ada Unlock** — in-memory MFG unlock addon for RTX 40 Series via the addon manager.
-- **RTX 50 Series MFG** — configure Fixed or Dynamic MFG mode, frame count (2x–6x), and dynamic target frame rate from the DLSS section.
+- **RTX 40 MFG Unlock** — включает множители DLSS MFG выше 2x (до 6x) на RTX 40-й серии через Ultimate ASI Loader. Установка в один клик из раздела «Дополнения».
+- **MFG Ada Unlock** — аддон разблокировки MFG в памяти для RTX 40-й серии через менеджер аддонов.
+- **MFG на RTX 50-й серии** — настройка фиксированного или динамического режима MFG, количества кадров (2x–6x) и динамической целевой частоты из раздела DLSS.
 
 ### OptiScaler
 
-- Redirects DLSS API calls to FSR4/FSR3/XeSS for AMD and Intel GPUs, and adds frame generation support where the game doesn't have it.
-- **Stable and Nightly channels** per game, selectable in the cog.
-- **Per-GPU INI templates** — six pre-configured templates (NVIDIA, AMD+DLSS, AMD no-DLSS, each in Stable and Nightly). Seeded on first launch, user-editable, never overwritten.
-- **Streamline + DLSS Enabler deploy** (Nightly) — toggle per game to deploy Streamline DLLs and DLSS Enabler to the OptiScaler folder. Pick the Streamline version per game.
-- Frame Generation settings: FG Input, FG Output, FG Nvngx Replacement, HUD Fix — all configurable in the cog.
-- **OptiPatcher** auto-deployed to all games with OptiScaler installed. Auto-updates silently.
-- ReShade coexistence handled automatically — filenames adjusted, `LoadReshade=true` set.
+- Перенаправляет вызовы API DLSS на FSR4/FSR3/XeSS для GPU AMD и Intel, а также добавляет поддержку генерации кадров там, где её нет в игре.
+- **Каналы Stable и Nightly** для каждой игры, выбираются в шестерёнке.
+- **INI-шаблоны под GPU** — шесть преднастроенных шаблонов (NVIDIA, AMD+DLSS, AMD без DLSS; каждый в вариантах Stable и Nightly). Создаются при первом запуске, редактируются пользователем и не перезаписываются.
+- **Развертывание Streamline + DLSS Enabler** (Nightly) — переключатель для каждой игры: развернуть DLL Streamline и DLSS Enabler в папку OptiScaler. Версия Streamline выбирается для каждой игры.
+- Настройки генерации кадров: FG Input, FG Output, замена FG Nvngx, HUD Fix — всё настраивается в шестерёнке.
+- **OptiPatcher** автоматически разворачивается во все игры с OptiScaler. Тихо обновляется сам.
+- Совместное существование с ReShade обрабатывается автоматически — имена файлов подгоняются, выставляется `LoadReshade=true`.
 
-### Frame Limiters
+### Ограничители FPS
 
-Two per-game frame limiters, mutually exclusive:
+Два ограничителя для каждой игры, взаимоисключающие:
 
-- **[ReLimiter](https://github.com/RankFTW/ReLimiter)** — precision frame pacing with predictive sleep, phase-locked timing grid, and closed-loop correction. VRR-aware, DLSS FG adaptive, Reflex-integrated.
-- **[Display Commander](https://github.com/pmnoxx/display-commander)** — alternative limiter supporting both 32-bit and 64-bit games.
+- **[ReLimiter](https://github.com/RankFTW/ReLimiter)** — прецизионный вывод кадров с предсказуемой задержкой сна, фазо-заблокированной сеткой таймингов и коррекцией с обратной связью. Учитывает VRR, адаптируется к DLSS FG, интегрирован с Reflex.
+- **[Display Commander](https://github.com/pmnoxx/display-commander)** — альтернативный ограничитель, поддерживающий и 32-, и 64-битные игры.
 
-Both deploy as ReShade addons. Both participate in Update All.
+Оба ставятся как аддоны ReShade. Оба участвуют в «Обновить всё».
 
-### NVIDIA Driver Profile Management — Replaces NVIDIA App and Profile Inspector
+### Управление профилями драйвера NVIDIA — замена NVIDIA App и Profile Inspector
 
-All per-game driver settings written directly via NvAPI. No NVIDIA Profile Inspector, no NVIDIA App needed.
+Все индивидуальные настройки драйвера записываются напрямую через NvAPI. Без NVIDIA Profile Inspector и NVIDIA App.
 
-**Per-game overrides:**
-- VSync Mode, Tear Control, Low Latency (Off/On/Ultra)
-- Smooth Motion: Enable, Allowed APIs, Flip Pacing — automatically sets Low Latency to Ultra when enabled
-- Power Management Mode
-- G-Sync per-game toggle
-- ReBAR: Enable (Off/On/Auto), Mode (Standard/Optimized), Size Limit
-- RTX HDR: Enable/Configure with Peak Brightness, Contrast, Saturation, Middle Grey, Debanding
-- DLSS Presets and Render Scale Override (no admin required)
+**Переопределения для каждой игры:**
+- Режим VSync, управление разрывами, Low Latency (выкл/вкл/Ultra)
+- Smooth Motion: включение, разрешённые API, Flip Pacing — при включении автоматически ставит Low Latency на Ultra
+- Режим управления питанием
+- Переключатель G-Sync для каждой игры
+- ReBAR: включение (выкл/вкл/авто), режим (Standard/Optimized), лимит размера
+- RTX HDR: включение/настройка — пиковая яркость, контраст, насыщенность, средний серый, устранение бандинга
+- Пресеты DLSS и переопределение масштаба рендеринга (без прав администратора)
 
-**Global settings (base profile):**
-- Shader Cache Size and Pre-Compile
-- G-Sync Mode and Preferred Refresh Rate
-- Global ReBAR
-- FPS Limit (Frame Rate Limiter V3)
-- Digital Vibrance per display — restored automatically on every app startup
+**Общие настройки (базовый профиль):**
+- Размер кеша шейдеров и предварительная компиляция
+- Режим G-Sync и предпочитаемая частота обновления
+- Глобальный ReBAR
+- Лимит FPS (Frame Rate Limiter V3)
+- Digital Vibrance для каждого дисплея — автоматически восстанавливается при каждом запуске приложения
 
-**Export/Import:** back up all per-game NVIDIA profiles to a JSON file. Import restores everything after a driver reinstall.
+**Экспорт/импорт:** резервное копирование всех индивидуальных профилей NVIDIA в JSON-файл. Импорт возвращает всё после переустановки драйвера.
 
-### HDR Auto-Toggle
+### Автопереключение HDR
 
-Enables Windows HDR when you launch a game through RHI and disables it when the game closes. Works with Steam, Epic, Xbox, and direct exe launches. Per-game override button next to Launch — purple when active.
+Включает Windows HDR при запуске игры через RHI и выключает при закрытии игры. Работает с Steam, Epic, Xbox и прямым запуском exe. Кнопка переопределения рядом с «Запустить» — фиолетовая, когда активно.
 
-### Shader Pack Management — 46 Packs
+### Управление наборами шейдеров — 46 наборов
 
-Every shader pack you'd ever want, all managed in one place:
+Все нужные наборы шейдеров, управляемые из одного места:
 
-- **Essential**: Lilium HDR Shaders (EndlesslyFlowering) — required for HDR tone mapping
-- **Recommended**: crosire reshade-shaders, PumboAutoHDR, MaxG2D Simple HDR, clshortfuse shaders, RenoFX HDR Toolkit, smolbbsoop shaders
-- **Extra**: 39 additional packs — SweetFX, OtisFX, qUINT, iMMERSE, Prod80, CorgiFX, ZenteonFX, CRT-Royale, VRToolkit, GShade-Shaders, QD-OLED APL Fixer, LumaBoost, and more
+- **Необходимые**: Lilium HDR Shaders (EndlesslyFlowering) — нужны для HDR-тонмаппинга
+- **Рекомендуемые**: crosire reshade-shaders, PumboAutoHDR, MaxG2D Simple HDR, шейдеры clshortfuse, RenoFX HDR Toolkit, smolbbsoop shaders
+- **Дополнительные**: ещё 39 наборов — SweetFX, OtisFX, qUINT, iMMERSE, Prod80, CorgiFX, ZenteonFX, CRT-Royale, VRToolkit, GShade-Shaders, QD-OLED APL Fixer, LumaBoost и другие
 
-Select packs globally or override per game. Expand any pack to pick individual `.fx` files — tri-state checkbox for partial selection. Save selections as named profiles, export as zip, share directly into Discord.
+Наборы выбираются глобально или индивидуально для игры. Разверните любой набор, чтобы отметить отдельные файлы `.fx` — трёхпозиционный чекбокс для частичного выбора. Сохраняйте подборки как именные профили, экспортируйте в zip, делитесь прямо в Discord.
 
-Custom shaders go in `%LocalAppData%\RHI\Custom\reshade\Shaders\` — RHI deploys them alongside managed packs.
+Свои шейдеры кладутся в `%LocalAppData%\RHI\Custom\reshade\Shaders\` — RHI разворачивает их вместе с управляемыми наборами.
 
-### ReShade Addon Management
+### Управление аддонами ReShade
 
-Global addon manager from the toolbar. Enable/disable per game or globally. Custom addons in `%LocalAppData%\RHI\Custom\Addons\` automatically appear in the picker.
+Глобальный менеджер аддонов на панели инструментов. Включение/отключение для каждой игры или глобально. Свои аддоны из `%LocalAppData%\RHI\Custom\Addons\` автоматически появляются в списке.
 
-### Update All
+### Обновить всё
 
-One button in the toolbar updates ReShade, RenoDX, ReLimiter, Display Commander, OptiScaler, and RE Framework across your entire library. Per-game update exclusions respected. Lights up purple when updates are available.
+Одна кнопка на панели инструментов обновляет ReShade, RenoDX, ReLimiter, Display Commander, OptiScaler и RE Framework во всей библиотеке. Индивидуальные исключения от обновлений учитываются. Кнопка подсвечивается фиолетовым, когда есть обновления.
 
-Background check every 4 hours while running — works from the system tray without keeping the window open.
+Фоновая проверка каждые 4 часа во время работы — прямо из системного трея, без открытого окна.
 
-### Manifest-Driven Updates
+### Обновления на основе манифеста
 
-Game data, mod links, Engine.ini configurations, launch fixes, and feature flags are all driven by a manifest fetched from GitHub on every launch. This means:
+Данные об играх, ссылки на моды, конфигурации Engine.ini, исправления запуска и флаги функций — всё управляется манифестом, который загружается с GitHub при каждом запуске. Это значит:
 
-- New game support, fixes, and tweaks deploy to all users immediately — no app update needed
-- Per-game Engine.ini overrides (like Black Myth: Wukong's custom HDR keys) delivered server-side
-- Feature flags enable dev-preview features for all users via manifest toggle
+- Поддержка новых игр, исправления и правки сразу достаются всем пользователям — обновление приложения не требуется
+- Индивидуальные переопределения Engine.ini (как кастомные HDR-ключи Black Myth: Wukong) доставляются сервером
+- Флаги функций включают дев-превью возможности для всех через переключение в манифесте
 
 ---
 
-## Supported Stores
+## Поддерживаемые магазины
 
 Steam · Epic Games · GOG · Xbox / Game Pass · EA App · Ubisoft Connect · Battle.net · Rockstar · itch.io
 
-Drag and drop any game's `.exe` onto the window to add it manually if it's not auto-detected.
+Если игра не определилась сама — перетащите её `.exe` на окно, чтобы добавить вручную.
 
 ---
 
-## Requirements
+## Требования
 
 - Windows 10 / 11
 - [.NET 8 Desktop Runtime](https://dotnet.microsoft.com/download/dotnet/8.0)
-- NVIDIA GPU recommended for driver profile features (ReShade and RenoDX work on all GPUs)
-- Admin mode required for: ReBAR, Smooth Motion, Low Latency (Ultra), Vulkan ReShade, driver export/import
+- Для драйверных функций рекомендуется GPU NVIDIA (ReShade и RenoDX работают на любых GPU)
+- Режим администратора нужен для: ReBAR, Smooth Motion, Low Latency (Ultra), Vulkan ReShade, экспорта/импорта настроек драйвера
 
 ---
 
-## Third-Party Components
+## Сторонние компоненты
 
-| Component | Author |
-|-----------|--------|
+| Компонент | Автор |
+|-----------|-------|
 | [ReShade](https://reshade.me) | Crosire |
-| [RenoDX](https://github.com/clshortfuse/renodx) | clshortfuse & contributors |
+| [RenoDX](https://github.com/clshortfuse/renodx) | clshortfuse и соавторы |
 | [ReLimiter](https://github.com/RankFTW/ReLimiter) | RankFTW |
 | [Display Commander](https://github.com/pmnoxx/display-commander) | pmnoxx |
 | [RE Framework](https://github.com/praydog/REFramework-nightly) | praydog |
 | [Luma Framework](https://github.com/Filoppi/Luma-Framework) | Pumbo (Filoppi) |
-| [OptiScaler](https://github.com/optiscaler/OptiScaler) | OptiScaler contributors |
-| [DXVK](https://github.com/doitsujin/dxvk) | doitsujin & contributors |
+| [OptiScaler](https://github.com/optiscaler/OptiScaler) | участники разработки OptiScaler |
+| [DXVK](https://github.com/doitsujin/dxvk) | doitsujin и соавторы |
 | [DXVK HDR-mod](https://github.com/EndlesslyFlowering/dxvk) | EndlesslyFlowering (Lilium) |
 | [Ultimate ASI Loader](https://github.com/ThirteenAG/Ultimate-ASI-Loader) | ThirteenAG |
diff --git a/README_NexusMods.bbcode b/README_NexusMods.bbcode
index 562ca1f..1628a22 100644
--- a/README_NexusMods.bbcode
+++ b/README_NexusMods.bbcode
@@ -1,200 +1,200 @@
-[center][size=6][b]RHI — Simplified PC Gaming[/b][/size][/center]
+[center][size=6][b]RHI — упрощённый ПК-гейминг[/b][/size][/center]
 
-One app to manage HDR mods across your entire PC game library. RHI auto-detects games from eight storefronts, installs and updates ReShade, RenoDX, frame limiters, DLSS/Streamline, OptiScaler, and more — all with per-game control and zero manual configuration.
+Одно приложение для управления HDR-модами во всей библиотеке ПК-игр. RHI сам находит игры из восьми магазинов, устанавливает и обновляет ReShade, RenoDX, ограничители FPS, DLSS/Streamline, OptiScaler и многое другое — с индивидуальными настройками для каждой игры и без ручной конфигурации.
 
 [center][img]https://raw.githubusercontent.com/RankFTW/RHI/main/screenshots/game_view.png[/img][/center]
 
-[color=#FF6600][b]⚠ Single-player only.[/b][/color] RHI installs ReShade with addon support, which may trigger anti-cheat in online games. Uninstall before playing multiplayer.
+[color=#FF6600][b]⚠ Только для одиночной игры.[/b][/color] RHI устанавливает ReShade с поддержкой аддонов, что может сработать в античите сетевых игр. Перед мультиплеером удаляйте.
 
 [line]
 
-[size=5][b]Why RHI?[/b][/size]
+[size=5][b]Почему RHI?[/b][/size]
 
 [list]
-[*][b]8-store detection[/b] — Steam, GOG, Epic, EA App, Ubisoft Connect, Xbox/Game Pass, Battle.net, Rockstar. No manual setup.
-[*][b]10 managed components[/b] — ReShade, RenoDX, RenoDX Upgrade, ReLimiter, Display Commander, OptiScaler, RE Framework, Luma Framework, DXVK, DOF Fix. One-click install, update, and removal for each.
-[*][b]46 shader packs[/b] — RHI Managed (built-in packs), Custom (your own shaders), or Off (no shader deployment). Global or per-game. Per-file selection lets you tick only the individual shaders you want. Save, load, and share named shader profiles — export to a zip and paste into Discord.
-[*][b]Generic Luma for all DX11 UE games[/b] — every DX11 Unreal Engine game in your library gets a Luma row. Game-specific Engine.ini tweaks and launch args are applied automatically on install.
-[*][b]DLSS & Streamline management[/b] — swap SR, Ray Reconstruction, and Frame Generation independently. Update or downgrade Streamline as a set. Per-game DLSS presets without NVIDIA Profile Inspector.
-[*][b]Nvidia Profile Overrides[/b] — VSync, Low Latency, Smooth Motion, Power Mode, ReBAR, Multi Frame Generation, DLSS render scale (33–100%). All per-game, written directly to NVIDIA driver profiles.
-[*][b]Batch deploy[/b] — update DLSS/Streamline versions and presets across multiple games at once.
-[*][b]DLSS & Streamline Defaults[/b] — configure preferred versions, presets, and render scales. One-click Quick Apply per game.
-[*][b]Profile Export/Import[/b] — back up all per-game NVIDIA profile settings to JSON. Restore after driver updates.
-[*][b]Drag-and-drop[/b] — drop an exe, addon, preset, Luma archive, or URL. RHI figures out what to do.
-[*][b]Two view modes[/b] — Detail and Compact. Toggle between them in one click.
-[*][b]Game Launch[/b] — Steam uses -applaunch (with overlay and playtime tracking), Epic uses its protocol, everything else launches directly. Custom exe and arguments per game.
-[*][b]HDR Auto-Toggle[/b] — automatically enables Windows HDR on game launch and disables on exit. Global or per-game. No more manual toggling in Windows Settings.
-[*][b]Running game indicator[/b] — sidebar highlights green when a game launched through RHI is running.
-[*][b]System Tray & Jump List[/b] — minimize to the system tray on close. Right-click the tray or taskbar icon to launch recent games instantly.
-[*][b]Automatic background updates[/b] — all mod and app updates re-checked every 4 hours while running.
-[*][b]Custom Addons[/b] — drop .addon64/.addon32 files in the Custom folder. They appear in the Addon Manager with on/off toggles.
-[*][b]Digital Vibrance[/b] — per-display color saturation control (0–100) restored automatically on startup.
-[*][b]DLSS / Streamline Auto-Update[/b] — games on the latest version are automatically swapped when a new release appears. Manually chosen older versions are left alone.
-[*][b]Peak Brightness[/b] — set your monitor's peak nits once, automatically written to all reshade.ini files on every deploy.
-[*][b]DOF Fix[/b] — one-click install for Unreal Engine 5.0–5.6 games with depth-of-field artifacts. Participates in Update All.
-[*][b]Luma + RenoDX coexistence[/b] — for compatible games, both frameworks run side by side.
-[*][b]Ryubing emulator support[/b] — 9 Switch game addons from Souperman9, downloaded and deployed in one click. Addons self-select which game is running.
-[*][b]RTX HDR per-game toggle[/b] — enable NVIDIA's driver-level HDR injection for any game. Configure Peak Brightness, Contrast, Saturation, Middle Grey, and Debanding. Requires NVIDIA App.
-[*][b]Custom ReShade auto-redeploy[/b] — update a custom ReShade DLL and it's automatically redeployed to all games using it.
-[*][b]Start with Windows[/b] — launch minimized to system tray on startup.
-[*][b]UE-Extended auto-configuration[/b] — reshade.ini [renodx] section and Engine.ini HDR settings are written automatically.
-[*][b]Nexus Mods update alerts[/b] — GraphQL API, no API key required.
-[*][b]Remote manifest[/b] — game-specific fixes pushed over the air without app updates.
-[*][b]Foreign DLL protection[/b] — binary signature scanning prevents accidental overwrites of DXVK, Special K, ENB, etc.
-[*][b]Addon file watcher[/b] — auto-detects new addons in your Downloads folder and prompts to install.
-[*][b]Message of the Day[/b] — important notices delivered on launch when something changes.
+[*][b]Определение из 8 магазинов[/b] — Steam, GOG, Epic, EA App, Ubisoft Connect, Xbox/Game Pass, Battle.net, Rockstar. Без ручной настройки.
+[*][b]10 управляемых компонентов[/b] — ReShade, RenoDX, RenoDX Upgrade, ReLimiter, Display Commander, OptiScaler, RE Framework, Luma Framework, DXVK, DOF Fix. Установка, обновление и удаление каждого в один клик.
+[*][b]46 наборов шейдеров[/b] — Под управлением RHI (встроенные наборы), Свои (ваши шейдеры) или Выкл (без развертывания шейдеров). Глобально или для каждой игры. Выбор по файлам: отмечайте только нужные шейдеры. Сохраняйте, загружайте и делитесь именными профилями шейдеров — экспорт в zip и вставка в Discord.
+[*][b]Универсальный Luma для всех DX11 UE-игр[/b] — каждая игра на DX11 Unreal Engine в библиотеке получает строку Luma. Игровые правки Engine.ini и аргументы запуска применяются автоматически при установке.
+[*][b]Управление DLSS и Streamline[/b] — независимая подмена SR, Ray Reconstruction и генерации кадров. Обновление или откат Streamline комплектом. Пресеты DLSS для каждой игры без NVIDIA Profile Inspector.
+[*][b]Переопределения профиля NVIDIA[/b] — VSync, Low Latency, Smooth Motion, режим питания, ReBAR, мульти-генерация кадров, масштаб рендеринга DLSS (33–100%). Всё индивидуально, записывается прямо в профили драйвера NVIDIA.
+[*][b]Массовое развертывание[/b] — обновление версий и пресетов DLSS/Streamline сразу в нескольких играх.
+[*][b]DLSS и Streamline по умолчанию[/b] — настройка предпочитаемых версий, пресетов и масштабов рендеринга. «Быстрое применение» к каждой игре в один клик.
+[*][b]Экспорт/импорт профилей[/b] — резервное копирование всех индивидуальных настроек профилей NVIDIA в JSON. Восстановление после обновлений драйвера.
+[*][b]Drag-and-drop[/b] — перетащите exe, аддон, пресет, архив Luma или ссылку. RHI сам разберётся, что делать.
+[*][b]Два режима просмотра[/b] — подробный и компактный. Переключение в один клик.
+[*][b]Запуск игр[/b] — Steam через -applaunch (с оверлеем и учётом времени), Epic через свой протокол, остальное — напрямую. Свои exe и аргументы для каждой игры.
+[*][b]Автопереключение HDR[/b] — автоматически включает Windows HDR при запуске игры и выключает при выходе. Глобально или для каждой игры. Больше не нужно лазить в параметры Windows.
+[*][b]Индикатор запущенной игры[/b] — боковая панель подсвечивается зелёным, когда запущенная через RHI игра работает.
+[*][b]Системный трей и список переходов[/b] — сворачивается в трей при закрытии. Правый клик по значку в трее или на панели задач — мгновенный запуск недавних игр.
+[*][b]Фоновые автообновления[/b] — все обновления модов и приложения перепроверяются каждые 4 часа во время работы.
+[*][b]Свои аддоны[/b] — положите файлы .addon64/.addon32 в папку Custom. Они появятся в менеджере аддонов с переключателями вкл/выкл.
+[*][b]Digital Vibrance[/b] — насыщенность цвета для каждого дисплея (0–100), автоматически восстанавливается при запуске.
+[*][b]Автообновление DLSS / Streamline[/b] — игры на последней версии автоматически переключаются на новую при её выходе. Вручную выбранные старые версии не трогаются.
+[*][b]Пиковая яркость[/b] — задайте пиковые ниты монитора один раз, значение автоматически записывается во все reshade.ini при каждом развертывании.
+[*][b]DOF Fix[/b] — установка в один клик для игр на Unreal Engine 5.0–5.6 с артефактами глубины резкости. Участвует в «Обновить всё».
+[*][b]Сосуществование Luma + RenoDX[/b] — для совместимых игр оба фреймворка работают бок о бок.
+[*][b]Поддержка эмулятора Ryubing[/b] — 9 аддонов Switch-игр от Souperman9, скачиваются и разворачиваются в один клик. Аддоны сами определяют запущенную игру.
+[*][b]Переключатель RTX HDR для каждой игры[/b] — включайте HDR-внедрение драйвера NVIDIA для любой игры. Настройка пиковой яркости, контраста, насыщенности, среднего серого и устранения бандинга. Требуется NVIDIA App.
+[*][b]Автоперенос своего ReShade[/b] — обновите свою DLL ReShade, и она автоматически разнесётся по всем играм, которые её используют.
+[*][b]Запуск с Windows[/b] — старт свёрнутым в системный трей.
+[*][b]Автонастройка UE-Extended[/b] — секция [renodx] в reshade.ini и HDR-настройки Engine.ini записываются автоматически.
+[*][b]Оповещения об обновлениях Nexus Mods[/b] — GraphQL API, без API-ключа.
+[*][b]Удалённый манифест[/b] — исправления для конкретных игр доставляются «по воздуху» без обновлений приложения.
+[*][b]Защита от чужих DLL[/b] — сканирование бинарных сигнатур предотвращает случайную перезапись DXVK, Special K, ENB и т.п.
+[*][b]Наблюдение за папкой аддонов[/b] — сам замечает новые аддоны в папке загрузок и предлагает установить.
+[*][b]Сообщение дня[/b] — важные объявления при запуске, когда что-то меняется.
 [/list]
 
 [line]
 
-[size=5][b]Managed Components[/b][/size]
+[size=5][b]Управляемые компоненты[/b][/size]
 
 [list]
-[*][b][url=https://reshade.me]ReShade[/url][/b] — Post-processing injection framework. Channels: Stable, Nightly, Custom (user DLLs), Legacy (pin to 6.0.0+), No Addons.
-[*][b][url=https://github.com/clshortfuse/renodx]RenoDX[/url][/b] — HDR mod framework. Game-specific mods matched from the RenoDX wiki with generic Unreal/Unity/UE-Extended fallbacks.
-[*][b][url=https://github.com/RankFTW/ReLimiter]ReLimiter[/url][/b] — Frame pacing addon with configurable OSD hotkey and shared presets.
-[*][b][url=https://github.com/pmnoxx/display-commander]Display Commander[/url][/b] — Alternative frame rate limiter. Mutually exclusive with ReLimiter.
-[*][b][url=https://github.com/optiscaler/OptiScaler]OptiScaler[/url][/b] — Upscaler redirection (DLSS ↔ FSR ↔ XeSS) and frame generation on any GPU. Stable and Nightly channels. Per-game FG settings, presets, and DLSS SR/RR/Render Scale controls via the ⚙ cog.
-[*][b][url=https://github.com/praydog/REFramework-nightly]RE Framework[/url][/b] — Required for ReShade on RE Engine games (Monster Hunter Wilds, Resident Evil, DMC5, SF6, etc.).
-[*][b][url=https://github.com/Filoppi/Luma-Framework]Luma Framework[/url][/b] — HDR modding framework for DX11 games. Supported for named mods and all DX11 Unreal Engine games (generic). ReShade and DLSS managed by RHI.
-[*][b][url=https://github.com/doitsujin/dxvk]DXVK[/url][/b] — DirectX-to-Vulkan translation for DX8–DX10 games. Variants: Development, Stable, Lilium HDR (scRGB output). Per-game selection.
-[*][b][url=https://github.com/RankFTW/rhi-repo/releases]DOF Fix[/url][/b] — Fixes depth-of-field stepping/tiling artifacts in Unreal Engine 5.0–5.6 games. One-click install, participates in Update All.
+[*][b][url=https://reshade.me]ReShade[/url][/b] — фреймворк внедрения пост-обработки. Каналы: Stable, Nightly, Custom (свои DLL), Legacy (фиксация на 6.0.0+), No Addons.
+[*][b][url=https://github.com/clshortfuse/renodx]RenoDX[/url][/b] — фреймворк HDR-модов. Игровые моды подбираются из вики RenoDX с универсальными запасными вариантами Unreal/Unity/UE-Extended.
+[*][b][url=https://github.com/RankFTW/ReLimiter]ReLimiter[/url][/b] — аддон вывода кадров с настраиваемой горячей клавишей OSD и общими пресетами.
+[*][b][url=https://github.com/pmnoxx/display-commander]Display Commander[/url][/b] — альтернативный ограничитель частоты кадров. Взаимоисключаем с ReLimiter.
+[*][b][url=https://github.com/optiscaler/OptiScaler]OptiScaler[/url][/b] — перенаправление апскейлеров (DLSS ↔ FSR ↔ XeSS) и генерация кадров на любом GPU. Каналы Stable и Nightly. Настройки FG, пресеты и управление DLSS SR/RR/масштабом для каждой игры через шестерёнку ⚙.
+[*][b][url=https://github.com/praydog/REFramework-nightly]RE Framework[/url][/b] — нужен для ReShade в играх на RE Engine (Monster Hunter Wilds, Resident Evil, DMC5, SF6 и т.д.).
+[*][b][url=https://github.com/Filoppi/Luma-Framework]Luma Framework[/url][/b] — фреймворк HDR-моддинга для DX11-игр. Поддерживается для именных модов и всех DX11 Unreal Engine игр (универсально). ReShade и DLSS под управлением RHI.
+[*][b][url=https://github.com/doitsujin/dxvk]DXVK[/url][/b] — трансляция DirectX в Vulkan для игр DX8–DX10. Варианты: Development, Stable, Lilium HDR (вывод scRGB). Выбор для каждой игры.
+[*][b][url=https://github.com/RankFTW/rhi-repo/releases]DOF Fix[/url][/b] — исправляет артефакты ступенчатости/тайлинга глубины резкости в играх на Unreal Engine 5.0–5.6. Установка в один клик, участвует в «Обновить всё».
 [/list]
 
 [line]
 
-[size=5][b]Key Features[/b][/size]
+[size=5][b]Ключевые возможности[/b][/size]
 
-[size=4][b]DLSS & Streamline[/b][/size]
+[size=4][b]DLSS и Streamline[/b][/size]
 
 [list]
-[*]Swap DLSS SR, Ray Reconstruction, and Frame Generation to any version independently
-[*]Update or downgrade Streamline as a complete set
-[*]DLSS presets per game — SR: Default/J/K/L/M · RR: Default/D/E · FG: Default/A/B
-[*]DLSS render scale override: 33–100% per game for both SR and RR
-[*]Multi Frame Generation (RTX 50 Series): Mode (Default/Fixed/Dynamic), Frame Count (2x–6x), Target FPS
-[*]Quick Apply deploys your configured defaults to any game in one click
-[*]Batch Deploy updates versions + presets across multiple games simultaneously
-[*]Backup/restore per game (.original files)
+[*]Независимая подмена DLSS SR, Ray Reconstruction и генерации кадров на любую версию
+[*]Обновление или откат Streamline единым комплектом
+[*]Пресеты DLSS для каждой игры — SR: Default/J/K/L/M · RR: Default/D/E · FG: Default/A/B
+[*]Переопределение масштаба рендеринга DLSS: 33–100% для каждой игры, для SR и RR
+[*]Мульти-генерация кадров (RTX 50-я серия): режим (Default/Фиксированный/Динамический), количество кадров (2x–6x), целевой FPS
+[*]«Быстрое применение» разворачивает настроенные значения в любой игре одним кликом
+[*]«Массовое развертывание» обновляет версии + пресеты в нескольких играх одновременно
+[*]Резервные копии для каждой игры (файлы .original)
 [/list]
 
-[size=4][b]Nvidia Profile Overrides[/b][/size]
+[size=4][b]Переопределения профиля NVIDIA[/b][/size]
 
-All per-game via NVIDIA driver profiles. Requires admin (Task Scheduler-based persistent elevation available).
+Всё индивидуально, через профили драйвера NVIDIA. Требуется администратор (доступно постоянное повышение через планировщик заданий).
 
 [list]
-[*][b]VSync[/b] — Mode + Tear Control
-[*][b]Low Latency[/b] — Off / On / Ultra
-[*][b]Smooth Motion[/b] — Enable + APIs + Flip Pacing
-[*][b]Power Mode[/b] — Adaptive / Prefer Max Performance / Optimal
-[*][b]ReBAR[/b] — Enable / Mode / Size Limit
-[*][b]Profile Export/Import[/b] — back up all settings to JSON, restore after driver updates
+[*][b]VSync[/b] — режим + управление разрывами
+[*][b]Low Latency[/b] — выкл / вкл / Ultra
+[*][b]Smooth Motion[/b] — включение + API + Flip Pacing
+[*][b]Режим питания[/b] — Adaptive / Prefer Max Performance / Optimal
+[*][b]ReBAR[/b] — включение / режим / лимит размера
+[*][b]Экспорт/импорт профилей[/b] — резервное копирование всех настроек в JSON, восстановление после обновлений драйвера
 [/list]
 
-[size=4][b]Global Nvidia Settings (Settings page)[/b][/size]
+[size=4][b]Глобальные настройки NVIDIA (страница настроек)[/b][/size]
 
 [list]
-[*]Shader Cache Size
-[*]Shader Pre-Compile
-[*]G-Sync Mode
-[*]Preferred Refresh Rate
-[*]Global ReBAR (On/Off + Size)
-[*]DLSS On-Screen Indicator
+[*]Размер кеша шейдеров
+[*]Предкомпиляция шейдеров
+[*]Режим G-Sync
+[*]Предпочитаемая частота обновления
+[*]Глобальный ReBAR (вкл/выкл + размер)
+[*]Экранный индикатор DLSS
 [/list]
 
-[size=4][b]Admin Mode[/b][/size]
+[size=4][b]Режим администратора[/b][/size]
 
-Task Scheduler-based persistent elevation. Toggle Off/On in Settings. When enabled, RHI silently relaunches elevated on startup — no per-operation UAC prompts. Required for ReBAR, Low Latency (ULL), Smooth Motion, and CPU Scheduling writes.
+Постоянное повышение прав через планировщик заданий. Включается/выключается в настройках. Когда включено, RHI тихо перезапускается с повышенными правами — без запросов UAC на каждую операцию. Необходимо для ReBAR, Low Latency (ULL), Smooth Motion и записи настроек планирования CPU.
 
-[size=4][b]Per-Game Overrides[/b][/size]
+[size=4][b]Переопределения для каждой игры[/b][/size]
 
-DLL naming · Shader mode (Global/Custom/Select/Off) · Addon mode (Global/Select/Off) · Bitness · Graphics API · ReShade channel (Stable/Nightly/Custom/Legacy/No Addons) · DXVK variant · Launch exe + arguments · Update inclusion toggles · Wiki name mapping · Keep ReShade.ini Updated
+Имена DLL · режим шейдеров (Глобальные/Свои/Выбрать/Выкл) · режим аддонов (Глобальные/Выбрать/Выкл) · разрядность · графический API · канал ReShade (Stable/Nightly/Custom/Legacy/No Addons) · вариант DXVK · запускаемый exe + аргументы · включение в обновления · привязка имени в вики · поддержание reshade.ini в актуальном виде
 
 [size=4][b]OptiScaler[/b][/size]
 
-Upscaler redirection and frame generation with Stable and Nightly channels. The ⚙ cog provides per-game settings: FG Input/Output/Nvngx Override, HUD Fix, DLSS SR/RR presets, Render Scale, Streamline/DLSS Enabler deployment, and 4 user-configurable presets (Nightly). Engine.ini tweaks for Unreal Engine games. A two-combo upscaler selector lets you pick the upscaler per API (DX11, DX12, Vulkan) without editing the INI manually.
+Перенаправление апскейлеров и генерация кадров с каналами Stable и Nightly. Шестерёнка ⚙ даёт настройки для каждой игры: FG Input/Output/Nvngx Override, HUD Fix, пресеты DLSS SR/RR, масштаб рендеринга, развертывание Streamline/DLSS Enabler и 4 пользовательских пресета (Nightly). Правки Engine.ini для игр на Unreal Engine. Двойной селектор апскейлера позволяет выбрать апскейлер для каждого API (DX11, DX12, Vulkan) без ручной правки INI.
 
 [size=4][b]HDR Gaming Database[/b][/size]
 
-The RenoDX Info button links directly to HDR Gaming Database entries for supported games.
+Кнопка «Инфо» RenoDX ведёт прямо на записи HDR Gaming Database для поддерживаемых игр.
 
-[size=4][b]UW Fix & Ultra+ Links[/b][/size]
+[size=4][b]UW Fix и ссылки Ultra+[/b][/size]
 
-Quick links to ultrawide fixes (Lyall, RoseTheFlower, p1xel8ted) and Ultra+ mods appear on game cards when available.
+Быстрые ссылки на ультраширокие исправления (Lyall, RoseTheFlower, p1xel8ted) и моды Ultra+ появляются на карточках игр, когда доступны.
 
 [line]
 
-[size=5][b]Quick Start[/b][/size]
+[size=5][b]Быстрый старт[/b][/size]
 
 [list=1]
-[*][b]Download and run RHI[/b] — your game library appears automatically.
-[*][b]Pick a game[/b] from the sidebar. Search or use filter chips to narrow the list.
-[*][b]Click Install[/b] on the components you want — ReShade, RenoDX, a frame limiter.
-[*][b]Launch the game[/b], press [b]Home[/b] to open ReShade, go to [b]Add-ons[/b], and configure RenoDX.
-[*][b]Pick your shaders[/b] — click the [b]Shaders/Addons[/b] toolbar button and choose Global Shaders. Expand any pack to select individual .fx files, save the selection as a named profile, or export it to share.
+[*][b]Скачайте и запустите RHI[/b] — библиотека игр появится автоматически.
+[*][b]Выберите игру[/b] в боковой панели. Ищите или сужайте список фильтрами.
+[*][b]Нажмите «Установить»[/b] у нужных компонентов — ReShade, RenoDX, ограничитель FPS.
+[*][b]Запустите игру[/b], нажмите [b]Home[/b], чтобы открыть ReShade, зайдите в [b]Add-ons[/b] и настройте RenoDX.
+[*][b]Подберите шейдеры[/b] — нажмите кнопку [b]Шейдеры/Аддоны[/b] на панели инструментов и выберите «Глобальные шейдеры». Разверните любой набор, чтобы отметить отдельные файлы .fx, сохраните подборку как именной профиль или экспортируйте для обмена.
 [/list]
 
 [line]
 
-[size=5][b]Download[/b][/size]
+[size=5][b]Загрузка[/b][/size]
 
-Grab the latest release from the [url=https://github.com/RankFTW/RHI/releases]GitHub Releases page[/url].
+Берите последний релиз на [url=https://github.com/RankFTW/RHI/releases]странице релизов GitHub[/url].
 
-[b]Requirements:[/b]
+[b]Требования:[/b]
 [list]
 [*]Windows 10/11 (x64)
 [*][url=https://dotnet.microsoft.com/download/dotnet/8.0].NET 8 Desktop Runtime[/url]
-[*]NVIDIA GPU recommended for DLSS/Streamline and Profile Override features (AMD/Intel supported for everything else)
+[*]Для DLSS/Streamline и переопределений профиля рекомендуется GPU NVIDIA (для всего остального поддерживаются AMD/Intel)
 [/list]
 
 [line]
 
-[size=5][b]Troubleshooting[/b][/size]
+[size=5][b]Решение проблем[/b][/size]
 
 [list]
-[*][b]Game not detected[/b] — Add Game in Settings — pick the game's exe and name it
-[*][b]Xbox games missing[/b] — Click Refresh — Game Pass detection may need a moment
-[*][b]ReShade not loading[/b] — Check the install path via 📁 — the DLL must sit next to the game exe
-[*][b]Black screen (Unreal)[/b] — ReShade → Add-ons → RenoDX → set R10G10B10A2_UNORM to output size
-[*][b]UE-Extended not working[/b] — Enable HDR in the game's display settings first
-[*][b]Downloads failing[/b] — Click Refresh, or clear cache from Settings → Open Downloads Cache
-[*][b]DLSS presets not applying[/b] — Enable Admin Mode in Settings, or run RHI as administrator
-[*][b]Everything out of sync[/b] — Settings → Full Refresh clears all caches and re-scans
+[*][b]Игра не найдена[/b] — «Добавить игру» в настройках: укажите exe игры и название
+[*][b]Игры Xbox отсутствуют[/b] — нажмите «Обновить»: определению Game Pass может понадобиться время
+[*][b]ReShade не загружается[/b] — проверьте путь установки через 📁: DLL должна лежать рядом с exe игры
+[*][b]Чёрный экран (Unreal)[/b] — ReShade → Add-ons → RenoDX → задайте R10G10B10A2_UNORM = output size
+[*][b]UE-Extended не работает[/b] — сначала включите HDR в настройках экрана самой игры
+[*][b]Ошибки загрузок[/b] — нажмите «Обновить» или очистите кеш: Настройки → открыть кеш загрузок
+[*][b]Пресеты DLSS не применяются[/b] — включите режим администратора в настройках или запустите RHI от администратора
+[*][b]Всё разъехалось[/b] — Настройки → «Полное обновление» очистит все кеши и пересканирует
 [/list]
 
-For the full reference covering every feature, see the [url=https://github.com/RankFTW/RHI/blob/main/docs/DETAILED_GUIDE.md]Detailed Guide[/url].
+Полный справочник по всем возможностям — в [url=https://github.com/RankFTW/RHI/blob/main/docs/DETAILED_GUIDE.md]подробном руководстве[/url].
 
 [line]
 
-[size=5][b]Third-Party Components[/b][/size]
+[size=5][b]Сторонние компоненты[/b][/size]
 
 [list]
 [*][url=https://reshade.me]ReShade[/url] — Crosire — [url=https://github.com/crosire/reshade/blob/main/LICENSE.md]BSD 3-Clause[/url]
-[*][url=https://github.com/clshortfuse/renodx]RenoDX[/url] — clshortfuse & contributors — [url=https://github.com/clshortfuse/renodx/blob/main/LICENSE]MIT[/url]
-[*][url=https://github.com/RankFTW/ReLimiter]ReLimiter[/url] — RankFTW — Source-available
+[*][url=https://github.com/clshortfuse/renodx]RenoDX[/url] — clshortfuse и соавторы — [url=https://github.com/clshortfuse/renodx/blob/main/LICENSE]MIT[/url]
+[*][url=https://github.com/RankFTW/ReLimiter]ReLimiter[/url] — RankFTW — открытый исходный код
 [*][url=https://github.com/pmnoxx/display-commander]Display Commander[/url] — pmnoxx — [url=https://github.com/pmnoxx/display-commander/blob/main/LICENSE]GPL-3[/url]
 [*][url=https://github.com/praydog/REFramework-nightly]RE Framework[/url] — praydog — [url=https://github.com/praydog/REFramework/blob/master/LICENSE]MIT[/url]
-[*][url=https://github.com/Filoppi/Luma-Framework]Luma Framework[/url] — Pumbo (Filoppi) — Source-available
-[*][url=https://github.com/optiscaler/OptiScaler]OptiScaler[/url] — OptiScaler contributors — Source-available
-[*][url=https://github.com/doitsujin/dxvk]DXVK[/url] — doitsujin & contributors — [url=https://github.com/doitsujin/dxvk/blob/master/LICENSE]Zlib[/url]
+[*][url=https://github.com/Filoppi/Luma-Framework]Luma Framework[/url] — Pumbo (Filoppi) — открытый исходный код
+[*][url=https://github.com/optiscaler/OptiScaler]OptiScaler[/url] — участники разработки OptiScaler — открытый исходный код
+[*][url=https://github.com/doitsujin/dxvk]DXVK[/url] — doitsujin и соавторы — [url=https://github.com/doitsujin/dxvk/blob/master/LICENSE]Zlib[/url]
 [*][url=https://github.com/EndlesslyFlowering/dxvk]DXVK HDR-mod[/url] — EndlesslyFlowering (Lilium) — [url=https://github.com/EndlesslyFlowering/dxvk/blob/HDR-mod/LICENSE]Zlib[/url]
-[*][url=https://www.7-zip.org/]7-Zip[/url] — Igor Pavlov — [url=https://www.7-zip.org/license.txt]LGPL-2.1 / BSD-3-Clause[/url]
+[*][url=https://www.7-zip.org/]7-Zip[/url] — Игорь Павлов — [url=https://www.7-zip.org/license.txt]LGPL-2.1 / BSD-3-Clause[/url]
 [/list]
 
-RHI is an unofficial third-party tool, not affiliated with or endorsed by the RenoDX project, Crosire, or the Luma Framework. All mod files are downloaded from their official sources at runtime and are not redistributed.
+RHI — неофициальный сторонний инструмент, не связанный с проектом RenoDX, Crosire или Luma Framework и не одобренный ими. Все файлы модов скачиваются из официальных источников во время работы и не распространяются в составе приложения.
 
 [line]
 
-[size=5][b]Acknowledgements[/b][/size]
+[size=5][b]Благодарности[/b][/size]
 
-RHI would not be possible without the hard work of the entire RenoDX team and [url=https://reshade.me]Crosire[/url], the creator of ReShade. Thank you to every mod author, contributor, and tester who keeps pushing PC HDR forward.
+RHI не состоялся бы без упорного труда всей команды RenoDX и [url=https://reshade.me]Crosire[/url], создателя ReShade. Спасибо каждому автору модов, участнику и тестировщику, кто двигает HDR на ПК вперёд.
 
 [line]
 
-[size=5][b]Links[/b][/size]
+[size=5][b]Ссылки[/b][/size]
 
-[url=https://github.com/clshortfuse/renodx]RenoDX[/url] · [url=https://github.com/clshortfuse/renodx/wiki/Mods]RenoDX Wiki[/url] · [url=https://reshade.me]ReShade[/url] · [url=https://github.com/Filoppi/Luma-Framework]Luma Framework[/url] · [url=https://github.com/Filoppi/Luma-Framework/wiki/Mods-List]Luma Mods List[/url] · [url=https://github.com/RankFTW/ReLimiter]ReLimiter[/url] · [url=https://www.hdrmods.com]HDR Guides[/url]
+[url=https://github.com/clshortfuse/renodx]RenoDX[/url] · [url=https://github.com/clshortfuse/renodx/wiki/Mods]Вики RenoDX[/url] · [url=https://reshade.me]ReShade[/url] · [url=https://github.com/Filoppi/Luma-Framework]Luma Framework[/url] · [url=https://github.com/Filoppi/Luma-Framework/wiki/Mods-List]Список модов Luma[/url] · [url=https://github.com/RankFTW/ReLimiter]ReLimiter[/url] · [url=https://www.hdrmods.com]HDR-руководства[/url]
 
-[url=https://discord.gg/gF4GRJWZ2A]RenoDX Discord[/url] · [url=https://discord.gg/k3cDruEQ]HDR Den Discord[/url] · [url=https://discordapp.com/channels/1296187754979528747/1475173660686815374]RHI Support[/url] · [url=https://discord.gg/pQtPYcdE]Ultra+ Discord[/url]
+[url=https://discord.gg/gF4GRJWZ2A]RenoDX Discord[/url] · [url=https://discord.gg/k3cDruEQ]HDR Den Discord[/url] · [url=https://discordapp.com/channels/1296187754979528747/1475173660686815374]Поддержка RHI[/url] · [url=https://discord.gg/pQtPYcdE]Ultra+ Discord[/url]
 
-[url=https://ko-fi.com/rankftw]Support RHI on Ko-Fi ☕[/url]
+[url=https://ko-fi.com/rankftw]Поддержать RHI на Ko-Fi ☕[/url]
diff --git a/RHI Setup.iss b/RHI Setup.iss
index 0f32b28..56b80cb 100644
--- a/RHI Setup.iss	
+++ b/RHI Setup.iss	
@@ -1,105 +1,134 @@
-; Script generated by the Inno Setup Script Wizard.
-; SEE THE DOCUMENTATION FOR DETAILS ON CREATING INNO SETUP SCRIPT FILES!
-; Non-commercial use only
-
-#define MyAppName "RHI"
-#define MyAppVersion "2.0.1"
-#define MyAppPublisher "RankFTW"
-#define MyAppURL "www.github.com/rankftw"
-#define MyAppExeName "RHI.exe"
-
-[Setup]
-; NOTE: The value of AppId uniquely identifies this application. Do not use the same AppId value in installers for other applications.
-; (To generate a new GUID, click Tools | Generate GUID inside the IDE.)
-AppId={{05342962-55F5-4BA3-BFD3-DBA01D2B8BCB}
-AppName={#MyAppName}
-AppVersion={#MyAppVersion}
-;AppVerName={#MyAppName} {#MyAppVersion}
-AppPublisher={#MyAppPublisher}
-AppPublisherURL={#MyAppURL}
-AppSupportURL={#MyAppURL}
-AppUpdatesURL={#MyAppURL}
-DefaultDirName={autopf}\{#MyAppName}
-UninstallDisplayIcon={app}\{#MyAppExeName}
-; "ArchitecturesAllowed=x64compatible" specifies that Setup cannot run
-; on anything but x64 and Windows 11 on Arm.
-ArchitecturesAllowed=x64compatible
-; "ArchitecturesInstallIn64BitMode=x64compatible" requests that the
-; install be done in "64-bit mode" on x64 or Windows 11 on Arm,
-; meaning it should use the native 64-bit Program Files directory and
-; the 64-bit view of the registry.
-ArchitecturesInstallIn64BitMode=x64compatible
-DisableProgramGroupPage=yes
-; Uncomment the following line to run in non administrative install mode (install for current user only).
-;PrivilegesRequired=lowest
-OutputDir=C:\Users\Mark\OneDrive\Documents\RDXC\Installers
-OutputBaseFilename=RHI-Setup
-SetupIconFile=C:\Users\Mark\OneDrive\Documents\RDXC\icon.ico
-SolidCompression=yes
-WizardStyle=modern dynamic
-
-[Languages]
-Name: "english"; MessagesFile: "compiler:Default.isl"
-
-[Tasks]
-Name: "desktopicon"; Description: "{cm:CreateDesktopIcon}"; GroupDescription: "{cm:AdditionalIcons}"; Flags: unchecked
-
-[Files]
-Source: "C:\Users\Mark\OneDrive\Documents\RDXC\Publish\RHI\{#MyAppExeName}"; DestDir: "{app}"; Flags: ignoreversion
-Source: "C:\Users\Mark\OneDrive\Documents\RDXC\Publish\RHI\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs createallsubdirs
-; NOTE: Don't use "Flags: ignoreversion" on any shared system files
-
-[Icons]
-Name: "{autoprograms}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"
-Name: "{autodesktop}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"; Tasks: desktopicon
-
-[UninstallDelete]
-Type: filesandordirs; Name: "{localappdata}\RHI"
-
-[Run]
-Filename: "{app}\{#MyAppExeName}"; Description: "{cm:LaunchProgram,{#StringChange(MyAppName, '&', '&&')}}"; Flags: nowait postinstall skipifsilent
-
-[Code]
-function IsRhiRunning(): Boolean;
-var
-  WMI: Variant;
-  Procs: Variant;
-begin
-  Result := False;
-  try
-    WMI := CreateOleObject('WbemScripting.SWbemLocator');
-    WMI := WMI.ConnectServer('.', 'root\cimv2');
-    Procs := WMI.ExecQuery('SELECT * FROM Win32_Process WHERE Name="RHI.exe"');
-    Result := (Procs.Count > 0);
-  except
-  end;
-end;
-
-function InitializeSetup(): Boolean;
-var
-  SignalDir, SignalPath: String;
-  WaitCount: Integer;
-begin
-  Result := True;
-
-  // Only signal if RHI is actually running
-  if not IsRhiRunning() then Exit;
-
-  // Write shutdown signal file to %LocalAppData%\RHI\
-  SignalDir := ExpandConstant('{localappdata}\RHI');
-  if not DirExists(SignalDir) then
-    ForceDirectories(SignalDir);
-  SignalPath := SignalDir + '\rhi_shutdown_requested';
-  SaveStringToFile(SignalPath, 'update', False);
-
-  // Wait up to 10 seconds for RHI to close itself
-  WaitCount := 0;
-  while (WaitCount < 20) and IsRhiRunning() do
-  begin
-    Sleep(500);
-    WaitCount := WaitCount + 1;
-  end;
-
-  // If still running, Inno's default CloseApplications will handle it
-end;
-
+; ============================================================
+;  Инсталлятор RHI — русская локализация
+;  Компилируется через Inno Setup 6 (ISCC.exe).
+;
+;  Пути параметризованы препроцессором ISPP: значения по
+;  умолчанию — локальные пути автора. Их можно переопределить
+;  из командной строки:
+;    ISCC.exe /DPublishDir="C:\путь\к\publish\RHI" ^
+;             /DIconFile="C:\путь\к\icon.ico" ^
+;             /DInstallerOutDir="C:\путь\к\Installers" ^
+;             "RHI Setup.iss"
+;  Либо просто запустите build-setup.bat из корня репозитория —
+;  он сам опубликует приложение и соберёт инсталлятор.
+; ============================================================
+
+#define MyAppName "RHI"
+; Версия в заголовке установщика — синхронизируйте с версией приложения вручную.
+#define MyAppVersion "2.0.1"
+#define MyAppPublisher "RankFTW"
+#define MyAppURL "www.github.com/rankftw"
+#define MyAppExeName "RHI.exe"
+
+#ifndef PublishDir
+#define PublishDir "C:\Users\Mark\OneDrive\Documents\RDXC\Publish\RHI"
+#endif
+
+#ifndef IconFile
+#define IconFile "C:\Users\Mark\OneDrive\Documents\RDXC\icon.ico"
+#endif
+
+#ifndef InstallerOutDir
+#define InstallerOutDir "C:\Users\Mark\OneDrive\Documents\RDXC\Installers"
+#endif
+
+[Setup]
+; Примечание: значение AppId уникально идентифицирует это приложение.
+; Не используйте тот же AppId в инсталляторах других приложений.
+; (Чтобы сгенерировать новый GUID: Tools | Generate GUID внутри IDE.)
+AppId={{05342962-55F5-4BA3-BFD3-DBA01D2B8BCB}
+AppName={#MyAppName}
+AppVersion={#MyAppVersion}
+;AppVerName={#MyAppName} {#MyAppVersion}
+AppPublisher={#MyAppPublisher}
+AppPublisherURL={#MyAppURL}
+AppSupportURL={#MyAppURL}
+AppUpdatesURL={#MyAppURL}
+DefaultDirName={autopf}\{#MyAppName}
+UninstallDisplayIcon={app}\{#MyAppExeName}
+; "ArchitecturesAllowed=x64compatible" — установка невозможна
+; нигде, кроме x64 и Windows 11 on Arm.
+ArchitecturesAllowed=x64compatible
+; "ArchitecturesInstallIn64BitMode=x64compatible" — на x64 и Windows 11 on Arm
+; установка идёт в 64-битном режиме: нативная папка Program Files
+; и 64-битный вид реестра.
+ArchitecturesInstallIn64BitMode=x64compatible
+DisableProgramGroupPage=yes
+; Раскомментируйте для установки без прав администратора (только текущий пользователь).
+;PrivilegesRequired=lowest
+OutputDir={#InstallerOutDir}
+OutputBaseFilename=RHI-Setup
+SetupIconFile={#IconFile}
+SolidCompression=yes
+WizardStyle=modern dynamic
+
+[Languages]
+; Русский — единственный язык мастера установки (диалог выбора языка не показывается).
+; Все стандартные тексты мастера (приветствие, лицензия, папки, завершение,
+; удаление) берутся из встроенного Russian.isl.
+; Чтобы вернуть английский, раскомментируйте следующую строку — тогда при запуске
+; появится диалог выбора языка:
+;Name: "english"; MessagesFile: "compiler:Default.isl"
+Name: "russian"; MessagesFile: "compiler:Languages\Russian.isl"
+
+[Tasks]
+Name: "desktopicon"; Description: "{cm:CreateDesktopIcon}"; GroupDescription: "{cm:AdditionalIcons}"; Flags: unchecked
+
+[Files]
+Source: "{#PublishDir}\{#MyAppExeName}"; DestDir: "{app}"; Flags: ignoreversion
+Source: "{#PublishDir}\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs createallsubdirs
+; Примечание: не используйте "Flags: ignoreversion" для общих системных файлов
+
+[Icons]
+Name: "{autoprograms}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"
+Name: "{autodesktop}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"; Tasks: desktopicon
+
+[UninstallDelete]
+Type: filesandordirs; Name: "{localappdata}\RHI"
+
+[Run]
+Filename: "{app}\{#MyAppExeName}"; Description: "{cm:LaunchProgram,{#StringChange(MyAppName, '&', '&&')}}"; Flags: nowait postinstall skipifsilent
+
+[Code]
+function IsRhiRunning(): Boolean;
+var
+  WMI: Variant;
+  Procs: Variant;
+begin
+  Result := False;
+  try
+    WMI := CreateOleObject('WbemScripting.SWbemLocator');
+    WMI := WMI.ConnectServer('.', 'root\cimv2');
+    Procs := WMI.ExecQuery('SELECT * FROM Win32_Process WHERE Name="RHI.exe"');
+    Result := (Procs.Count > 0);
+  except
+  end;
+end;
+
+function InitializeSetup(): Boolean;
+var
+  SignalDir, SignalPath: String;
+  WaitCount: Integer;
+begin
+  Result := True;
+
+  // Сигналим только если RHI реально запущен
+  if not IsRhiRunning() then Exit;
+
+  // Записываем файл сигнала завершения в %LocalAppData%\RHI\
+  SignalDir := ExpandConstant('{localappdata}\RHI');
+  if not DirExists(SignalDir) then
+    ForceDirectories(SignalDir);
+  SignalPath := SignalDir + '\rhi_shutdown_requested';
+  SaveStringToFile(SignalPath, 'update', False);
+
+  // Ждём до 10 секунд, пока RHI закроется сам
+  WaitCount := 0;
+  while (WaitCount < 20) and IsRhiRunning() do
+  begin
+    Sleep(500);
+    WaitCount := WaitCount + 1;
+  end;
+
+  // Если всё ещё запущен, стандартный CloseApplications InnoSetup разберётся сам
+end;
diff --git a/RenoDXCommander.Tests/CoreLogicTests.cs b/RenoDXCommander.Tests/CoreLogicTests.cs
index af7934f..1c6b975 100644
--- a/RenoDXCommander.Tests/CoreLogicTests.cs
+++ b/RenoDXCommander.Tests/CoreLogicTests.cs
@@ -163,35 +163,35 @@ public class CoreLogicTests
     public void FormatAge_LessThanOneMinute_ReturnsJustNow()
     {
         var utc = DateTime.UtcNow.AddSeconds(-30);
-        Assert.Equal("just now", FormatAgeHelper(utc));
+        Assert.Equal("только что", FormatAgeHelper(utc));
     }
 
     [Fact]
     public void FormatAge_FiveMinutes_Returns5mAgo()
     {
         var utc = DateTime.UtcNow.AddMinutes(-5);
-        Assert.Equal("5m ago", FormatAgeHelper(utc));
+        Assert.Equal("5 мин назад", FormatAgeHelper(utc));
     }
 
     [Fact]
     public void FormatAge_TwoHours_Returns2hAgo()
     {
         var utc = DateTime.UtcNow.AddHours(-2);
-        Assert.Equal("2h ago", FormatAgeHelper(utc));
+        Assert.Equal("2 ч назад", FormatAgeHelper(utc));
     }
 
     [Fact]
     public void FormatAge_ThreeDays_Returns3dAgo()
     {
         var utc = DateTime.UtcNow.AddDays(-3);
-        Assert.Equal("3d ago", FormatAgeHelper(utc));
+        Assert.Equal("3 дн назад", FormatAgeHelper(utc));
     }
 
     /// <summary>Mirrors MainViewModel.FormatAge exactly.</summary>
     private static string FormatAgeHelper(DateTime utc)
     {
         var age = DateTime.UtcNow - utc;
-        if (age.TotalMinutes < 1) return "just now";
+        if (age.TotalMinutes < 1) return "только что";
         if (age.TotalHours   < 1) return $"{(int)age.TotalMinutes}m ago";
         if (age.TotalDays    < 1) return $"{(int)age.TotalHours}h ago";
         return $"{(int)age.TotalDays}d ago";
diff --git a/RenoDXCommander/AddonManagerDialog.cs b/RenoDXCommander/AddonManagerDialog.cs
index 8a06177..6d1cd82 100644
--- a/RenoDXCommander/AddonManagerDialog.cs
+++ b/RenoDXCommander/AddonManagerDialog.cs
@@ -29,14 +29,14 @@ public static class AddonManagerDialog
         {
             var emptyDlg = new ContentDialog
             {
-                Title = "ReShade Addon Manager",
+                Title = "Менеджер аддонов ReShade",
                 Content = new TextBlock
                 {
-                    Text = "No addons available. Try refreshing.",
+                    Text = "Доступных аддонов нет. Попробуйте обновить.",
                     FontSize = 13,
                     Foreground = Brush(ResourceKeys.TextPrimaryBrush),
                 },
-                CloseButtonText = "Close",
+                CloseButtonText = "Закрыть",
                 XamlRoot = xamlRoot,
                 Background = Brush(ResourceKeys.SurfaceOverlayBrush),
                 MinWidth = 750,
@@ -87,7 +87,7 @@ public static class AddonManagerDialog
         {
             panel.Children.Add(new TextBlock
             {
-                Text = "Custom Addons",
+                Text = "Свои аддоны",
                 FontSize = 12,
                 FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
                 Foreground = Brush(ResourceKeys.TextPrimaryBrush),
@@ -111,9 +111,9 @@ public static class AddonManagerDialog
 
         var dlg = new ContentDialog
         {
-            Title = "ReShade Addon Manager",
+            Title = "Менеджер аддонов ReShade",
             Content = scrollViewer,
-            CloseButtonText = "Close",
+            CloseButtonText = "Закрыть",
             XamlRoot = xamlRoot,
             Background = Brush(ResourceKeys.SurfaceOverlayBrush),
             MinWidth = 750,
@@ -300,7 +300,7 @@ public static class AddonManagerDialog
         {
             rightElement = new HyperlinkButton
             {
-                Content = "Repository",
+                Content = "Репозиторий",
                 NavigateUri = new Uri(entry.RepositoryUrl!),
                 FontSize = 11,
                 Foreground = Brush(ResourceKeys.AccentBlueBrush),
diff --git a/RenoDXCommander/AddonPopupHelper.cs b/RenoDXCommander/AddonPopupHelper.cs
index 6ac4eac..1ce9462 100644
--- a/RenoDXCommander/AddonPopupHelper.cs
+++ b/RenoDXCommander/AddonPopupHelper.cs
@@ -44,14 +44,14 @@ public static class AddonPopupHelper
         {
             var emptyDlg = new ContentDialog
             {
-                Title = "Select Addons",
+                Title = "Выбрать аддоны",
                 Content = new TextBlock
                 {
-                    Text = "No addons available.",
+                    Text = "Доступных аддонов нет.",
                     FontSize = 13,
                     Foreground = Brush(ResourceKeys.TextPrimaryBrush),
                 },
-                CloseButtonText = "Close",
+                CloseButtonText = "Закрыть",
                 XamlRoot = xamlRoot,
                 Background = Brush(ResourceKeys.SurfaceOverlayBrush),
                 MinWidth = 750,
@@ -203,8 +203,8 @@ public static class AddonPopupHelper
             };
             if (peerIsSelected)
                 ToolTipService.SetToolTip(toggle, rtx40MfgConflict
-                    ? "RTX 40 MFG Unlock is already installed and conflicts with this addon. Remove it from the Extras section first."
-                    : $"Disable {mutualExclusivePeer} first to enable this addon.");
+                    ? "Разблокировка RTX 40 MFG уже установлена и конфликтует с этим аддоном. Сначала удалите её в разделе «Дополнения»."
+                    : $"Сначала отключите {mutualExclusivePeer}, чтобы включить этот аддон.");
 
             // Capture for the lambda
             var capturedEntry = entry;
@@ -252,7 +252,7 @@ public static class AddonPopupHelper
                         peer.Toggle.IsEnabled = !toggle.IsOn;
                         peer.Toggle.Opacity = toggle.IsOn ? 0.35 : 1.0;
                         if (toggle.IsOn)
-                            ToolTipService.SetToolTip(peer.Toggle, $"Disable {entry.PackageName} first to enable this addon.");
+                            ToolTipService.SetToolTip(peer.Toggle, $"Сначала отключите {entry.PackageName}, чтобы включить этот аддон.");
                         else
                             ToolTipService.SetToolTip(peer.Toggle, null);
                     }
@@ -306,7 +306,7 @@ public static class AddonPopupHelper
         {
             panel.Children.Add(new TextBlock
             {
-                Text = "Custom Addons",
+                Text = "Свои аддоны",
                 FontSize = 12,
                 FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
                 Foreground = Brush(ResourceKeys.TextPrimaryBrush),
@@ -391,10 +391,10 @@ public static class AddonPopupHelper
 
         var dlg = new ContentDialog
         {
-            Title = "Select Addons",
+            Title = "Выбрать аддоны",
             Content = scrollViewer,
-            PrimaryButtonText = "Deploy",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Развернуть",
+            CloseButtonText = "Отмена",
             XamlRoot = xamlRoot,
             Background = Brush(ResourceKeys.SurfaceOverlayBrush),
             MinWidth = 750,
diff --git a/RenoDXCommander/DetailPanelBuilder.Extras.cs b/RenoDXCommander/DetailPanelBuilder.Extras.cs
index a114b01..c445550 100644
--- a/RenoDXCommander/DetailPanelBuilder.Extras.cs
+++ b/RenoDXCommander/DetailPanelBuilder.Extras.cs
@@ -39,7 +39,7 @@ public partial class DetailPanelBuilder
         };
         var exTitle = new TextBlock
         {
-            Text       = "Extras",
+            Text       = "Дополнения",
             FontSize   = 13,
             FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
@@ -77,7 +77,7 @@ public partial class DetailPanelBuilder
         CrashReporter.Log($"[BuildExtrasSection] UalRow: {__exSw.ElapsedMilliseconds - __t0}ms '{card.GameName}'");
 
         // ── MFG Unlocks separator ─────────────────────────────────────────────
-        exBody.Children.Add(MakeExtrasSeparator("MFG Unlocks"));
+        exBody.Children.Add(MakeExtrasSeparator("Разблокировки MFG"));
 
         // ── RTX 40 MFG Unlock row ─────────────────────────────────────────────
         __t0 = __exSw.ElapsedMilliseconds;
@@ -110,7 +110,7 @@ public partial class DetailPanelBuilder
         // ── API Upgrades sub-header + DXVK row ────────────────────────────────
         if (card.IsDxvkToggleVisible)
         {
-            exBody.Children.Add(MakeExtrasSeparator("API Upgrades"));
+            exBody.Children.Add(MakeExtrasSeparator("API-апгрейды"));
             __t0 = __exSw.ElapsedMilliseconds;
             BuildDxvkRow(card, exBody);
             CrashReporter.Log($"[BuildExtrasSection] DxvkRow: {__exSw.ElapsedMilliseconds - __t0}ms '{card.GameName}'");
@@ -229,7 +229,7 @@ public partial class DetailPanelBuilder
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             VerticalAlignment = VerticalAlignment.Center,
         };
-        ToolTipService.SetToolTip(label, "Ultimate ASI Loader — proxy DLL that loads .asi plugins into game processes.");
+        ToolTipService.SetToolTip(label, "Ultimate ASI Loader — DLL-прокси, загружающая плагины .asi в процессы игр.");
         Grid.SetColumn(label, 0);
         row.Children.Add(label);
 
@@ -245,7 +245,7 @@ public partial class DetailPanelBuilder
         };
         if (isInstalled)
         {
-            ToolTipService.SetToolTip(statusBlock, $"Installed as: {installedAs}\nClick to open GitHub releases");
+            ToolTipService.SetToolTip(statusBlock, $"Установлен как: {installedAs}\nНажмите, чтобы открыть релизы на GitHub");
             statusBlock.PointerPressed += (s, e) =>
                 _ = Windows.System.Launcher.LaunchUriAsync(new Uri("https://github.com/ThirteenAG/Ultimate-ASI-Loader/releases"));
         }
@@ -255,7 +255,7 @@ public partial class DetailPanelBuilder
         // Col 2 — Info button (matches Components style)
         var infoBtn = new Button
         {
-            Content = "Info",
+            Content = "Инфо",
             FontSize = 11,
             Padding = new Thickness(6, 2, 6, 2),
             Width = 36,
@@ -266,7 +266,7 @@ public partial class DetailPanelBuilder
             BorderThickness = new Thickness(1),
             CornerRadius = new CornerRadius(8),
         };
-        ToolTipService.SetToolTip(infoBtn, "Open Ultimate ASI Loader GitHub releases page");
+        ToolTipService.SetToolTip(infoBtn, "Открыть страницу релизов Ultimate ASI Loader на GitHub");
         infoBtn.Click += (s, e) =>
             _ = Windows.System.Launcher.LaunchUriAsync(new Uri("https://github.com/ThirteenAG/Ultimate-ASI-Loader/releases"));
         Grid.SetColumn(infoBtn, 2);
@@ -275,7 +275,7 @@ public partial class DetailPanelBuilder
         // Col 3 — Install button
         var installBtn = new Button
         {
-            Content = isInstalled ? "↺  Reinstall ASI Loader" : "⬇  Install ASI Loader",
+            Content = isInstalled ? "↺  Переустановить ASI Loader" : "⬇  Установить ASI Loader",
             FontSize = 12,
             Height = 32,
             HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -293,8 +293,8 @@ public partial class DetailPanelBuilder
             BorderThickness = new Thickness(1),
         };
         ToolTipService.SetToolTip(installBtn, isInstalled
-            ? $"Reinstall Ultimate ASI Loader (currently '{installedAs}')"
-            : "Install Ultimate ASI Loader — choose which DLL name to use");
+            ? $"Переустановить Ultimate ASI Loader (сейчас «{installedAs}»)"
+            : "Установить Ultimate ASI Loader — выберите имя DLL");
         installBtn.Click += async (s, e) =>
         {
             if (string.IsNullOrEmpty(installPath)) return;
@@ -313,8 +313,8 @@ public partial class DetailPanelBuilder
                     {
                         _ = DialogService.ShowSafeAsync(new ContentDialog
                         {
-                            Title = "Original DLL chained",
-                            Content = $"The existing '{chosen}' was renamed to '{hookedOriginal}' so ASI Loader can chain-load it automatically.",
+                            Title = "Исходная DLL в цепочке",
+                            Content = $"Существующий «{chosen}» переименован в «{hookedOriginal}», чтобы ASI Loader мог автоматически подгружать его по цепочке.",
                             CloseButtonText = "OK",
                             XamlRoot = _window.Content.XamlRoot,
                         });
@@ -323,7 +323,7 @@ public partial class DetailPanelBuilder
                 }
                 else
                 {
-                    installBtn.Content = "❌ Install failed";
+                    installBtn.Content = "❌ Не удалось установить";
                 }
             }
             finally { installBtn.IsEnabled = true; }
@@ -345,15 +345,15 @@ public partial class DetailPanelBuilder
             Content = new TextBlock { Text = "⚙", FontSize = 14, HorizontalAlignment = HorizontalAlignment.Center },
             Tag = card,
         };
-        ToolTipService.SetToolTip(cogBtn, "ASI Loader settings (coming soon)");
+        ToolTipService.SetToolTip(cogBtn, "Настройки ASI Loader (скоро)");
         cogBtn.Click += async (s, e) =>
         {
             // Placeholder — settings dialog will be added later
             var dlg = new ContentDialog
             {
-                Title = "ASI Loader Settings",
-                Content = new TextBlock { Text = "No settings available yet.", FontSize = 12 },
-                CloseButtonText = "Close",
+                Title = "Настройки ASI Loader",
+                Content = new TextBlock { Text = "Настройки пока недоступны.", FontSize = 12 },
+                CloseButtonText = "Закрыть",
                 XamlRoot = _window.Content.XamlRoot,
             };
             await DialogService.ShowSafeAsync(dlg);
@@ -376,7 +376,7 @@ public partial class DetailPanelBuilder
             Opacity = isInstalled ? 1.0 : 0,
             IsHitTestVisible = isInstalled,
         };
-        ToolTipService.SetToolTip(removeBtn, "Remove Ultimate ASI Loader from this game");
+        ToolTipService.SetToolTip(removeBtn, "Удалить Ultimate ASI Loader из этой игры");
         removeBtn.Click += (s, e) =>
         {
             if (string.IsNullOrEmpty(installPath)) return;
@@ -429,7 +429,7 @@ public partial class DetailPanelBuilder
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             VerticalAlignment = VerticalAlignment.Center,
         };
-        ToolTipService.SetToolTip(label, "MFG Ada Unlock — unlocks DLSS Multi Frame Generation (3x/4x+) on RTX 40-series GPUs. Requires ReShade. In-memory only, no files modified.");
+        ToolTipService.SetToolTip(label, "MFG Ada Unlock — открывает мульти-генерацию кадров DLSS (3x/4x+) на GPU RTX 40-й серии. Требуется ReShade. Работает только в памяти, файлы не изменяются.");
         Grid.SetColumn(label, 0);
         row.Children.Add(label);
 
@@ -445,7 +445,7 @@ public partial class DetailPanelBuilder
         };
         if (isInstalled)
         {
-            ToolTipService.SetToolTip(statusBlock, "Click to open GitHub releases");
+            ToolTipService.SetToolTip(statusBlock, "Открыть релизы на GitHub");
             statusBlock.PointerPressed += (s, e) =>
                 _ = Windows.System.Launcher.LaunchUriAsync(new Uri("https://github.com/mavismmg/MFGAdaUnlock-RenoDx/releases"));
         }
@@ -455,7 +455,7 @@ public partial class DetailPanelBuilder
         // Col 2 — Info button (always blue)
         var infoBtn = new Button
         {
-            Content = "Info",
+            Content = "Инфо",
             FontSize = 11,
             Padding = new Thickness(6, 2, 6, 2),
             Width = 36,
@@ -466,7 +466,7 @@ public partial class DetailPanelBuilder
             BorderThickness = new Thickness(1),
             CornerRadius = new CornerRadius(8),
         };
-        ToolTipService.SetToolTip(infoBtn, "Open MFG Ada Unlock GitHub page");
+        ToolTipService.SetToolTip(infoBtn, "Открыть страницу MFG Ada Unlock на GitHub");
         infoBtn.Click += (s, e) =>
             _ = Windows.System.Launcher.LaunchUriAsync(new Uri("https://github.com/mavismmg/MFGAdaUnlock-RenoDx"));
         Grid.SetColumn(infoBtn, 2);
@@ -477,22 +477,22 @@ public partial class DetailPanelBuilder
         bool btnEnabled = true;
         if (!rsInstalled)
         {
-            btnLabel   = "⚠  ReShade required";
+            btnLabel   = "⚠  Требуется ReShade";
             btnEnabled = false;
         }
         else if (rtx40Conflict)
         {
-            btnLabel   = "⚠  RTX 40 MFG installed";
+            btnLabel   = "⚠  Установлен RTX 40 MFG";
             btnEnabled = false;
         }
         else if (!staged)
         {
-            btnLabel   = "⬇  Install MFG Ada Unlock";
+            btnLabel   = "⬇  Установить MFG Ada Unlock";
             btnEnabled = true; // will download on demand when clicked
         }
         else
         {
-            btnLabel = isInstalled ? "↺  Reinstall MFG Ada Unlock" : "⬇  Install MFG Ada Unlock";
+            btnLabel = isInstalled ? "↺  Переустановить MFG Ada Unlock" : "⬇  Установить MFG Ada Unlock";
         }
 
         var installBtn = new Button
@@ -511,9 +511,9 @@ public partial class DetailPanelBuilder
         };
 
         if (!rsInstalled)
-            ToolTipService.SetToolTip(installBtn, "Install ReShade first — MFG Ada Unlock requires it");
+            ToolTipService.SetToolTip(installBtn, "Сначала установите ReShade — MFG Ada Unlock требует его");
         else if (rtx40Conflict)
-            ToolTipService.SetToolTip(installBtn, "RTX 40 MFG Unlock (ASI version) is already installed and conflicts. Remove it first.");
+            ToolTipService.SetToolTip(installBtn, "Разблокировка RTX 40 MFG (версия ASI) уже установлена и конфликтует. Сначала удалите её.");
 
         installBtn.Click += async (s, e) =>
         {
@@ -535,7 +535,7 @@ public partial class DetailPanelBuilder
                 if (!File.Exists(stagedPath))
                 {
                     CrashReporter.Log("[BuildMfgAdaUnlockRow] Staged file still not found after download attempt");
-                    _window.DispatcherQueue?.TryEnqueue(() => installBtn.Content = "Download failed");
+                    _window.DispatcherQueue?.TryEnqueue(() => installBtn.Content = "Не удалось скачать");
                     return;
                 }
 
@@ -566,7 +566,7 @@ public partial class DetailPanelBuilder
             CornerRadius = new CornerRadius(8),
             Content = new TextBlock { Text = "⚙", FontSize = 14, HorizontalAlignment = HorizontalAlignment.Center },
         };
-        ToolTipService.SetToolTip(cogBtn, "MFG Ada Unlock settings");
+        ToolTipService.SetToolTip(cogBtn, "Настройки MFG Ada Unlock");
         cogBtn.Click += async (s, e) =>
         {
             var dlg = new ContentDialog
@@ -574,13 +574,13 @@ public partial class DetailPanelBuilder
                 Title = "MFG Ada Unlock",
                 Content = new TextBlock
                 {
-                    Text = "MFG Ada Unlock unlocks DLSS Multi Frame Generation (3x/4x and above) on RTX 40-series GPUs.\n\n" +
-                           "Configure via the ReShade overlay in-game.",
+                    Text = "MFG Ada Unlock открывает мульти-генерацию кадров DLSS (3x/4x и выше) на GPU RTX 40-й серии.\n\n" +
+                           "Настраивается через оверлей ReShade в игре.",
                     FontSize = 12,
                     TextWrapping = TextWrapping.Wrap,
                 },
-                PrimaryButtonText = "Open GitHub",
-                CloseButtonText = "Close",
+                PrimaryButtonText = "Открыть GitHub",
+                CloseButtonText = "Закрыть",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
             };
@@ -606,7 +606,7 @@ public partial class DetailPanelBuilder
             Opacity = isInstalled ? 1.0 : 0,
             IsHitTestVisible = isInstalled,
         };
-        ToolTipService.SetToolTip(removeBtn, "Remove MFG Ada Unlock from this game");
+        ToolTipService.SetToolTip(removeBtn, "Удалить MFG Ada Unlock из этой игры");
         removeBtn.Click += (s, e) =>
         {
             if (string.IsNullOrEmpty(installPath)) return;
@@ -703,7 +703,7 @@ public partial class DetailPanelBuilder
         };
         if (card.IsOsInstalled && !osGreyed)
         {
-            ToolTipService.SetToolTip(statusBlock, "Click to open OptiScaler wiki");
+            ToolTipService.SetToolTip(statusBlock, "Открыть вики OptiScaler");
             statusBlock.PointerPressed += async (s, e) =>
                 await Windows.System.Launcher.LaunchUriAsync(new Uri("https://github.com/optiscaler/OptiScaler/wiki"));
         }
@@ -713,7 +713,7 @@ public partial class DetailPanelBuilder
         // Col 2 — Info button
         var infoBtn = new Button
         {
-            Content = "Info",
+            Content = "Инфо",
             FontSize = 11,
             Padding = new Thickness(6, 2, 6, 2),
             Width = 36,
@@ -765,7 +765,7 @@ public partial class DetailPanelBuilder
             IsEnabled = !osGreyed,
             Opacity = osGreyed ? 0.35 : 1.0,
         };
-        ToolTipService.SetToolTip(cogBtn, "OptiScaler Settings");
+        ToolTipService.SetToolTip(cogBtn, "Настройки OptiScaler");
         cogBtn.Click += (s, e) => _window.OsCogButton_ClickInternal(s, e);
         Grid.SetColumn(cogBtn, 4);
         row.Children.Add(cogBtn);
@@ -787,7 +787,7 @@ public partial class DetailPanelBuilder
             Opacity = (osGreyed || !osShow) ? 0 : 1.0,
             IsHitTestVisible = osShow && !osGreyed,
         };
-        ToolTipService.SetToolTip(deleteBtn, "Remove OptiScaler");
+        ToolTipService.SetToolTip(deleteBtn, "Удалить OptiScaler");
         deleteBtn.Click += (s, e) => _window.UninstallOsButton_Click(s, e);
         Grid.SetColumn(deleteBtn, 5);
         row.Children.Add(deleteBtn);
@@ -875,11 +875,11 @@ public partial class DetailPanelBuilder
             if (isRecommended)
                 contentRow.Children.Add(MakeBadge("Recommended", "#1A3A20", "#6AE87A", "#2A5A30"));
             if (isTaken)
-                contentRow.Children.Add(MakeBadge("In use", "#2A1818", "#CC6666", "#5A2828"));
+                contentRow.Children.Add(MakeBadge("Используется", "#2A1818", "#CC6666", "#5A2828"));
             if (isRhiOwned)
-                contentRow.Children.Add(MakeBadge("Used by RHI", "#2A1818", "#CC6666", "#5A2828"));
+                contentRow.Children.Add(MakeBadge("Используется RHI", "#2A1818", "#CC6666", "#5A2828"));
             if (isRhiConflict && !isRhiOwned)
-                contentRow.Children.Add(MakeBadge("May conflict with ReShade/OS", "#2A1A10", "#CC9955", "#5A3A18"));
+                contentRow.Children.Add(MakeBadge("Может конфликтовать с ReShade/ОС", "#2A1A10", "#CC9955", "#5A3A18"));
             if (isCurrent)
                 contentRow.Children.Add(MakeBadge("Current", "#182840", "#7AACDD", "#2A4468"));
 
@@ -889,7 +889,7 @@ public partial class DetailPanelBuilder
             if (isTaken)
                 ToolTipService.SetToolTip(btn, $"'{name}' already exists in the game folder. Selecting it will rename the existing file to '{Path.GetFileNameWithoutExtension(name)}Hooked.dll' so ASI Loader can chain-load it.");
             else if (isRhiOwned)
-                ToolTipService.SetToolTip(btn, "This filename is already used by an RHI-managed component (ReShade, OptiScaler, or DC). Choose a different name.");
+                ToolTipService.SetToolTip(btn, "Это имя уже занято компонентом под управлением RHI (ReShade, OptiScaler или DC). Выберите другое.");
 
             btn.Tag = name;
             btn.Click += (s, ev) =>
@@ -908,7 +908,7 @@ public partial class DetailPanelBuilder
 
         var dialog = new ContentDialog
         {
-            Title = "Choose ASI Loader DLL name",
+            Title = "Выберите имя DLL для ASI Loader",
             Content = new StackPanel
             {
                 Spacing = 8,
@@ -924,7 +924,7 @@ public partial class DetailPanelBuilder
                     scrollViewer,
                 }
             },
-            CloseButtonText = "Cancel",
+            CloseButtonText = "Отмена",
             XamlRoot = _window.Content.XamlRoot,
         };
 
@@ -998,12 +998,12 @@ public partial class DetailPanelBuilder
         // Col 0 — label
         var label = new TextBlock
         {
-            Text = "20/30 FG Unlock",
+            Text = "Разблокировка FG 20/30",
             FontSize = 12,
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             VerticalAlignment = VerticalAlignment.Center,
         };
-        ToolTipService.SetToolTip(label, "20/30 FG Unlock — enables DLSS Frame Generation on RTX 20 and 30 series GPUs. D3D12 only. No ASI Loader or ReShade required.");
+        ToolTipService.SetToolTip(label, "Разблокировка FG 20/30 — включает генерацию кадров DLSS на видеокартах RTX 20-й и 30-й серий. Только D3D12. ASI Loader и ReShade не требуются.");
         Grid.SetColumn(label, 0);
         row.Children.Add(label);
 
@@ -1019,7 +1019,7 @@ public partial class DetailPanelBuilder
         };
         if (isInstalled)
         {
-            ToolTipService.SetToolTip(statusBlock, $"Installed as: {currentDllName}\nClick to open GitHub releases");
+            ToolTipService.SetToolTip(statusBlock, $"Установлен как: {currentDllName}\nНажмите, чтобы открыть релизы на GitHub");
             statusBlock.PointerPressed += (s, e) =>
                 _ = Windows.System.Launcher.LaunchUriAsync(new Uri("https://github.com/sdli1995/dlssg_for_sm86"));
         }
@@ -1029,7 +1029,7 @@ public partial class DetailPanelBuilder
         // Col 2 — Info button
         var infoBtn = new Button
         {
-            Content = "Info",
+            Content = "Инфо",
             FontSize = 11,
             Padding = new Thickness(6, 2, 6, 2),
             Width = 36,
@@ -1040,7 +1040,7 @@ public partial class DetailPanelBuilder
             BorderThickness = new Thickness(1),
             CornerRadius = new CornerRadius(8),
         };
-        ToolTipService.SetToolTip(infoBtn, "Open 20/30 FG Unlock GitHub page");
+        ToolTipService.SetToolTip(infoBtn, "Открыть страницу разблокировки FG 20/30 на GitHub");
         infoBtn.Click += (s, e) =>
             _ = Windows.System.Launcher.LaunchUriAsync(new Uri("https://github.com/sdli1995/dlssg_for_sm86"));
         Grid.SetColumn(infoBtn, 2);
@@ -1049,7 +1049,7 @@ public partial class DetailPanelBuilder
         // Col 3 — Install button
         var installBtn = new Button
         {
-            Content = isInstalled ? "↺  Reinstall 20/30 FG" : "⬇  Install 20/30 FG",
+            Content = isInstalled ? "↺  Переустановить FG 20/30" : "⬇  Установить FG 20/30",
             FontSize = 12,
             Height = 32,
             HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -1064,13 +1064,13 @@ public partial class DetailPanelBuilder
         {
             installBtn.IsEnabled = false;
             installBtn.Opacity   = 0.35;
-            installBtn.Content   = "Uninstall MFG Ada Unlock first";
-            ToolTipService.SetToolTip(installBtn, "MFG Ada Unlock (addon) is installed and conflicts. Remove it from the addon picker first.");
+            installBtn.Content   = "Сначала удалите MFG Ada Unlock";
+            ToolTipService.SetToolTip(installBtn, "MFG Ada Unlock (аддон) установлен и конфликтует. Сначала удалите его в выборе аддонов.");
         }
         else
         {
             ToolTipService.SetToolTip(installBtn, isInstalled
-                ? $"Reinstall 20/30 FG Unlock (currently deployed as {currentDllName})"
+                ? $"Переустановить разблокировку FG 20/30 (сейчас развернута как {currentDllName})"
                 : "Install 20/30 FG Unlock — deploys version.dll under a name you choose");
         }
 
@@ -1105,14 +1105,14 @@ public partial class DetailPanelBuilder
                 }
                 else
                 {
-                    installBtn.Content   = "Download failed — try again";
+                    installBtn.Content   = "Не удалось скачать — попробуйте снова";
                     installBtn.IsEnabled = true;
                 }
             }
             catch (Exception ex)
             {
                 CrashReporter.Log($"[BuildDlssg2030Row] Install failed — {ex.Message}");
-                installBtn.Content   = "Install failed";
+                installBtn.Content   = "Не удалось установить";
                 installBtn.IsEnabled = true;
             }
         };
@@ -1130,7 +1130,7 @@ public partial class DetailPanelBuilder
             CornerRadius = new CornerRadius(8),
             Content = new TextBlock { Text = "⚙", FontSize = 14, HorizontalAlignment = HorizontalAlignment.Center },
         };
-        ToolTipService.SetToolTip(cogBtn, "Select GPU generation");
+        ToolTipService.SetToolTip(cogBtn, "Выберите поколение GPU");
         cogBtn.Click += async (s, e) =>
         {
             var currentGen = _window.ViewModel.GetDlssg2030GpuGen(gameName, store);
@@ -1142,7 +1142,7 @@ public partial class DetailPanelBuilder
             };
             var dlg = new ContentDialog
             {
-                Title = "20/30 FG Unlock — GPU Generation",
+                Title = "Разблокировка FG 20/30 — генерация кадров на GPU",
                 Content = new StackPanel
                 {
                     Spacing = 8,
@@ -1150,18 +1150,18 @@ public partial class DetailPanelBuilder
                     {
                         new TextBlock
                         {
-                            Text = "Select your GPU generation. This controls which rendering path is used.\n\n" +
-                                   "RTX 30 Series (SM86) — Ampere\n" +
-                                   "RTX 20 Series (SM75) — Turing\n\n" +
-                                   "Reinstall after changing to apply the new setting.",
+                            Text = "Выберите поколение GPU. От этого зависит используемый путь рендеринга.\n\n" +
+                                   "RTX 30-й серии (SM86) — Ampere\n" +
+                                   "RTX 20-й серии (SM75) — Turing\n\n" +
+                                   "После изменения переустановите, чтобы применить новую настройку.",
                             FontSize = 12,
                             TextWrapping = TextWrapping.Wrap,
                         },
                         combo,
                     },
                 },
-                PrimaryButtonText = "Save",
-                CloseButtonText   = "Cancel",
+                PrimaryButtonText = "Сохранить",
+                CloseButtonText   = "Отмена",
                 XamlRoot          = _window.Content.XamlRoot,
                 RequestedTheme    = ElementTheme.Dark,
             };
@@ -1196,7 +1196,7 @@ public partial class DetailPanelBuilder
             Opacity = isInstalled ? 1.0 : 0,
             IsHitTestVisible = isInstalled,
         };
-        ToolTipService.SetToolTip(removeBtn, "Remove 20/30 FG Unlock from this game");
+        ToolTipService.SetToolTip(removeBtn, "Удалить разблокировку FG 20/30 из этой игры");
         removeBtn.Click += (s, e) =>
         {
             if (string.IsNullOrEmpty(installPath)) return;
@@ -1261,13 +1261,13 @@ public partial class DetailPanelBuilder
             if (isRecommended)
                 contentRow.Children.Add(MakeBadge("Recommended", "#1A3A20", "#6AE87A", "#2A5A30"));
             if (isRhiOwned)
-                contentRow.Children.Add(MakeBadge("Used by RHI", "#2A1818", "#CC6666", "#5A2828"));
+                contentRow.Children.Add(MakeBadge("Используется RHI", "#2A1818", "#CC6666", "#5A2828"));
             if (isCurrent)
                 contentRow.Children.Add(MakeBadge("Current", "#182840", "#7AACDD", "#2A4468"));
 
             btn.Content = contentRow;
             if (isRhiOwned)
-                ToolTipService.SetToolTip(btn, "This filename is already used by an RHI-managed component. Choose a different name.");
+                ToolTipService.SetToolTip(btn, "Это имя уже занято компонентом под управлением RHI. Выберите другое.");
 
             btn.Tag    = name;
             btn.Click += (s, ev) =>
@@ -1284,7 +1284,7 @@ public partial class DetailPanelBuilder
 
         var pickerDialog = new ContentDialog
         {
-            Title = "Choose 20/30 FG Unlock DLL name",
+            Title = "Выберите имя DLL для разблокировки FG 20/30",
             Content = new StackPanel
             {
                 Spacing = 8,
@@ -1292,7 +1292,7 @@ public partial class DetailPanelBuilder
                 {
                     new TextBlock
                     {
-                        Text = "Select your GPU generation in the cog (⚙) before installing. Then choose the filename to deploy the DLL as.",
+                        Text = "Перед установкой выберите поколение GPU в шестерёнке (⚙). Затем выберите имя файла для развертывания DLL.",
                         FontSize = 11,
                         Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush),
                         TextWrapping = TextWrapping.Wrap,
@@ -1300,7 +1300,7 @@ public partial class DetailPanelBuilder
                     scrollViewer,
                 }
             },
-            CloseButtonText = "Cancel",
+            CloseButtonText = "Отмена",
             XamlRoot        = _window.Content.XamlRoot,
         };
 
@@ -1342,7 +1342,7 @@ public partial class DetailPanelBuilder
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             VerticalAlignment = VerticalAlignment.Center,
         };
-        ToolTipService.SetToolTip(label, "RTX 40 MFG Unlock — enables DLSS Multi Frame Generation multipliers beyond 2x (up to 6x) on RTX 40 Series GPUs. Standalone DLL, no ASI Loader required.");
+        ToolTipService.SetToolTip(label, "Разблокировка RTX 40 MFG — включает множители мульти-генерации кадров DLSS выше 2x (до 6x) на GPU RTX 40-й серии. Отдельная DLL, ASI Loader не нужен.");
         Grid.SetColumn(label, 0);
         row.Children.Add(label);
 
@@ -1358,7 +1358,7 @@ public partial class DetailPanelBuilder
         };
         if (isInstalled)
         {
-            ToolTipService.SetToolTip(statusBlock, $"Installed as: {currentDllName}\nClick to open GitHub releases page");
+            ToolTipService.SetToolTip(statusBlock, $"Установлен как: {currentDllName}\nНажмите, чтобы открыть страницу релизов на GitHub");
             statusBlock.PointerPressed += (s, e) =>
                 _ = Windows.System.Launcher.LaunchUriAsync(new Uri("https://github.com/dashdogy/RTX40MFG-Unlock/releases"));
         }
@@ -1368,7 +1368,7 @@ public partial class DetailPanelBuilder
         // Col 2 — Info button
         var infoBtn = new Button
         {
-            Content = "Info",
+            Content = "Инфо",
             FontSize = 11,
             Padding = new Thickness(6, 2, 6, 2),
             Width = 36,
@@ -1379,7 +1379,7 @@ public partial class DetailPanelBuilder
             BorderThickness = new Thickness(1),
             CornerRadius = new CornerRadius(8),
         };
-        ToolTipService.SetToolTip(infoBtn, "Open RTX 40 MFG Unlock GitHub page");
+        ToolTipService.SetToolTip(infoBtn, "Открыть страницу разблокировки RTX 40 MFG на GitHub");
         infoBtn.Click += (s, e) =>
             _ = Windows.System.Launcher.LaunchUriAsync(new Uri("https://github.com/dashdogy/RTX40MFG-Unlock"));
         Grid.SetColumn(infoBtn, 2);
@@ -1388,7 +1388,7 @@ public partial class DetailPanelBuilder
         // Col 3 — Install button
         var installBtn = new Button
         {
-            Content = isInstalled ? "↺  Reinstall RTX 40 MFG" : "⬇  Install RTX 40 MFG",
+            Content = isInstalled ? "↺  Переустановить RTX 40 MFG" : "⬇  Установить RTX 40 MFG",
             FontSize = 12,
             Height = 32,
             HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -1409,13 +1409,13 @@ public partial class DetailPanelBuilder
         {
             installBtn.IsEnabled = false;
             installBtn.Opacity   = 0.35;
-            installBtn.Content   = "Uninstall MFG Ada Unlock first";
-            ToolTipService.SetToolTip(installBtn, "MFG Ada Unlock (addon) is already installed and conflicts with RTX 40 MFG Unlock. Remove it from the addon picker first.");
+            installBtn.Content   = "Сначала удалите MFG Ada Unlock";
+            ToolTipService.SetToolTip(installBtn, "MFG Ada Unlock (аддон) уже установлен и конфликтует с разблокировкой RTX 40 MFG. Сначала удалите его в выборе аддонов.");
         }
         else
         {
             ToolTipService.SetToolTip(installBtn, isInstalled
-                ? $"Reinstall RTX 40 MFG Unlock (currently deployed as {currentDllName})"
+                ? $"Переустановить разблокировку RTX 40 MFG (сейчас развернута как {currentDllName})"
                 : "Install RTX 40 MFG Unlock — deploys RTXMFG.dll under a name you choose");
         }
 
@@ -1449,14 +1449,14 @@ public partial class DetailPanelBuilder
                 }
                 else
                 {
-                    installBtn.Content   = "Download failed — try again";
+                    installBtn.Content   = "Не удалось скачать — попробуйте снова";
                     installBtn.IsEnabled = true;
                 }
             }
             catch (Exception ex)
             {
                 CrashReporter.Log($"[BuildRtx40MfgRow] Install failed — {ex.Message}");
-                installBtn.Content   = "Install failed";
+                installBtn.Content   = "Не удалось установить";
                 installBtn.IsEnabled = true;
             }
         };
@@ -1476,26 +1476,26 @@ public partial class DetailPanelBuilder
             CornerRadius = new CornerRadius(8),
             Content = new TextBlock { Text = "⚙", FontSize = 14, HorizontalAlignment = HorizontalAlignment.Center },
         };
-        ToolTipService.SetToolTip(cogBtn, "RTX 40 MFG settings — configure multiplier mode");
+        ToolTipService.SetToolTip(cogBtn, "Настройки RTX 40 MFG — режим множителя");
         cogBtn.Click += async (s, e) =>
         {
-            var installedNote = isInstalled ? $"\n\nCurrently installed as: {currentDllName}" : "";
+            var installedNote = isInstalled ? $"\n\nСейчас установлен как: {currentDllName}" : "";
             var dlg = new ContentDialog
             {
-                Title = "RTX 40 MFG Settings",
+                Title = "Настройки RTX 40 MFG",
                 Content = new TextBlock
                 {
-                    Text = "Press Backspace in-game to open the RTX 40 MFG menu.\n\n" +
-                           "• Follow game — uses the game's own MFG setting\n" +
-                           "• Fixed 2x–6x — forces a specific multiplier\n" +
-                           "• Dynamic — targets the display refresh rate or a custom FPS value\n\n" +
-                           "If frames freeze above 2x, try setting Frame Generation to Preset B in the NVIDIA App." +
+                    Text = "Нажмите Backspace в игре, чтобы открыть меню RTX 40 MFG.\n\n" +
+                           "• Следовать игре — использует собственную настройку MFG игры\n" +
+                           "• Фиксированный 2x–6x — принудительный множитель\n" +
+                           "• Динамический — ориентируется на частоту обновления дисплея или свой FPS\n\n" +
+                           "Если кадры замирают на множителях выше 2x, попробуйте задать Preset B для генерации кадров в NVIDIA App." +
                            installedNote,
                     FontSize = 12,
                     TextWrapping = TextWrapping.Wrap,
                 },
-                PrimaryButtonText = "Open GitHub",
-                CloseButtonText   = "Close",
+                PrimaryButtonText = "Открыть GitHub",
+                CloseButtonText   = "Закрыть",
                 XamlRoot          = _window.Content.XamlRoot,
                 RequestedTheme    = ElementTheme.Dark,
             };
@@ -1521,7 +1521,7 @@ public partial class DetailPanelBuilder
             Opacity = isInstalled ? 1.0 : 0,
             IsHitTestVisible = isInstalled,
         };
-        ToolTipService.SetToolTip(removeBtn, "Remove RTX 40 MFG Unlock from this game");
+        ToolTipService.SetToolTip(removeBtn, "Удалить разблокировку RTX 40 MFG из этой игры");
         removeBtn.Click += (s, e) =>
         {
             if (string.IsNullOrEmpty(installPath)) return;
@@ -1574,7 +1574,7 @@ public partial class DetailPanelBuilder
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             VerticalAlignment = VerticalAlignment.Center,
         };
-        ToolTipService.SetToolTip(label, "DLSS Enabler — standalone proxy DLL that enables DLSS in games that don't natively support it.");
+        ToolTipService.SetToolTip(label, "DLSS Enabler — отдельная DLL-прокси, включающая DLSS в играх без нативной поддержки.");
         Grid.SetColumn(label, 0);
         row.Children.Add(label);
 
@@ -1590,7 +1590,7 @@ public partial class DetailPanelBuilder
         };
         if (isInstalled)
         {
-            ToolTipService.SetToolTip(statusBlock, $"Installed as: {currentDllName}\nClick to open GitHub releases");
+            ToolTipService.SetToolTip(statusBlock, $"Установлен как: {currentDllName}\nНажмите, чтобы открыть релизы на GitHub");
             statusBlock.PointerPressed += (s, e) =>
                 _ = Windows.System.Launcher.LaunchUriAsync(new Uri("https://github.com/RankFTW/rhi-repo/releases"));
         }
@@ -1600,7 +1600,7 @@ public partial class DetailPanelBuilder
         // Col 2 — Info button
         var infoBtn = new Button
         {
-            Content = "Info",
+            Content = "Инфо",
             FontSize = 11,
             Padding = new Thickness(6, 2, 6, 2),
             Width = 36,
@@ -1611,7 +1611,7 @@ public partial class DetailPanelBuilder
             BorderThickness = new Thickness(1),
             CornerRadius = new CornerRadius(8),
         };
-        ToolTipService.SetToolTip(infoBtn, "Open DLSS Enabler Nexus page");
+        ToolTipService.SetToolTip(infoBtn, "Открыть страницу DLSS Enabler на Nexus");
         infoBtn.Click += (s, e) =>
             _ = Windows.System.Launcher.LaunchUriAsync(new Uri("https://www.nexusmods.com/site/mods/757"));
         Grid.SetColumn(infoBtn, 2);
@@ -1620,9 +1620,9 @@ public partial class DetailPanelBuilder
         // Col 3 — Install button
         string installBtnLabel;
         if (osConflict)
-            installBtnLabel = "Installed via OptiScaler";
+            installBtnLabel = "Установлено через OptiScaler";
         else
-            installBtnLabel = isInstalled ? "↺  Reinstall DLSS Enabler" : "⬇  Install DLSS Enabler";
+            installBtnLabel = isInstalled ? "↺  Переустановить DLSS Enabler" : "⬇  Установить DLSS Enabler";
 
         var installBtn = new Button
         {
@@ -1648,13 +1648,13 @@ public partial class DetailPanelBuilder
             installBtn.IsEnabled = false;
             installBtn.IsHitTestVisible = false;
             installBtn.Opacity = 0.35;
-            ToolTipService.SetToolTip(installBtn, "Cannot install alongside OptiScaler — DLSS Enabler is already included within OptiScaler");
+            ToolTipService.SetToolTip(installBtn, "Нельзя установить вместе с OptiScaler — DLSS Enabler уже входит в состав OptiScaler");
         }
         else
         {
             ToolTipService.SetToolTip(installBtn, isInstalled
-                ? $"Reinstall DLSS Enabler (currently '{currentDllName}')"
-                : "Install DLSS Enabler — choose which DLL name to use");
+                ? $"Переустановить DLSS Enabler (сейчас «{currentDllName}»)"
+                : "Установить DLSS Enabler — выберите имя DLL");
 
             installBtn.Click += async (s, e) =>
             {
@@ -1674,14 +1674,14 @@ public partial class DetailPanelBuilder
                     }
                     else
                     {
-                        installBtn.Content   = "Install failed";
+                        installBtn.Content   = "Не удалось установить";
                         installBtn.IsEnabled = true;
                     }
                 }
                 catch (Exception ex)
                 {
                     CrashReporter.Log($"[BuildDlssEnablerRow] Install failed — {ex.Message}");
-                    installBtn.Content   = "Install failed";
+                    installBtn.Content   = "Не удалось установить";
                     installBtn.IsEnabled = true;
                 }
             };
@@ -1705,14 +1705,14 @@ public partial class DetailPanelBuilder
             IsHitTestVisible = !osConflict,
             Opacity = osConflict ? 0.35 : 1.0,
         };
-        ToolTipService.SetToolTip(cogBtn, osConflict ? "Cannot install alongside OptiScaler" : "DLSS Enabler settings");
+        ToolTipService.SetToolTip(cogBtn, osConflict ? "Нельзя установить вместе с OptiScaler" : "Настройки DLSS Enabler");
         cogBtn.Click += async (s, e) =>
         {
             var dlg = new ContentDialog
             {
-                Title   = "DLSS Enabler Settings",
-                Content = new TextBlock { Text = "No settings available.", FontSize = 12 },
-                CloseButtonText   = "Close",
+                Title   = "Настройки DLSS Enabler",
+                Content = new TextBlock { Text = "Настройки недоступны.", FontSize = 12 },
+                CloseButtonText   = "Закрыть",
                 XamlRoot          = _window.Content.XamlRoot,
                 RequestedTheme    = ElementTheme.Dark,
             };
@@ -1736,7 +1736,7 @@ public partial class DetailPanelBuilder
             Opacity = isInstalled ? 1.0 : 0,
             IsHitTestVisible = isInstalled,
         };
-        ToolTipService.SetToolTip(removeBtn, "Remove standalone DLSS Enabler from this game");
+        ToolTipService.SetToolTip(removeBtn, "Удалить отдельный DLSS Enabler из этой игры");
         removeBtn.Click += (s, e) =>
         {
             if (string.IsNullOrEmpty(installPath) || string.IsNullOrEmpty(currentDllName)) return;
@@ -1803,16 +1803,16 @@ public partial class DetailPanelBuilder
             if (isRecommended)
                 contentRow.Children.Add(MakeBadge("Recommended", "#1A3A20", "#6AE87A", "#2A5A30"));
             if (isRhiOwned)
-                contentRow.Children.Add(MakeBadge("Used by RHI", "#2A1818", "#CC6666", "#5A2828"));
+                contentRow.Children.Add(MakeBadge("Используется RHI", "#2A1818", "#CC6666", "#5A2828"));
             else if (isDxgiConflict)
-                contentRow.Children.Add(MakeBadge("May conflict with ReShade/OS", "#2A1A10", "#CC9955", "#5A3A18"));
+                contentRow.Children.Add(MakeBadge("Может конфликтовать с ReShade/ОС", "#2A1A10", "#CC9955", "#5A3A18"));
             if (isCurrent)
                 contentRow.Children.Add(MakeBadge("Current", "#182840", "#7AACDD", "#2A4468"));
 
             btn.Content = contentRow;
 
             if (isRhiOwned)
-                ToolTipService.SetToolTip(btn, "This filename is already used by an RHI-managed component. Choose a different name.");
+                ToolTipService.SetToolTip(btn, "Это имя уже занято компонентом под управлением RHI. Выберите другое.");
             else if (isDxgiConflict)
                 ToolTipService.SetToolTip(btn, "dxgi.dll may conflict with ReShade or OptiScaler if they also use this name.");
 
@@ -1832,7 +1832,7 @@ public partial class DetailPanelBuilder
 
         var pickerDialog = new ContentDialog
         {
-            Title = "Choose RTX 40 MFG DLL name",
+            Title = "Выберите имя DLL для RTX 40 MFG",
             Content = new StackPanel
             {
                 Spacing = 8,
@@ -1848,7 +1848,7 @@ public partial class DetailPanelBuilder
                     scrollViewer,
                 }
             },
-            CloseButtonText = "Cancel",
+            CloseButtonText = "Отмена",
             XamlRoot        = _window.Content.XamlRoot,
         };
 
@@ -1920,16 +1920,16 @@ public partial class DetailPanelBuilder
             if (isRecommended)
                 contentRow.Children.Add(MakeBadge("Recommended", "#1A3A20", "#6AE87A", "#2A5A30"));
             if (isRhiOwned)
-                contentRow.Children.Add(MakeBadge("Used by RHI", "#2A1818", "#CC6666", "#5A2828"));
+                contentRow.Children.Add(MakeBadge("Используется RHI", "#2A1818", "#CC6666", "#5A2828"));
             else if (isDxgiConflict)
-                contentRow.Children.Add(MakeBadge("May conflict with ReShade/OS", "#2A1A10", "#CC9955", "#5A3A18"));
+                contentRow.Children.Add(MakeBadge("Может конфликтовать с ReShade/ОС", "#2A1A10", "#CC9955", "#5A3A18"));
             if (isCurrent)
                 contentRow.Children.Add(MakeBadge("Current", "#182840", "#7AACDD", "#2A4468"));
 
             btn.Content = contentRow;
 
             if (isRhiOwned)
-                ToolTipService.SetToolTip(btn, "This filename is already used by an RHI-managed component (ReShade, OptiScaler, or DC). Choose a different name.");
+                ToolTipService.SetToolTip(btn, "Это имя уже занято компонентом под управлением RHI (ReShade, OptiScaler или DC). Выберите другое.");
             else if (isDxgiConflict)
                 ToolTipService.SetToolTip(btn, "dxgi.dll may conflict with ReShade or OS components if they also use this name.");
 
@@ -1949,7 +1949,7 @@ public partial class DetailPanelBuilder
 
         var pickerDialog = new ContentDialog
         {
-            Title = "Choose DLSS Enabler DLL name",
+            Title = "Выберите имя DLL для DLSS Enabler",
             Content = new StackPanel
             {
                 Spacing = 8,
@@ -1965,7 +1965,7 @@ public partial class DetailPanelBuilder
                     scrollViewer,
                 }
             },
-            CloseButtonText = "Cancel",
+            CloseButtonText = "Отмена",
             XamlRoot        = _window.Content.XamlRoot,
         };
 
@@ -2017,7 +2017,7 @@ public partial class DetailPanelBuilder
         };
         if (card.IsDxvkInstalled && !isDisabled)
         {
-            ToolTipService.SetToolTip(statusBlock, "Click to open DXVK releases");
+            ToolTipService.SetToolTip(statusBlock, "Открыть релизы DXVK");
             statusBlock.PointerPressed += (s, e) => _window.DetailDxvkStatus_PointerPressed(s, e);
             statusBlock.PointerEntered += (s, e) => _window.LinkText_PointerEntered(s, e);
             statusBlock.PointerExited  += (s, e) => _window.LinkText_PointerExited(s, e);
@@ -2028,7 +2028,7 @@ public partial class DetailPanelBuilder
         // Col 2 — Info button
         var infoBtn = new Button
         {
-            Content = "Info",
+            Content = "Инфо",
             FontSize = 11,
             Padding = new Thickness(6, 2, 6, 2),
             Width = 36,
@@ -2083,7 +2083,7 @@ public partial class DetailPanelBuilder
             IsEnabled = !isDisabled,
             Opacity = isDisabled ? 0.35 : 1.0,
         };
-        ToolTipService.SetToolTip(cogBtn, "DXVK Settings");
+        ToolTipService.SetToolTip(cogBtn, "Настройки DXVK");
         cogBtn.Click += (s, e) => _window.DxvkCogButton_Click(s, e);
         Grid.SetColumn(cogBtn, 4);
         row.Children.Add(cogBtn);
@@ -2105,7 +2105,7 @@ public partial class DetailPanelBuilder
             Opacity = showDelete ? 1.0 : 0.0,
             IsHitTestVisible = showDelete,
         };
-        ToolTipService.SetToolTip(deleteBtn, "Remove DXVK");
+        ToolTipService.SetToolTip(deleteBtn, "Удалить DXVK");
         deleteBtn.Click += (s, e) => _window.UninstallDxvkButton_Click(s, e);
         Grid.SetColumn(deleteBtn, 5);
         row.Children.Add(deleteBtn);
diff --git a/RenoDXCommander/DetailPanelBuilder.NeuralRendering.cs b/RenoDXCommander/DetailPanelBuilder.NeuralRendering.cs
index 6e6db67..db73e1b 100644
--- a/RenoDXCommander/DetailPanelBuilder.NeuralRendering.cs
+++ b/RenoDXCommander/DetailPanelBuilder.NeuralRendering.cs
@@ -151,7 +151,7 @@ public partial class DetailPanelBuilder
         };
         var nrTitle = new TextBlock
         {
-            Text       = "Neural Rendering",
+            Text       = "Нейронный рендеринг",
             FontSize   = 13,
             FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
@@ -196,7 +196,7 @@ public partial class DetailPanelBuilder
 
         // Method combo (col 0)
         var methodStack = new StackPanel { Spacing = 2 };
-        methodStack.Children.Add(new TextBlock { Text = "Method", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush) });
+        methodStack.Children.Add(new TextBlock { Text = "Метод", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush) });
 
         var methodCombo = new ComboBox
         {
@@ -214,17 +214,17 @@ public partial class DetailPanelBuilder
         }
         if (methodCombo.SelectedIndex < 0) methodCombo.SelectedIndex = 0;
         ToolTipService.SetToolTip(methodCombo,
-            "DLSS5 Tool: for DX12 native-DLSS games.\n" +
-            "DLSS5 Tool + DX11 Bridge: for DX11/Vulkan native-DLSS games.\n" +
-            "DLSS Tool (ShortFuse): alternative full-stack install for native-DLSS games.\n" +
-            "DLSS5 Feeder: for games with no native DLSS (DX11, DX12, Vulkan, 32-bit).");
+            "DLSS5 Tool: для DX12-игр с нативным DLSS.\n" +
+            "DLSS5 Tool + DX11 Bridge: для DX11/Vulkan-игр с нативным DLSS.\n" +
+            "DLSS Tool (ShortFuse): альтернативная полнофункциональная установка для игр с нативным DLSS.\n" +
+            "DLSS5 Feeder: для игр без нативного DLSS (DX11, DX12, Vulkan, 32 бита).");
         methodStack.Children.Add(methodCombo);
         Grid.SetColumn(methodStack, 0);
         row1.Children.Add(methodStack);
 
         // Pack version combo (col 1) — Feeder version or Bridge version, always "Latest" (managed by AddonPackService)
         var packVersionLabel = new TextBlock { FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush) };
-        packVersionLabel.Text = effectiveMethod == NrMethodFeeder ? "Feeder Version" : "Bridge Version";
+        packVersionLabel.Text = effectiveMethod == NrMethodFeeder ? "Версия Feeder" : "Версия моста";
         var packVersionStack = new StackPanel { Spacing = 2, Visibility = isFeederOrBridge ? Visibility.Visible : Visibility.Collapsed };
         packVersionStack.Children.Add(packVersionLabel);
         var packVersionCombo = new ComboBox
@@ -298,10 +298,10 @@ public partial class DetailPanelBuilder
 
         // DLSS5 Tool / SF version combo (col 2)
         var addonVersionLabel = new TextBlock { FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush) };
-        addonVersionLabel.Text = effectiveMethod == NrMethodShortFuse ? "SF Version"
-                                : effectiveMethod == NrMethodFeeder    ? "DLSS5 Tool Version"
-                                : effectiveMethod == NrMethodDlss5ToolBridge ? "DLSS5 Tool Version"
-                                : "DLSS5 Tool Version";
+        addonVersionLabel.Text = effectiveMethod == NrMethodShortFuse ? "Версия SF"
+                                : effectiveMethod == NrMethodFeeder    ? "Версия DLSS5 Tool"
+                                : effectiveMethod == NrMethodDlss5ToolBridge ? "Версия DLSS5 Tool"
+                                : "Версия DLSS5 Tool";
         var addonVersionStack = new StackPanel { Spacing = 2 };
         addonVersionStack.Children.Add(addonVersionLabel);
 
@@ -355,14 +355,14 @@ public partial class DetailPanelBuilder
         ToolTipService.SetToolTip(addonVersionCombo,
             effectiveMethod == NrMethodFeeder
                 ? "Version of renodx-dlss5.addon64 deployed as the neural consumer. The Feeder addon itself always uses the latest version."
-                : "Addon version to install. 'Latest' always installs the newest available and auto-updates.");
+                : "Версия аддона для установки. «Latest» всегда ставит самую свежую и обновляет автоматически.");
         addonVersionStack.Children.Add(addonVersionCombo);
         Grid.SetColumn(addonVersionStack, 2);
         row1.Children.Add(addonVersionStack);
 
         // NR version combo (col 3)
         var nrVersionStack = new StackPanel { Spacing = 2 };
-        nrVersionStack.Children.Add(new TextBlock { Text = "NR DLL Version", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush) });
+        nrVersionStack.Children.Add(new TextBlock { Text = "Версия NR DLL", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush) });
 
         var nrVersionCombo = new ComboBox
         {
@@ -375,7 +375,7 @@ public partial class DetailPanelBuilder
         foreach (var v in nrVersions)
             nrVersionCombo.Items.Add(v);
         nrVersionCombo.SelectedIndex = 0;
-        ToolTipService.SetToolTip(nrVersionCombo, "NR DLL version to deploy. 'Latest' always uses the newest available.");
+        ToolTipService.SetToolTip(nrVersionCombo, "Версия NR DLL для развертывания. «Latest» всегда использует новейшую доступную.");
         nrVersionStack.Children.Add(nrVersionCombo);
         Grid.SetColumn(nrVersionStack, 3);
         row1.Children.Add(nrVersionStack);
@@ -525,7 +525,7 @@ public partial class DetailPanelBuilder
                 }
                     break;
                 case NrMethodFeeder:
-                    Tag(fei   ? "✓ Feeder Addon"            : "✗ Feeder Addon",  fei);
+                    Tag(fei   ? "✓ Аддон Feeder"            : "✗ Аддон Feeder",  fei);
                     // For 32-bit games DLSS5 Tool lives in host64\ — label accordingly
                     if (card.Is32Bit)
                     {
@@ -555,7 +555,7 @@ public partial class DetailPanelBuilder
                     }
                     break;
                 default:
-                    Tag("Not installed", false);
+                    Tag("Не установлено", false);
                     break;
             }
         }
@@ -595,25 +595,25 @@ public partial class DetailPanelBuilder
                 case NrMethodDlss5Tool:
                     descText.Text = hasDlss
                         ? "For DX12 games with native DLSS. Deploys the DLSS5 Tool ReShade addon and nvngx_dlssnr.dll. Lighter alternative to ShortFuse when you don't need the full Streamline stack."
-                        : "For DX12 games with native DLSS. This game has no detected DLSS — consider DLSS Tool (ShortFuse) instead.";
-                    descLink.Content = "DLSS5 Tool info →";
+                        : "Для DX12-игр с нативным DLSS. У этой игры DLSS не обнаружен — возможно, лучше подойдёт DLSS Tool (ShortFuse).";
+                    descLink.Content = "Информация о DLSS5 Tool →";
                     descLink.NavigateUri = new Uri("https://discord.com/channels/1408098019194310818/1543802634991968366");
                     break;
                 case NrMethodDlss5ToolBridge:
-                    descText.Text = "For DX11 and Vulkan games with native DLSS. The bridge mirrors the game's DLSS onto a private DX12 session so the NR addon can hook it.";
-                    descLink.Content = "DX11 Bridge info →";
+                    descText.Text = "Для DX11- и Vulkan-игр с нативным DLSS. Мост транслирует DLSS игры в отдельную DX12-сессию, чтобы аддон NR мог к нему подключиться.";
+                    descLink.Content = "Информация о DX11 Bridge →";
                     descLink.NavigateUri = new Uri("https://github.com/NIGos/dlss5-bridge");
                     break;
                 case NrMethodShortFuse:
                     descText.Text = "Recommended for most games with native DLSS. Deploys the full DLSS SR/RR/FG/NR stack and Streamline alongside the ReShade addon. Supports DX12, DX11, DX9, and Vulkan.";
-                    descLink.Content = "ShortFuse info →";
+                    descLink.Content = "Информация о ShortFuse →";
                     descLink.NavigateUri = new Uri("https://discord.com/channels/1408098019194310818/1543975158937821315");
                     break;
                 case NrMethodFeeder:
                     descText.Text = is32Bit
-                        ? "For 32-bit games. Feeds a synthetic DLSS contract from ReShade depth and motion vectors. Deploys the Feeder addon, DLSS5 Tool (neural consumer), NR DLL, DLSS SR DLL, and the required shaders (DLSS5_Feed.fx + LumeniteFX)."
-                        : "For games with no native DLSS (DX11, DX12, Vulkan, OpenGL). Feeds a synthetic DLSS contract from ReShade depth and motion vectors. Deploys the Feeder addon, DLSS5 Tool (neural consumer), NR DLL, DLSS SR DLL, and required shaders.";
-                    descLink.Content = "Feeder setup guide →";
+                        ? "Для 32-битных игр. Формирует синтетический DLSS-контракт из карты глубины и векторов движения ReShade. Разворачивает аддон Feeder, DLSS5 Tool (нейронный потребитель), NR DLL, DLSS SR DLL и необходимые шейдеры (DLSS5_Feed.fx + LumeniteFX)."
+                        : "Для игр без нативного DLSS (DX11, DX12, Vulkan, OpenGL). Формирует синтетический DLSS-контракт из карты глубины и векторов движения ReShade. Разворачивает аддон Feeder, DLSS5 Tool (нейронный потребитель), NR DLL, DLSS SR DLL и необходимые шейдеры.";
+                    descLink.Content = "Руководство по настройке Feeder →";
                     descLink.NavigateUri = new Uri("https://github.com/jlrouzies-fr/DLSS5-Feeder");
                     break;
             }
@@ -638,7 +638,7 @@ public partial class DetailPanelBuilder
 
         var removeBtn = new Button
         {
-            Content = "Remove",
+            Content = "Удалить",
             FontSize = 12,
             Height = 34,
             CornerRadius = new CornerRadius(8),
@@ -661,7 +661,7 @@ public partial class DetailPanelBuilder
             Content = new TextBlock { Text = "⚙", FontSize = 14, HorizontalAlignment = HorizontalAlignment.Center },
             Visibility = effectiveMethod == NrMethodShortFuse ? Visibility.Visible : Visibility.Collapsed,
         };
-        ToolTipService.SetToolTip(sfCogBtn, "ShortFuse settings — auto-configure ReShade for FrameGen");
+        ToolTipService.SetToolTip(sfCogBtn, "Настройки ShortFuse — автонастройка ReShade для FrameGen");
         sfCogBtn.Click += async (s, e) =>
         {
             bool currentEnabled = _window.ViewModel.GetSfAutoConfigEnabled(gameName, store);
@@ -670,7 +670,7 @@ public partial class DetailPanelBuilder
             var toggleRow = new StackPanel { Orientation = Orientation.Horizontal, Spacing = 12 };
             var togLabel = new TextBlock
             {
-                Text = "Auto-configure ReShade for FrameGen",
+                Text = "Автонастройка ReShade для FrameGen",
                 FontSize = 12,
                 Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
                 VerticalAlignment = VerticalAlignment.Center,
@@ -688,12 +688,12 @@ public partial class DetailPanelBuilder
 
             var desc = new TextBlock
             {
-                Text = "When On, installing ShortFuse will automatically:\n" +
-                       "• Rename ReShade to Reshade64.asi\n" +
-                       "• Install ASI Loader (winmm → version → dinput8)\n" +
+                Text = "Вкл — установка ShortFuse автоматически:\n" +
+                       "• Переименовать ReShade в Reshade64.asi\n" +
+                       "• Установить ASI Loader (winmm → version → dinput8)\n" +
                        "• Write HookStreamline=1 and HookDirectX=1 to reshade.ini\n\n" +
-                       "These steps are needed for FrameGen to work correctly after ReShade.\n\n" +
-                       "Note: no longer required on ShortFuse v0.54 and above.",
+                       "Эти шаги нужны, чтобы FrameGen корректно работал вместе с ReShade.\n\n" +
+                       "Примечание: для ShortFuse v0.54 и новее не требуется.",
                 FontSize = 11,
                 Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush),
                 TextWrapping = TextWrapping.Wrap,
@@ -708,10 +708,10 @@ public partial class DetailPanelBuilder
 
             var dlg = new ContentDialog
             {
-                Title = "ShortFuse Settings",
+                Title = "Настройки ShortFuse",
                 Content = content,
-                PrimaryButtonText = "Save",
-                CloseButtonText = "Cancel",
+                PrimaryButtonText = "Сохранить",
+                CloseButtonText = "Отмена",
                 XamlRoot = _window.Content.XamlRoot,
             };
             var result = await DialogService.ShowSafeAsync(dlg);
@@ -738,7 +738,7 @@ public partial class DetailPanelBuilder
             // Install button appearance
             if (isFeeder)
             {
-                installBtn.Content = "Install Feeder Addon";
+                installBtn.Content = "Установить аддон Feeder";
                 installBtn.Background  = UIFactory.Brush(ResourceKeys.AccentBlueBgBrush);
                 installBtn.Foreground  = UIFactory.Brush(ResourceKeys.AccentBlueBrush);
                 installBtn.BorderBrush = UIFactory.Brush(ResourceKeys.AccentBlueBorderBrush);
@@ -752,7 +752,7 @@ public partial class DetailPanelBuilder
             }
             else
             {
-                installBtn.Content = "Install Neural Rendering";
+                installBtn.Content = "Установить нейронный рендеринг";
                 installBtn.Background  = UIFactory.Brush(ResourceKeys.AccentBlueBgBrush);
                 installBtn.Foreground  = UIFactory.Brush(ResourceKeys.AccentBlueBrush);
                 installBtn.BorderBrush = UIFactory.Brush(ResourceKeys.AccentBlueBorderBrush);
@@ -767,7 +767,7 @@ public partial class DetailPanelBuilder
             // Pack version column (Feeder/Bridge only)
             bool showPackVersion = selKey == NrMethodFeeder || selKey == NrMethodDlss5ToolBridge;
             packVersionStack.Visibility = showPackVersion ? Visibility.Visible : Visibility.Collapsed;
-            packVersionLabel.Text = selKey == NrMethodFeeder ? "Feeder Version" : "Bridge Version";
+            packVersionLabel.Text = selKey == NrMethodFeeder ? "Версия Feeder" : "Версия моста";
             if (showPackVersion)
             {
                 PopulatePackVersionCombo(selKey);
@@ -778,7 +778,7 @@ public partial class DetailPanelBuilder
             }
 
             // DLSS5 Tool / SF version label
-            addonVersionLabel.Text = selKey == NrMethodShortFuse ? "SF Version" : "DLSS5 Tool Version";
+            addonVersionLabel.Text = selKey == NrMethodShortFuse ? "Версия SF" : "Версия DLSS5 Tool";
 
             // Addon version combo relevant for all methods (controls renodx-dlss5.addon64 version)
             // For Feeder: controls the neural consumer (renodx-dlss5.addon64) — feeder addon itself always latest
@@ -787,7 +787,7 @@ public partial class DetailPanelBuilder
             addonVersionStack.Opacity   = anyInstalled ? 0.4 : 1.0;
             addonVersionCombo.IsEnabled = addonVersionEditable;
             ToolTipService.SetToolTip(addonVersionStack, anyInstalled
-                ? "Uninstall Neural Rendering first to change the addon version."
+                ? "Сначала удалите нейронный рендеринг, чтобы сменить версию аддона."
                 : selKey == NrMethodFeeder
                     ? "Version of renodx-dlss5.addon64 deployed as the neural consumer. The Feeder addon itself always uses the latest version."
                     : null);
@@ -947,7 +947,7 @@ public partial class DetailPanelBuilder
                 // Ensure ReShade is installed first — all NR methods require it
                 if (!card.IsRsInstalled)
                 {
-                    _window.DispatcherQueue?.TryEnqueue(() => installBtn.Content = "Installing ReShade...");
+                    _window.DispatcherQueue?.TryEnqueue(() => installBtn.Content = "Установка ReShade...");
 
                     // For DX9 Feeder: dgVoodoo2 will own d3d9.dll, so ReShade must be dxgi.dll.
                     // Call InstallReShadeInternalAsync directly with the forced filename instead of
@@ -969,7 +969,7 @@ public partial class DetailPanelBuilder
                     && card.RsRecord?.InstalledAs?.Equals("d3d9.dll", StringComparison.OrdinalIgnoreCase) == true)
                 {
                     // ReShade already installed as d3d9.dll (wrong for dgVoodoo) — reinstall as dxgi.dll
-                    _window.DispatcherQueue?.TryEnqueue(() => installBtn.Content = "Fixing ReShade filename...");
+                    _window.DispatcherQueue?.TryEnqueue(() => installBtn.Content = "Исправление имени файла ReShade...");
                     await _window.ViewModel.InstallReShadeInternalAsync(card, "dxgi.dll").ConfigureAwait(false);
                     await Task.Delay(300).ConfigureAwait(false);
                 }
@@ -1040,7 +1040,7 @@ public partial class DetailPanelBuilder
             catch (Exception ex)
             {
                 CrashReporter.Log($"[NeuralRendering.Install] Failed for '{gameName}' — {ex.Message}");
-                _window.DispatcherQueue?.TryEnqueue(() => installBtn.Content = "Install failed");
+                _window.DispatcherQueue?.TryEnqueue(() => installBtn.Content = "Не удалось установить");
             }
             finally
             {
@@ -1213,8 +1213,8 @@ public partial class DetailPanelBuilder
             VerticalAlignment = VerticalAlignment.Center,
         };
         ToolTipService.SetToolTip(costScalerLabel,
-            "When On, installing a Neural Rendering method will also deploy the DLSS NR Cost Scaler proxy. " +
-            "Runs the neural model at reduced resolution (default 75%) for significant GPU savings while keeping native detail.");
+            "Вкл — при установке метода нейронного рендеринга также развернётся прокси DLSS NR Cost Scaler. " +
+            "Запускает нейронную модель в пониженном разрешении (по умолчанию 75%), заметно экономя GPU при сохранении деталей.");
         var costScalerToggle = new ToggleSwitch
         {
             IsOn = costScalerPref,
@@ -1225,15 +1225,15 @@ public partial class DetailPanelBuilder
             Opacity = costScalerToggleEnabled ? 1.0 : 0.45,
         };
         if (!costScalerSvc.IsStagingReady)
-            ToolTipService.SetToolTip(costScalerToggle, "Cost Scaler not yet staged — will be available after first launch");
+            ToolTipService.SetToolTip(costScalerToggle, "Cost Scaler ещё не загружен — появится после первого запуска");
         else if (nrMethodInstalled)
-            ToolTipService.SetToolTip(costScalerToggle, "Remove the installed NR method first, then toggle Cost Scaler On before reinstalling");
+            ToolTipService.SetToolTip(costScalerToggle, "Сначала удалите установленный метод NR, затем включите Cost Scaler и переустановите");
 
         // Installed indicator
         bool costScalerInstalled = DlssNrCostScalerService.IsInstalled(installPath);
         var costScalerStatus = new TextBlock
         {
-            Text = costScalerInstalled ? "Installed" : "",
+            Text = costScalerInstalled ? "Установлено" : "",
             FontSize = 11,
             Foreground = UIFactory.GetBrush("#5ECB7D"),
             VerticalAlignment = VerticalAlignment.Center,
@@ -1253,7 +1253,7 @@ public partial class DetailPanelBuilder
         {
             nrBody.Children.Add(new TextBlock
             {
-                Text = "Cost Scaler is built into the ShortFuse addon — this toggle is no longer required but remains available if you prefer the standalone version.",
+                Text = "Cost Scaler встроен в аддон ShortFuse — этот переключатель больше не нужен, но остался на случай, если предпочитаете отдельную версию.",
                 FontSize = 10,
                 Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush),
                 TextWrapping = TextWrapping.Wrap,
@@ -1303,10 +1303,10 @@ public partial class DetailPanelBuilder
         if (useLatest)
         {
             // Use the flat staging file (latest) — same as before
-            _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Staging DLSS5 Tool...");
+            _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Загрузка DLSS5 Tool...");
             await rdx5Svc.EnsureStagingAsync().ConfigureAwait(false);
             if (!rdx5Svc.IsStagingReady)
-                throw new InvalidOperationException("DLSS5 Tool staging not ready");
+                throw new InvalidOperationException("DLSS5 Tool ещё не загружен");
             addonSourcePath = rdx5Svc.StagedFilePath;
             // Persist "Latest" (clears any pinned version)
             _window.ViewModel.SetNrAddonVersion(gameName, null, store);
@@ -1321,7 +1321,7 @@ public partial class DetailPanelBuilder
                 CrashReporter.Log($"[NeuralRendering] Could not stage DLSS5 Tool v{requestedVersion} — falling back to latest");
                 await rdx5Svc.EnsureStagingAsync().ConfigureAwait(false);
                 if (!rdx5Svc.IsStagingReady)
-                    throw new InvalidOperationException("DLSS5 Tool staging not ready");
+                    throw new InvalidOperationException("DLSS5 Tool ещё не загружен");
                 addonSourcePath = rdx5Svc.StagedFilePath;
                 _window.ViewModel.SetNrAddonVersion(gameName, null, store);
             }
@@ -1344,7 +1344,7 @@ public partial class DetailPanelBuilder
         }).ConfigureAwait(false);
 
         // Upgrade all DLSS DLLs to latest (SR/RR/FG + NR)
-        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Upgrading DLSS DLLs...");
+        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Обновление DLL DLSS...");
         await UpgradeDlssDllsAsync(card, dlssSvc, nrVersionCombo).ConfigureAwait(false);
 
         // Append the addon file to the Dlss5Tool component record (DLLs recorded in UpgradeDlssDllsAsync)
@@ -1530,7 +1530,7 @@ public partial class DetailPanelBuilder
         }
         bool useLatestBridge = string.IsNullOrEmpty(requestedBridgeVersion) || requestedBridgeVersion == "Latest";
 
-        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Downloading DX11 Bridge...");
+        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Загрузка DX11 Bridge...");
 
         // Resolve Bridge staged path — versioned or latest via AddonPackService
         string? bridgeSourcePath = null;
@@ -1564,7 +1564,7 @@ public partial class DetailPanelBuilder
         }
 
         // Deploy — Bridge goes in the game root (next to ReShade / exe), not reshade-addons
-        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Deploying DX11 Bridge...");
+        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Развертывание DX11 Bridge...");
         await Task.Run(() =>
         {
             if (bridgeSourcePath == null || !File.Exists(bridgeSourcePath))
@@ -1597,10 +1597,10 @@ public partial class DetailPanelBuilder
         string sfSourcePath;
         if (useLatest)
         {
-            _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Staging ShortFuse...");
+            _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Загрузка ShortFuse...");
             await rdx5Svc.EnsureSfStagingAsync().ConfigureAwait(false);
             if (!rdx5Svc.IsSfStagingReady)
-                throw new InvalidOperationException("ShortFuse staging not ready");
+                throw new InvalidOperationException("ShortFuse ещё не загружен");
             sfSourcePath = rdx5Svc.SfStagedFilePath;
             _window.ViewModel.SetNrAddonVersion(gameName, null, store);
         }
@@ -1613,7 +1613,7 @@ public partial class DetailPanelBuilder
                 CrashReporter.Log($"[NeuralRendering] Could not stage ShortFuse v{requestedVersion} — falling back to latest");
                 await rdx5Svc.EnsureSfStagingAsync().ConfigureAwait(false);
                 if (!rdx5Svc.IsSfStagingReady)
-                    throw new InvalidOperationException("ShortFuse staging not ready");
+                    throw new InvalidOperationException("ShortFuse ещё не загружен");
                 sfSourcePath = rdx5Svc.SfStagedFilePath;
                 _window.ViewModel.SetNrAddonVersion(gameName, null, store);
             }
@@ -1624,7 +1624,7 @@ public partial class DetailPanelBuilder
             }
         }
 
-        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Installing DLSS stack...");
+        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Установка стека DLSS...");
 
         // Deploy the SF addon from the resolved source path
         try
@@ -1663,7 +1663,7 @@ public partial class DetailPanelBuilder
         // Apply auto-config (rename ReShade, install UAL, write reshade.ini [INSTALL] keys)
         if (_window.ViewModel.GetSfAutoConfigEnabled(card.GameName, card.Source ?? ""))
         {
-            _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Configuring ReShade...");
+            _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Настройка ReShade...");
             await _window.ViewModel.ApplySfAutoConfigAsync(card).ConfigureAwait(false);
         }
     }
@@ -1728,7 +1728,7 @@ public partial class DetailPanelBuilder
                     {
                         _window.ViewModel.SetUalInstalledAs(gameName, chosenName, store);
                         CrashReporter.Log($"[SfAutoConfig] Installed UAL as '{chosenName}' for '{gameName}'" +
-                            (hookedOriginal != null ? $" (chained '{hookedOriginal}')" : ""));
+                            (hookedOriginal != null ? $" (в цепочке: «{hookedOriginal}»)" : ""));
                     }
                 }
                 catch (Exception ex) { CrashReporter.Log($"[SfAutoConfig] UAL install failed — {ex.Message}"); }
@@ -1796,7 +1796,7 @@ public partial class DetailPanelBuilder
         }
         bool useLatestConsumer = string.IsNullOrEmpty(requestedVersion) || requestedVersion == "Latest";
 
-        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Downloading Feeder...");
+        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Загрузка Feeder...");
 
         // Resolve Feeder staged path — versioned or latest via AddonPackService
         string? feederSourcePath = null;
@@ -1833,7 +1833,7 @@ public partial class DetailPanelBuilder
             _window.ViewModel.SetNrPackVersion(gameName, null, store); // clear version pin — using latest
         }
 
-        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Deploying Feeder...");
+        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Развертывание Feeder...");
         await Task.Run(() =>
         {
             if (feederSourcePath == null || !File.Exists(feederSourcePath))
@@ -1853,7 +1853,7 @@ public partial class DetailPanelBuilder
         // Deploy DLSS5 Tool as neural consumer (Feeder needs renodx-dlss5.addon64 alongside it)
         // For 32-bit games the neural consumer runs in host64\ — it must NOT be in the game folder
         // (32-bit ReShade cannot load .addon64 files).
-        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Deploying DLSS5 Tool...");
+        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Развертывание DLSS5 Tool...");
 
         // Resolve the source path for the neural consumer — versioned or latest
         string? consumerSourcePath = null;
@@ -1893,7 +1893,7 @@ public partial class DetailPanelBuilder
         }
 
         // Deploy newest nvngx_dlss.dll — required by Feeder beside the game exe (install root, not detected plugin path)
-        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Deploying DLSS SR...");
+        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Развертывание DLSS SR...");
         var cachedDlss = await _dlssStreamlineService.EnsureNewestDlssCachedAsync().ConfigureAwait(false);
         if (cachedDlss != null && new FileInfo(cachedDlss).Length > 0)
         {
@@ -1915,7 +1915,7 @@ public partial class DetailPanelBuilder
         // DLSS5_Feed.fx is seeded from the Feeder addon zip into the DLSS5Feeder staging folder.
         // LumeniteFX is downloaded via the pack system.
         // We do NOT call EnsurePacksAsync for DLSS5Feeder — its URL is a dead fallback that 404s.
-        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Deploying shaders...");
+        _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Развертывание шейдеров...");
         try
         {
             // Ensure LumeniteFX is staged (DLSS5Feeder is self-contained — no download needed)
@@ -1967,7 +1967,7 @@ public partial class DetailPanelBuilder
                     // No zip available locally — force re-download the Feeder addon so the zip
                     // is fetched fresh and DLSS5_Feed.fx is extracted from it.
                     CrashReporter.Log("[NeuralRendering] DLSS5_Feed.fx missing and no zip found — re-downloading Feeder addon to seed it");
-                    _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Downloading Feed.fx...");
+                    _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Загрузка Feed.fx...");
                     var feederEntry = addonSvc.AvailablePacks.FirstOrDefault(p =>
                         p.PackageName.Equals(FeederPackageName, StringComparison.OrdinalIgnoreCase));
                     if (feederEntry != null)
@@ -2158,7 +2158,7 @@ public partial class DetailPanelBuilder
             var manifest = _window.ViewModel.Manifest;
             if (manifest?.DgVoodooVersions?.Count > 0)
             {
-                _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Deploying dgVoodoo2...");
+                _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Развертывание dgVoodoo2...");
                 try
                 {
                     var dgSvc = App.Services.GetRequiredService<DgVoodooService>();
@@ -2180,7 +2180,7 @@ public partial class DetailPanelBuilder
         // Note: NOT gated on isDx9 — host64 is needed for all 32-bit Feeder installs
         if (card.Is32Bit)
         {
-                _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Setting up host64\\...");
+                _window.DispatcherQueue?.TryEnqueue(() => statusBtn.Content = "Настройка host64\\...");
                 await Task.Run(async () =>
                 {
                     try
diff --git a/RenoDXCommander/DetailPanelBuilder.Overrides.Dlss.cs b/RenoDXCommander/DetailPanelBuilder.Overrides.Dlss.cs
index f25322c..a8bf912 100644
--- a/RenoDXCommander/DetailPanelBuilder.Overrides.Dlss.cs
+++ b/RenoDXCommander/DetailPanelBuilder.Overrides.Dlss.cs
@@ -32,7 +32,7 @@ public partial class DetailPanelBuilder
         });
 
         // Version ComboBox
-        var versionLabel = new TextBlock { Text = "Version", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) };
+        var versionLabel = new TextBlock { Text = "Версия", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) };
         col.Children.Add(versionLabel);
 
         // Build items list with (Default) marker on the game's original/default version
@@ -121,7 +121,7 @@ public partial class DetailPanelBuilder
         };
 
         if (driverOverrideActive)
-            ToolTipService.SetToolTip(versionCombo, "NVIDIA Override is active — the driver is injecting its own latest DLL for this game. Select any other version to disable the override and deploy that version instead.");
+            ToolTipService.SetToolTip(versionCombo, "Активно переопределение NVIDIA — драйвер подставляет собственную новейшую DLL для этой игры. Выберите любую другую версию, чтобы отключить переопределение и развернуть выбранную.");
         else if (onDriverOverrideToggled != null)
             ToolTipService.SetToolTip(versionCombo, "Selects which DLL version is copied into the game folder. Default restores the original game DLL. Custom uses your own file from %LocalAppData%\\RHI\\Custom\\DLSS\\. NVIDIA Override lets the driver inject its own latest version instead of a file on disk — equivalent to enabling DLSS Override in NVIDIA App or Profile Inspector.");
         else
@@ -136,7 +136,7 @@ public partial class DetailPanelBuilder
             var selected = versionCombo.SelectedItem as string;
             if (string.IsNullOrEmpty(selected)) return;
 
-            if (selected == "NVIDIA Override")
+            if (selected == "Переопределение NVIDIA")
             {
                 // Enable driver DLL override — no DLL swap needed
                 onDriverOverrideToggled?.Invoke(true);
@@ -144,7 +144,7 @@ public partial class DetailPanelBuilder
             }
 
             // If we were on NVIDIA Override and switched away, disable it first
-            if (driverOverrideActive || (ev.RemovedItems.Count > 0 && ev.RemovedItems[0] as string == "NVIDIA Override"))
+            if (driverOverrideActive || (ev.RemovedItems.Count > 0 && ev.RemovedItems[0] as string == "Переопределение NVIDIA"))
                 onDriverOverrideToggled?.Invoke(false);
 
             versionCombo.IsEnabled = false;
@@ -166,7 +166,7 @@ public partial class DetailPanelBuilder
         // Preset ComboBox (only for SR, RR, FG)
         if (presets != null && isPresent)
         {
-            col.Children.Add(new TextBlock { Text = "Preset", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
+            col.Children.Add(new TextBlock { Text = "Пресет", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
 
             var presetItems = presets.Select(p => p.Name).ToList();
             int presetIdx = 0;
@@ -187,9 +187,9 @@ public partial class DetailPanelBuilder
             // Add tooltip explaining presets
             string presetTooltip = label switch
             {
-                "DLSS Super Resolution" => "Override the DLSS upscaling model. J/K use the 1st-gen transformer (DLSS 4.0). L/M use the 2nd-gen transformer (DLSS 4.5) with better temporal stability. NVIDIA Recommended uses NVIDIA's per-resolution preset selection.",
-                "Ray Reconstruction" => "Override the Ray Reconstruction denoising model. Higher presets are newer model iterations. NVIDIA Recommended uses NVIDIA's per-resolution preset selection.",
-                "Frame Generation" => "Override the Frame Generation interpolation model. Higher presets are newer model iterations. NVIDIA Recommended uses NVIDIA's per-resolution preset selection.",
+                "Суперразрешение DLSS (SR)" => "Переопределить модель масштабирования DLSS. J/K — трансформер 1-го поколения (DLSS 4.0). L/M — трансформер 2-го поколения (DLSS 4.5) с лучшей временной стабильностью. «Рекомендовано NVIDIA» использует выбор пресета по разрешению от NVIDIA.",
+                "Ray Reconstruction" => "Переопределить модель шумоподавления Ray Reconstruction. Пресеты выше — более новые итерации модели. «Рекомендовано NVIDIA» использует выбор пресета по разрешению от NVIDIA.",
+                "Генерация кадров" => "Переопределить модель интерполяции генерации кадров. Пресеты выше — более новые итерации модели. «Рекомендовано NVIDIA» использует выбор пресета по разрешению от NVIDIA.",
                 _ => ""
             };
             if (!string.IsNullOrEmpty(presetTooltip))
@@ -210,7 +210,7 @@ public partial class DetailPanelBuilder
         // Render Scale ComboBox (only for SR and RR)
         if (onRenderScaleSelected != null && isPresent)
         {
-            col.Children.Add(new TextBlock { Text = "Render Scale", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
+            col.Children.Add(new TextBlock { Text = "Масштаб рендеринга", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
             var rsOptions = DlssPresetService.RenderScaleOptions;
             var rsItems = rsOptions.Select(o => o.Name).ToList();
 
@@ -239,7 +239,7 @@ public partial class DetailPanelBuilder
                 IsEnabled = isPresent,
             };
             ToolTipService.SetToolTip(rsCombo,
-                "Override the DLSS render resolution scale. Off = game controls the scale.\nNamed presets set a fixed percentage. Custom lets you enter any value from 33-100%.");
+                "Переопределить масштаб рендеринга DLSS. Выкл — масштабом управляет игра.\nИменные пресеты задают фиксированный процент. «Свой» позволяет ввести любое значение от 33 до 100%.");
 
             bool rsInit = true;
             rsCombo.SelectionChanged += (s, ev) =>
diff --git a/RenoDXCommander/DetailPanelBuilder.Overrides.DriverSettings.cs b/RenoDXCommander/DetailPanelBuilder.Overrides.DriverSettings.cs
index 75e0479..125e87d 100644
--- a/RenoDXCommander/DetailPanelBuilder.Overrides.DriverSettings.cs
+++ b/RenoDXCommander/DetailPanelBuilder.Overrides.DriverSettings.cs
@@ -39,8 +39,8 @@ public partial class DetailPanelBuilder
             (_nvBodyPanel ?? _window.NvidiaProfilePanel).Children.Add(new TextBlock
             {
                 Text = elevated
-                    ? "✓ Running as admin — all driver profile settings are writable."
-                    : "⚠ Admin rights required to write driver profile settings. Enable Admin Mode in Settings or restart as admin.",
+                    ? "✓ Запущено от администратора — все настройки профиля доступны для записи."
+                    : "⚠ Для записи настроек профиля драйвера нужны права администратора. Включите режим администратора в настройках или перезапустите RHI от его имени.",
                 FontSize = 10,
                 Foreground = UIFactory.Brush(elevated ? ResourceKeys.TextTertiaryBrush : ResourceKeys.AccentAmberDimBrush),
                 TextWrapping = TextWrapping.Wrap,
@@ -153,12 +153,12 @@ public partial class DetailPanelBuilder
             // ── Column 0: VSync ──
             var vsyncCol = new StackPanel { Spacing = 4 };
             var vsyncLabel = new TextBlock { Text = "VSync", FontSize = 11, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) };
-            ToolTipService.SetToolTip(vsyncLabel, "Vertical Sync settings — controls how the driver synchronizes frame rendering with your display's refresh rate.");
+            ToolTipService.SetToolTip(vsyncLabel, "Настройки вертикальной синхронизации — как драйвер согласует рендеринг кадров с частотой обновления дисплея.");
             vsyncCol.Children.Add(vsyncLabel);
 
             // VSync Mode
             {
-                vsyncCol.Children.Add(new TextBlock { Text = "Mode", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
+                vsyncCol.Children.Add(new TextBlock { Text = "Режим", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
                 var options = DlssPresetService.VSyncModeOptions;
                 uint current = d.VSyncMode;
                 var globalVSync = d.GlobalVSyncMode;
@@ -166,7 +166,7 @@ public partial class DetailPanelBuilder
                 var itemsList = new List<string>();
                 if (globalVSync.HasValue)
                 {
-                    var globalName = options.FirstOrDefault(o => o.Value == globalVSync.Value).Name ?? "App Controlled";
+                    var globalName = options.FirstOrDefault(o => o.Value == globalVSync.Value).Name ?? "Управляется игрой";
                     itemsList.Add($"Global ({globalName})");
                 }
                 itemsList.AddRange(options.Select(o => o.Name));
@@ -195,8 +195,8 @@ public partial class DetailPanelBuilder
                     CornerRadius = new CornerRadius(6),
                 };
                 ToolTipService.SetToolTip(combo, globalVSync.HasValue
-                    ? "Global = inherit from global setting. App Controlled: let the game decide. Force Off: disables VSync. Force On: locks to refresh rate. Fast Sync: renders freely, displays latest complete frame."
-                    : "VSync Mode — App Controlled: let the game decide. Force Off: disables VSync entirely. Force On: locks to refresh rate. Fast Sync: renders freely, displays latest complete frame.");
+                    ? "Глобально — наследовать общую настройку. Управляется игрой: решает сама игра. Принудительно выкл: отключает VSync. Принудительно вкл: привязка к частоте обновления. Fast Sync: рендер без ограничений, выводится последний завершённый кадр."
+                    : "Режим VSync — управление игрой: решает сама игра. Принудительно выкл: полностью отключает VSync. Принудительно вкл: привязка к частоте обновления. Fast Sync: рендер без ограничений, выводится последний завершённый кадр.");
                 var init = true;
                 combo.SelectionChanged += (s, ev) =>
                 {
@@ -226,7 +226,7 @@ public partial class DetailPanelBuilder
 
             // VSync Tear Control
             {
-                vsyncCol.Children.Add(new TextBlock { Text = "Tear Control", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
+                vsyncCol.Children.Add(new TextBlock { Text = "Управление разрывами", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
                 var options = DlssPresetService.VSyncTearControlOptions;
                 uint current = d.VSyncTearControl;
                 var items = options.Select(o => o.Name).ToArray();
@@ -240,7 +240,7 @@ public partial class DetailPanelBuilder
                     HorizontalAlignment = HorizontalAlignment.Stretch,
                     CornerRadius = new CornerRadius(6),
                 };
-                ToolTipService.SetToolTip(combo, "VSync Tear Control — Standard: normal VSync behavior. Adaptive: VSync on when FPS ≥ refresh rate, off when below (reduces stuttering at low FPS).");
+                ToolTipService.SetToolTip(combo, "Управление разрывами VSync — стандартно: обычное поведение VSync. Адаптивно: VSync включён при FPS ≥ частоты обновления, выключен ниже (меньше фризов при низком FPS).");
                 var init = true;
                 combo.SelectionChanged += (s, ev) =>
                 {
@@ -276,8 +276,8 @@ public partial class DetailPanelBuilder
                     Opacity = latencyLocked ? 0.4 : 1.0,
                 };
                 ToolTipService.SetToolTip(combo2, latencyLocked
-                    ? "Low Latency is locked to Ultra while Smooth Motion is enabled. Turn off Smooth Motion to change this setting."
-                    : "Low Latency Mode — Off: game controls frame queue. On: limits pre-rendered frames to 1 (lower latency). Ultra: just-in-time frame submission (lowest latency, may reduce FPS slightly).");
+                    ? "Пока включён Smooth Motion, Low Latency зафиксирован на Ultra. Выключите Smooth Motion, чтобы изменить настройку."
+                    : "Режим Low Latency — выкл: очередью кадров управляет игра. Вкл: не более 1 предрендеренного кадра (ниже задержка). Ultra: отправка кадров точно ко времени (минимальная задержка, может слегка снизить FPS).");
                 var init2 = true;
                 combo2.SelectionChanged += (s, ev) =>
                 {
@@ -298,13 +298,13 @@ public partial class DetailPanelBuilder
             // ── Column 4: Smooth Motion ──
             var smoothCol = new StackPanel { Spacing = 4 };
             var smoothLabel = new TextBlock { Text = "Smooth Motion", FontSize = 11, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) };
-            ToolTipService.SetToolTip(smoothLabel, "NVIDIA Smooth Motion — driver-level frame generation. Adds interpolated frames for smoother visuals. RTX 40 Series+ required.");
+            ToolTipService.SetToolTip(smoothLabel, "NVIDIA Smooth Motion — генерация кадров на уровне драйвера. Добавляет интерполированные кадры для более плавной картинки. Требуется RTX 40-й серии и новее.");
             smoothCol.Children.Add(smoothLabel);
 
             // Enable
             bool smoothMotionEnabled;
             {
-                smoothCol.Children.Add(new TextBlock { Text = "Enable", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
+                smoothCol.Children.Add(new TextBlock { Text = "Включить", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
                 var options = DlssPresetService.SmoothMotionEnableOptions;
                 uint current = d.SmoothMotionEnable;
                 smoothMotionEnabled = current != 0;
@@ -319,7 +319,7 @@ public partial class DetailPanelBuilder
                     HorizontalAlignment = HorizontalAlignment.Stretch,
                     CornerRadius = new CornerRadius(6),
                 };
-                ToolTipService.SetToolTip(combo, "Smooth Motion Enable — Off: disabled. On: enables driver-level frame generation (RTX 40 Series+ only).");
+                ToolTipService.SetToolTip(combo, "Включение Smooth Motion — выкл: отключено. Вкл: включает генерацию кадров на уровне драйвера (только RTX 40-й серии и новее).");
                 var init = true;
                 combo.SelectionChanged += (s, ev) =>
                 {
@@ -362,7 +362,7 @@ public partial class DetailPanelBuilder
 
             // APIs
             {
-                smoothCol.Children.Add(new TextBlock { Text = "Allowed APIs", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
+                smoothCol.Children.Add(new TextBlock { Text = "Разрешённые API", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
                 var options = DlssPresetService.SmoothMotionApisOptions;
                 uint current = d.SmoothMotionApis;
                 var items = options.Select(o => o.Name).ToArray();
@@ -378,7 +378,7 @@ public partial class DetailPanelBuilder
                     IsEnabled = smoothMotionEnabled,
                     Opacity = smoothMotionEnabled ? 1.0 : 0.4,
                 };
-                ToolTipService.SetToolTip(combo, "Smooth Motion APIs — which graphics APIs Smooth Motion is allowed to hook. None = disabled for all APIs.");
+                ToolTipService.SetToolTip(combo, "API Smooth Motion — какие графические API может подхватывать Smooth Motion. «Нет» — отключено для всех API.");
                 var init = true;
                 combo.SelectionChanged += (s, ev) =>
                 {
@@ -410,7 +410,7 @@ public partial class DetailPanelBuilder
                     IsEnabled = smoothMotionEnabled,
                     Opacity = smoothMotionEnabled ? 1.0 : 0.4,
                 };
-                ToolTipService.SetToolTip(combo, "Flip Pacing — Off: prioritize lower latency. On: prioritize smoother frame pacing. Sets both fullscreen and windowed modes together.");
+                ToolTipService.SetToolTip(combo, "Flip Pacing — выкл: приоритет низкой задержки. Вкл: приоритет плавности вывода кадров. Устанавливается сразу для полноэкранного и оконного режимов.");
                 var init = true;
                 combo.SelectionChanged += (s, ev) =>
                 {
@@ -436,13 +436,13 @@ public partial class DetailPanelBuilder
 
             // ── Column 6: Other (Power, G-Sync, Restore) ──
             var powerCol = new StackPanel { Spacing = 4 };
-            var powerLabel = new TextBlock { Text = "Other", FontSize = 11, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) };
-            ToolTipService.SetToolTip(powerLabel, "Power management, G-Sync control, and profile reset.");
+            var powerLabel = new TextBlock { Text = "Другое", FontSize = 11, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) };
+            ToolTipService.SetToolTip(powerLabel, "Управление питанием, контроль G-Sync и сброс профиля.");
             powerCol.Children.Add(powerLabel);
 
             // Power Management Mode
             {
-                powerCol.Children.Add(new TextBlock { Text = "Power Mode", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
+                powerCol.Children.Add(new TextBlock { Text = "Режим питания", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
                 var options = DlssPresetService.PowerManagementOptions;
                 uint current = d.PowerManagementMode;
                 var items = options.Select(o => o.Name).ToArray();
@@ -456,7 +456,7 @@ public partial class DetailPanelBuilder
                     HorizontalAlignment = HorizontalAlignment.Stretch,
                     CornerRadius = new CornerRadius(6),
                 };
-                ToolTipService.SetToolTip(combo, "Power Management — Adaptive: GPU clocks down at idle. Maximum: locks GPU to highest clocks. Optimal: balanced (NVIDIA recommended).");
+                ToolTipService.SetToolTip(combo, "Управление питанием — Adaptive: GPU снижает частоты в простое. Maximum: фиксирует максимальные частоты. Optimal: баланс (рекомендация NVIDIA).");
                 var init = true;
                 combo.SelectionChanged += (s, ev) =>
                 {
@@ -482,7 +482,7 @@ public partial class DetailPanelBuilder
                     HorizontalAlignment = HorizontalAlignment.Stretch,
                     CornerRadius = new CornerRadius(6),
                 };
-                ToolTipService.SetToolTip(gsyncCombo, "Per-game G-Sync control. Disabled forces G-Sync off for this game regardless of global setting.");
+                ToolTipService.SetToolTip(gsyncCombo, "Управление G-Sync для отдельной игры. «Отключено» принудительно выключает G-Sync для игры независимо от общей настройки.");
                 var gsyncInit = true;
                 gsyncCombo.SelectionChanged += (s, ev) =>
                 {
@@ -498,7 +498,7 @@ public partial class DetailPanelBuilder
             powerCol.Children.Add(new TextBlock { Text = " ", FontSize = 10, Margin = new Thickness(0, 2, 0, 0) });
             var restoreProfileBtn = new Button
             {
-                Content = "Restore Defaults",
+                Content = "Восстановить значения по умолчанию",
                 FontSize = 11,
                 Height = 32,
                 HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -510,21 +510,21 @@ public partial class DetailPanelBuilder
                 IsEnabled = nvidiaPresetService.IsSupported,
             };
             ToolTipService.SetToolTip(restoreProfileBtn,
-                "Restore this game's NVIDIA driver profile to factory defaults. Removes all custom settings (presets, render scale, MFG, driver overrides). This action is irreversible.");
+                "Вернуть профиль драйвера NVIDIA этой игры к заводским значениям. Удаляет все пользовательские настройки (пресеты, масштаб рендеринга, MFG, переопределения драйвера). Действие необратимо.");
             restoreProfileBtn.Click += async (s, ev) =>
             {
                 var xamlRoot = (s as FrameworkElement)?.XamlRoot ?? _window.Content.XamlRoot;
                 var warningDialog = new ContentDialog
                 {
-                    Title = "Restore driver settings?",
+                    Title = "Восстановить настройки драйвера?",
                     Content = new TextBlock
                     {
-                        Text = $"This will restore driver settings for {capturedName} back to the factory default and restore DLSS/Streamline DLLs to their original versions. This action is irreversible.",
+                        Text = $"Настройки драйвера для {capturedName} будут возвращены к заводским, а DLL DLSS/Streamline — к исходным версиям. Действие необратимо.",
                         TextWrapping = TextWrapping.Wrap,
                         FontSize = 13,
                     },
-                    PrimaryButtonText = "Restore",
-                    CloseButtonText = "Cancel",
+                    PrimaryButtonText = "Восстановить",
+                    CloseButtonText = "Отмена",
                     XamlRoot = xamlRoot,
                     RequestedTheme = ElementTheme.Dark,
                 };
@@ -558,7 +558,7 @@ public partial class DetailPanelBuilder
             // ── Column 8: ReBAR ──
             var rebarCol = new StackPanel { Spacing = 4 };
             var rebarLabel = new TextBlock { Text = "ReBAR", FontSize = 11, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) };
-            ToolTipService.SetToolTip(rebarLabel, "Resizable BAR — allows the CPU to access full GPU VRAM at once. Can improve performance by 5-10% in some titles. RTX 30+ and BIOS support required.");
+            ToolTipService.SetToolTip(rebarLabel, "Resizable BAR — позволяет CPU обращаться ко всей памяти GPU сразу. Может поднять производительность на 5–10% в отдельных играх. Нужны RTX 30+ и поддержка в BIOS.");
             rebarCol.Children.Add(rebarLabel);
 
             bool rebarEnabled = false; // set inside Enable block below
@@ -566,7 +566,7 @@ public partial class DetailPanelBuilder
 
             // Enable — Auto (Default) / Off / On using new 0x000BFA21 setting
             {
-                rebarCol.Children.Add(new TextBlock { Text = "Enable", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
+                rebarCol.Children.Add(new TextBlock { Text = "Включить", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
 
                 uint rebarEnableMode = d.ReBarEnableMode;
                 // 0=Off→index 1, 1=Auto→index 0, 2=On→index 2
@@ -574,13 +574,13 @@ public partial class DetailPanelBuilder
 
                 var rebarEnableCombo = new ComboBox
                 {
-                    ItemsSource = new[] { "Auto (Default)", "Off", "On" },
+                    ItemsSource = new[] { "Авто (по умолчанию)", "Off", "On" },
                     SelectedIndex = enableIdx,
                     FontSize = 11,
                     HorizontalAlignment = HorizontalAlignment.Stretch,
                     CornerRadius = new CornerRadius(6),
                 };
-                ToolTipService.SetToolTip(rebarEnableCombo, "Auto = driver decides. On = force-enable ReBAR. Off = force-disable ReBAR.");
+                ToolTipService.SetToolTip(rebarEnableCombo, "Авто — решает драйвер. Вкл — принудительно включить ReBAR. Выкл — принудительно выключить ReBAR.");
                 var rebarComboInit = true;
                 rebarEnableCombo.SelectionChanged += (s, ev) =>
                 {
@@ -599,7 +599,7 @@ public partial class DetailPanelBuilder
 
             // Mode
             {
-                rebarCol.Children.Add(new TextBlock { Text = "Mode", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
+                rebarCol.Children.Add(new TextBlock { Text = "Режим", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
                 uint rebarMode = d.ReBarMode;
                 var modeItems = DlssPresetService.ReBarModes.Select(m => m.Name).ToList();
 
@@ -617,7 +617,7 @@ public partial class DetailPanelBuilder
                     IsEnabled = rebarEnabled,
                     Opacity = rebarEnabled ? 1.0 : 0.4,
                 };
-                ToolTipService.SetToolTip(rebarModeCombo, "Standard = conservative. Optimized = aggressive driver scheduling (used by NVIDIA-whitelisted titles).");
+                ToolTipService.SetToolTip(rebarModeCombo, "Standard — консервативно. Optimized — агрессивное планирование драйвера (используется для игр из белого списка NVIDIA).");
                 var modeComboInit = true;
                 rebarModeCombo.SelectionChanged += (s, ev) =>
                 {
@@ -633,7 +633,7 @@ public partial class DetailPanelBuilder
 
             // Size Limit — always shows actual size values (no Global option)
             {
-                rebarCol.Children.Add(new TextBlock { Text = "Size Limit", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
+                rebarCol.Children.Add(new TextBlock { Text = "Лимит размера", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0) });
 
                 var sizeItems = new List<string>();
                 var sizeValues = new List<ulong>();
@@ -660,7 +660,7 @@ public partial class DetailPanelBuilder
                     IsEnabled = rebarEnabled,
                     Opacity = rebarEnabled ? 1.0 : 0.4,
                 };
-                ToolTipService.SetToolTip(rebarSizeCombo, "1GB is optimal for most games. Decrease to 512MB if experiencing ReBAR-related stutters.");
+                ToolTipService.SetToolTip(rebarSizeCombo, "1 ГБ оптимален для большинства игр. Уменьшите до 512 МБ, если возникают фризы, связанные с ReBAR.");
                 var sizeComboInit = true;
                 rebarSizeCombo.SelectionChanged += (s, ev) =>
                 {
@@ -686,8 +686,8 @@ public partial class DetailPanelBuilder
         (nvBody ?? _window.NvidiaProfilePanel).Children.Add(new TextBlock
         {
             Text = isElevated
-                ? "✓ Running as admin — all driver profile settings are writable."
-                : "⚠ Admin rights required to write driver profile settings. Enable Admin Mode in Settings or restart as admin.",
+                ? "✓ Запущено от администратора — все настройки профиля доступны для записи."
+                : "⚠ Для записи настроек профиля драйвера нужны права администратора. Включите режим администратора в настройках или перезапустите RHI от его имени.",
             FontSize = 10,
             Foreground = UIFactory.Brush(isElevated ? ResourceKeys.TextTertiaryBrush : ResourceKeys.AccentAmberDimBrush),
             TextWrapping = TextWrapping.Wrap,
diff --git a/RenoDXCommander/DetailPanelBuilder.Overrides.Dxvk.cs b/RenoDXCommander/DetailPanelBuilder.Overrides.Dxvk.cs
index 95c1f0f..46a55a0 100644
--- a/RenoDXCommander/DetailPanelBuilder.Overrides.Dxvk.cs
+++ b/RenoDXCommander/DetailPanelBuilder.Overrides.Dxvk.cs
@@ -36,7 +36,7 @@ public partial class DetailPanelBuilder
         };
         var mgmtTitle = new TextBlock
         {
-            Text       = "Management",
+            Text       = "Управление",
             FontSize   = 13,
             FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
@@ -80,7 +80,7 @@ public partial class DetailPanelBuilder
 
         var changeFolderBtn = new Button
         {
-            Content = "Change install folder",
+            Content = "Изменить папку установки",
             FontSize = 11,
             Height = 32,
             HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -92,7 +92,7 @@ public partial class DetailPanelBuilder
             Tag = card,
         };
         changeFolderBtn.Click += (s, ev) => _window.BrowseFolder_Click(s, ev);
-        ToolTipService.SetToolTip(changeFolderBtn, "Change the install folder for this game. Use when auto-detection picked the wrong directory.");
+        ToolTipService.SetToolTip(changeFolderBtn, "Изменить папку установки для этой игры. Пригодится, если автоопределение выбрало неверный каталог.");
         Grid.SetColumn(changeFolderBtn, 0);
         mgmtRow.Children.Add(changeFolderBtn);
 
@@ -102,7 +102,7 @@ public partial class DetailPanelBuilder
 
         var removeGameBtn = new Button
         {
-            Content = "Reset / Remove game",
+            Content = "Сбросить / удалить игру",
             FontSize = 11,
             Height = 32,
             HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -114,7 +114,7 @@ public partial class DetailPanelBuilder
             Tag = card,
         };
         removeGameBtn.Click += (s, ev) => _window.RemoveManualGame_Click(s, ev);
-        ToolTipService.SetToolTip(removeGameBtn, "Reset the install folder to auto-detected, or remove a manually added game entirely.");
+        ToolTipService.SetToolTip(removeGameBtn, "Вернуть папку установки к автоопределённой или полностью удалить вручную добавленную игру.");
         Grid.SetColumn(removeGameBtn, 2);
         mgmtRow.Children.Add(removeGameBtn);
 
@@ -124,7 +124,7 @@ public partial class DetailPanelBuilder
 
         var mgmtResetOverridesBtn = new Button
         {
-            Content = "Reset Overrides",
+            Content = "Сбросить переопределения",
             FontSize = 11,
             Height = 32,
             HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -140,7 +140,7 @@ public partial class DetailPanelBuilder
             ctx.ResetAction?.Invoke();
         };
         Grid.SetColumn(mgmtResetOverridesBtn, 4);
-        ToolTipService.SetToolTip(mgmtResetOverridesBtn, "Reset all per-game overrides back to defaults (DLL names, channels, shaders, addons, launch settings, update inclusion).");
+        ToolTipService.SetToolTip(mgmtResetOverridesBtn, "Сбросить все переопределения игр к значениям по умолчанию (имена DLL, каналы, шейдеры, аддоны, параметры запуска, обновления).");
         mgmtRow.Children.Add(mgmtResetOverridesBtn);
 
         var sep3 = new Border { Width = 1, Background = UIFactory.Brush(ResourceKeys.BorderDefaultBrush), Margin = new Thickness(8, 4, 8, 4) };
@@ -149,7 +149,7 @@ public partial class DetailPanelBuilder
 
         var reportBtn = new Button
         {
-            Content = "Copy Report",
+            Content = "Скопировать отчёт",
             FontSize = 11,
             Height = 32,
             HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -167,7 +167,7 @@ public partial class DetailPanelBuilder
                 await GameReportEncoder.ShowAndCopyAsync(_window.Content.XamlRoot, targetCard, _window.ViewModel);
         };
         Grid.SetColumn(reportBtn, 6);
-        ToolTipService.SetToolTip(reportBtn, "Copy a diagnostic report for this game to the clipboard. Useful for Discord or GitHub support.");
+        ToolTipService.SetToolTip(reportBtn, "Скопировать диагностический отчёт по этой игре в буфер обмена. Пригодится для поддержки в Discord или на GitHub.");
         mgmtRow.Children.Add(reportBtn);
 
         mgmtBody.Children.Add(mgmtRow);
diff --git a/RenoDXCommander/DetailPanelBuilder.Overrides.NvidiaProfile.cs b/RenoDXCommander/DetailPanelBuilder.Overrides.NvidiaProfile.cs
index 639284b..df34b92 100644
--- a/RenoDXCommander/DetailPanelBuilder.Overrides.NvidiaProfile.cs
+++ b/RenoDXCommander/DetailPanelBuilder.Overrides.NvidiaProfile.cs
@@ -24,7 +24,7 @@ public partial class DetailPanelBuilder
         var nvSettings   = _window.ViewModel.Settings;
         bool nvCollapsed = nvSettings.CollapsedDetailSections.Contains(nvSectionKey);
 
-        var nvidiaHeaderText = "Nvidia Profile Overrides";
+        var nvidiaHeaderText = "Переопределения профиля NVIDIA";
         var driverVer = _dlssPresetService.DriverVersionString;
         if (!string.IsNullOrEmpty(driverVer))
             nvidiaHeaderText += $" — Driver {driverVer}";
@@ -191,7 +191,7 @@ public partial class DetailPanelBuilder
             // Disable for DLSS 1.x (not compatible with 2.x+ versions in manifest)
             bool srEnabled = hasDlss && !(card.DlssInstalledVersion?.StartsWith("1.") == true);
             bool srDriverOverride = dlssData?.SrDriverOverride == true;
-            var srCol = BuildDlssColumn("DLSS Super Resolution", srEnabled, dlssService.DlssVersions,
+            var srCol = BuildDlssColumn("Суперразрешение DLSS (SR)", srEnabled, dlssService.DlssVersions,
                 card.DlssInstalledVersion, DlssPresetService.SrPresets,
                 presetService.IsSupported && srEnabled ? (dlssData?.SrPreset ?? 0u) : 0u,
                 async (version) =>
@@ -248,7 +248,7 @@ public partial class DetailPanelBuilder
             // FG column — no v1.x guard (FG can be updated from v1.0.0 to newer versions)
             bool fgEnabled = hasDlssg;
             bool fgDriverOverride = dlssData?.FgDriverOverride == true;
-            var fgCol = BuildDlssColumn("Frame Generation", fgEnabled, dlssService.DlssgVersions,
+            var fgCol = BuildDlssColumn("Генерация кадров", fgEnabled, dlssService.DlssgVersions,
                 card.DlssgInstalledVersion, DlssPresetService.FgPresets,
                 presetService.IsSupported && fgEnabled ? (dlssData?.FgPreset ?? 0u) : 0u,
                 async (version) =>
@@ -272,7 +272,7 @@ public partial class DetailPanelBuilder
             fgCol.Children.Add(new TextBlock { Text = " ", FontSize = 10, Margin = new Thickness(0, 2, 0, 0) });
             var mfgBtn = new Button
             {
-                Content = "Multi Frame Gen",
+                Content = "Мульти-генерация кадров",
                 FontSize = 11,
                 Height = 32,
                 HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -284,7 +284,7 @@ public partial class DetailPanelBuilder
                 IsEnabled = fgEnabled && presetService.IsSupported,
                 Opacity = (fgEnabled && presetService.IsSupported) ? 1.0 : 0.4,
             };
-            ToolTipService.SetToolTip(mfgBtn, "Configure NVIDIA Multi Frame Generation: mode, frame count multiplier, and dynamic target frame rate. Requires 50 Series GPU.");
+            ToolTipService.SetToolTip(mfgBtn, "Настройка мульти-генерации кадров NVIDIA (MFG): режим, множитель кадров и динамическая целевая частота. Требуется GPU серии 50.");
             mfgBtn.Click += async (s, ev) =>
             {
                 var xamlRoot = (s as FrameworkElement)?.XamlRoot ?? _window.Content.XamlRoot;
@@ -327,7 +327,7 @@ public partial class DetailPanelBuilder
                 string nrSelectedVersion = nrInstalledVersion ?? "";
 
                 bool nrDriverOverride = dlssData?.NrDriverOverride == true;
-                var nrCol = BuildDlssColumn("Neural Rendering", hasDlssnr, dlssService.DlssnrVersions,
+                var nrCol = BuildDlssColumn("Нейронный рендеринг", hasDlssnr, dlssService.DlssnrVersions,
                     nrInstalledVersion, DlssPresetService.NrPresets,
                     presetService.IsSupported && hasDlssnr ? (dlssData?.NrPreset ?? 0u) : 0u,
                     async (version) =>
@@ -361,7 +361,7 @@ public partial class DetailPanelBuilder
                 // When NR is not installed the placeholder is invisible but still takes space.
                 if (!hasDlssnr)
                 {
-                    var presetPlaceholderLabel = new TextBlock { Text = "Preset", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0), Opacity = 0 };
+                    var presetPlaceholderLabel = new TextBlock { Text = "Пресет", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Thickness(0, 2, 0, 0), Opacity = 0 };
                     var presetPlaceholderCombo = new ComboBox { ItemsSource = new[] { "Default" }, SelectedIndex = 0, FontSize = 11, HorizontalAlignment = HorizontalAlignment.Stretch, IsEnabled = false, Opacity = 0 };
                     nrCol.Children.Add(presetPlaceholderLabel);
                     nrCol.Children.Add(presetPlaceholderCombo);
@@ -373,7 +373,7 @@ public partial class DetailPanelBuilder
 
                 var deployNrBtn = new Button
                 {
-                    Content = "Deploy DLL",
+                    Content = "Развернуть DLL",
                     FontSize = 11,
                     Height = 32,
                     HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -433,7 +433,7 @@ public partial class DetailPanelBuilder
                                     "RHI", "Custom", "DLSS", "nvngx_dlssnr.dll");
                                 if (!File.Exists(customSrc))
                                 {
-                                    _window.DispatcherQueue?.TryEnqueue(() => { deployNrBtn.Content = "Not in Custom/DLSS"; deployNrBtn.IsEnabled = true; });
+                                    _window.DispatcherQueue?.TryEnqueue(() => { deployNrBtn.Content = "Не в Custom/DLSS"; deployNrBtn.IsEnabled = true; });
                                     return;
                                 }
                                 File.Copy(customSrc, destPath, overwrite: true);
@@ -464,7 +464,7 @@ public partial class DetailPanelBuilder
                             }
                             else if (cachedPath == null)
                             {
-                                _window.DispatcherQueue?.TryEnqueue(() => { deployNrBtn.Content = "Not available"; deployNrBtn.IsEnabled = true; });
+                                _window.DispatcherQueue?.TryEnqueue(() => { deployNrBtn.Content = "Недоступно"; deployNrBtn.IsEnabled = true; });
                                 return;
                             }
                             else if (isDefault)
@@ -513,7 +513,7 @@ public partial class DetailPanelBuilder
                     catch (Exception ex)
                     {
                         CrashReporter.Log($"[NrDeployBtn] Failed — {ex.Message}");
-                        _window.DispatcherQueue?.TryEnqueue(() => { deployNrBtn.Content = "Deploy DLL"; deployNrBtn.IsEnabled = true; });
+                        _window.DispatcherQueue?.TryEnqueue(() => { deployNrBtn.Content = "Развернуть DLL"; deployNrBtn.IsEnabled = true; });
                     }
                 };
 
@@ -609,7 +609,7 @@ public partial class DetailPanelBuilder
             bool restoreEnabled = card.HasAnyDlssBackup || hasNonDefaultPreset;
             var dlssRestoreBtn = new Button
             {
-                Content = "Restore DLSS/SL",
+                Content = "Восстановить DLSS/SL",
                 FontSize = 11,
                 Height = 32,
                 HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -672,7 +672,7 @@ public partial class DetailPanelBuilder
 
             var applyBtn = new Button
             {
-                Content = "Quick Apply",
+                Content = "Быстрое применение",
                 FontSize = 11,
                 Height = 32,
                 HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -683,7 +683,7 @@ public partial class DetailPanelBuilder
                 CornerRadius = new CornerRadius(8),
                 IsEnabled = hasDefaults && card.HasAnyDlssStreamline,
             };
-            ToolTipService.SetToolTip(applyBtn, "Apply your configured DLSS/Streamline default versions, presets, and render scales to this game. Downloads versions on-demand if not cached.");
+            ToolTipService.SetToolTip(applyBtn, "Применить к этой игре настроенные версии, пресеты и масштабы рендеринга DLSS/Streamline по умолчанию. Версии скачиваются по мере необходимости, если их нет в кеше.");
             applyBtn.Click += async (s, ev) =>
             {
                 var targetCard = _window.ViewModel.AllCards.FirstOrDefault(c =>
@@ -786,7 +786,7 @@ public partial class DetailPanelBuilder
                         child.Opacity = 0.4;
                 }
             }
-            ToolTipService.SetToolTip(dlssRestoreBtn, "Restore all DLSS and Streamline DLLs to their original game versions and reset presets to Default.");
+            ToolTipService.SetToolTip(dlssRestoreBtn, "Вернуть все DLL DLSS и Streamline к исходным версиям игры и сбросить пресеты на «По умолчанию».");
 
             Grid.SetColumn(slCol, slColumn);
             dlssRowGrid.Children.Add(slCol);
diff --git a/RenoDXCommander/DetailPanelBuilder.Overrides.RsChannel.cs b/RenoDXCommander/DetailPanelBuilder.Overrides.RsChannel.cs
index 4a4ea4f..fe0ff28 100644
--- a/RenoDXCommander/DetailPanelBuilder.Overrides.RsChannel.cs
+++ b/RenoDXCommander/DetailPanelBuilder.Overrides.RsChannel.cs
@@ -23,14 +23,14 @@ public partial class DetailPanelBuilder
 
         var channelLabel = new TextBlock
         {
-            Text = "ReShade Channel",
+            Text = "Канал ReShade",
             FontSize = 12,
             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
         };
         ToolTipService.SetToolTip(channelLabel,
-            "Override the global ReShade build channel for this game.\nVulkan games: changing this affects ALL Vulkan games.");
+            "Переопределить общий канал сборки ReShade для этой игры.\nVulkan-игры: изменение затронет ВСЕ Vulkan-игры.");
 
-        var channelItems = new[] { "Stable", "Nightly", "Custom", "No Addons", "Legacy..." };
+        var channelItems = new[] { "Stable", "Nightly", "Custom", "Без аддонов", "Legacy..." };
         // For Vulkan games, show the effective Vulkan-wide override (any Vulkan game's override applies to all)
         var currentChannelOverride = _window.ViewModel.GetReShadeChannelOverride(gameName, card.Source);
         if (currentChannelOverride == null && card.RequiresVulkanInstall)
@@ -46,7 +46,7 @@ public partial class DetailPanelBuilder
         string defaultChannelSelection;
         if (card.UseNormalReShade)
         {
-            defaultChannelSelection = "No Addons";
+            defaultChannelSelection = "Без аддонов";
         }
         else if (string.Equals(currentChannelOverride, "Custom", StringComparison.OrdinalIgnoreCase))
         {
@@ -74,7 +74,7 @@ public partial class DetailPanelBuilder
             HorizontalAlignment = HorizontalAlignment.Stretch,
         };
         ToolTipService.SetToolTip(channelCombo,
-            "Override the ReShade build channel for this game.\nVulkan games: changing this affects ALL Vulkan games.");
+            "Переопределить канал сборки ReShade для этой игры.\nVulkan-игры: изменение затронет ВСЕ Vulkan-игры.");
 
         bool channelComboInitializing = true;
         ctx.ChannelComboInitializing = true;
@@ -117,7 +117,7 @@ public partial class DetailPanelBuilder
                 var pickerContent = new StackPanel { Spacing = 12 };
                 pickerContent.Children.Add(new TextBlock
                 {
-                    Text = "⚠ Older ReShade versions may not support newer addons.\nThe game will be excluded from automatic ReShade updates.",
+                    Text = "⚠ Старые версии ReShade могут не поддерживать новые аддоны.\nИгра будет исключена из автоматических обновлений ReShade.",
                     TextWrapping = TextWrapping.Wrap,
                     Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
                     FontSize = 12,
@@ -126,10 +126,10 @@ public partial class DetailPanelBuilder
 
                 var pickerDialog = new ContentDialog
                 {
-                    Title = "Select Legacy ReShade Version",
+                    Title = "Выберите устаревшую версию ReShade",
                     Content = new ScrollViewer { Content = pickerContent, MaxHeight = 400 },
-                    PrimaryButtonText = "Confirm",
-                    CloseButtonText = "Cancel",
+                    PrimaryButtonText = "Подтвердить",
+                    CloseButtonText = "Отмена",
                     XamlRoot = _window.Content.XamlRoot,
                     RequestedTheme = ElementTheme.Dark,
                 };
@@ -185,7 +185,7 @@ public partial class DetailPanelBuilder
 
             // ── If a legacy version is already selected and user picks it again, do nothing ──
             if (selected != "Global" && selected != "Stable" && selected != "Nightly"
-                && selected != "No Addons" && selected != "Legacy..." && selected != "Custom"
+                && selected != "Без аддонов" && selected != "Legacy..." && selected != "Custom"
                 && MainViewModel.IsLegacyVersion(selected))
             {
                 return;
@@ -233,7 +233,7 @@ public partial class DetailPanelBuilder
 
                     var warnDialog = new ContentDialog
                     {
-                        Title = "Custom ReShade Not Found",
+                        Title = "Пользовательский ReShade не найден",
                         Content = warnContent,
                         CloseButtonText = "OK",
                         XamlRoot = _window.Content.XamlRoot,
@@ -286,9 +286,9 @@ public partial class DetailPanelBuilder
 
                 var pickerDialog = new ContentDialog
                 {
-                    Title = "Select Custom ReShade",
-                    PrimaryButtonText = "Deploy",
-                    CloseButtonText = "Cancel",
+                    Title = "Выбрать свой ReShade",
+                    PrimaryButtonText = "Развернуть",
+                    CloseButtonText = "Отмена",
                     DefaultButton = ContentDialogButton.Primary,
                     XamlRoot = _window.Content.XamlRoot,
                     RequestedTheme = ElementTheme.Dark,
@@ -323,11 +323,11 @@ public partial class DetailPanelBuilder
                 {
                     var vDialog = new ContentDialog
                     {
-                        Title = "Vulkan ReShade Channel Override",
-                        Content = "Vulkan games share a global ReShade layer.\n\n" +
-                            "Changing the channel for this game will change it for ALL Vulkan games.",
-                        PrimaryButtonText = "Apply to All Vulkan Games",
-                        CloseButtonText = "Cancel",
+                        Title = "Переопределение канала Vulkan ReShade",
+                        Content = "Vulkan-игры используют общий слой ReShade.\n\n" +
+                            "Смена канала для этой игры изменит его для ВСЕХ Vulkan-игр.",
+                        PrimaryButtonText = "Применить ко всем Vulkan-играм",
+                        CloseButtonText = "Отмена",
                         XamlRoot = _window.Content.XamlRoot,
                         RequestedTheme = ElementTheme.Dark,
                     };
@@ -390,7 +390,7 @@ public partial class DetailPanelBuilder
             }
 
             // ── "No Addons" — switch to normal (non-addon) ReShade ──
-            if (selected == "No Addons")
+            if (selected == "Без аддонов")
             {
                 CrashReporter.Log($"[DetailPanelBuilder.RSChannel] '{ctx.CapturedName}' → No Addons mode");
                 var targetCardNoAddon = _window.ViewModel.AllCards.FirstOrDefault(c =>
@@ -404,7 +404,7 @@ public partial class DetailPanelBuilder
                     if (targetCardNoAddon.RsStatus == GameStatus.NotInstalled || targetCardNoAddon.IsRsInstalled)
                         await _window.ViewModel.InstallReShadeCommand.ExecuteAsync(targetCardNoAddon);
                 }
-                defaultChannelSelection = "No Addons";
+                defaultChannelSelection = "Без аддонов";
                 // Rebuild to grey out addon controls
                 var refreshCardNoAddon = _window.ViewModel.AllCards.FirstOrDefault(c =>
                     c.GameName.Equals(ctx.CapturedName, StringComparison.OrdinalIgnoreCase));
@@ -442,11 +442,11 @@ public partial class DetailPanelBuilder
                     // Setting a specific override on a Vulkan game
                     var dialog = new ContentDialog
                     {
-                        Title = "Vulkan ReShade Channel Override",
-                        Content = "Vulkan games share a global ReShade layer.\n\n" +
-                            "Changing the channel for this game will change it for ALL Vulkan games.",
-                        PrimaryButtonText = "Apply to All Vulkan Games",
-                        CloseButtonText = "Cancel",
+                        Title = "Переопределение канала Vulkan ReShade",
+                        Content = "Vulkan-игры используют общий слой ReShade.\n\n" +
+                            "Смена канала для этой игры изменит его для ВСЕХ Vulkan-игр.",
+                        PrimaryButtonText = "Применить ко всем Vulkan-играм",
+                        CloseButtonText = "Отмена",
                         XamlRoot = _window.Content.XamlRoot,
                         RequestedTheme = ElementTheme.Dark,
                     };
@@ -552,7 +552,7 @@ public partial class DetailPanelBuilder
         ctx.UpdateSummaryText = updateSummaryText; // assign for reset action to use
 
         var toggleRow = new StackPanel { Spacing = 0 };
-        ToolTipService.SetToolTip(updateInclusionBtn, "Choose which components are included in Update All for this game.");
+        ToolTipService.SetToolTip(updateInclusionBtn, "Выберите, какие компоненты включать в «Обновить всё» для этой игры.");
         toggleRow.Children.Add(updateInclusionBtn);
         toggleRow.Children.Add(updateSummaryText);
 
@@ -560,7 +560,7 @@ public partial class DetailPanelBuilder
         var globalUpdateColumn = new StackPanel { Spacing = 0 };
         globalUpdateColumn.Children.Add(new TextBlock
         {
-            Text = "Global update inclusion",
+            Text = "Включение в глобальное обновление",
             FontSize = 12,
             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
             Margin = new Thickness(0, 0, 0, 8),
diff --git a/RenoDXCommander/DetailPanelBuilder.Overrides.ShadersAddons.cs b/RenoDXCommander/DetailPanelBuilder.Overrides.ShadersAddons.cs
index bdb3a32..c2cde25 100644
--- a/RenoDXCommander/DetailPanelBuilder.Overrides.ShadersAddons.cs
+++ b/RenoDXCommander/DetailPanelBuilder.Overrides.ShadersAddons.cs
@@ -27,7 +27,7 @@ public partial class DetailPanelBuilder
         var shadersAddonsLeftColumn = new StackPanel { Spacing = 6 };
         shadersAddonsLeftColumn.Children.Add(new TextBlock
         {
-            Text = "Shaders and Addons",
+            Text = "Шейдеры и аддоны",
             FontSize = 12,
             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
             Margin = new Thickness(0, 0, 0, 4),
@@ -42,7 +42,7 @@ public partial class DetailPanelBuilder
 
         var shaderLabel = new TextBlock
         {
-            Text = "Shaders",
+            Text = "Шейдеры",
             FontSize = 11,
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
         };
@@ -65,7 +65,7 @@ public partial class DetailPanelBuilder
             IsEnabled = !card.UseNormalReShade,
         };
         ToolTipService.SetToolTip(addonModeCombo,
-            "Global = use global addon set. Select = pick per-game addons. Off = no addons for this game.");
+            "Глобально — использовать общий набор аддонов. Выбрать — задать аддоны для этой игры. Выкл — без аддонов.");
 
         // Allow re-opening the Select picker when already on Select
         addonModeCombo.DropDownClosed += (s, ev) =>
@@ -105,10 +105,10 @@ public partial class DetailPanelBuilder
                 {
                     var infoDlg = new ContentDialog
                     {
-                        Title = "Select Addons",
+                        Title = "Выбрать аддоны",
                         Content = new TextBlock
                         {
-                            Text = "Addon service is not yet wired. Complete Task 9.1 to enable addon selection.",
+                            Text = "Сервис аддонов ещё не подключен. Завершите задачу 9.1, чтобы включить выбор аддонов.",
                             FontSize = 13,
                             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
                         },
@@ -164,7 +164,7 @@ public partial class DetailPanelBuilder
 
         var addonLabel = new TextBlock
         {
-            Text = "Addons",
+            Text = "Аддоны",
             FontSize = 11,
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
         };
@@ -178,7 +178,7 @@ public partial class DetailPanelBuilder
         // "Select ReShade Preset" button
         var presetBtn = new Button
         {
-            Content = "Select ReShade Preset",
+            Content = "Выбрать пресет ReShade",
             FontSize = 12,
             Height = 32,
             HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -207,10 +207,10 @@ public partial class DetailPanelBuilder
                     {
                         var shaderDialog = new ContentDialog
                         {
-                            Title = "🔧 Install Shaders?",
-                            Content = "Also install the required shaders and textures?",
-                            PrimaryButtonText = "Yes",
-                            CloseButtonText = "No",
+                            Title = "🔧 Установить шейдеры?",
+                            Content = "Также установить необходимые шейдеры и текстуры?",
+                            PrimaryButtonText = "Да",
+                            CloseButtonText = "Нет",
                             XamlRoot = _window.Content.XamlRoot,
                             RequestedTheme = ElementTheme.Dark,
                         };
@@ -271,8 +271,8 @@ public partial class DetailPanelBuilder
                 : null); // exe scan deferred — label updates asynchronously
 
         var headerText = string.IsNullOrEmpty(effectiveExe)
-            ? "Launch executable"
-            : $"Launch executable  —  {effectiveExe}";
+            ? "Запускаемый файл"
+            : $"Запускаемый файл  —  {effectiveExe}";
         var exeHeaderText = new TextBlock
         {
             Text = headerText,
@@ -303,7 +303,7 @@ public partial class DetailPanelBuilder
                     _window.DispatcherQueue?.TryEnqueue(() =>
                     {
                         if (_window.ViewModel.SelectedGame == card)
-                            exeHeaderText.Text = $"Launch executable  —  {scannedExe}";
+                            exeHeaderText.Text = $"Запускаемый файл  —  {scannedExe}";
                     });
             });
         }
@@ -320,12 +320,12 @@ public partial class DetailPanelBuilder
         var launchExeBox = new TextBox
         {
             Text = currentLaunchExe,
-            PlaceholderText = "Auto-detect (or paste path)",
+            PlaceholderText = "Автоопределение (или вставьте путь)",
             FontSize = 11,
             HorizontalAlignment = HorizontalAlignment.Stretch,
         };
         ToolTipService.SetToolTip(launchExeBox,
-            "Override the executable used when launching this game. Leave blank for auto-detection (largest exe in install folder).");
+            "Переопределить исполняемый файл для запуска этой игры. Оставьте пустым для автоопределения (наибольший exe в папке установки).");
         launchExeBox.LostFocus += (s, ev) =>
         {
             var newPath = launchExeBox.Text.Trim();
@@ -341,13 +341,13 @@ public partial class DetailPanelBuilder
         var launchArgsBox = new TextBox
         {
             Text = currentLaunchArgs,
-            PlaceholderText = "Launch arguments",
+            PlaceholderText = "Аргументы запуска",
             FontSize = 11,
             HorizontalAlignment = HorizontalAlignment.Stretch,
         };
-        var argsTooltip = "Command-line arguments passed to the game on launch. Saves on focus lost.";
+        var argsTooltip = "Аргументы командной строки, передаваемые игре при запуске. Сохраняются при потере фокуса.";
         if (card.Source.Equals("Epic", StringComparison.OrdinalIgnoreCase))
-            argsTooltip += "\n\nNote: Setting arguments disables Epic protocol launch. EOS-protected games may fail to launch with arguments.";
+            argsTooltip += "\n\nПримечание: задание аргументов отключает запуск по протоколу Epic. Игры с защитой EOS могут не запуститься с аргументами.";
         ToolTipService.SetToolTip(launchArgsBox, argsTooltip);
         launchArgsBox.LostFocus += (s, ev) =>
         {
@@ -375,7 +375,7 @@ public partial class DetailPanelBuilder
         launchBtnRow.ColumnDefinitions.Add(new ColumnDefinition { Width = new GridLength(1, GridUnitType.Star) });
         var browseLaunchBtn = new Button
         {
-            Content = "Browse",
+            Content = "Обзор",
             FontSize = 12,
             Height = 32,
             HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -412,12 +412,12 @@ public partial class DetailPanelBuilder
             }
         };
         Grid.SetColumn(browseLaunchBtn, 0);
-        ToolTipService.SetToolTip(browseLaunchBtn, "Browse for a game executable to use as the launch target.");
+        ToolTipService.SetToolTip(browseLaunchBtn, "Выберите исполняемый файл игры для запуска.");
         launchBtnRow.Children.Add(browseLaunchBtn);
 
         var resetLaunchBtn = new Button
         {
-            Content = "Reset",
+            Content = "Сбросить",
             FontSize = 12,
             Height = 32,
             HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -434,7 +434,7 @@ public partial class DetailPanelBuilder
             _window.ViewModel.SaveSettingsPublic();
         };
         Grid.SetColumn(resetLaunchBtn, 1);
-        ToolTipService.SetToolTip(resetLaunchBtn, "Clear the launch executable override and revert to auto-detection.");
+        ToolTipService.SetToolTip(resetLaunchBtn, "Сбросить переопределение запускаемого exe и вернуться к автоопределению.");
         launchBtnRow.Children.Add(resetLaunchBtn);
         Grid.SetRow(launchBtnRow, 2);
         shadersAddonsRightColumn.Children.Add(launchBtnRow);
@@ -446,7 +446,7 @@ public partial class DetailPanelBuilder
 
         var resetOverridesBtn = new Button
         {
-            Content = "Reset Overrides",
+            Content = "Сбросить переопределения",
             FontSize = 12,
             Height = 32,
             HorizontalAlignment = HorizontalAlignment.Stretch,
diff --git a/RenoDXCommander/DetailPanelBuilder.Overrides.cs b/RenoDXCommander/DetailPanelBuilder.Overrides.cs
index aee35ad..de131f6 100644
--- a/RenoDXCommander/DetailPanelBuilder.Overrides.cs
+++ b/RenoDXCommander/DetailPanelBuilder.Overrides.cs
@@ -67,7 +67,7 @@ public partial class DetailPanelBuilder
         };
         var ovTitle = new TextBlock
         {
-            Text       = "Game Overrides",
+            Text       = "Переопределения игры",
             FontSize   = 13,
             FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
@@ -115,23 +115,23 @@ public partial class DetailPanelBuilder
         // ── Game name + Wiki name ────────────────────────────────────────────────
         var detectedBox = new TextBox
         {
-            Header = "Game name (editable)",
+            Header = "Название игры (можно изменить)",
             Text = gameName,
             FontSize = 12,
             HorizontalAlignment = HorizontalAlignment.Stretch,
         };
         ToolTipService.SetToolTip(detectedBox,
-            "The display name for this game. Edit and press Enter to rename. Reset reverts to the auto-detected store name.");
+            "Отображаемое название игры. Измените и нажмите Enter. «Сбросить» вернёт автоопределённое название из магазина.");
         var wikiBox = new TextBox
         {
-            Header = "Wiki mod name",
-            PlaceholderText = "Exact wiki name",
+            Header = "Имя мода в вики",
+            PlaceholderText = "Точное имя в вики",
             Text = _window.ViewModel.GetUserNameMapping(gameName) ?? "",
             FontSize = 12,
             HorizontalAlignment = HorizontalAlignment.Stretch,
         };
         ToolTipService.SetToolTip(wikiBox,
-            "Override the name used to look up this game on the RenoDX/Luma wiki. Leave blank to use the game name. Press Enter to save.");
+            "Переопределить имя для поиска этой игры в вики RenoDX/Luma. Пусто — использовать название игры. Enter — сохранить.");
         var originalStoreName = _window.ViewModel.GetOriginalStoreName(gameName);
 
         // Mutable captured name so rename handler can update it for subsequent handlers
@@ -139,7 +139,7 @@ public partial class DetailPanelBuilder
 
         var resetBtn = new Button
         {
-            Content = "Reset",
+            Content = "Сбросить",
             FontSize = 12,
             VerticalAlignment = VerticalAlignment.Bottom,
             Padding = new Thickness(10, 6, 10, 6),
@@ -147,7 +147,7 @@ public partial class DetailPanelBuilder
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             BorderBrush = UIFactory.Brush(ResourceKeys.BorderDefaultBrush),
         };
-        ToolTipService.SetToolTip(resetBtn, "Reset game name back to auto-detected and clear wiki name mapping.");
+        ToolTipService.SetToolTip(resetBtn, "Вернуть название игры к автоопределённому и очистить привязку к имени в вики.");
         resetBtn.Click += (s, ev) =>
         {
             var resetName = (originalStoreName ?? gameName).Trim();
@@ -175,21 +175,21 @@ public partial class DetailPanelBuilder
 
         var dllOverrideToggle = new ToggleSwitch
         {
-            Header = "DLL naming overrides",
+            Header = "Переопределение имён DLL",
             IsOn = isDllOverride,
             IsEnabled = true,
-            OnContent = "Custom filenames enabled",
-            OffContent = "Override DLL filenames",
+            OnContent = "Своё имя файла включено",
+            OffContent = "Переопределить имена DLL",
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             FontSize = 12,
         };
         ToolTipService.SetToolTip(dllOverrideToggle,
-            "Override the filenames ReShade is installed as. When enabled, existing RS files are renamed to the custom filenames.");
+            "Переопределить имена файлов, под которыми ставится ReShade. При включении существующие файлы RS переименовываются в пользовательские имена.");
         var existingRsName = existingCfg?.ReShadeFileName ?? "";
 
         var rsNameBox = new ComboBox
         {
-            PlaceholderText = "Select ReShade DLL name",
+            PlaceholderText = "Выберите имя DLL для ReShade",
             Header = (object?)null,
             FontSize = 12,
             IsEnabled = isDllOverride,
@@ -199,7 +199,7 @@ public partial class DetailPanelBuilder
         if (card.IsOsInstalled)
         {
             ToolTipService.SetToolTip(rsNameBox,
-                "Override the ReShade DLL name. OptiScaler is installed — RHI will rename ReShade to this filename after OptiScaler is uninstalled.");
+                "Переопределить имя DLL ReShade. Установлен OptiScaler — после его удаления RHI переименует ReShade в это имя.");
         }
         if (!string.IsNullOrEmpty(existingRsName))
         {
@@ -220,7 +220,7 @@ public partial class DetailPanelBuilder
 
         var dcNameBox = new ComboBox
         {
-            PlaceholderText = "Select DC DLL name",
+            PlaceholderText = "Выберите имя DLL для DC",
             FontSize = 12,
             IsEnabled = isDcDllOverrideOn,
             HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -259,7 +259,7 @@ public partial class DetailPanelBuilder
 
         var osNameBox = new ComboBox
         {
-            PlaceholderText = "Select OptiScaler DLL name",
+            PlaceholderText = "Выберите имя DLL для OptiScaler",
             FontSize = 12,
             IsEnabled = isDllOverride,
             HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -579,12 +579,12 @@ public partial class DetailPanelBuilder
                 else if (!result.DcReverted)
                 {
                     ToolTipService.SetToolTip(dllOverrideToggle,
-                        "Could not revert Display Commander to its default name — the filename is occupied by another file. DC was kept under its current name.");
+                        "Не удалось вернуть Display Commander имя по умолчанию — это имя занято другим файлом. DC оставлен под текущим именем.");
                 }
                 else
                 {
                     ToolTipService.SetToolTip(dllOverrideToggle,
-                        "Override the filenames ReShade is installed as. When enabled, existing RS files are renamed to the custom filenames.");
+                        "Переопределить имена файлов, под которыми ставится ReShade. При включении существующие файлы RS переименовываются в пользовательские имена.");
                 }
             } // end if/else dllOverrideToggle.IsOn
             } // end try body
@@ -685,7 +685,7 @@ public partial class DetailPanelBuilder
             HorizontalAlignment = HorizontalAlignment.Stretch,
         };
         ToolTipService.SetToolTip(wikiExcludeCombo,
-            "Included = this game is looked up on the RenoDX and Luma wikis. Excluded = skip wiki lookups for this game.");
+            "Включено — игра ищется в вики RenoDX и Luma. Исключено — не искать в вики для этой игры.");
         wikiExcludeCombo.SelectionChanged += (s, ev) =>
         {
             var selected = wikiExcludeCombo.SelectedItem as string;
@@ -816,7 +816,7 @@ public partial class DetailPanelBuilder
             IsEnabled = !card.UseNormalReShade,
         };
         ToolTipService.SetToolTip(shaderModeCombo,
-            "Global = use global shader selection. Custom = use custom shader directories. Select = pick per-game packs. Off = no shaders.");
+            "Глобально — использовать общий набор шейдеров. Свои — использовать свои каталоги шейдеров. Выбрать — задать наборы для этой игры. Выкл — без шейдеров.");
 
         // Allow re-opening the Select picker when already on Select
         shaderModeCombo.DropDownClosed += (s, ev) =>
@@ -923,7 +923,7 @@ public partial class DetailPanelBuilder
         // ── Bitness Override ComboBox (left column of Bitness & API Row) ─────────
         var bitnessLabel = new TextBlock
         {
-            Text = "Bitness",
+            Text = "Разрядность",
             FontSize = 12,
             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
             Margin = new Thickness(0, 0, 0, 8),
@@ -946,7 +946,7 @@ public partial class DetailPanelBuilder
             HorizontalAlignment = HorizontalAlignment.Stretch,
         };
         ToolTipService.SetToolTip(bitnessCombo,
-            "Override the auto-detected bitness for this game. Auto uses PE header detection. 32-bit or 64-bit forces the value.");
+            "Переопределить определённую разрядность этой игры. «Авто» определяет по PE-заголовку. «32-бит» или «64-бит» задают значение принудительно.");
 
         bitnessCombo.SelectionChanged += (s, e) =>
         {
@@ -1029,15 +1029,15 @@ public partial class DetailPanelBuilder
         // ── API Override ComboBox (single selection, placed in left panel below bitness) ──────
         var apiLabel = new TextBlock
         {
-            Text = "Graphics API",
+            Text = "Графический API",
             FontSize = 12,
             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
         };
         ToolTipService.SetToolTip(apiLabel,
-            "Override the detected graphics API for this game.\n\n" +
-            "Auto uses the auto-detected value from PE header scanning.\n" +
-            "User overrides set here take precedence over manifest and auto-detected values.\n" +
-            "Reset Overrides reverts to auto-detection.");
+            "Переопределить определённый графический API этой игры.\n\n" +
+            "Авто использует значение, определённое по PE-заголовку.\n" +
+            "Заданные здесь переопределения важнее манифеста и автоопределённых значений.\n" +
+            "«Сбросить переопределения» возвращает автоопределение.");
 
         var apiDropdownItems = new[] { "Auto", "DirectX8", "DirectX9", "DirectX10", "DirectX11", "DirectX12", "Vulkan", "OpenGL" };
         var existingApiOverride = _window.ViewModel.GetApiOverride(gameName, card.Source);
@@ -1071,7 +1071,7 @@ public partial class DetailPanelBuilder
             HorizontalAlignment = HorizontalAlignment.Stretch,
         };
         ToolTipService.SetToolTip(apiCombo,
-            "Override the detected graphics API for this game.\nAuto uses PE header scanning. Reset Overrides reverts to auto-detection.");
+            "Переопределить определённый графический API этой игры.\n«Авто» определяет по PE-заголовку. «Сбросить переопределения» возвращает автоопределение.");
 
         apiCombo.SelectionChanged += (s, ev) =>
         {
diff --git a/RenoDXCommander/DetailPanelBuilder.cs b/RenoDXCommander/DetailPanelBuilder.cs
index 312f25b..8ff1b34 100644
--- a/RenoDXCommander/DetailPanelBuilder.cs
+++ b/RenoDXCommander/DetailPanelBuilder.cs
@@ -191,11 +191,11 @@ public partial class DetailPanelBuilder
                     var cursorProp = DetailPanelBuilder.CursorProp;
                     badge.PointerEntered += (s, e) => cursorProp?.SetValue(badge, handCursor);
                     badge.PointerExited += (s, e) => cursorProp?.SetValue(badge, arrowCursor);
-                    ToolTipService.SetToolTip(badge, $"Mod author: {author} — click to open Ko-fi donation page");
+                    ToolTipService.SetToolTip(badge, $"Автор мода: {author} — нажмите, чтобы открыть страницу пожертвований на Ko-fi");
                 }
                 else
                 {
-                    ToolTipService.SetToolTip(badge, $"Mod author: {author}");
+                    ToolTipService.SetToolTip(badge, $"Автор мода: {author}");
                 }
                 _window.DetailAuthorBadgePanel.Children.Add(badge);
             }
@@ -229,14 +229,14 @@ public partial class DetailPanelBuilder
                 _window.DetailModStatusIcon.Text = "✓";
                 _window.DetailModStatusIcon.Foreground = UIFactory.Brush(ResourceKeys.AccentGreenBrush);
                 _window.DetailModStatusIcon.Visibility = Visibility.Visible;
-                ToolTipService.SetToolTip(_window.DetailModStatusIcon, "HDR mod complete");
+                ToolTipService.SetToolTip(_window.DetailModStatusIcon, "HDR-мод завершён");
             }
             else if (string.Equals(dbEntry.Status, "WIP", StringComparison.OrdinalIgnoreCase))
             {
                 _window.DetailModStatusIcon.Text = "🔨";
                 _window.DetailModStatusIcon.Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush);
                 _window.DetailModStatusIcon.Visibility = Visibility.Visible;
-                ToolTipService.SetToolTip(_window.DetailModStatusIcon, "HDR mod in progress");
+                ToolTipService.SetToolTip(_window.DetailModStatusIcon, "HDR-мод в разработке");
             }
             else
             {
@@ -330,7 +330,7 @@ public partial class DetailPanelBuilder
         if (card.HasUwFixUrl)
         {
             var source = card.UwFixSource ?? "creator";
-            ToolTipService.SetToolTip(_window.DetailUwFixBtn, $"Open {source}'s ultrawide fix page");
+            ToolTipService.SetToolTip(_window.DetailUwFixBtn, $"Открыть страницу ультраширокого исправления для {source}");
         }
 
         // Ultra+ link button
diff --git a/RenoDXCommander/DialogService.Game.cs b/RenoDXCommander/DialogService.Game.cs
index 2efae60..af6e77d 100644
--- a/RenoDXCommander/DialogService.Game.cs
+++ b/RenoDXCommander/DialogService.Game.cs
@@ -26,14 +26,14 @@ public partial class DialogService
                 TextWrapping = TextWrapping.Wrap,
                 Foreground   = Brush(ResourceKeys.AccentAmberBrush),
                 FontSize     = 13,
-                Text         = $"A dxgi.dll file was found in:\n{card.InstallPath}\n\n" +
-                               $"File size: {sizeKB:N0} KB\n\n" +
-                               "RHI cannot identify this file as ReShade or Display Commander. " +
-                               "It may belong to another mod (e.g. DXVK, Special K, ENB).\n\n" +
-                               "Overwriting it may break the existing mod. Do you want to proceed?",
+                Text         = $"В папке найден файл dxgi.dll:\n{card.InstallPath}\n\n" +
+                               $"Размер файла: {sizeKB:N0} КБ\n\n" +
+                               "RHI не смог опознать этот файл как ReShade или Display Commander. " +
+                               "Он может принадлежать другому моду (например, DXVK, Special K, ENB).\n\n" +
+                               "Перезапись может сломать существующий мод. Продолжить?",
             },
-            PrimaryButtonText   = "Overwrite",
-            CloseButtonText     = "Cancel",
+            PrimaryButtonText   = "Перезаписать",
+            CloseButtonText     = "Отмена",
             XamlRoot            = _window.Content.XamlRoot,
             Background          = Brush(ResourceKeys.SurfaceOverlayBrush),
             RequestedTheme      = ElementTheme.Dark,
@@ -56,14 +56,14 @@ public partial class DialogService
                 TextWrapping = TextWrapping.Wrap,
                 Foreground   = Brush(ResourceKeys.AccentAmberBrush),
                 FontSize     = 13,
-                Text         = $"A winmm.dll file was found in:\n{card.InstallPath}\n\n" +
-                               $"File size: {sizeKB:N0} KB\n\n" +
-                               "RHI cannot identify this file as Display Commander. " +
-                               "It may belong to another mod or DLL injector.\n\n" +
-                               "Overwriting it may break the existing mod. Do you want to proceed?",
+                Text         = $"В папке найден файл winmm.dll:\n{card.InstallPath}\n\n" +
+                               $"Размер файла: {sizeKB:N0} КБ\n\n" +
+                               "RHI не смог опознать этот файл как Display Commander. " +
+                               "Он может принадлежать другому моду или инъектору DLL.\n\n" +
+                               "Перезапись может сломать существующий мод. Продолжить?",
             },
-            PrimaryButtonText   = "Overwrite",
-            CloseButtonText     = "Cancel",
+            PrimaryButtonText   = "Перезаписать",
+            CloseButtonText     = "Отмена",
             XamlRoot            = _window.Content.XamlRoot,
             Background          = Brush(ResourceKeys.SurfaceOverlayBrush),
             RequestedTheme      = ElementTheme.Dark,
@@ -192,7 +192,7 @@ public partial class DialogService
             {
                 Title           = $"{addonName} — {card.GameName}",
                 Content         = scrollContent,
-                CloseButtonText = "Close",
+                CloseButtonText = "Закрыть",
                 XamlRoot        = _window.Content.XamlRoot,
                 Background      = Brush(ResourceKeys.SurfaceToolbarBrush),
                 RequestedTheme  = ElementTheme.Dark,
@@ -248,7 +248,7 @@ public partial class DialogService
                 BorderThickness     = new Thickness(1),
                 Child = new TextBlock
                 {
-                    Text       = "RTX HDR Enabled",
+                    Text       = "RTX HDR включён",
                     FontSize   = 12,
                     Foreground = Brush(ResourceKeys.AccentGreenBrush),
                 }
@@ -257,21 +257,21 @@ public partial class DialogService
 
             panel.Children.Add(new TextBlock
             {
-                Text = "RTX HDR uses NVIDIA's driver-level HDR injection to upgrade SDR games to HDR. " +
-                       "It works at the GPU level without injecting DLLs into the game, making it compatible with anti-cheat systems.\n\n" +
-                       "Requires: NVIDIA App installed, Game Filter/Freestyle enabled, RTX GPU, driver 550+.",
+                Text = "RTX HDR использует внедрение HDR на уровне драйвера NVIDIA, превращая SDR-игры в HDR. " +
+                       "Работает на уровне GPU без внедрения DLL в игру, поэтому совместим с античит-системами.\n\n" +
+                       "Требуется: установленный NVIDIA App, включённый Game Filter/Freestyle, GPU RTX, драйвер 550+.",
                 TextWrapping = TextWrapping.Wrap,
                 Foreground   = textColour,
                 FontSize     = 13,
                 LineHeight   = 22,
             });
 
-            AddHyperlinkBlock(panel, "RTX HDR Calibration Guide", manifest?.RtxHdrInfoUrl ?? "https://www.reddit.com/r/nvidia/comments/1b03yfg/rtx_hdr_paper_white_gamma_reference_settings/", linkColour);
+            AddHyperlinkBlock(panel, "Руководство по калибровке RTX HDR", manifest?.RtxHdrInfoUrl ?? "https://www.reddit.com/r/nvidia/comments/1b03yfg/rtx_hdr_paper_white_gamma_reference_settings/", linkColour);
 
             // Still show HDR Gaming Database link if available
             if (!string.IsNullOrEmpty(result.HdrAnalysisUrl))
             {
-                AddHyperlinkBlock(panel, "HDR Analysis — HDR Gaming Database", result.HdrAnalysisUrl, linkColour);
+                AddHyperlinkBlock(panel, "HDR-анализ — HDR Gaming Database", result.HdrAnalysisUrl, linkColour);
             }
 
             return; // Skip regular RenoDX content
@@ -328,19 +328,19 @@ public partial class DialogService
         // ── Wiki page link (NameUrl) as clickable hyperlink ──────────────────
         if (!string.IsNullOrEmpty(result.Url))
         {
-            AddHyperlinkBlock(panel, result.UrlLabel ?? "View wiki page", result.Url, linkColour);
+            AddHyperlinkBlock(panel, result.UrlLabel ?? "Открыть страницу вики", result.Url, linkColour);
         }
 
         // ── "Also available on Nexus" link (Snapshot+Nexus games only) ───────
         if (!card.IsExternalOnly && card.Mod?.SnapshotUrl != null && !string.IsNullOrEmpty(card.NexusUrl))
         {
-            AddHyperlinkBlock(panel, "Also available on Nexus Mods", card.NexusUrl, linkColour);
+            AddHyperlinkBlock(panel, "Также доступно на Nexus Mods", card.NexusUrl, linkColour);
         }
 
         // ── HDR Gaming Database link (supplementary) ─────────────────────────
         if (!string.IsNullOrEmpty(result.HdrAnalysisUrl))
         {
-            AddHyperlinkBlock(panel, "HDR Analysis — HDR Gaming Database", result.HdrAnalysisUrl, linkColour);
+            AddHyperlinkBlock(panel, "HDR-анализ — HDR Gaming Database", result.HdrAnalysisUrl, linkColour);
         }
 
         // ── Fallback if no content at all ────────────────────────────────────
@@ -348,7 +348,7 @@ public partial class DialogService
         {
             panel.Children.Add(new TextBlock
             {
-                Text       = "No additional RenoDX notes for this game.",
+                Text       = "Дополнительных заметок RenoDX для этой игры нет.",
                 Foreground = dimColour,
                 FontSize   = 13,
             });
@@ -481,7 +481,7 @@ public partial class DialogService
         // ── HDR Gaming Database link (supplementary) ─────────────────────────
         if (!string.IsNullOrEmpty(result.HdrAnalysisUrl))
         {
-            AddHyperlinkBlock(panel, "HDR Analysis — HDR Gaming Database", result.HdrAnalysisUrl, linkColour);
+            AddHyperlinkBlock(panel, "HDR-анализ — HDR Gaming Database", result.HdrAnalysisUrl, linkColour);
         }
 
         // ── Fallback if no content at all ────────────────────────────────────
@@ -502,7 +502,7 @@ public partial class DialogService
             {
                 panel.Children.Add(new TextBlock
                 {
-                    Text       = "No additional Luma notes for this game.",
+                    Text       = "Дополнительных заметок Luma для этой игры нет.",
                     Foreground = dimColour,
                     FontSize   = 13,
                 });
@@ -529,7 +529,7 @@ public partial class DialogService
             // ── Standard OptiScaler Compatibility section ─────────────────────
             if (result.OptiScalerCompat != null)
             {
-                BuildOptiScalerSection(panel, "OptiScaler Compatibility",
+                BuildOptiScalerSection(panel, "Совместимость OptiScaler",
                     result.OptiScalerCompat, textColour, linkColour);
             }
 
@@ -540,7 +540,7 @@ public partial class DialogService
                 if (result.OptiScalerCompat != null)
                     panel.Children.Add(new Border { Height = 4 });
 
-                BuildOptiScalerSection(panel, "FSR4 Compatibility",
+                BuildOptiScalerSection(panel, "Совместимость FSR4",
                     result.OptiScalerFsr4Compat, textColour, linkColour);
             }
 
@@ -560,7 +560,7 @@ public partial class DialogService
                 });
                 // Show URL only for the unstructured fallback path (no per-section links)
                 if (!string.IsNullOrEmpty(result.Url))
-                    AddHyperlinkBlock(panel, result.UrlLabel ?? "View wiki page", result.Url, linkColour);
+                    AddHyperlinkBlock(panel, result.UrlLabel ?? "Открыть страницу вики", result.Url, linkColour);
             }
         }
         else
@@ -573,7 +573,7 @@ public partial class DialogService
         {
             panel.Children.Add(new TextBlock
             {
-                Text       = "No OptiScaler compatibility data available for this game.",
+                Text       = "Для этой игры нет данных о совместимости с OptiScaler.",
                 Foreground = dimColour,
                 FontSize   = 13,
             });
@@ -593,7 +593,7 @@ public partial class DialogService
     {
         var upscalers = entry.Upscalers.Count > 0
             ? string.Join(", ", entry.Upscalers)
-            : "None listed";
+            : "Не указано";
 
         // Section header with status
         panel.Children.Add(new TextBlock
@@ -639,7 +639,7 @@ public partial class DialogService
             };
             link.Inlines.Add(new Microsoft.UI.Xaml.Documents.Run
             {
-                Text     = "View details",
+                Text     = "Подробнее",
                 FontSize = 13,
             });
             var para = new Microsoft.UI.Xaml.Documents.Paragraph();
@@ -679,7 +679,7 @@ public partial class DialogService
 
         // ── HDR Gaming Database link (supplementary) ─────────────────────────
         if (!string.IsNullOrEmpty(result.HdrAnalysisUrl))
-            AddHyperlinkBlock(panel, "HDR Analysis — HDR Gaming Database", result.HdrAnalysisUrl, linkColour);
+            AddHyperlinkBlock(panel, "HDR-анализ — HDR Gaming Database", result.HdrAnalysisUrl, linkColour);
     }
 
     // ── Release notes content rendering (ReLimiter, Display Commander) ───────────
@@ -766,7 +766,7 @@ public partial class DialogService
         }
 
         // Always show link to the releases page
-        AddHyperlinkBlock(panel, "View all releases on GitHub", releasesPageUrl, linkColour);
+        AddHyperlinkBlock(panel, "Все релизы на GitHub", releasesPageUrl, linkColour);
     }
 
     // ── Shared helpers for dialog content ────────────────────────────────────────
@@ -859,25 +859,25 @@ public partial class DialogService
 
             var hasNotes = !string.IsNullOrWhiteSpace(card.Notes);
             var notesHint = hasNotes
-                ? "\n\nCheck the Info button for any additional compatibility information for this game."
-                : "\n\nNo specific notes are available for this game — check the RenoDX Discord for community reports.";
+                ? "\n\nПроверьте кнопку «Инфо» — там может быть дополнительная информация о совместимости с этой игрой."
+                : "\n\nСпециальных заметок по этой игре нет — загляните в RenoDX Discord за отзывами сообщества.";
 
             var dlg = new ContentDialog
             {
-                Title               = "⚠ UE-Extended Compatibility Warning",
+                Title               = "⚠ Предупреждение о совместимости UE-Extended",
                 Content             = new TextBlock
                 {
                     TextWrapping = TextWrapping.Wrap,
                     FontSize     = 13,
-                    Text         = "Not all Unreal Engine games are compatible with UE-Extended.\n\n" +
-                                   "UE-Extended uses a generic injection method that works with most " +
-                                   "Unreal Engine games but may cause crashes or visual issues with others. " +
-                                   "If the game has a named RenoDX mod, that mod is specifically tailored " +
-                                   "for the game and may provide better results." +
+                    Text         = "Не все игры на Unreal Engine совместимы с UE-Extended.\n\n" +
+                                   "UE-Extended использует универсальный метод инъекции, работающий с большинством " +
+                                   "играми на Unreal Engine, но может вызывать вылеты или визуальные проблемы в других. " +
+                                   "Если у игры есть именной мод RenoDX, он специально адаптирован " +
+                                   "для этой игры и может дать лучший результат." +
                                    notesHint,
                 },
-                PrimaryButtonText   = "OK, I understand",
-                SecondaryButtonText = "Don't show again",
+                PrimaryButtonText   = "Хорошо, понятно",
+                SecondaryButtonText = "Больше не показывать",
                 XamlRoot            = _window.Content.XamlRoot,
                 Background          = Brush(ResourceKeys.SurfaceOverlayBrush),
                 RequestedTheme      = ElementTheme.Dark,
@@ -908,19 +908,19 @@ public partial class DialogService
 
             var dlg = new ContentDialog
             {
-                Title = "Administrator Privileges Required",
+                Title = "Требуются права администратора",
                 Content = new TextBlock
                 {
                     TextWrapping = TextWrapping.Wrap,
                     FontSize = 13,
                     Text = "Installing the Vulkan ReShade layer requires writing to C:\\ProgramData\\ReShade\\ " +
-                           "and modifying system registry keys, which needs administrator privileges.\n\n" +
-                           "Enable Admin Mode — RHI will always launch elevated (no UAC prompt after setup).\n\n" +
-                           "Restart as Admin — one-time elevated restart to complete this install.",
+                           "и изменение системных ключей реестра, что требует прав администратора.\n\n" +
+                           "Включить режим администратора — RHI всегда будет запускаться с повышенными правами (после настройки запрос UAC не появляется).\n\n" +
+                           "Перезапуск от администратора — разовый запуск с повышенными правами для завершения установки.",
                 },
-                PrimaryButtonText = "Enable Admin Mode",
-                SecondaryButtonText = "Restart as Admin",
-                CloseButtonText = "Cancel",
+                PrimaryButtonText = "Включить режим администратора",
+                SecondaryButtonText = "Перезапуск от администратора",
+                CloseButtonText = "Отмена",
                 XamlRoot = _window.Content.XamlRoot,
                 Background = Brush(ResourceKeys.SurfaceOverlayBrush),
                 RequestedTheme = ElementTheme.Dark,
@@ -958,7 +958,7 @@ public partial class DialogService
     private static void CreateAdminTaskAndRestart()
     {
         var exePath = Environment.ProcessPath ?? System.Reflection.Assembly.GetExecutingAssembly().Location;
-        var taskName = "RHI Admin Mode";
+        var taskName = "Режим администратора RHI";
 
         // Create the task (triggers UAC)
         var createArgs = $"/Create /TN \"{taskName}\" /TR \"\\\"{exePath}\\\"\" /SC ONCE /ST 00:00 /SD 01/01/2000 /RL HIGHEST /F";
diff --git a/RenoDXCommander/DialogService.Update.cs b/RenoDXCommander/DialogService.Update.cs
index 2c94031..7249839 100644
--- a/RenoDXCommander/DialogService.Update.cs
+++ b/RenoDXCommander/DialogService.Update.cs
@@ -64,7 +64,7 @@ public partial class DialogService
     {
         var dlg = new ContentDialog
         {
-            Title   = "🔄 Update Available",
+            Title   = "🔄 Доступно обновление",
             Content = new StackPanel
             {
                 Spacing = 8,
@@ -75,15 +75,15 @@ public partial class DialogService
                         TextWrapping = TextWrapping.Wrap,
                         Foreground   = Brush(ResourceKeys.TextSecondaryBrush),
                         FontSize     = 14,
-                        Text         = $"A new version of RHI is available!\n\n" +
+                        Text         = $"Доступна новая версия RHI!\n\n" +
                                        $"Installed:  v{updateInfo.CurrentVersion}\n" +
                                        $"Available:  v{updateInfo.DisplayVersion ?? updateInfo.RemoteVersion.ToString()}\n\n" +
-                                       "Would you like to update now?",
+                                       "Обновить сейчас?",
                     },
                 },
             },
-            PrimaryButtonText   = "Update Now",
-            CloseButtonText     = "Later",
+            PrimaryButtonText   = "Обновить сейчас",
+            CloseButtonText     = "Позже",
             XamlRoot            = _window.Content.XamlRoot,
             Background          = Brush(ResourceKeys.SurfaceRaisedBrush),
             RequestedTheme      = ElementTheme.Dark,
@@ -101,7 +101,7 @@ public partial class DialogService
         // Create a non-dismissable progress dialog
         var progressText = new TextBlock
         {
-            Text         = "Starting download...",
+            Text         = "Начинаю загрузку...",
             TextWrapping = TextWrapping.Wrap,
             Foreground   = Brush(ResourceKeys.TextSecondaryBrush),
             FontSize     = 13,
@@ -116,7 +116,7 @@ public partial class DialogService
         };
         var downloadDlg = new ContentDialog
         {
-            Title   = "⬇ Downloading Update",
+            Title   = "⬇ Скачивание обновления",
             Content = new StackPanel
             {
                 Spacing = 12,
@@ -161,7 +161,7 @@ public partial class DialogService
             // Download failed — update dialog to show error with a Close button
             _dispatcherQueue.TryEnqueue(() =>
             {
-                progressText.Text = "❌ Download failed. Please try again later or download manually from GitHub.";
+                progressText.Text = "❌ Не удалось скачать. Попробуйте позже или скачайте вручную с GitHub.";
                 progressBar.Value = 0;
                 downloadDlg.CloseButtonText = "Close";
             });
@@ -220,7 +220,7 @@ public partial class DialogService
             try
             {
                 Directory.CreateDirectory(PatchNotesDir);
-                File.WriteAllText(markerFile, $"Patch notes shown for v{versionStr}");
+                File.WriteAllText(markerFile, $"Список изменений показан для v{versionStr}");
             }
             catch (Exception ex)
             {
@@ -268,9 +268,9 @@ public partial class DialogService
 
         var dlg = new ContentDialog
         {
-            Title              = "📋 Patch Notes — What's New",
+            Title              = "📋 Список изменений — что нового",
             Content            = scrollViewer,
-            CloseButtonText    = "Close",
+            CloseButtonText    = "Закрыть",
             XamlRoot           = _window.Content.XamlRoot,
             Background         = Brush(ResourceKeys.SurfaceToolbarBrush),
             RequestedTheme     = ElementTheme.Dark,
@@ -321,7 +321,7 @@ public partial class DialogService
     {
         var dlg = new ContentDialog
         {
-            Title = "📢 Message from RHI",
+            Title = "📢 Сообщение от RHI",
             Content = new ScrollViewer
             {
                 Content = new TextBlock
diff --git a/RenoDXCommander/DlssDefaultsDialog.cs b/RenoDXCommander/DlssDefaultsDialog.cs
index 88ee718..a5163aa 100644
--- a/RenoDXCommander/DlssDefaultsDialog.cs
+++ b/RenoDXCommander/DlssDefaultsDialog.cs
@@ -29,15 +29,15 @@ public static class DlssDefaultsDialog
         var srCol = new StackPanel { Spacing = 4 };
         srCol.Children.Add(new TextBlock { Text = "DLSS", FontSize = 11, FontWeight = Microsoft.UI.Text.FontWeights.SemiBold, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) });
 
-        srCol.Children.Add(new TextBlock { Text = "Version", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
+        srCol.Children.Add(new TextBlock { Text = "Версия", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
         var srVersionCombo = BuildCombo(dlssService.DlssVersions, settings.DefaultDlssVersion, includeDriverOverride: true, currentDriverOverride: settings.DefaultSrDriverOverride);
         srCol.Children.Add(srVersionCombo);
 
-        srCol.Children.Add(new TextBlock { Text = "Preset", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
+        srCol.Children.Add(new TextBlock { Text = "Пресет", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
         var srPresetCombo = BuildPresetComboBox(DlssPresetService.SrPresets, settings.DefaultSrPreset);
         srCol.Children.Add(srPresetCombo);
 
-        srCol.Children.Add(new TextBlock { Text = "Render Scale", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
+        srCol.Children.Add(new TextBlock { Text = "Масштаб рендеринга", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
         var srScaleCombo = BuildRenderScaleComboBox(settings.DefaultSrRenderScale);
         srCol.Children.Add(srScaleCombo);
 
@@ -51,15 +51,15 @@ public static class DlssDefaultsDialog
         var rrCol = new StackPanel { Spacing = 4 };
         rrCol.Children.Add(new TextBlock { Text = "Ray Reconstruction", FontSize = 11, FontWeight = Microsoft.UI.Text.FontWeights.SemiBold, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) });
 
-        rrCol.Children.Add(new TextBlock { Text = "Version", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
+        rrCol.Children.Add(new TextBlock { Text = "Версия", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
         var rrVersionCombo = BuildCombo(dlssService.DlssdVersions, settings.DefaultDlssdVersion, includeDriverOverride: true, currentDriverOverride: settings.DefaultRrDriverOverride);
         rrCol.Children.Add(rrVersionCombo);
 
-        rrCol.Children.Add(new TextBlock { Text = "Preset", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
+        rrCol.Children.Add(new TextBlock { Text = "Пресет", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
         var rrPresetCombo = BuildPresetComboBox(DlssPresetService.RrPresets, settings.DefaultRrPreset);
         rrCol.Children.Add(rrPresetCombo);
 
-        rrCol.Children.Add(new TextBlock { Text = "Render Scale", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
+        rrCol.Children.Add(new TextBlock { Text = "Масштаб рендеринга", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
         var rrScaleCombo = BuildRenderScaleComboBox(settings.DefaultRrRenderScale);
         rrCol.Children.Add(rrScaleCombo);
 
@@ -71,13 +71,13 @@ public static class DlssDefaultsDialog
 
         // ── FG Column ──
         var fgCol = new StackPanel { Spacing = 4 };
-        fgCol.Children.Add(new TextBlock { Text = "Frame Generation", FontSize = 11, FontWeight = Microsoft.UI.Text.FontWeights.SemiBold, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) });
+        fgCol.Children.Add(new TextBlock { Text = "Генерация кадров", FontSize = 11, FontWeight = Microsoft.UI.Text.FontWeights.SemiBold, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) });
 
-        fgCol.Children.Add(new TextBlock { Text = "Version", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
+        fgCol.Children.Add(new TextBlock { Text = "Версия", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
         var fgVersionCombo = BuildCombo(dlssService.DlssgVersions, settings.DefaultDlssgVersion, includeDriverOverride: true, currentDriverOverride: settings.DefaultFgDriverOverride);
         fgCol.Children.Add(fgVersionCombo);
 
-        fgCol.Children.Add(new TextBlock { Text = "Preset", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
+        fgCol.Children.Add(new TextBlock { Text = "Пресет", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
         var fgPresetCombo = BuildPresetComboBox(DlssPresetService.FgPresets, settings.DefaultFgPreset);
         fgCol.Children.Add(fgPresetCombo);
 
@@ -97,13 +97,13 @@ public static class DlssDefaultsDialog
             grid.ColumnDefinitions.Add(new ColumnDefinition { Width = new GridLength(1, GridUnitType.Star) });
 
             var nrCol = new StackPanel { Spacing = 4 };
-            nrCol.Children.Add(new TextBlock { Text = "Neural Rendering", FontSize = 11, FontWeight = Microsoft.UI.Text.FontWeights.SemiBold, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) });
+            nrCol.Children.Add(new TextBlock { Text = "Нейронный рендеринг", FontSize = 11, FontWeight = Microsoft.UI.Text.FontWeights.SemiBold, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) });
 
-            nrCol.Children.Add(new TextBlock { Text = "Version", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
+            nrCol.Children.Add(new TextBlock { Text = "Версия", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
             nrVersionCombo = BuildCombo(dlssService.DlssnrVersions, settings.DefaultDlssnrVersion);
             nrCol.Children.Add(nrVersionCombo);
 
-            nrCol.Children.Add(new TextBlock { Text = "Preset", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
+            nrCol.Children.Add(new TextBlock { Text = "Пресет", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
             nrPresetCombo = BuildPresetComboBox(DlssPresetService.NrPresets, settings.DefaultNrPreset);
             nrCol.Children.Add(nrPresetCombo);
 
@@ -119,7 +119,7 @@ public static class DlssDefaultsDialog
         var slCol = new StackPanel { Spacing = 4 };
         slCol.Children.Add(new TextBlock { Text = "Streamline", FontSize = 11, FontWeight = Microsoft.UI.Text.FontWeights.SemiBold, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) });
 
-        slCol.Children.Add(new TextBlock { Text = "Version", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
+        slCol.Children.Add(new TextBlock { Text = "Версия", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush), Margin = new Microsoft.UI.Xaml.Thickness(0, 2, 0, 0) });
         var slVersionCombo = BuildCombo(dlssService.StreamlineVersions, settings.DefaultStreamlineVersion);
         slCol.Children.Add(slVersionCombo);
 
@@ -128,10 +128,10 @@ public static class DlssDefaultsDialog
 
         var dialog = new ContentDialog
         {
-            Title = "DLSS & Streamline Defaults",
+            Title = "DLSS и Streamline по умолчанию",
             Content = grid,
-            PrimaryButtonText = "Save",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Сохранить",
+            CloseButtonText = "Отмена",
             XamlRoot = xamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -143,15 +143,15 @@ public static class DlssDefaultsDialog
 
         // Save selections — "NVIDIA Override" stores the bool, not the version string
         var srVersionSelected = GetSelectedVersion(srVersionCombo);
-        settings.DefaultSrDriverOverride = srVersionSelected == "NVIDIA Override";
+        settings.DefaultSrDriverOverride = srVersionSelected == "Переопределение NVIDIA";
         settings.DefaultDlssVersion = settings.DefaultSrDriverOverride ? "" : srVersionSelected;
 
         var rrVersionSelected = GetSelectedVersion(rrVersionCombo);
-        settings.DefaultRrDriverOverride = rrVersionSelected == "NVIDIA Override";
+        settings.DefaultRrDriverOverride = rrVersionSelected == "Переопределение NVIDIA";
         settings.DefaultDlssdVersion = settings.DefaultRrDriverOverride ? "" : rrVersionSelected;
 
         var fgVersionSelected = GetSelectedVersion(fgVersionCombo);
-        settings.DefaultFgDriverOverride = fgVersionSelected == "NVIDIA Override";
+        settings.DefaultFgDriverOverride = fgVersionSelected == "Переопределение NVIDIA";
         settings.DefaultDlssgVersion = settings.DefaultFgDriverOverride ? "" : fgVersionSelected;
 
         settings.DefaultStreamlineVersion = GetSelectedVersion(slVersionCombo);
diff --git a/RenoDXCommander/DragDropHandler.Addon.cs b/RenoDXCommander/DragDropHandler.Addon.cs
index 42873de..6acc21d 100644
--- a/RenoDXCommander/DragDropHandler.Addon.cs
+++ b/RenoDXCommander/DragDropHandler.Addon.cs
@@ -32,8 +32,8 @@ public partial class DragDropHandler
         {
             var errDialog = new ContentDialog
             {
-                Title = "7-Zip Not Found",
-                Content = "Cannot extract archive — 7-Zip was not found. Please reinstall RDXC.",
+                Title = "7-Zip не найден",
+                Content = "Не удалось распаковать архив — 7-Zip не найден. Переустановите RDXC.",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -81,8 +81,8 @@ public partial class DragDropHandler
                 _crashReporter.Log($"[DragDropHandler.ProcessDroppedArchive] 7z exit code {proc.ExitCode}");
                 var failDialog = new ContentDialog
                 {
-                    Title = "Archive Extraction Failed",
-                    Content = $"Failed to extract '{archiveName}'. The file may be corrupt or in an unsupported format.",
+                    Title = "Не удалось распаковать архив",
+                    Content = $"Не удалось распаковать «{archiveName}». Файл может быть повреждён или в неподдерживаемом формате.",
                     CloseButtonText = "OK",
                     XamlRoot = _window.Content.XamlRoot,
                     RequestedTheme = ElementTheme.Dark,
@@ -105,8 +105,8 @@ public partial class DragDropHandler
                 _crashReporter.Log($"[DragDropHandler.ProcessDroppedArchive] No addon files found in '{archiveName}'");
                 var noAddonDialog = new ContentDialog
                 {
-                    Title = "No Addon Found",
-                    Content = $"No .addon64, .addon32, or .addon files were found inside '{archiveName}'.",
+                    Title = "Аддон не найден",
+                    Content = $"Внутри «{archiveName}» не найдено файлов .addon64, .addon32 или .addon.",
                     CloseButtonText = "OK",
                     XamlRoot = _window.Content.XamlRoot,
                     RequestedTheme = ElementTheme.Dark,
@@ -129,7 +129,7 @@ public partial class DragDropHandler
                 var combo = new ComboBox
                 {
                     HorizontalAlignment = HorizontalAlignment.Stretch,
-                    PlaceholderText = "Select addon to install...",
+                    PlaceholderText = "Выберите аддон для установки...",
                 };
                 foreach (var af in addonFiles)
                     combo.Items.Add(new ComboBoxItem { Content = Path.GetFileName(af), Tag = af });
@@ -139,8 +139,8 @@ public partial class DragDropHandler
                 {
                     Title = $"Multiple Addons in '{archiveName}'",
                     Content = combo,
-                    PrimaryButtonText = "Install",
-                    CloseButtonText = "Cancel",
+                    PrimaryButtonText = "Установить",
+                    CloseButtonText = "Отмена",
                     XamlRoot = _window.Content.XamlRoot,
                     RequestedTheme = ElementTheme.Dark,
                 };
@@ -173,7 +173,7 @@ public partial class DragDropHandler
                     };
                     var lumaPickDialog = new ContentDialog
                     {
-                        Title = "🌙 Install Luma Addon",
+                        Title = "🌙 Установить аддон Luma",
                         Content = new StackPanel
                         {
                             Spacing = 8,
@@ -183,8 +183,8 @@ public partial class DragDropHandler
                                 lumaCombo,
                             }
                         },
-                        PrimaryButtonText = "Install",
-                        CloseButtonText = "Cancel",
+                        PrimaryButtonText = "Установить",
+                        CloseButtonText = "Отмена",
                         XamlRoot = _window.Content.XamlRoot,
                         RequestedTheme = ElementTheme.Dark,
                     };
@@ -229,8 +229,8 @@ public partial class DragDropHandler
         {
             var noGamesDialog = new ContentDialog
             {
-                Title = "No Games Available",
-                Content = "No games are currently detected. Add a game first.",
+                Title = "Нет доступных игр",
+                Content = "Игры пока не обнаружены. Сначала добавьте игру.",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -243,7 +243,7 @@ public partial class DragDropHandler
         var combo = new ComboBox
         {
             HorizontalAlignment = HorizontalAlignment.Stretch,
-            PlaceholderText = "Select a game...",
+            PlaceholderText = "Выберите игру...",
         };
 
         // Sort alphabetically and populate
@@ -297,7 +297,7 @@ public partial class DragDropHandler
         var panel = new StackPanel { Spacing = 12 };
         panel.Children.Add(new TextBlock
         {
-            Text = $"Install {addonFileName} to a game folder.",
+            Text = $"Установите {addonFileName} в папку игры.",
             TextWrapping = TextWrapping.Wrap,
             FontSize = 13,
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
@@ -306,10 +306,10 @@ public partial class DragDropHandler
 
         var pickDialog = new ContentDialog
         {
-            Title = isLumaAddon ? "🌙 Install Luma Addon" : "📦 Install RenoDX Addon",
+            Title = isLumaAddon ? "🌙 Установить аддон Luma" : "📦 Установить аддон RenoDX",
             Content = panel,
-            PrimaryButtonText = "Next",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Далее",
+            CloseButtonText = "Отмена",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -321,8 +321,8 @@ public partial class DragDropHandler
         {
             var noSelection = new ContentDialog
             {
-                Title = "No Game Selected",
-                Content = "Please select a game to install the addon to.",
+                Title = "Игра не выбрана",
+                Content = "Выберите игру для установки аддона.",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -355,22 +355,22 @@ public partial class DragDropHandler
         catch (Exception ex) { _crashReporter.Log($"[DragDropHandler.ProcessDroppedAddon] Failed to check existing addons in '{installPath}' — {ex.Message}"); }
 
         // Confirmation dialog
-        var warningText = $"Are you sure you want to install {addonFileName} for {gameName}?";
+        var warningText = $"Установить {addonFileName} для игры {gameName}?";
         if (!string.IsNullOrEmpty(existingAddon))
-            warningText += $"\n\nThis will replace the existing addon: {existingAddon}";
-        warningText += $"\n\nInstall path: {installPath}";
+            warningText += $"\n\nСуществующий аддон будет заменён: {existingAddon}";
+        warningText += $"\n\nПуть установки: {installPath}";
 
         var confirmDialog = new ContentDialog
         {
-            Title = "⚠ Confirm Addon Install",
+            Title = "⚠ Подтвердите установку аддона",
             Content = new TextBlock
             {
                 Text = warningText,
                 TextWrapping = TextWrapping.Wrap,
                 FontSize = 13,
             },
-            PrimaryButtonText = "Install",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Установить",
+            CloseButtonText = "Отмена",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -470,7 +470,7 @@ public partial class DragDropHandler
                 };
                 targetCard.IsExternalOnly = true;
                 targetCard.ExternalUrl = "https://discord.gg/gF4GRJWZ2A";
-                targetCard.ExternalLabel = "Download from Discord";  // ExternalDisplayLabel does the Replace("Download", "Redownload")
+                targetCard.ExternalLabel = "Скачать с Discord";  // ExternalDisplayLabel does the Replace("Download", "Redownload")
             }
 
             // Update card status
@@ -497,8 +497,8 @@ public partial class DragDropHandler
 
             var successDialog = new ContentDialog
             {
-                Title = "✅ Addon Installed",
-                Content = $"{addonFileName} has been installed for {gameName}.",
+                Title = "✅ Аддон установлен",
+                Content = $"{addonFileName} установлен для {gameName}.",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -510,8 +510,8 @@ public partial class DragDropHandler
             _crashReporter.Log($"[DragDropHandler.ProcessDroppedAddon] Install failed — {ex.Message}");
             var errDialog = new ContentDialog
             {
-                Title = "❌ Install Failed",
-                Content = $"Failed to install addon: {ex.Message}",
+                Title = "❌ Не удалось установить",
+                Content = $"Не удалось установить аддон: {ex.Message}",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -534,8 +534,8 @@ public partial class DragDropHandler
             _crashReporter.Log($"[DragDropHandler.ProcessDroppedUrl] Invalid URL: {url}");
             var errDialog = new ContentDialog
             {
-                Title = "❌ Invalid URL",
-                Content = "The dropped URL could not be parsed. Please check the link and try again.",
+                Title = "❌ Неверная ссылка",
+                Content = "Не удалось разобрать перетащенную ссылку. Проверьте её и попробуйте снова.",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -551,8 +551,8 @@ public partial class DragDropHandler
             _crashReporter.Log($"[DragDropHandler.ProcessDroppedUrl] Could not extract filename from URL: {url}");
             var errDialog = new ContentDialog
             {
-                Title = "❌ Invalid URL",
-                Content = "Could not determine a filename from the dropped URL.",
+                Title = "❌ Неверная ссылка",
+                Content = "Не удалось определить имя файла из перетащенной ссылки.",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -595,18 +595,18 @@ public partial class DragDropHandler
                         };
                         var pickerDialog = new ContentDialog
                         {
-                            Title = "Install Luma Mod",
+                            Title = "Установить мод Luma",
                             Content = new StackPanel
                             {
                                 Spacing = 8,
                                 Children =
                                 {
-                                    new TextBlock { Text = $"Luma mod detected: {filename}\n\nSelect game to install to:", TextWrapping = TextWrapping.Wrap, FontSize = 12 },
+                                    new TextBlock { Text = $"Обнаружен мод Luma: {filename}\n\nВыберите игру для установки:", TextWrapping = TextWrapping.Wrap, FontSize = 12 },
                                     combo,
                                 }
                             },
-                            PrimaryButtonText = "Install",
-                            CloseButtonText = "Cancel",
+                            PrimaryButtonText = "Установить",
+                            CloseButtonText = "Отмена",
                             XamlRoot = _window.Content.XamlRoot,
                             RequestedTheme = ElementTheme.Dark,
                         };
@@ -643,8 +643,8 @@ public partial class DragDropHandler
             _crashReporter.Log($"[DragDropHandler.ProcessDroppedUrl] Unsupported extension '{ext}' for file '{filename}' from URL: {url}");
             var errDialog = new ContentDialog
             {
-                Title = "❌ Unsupported File Type",
-                Content = $"Only .addon64 and .addon32 files are supported.\n\nThe URL points to: {filename}",
+                Title = "❌ Неподдерживаемый тип файла",
+                Content = $"Поддерживаются только файлы .addon64 и .addon32.\n\nСсылка указывает на: {filename}",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -678,7 +678,7 @@ public partial class DragDropHandler
         };
         var progressDialog = new ContentDialog
         {
-            Title = "⬇ Downloading Addon",
+            Title = "⬇ Скачивание аддона",
             Content = new StackPanel
             {
                 Spacing = 12,
@@ -711,8 +711,8 @@ public partial class DragDropHandler
                     if (!gateReleased) { gateReleased = true; DialogService.ReleaseDialogGate(); }
                     var errDialog = new ContentDialog
                     {
-                        Title = "❌ Download Failed",
-                        Content = $"The server returned HTTP {(int)response.StatusCode}.\n\nURL: {url}",
+                        Title = "❌ Ошибка загрузки",
+                        Content = $"Сервер вернул HTTP {(int)response.StatusCode}.\n\nСсылка: {url}",
                         CloseButtonText = "OK",
                         XamlRoot = _window.Content.XamlRoot,
                         RequestedTheme = ElementTheme.Dark,
@@ -767,8 +767,8 @@ public partial class DragDropHandler
                 if (!gateReleased) { gateReleased = true; DialogService.ReleaseDialogGate(); }
                 var errDialog = new ContentDialog
                 {
-                    Title = "❌ Download Failed",
-                    Content = $"A network error occurred while downloading the addon.\n\n{ex.Message}",
+                    Title = "❌ Ошибка загрузки",
+                    Content = $"Сетевая ошибка при загрузке аддона.\n\n{ex.Message}",
                     CloseButtonText = "OK",
                     XamlRoot = _window.Content.XamlRoot,
                     RequestedTheme = ElementTheme.Dark,
@@ -783,8 +783,8 @@ public partial class DragDropHandler
                 if (!gateReleased) { gateReleased = true; DialogService.ReleaseDialogGate(); }
                 var errDialog = new ContentDialog
                 {
-                    Title = "❌ Download Timed Out",
-                    Content = "The download timed out. Please check your connection and try again.",
+                    Title = "❌ Истекло время ожидания загрузки",
+                    Content = "Загрузка не уложилась по времени. Проверьте соединение и попробуйте снова.",
                     CloseButtonText = "OK",
                     XamlRoot = _window.Content.XamlRoot,
                     RequestedTheme = ElementTheme.Dark,
@@ -807,8 +807,8 @@ public partial class DragDropHandler
                 if (!gateReleased) { gateReleased = true; DialogService.ReleaseDialogGate(); }
                 var errDialog = new ContentDialog
                 {
-                    Title = "❌ Invalid Addon File",
-                    Content = "The downloaded file is not a valid addon binary. The server may have returned an error page.",
+                    Title = "❌ Неверный файл аддона",
+                    Content = "Скачанный файл не является бинарником аддона. Возможно, сервер вернул страницу с ошибкой.",
                     CloseButtonText = "OK",
                     XamlRoot = _window.Content.XamlRoot,
                     RequestedTheme = ElementTheme.Dark,
diff --git a/RenoDXCommander/DragDropHandler.Exe.cs b/RenoDXCommander/DragDropHandler.Exe.cs
index 3120017..a425253 100644
--- a/RenoDXCommander/DragDropHandler.Exe.cs
+++ b/RenoDXCommander/DragDropHandler.Exe.cs
@@ -55,8 +55,8 @@ public partial class DragDropHandler
         {
             var dupDialog = new ContentDialog
             {
-                Title           = "Game Already Exists",
-                Content         = $"\"{existingCard.GameName}\" is already in your library at:\n{existingCard.InstallPath}",
+                Title           = "Игра уже добавлена",
+                Content         = $"«{existingCard.GameName}» уже есть в вашей библиотеке:\n{existingCard.InstallPath}",
                 CloseButtonText = "OK",
                 XamlRoot        = _window.Content.XamlRoot,
                 Background      = UIFactory.Brush(ResourceKeys.SurfaceToolbarBrush),
@@ -71,7 +71,7 @@ public partial class DragDropHandler
         var engineLabel = engine switch
         {
             EngineType.Unreal       => "Unreal Engine",
-            EngineType.UnrealLegacy => "Unreal Engine (Legacy)",
+            EngineType.UnrealLegacy => "Unreal Engine (устаревшие)",
             EngineType.Unity        => "Unity",
             _                       => "Unknown"
         };
@@ -79,12 +79,12 @@ public partial class DragDropHandler
         var confirmPanel = new StackPanel { Spacing = 8 };
         confirmPanel.Children.Add(new TextBlock
         {
-            Text = "Game name:", Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
+            Text = "Название игры:", Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
         });
         confirmPanel.Children.Add(nameBox);
         confirmPanel.Children.Add(new TextBlock
         {
-            Text = $"Engine: {engineLabel}\nInstall path: {installPath}",
+            Text = $"Движок: {engineLabel}\nПуть установки: {installPath}",
             TextWrapping = TextWrapping.Wrap,
             Foreground   = UIFactory.Brush(ResourceKeys.TextTertiaryBrush),
             FontSize     = 12, Margin = new Thickness(0, 6, 0, 0),
@@ -92,10 +92,10 @@ public partial class DragDropHandler
 
         var confirmDialog = new ContentDialog
         {
-            Title             = "➕ Add Dropped Game",
+            Title             = "➕ Добавить перенесённую игру",
             Content           = confirmPanel,
-            PrimaryButtonText = "Add Game",
-            CloseButtonText   = "Cancel",
+            PrimaryButtonText = "Добавить игру",
+            CloseButtonText   = "Отмена",
             XamlRoot          = _window.Content.XamlRoot,
             Background        = UIFactory.Brush(ResourceKeys.SurfaceToolbarBrush),
             RequestedTheme    = ElementTheme.Dark,
diff --git a/RenoDXCommander/DragDropHandler.Luma.cs b/RenoDXCommander/DragDropHandler.Luma.cs
index 9269b3a..4b61415 100644
--- a/RenoDXCommander/DragDropHandler.Luma.cs
+++ b/RenoDXCommander/DragDropHandler.Luma.cs
@@ -155,7 +155,7 @@ public partial class DragDropHandler
                         };
                         dialog = new Microsoft.UI.Xaml.Controls.ContentDialog
                         {
-                            Title = "Select Folder",
+                            Title = "Выбрать папку",
                             Content = new Microsoft.UI.Xaml.Controls.StackPanel
                             {
                                 Spacing = 8,
@@ -163,15 +163,15 @@ public partial class DragDropHandler
                                 {
                                     new Microsoft.UI.Xaml.Controls.TextBlock
                                     {
-                                        Text = "This archive contains multiple game folders.\nSelect the folder to install:",
+                                        Text = "Этот архив содержит несколько папок игр.\nВыберите папку для установки:",
                                         TextWrapping = Microsoft.UI.Xaml.TextWrapping.Wrap,
                                         FontSize = 12,
                                     },
                                     combo,
                                 }
                             },
-                            PrimaryButtonText = "Install",
-                            CloseButtonText = "Cancel",
+                            PrimaryButtonText = "Установить",
+                            CloseButtonText = "Отмена",
                             XamlRoot = _window.Content.XamlRoot,
                             RequestedTheme = Microsoft.UI.Xaml.ElementTheme.Dark,
                         };
diff --git a/RenoDXCommander/DragDropHandler.Preset.cs b/RenoDXCommander/DragDropHandler.Preset.cs
index 5e745c2..6630967 100644
--- a/RenoDXCommander/DragDropHandler.Preset.cs
+++ b/RenoDXCommander/DragDropHandler.Preset.cs
@@ -28,8 +28,8 @@ public partial class DragDropHandler
             _crashReporter.Log($"[DragDropHandler.ProcessDroppedPreset] Failed to read '{iniPath}' — {ex.Message}");
             var errDialog = new ContentDialog
             {
-                Title = "❌ Read Error",
-                Content = $"Failed to read the file: {ex.Message}",
+                Title = "❌ Ошибка чтения",
+                Content = $"Не удалось прочитать файл: {ex.Message}",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -43,8 +43,8 @@ public partial class DragDropHandler
             _crashReporter.Log($"[DragDropHandler.ProcessDroppedPreset] '{fileName}' is not a recognised ReShade preset");
             var errDialog = new ContentDialog
             {
-                Title = "❌ Not a ReShade Preset",
-                Content = "This file is not a recognised ReShade preset. A valid preset must contain a Techniques= line with at least one @.fx entry.",
+                Title = "❌ Это не пресет ReShade",
+                Content = "Этот файл не распознан как пресет ReShade. В корректном пресете есть строка Techniques= хотя бы с одной записью @.fx.",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -66,8 +66,8 @@ public partial class DragDropHandler
             _crashReporter.Log($"[DragDropHandler.ProcessDroppedPreset] Failed to copy to presets folder — {ex.Message}");
             var errDialog = new ContentDialog
             {
-                Title = "❌ Storage Error",
-                Content = $"Failed to save preset to the presets folder: {ex.Message}",
+                Title = "❌ Ошибка хранилища",
+                Content = $"Не удалось сохранить пресет в папку пресетов: {ex.Message}",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -82,8 +82,8 @@ public partial class DragDropHandler
         {
             var noGamesDialog = new ContentDialog
             {
-                Title = "No Games Available",
-                Content = "No games are currently detected. Add a game first.",
+                Title = "Нет доступных игр",
+                Content = "Игры пока не обнаружены. Сначала добавьте игру.",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -95,7 +95,7 @@ public partial class DragDropHandler
         var combo = new ComboBox
         {
             HorizontalAlignment = HorizontalAlignment.Stretch,
-            PlaceholderText = "Select a game...",
+            PlaceholderText = "Выберите игру...",
         };
 
         var sortedCards = cards.OrderBy(c => c.GameName, StringComparer.OrdinalIgnoreCase).ToList();
@@ -118,7 +118,7 @@ public partial class DragDropHandler
         var panel = new StackPanel { Spacing = 12 };
         panel.Children.Add(new TextBlock
         {
-            Text = $"Install {fileName} to a game folder.",
+            Text = $"Установите {fileName} в папку игры.",
             TextWrapping = TextWrapping.Wrap,
             FontSize = 13,
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
@@ -127,10 +127,10 @@ public partial class DragDropHandler
 
         var pickDialog = new ContentDialog
         {
-            Title = "🎨 Install ReShade Preset",
+            Title = "🎨 Установить пресет ReShade",
             Content = panel,
-            PrimaryButtonText = "Next",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Далее",
+            CloseButtonText = "Отмена",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -142,8 +142,8 @@ public partial class DragDropHandler
         {
             var noSelection = new ContentDialog
             {
-                Title = "No Game Selected",
-                Content = "Please select a game to install the preset to.",
+                Title = "Игра не выбрана",
+                Content = "Выберите игру для установки пресета.",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -167,8 +167,8 @@ public partial class DragDropHandler
             _crashReporter.Log($"[DragDropHandler.ProcessDroppedPreset] Failed to deploy preset — {ex.Message}");
             var errDialog = new ContentDialog
             {
-                Title = "❌ Deploy Failed",
-                Content = $"Failed to copy preset to game folder: {ex.Message}",
+                Title = "❌ Не удалось развернуть",
+                Content = $"Не удалось скопировать пресет в папку игры: {ex.Message}",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -180,10 +180,10 @@ public partial class DragDropHandler
         // ── Step 5: Shader confirmation dialog ───────────────────────────────
         var shaderDialog = new ContentDialog
         {
-            Title = "🔧 Install Shaders?",
-            Content = "Also install the required shaders and textures?",
-            PrimaryButtonText = "Yes",
-            CloseButtonText = "No",
+            Title = "🔧 Установить шейдеры?",
+            Content = "Также установить необходимые шейдеры и текстуры?",
+            PrimaryButtonText = "Да",
+            CloseButtonText = "Нет",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
diff --git a/RenoDXCommander/DragDropHandler.cs b/RenoDXCommander/DragDropHandler.cs
index e3fd290..68d7c7e 100644
--- a/RenoDXCommander/DragDropHandler.cs
+++ b/RenoDXCommander/DragDropHandler.cs
@@ -78,14 +78,14 @@ public partial class DragDropHandler
         if (e.DataView.Contains(Windows.ApplicationModel.DataTransfer.StandardDataFormats.StorageItems))
         {
             e.AcceptedOperation = Windows.ApplicationModel.DataTransfer.DataPackageOperation.Copy;
-            e.DragUIOverride.Caption = "Drop to add game, install addon, install preset, or extract archive";
+            e.DragUIOverride.Caption = "Перетащите сюда игру, аддон, пресет или архив";
             e.DragUIOverride.IsCaptionVisible = true;
             e.DragUIOverride.IsGlyphVisible = true;
         }
         else if (e.DataView.Contains(Windows.ApplicationModel.DataTransfer.StandardDataFormats.Text))
         {
             e.AcceptedOperation = Windows.ApplicationModel.DataTransfer.DataPackageOperation.Copy;
-            e.DragUIOverride.Caption = "Drop URL to download addon";
+            e.DragUIOverride.Caption = "Перетащите ссылку, чтобы скачать аддон";
             e.DragUIOverride.IsCaptionVisible = true;
             e.DragUIOverride.IsGlyphVisible = true;
         }
@@ -213,7 +213,7 @@ public partial class DragDropHandler
                                 };
                                 var pickerDialog = new Microsoft.UI.Xaml.Controls.ContentDialog
                                 {
-                                    Title = "🌙 Install Luma Addon",
+                                    Title = "🌙 Установить аддон Luma",
                                     Content = new Microsoft.UI.Xaml.Controls.StackPanel
                                     {
                                         Spacing = 8,
@@ -223,8 +223,8 @@ public partial class DragDropHandler
                                             combo,
                                         }
                                     },
-                                    PrimaryButtonText = "Install",
-                                    CloseButtonText = "Cancel",
+                                    PrimaryButtonText = "Установить",
+                                    CloseButtonText = "Отмена",
                                     XamlRoot = _window.Content.XamlRoot,
                                     RequestedTheme = Microsoft.UI.Xaml.ElementTheme.Dark,
                                 };
@@ -283,18 +283,18 @@ public partial class DragDropHandler
                                 };
                                 var pickerDialog = new Microsoft.UI.Xaml.Controls.ContentDialog
                                 {
-                                    Title = "Install Luma Mod",
+                                    Title = "Установить мод Luma",
                                     Content = new Microsoft.UI.Xaml.Controls.StackPanel
                                     {
                                         Spacing = 8,
                                         Children =
                                         {
-                                            new Microsoft.UI.Xaml.Controls.TextBlock { Text = $"Luma mod detected: {Path.GetFileName(file.Path)}\n\nSelect game to install to:", TextWrapping = Microsoft.UI.Xaml.TextWrapping.Wrap, FontSize = 12 },
+                                            new Microsoft.UI.Xaml.Controls.TextBlock { Text = $"Обнаружен мод Luma: {Path.GetFileName(file.Path)}\n\nВыберите игру для установки:", TextWrapping = Microsoft.UI.Xaml.TextWrapping.Wrap, FontSize = 12 },
                                             combo,
                                         }
                                     },
-                                    PrimaryButtonText = "Install",
-                                    CloseButtonText = "Cancel",
+                                    PrimaryButtonText = "Установить",
+                                    CloseButtonText = "Отмена",
                                     XamlRoot = _window.Content.XamlRoot,
                                     RequestedTheme = Microsoft.UI.Xaml.ElementTheme.Dark,
                                 };
diff --git a/RenoDXCommander/FEATURES.txt b/RenoDXCommander/FEATURES.txt
index b37e885..78b2640 100644
--- a/RenoDXCommander/FEATURES.txt
+++ b/RenoDXCommander/FEATURES.txt
@@ -1,146 +1,153 @@
-ReLimiter — Feature Reference
+ReLimiter — справочник по возможностям
 ==================================
 
-Core Features
+Основные функции
 -------------
 
-FPS Limit
-  Slider 0-500. Quick buttons: 30, 60, 120, 240, Reflex Cap.
-  Reflex Cap formula: fps = refresh - (refresh^2 / 4096), floored.
-  Example: 165 Hz -> 165 - (27225/4096) = 165 - 6.64 = 158.
+Ограничение FPS
+  Ползунок 0-500. Быстрые кнопки: 30, 60, 120, 240, лимит Reflex.
+  Формула лимита Reflex: fps = refresh - (refresh^2 / 4096), с округлением вниз.
+  Пример: 165 Гц -> 165 - (27225/4096) = 165 - 6.64 = 158.
 
-VSync Override
-  Game (passthrough), On (SyncInterval=1), Off (SyncInterval=0 + ALLOW_TEARING).
-  Hooks IDXGISwapChain::Present. Tearing auto-detected via IDXGIFactory5.
-  Also applied in Streamline proxy Present hook.
+Переопределение VSync
+  Игра (сквозной режим), Вкл (SyncInterval=1), Выкл (SyncInterval=0 + ALLOW_TEARING).
+  Хук IDXGISwapChain::Present. Разрывы определяются автоматически через IDXGIFactory5.
+  Также применяется в прокси-хуке Present у Streamline.
 
-5XXX Exclusive Pacing Optimization
-  Hooks IDXGISwapChain2::SetMaximumFrameLatency, overrides to 1.
-  Only on waitable swapchains. Game's original value captured and restored.
-  Designed for NVIDIA 50-series GPUs with flip metering support.
+Оптимизация pacing для серии 5XXX
+  Хук IDXGISwapChain2::SetMaximumFrameLatency, переопределяет значение на 1.
+  Только для waitable-swapchain. Исходное значение игры сохраняется и восстанавливается.
+  Рассчитано на GPU NVIDIA 50-й серии с поддержкой flip metering.
 
-FG Mode
-  Auto-detects DLSS FG, FSR FG, Smooth Motion from loaded DLLs.
-  Manual override: Off, 2x, 3x, 4x.
-  Divides target FPS by multiplier for real render rate.
+Режим FG
+  Автоопределение DLSS FG, FSR FG, Smooth Motion по загруженным DLL.
+  Ручное переопределение: Выкл, 2x, 3x, 4x.
+  Целевой FPS делится на множитель для получения реальной частоты рендеринга.
 
-Boost Override
-  Game (passthrough), On (force boost), Off (force no boost).
-  Controls Reflex Low Latency Boost (GPU clock management).
+Переопределение Boost
+  Игра (сквозной режим), Вкл (принудительный boost), Выкл (принудительно без boost).
+  Управляет Reflex Low Latency Boost (управление частотами GPU).
 
-Pacing Presets
-  Native Pacing    — SIM_START, no queue limit. Lowest latency.
-  Marker Low Lat   — Reflex markers, max 1 queued frame.
-  Balanced         — Reflex markers, max 2 queued frames.
-  Stability        — Reflex markers, max 3 queued frames.
-  SL Proxy         — Pace generated frames via Streamline proxy.
-  Custom           — Manual control of all sub-settings.
+Пресеты pacing
+  Native Pacing    — SIM_START, без лимита очереди. Минимальная задержка.
+  Marker Low Lat   — маркеры Reflex, не более 1 кадра в очереди.
+  Balanced         — маркеры Reflex, не более 2 кадров в очереди.
+  Stability        — маркеры Reflex, не более 3 кадров в очереди.
+  SL Proxy         — pacing генерируемых кадров через прокси Streamline.
+  Custom           — ручное управление всеми подпараметрами.
 
 
-UL-Exclusive Adaptive Features
+Адаптивные функции, уникальные для UL
 ------------------------------
 
-These features are unique to ReLimiter. Neither Special K nor Display
-Commander implement any of them.
-
-1. Predictive Sleep
-   GPU-aware wake scheduling. Tracks 8-sample GPU time history with weighted
-   averaging (4x/3x/2x/1x). Detects upward trends (3+ frames rising) and
-   biases prediction upward. Adaptive safety margin: 500us start, +50us per
-   miss, -10us per 30 stable frames, clamped 200-2000us. Only active when
-   GPU-bound. Predictive interval can only tighten the base interval.
-
-2. Phase-Locked Timing Grid
-   target[k] = epoch + k * interval. No drift accumulation. Late frames snap
-   forward. Uses CREATE_WAITABLE_TIMER_HIGH_RESOLUTION + busy-wait tail.
-   ZwSetTimerResolution for max kernel timer precision.
-
-3. Present-to-Present Feedback Loop
-   Closed-loop correction. Accumulates P2P error over 30 frames. Nudges
-   interval +/-1us per window. Dead zone +/-1us. Clamped +/-8us total.
-   Only active without FG. Resets on FPS limit change.
-
-4. Automatic Enforcement Site
-   GPU load > 85% -> SIM_START (lowest latency).
-   GPU load < 65% -> PRESENT_FINISH (best pacing).
-   65-85% hysteresis band prevents flip-flopping.
-
-5. Queue Pressure Detection
-   Monitors OS render queue time from GetLatency. If avg queue time > 1.5x
-   target interval, adds 4us to sleep interval to throttle proactively.
-
-6. Adaptive DLSS FG Pacing
-   Measures actual FG overhead (GPU render end to present end) via GetLatency.
-   EMA smoothed (alpha=0.1). Replaces static +24us offset. Clamped 4-60us.
-
-7. Hybrid Queue Wait
-   Two-phase: waitable timer sleeps (~0.5ms) then busy-wait for final <1ms.
-   50ms timeout. Saves CPU vs pure spin-loop used by SK and DC.
-
-8. Time-Based Warmup
-   2-second wall-clock warmup from first present. Consistent across frame
-   rates (frame-count warmup is 10s at 30fps but 1.25s at 240fps).
-
-9. Proper Reflex Restore
-   Captures game's SetSleepMode params on hook. Restores on shutdown.
-   If game never called SetSleepMode, disables Reflex cleanly.
-
-10. VRR / GSync Awareness
-    Ceiling = 3600 * hz / (hz + 3600). 0.5% safety margin. Clamps interval
-    so limiter never exceeds VRR window. Cached 2s. Disabled when VSync off.
-
-11. Adaptive Interval Adjustment
-    GPU load < 70%: -3us (tighten). GPU load > 90%: up to +4us (buffer).
-    Driver shave: always -2us to compensate for systematic overshoot.
-
-12. Flip Metering Block
-    Hooks nvapi_QueryInterface. Blocks ordinal 0xF3148C42 (SetFlipConfig)
-    by returning nullptr. Exception: Smooth Motion (needs flip metering).
-    UL's own lookups use original QI pointer. Always active, no toggle.
-
-
-Interval Computation Pipeline
+Эти функции есть только в ReLimiter. Ни Special K, ни Display
+Commander их не реализуют.
+
+1. Предиктивная задержка сна
+   Планирование пробуждения с учётом GPU. Хранит историю из 8 замеров времени GPU
+   со взвешенным усреднением (4x/3x/2x/1x). Определяет восходящие тренды
+   (3+ кадра роста подряд) и смещает прогноз вверх. Адаптивный запас безопасности:
+   старт с 500 мкс, +50 мкс за промах, -10 мкс за 30 стабильных кадров,
+   ограничение 200–2000 мкс. Активно только при упоре в GPU.
+   Предиктивный интервал может только сокращать базовый.
+
+2. Фазо-заблокированная сетка таймингов
+   target[k] = epoch + k * interval. Накопления дрейфа нет. Опоздавшие кадры
+   подтягиваются вперёд. Использует CREATE_WAITABLE_TIMER_HIGH_RESOLUTION + активное
+   ожидание на хвосте. ZwSetTimerResolution для максимальной точности таймера ядра.
+
+3. Контур обратной связи Present-to-Present
+   Коррекция с замкнутым контуром. Накапливает ошибку P2P за 30 кадров.
+   Сдвигает интервал на +/-1 мкс за окно. Мёртвая зона +/-1 мкс.
+   Общий предел +/-8 мкс. Активно только без FG. Сброс при смене лимита FPS.
+
+4. Автоматический выбор точки принудительного применения
+   Нагрузка GPU > 85% -> SIM_START (минимальная задержка).
+   Нагрузка GPU < 65% -> PRESENT_FINISH (лучший pacing).
+   Гистерезис 65–85% предотвращает «дёрганье» между режимами.
+
+5. Определение давления очереди
+   Следит за временем очереди рендеринга ОС через GetLatency. Если среднее время
+   очереди > 1.5x целевого интервала, добавляет 4 мкс к интервалу сна,
+   снижая нагрузку проактивно.
+
+6. Адаптивный pacing для DLSS FG
+   Измеряет фактические накладные расходы FG (от конца рендеринга GPU до конца
+   present) через GetLatency. Сглаживание EMA (alpha=0.1). Заменяет статическую
+   прибавку +24 мкс. Ограничение 4–60 мкс.
+
+7. Гибридное ожидание очереди
+   Две фазы: сон на waitable-таймере (~0.5 мс), затем активное ожидание последних <1 мс.
+   Таймаут 50 мс. Экономит CPU по сравнению с чистым спин-ожиданием в SK и DC.
+
+8. Прогрев по времени
+   Двухсекундный прогрев от первого present по настенным часам. Одинаков
+   для любых частот кадров (прогрев по числу кадров — это 10 с при 30 FPS,
+   но всего 1.25 с при 240 FPS).
+
+9. Корректное восстановление Reflex
+   При установке хука сохраняет параметры SetSleepMode игры. Восстанавливает
+   при завершении. Если игра ни разу не вызывала SetSleepMode — аккуратно
+   отключает Reflex.
+
+10. Учёт VRR / GSync
+    Потолок = 3600 * hz / (hz + 3600). Запас безопасности 0.5%. Ограничивает
+    интервал так, чтобы лимитер не выходил за окно VRR. Кешируется на 2 с.
+    Отключается при выключенном VSync.
+
+11. Адаптивная подстройка интервала
+    Нагрузка GPU < 70%: -3 мкс (ужесточение). Нагрузка GPU > 90%: до +4 мкс (запас).
+    Срез драйвера: всегда -2 мкс для компенсации систематического перебега.
+
+12. Блокировка flip metering
+    Хук nvapi_QueryInterface. Блокирует ординал 0xF3148C42 (SetFlipConfig),
+    возвращая nullptr. Исключение: Smooth Motion (flip metering ему нужен).
+    Собственные запросы UL идут через оригинальный указатель QI.
+    Активно всегда, без переключателя.
+
+
+Конвейер вычисления интервала
 -----------------------------
 
-Raw interval (from fps_limit / FG divisor)
-  -> VRR floor clamp
-  -> FG offset (adaptive or +24us static)
-  -> Queue pressure (+4us if flagged)
-  -> GPU headroom adjustment (-3us to +4us)
-  -> P2P feedback correction (-8us to +8us)
-  -> Driver shave (-2us)
-  -> Predictive sleep (can only tighten)
-  = Final minimumIntervalUs sent to driver
+Сырой интервал (из fps_limit / делителя FG)
+  -> ограничение по полу VRR
+  -> прибавка FG (адаптивная или статические +24 мкс)
+  -> давление очереди (+4 мкс при срабатывании)
+  -> коррекция запаса GPU (-3 мкс до +4 мкс)
+  -> коррекция обратной связи P2P (-8 мкс до +8 мкс)
+  -> срез драйвера (-2 мкс)
+  -> предиктивный сон (может только сократить)
+  = Итоговый minimumIntervalUs, отправляемый драйверу
 
 
-Reflex Hook Summary
+Сводка по хукам Reflex
 -------------------
 
-SetSleepMode  — Swallowed. Game params captured. UL drives sleep mode.
-Sleep         — Swallowed. UL calls Sleep on its own schedule.
-SetMarker     — Intercepted. RTSS filtered. INPUT_SAMPLE reordered.
-                Timestamps in 64-slot ring buffer. Dedup per frame.
-GetLatency    — Not hooked. Called directly for adaptive features.
-QueryInterface — Hooked. Blocks flip metering (SetFlipConfig).
+SetSleepMode  — Перехватывается. Параметры игры сохраняются. Режимом сна управляет UL.
+Sleep         — Перехватывается. UL вызывает Sleep по собственному расписанию.
+SetMarker     — Перехватывается. RTSS отфильтровывается. INPUT_SAMPLE переставляется.
+                Метки времени в кольцевом буфере на 64 слота. Дедупликация по кадру.
+GetLatency    — Не хукается. Вызывается напрямую для адаптивных функций.
+QueryInterface — Хукается. Блокирует flip metering (SetFlipConfig).
 
 
 Smooth Motion
 -------------
 
-Detected via NvPresent64.dll. NOT the same as DLSS Frame Generation.
-When active: Reflex Sleep bypassed, timing-only pacing, flip metering
-block disabled, OSD FPS/Native swapped.
+Определяется по NvPresent64.dll. Это НЕ то же самое, что генерация кадров DLSS.
+Когда активен: обход Reflex Sleep, только тайминг-pacing, блокировка flip metering
+отключена, FPS/Native в OSD меняются местами.
 
 
-OSD Elements
+Элементы OSD
 ------------
 
-FPS           White   Output frame rate (incl. generated frames)
-Native FPS    White   Real render rate from SIM_START markers
-Frame         White   Rolling avg frame time (64-frame window)
-GPU           Cyan    GPU active render time from GetLatency
-Render Lat    Cyan    Sim-start to GPU-render-end
-Present Lat   Cyan    Present start-to-end
-FG            Gold    Detected FG tech + multiplier
-Res           Gold    Render -> output resolution with scale %
-Graph         Green   128-sample frametime history, auto-scaled
+FPS           Белый    Частота на выходе (включая сгенерированные кадры)
+Native FPS    Белый    Реальная частота рендеринга по меткам SIM_START
+Frame         Белый    Скользящее среднее времени кадра (окно 64 кадра)
+GPU           Циан     Активное время рендеринга GPU из GetLatency
+Render Lat    Циан     От sim-start до конца рендеринга GPU
+Present Lat   Циан     От начала до конца present
+FG            Золотой  Определённая технология FG + множитель
+Res           Золотой  Разрешение рендеринга -> вывода с процентом масштаба
+Graph         Зелёный  История фреймтайма на 128 замеров, автомасштабирование
diff --git a/RenoDXCommander/GameReportEncoder.cs b/RenoDXCommander/GameReportEncoder.cs
index 0a82c4e..0ec6c36 100644
--- a/RenoDXCommander/GameReportEncoder.cs
+++ b/RenoDXCommander/GameReportEncoder.cs
@@ -26,18 +26,18 @@ public static class GameReportEncoder
         // Gatekeep: ask user to correct overrides first
         var gateDlg = new ContentDialog
         {
-            Title = "Before you submit",
+            Title = "Перед отправкой",
             Content = new TextBlock
             {
-                Text = "Please use the overrides on this panel to correct any wrong values " +
-                       "(bitness, graphics API, game name, etc.) before generating a report. " +
-                       "This helps us update the manifest faster.\n\n" +
-                       "Have you corrected everything you can?",
+                Text = "Используйте переопределения на этой панели, чтобы исправить неверные значения " +
+                       "(разрядность, графический API, название игры и т.д.) перед формированием отчёта. " +
+                       "Это помогает нам обновлять манифест быстрее.\n\n" +
+                       "Вы исправили всё, что могли?",
                 TextWrapping = TextWrapping.Wrap,
                 FontSize = 12,
             },
-            PrimaryButtonText = "Yes, continue",
-            CloseButtonText = "Go back",
+            PrimaryButtonText = "Да, продолжить",
+            CloseButtonText = "Назад",
             XamlRoot = xamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -48,7 +48,7 @@ public static class GameReportEncoder
         // Show dialog with optional note
         var noteBox = new TextBox
         {
-            PlaceholderText = "Describe the issue (optional)",
+            PlaceholderText = "Опишите проблему (необязательно)",
             AcceptsReturn = true,
             TextWrapping = TextWrapping.Wrap,
             MinHeight = 80,
@@ -57,7 +57,7 @@ public static class GameReportEncoder
 
         var dlg = new ContentDialog
         {
-            Title = "Copy Game Report",
+            Title = "Скопировать отчёт об игре",
             Content = new StackPanel
             {
                 Spacing = 8,
@@ -65,8 +65,8 @@ public static class GameReportEncoder
                 {
                     new TextBlock
                     {
-                        Text = "This saves a report file and copies it to your clipboard. " +
-                               "Paste it directly into Discord or attach it to a GitHub issue.",
+                        Text = "Будет сохранён файл отчёта и скопирован в буфер обмена. " +
+                               "Вставьте его прямо в Discord или приложите к задаче на GitHub.",
                         TextWrapping = TextWrapping.Wrap,
                         FontSize = 12,
                         Opacity = 0.7,
@@ -74,8 +74,8 @@ public static class GameReportEncoder
                     noteBox,
                 },
             },
-            PrimaryButtonText = "Copy to Clipboard",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Скопировать в буфер обмена",
+            CloseButtonText = "Отмена",
             XamlRoot = xamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -153,7 +153,7 @@ public static class GameReportEncoder
         {
             sb.AppendLine("## Components");
             sb.AppendLine();
-            sb.AppendLine("| Component | Status | Version | Filename |");
+            sb.AppendLine("| Компонент | Статус | Версия | Файл |");
             sb.AppendLine("|-----------|--------|---------|----------|");
             foreach (var c in comps)
                 sb.AppendLine($"| {c["name"]} | {c["status"]} | {c["version"]} | {c["filename"]} |");
diff --git a/RenoDXCommander/InstallEventHandler.cs b/RenoDXCommander/InstallEventHandler.cs
index 108b3b6..27afe1b 100644
--- a/RenoDXCommander/InstallEventHandler.cs
+++ b/RenoDXCommander/InstallEventHandler.cs
@@ -198,10 +198,10 @@ public class InstallEventHandler
             {
                 var warningDialog = new ContentDialog
                 {
-                    Title = "⚠ OptiScaler Setup",
-                    Content = "Before installing OptiScaler, please configure your GPU type and DLSS input (AMD/Intel only) settings in the OptiScaler Settings section on the Settings page.\n\nThis ensures OptiScaler is configured correctly for your hardware.",
-                    PrimaryButtonText = "Continue",
-                    CloseButtonText = "Cancel",
+                    Title = "⚠ Настройка OptiScaler",
+                    Content = "Перед установкой OptiScaler настройте тип видеокарты и DLSS-вход (только для AMD/Intel) в разделе «Настройки OptiScaler» на странице настроек.\n\nТак OptiScaler будет корректно настроен под ваше железо.",
+                    PrimaryButtonText = "Продолжить",
+                    CloseButtonText = "Отмена",
                     XamlRoot = xamlRoot,
                     RequestedTheme = ElementTheme.Dark,
                 };
@@ -220,7 +220,7 @@ public class InstallEventHandler
         var osVariant = ViewModel.GetOsVariant(card.GameName, card.Source ?? "");
 
         card.OsIsInstalling = true;
-        card.OsActionMessage = "Installing OptiScaler...";
+        card.OsActionMessage = "Установка OptiScaler...";
         card.OsProgress = 0;
         try
         {
@@ -242,7 +242,7 @@ public class InstallEventHandler
             {
                 try
                 {
-                    card.OsActionMessage = "Installing PD-Upscaler REFramework...";
+                    card.OsActionMessage = "Установка PD-Upscaler REFramework...";
                     await _reFrameworkService.InstallPdUpscalerAsync(
                         card.GameName, card.InstallPath, pdArtifact,
                         new Progress<(string message, double percent)>(p =>
@@ -260,7 +260,7 @@ public class InstallEventHandler
                 }
             }
 
-            card.OsActionMessage = "✅ OptiScaler installed!";
+            card.OsActionMessage = "✅ OptiScaler установлен!";
             card.OsStatus = GameStatus.Installed;
             card.NotifyAll();
             card.FadeMessage(m => card.OsActionMessage = m, card.OsActionMessage);
@@ -303,7 +303,7 @@ public class InstallEventHandler
         }
         catch (Exception ex)
         {
-            card.OsActionMessage = $"❌ Install failed: {ex.Message}";
+            card.OsActionMessage = $"❌ Не удалось установить: {ex.Message}";
         }
         finally
         {
@@ -371,14 +371,14 @@ public class InstallEventHandler
             }
             catch (Exception cleanEx) { CrashReporter.Log($"[InstallEventHandler.UninstallOptiScaler] Settings cleanup failed — {cleanEx.Message}"); }
 
-            card.OsActionMessage = "✖ OptiScaler removed.";
+            card.OsActionMessage = "✖ OptiScaler удалён.";
             card.OsStatus = GameStatus.Available;
             card.NotifyAll();
             card.FadeMessage(m => card.OsActionMessage = m, card.OsActionMessage);
         }
         catch (Exception ex)
         {
-            card.OsActionMessage = $"❌ Uninstall failed: {ex.Message}";
+            card.OsActionMessage = $"❌ Не удалось удалить: {ex.Message}";
         }
 
         _window.DispatcherQueue?.TryEnqueue(() =>
@@ -514,7 +514,7 @@ public class InstallEventHandler
         ViewModel.ToggleUeExtended(card);
 
         // Directly update the badge text based on the new state
-        string newLabel = card.UseUeExtended ? "UE Extended" : "Generic UE";
+        string newLabel = card.UseUeExtended ? "UE Extended" : "Универсальный UE";
         _window.DetailGenericText.Text = newLabel;
 
         // Update the UE button styling
@@ -533,7 +533,7 @@ public class InstallEventHandler
 
         // Update tooltip
         ToolTipService.SetToolTip(_window.DetailUeExtendedBtn,
-            card.UseUeExtended ? "Disable UE Extended" : "Enable UE Extended");
+            card.UseUeExtended ? "Отключить UE Extended" : "Включить UE Extended");
 
         // Show inline message or warning dialog
         if (card.UseUeExtended)
@@ -577,16 +577,16 @@ public class InstallEventHandler
 
         var dontShowCheck = new CheckBox
         {
-            Content = "Don't show this again",
+            Content = "Больше не показывать",
             FontSize = 12,
             Margin = new Thickness(0, 12, 0, 0),
         };
 
         var messageText = new TextBlock
         {
-            Text = "Heads up — you're installing both RenoDX and Luma on this game.\n\n" +
-                   "There's no guarantee they'll work well together. If you're using RenoDX for HDR and just want Luma for DLAA, make sure to disable HDR in the Luma mod settings to avoid conflicts.\n\n" +
-                   "If something doesn't look right, uninstalling one of them is the first thing to try. We can't offer support for issues that come from running both together.",
+            Text = "Внимание — вы устанавливаете на эту игру и RenoDX, и Luma.\n\n" +
+                   "Гарантии, что они будут хорошо работать вместе, нет. Если используете RenoDX для HDR, а Luma — только ради DLAA, обязательно отключите HDR в настройках мода Luma, чтобы избежать конфликтов.\n\n" +
+                   "Если что-то работает не так, первым делом попробуйте удалить один из них. Мы не можем поддерживать проблемы от одновременного использования обоих.",
             TextWrapping = TextWrapping.Wrap,
             FontSize = 13,
             LineHeight = 22,
@@ -599,10 +599,10 @@ public class InstallEventHandler
 
         var dialog = new ContentDialog
         {
-            Title = "Installing both RenoDX and Luma",
+            Title = "Установка RenoDX и Luma вместе",
             Content = content,
-            PrimaryButtonText = "Continue",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Продолжить",
+            CloseButtonText = "Отмена",
             DefaultButton = ContentDialogButton.Primary,
             XamlRoot = xamlRoot,
             RequestedTheme = ElementTheme.Dark,
diff --git a/RenoDXCommander/MainWindow.Events.Components.cs b/RenoDXCommander/MainWindow.Events.Components.cs
index ed474f9..9cda2ee 100644
--- a/RenoDXCommander/MainWindow.Events.Components.cs
+++ b/RenoDXCommander/MainWindow.Events.Components.cs
@@ -201,12 +201,12 @@ public sealed partial class MainWindow
         {
             Text = HotkeyManager.FormatHotkeyDisplay(hotkeyString),
             IsReadOnly = true,
-            PlaceholderText = "Click then press a key...",
+            PlaceholderText = "Нажмите сюда, затем нажмите клавишу...",
             FontSize = 12,
             HorizontalAlignment = HorizontalAlignment.Stretch,
         };
         ToolTipService.SetToolTip(hotkeyBox, "Click here then press your desired key. Written to all reshade*.ini files for this game.");
-        hotkeyBox.GotFocus  += (s, ev) => hotkeyBox.Text = "Press a key...";
+        hotkeyBox.GotFocus  += (s, ev) => hotkeyBox.Text = "Нажмите клавишу...";
         hotkeyBox.KeyDown   += (s, ev) =>
         {
             var vk = (int)ev.Key;
@@ -218,9 +218,9 @@ public sealed partial class MainWindow
             hotkeyBox.Text = HotkeyManager.FormatHotkeyDisplay(hotkeyString);
             ev.Handled = true;
         };
-        hotkeyBox.LostFocus += (s, ev) => { if (hotkeyBox.Text == "Press a key...") hotkeyBox.Text = HotkeyManager.FormatHotkeyDisplay(hotkeyString); };
+        hotkeyBox.LostFocus += (s, ev) => { if (hotkeyBox.Text == "Нажмите клавишу...") hotkeyBox.Text = HotkeyManager.FormatHotkeyDisplay(hotkeyString); };
 
-        var applyKeyBtn = new Button { Content = "Apply", FontSize = 12, Padding = new Thickness(16, 7, 16, 7), HorizontalAlignment = HorizontalAlignment.Right };
+        var applyKeyBtn = new Button { Content = "Применить", FontSize = 12, Padding = new Thickness(16, 7, 16, 7), HorizontalAlignment = HorizontalAlignment.Right };
         applyKeyBtn.Click += (s, ev) =>
         {
             if (string.IsNullOrEmpty(card.InstallPath)) return;
@@ -242,12 +242,12 @@ public sealed partial class MainWindow
         {
             Text = HotkeyManager.FormatHotkeyDisplay(screenshotHotkeyString),
             IsReadOnly = true,
-            PlaceholderText = "Click then press a key...",
+            PlaceholderText = "Нажмите сюда, затем нажмите клавишу...",
             FontSize = 12,
             HorizontalAlignment = HorizontalAlignment.Stretch,
         };
         ToolTipService.SetToolTip(screenshotHotkeyBox, "Press a key to assign it, or Backspace to clear it. Written to all reshade*.ini files for this game.");
-        screenshotHotkeyBox.GotFocus  += (s, ev) => screenshotHotkeyBox.Text = "Press a key...";
+        screenshotHotkeyBox.GotFocus  += (s, ev) => screenshotHotkeyBox.Text = "Нажмите клавишу...";
         screenshotHotkeyBox.KeyDown   += (s, ev) =>
         {
             var vk2 = (int)ev.Key;
@@ -259,9 +259,9 @@ public sealed partial class MainWindow
             screenshotHotkeyBox.Text = HotkeyManager.FormatHotkeyDisplay(screenshotHotkeyString);
             ev.Handled = true;
         };
-        screenshotHotkeyBox.LostFocus += (s, ev) => { if (screenshotHotkeyBox.Text == "Press a key...") screenshotHotkeyBox.Text = HotkeyManager.FormatHotkeyDisplay(screenshotHotkeyString); };
+        screenshotHotkeyBox.LostFocus += (s, ev) => { if (screenshotHotkeyBox.Text == "Нажмите клавишу...") screenshotHotkeyBox.Text = HotkeyManager.FormatHotkeyDisplay(screenshotHotkeyString); };
 
-        var applyScreenshotKeyBtn = new Button { Content = "Apply", FontSize = 12, Padding = new Thickness(16, 7, 16, 7), HorizontalAlignment = HorizontalAlignment.Right };
+        var applyScreenshotKeyBtn = new Button { Content = "Применить", FontSize = 12, Padding = new Thickness(16, 7, 16, 7), HorizontalAlignment = HorizontalAlignment.Right };
         applyScreenshotKeyBtn.Click += (s, ev) =>
         {
             if (string.IsNullOrEmpty(card.InstallPath)) return;
@@ -285,7 +285,7 @@ public sealed partial class MainWindow
         hotkeysGrid.ColumnDefinitions.Add(new ColumnDefinition { Width = new GridLength(1, GridUnitType.Star) });
 
         var overlayCol = new StackPanel { Spacing = 4 };
-        overlayCol.Children.Add(new TextBlock { Text = "Overlay Key", FontSize = 12, Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush) });
+        overlayCol.Children.Add(new TextBlock { Text = "Клавиша оверлея", FontSize = 12, Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush) });
         var overlayRow = new Grid { ColumnSpacing = 8 };
         overlayRow.ColumnDefinitions.Add(new ColumnDefinition { Width = new GridLength(1, GridUnitType.Star) });
         overlayRow.ColumnDefinitions.Add(new ColumnDefinition { Width = GridLength.Auto });
@@ -294,7 +294,7 @@ public sealed partial class MainWindow
         overlayCol.Children.Add(overlayRow);
 
         var screenshotCol = new StackPanel { Spacing = 4 };
-        screenshotCol.Children.Add(new TextBlock { Text = "Screenshot Key", FontSize = 12, Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush) });
+        screenshotCol.Children.Add(new TextBlock { Text = "Клавиша скриншота", FontSize = 12, Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush) });
         var screenshotRow = new Grid { ColumnSpacing = 8 };
         screenshotRow.ColumnDefinitions.Add(new ColumnDefinition { Width = new GridLength(1, GridUnitType.Star) });
         screenshotRow.ColumnDefinitions.Add(new ColumnDefinition { Width = GridLength.Auto });
@@ -346,9 +346,9 @@ public sealed partial class MainWindow
 
         var dialog = new ContentDialog
         {
-            Title = "ReShade Settings",
+            Title = "Настройки ReShade",
             Content = content,
-            CloseButtonText = "Close",
+            CloseButtonText = "Закрыть",
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -370,7 +370,7 @@ public sealed partial class MainWindow
         {
             content.Children.Add(new TextBlock
             {
-                Text = "UE-Extended Settings",
+                Text = "Настройки UE-Extended",
                 FontSize = 13,
                 Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
             });
@@ -393,7 +393,7 @@ public sealed partial class MainWindow
             var ueCombo = new ComboBox { FontSize = 11, MinWidth = 100, HorizontalAlignment = HorizontalAlignment.Stretch };
             ueCombo.Items.Add("Off");
             ueCombo.Items.Add("On");
-            ToolTipService.SetToolTip(ueCombo, "Switch between using UE-Extended or the game specific mod/generic Unreal RenoDX mod.");
+            ToolTipService.SetToolTip(ueCombo, "Переключение между UE-Extended и именным/универсальным модом RenoDX для Unreal.");
             ueCombo.SelectedIndex = card.UseUeExtended ? 1 : 0;
             ueCombo.SelectionChanged += (s, ev) =>
             {
@@ -423,7 +423,7 @@ public sealed partial class MainWindow
             // Label in column 0
             var nitsLabel = new TextBlock
             {
-                Text = "Set Maximum Nits",
+                Text = "Задать макс. нит",
                 FontSize = 11,
                 Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
                 VerticalAlignment = VerticalAlignment.Center,
@@ -437,7 +437,7 @@ public sealed partial class MainWindow
                 Text = currentNits,
                 Width = 100,
                 FontSize = 11,
-                PlaceholderText = "nits",
+                PlaceholderText = "нит",
                 VerticalAlignment = VerticalAlignment.Center,
             };
 
@@ -446,7 +446,7 @@ public sealed partial class MainWindow
             {
                 if (!int.TryParse(nitsValue, out var val) || val <= 0)
                 {
-                    card.ActionMessage = "❌ Enter a valid number.";
+                    card.ActionMessage = "❌ Введите корректное число.";
                     return;
                 }
                 try
@@ -468,7 +468,7 @@ public sealed partial class MainWindow
                     }
                     AuxInstallService.WriteIni(iniPath, freshIni);
                     nitsBox.Text = val.ToString();
-                    card.ActionMessage = $"✅ Set toneMapPeakNits={val} in {updated} preset(s).";
+                    card.ActionMessage = $"✅ Задано toneMapPeakNits={val} в пресетах ({updated}).";
                     card.FadeMessage(m => card.ActionMessage = m, card.ActionMessage);
                 }
                 catch (Exception ex) { card.ActionMessage = $"❌ {ex.Message}"; }
@@ -488,21 +488,21 @@ public sealed partial class MainWindow
 
             var autoBtn = new Button
             {
-                Content = "Auto",
+                Content = "Авто",
                 Background = UIFactory.Brush(ResourceKeys.AccentBlueBgBrush),
                 Foreground = UIFactory.Brush(ResourceKeys.AccentBlueBrush),
                 BorderBrush = UIFactory.Brush(ResourceKeys.AccentBlueBorderBrush),
                 BorderThickness = new Thickness(1),
                 CornerRadius = new CornerRadius(8), Padding = new Thickness(10, 5, 10, 5), FontSize = 11,
             };
-            ToolTipService.SetToolTip(autoBtn, "Reads your monitor's peak brightness automatically.");
+            ToolTipService.SetToolTip(autoBtn, "Автоматически считывает пиковую яркость монитора.");
             autoBtn.Click += async (s, ev) =>
             {
                 try
                 {
                     var devices = await Windows.Devices.Enumeration.DeviceInformation.FindAllAsync(
                         Windows.Devices.Display.DisplayMonitor.GetDeviceSelector());
-                    if (devices.Count == 0) { card.ActionMessage = "❌ No display found."; return; }
+                    if (devices.Count == 0) { card.ActionMessage = "❌ Дисплей не найден."; return; }
 
                     float maxNitsFound = 0;
                     foreach (var device in devices)
@@ -516,7 +516,7 @@ public sealed partial class MainWindow
                         catch { }
                     }
                     var peakNits = (int)maxNitsFound;
-                    if (peakNits <= 0) { card.ActionMessage = "❌ Could not read peak brightness."; return; }
+                    if (peakNits <= 0) { card.ActionMessage = "❌ Не удалось считать пиковую яркость."; return; }
 
                     ApplyNitsValue(peakNits.ToString());
                 }
@@ -557,7 +557,7 @@ public sealed partial class MainWindow
                     content.Children.Add(new Border { Height = 1, Background = UIFactory.Brush(ResourceKeys.BorderDefaultBrush), Margin = new Thickness(0, 10, 0, 2) });
                     content.Children.Add(new TextBlock
                     {
-                        Text = "Compatibility Settings",
+                        Text = "Настройки совместимости",
                         FontSize = 13,
                         Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
                         Margin = new Thickness(0, 4, 0, 0),
@@ -585,7 +585,7 @@ public sealed partial class MainWindow
 
                         var label = new TextBlock
                         {
-                            Text = isSetPath ? "Upgrade Path" : isDumpLut ? "Dump LUT Shaders" : kv.Key.StartsWith("Upgrade_", StringComparison.OrdinalIgnoreCase) ? kv.Key.Substring(8) : kv.Key,
+                            Text = isSetPath ? "Путь обновления" : isDumpLut ? "Дамп LUT-шейдеров" : kv.Key.StartsWith("Upgrade_", StringComparison.OrdinalIgnoreCase) ? kv.Key.Substring(8) : kv.Key,
                             FontSize = 11,
                             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
                             VerticalAlignment = VerticalAlignment.Center,
@@ -685,7 +685,7 @@ public sealed partial class MainWindow
             {
                 content.Children.Add(new TextBlock
                 {
-                    Text = "Run the game once with RenoDX installed to generate settings.",
+                    Text = "Запустите игру один раз с установленным RenoDX, чтобы создать настройки.",
                     FontSize = 11,
                     Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
                     FontStyle = Windows.UI.Text.FontStyle.Italic,
@@ -731,7 +731,7 @@ public sealed partial class MainWindow
             {
                 var hdrLabel = new TextBlock
                 {
-                    Text = "HDR Settings",
+                    Text = "Настройки HDR",
                     FontSize = 11,
                     Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
                     VerticalAlignment = VerticalAlignment.Center,
@@ -783,7 +783,7 @@ public sealed partial class MainWindow
             int lutCol = card.UseUeExtended ? 2 : 0;
             var lutLabel = new TextBlock
             {
-                Text = "LUT Update Every Frame",
+                Text = "Обновлять LUT каждый кадр",
                 FontSize = 11,
                 Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
                 VerticalAlignment = VerticalAlignment.Center,
@@ -853,7 +853,7 @@ public sealed partial class MainWindow
         content.Children.Add(new Border { Height = 1, Background = UIFactory.Brush(ResourceKeys.BorderDefaultBrush), Margin = new Thickness(0, 10, 0, 2) });
         content.Children.Add(new TextBlock
         {
-            Text = "RenoDX Presets",
+            Text = "Пресеты RenoDX",
             FontSize = 13,
             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
             Margin = new Thickness(0, 8, 0, 0),
@@ -862,7 +862,7 @@ public sealed partial class MainWindow
 
         var exportBtn = new Button
         {
-            Content = "Export Presets",
+            Content = "Экспортировать пресеты",
             HorizontalAlignment = HorizontalAlignment.Stretch,
             Background = UIFactory.Brush(ResourceKeys.AccentBlueBgBrush),
             Foreground = UIFactory.Brush(ResourceKeys.AccentBlueBrush),
@@ -898,7 +898,7 @@ public sealed partial class MainWindow
 
                 if (presetLines.Count == 0)
                 {
-                    card.ActionMessage = "❌ No [renodx-preset*] sections found.";
+                    card.ActionMessage = "❌ Секции [renodx-preset*] не найдены.";
                     return;
                 }
 
@@ -919,17 +919,17 @@ public sealed partial class MainWindow
                     Windows.ApplicationModel.DataTransfer.Clipboard.Flush();
                 }
                 catch { /* clipboard copy is best-effort */ }
-                card.ActionMessage = $"✅ Exported {presetLines.Count(l => l.StartsWith("["))} preset(s) & copied to clipboard.";
+                card.ActionMessage = $"✅ Экспортировано пресетов: {presetLines.Count(l => l.StartsWith("["))}, скопировано в буфер обмена.";
                 card.FadeMessage(m => card.ActionMessage = m, card.ActionMessage);
             }
             catch (Exception ex) { card.ActionMessage = $"❌ {ex.Message}"; }
         };
-        ToolTipService.SetToolTip(exportBtn, "Save all RenoDX presets to a file and copy to clipboard for sharing.");
+        ToolTipService.SetToolTip(exportBtn, "Сохранить все пресеты RenoDX в файл и скопировать в буфер обмена для обмена.");
         presetRow.Children.Add(exportBtn);
 
         var importBtn = new Button
         {
-            Content = "Import Presets",
+            Content = "Импортировать пресеты",
             HorizontalAlignment = HorizontalAlignment.Stretch,
             Background = UIFactory.Brush(ResourceKeys.AccentBlueBgBrush),
             Foreground = UIFactory.Brush(ResourceKeys.AccentBlueBrush),
@@ -977,13 +977,13 @@ public sealed partial class MainWindow
                 filtered.AddRange(presetLines);
 
                 File.WriteAllLines(iniPath, filtered);
-                card.ActionMessage = $"✅ Imported {presetSections.Count} preset(s).";
+                card.ActionMessage = $"✅ Импортировано пресетов: {presetSections.Count}.";
                 card.FadeMessage(m => card.ActionMessage = m, card.ActionMessage);
             }
             catch (Exception ex) { card.ActionMessage = $"❌ {ex.Message}"; }
         };
         if (!File.Exists(presetPath))
-            ToolTipService.SetToolTip(importBtn, "No RHI-RenoDX-Preset.txt file found. Export first.");
+            ToolTipService.SetToolTip(importBtn, "Файл RHI-RenoDX-Preset.txt не найден. Сначала экспортируйте.");
         else
             ToolTipService.SetToolTip(importBtn, "Restore presets from the exported backup file into reshade.ini.");
         presetRow.Children.Add(importBtn);
@@ -1001,7 +1001,7 @@ public sealed partial class MainWindow
         });
         content.Children.Add(new TextBlock
         {
-            Text = "Requires NVIDIA App with Overlay and Game Filters enabled.",
+            Text = "Требуется NVIDIA App с включёнными Overlay и Game Filters.",
             FontSize = 11,
             Foreground = UIFactory.Brush(ResourceKeys.InlineDescriptionBrush),
             TextWrapping = Microsoft.UI.Xaml.TextWrapping.Wrap,
@@ -1105,7 +1105,7 @@ public sealed partial class MainWindow
         var rtxHdrRow = new StackPanel { Orientation = Microsoft.UI.Xaml.Controls.Orientation.Horizontal, Spacing = 12 };
         rtxHdrRow.Children.Add(new TextBlock
         {
-            Text = "Enable RTX HDR",
+            Text = "Включить RTX HDR",
             FontSize = 11,
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             VerticalAlignment = VerticalAlignment.Center,
@@ -1115,9 +1115,9 @@ public sealed partial class MainWindow
 
         var dialog = new ContentDialog
         {
-            Title = "RenoDX Settings",
+            Title = "Настройки RenoDX",
             Content = new ScrollViewer { Content = content, MaxHeight = 620, Padding = new Thickness(0, 0, 16, 0) },
-            CloseButtonText = "Close",
+            CloseButtonText = "Закрыть",
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -1150,14 +1150,14 @@ public sealed partial class MainWindow
 
         // ── Peak Brightness ───────────────────────────────────────────────────
         var nitsRow = new StackPanel { Orientation = Orientation.Horizontal, Spacing = 8 };
-        var nitsLabel = new TextBlock { Text = $"Peak Brightness: {peakBrightnessDisplay} nits", FontSize = 12, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush), MinWidth = 175 };
-        var nitsWarning = new TextBlock { Text = "⚠ High values may look unnatural", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.AccentAmberBrush), VerticalAlignment = VerticalAlignment.Center, Opacity = peakBrightnessDisplay > 600 ? 1.0 : 0.0 };
+        var nitsLabel = new TextBlock { Text = $"Пиковая яркость: {peakBrightnessDisplay} нит", FontSize = 12, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush), MinWidth = 175 };
+        var nitsWarning = new TextBlock { Text = "⚠ Высокие значения могут выглядеть неестественно", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.AccentAmberBrush), VerticalAlignment = VerticalAlignment.Center, Opacity = peakBrightnessDisplay > 600 ? 1.0 : 0.0 };
         nitsRow.Children.Add(nitsLabel);
         nitsRow.Children.Add(nitsWarning);
         var nitsSlider = new Slider { Minimum = 400, Maximum = 2000, StepFrequency = 10, Value = peakBrightnessDisplay, HorizontalAlignment = HorizontalAlignment.Stretch };
         nitsSlider.ValueChanged += (s, ev) =>
         {
-            nitsLabel.Text = $"Peak Brightness: {(int)nitsSlider.Value} nits";
+            nitsLabel.Text = $"Пиковая яркость: {(int)nitsSlider.Value} нит";
             nitsWarning.Opacity = (int)nitsSlider.Value > 600 ? 1.0 : 0.0;
         };
         content.Children.Add(nitsRow);
@@ -1167,9 +1167,9 @@ public sealed partial class MainWindow
         // ── Contrast ──────────────────────────────────────────────────────────
         string ContrastLabel(int val) => val switch
         {
-            0 => "Contrast: 0 — Gamma 2.0 (Default)",
-            25 => "Contrast: +25 — Gamma 2.2",
-            50 => "Contrast: +50 — Gamma 2.4",
+            0 => "Контраст: 0 — Гамма 2.0 (по умолчанию)",
+            25 => "Контраст: +25 — Гамма 2.2",
+            50 => "Контраст: +50 — Гамма 2.4",
             _ => $"Contrast: {(val >= 0 ? "+" : "")}{val}",
         };
         var contrastLabel = new TextBlock { Text = ContrastLabel(contrastDisplay), FontSize = 12, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) };
@@ -1180,7 +1180,7 @@ public sealed partial class MainWindow
 
         // ── Gamma preset buttons ──────────────────────────────────────────────
         var gammaPanel = new StackPanel { Orientation = Orientation.Horizontal, Spacing = 6, Margin = new Thickness(0, 2, 0, 4) };
-        foreach (var (label, value) in new[] { ("Gamma 2.0", 0), ("Gamma 2.2", 25), ("Gamma 2.4", 50) })
+        foreach (var (label, value) in new[] { ("Гамма 2.0", 0), ("Гамма 2.2", 25), ("Гамма 2.4", 50) })
         {
             var btn = new Button
             {
@@ -1253,13 +1253,13 @@ public sealed partial class MainWindow
         string MiddleGreyLabel(int val, int contrastVal)
         {
             var perceivedPw = CalcPerceivedPaperwhite(val, contrastVal);
-            return $"Middle Grey: {val} ({perceivedPw} nits)";
+            return $"Средний серый: {val} ({perceivedPw} нит)";
         }
         
         int mgInitialPw = CalcPerceivedPaperwhite(mgInitial, (int)contrastSlider.Value);
         var mgRow = new StackPanel { Orientation = Orientation.Horizontal, Spacing = 8 };
         var mgLabel = new TextBlock { Text = MiddleGreyLabel(mgInitial, (int)contrastSlider.Value), FontSize = 12, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush), MinWidth = 175 };
-        var mgWarning = new TextBlock { Text = "⚠ High values may look washed out", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.AccentAmberBrush), VerticalAlignment = VerticalAlignment.Center, Opacity = mgInitialPw > 203 ? 1.0 : 0.0 };
+        var mgWarning = new TextBlock { Text = "⚠ Высокие значения могут выглядеть блёкло", FontSize = 10, Foreground = UIFactory.Brush(ResourceKeys.AccentAmberBrush), VerticalAlignment = VerticalAlignment.Center, Opacity = mgInitialPw > 203 ? 1.0 : 0.0 };
         mgRow.Children.Add(mgLabel);
         mgRow.Children.Add(mgWarning);
         var mgSlider = new Slider { Minimum = 10, Maximum = 100, StepFrequency = 1, Value = mgInitial, HorizontalAlignment = HorizontalAlignment.Stretch };
@@ -1281,11 +1281,11 @@ public sealed partial class MainWindow
         var mgButtonsPanel = new StackPanel { Orientation = Orientation.Horizontal, Spacing = 6, Margin = new Thickness(0, 2, 0, 4) };
         var autoMgBtn = new Button
         {
-            Content = "Auto",
+            Content = "Авто",
             FontSize = 11,
             Padding = new Thickness(10, 4, 10, 4),
         };
-        ToolTipService.SetToolTip(autoMgBtn, "Calculate Middle Grey from Peak Brightness and Gamma using the ITU formula");
+        ToolTipService.SetToolTip(autoMgBtn, "Вычислять средний серый из пиковой яркости и гаммы по формуле ITU");
         autoMgBtn.Click += (s, ev) =>
         {
             var autoVal = CalcAutoMiddleGrey((int)nitsSlider.Value, (int)contrastSlider.Value);
@@ -1309,7 +1309,7 @@ public sealed partial class MainWindow
                 MinWidth = 36,
             };
             var capturedPw = presetPw;
-            ToolTipService.SetToolTip(presetBtn, $"Set Middle Grey to achieve ~{presetPw} nits paperwhite");
+            ToolTipService.SetToolTip(presetBtn, $"Задайте средний серый для яркости белого ~{presetPw} нит");
             presetBtn.Click += (s, ev) =>
             {
                 // Reverse formula: midGrey = paperwhite × (0.5 ^ gamma)
@@ -1329,7 +1329,7 @@ public sealed partial class MainWindow
         // ── Saturation ────────────────────────────────────────────────────────
         string SaturationLabel(int val) => val switch
         {
-            -25 => "Saturation: -25 — Neutral Saturation",
+            -25 => "Насыщенность: -25 — нейтральная насыщенность",
             _ => $"Saturation: {(val >= 0 ? "+" : "")}{val}",
         };
         var satLabel = new TextBlock { Text = SaturationLabel(saturationDisplay), FontSize = 12, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) };
@@ -1342,11 +1342,11 @@ public sealed partial class MainWindow
         // ── Debanding ─────────────────────────────────────────────────────────
         var debandingOptions = new (string name, uint value)[]
         {
-            ("No Debanding", 0x06),
-            ("Low Debanding", 0x0A),
-            ("High Debanding", 0x02),
-            ("High Debanding (Indicator)", 0x03),
-            ("High Debanding (Indicator + Debug)", 0x23),
+            ("Без устранения бандинга", 0x06),
+            ("Слабое устранение бандинга", 0x0A),
+            ("Сильное устранение бандинга", 0x02),
+            ("Сильное устранение бандинга (индикатор)", 0x03),
+            ("Сильное устранение бандинга (индикатор + отладка)", 0x23),
         };
         bool isAdmin = VulkanLayerService.IsRunningAsAdmin();
         var debandingCombo = new ComboBox { FontSize = 12, HorizontalAlignment = HorizontalAlignment.Stretch, IsEnabled = isAdmin, Opacity = isAdmin ? 1.0 : 0.4 };
@@ -1357,13 +1357,13 @@ public sealed partial class MainWindow
             if (currentDebanding == (int)debandingOptions[i].value) selectedDbIndex = i;
         }
         debandingCombo.SelectedIndex = selectedDbIndex;
-        var dbLabel = new TextBlock { Text = "Debanding", FontSize = 12, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) };
+        var dbLabel = new TextBlock { Text = "Устранение бандинга", FontSize = 12, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) };
         content.Children.Add(dbLabel);
         content.Children.Add(debandingCombo);
         if (!isAdmin)
             content.Children.Add(new TextBlock
             {
-                Text = "Requires admin mode to change",
+                Text = "Для изменения нужен режим администратора",
                 FontSize = 10,
                 Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
                 Margin = new Thickness(0, -4, 0, 0),
@@ -1380,21 +1380,21 @@ public sealed partial class MainWindow
         var defaultsPanel = new StackPanel { Orientation = Orientation.Horizontal, Spacing = 8, Margin = new Thickness(0, 4, 0, 0) };
         var saveDefaultBtn = new Button
         {
-            Content = "Save as Default",
+            Content = "Сохранить по умолчанию",
             FontSize = 11,
             Padding = new Thickness(12, 6, 12, 6),
             HorizontalAlignment = HorizontalAlignment.Stretch,
         };
-        ToolTipService.SetToolTip(saveDefaultBtn, "Save current slider values as your default RTX HDR preset");
+        ToolTipService.SetToolTip(saveDefaultBtn, "Сохранить текущие ползунки как пресет RTX HDR по умолчанию");
         var setDefaultBtn = new Button
         {
-            Content = "Set Default",
+            Content = "Установить по умолчанию",
             FontSize = 11,
             Padding = new Thickness(12, 6, 12, 6),
             HorizontalAlignment = HorizontalAlignment.Stretch,
             IsEnabled = hasDefaults,
         };
-        ToolTipService.SetToolTip(setDefaultBtn, hasDefaults ? "Apply your saved default preset to the sliders" : "No default saved yet — use 'Save as Default' first");
+        ToolTipService.SetToolTip(setDefaultBtn, hasDefaults ? "Применить сохранённый пресет по умолчанию к ползункам" : "Значения по умолчанию ещё не сохранены — сначала нажмите «Сохранить по умолчанию»");
 
         saveDefaultBtn.Click += (s, ev) =>
         {
@@ -1412,7 +1412,7 @@ public sealed partial class MainWindow
                 File.WriteAllText(defaultsPath, JsonSerializer.Serialize(defaults,
                     new JsonSerializerOptions { WriteIndented = true }));
                 setDefaultBtn.IsEnabled = true;
-                ToolTipService.SetToolTip(setDefaultBtn, "Apply your saved default preset to the sliders");
+                ToolTipService.SetToolTip(setDefaultBtn, "Применить сохранённый пресет по умолчанию к ползункам");
                 saveDefaultBtn.Content = "Saved!";
             }
             catch (Exception ex) { CrashReporter.Log($"[RtxHdrConfigButton_Click] Failed to save defaults — {ex.Message}"); }
@@ -1453,10 +1453,10 @@ public sealed partial class MainWindow
         // ── Dialog ────────────────────────────────────────────────────────────
         var dialog = new ContentDialog
         {
-            Title = "RTX HDR Settings",
+            Title = "Настройки RTX HDR",
             Content = new ScrollViewer { Content = content, MaxHeight = 600, Padding = new Thickness(0, 0, 16, 0) },
-            PrimaryButtonText = "Apply",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Применить",
+            CloseButtonText = "Отмена",
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -1480,7 +1480,7 @@ public sealed partial class MainWindow
             dlssPresetService.SetRtxHdrDebanding(card.GameName, card.InstallPath, debanding);
             CrashReporter.Log($"[RtxHdrConfigButton_Click] Applied RTX HDR settings for '{card.GameName}': PeakNits={peakNits}, Contrast={contrastStored}, Sat={satStored}, MidGrey={middleGrey}, Deband=0x{debanding:X2}");
         });
-        card.ActionMessage = "✅ RTX HDR settings applied.";
+        card.ActionMessage = "✅ Настройки RTX HDR применены.";
         card.FadeMessage(m => card.ActionMessage = m, card.ActionMessage);
     }
 
@@ -1526,7 +1526,7 @@ public sealed partial class MainWindow
         // Open relimiter log
         var openLogBtn = new Button
         {
-            Content = "Open ReLimiter log",
+            Content = "Открыть журнал ReLimiter",
             HorizontalAlignment = HorizontalAlignment.Stretch,
             Background = UIFactory.Brush(ResourceKeys.SurfaceOverlayBrush),
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
@@ -1545,7 +1545,7 @@ public sealed partial class MainWindow
         // Copy relimiter log to clipboard (as file with correct name)
         var copyLogBtn = new Button
         {
-            Content = "Copy ReLimiter log to clipboard",
+            Content = "Скопировать журнал ReLimiter в буфер обмена",
             HorizontalAlignment = HorizontalAlignment.Stretch,
             Background = UIFactory.Brush(ResourceKeys.SurfaceOverlayBrush),
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
@@ -1570,7 +1570,7 @@ public sealed partial class MainWindow
                     dataPackage.SetStorageItems(new[] { storageFile });
                     Windows.ApplicationModel.DataTransfer.Clipboard.SetContent(dataPackage);
                     Windows.ApplicationModel.DataTransfer.Clipboard.Flush();
-                    card.UlActionMessage = $"✅ {Path.GetFileName(logFile)} copied to clipboard.";
+                    card.UlActionMessage = $"✅ Скопировано в буфер обмена: {Path.GetFileName(logFile)}.";
                     card.FadeMessage(m => card.UlActionMessage = m, card.UlActionMessage);
                 }
                 catch (Exception ex) { card.UlActionMessage = $"❌ {ex.Message}"; }
@@ -1585,7 +1585,7 @@ public sealed partial class MainWindow
         content.Children.Add(new Border { Height = 1, Background = UIFactory.Brush(ResourceKeys.BorderDefaultBrush), Margin = new Thickness(0, 10, 0, 2) });
         content.Children.Add(new TextBlock
         {
-            Text = "Frame Limiter",
+            Text = "Ограничитель FPS",
             FontSize = 13,
             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
             Margin = new Thickness(0, 4, 0, 0),
@@ -1597,7 +1597,7 @@ public sealed partial class MainWindow
         targetFpsPanel.ColumnDefinitions.Add(new ColumnDefinition { Width = GridLength.Auto });
         var targetFpsLabel = new TextBlock
         {
-            Text = "Target FPS",
+            Text = "Целевой FPS",
             FontSize = 12,
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             VerticalAlignment = VerticalAlignment.Center,
@@ -1605,7 +1605,7 @@ public sealed partial class MainWindow
         Grid.SetColumn(targetFpsLabel, 0);
         targetFpsPanel.Children.Add(targetFpsLabel);
         var targetFpsCombo = new ComboBox { FontSize = 12, MinWidth = 140, HorizontalAlignment = HorizontalAlignment.Right };
-        ToolTipService.SetToolTip(targetFpsCombo, "FPS cap for this game. Select a VRR preset or Custom for a manual value.");
+        ToolTipService.SetToolTip(targetFpsCombo, "Лимит FPS для этой игры. Выберите пресет VRR или «Свой», чтобы задать вручную.");
         Grid.SetColumn(targetFpsCombo, 1);
         targetFpsPanel.Children.Add(targetFpsCombo);
 
@@ -1653,7 +1653,7 @@ public sealed partial class MainWindow
         // Inline custom FPS input (shown when "Custom..." is selected)
         var customFpsPanel = new StackPanel { Orientation = Orientation.Horizontal, Spacing = 8, Visibility = Visibility.Collapsed };
         var customFpsBox = new TextBox { PlaceholderText = "20-1000", FontSize = 12, MinWidth = 100 };
-        var customFpsBtn = new Button { Content = "Set", FontSize = 12 };
+        var customFpsBtn = new Button { Content = "Задать", FontSize = 12 };
         customFpsPanel.Children.Add(customFpsBox);
         customFpsPanel.Children.Add(customFpsBtn);
 
@@ -1748,8 +1748,8 @@ public sealed partial class MainWindow
                     AuxInstallService.ApplyUlTargetFps(iniFile, newFps);
                     currentTargetFps = newFps;
                     card.UlActionMessage = newFps == 0
-                        ? "✅ Target FPS disabled for this game."
-                        : $"✅ Target FPS set to {newFps} for this game.";
+                        ? "✅ Целевой FPS отключён для этой игры."
+                        : $"✅ Целевой FPS для этой игры: {newFps}.";
                     card.FadeMessage(m => card.UlActionMessage = m, card.UlActionMessage);
                 }
                 catch (Exception ex) { card.UlActionMessage = $"❌ {ex.Message}"; }
@@ -1768,7 +1768,7 @@ public sealed partial class MainWindow
                     try
                     {
                         AuxInstallService.ApplyUlTargetFps(iniFile, customFps);
-                        card.UlActionMessage = $"✅ Target FPS set to {customFps} for this game.";
+                        card.UlActionMessage = $"✅ Целевой FPS для этой игры: {customFps}.";
                         card.FadeMessage(m => card.UlActionMessage = m, card.UlActionMessage);
                         RefreshFpsCombo(customFps);
                     }
@@ -1791,7 +1791,7 @@ public sealed partial class MainWindow
                         try
                         {
                             AuxInstallService.ApplyUlTargetFps(iniFile, customFps);
-                            card.UlActionMessage = $"✅ Target FPS set to {customFps} for this game.";
+                            card.UlActionMessage = $"✅ Целевой FPS для этой игры: {customFps}.";
                             card.FadeMessage(m => card.UlActionMessage = m, card.UlActionMessage);
                             RefreshFpsCombo(customFps);
                         }
@@ -1820,7 +1820,7 @@ public sealed partial class MainWindow
         content.Children.Add(new Border { Height = 1, Background = UIFactory.Brush(ResourceKeys.BorderDefaultBrush), Margin = new Thickness(0, 10, 0, 2) });
         content.Children.Add(new TextBlock
         {
-            Text = "Compatibility Settings",
+            Text = "Настройки совместимости",
             FontSize = 13,
             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
             Margin = new Thickness(0, 4, 0, 0),
@@ -1832,7 +1832,7 @@ public sealed partial class MainWindow
         dlssHooksPanel.ColumnDefinitions.Add(new ColumnDefinition { Width = GridLength.Auto });
         var dlssHooksLabel = new TextBlock
         {
-            Text = "DLSS Hooks",
+            Text = "Хуки DLSS",
             FontSize = 12,
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             VerticalAlignment = VerticalAlignment.Center,
@@ -1842,7 +1842,7 @@ public sealed partial class MainWindow
         var dlssHooksCombo = new ComboBox { FontSize = 12, MinWidth = 80, HorizontalAlignment = HorizontalAlignment.Right };
         dlssHooksCombo.Items.Add("Off");
         dlssHooksCombo.Items.Add("On");
-        ToolTipService.SetToolTip(dlssHooksCombo, "Shows DLSS version/preset info on the ReLimiter OSD. Disable if causing crashes.");
+        ToolTipService.SetToolTip(dlssHooksCombo, "Показывает версию/пресет DLSS в OSD ReLimiter. Отключите, если вызывает вылеты.");
         Grid.SetColumn(dlssHooksCombo, 1);
         dlssHooksPanel.Children.Add(dlssHooksCombo);
 
@@ -1876,8 +1876,8 @@ public sealed partial class MainWindow
                 {
                     AuxInstallService.ApplyUlDlssHooks(ulIniFile, dlssHooksCombo.SelectedIndex == 1);
                     card.UlActionMessage = dlssHooksCombo.SelectedIndex == 1
-                        ? "✅ DLSS Hooks enabled for this game."
-                        : "✅ DLSS Hooks disabled for this game.";
+                        ? "✅ Хуки DLSS включены для этой игры."
+                        : "✅ Хуки DLSS отключены для этой игры.";
                     card.FadeMessage(m => card.UlActionMessage = m, card.UlActionMessage);
                 }
                 catch (Exception ex) { card.UlActionMessage = $"❌ {ex.Message}"; }
@@ -1892,9 +1892,9 @@ public sealed partial class MainWindow
 
         var dialog = new ContentDialog
         {
-            Title = "ReLimiter Settings",
+            Title = "Настройки ReLimiter",
             Content = content,
-            CloseButtonText = "Close",
+            CloseButtonText = "Закрыть",
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -1930,9 +1930,9 @@ public sealed partial class MainWindow
 
         var dialog = new ContentDialog
         {
-            Title = "Display Commander Settings",
+            Title = "Настройки Display Commander",
             Content = content,
-            CloseButtonText = "Close",
+            CloseButtonText = "Закрыть",
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -1997,7 +1997,7 @@ public sealed partial class MainWindow
         };
         var fpsLabels = new[] { "Off" }.Concat(vrrPresetsOs.Select(p => p.Label)).ToArray();
         var fpsLimitCombo = new ComboBox { ItemsSource = fpsLabels, FontSize = 12, HorizontalAlignment = HorizontalAlignment.Stretch };
-        ToolTipService.SetToolTip(fpsLimitCombo, "Framerate limit using Reflex whenever possible. 'Off' disables the limit.");
+        ToolTipService.SetToolTip(fpsLimitCombo, "Ограничение частоты кадров с использованием Reflex, где возможно. «Выкл» отключает лимит.");
 
         // Read current value from OptiScaler.ini
         string currentFpsStr = "Off";
@@ -2022,12 +2022,12 @@ public sealed partial class MainWindow
         }
         fpsLimitCombo.SelectedItem = currentFpsStr;
 
-        AddRow(unifiedGrid, 0, "OptiScaler Version",
+        AddRow(unifiedGrid, 0, "Версия OptiScaler",
             new ComboBox { ItemsSource = new[] { "Stable", "Nightly", "DLSS NR" }, SelectedItem = ViewModel.GetOsVariant(card.GameName, card.Source ?? "") == "DlssNr" ? "DLSS NR" : ViewModel.GetOsVariant(card.GameName, card.Source ?? ""), FontSize = 12, HorizontalAlignment = HorizontalAlignment.Stretch },
-            "Framerate Limit", fpsLimitCombo);
+            "Лимит частоты кадров", fpsLimitCombo);
         // Grab the variant combo we just added
         var variantCombo = (ComboBox)unifiedGrid.Children.Cast<UIElement>().Where(c => c is ComboBox).First();
-        ToolTipService.SetToolTip(variantCombo, "Stable: official release. Nightly: daily build. DLSS NR: Neural Rendering fork with multi-pass NR support.");
+        ToolTipService.SetToolTip(variantCombo, "Stable: официальный релиз. Nightly: ежедневная сборка. DLSS NR: форк Neural Rendering с поддержкой многопроходного NR.");
 
         fpsLimitCombo.SelectionChanged += (s, ev) =>
         {
@@ -2047,21 +2047,21 @@ public sealed partial class MainWindow
         // Left combo: which API to configure. Right combo: upscaler for that API.
         static string[] GetUpscalerOptions(string api) => api switch
         {
-            "DX12"   => new[] { "Auto (Default)", "DLSS", "XeSS", "FSR 2.1", "FSR 2.2", "FSR 3.x / FFX" },
-            "Vulkan" => new[] { "Auto (Default)", "DLSS", "FSR 2.1", "FSR 2.2", "FSR 3.x / FFX", "XeSS", "FSR2.1 on DX12", "FSR3 on DX12" },
-            _        => new[] { "Auto (Default)", "DLSS", "FSR 2.2", "FSR 3.1", "XeSS (Arc only)", "XeSS on DX12", "FSR2.1 on DX12", "FSR2.2 on DX12", "FSR3 on DX12" }, // DX11
+            "DX12"   => new[] { "Авто (по умолчанию)", "DLSS", "XeSS", "FSR 2.1", "FSR 2.2", "FSR 3.x / FFX" },
+            "Vulkan" => new[] { "Авто (по умолчанию)", "DLSS", "FSR 2.1", "FSR 2.2", "FSR 3.x / FFX", "XeSS", "FSR2.1 на DX12", "FSR3 на DX12" },
+            _        => new[] { "Авто (по умолчанию)", "DLSS", "FSR 2.2", "FSR 3.1", "XeSS (только Arc)", "XeSS на DX12", "FSR2.1 на DX12", "FSR2.2 на DX12", "FSR3 на DX12" }, // DX11
         };
         static string UpscalerOptionToIni(string api, string display) => api switch
         {
             "DX12"   => display switch { "DLSS" => "dlss", "XeSS" => "xess", "FSR 2.1" => "fsr21", "FSR 2.2" => "fsr22", "FSR 3.x / FFX" => "ffx", _ => "auto" },
-            "Vulkan" => display switch { "DLSS" => "dlss", "FSR 2.1" => "fsr21", "FSR 2.2" => "fsr22", "FSR 3.x / FFX" => "ffx", "XeSS" => "xess", "FSR2.1 on DX12" => "fsr21_12", "FSR3 on DX12" => "ffx_12", _ => "auto" },
-            _        => display switch { "DLSS" => "dlss", "FSR 2.2" => "fsr22", "FSR 3.1" => "fsr31", "XeSS (Arc only)" => "xess", "XeSS on DX12" => "xess_12", "FSR2.1 on DX12" => "fsr21_12", "FSR2.2 on DX12" => "fsr22_12", "FSR3 on DX12" => "ffx_12", _ => "auto" },
+            "Vulkan" => display switch { "DLSS" => "dlss", "FSR 2.1" => "fsr21", "FSR 2.2" => "fsr22", "FSR 3.x / FFX" => "ffx", "XeSS" => "xess", "FSR2.1 на DX12" => "fsr21_12", "FSR3 на DX12" => "ffx_12", _ => "auto" },
+            _        => display switch { "DLSS" => "dlss", "FSR 2.2" => "fsr22", "FSR 3.1" => "fsr31", "XeSS (только Arc)" => "xess", "XeSS на DX12" => "xess_12", "FSR2.1 на DX12" => "fsr21_12", "FSR2.2 на DX12" => "fsr22_12", "FSR3 на DX12" => "ffx_12", _ => "auto" },
         };
         static string IniToUpscalerOption(string api, string ini) => api switch
         {
-            "DX12"   => ini switch { "dlss" => "DLSS", "xess" => "XeSS", "fsr21" => "FSR 2.1", "fsr22" => "FSR 2.2", "ffx" => "FSR 3.x / FFX", _ => "Auto (Default)" },
-            "Vulkan" => ini switch { "dlss" => "DLSS", "fsr21" => "FSR 2.1", "fsr22" => "FSR 2.2", "ffx" => "FSR 3.x / FFX", "xess" => "XeSS", "fsr21_12" => "FSR2.1 on DX12", "ffx_12" => "FSR3 on DX12", _ => "Auto (Default)" },
-            _        => ini switch { "dlss" => "DLSS", "fsr22" => "FSR 2.2", "fsr31" => "FSR 3.1", "xess" => "XeSS (Arc only)", "xess_12" => "XeSS on DX12", "fsr21_12" => "FSR2.1 on DX12", "fsr22_12" => "FSR2.2 on DX12", "ffx_12" => "FSR3 on DX12", _ => "Auto (Default)" },
+            "DX12"   => ini switch { "dlss" => "DLSS", "xess" => "XeSS", "fsr21" => "FSR 2.1", "fsr22" => "FSR 2.2", "ffx" => "FSR 3.x / FFX", _ => "Авто (по умолчанию)" },
+            "Vulkan" => ini switch { "dlss" => "DLSS", "fsr21" => "FSR 2.1", "fsr22" => "FSR 2.2", "ffx" => "FSR 3.x / FFX", "xess" => "XeSS", "fsr21_12" => "FSR2.1 на DX12", "ffx_12" => "FSR3 на DX12", _ => "Авто (по умолчанию)" },
+            _        => ini switch { "dlss" => "DLSS", "fsr22" => "FSR 2.2", "fsr31" => "FSR 3.1", "xess" => "XeSS (только Arc)", "xess_12" => "XeSS на DX12", "fsr21_12" => "FSR2.1 на DX12", "fsr22_12" => "FSR2.2 на DX12", "ffx_12" => "FSR3 на DX12", _ => "Авто (по умолчанию)" },
         };
         static string ApiToIniKey(string api) => api switch { "DX12" => "Dx12Upscaler", "Vulkan" => "VulkanUpscaler", _ => "Dx11Upscaler" };
 
@@ -2088,8 +2088,8 @@ public sealed partial class MainWindow
 
         var apiCombo = new ComboBox { ItemsSource = new[] { "DX11", "DX12", "Vulkan" }, SelectedItem = apiDefault, FontSize = 12, HorizontalAlignment = HorizontalAlignment.Stretch };
         var apiUpscalerCombo = new ComboBox { FontSize = 12, HorizontalAlignment = HorizontalAlignment.Stretch };
-        ToolTipService.SetToolTip(apiCombo, "Select which graphics API's upscaler to configure.");
-        ToolTipService.SetToolTip(apiUpscalerCombo, "Upscaler for the selected API. 'Auto' lets OptiScaler choose based on your GPU.");
+        ToolTipService.SetToolTip(apiCombo, "Выберите апскейлер какого графического API настроить.");
+        ToolTipService.SetToolTip(apiUpscalerCombo, "Апскейлер для выбранного API. «Авто» — OptiScaler выберет по вашей видеокарте.");
 
         // Populate upscaler combo for initial API and select current INI value
         void RefreshUpscalerCombo(string api)
@@ -2104,7 +2104,7 @@ public sealed partial class MainWindow
         RefreshUpscalerCombo(apiDefault);
 
         bool apiComboInitializing = true;
-        AddRow(unifiedGrid, 1, "Upscaler API", apiCombo, "Upscaler", apiUpscalerCombo);
+        AddRow(unifiedGrid, 1, "API апскейлера", apiCombo, "Upscaler", apiUpscalerCombo);
         apiComboInitializing = false;
 
         apiCombo.SelectionChanged += (s, ev) =>
@@ -2176,12 +2176,12 @@ public sealed partial class MainWindow
         if (isNightly || isDlssNr)
         {
             // ── INI value converters ───────────────────────────────────────
-            FgInputToIni  = (string d) => d switch { "OptiFG (Upscaler)" => "upscaler", "DLSSG via Streamline" => "dlssg", "DLSSG via Nvngx" => "nvngxfg", "FSR 3.1 FG" => "fsrfg", "FSR 3.0 FG" => "fsrfg30", "XeFG" => "xefg", _ => "auto" };
+            FgInputToIni  = (string d) => d switch { "OptiFG (апскейлер)" => "upscaler", "DLSSG через Streamline" => "dlssg", "DLSSG через Nvngx" => "nvngxfg", "FSR 3.1 FG" => "fsrfg", "FSR 3.0 FG" => "fsrfg30", "XeFG" => "xefg", _ => "auto" };
             FgOutputToIni = (string d) => d switch { "FSR FG" => "fsrfg", "DLSSG" => "dlssg", "XeFG" => "xefg", _ => "auto" };
             FgNvngxToIni  = (string d) => d switch { "Nukem's" => "Nukems", "Enabler" => "Arturs", "FSR 3/4 FG" => "FFX", _ => "None" };
-            string IniToFgInput(string v) => v switch { "upscaler" => "OptiFG (Upscaler)", "dlssg" => "DLSSG via Streamline", "nvngxfg" => "DLSSG via Nvngx", "fsrfg" => "FSR 3.1 FG", "fsrfg30" => "FSR 3.0 FG", "xefg" => "XeFG", _ => "Auto (Default)" };
-            string IniToFgOutput(string v) => v switch { "fsrfg" => "FSR FG", "dlssg" => "DLSSG", "xefg" => "XeFG", _ => "Auto (Default)" };
-            string IniToFgNvngx(string v) => v switch { "Nukems" => "Nukem's", "Arturs" => "Enabler", "FFX" => "FSR 3/4 FG", _ => "None (Real DLSSG)" };
+            string IniToFgInput(string v) => v switch { "upscaler" => "OptiFG (апскейлер)", "dlssg" => "DLSSG через Streamline", "nvngxfg" => "DLSSG через Nvngx", "fsrfg" => "FSR 3.1 FG", "fsrfg30" => "FSR 3.0 FG", "xefg" => "XeFG", _ => "Авто (по умолчанию)" };
+            string IniToFgOutput(string v) => v switch { "fsrfg" => "FSR FG", "dlssg" => "DLSSG", "xefg" => "XeFG", _ => "Авто (по умолчанию)" };
+            string IniToFgNvngx(string v) => v switch { "Nukems" => "Nukem's", "Arturs" => "Enabler", "FFX" => "FSR 3/4 FG", _ => "Нет (настоящий DLSSG)" };
 
             // Separator row between version and nightly settings (spans all 4 columns)
             unifiedGrid.RowDefinitions.Add(new RowDefinition { Height = GridLength.Auto });
@@ -2191,7 +2191,7 @@ public sealed partial class MainWindow
 
             // Section heading: Frame Generation Settings
             unifiedGrid.RowDefinitions.Add(new RowDefinition { Height = GridLength.Auto });
-            var fgHeading = new TextBlock { Text = "Frame Generation Settings", FontSize = 13, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush), Margin = new Thickness(0, 2, 0, 0) };
+            var fgHeading = new TextBlock { Text = "Настройки генерации кадров", FontSize = 13, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush), Margin = new Thickness(0, 2, 0, 0) };
             Grid.SetRow(fgHeading, 3); Grid.SetColumn(fgHeading, 0); Grid.SetColumnSpan(fgHeading, 4);
             unifiedGrid.Children.Add(fgHeading);
 
@@ -2210,12 +2210,12 @@ public sealed partial class MainWindow
 
             bool combinedOn = ViewModel.GetOsDeployStreamline(card.GameName, card.Source ?? "");
             combinedCombo = new ComboBox { ItemsSource = new[] { "No", "Yes" }, SelectedItem = combinedOn ? "Yes" : "No" };
-            ToolTipService.SetToolTip(combinedCombo, "Deploys Streamline and DLSS Enabler to the game's OptiScaler folder. Required for DLSS Frame Generation with OptiScaler.");
+            ToolTipService.SetToolTip(combinedCombo, "Разворачивает Streamline и DLSS Enabler в папку OptiScaler игры. Необходимо для генерации кадров DLSS вместе с OptiScaler.");
             var slVersionCombo = new ComboBox { ItemsSource = slVersions.Count > 0 ? (IEnumerable<string>)slVersions : new[] { slVersionDefault }, SelectedItem = slVersionDefault, IsEnabled = combinedOn };
-            AddRow(unifiedGrid, 4, "Streamline/DLSS Enabler", combinedCombo, "Streamline Version", slVersionCombo);
+            AddRow(unifiedGrid, 4, "Streamline/DLSS Enabler", combinedCombo, "Версия Streamline", slVersionCombo);
 
             // Row 4: FG Input (left) | HUD Fix (right)
-            fgInputCombo = new ComboBox { ItemsSource = new[] { "Auto (Default)", "OptiFG (Upscaler)", "DLSSG via Streamline", "DLSSG via Nvngx", "FSR 3.1 FG", "FSR 3.0 FG", "XeFG" }, SelectedItem = IniToFgInput(ViewModel.GetOsFgInput(card.GameName, card.Source ?? "")) };
+            fgInputCombo = new ComboBox { ItemsSource = new[] { "Авто (по умолчанию)", "OptiFG (апскейлер)", "DLSSG через Streamline", "DLSSG через Nvngx", "FSR 3.1 FG", "FSR 3.0 FG", "XeFG" }, SelectedItem = IniToFgInput(ViewModel.GetOsFgInput(card.GameName, card.Source ?? "")) };
 
             // Read HUD Fix from OptiScaler.ini
             string hudFixCurrent = "auto";
@@ -2238,19 +2238,19 @@ public sealed partial class MainWindow
                                : hudFixCurrent.Equals("false", StringComparison.OrdinalIgnoreCase) ? "Off"
                                : "Default";
             hudFixCombo = new ComboBox { ItemsSource = new[] { "Default", "On", "Off" }, SelectedItem = hudFixSelected };
-            ToolTipService.SetToolTip(hudFixCombo!, "HUD Fix: enables hudless resource tracking for Frame Generation. On = HUDFix=true in [OptiFG].");
+            ToolTipService.SetToolTip(hudFixCombo!, "HUD Fix: включает отслеживание ресурсов без HUD для генерации кадров. Вкл = HUDFix=true в [OptiFG].");
 
-            AddRow(unifiedGrid, 5, "FG Input", fgInputCombo!, "HUD Fix", hudFixCombo!);
+            AddRow(unifiedGrid, 5, "Вход FG", fgInputCombo!, "HUD Fix", hudFixCombo!);
 
             // Row 5: FG Output (left) | FG Nvngx Override (right)
-            fgOutputCombo = new ComboBox { ItemsSource = new[] { "Auto (Default)", "FSR FG", "DLSSG", "XeFG" }, SelectedItem = IniToFgOutput(ViewModel.GetOsFgOutput(card.GameName, card.Source ?? "")) };
+            fgOutputCombo = new ComboBox { ItemsSource = new[] { "Авто (по умолчанию)", "FSR FG", "DLSSG", "XeFG" }, SelectedItem = IniToFgOutput(ViewModel.GetOsFgOutput(card.GameName, card.Source ?? "")) };
             bool enablerAvail = true; // Always allow Enabler — requires Streamline deployed, user responsibility
-            var nvngxItems = new List<object> { "None (Real DLSSG)", "Nukem's", new ComboBoxItem { Content = "Enabler", IsEnabled = enablerAvail }, "FSR 3/4 FG" };
+            var nvngxItems = new List<object> { "Нет (настоящий DLSSG)", "Nukem's", new ComboBoxItem { Content = "Enabler", IsEnabled = enablerAvail }, "FSR 3/4 FG" };
             var currentNvngxDisplay = IniToFgNvngx(ViewModel.GetOsFgNvngxReplacement(card.GameName, card.Source ?? ""));
             object? nvngxSelected = nvngxItems.FirstOrDefault(i => i is ComboBoxItem cb ? (cb.Content as string) == currentNvngxDisplay : (i as string) == currentNvngxDisplay) ?? nvngxItems[0];
             fgNvngxCombo = new ComboBox { ItemsSource = nvngxItems, SelectedItem = nvngxSelected };
-            ToolTipService.SetToolTip(fgNvngxCombo!, "Only relevant when FG Output = DLSSG. Enabler requires Deploy Streamline + Deploy DLSS Enabler.");
-            AddRow(unifiedGrid, 6, "FG Output", fgOutputCombo!, "FG Nvngx Override", fgNvngxCombo!);
+            ToolTipService.SetToolTip(fgNvngxCombo!, "Важно только при FG Output = DLSSG. Для Enabler нужны «Развернуть Streamline» + «Развернуть DLSS Enabler».");
+            AddRow(unifiedGrid, 6, "Выход FG", fgOutputCombo!, "Переопределение FG Nvngx", fgNvngxCombo!);
 
             bool fgOutputIsDlssg = fgOutputCombo!.SelectedItem as string == "DLSSG";
             fgNvngxCombo!.Opacity = fgOutputIsDlssg ? 1.0 : 0.35;
@@ -2335,7 +2335,7 @@ public sealed partial class MainWindow
 
             // ── Additional Settings ────────────────────────────────────────
             content.Children.Add(MakeSeparator());
-            content.Children.Add(new TextBlock { Text = "Additional Settings", FontSize = 13, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush), Margin = new Thickness(0, 2, 0, 0) });
+            content.Children.Add(new TextBlock { Text = "Дополнительные настройки", FontSize = 13, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush), Margin = new Thickness(0, 2, 0, 0) });
 
             var addGrid = MakeSettingsGrid();
 
@@ -2362,22 +2362,22 @@ public sealed partial class MainWindow
             var srCurrent = ReadIniValue("DLSS", "RenderPresetForAll");
             var srSelected = srPresetMap.FirstOrDefault(p => p.Item2 == srCurrent).Item1 ?? "Default";
             srPresetCombo = new ComboBox { ItemsSource = srPresetMap.Select(p => p.Item1).ToArray(), SelectedItem = srSelected };
-            ToolTipService.SetToolTip(srPresetCombo!, "DLSS Super Resolution render preset. J-M are the recommended modern presets.");
+            ToolTipService.SetToolTip(srPresetCombo!, "Пресет рендеринга DLSS Super Resolution. J–M — рекомендуемые современные пресеты.");
 
             // DLSS RR preset: 3=D, 4=E
             rrPresetMap = new[] { ("Default", "auto"), ("D", "3"), ("E", "4") };
             var rrCurrent = ReadIniValue("DLSSD", "RenderPresetForAll");
             var rrSelected = rrPresetMap.FirstOrDefault(p => p.Item2 == rrCurrent).Item1 ?? "Default";
             rrPresetCombo = new ComboBox { ItemsSource = rrPresetMap.Select(p => p.Item1).ToArray(), SelectedItem = rrSelected };
-            ToolTipService.SetToolTip(rrPresetCombo!, "DLSS Ray Reconstruction render preset.");
+            ToolTipService.SetToolTip(rrPresetCombo!, "Пресет рендеринга DLSS Ray Reconstruction.");
 
-            AddRow(addGrid, 0, "DLSS SR Preset", srPresetCombo!, "DLSS RR Preset", rrPresetCombo!);
+            AddRow(addGrid, 0, "Пресет DLSS SR", srPresetCombo!, "Пресет DLSS RR", rrPresetCombo!);
 
             // Disable Flip Metering + Render Scale
             var flipCurrent = ReadIniValue("NvApi", "DisableFlipMetering");
             var flipSelected = flipCurrent == "true" ? "On" : "Default";
             flipCombo = new ComboBox { ItemsSource = new[] { "Default", "On" }, SelectedItem = flipSelected };
-            ToolTipService.SetToolTip(flipCombo!, "On: DisableFlipMetering=true — fixes thick frametime graph with NukemFG + fakenvapi.");
+            ToolTipService.SetToolTip(flipCombo!, "Вкл: DisableFlipMetering=true — исправляет толстый график фреймтайма с NukemFG + fakenvapi.");
 
             // Render Scale — UpscaleRatioOverride
             renderScaleMap = new[] {
@@ -2403,9 +2403,9 @@ public sealed partial class MainWindow
                 if (match.Item1 != null) rsSelected = match.Item1;
             }
             rsCombo = new ComboBox { ItemsSource = renderScaleMap.Select(p => p.Item1).ToArray(), SelectedItem = rsSelected };
-            ToolTipService.SetToolTip(rsCombo!, "Override the internal render resolution. Off = use in-game quality preset as-is.");
+            ToolTipService.SetToolTip(rsCombo!, "Переопределить внутреннее разрешение рендеринга. Выкл — использовать игровой пресет качества как есть.");
 
-            AddRow(addGrid, 1, "Render Scale", rsCombo!, "Disable Flip Metering", flipCombo!);
+            AddRow(addGrid, 1, "Масштаб рендеринга", rsCombo!, "Отключить Flip Metering", flipCombo!);
 
             content.Children.Add(addGrid);
 
@@ -2457,16 +2457,16 @@ public sealed partial class MainWindow
                 var ueGrid = MakeSettingsGrid();
 
                 var dmvCombo = new ComboBox { ItemsSource = new[] { "Default", "Off" }, SelectedItem = ViewModel.GetOsDilatedMotionVectorsOff(card.GameName, card.Source ?? "") ? "Off" : "Default" };
-                ToolTipService.SetToolTip(dmvCombo, "Off: r.NGX.DLSS.DilateMotionVectors=0 + r.Streamline.DilateMotionVectors=0");
+                ToolTipService.SetToolTip(dmvCombo, "Выкл: r.NGX.DLSS.DilateMotionVectors=0 + r.Streamline.DilateMotionVectors=0");
                 var fsrCombo = new ComboBox { ItemsSource = new[] { "None", "FSR2", "FSR3", "FSR3.1" }, SelectedItem = ViewModel.GetOsFsrCrashFix(card.GameName, card.Source ?? "") };
-                ToolTipService.SetToolTip(fsrCombo, "FSR2: r.FidelityFX.FSR2.UseNativeDX12=1\nFSR3: r.FidelityFX.FSR3.UseNativeDX12=1\nFSR3.1: above + r.FidelityFX.FSR3.UseRHI=0");
-                AddRow(ueGrid, 0, "Dilated Motion Vectors", dmvCombo, "FSR Crash Fix", fsrCombo);
+                ToolTipService.SetToolTip(fsrCombo, "FSR2: r.FidelityFX.FSR2.UseNativeDX12=1\nFSR3: r.FidelityFX.FSR3.UseNativeDX12=1\nFSR3.1: то же + r.FidelityFX.FSR3.UseRHI=0");
+                AddRow(ueGrid, 0, "Размытые векторы движения", dmvCombo, "Исправление вылетов FSR", fsrCombo);
 
                 var fgSwapCombo = new ComboBox { ItemsSource = new[] { "Default", "On" }, SelectedItem = ViewModel.GetOsFsrFgSwapchain(card.GameName, card.Source ?? "") ? "On" : "Default" };
-                ToolTipService.SetToolTip(fgSwapCombo, "On: r.FidelityFX.FI.OverrideSwapChainDX12=1");
+                ToolTipService.SetToolTip(fgSwapCombo, "Вкл: r.FidelityFX.FI.OverrideSwapChainDX12=1");
                 var upscalerCombo = new ComboBox { ItemsSource = new[] { "Default", "On" }, SelectedItem = ViewModel.GetOsUpscalerPlugin(card.GameName, card.Source ?? "") ? "On" : "Default" };
-                ToolTipService.SetToolTip(upscalerCombo, "On: r.AntiAliasingMethod=4 + r.TemporalAA.Upscaler=1");
-                AddRow(ueGrid, 1, "FSR-FG Swapchain", fgSwapCombo, "Upscaler Plugin", upscalerCombo);
+                ToolTipService.SetToolTip(upscalerCombo, "Вкл: r.AntiAliasingMethod=4 + r.TemporalAA.Upscaler=1");
+                AddRow(ueGrid, 1, "FSR-FG swapchain", fgSwapCombo, "Плагин апскейлера", upscalerCombo);
 
                 content.Children.Add(ueGrid);
 
@@ -2497,7 +2497,7 @@ public sealed partial class MainWindow
             if (isDlssNr)
             {
                 content.Children.Add(MakeSeparator());
-                content.Children.Add(new TextBlock { Text = "Neural Rendering Settings", FontSize = 13, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush), Margin = new Thickness(0, 2, 0, 0) });
+                content.Children.Add(new TextBlock { Text = "Настройки нейронного рендеринга", FontSize = 13, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush), Margin = new Thickness(0, 2, 0, 0) });
                 var nrGrid = MakeSettingsGrid();
 
                 // Helper: read a key from the [DlssNr] section of OptiScaler.ini
@@ -2537,7 +2537,7 @@ public sealed partial class MainWindow
                                  : nrEnabledRaw.Equals("false", StringComparison.OrdinalIgnoreCase) ? "Off"
                                  : "Default" };
                 ToolTipService.SetToolTip(nrEnabledCombo, "Enable DLSS Neural Rendering. Requires nvngx_dlssnr.dll + nvngx.dll_dlssnr.dll in game folder.");
-                AddRow(nrGrid, 0, "NR Runtime", nrRuntimeCombo!, "NR Enabled", nrEnabledCombo!);
+                AddRow(nrGrid, 0, "Среда выполнения NR", nrRuntimeCombo!, "NR включён", nrEnabledCombo!);
 
                 // Row 1: Run Before SR | Passes
                 var nrRunBeforeRaw = ReadNrIni("RunBeforeSR");
@@ -2545,26 +2545,26 @@ public sealed partial class MainWindow
                     SelectedItem = nrRunBeforeRaw.Equals("true", StringComparison.OrdinalIgnoreCase) ? "On"
                                  : nrRunBeforeRaw.Equals("false", StringComparison.OrdinalIgnoreCase) ? "Off"
                                  : "Default" };
-                ToolTipService.SetToolTip(nrRunBeforeSrCombo, "Run NR before Super Resolution. On = before upscaling; Off = after upscaling.");
+                ToolTipService.SetToolTip(nrRunBeforeSrCombo, "Запускать NR до Super Resolution. Вкл — до масштабирования; выкл — после.");
 
                 var nrPassesRaw = ReadNrIni("Passes");
                 nrPassesCombo = new ComboBox { ItemsSource = new[] { "Default", "1", "2", "3" },
                     SelectedItem = nrPassesRaw is "1" or "2" or "3" ? nrPassesRaw : "Default" };
-                ToolTipService.SetToolTip(nrPassesCombo, "Number of NR model passes. Each extra pass costs ~2x the model time but increases quality.");
-                AddRow(nrGrid, 1, "Run Before SR", nrRunBeforeSrCombo!, "Passes", nrPassesCombo!);
+                ToolTipService.SetToolTip(nrPassesCombo, "Количество проходов модели NR. Каждый дополнительный проход удваивает время модели, но повышает качество.");
+                AddRow(nrGrid, 1, "Запускать до SR", nrRunBeforeSrCombo!, "Passes", nrPassesCombo!);
 
                 // Row 2: Working Scale | Finished Picture
                 var nrScaleRaw = ReadNrIni("WorkingScale");
-                nrScaleMap = new[] { ("Default", "auto"), ("0.5x (half)", "0.5"), ("0.75x", "0.75"), ("1.0x (full)", "1.0"), ("1.5x (supersample)", "1.5") };
+                nrScaleMap = new[] { ("Default", "auto"), ("0.5x (половина)", "0.5"), ("0.75x", "0.75"), ("1.0x (полный)", "1.0"), ("1.5x (суперсэмплинг)", "1.5") };
                 var nrScaleSelected = nrScaleMap.FirstOrDefault(p => p.Item2 == nrScaleRaw).Item1 ?? "Default";
                 nrWorkingScaleCombo = new ComboBox { ItemsSource = nrScaleMap.Select(p => p.Item1).ToArray(), SelectedItem = nrScaleSelected };
-                ToolTipService.SetToolTip(nrWorkingScaleCombo, "Model work resolution as a fraction of frame size. Lower = faster; above 1.0 supersamples the model.");
+                ToolTipService.SetToolTip(nrWorkingScaleCombo, "Рабочее разрешение модели как доля от размера кадра. Ниже — быстрее; выше 1.0 — суперсэмплинг модели.");
 
                 var nrFinishedRaw = ReadNrIni("FinishedPicture");
                 nrFinishedPicCombo = new ComboBox { ItemsSource = new[] { "Default", "On" },
                     SelectedItem = nrFinishedRaw.Equals("true", StringComparison.OrdinalIgnoreCase) ? "On" : "Default" };
-                ToolTipService.SetToolTip(nrFinishedPicCombo, "Apply NR to the finished picture (after all game lighting and effects). Helps with green noise. DX12 native only.");
-                AddRow(nrGrid, 2, "Working Scale", nrWorkingScaleCombo!, "Apply to Finished Picture", nrFinishedPicCombo!);
+                ToolTipService.SetToolTip(nrFinishedPicCombo, "Применять NR к готовой картинке (после всего игрового освещения и эффектов). Помогает против зелёного шума. Только для нативного DX12.");
+                AddRow(nrGrid, 2, "Рабочий масштаб", nrWorkingScaleCombo!, "Применять к готовой картинке", nrFinishedPicCombo!);
 
                 content.Children.Add(nrGrid);
 
@@ -2644,7 +2644,7 @@ public sealed partial class MainWindow
 
         if (isNightly || isDlssNr)
         {
-            var presetsLabel = new TextBlock { Text = "Presets", FontSize = 13, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) };
+            var presetsLabel = new TextBlock { Text = "Пресеты", FontSize = 13, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush) };
             bottomBorder.Children.Add(presetsLabel);
 
             // Load global presets from disk
@@ -2678,13 +2678,13 @@ public sealed partial class MainWindow
 
                 var saveBtn = new Button
                 {
-                    Content = "Save",
+                    Content = "Сохранить",
                     FontSize = 11,
                     Width = 42,
                     Padding = new Thickness(4),
                     CornerRadius = new CornerRadius(4),
                 };
-                ToolTipService.SetToolTip(saveBtn, "Save current cog settings into this slot");
+                ToolTipService.SetToolTip(saveBtn, "Сохранить текущие настройки шестерёнки в этот слот");
 
                 // Stretch the textbox to fill remaining space
                 var nameRowGrid = new Grid { ColumnSpacing = 4 };
@@ -2706,7 +2706,7 @@ public sealed partial class MainWindow
                     IsEnabled = hasData,
                     Opacity = hasData ? 1.0 : 0.40,
                 };
-                ToolTipService.SetToolTip(applyBtn, hasData ? "Apply this preset to the current game" : "No preset saved yet — click Save to capture current settings");
+                ToolTipService.SetToolTip(applyBtn, hasData ? "Применить этот пресет к текущей игре" : "Пресет ещё не сохранён — нажмите «Сохранить», чтобы зафиксировать текущие настройки");
 
                 // ── Wire: TextBox name editing ─────────────────────────────
                 nameBox.TextChanged += (s, ev) =>
@@ -2770,7 +2770,7 @@ public sealed partial class MainWindow
                     applyBtn.IsEnabled = true;
                     applyBtn.Opacity = 1.0;
                     applyBtn.Content = p.Name ?? $"Slot {i + 1}";
-                    ToolTipService.SetToolTip(applyBtn, "Apply this preset to the current game");
+                    ToolTipService.SetToolTip(applyBtn, "Применить этот пресет к текущей игре");
                 };
 
                 // ── Wire: Apply button ─────────────────────────────────────
@@ -2986,9 +2986,9 @@ public sealed partial class MainWindow
 
         var dialog = new ContentDialog
         {
-            Title = "OptiScaler Settings",
+            Title = "Настройки OptiScaler",
             Content = rootGrid,
-            CloseButtonText = "Close",
+            CloseButtonText = "Закрыть",
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -3040,12 +3040,12 @@ public sealed partial class MainWindow
 
         var variantLabel = new TextBlock
         {
-            Text = "Variant",
+            Text = "Вариант",
             FontSize = 11,
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             VerticalAlignment = VerticalAlignment.Center,
         };
-        ToolTipService.SetToolTip(variantLabel, "Development: latest nightly build. Stable: last stable release. Lilium HDR: HDR-optimised variant (default).");
+        ToolTipService.SetToolTip(variantLabel, "Development: новейшая ночная сборка. Stable: последний стабильный релиз. Lilium HDR: вариант с оптимизацией HDR (по умолчанию).");
         Grid.SetRow(variantLabel, 0); Grid.SetColumn(variantLabel, 0);
         variantGrid.Children.Add(variantLabel);
 
@@ -3069,14 +3069,14 @@ public sealed partial class MainWindow
 
         var liliumPresetLabel = new TextBlock
         {
-            Text = "Lilium Preset",
+            Text = "Пресет Lilium",
             FontSize = 11,
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             VerticalAlignment = VerticalAlignment.Center,
             Visibility = isLiliumSelected ? Visibility.Visible : Visibility.Collapsed,
         };
         ToolTipService.SetToolTip(liliumPresetLabel,
-            "Safest = swap chain only (near 100% compatible).\nHigher tiers upgrade back buffers and render targets — better HDR but may cause visual issues.");
+            "Самый безопасный = только цепочка обмена (почти 100% совместимость).\nБолее высокие уровни затрагивают буферы и цели рендеринга — лучше HDR, но возможны артефакты.");
         Grid.SetRow(liliumPresetLabel, 1); Grid.SetColumn(liliumPresetLabel, 0);
         variantGrid.Children.Add(liliumPresetLabel);
 
@@ -3154,7 +3154,7 @@ public sealed partial class MainWindow
         // ── Deploy dxvk.conf button ───────────────────────────────────────────
         var deployBtn = new Button
         {
-            Content = "Deploy dxvk.conf",
+            Content = "Развернуть dxvk.conf",
             HorizontalAlignment = HorizontalAlignment.Stretch,
             Background = UIFactory.Brush(ResourceKeys.AccentBlueBgBrush),
             Foreground = UIFactory.Brush(ResourceKeys.AccentBlueBrush),
@@ -3176,12 +3176,12 @@ public sealed partial class MainWindow
         // Row 0: Prefer DXGI Swapchain
         var presentLabel = new TextBlock
         {
-            Text = "Prefer DXGI Swapchain",
+            Text = "Предпочитать DXGI swapchain",
             FontSize = 11,
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             VerticalAlignment = VerticalAlignment.Center,
         };
-        ToolTipService.SetToolTip(presentLabel, "Sets Vulkan/OpenGL Present Method to 'Preferred layered on DXGI Swapchain' in the NVIDIA driver profile. Recommended for DXVK — improves compatibility and HDR support.");
+        ToolTipService.SetToolTip(presentLabel, "Задаёт в профиле драйвера NVIDIA метод вывода Vulkan/OpenGL «Preferred layered on DXGI Swapchain». Рекомендуется для DXVK — улучшает совместимость и поддержку HDR.");
         Grid.SetRow(presentLabel, 0); Grid.SetColumn(presentLabel, 0);
         presentGrid.Children.Add(presentLabel);
 
@@ -3202,12 +3202,12 @@ public sealed partial class MainWindow
         // Row 1: DXVK as Native Flags
         var flagsLabel = new TextBlock
         {
-            Text = "DXVK as Native",
+            Text = "DXVK как нативный",
             FontSize = 11,
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             VerticalAlignment = VerticalAlignment.Center,
         };
-        ToolTipService.SetToolTip(flagsLabel, "Standard: Treat DXVK as Native (0x000802A5). Alternative: Allow DXVK Promotion + DirectFlip (0x00080004). Only active when Prefer DXGI Swapchain is Yes.");
+        ToolTipService.SetToolTip(flagsLabel, "Стандартный: считать DXVK нативным (0x000802A5). Альтернативный: разрешить DXVK Promotion + DirectFlip (0x00080004). Действует только при «Предпочитать DXGI swapchain» = Да.");
         Grid.SetRow(flagsLabel, 1); Grid.SetColumn(flagsLabel, 0);
         presentGrid.Children.Add(flagsLabel);
 
@@ -3218,7 +3218,7 @@ public sealed partial class MainWindow
         };
         var flagsCombo = new ComboBox { FontSize = 11, HorizontalAlignment = HorizontalAlignment.Stretch };
         foreach (var (lbl, _) in flagOptions) flagsCombo.Items.Add(lbl);
-        ToolTipService.SetToolTip(flagsCombo, "Standard (0x000802A5): Treat DXVK as Native\nAlternative (0x00080004): Allow DXVK Promotion + DirectFlip");
+        ToolTipService.SetToolTip(flagsCombo, "Стандартный (0x000802A5): считать DXVK нативным\nАльтернативный (0x00080004): разрешить DXVK Promotion + DirectFlip");
 
         var currentFlags = _dlssPresetService.GetVulkanPresentMethodFlags(card.GameName, card.InstallPath ?? "");
         bool presentIsYes = currentPresentMethod == 0x00000001;
@@ -3269,9 +3269,9 @@ public sealed partial class MainWindow
 
         var dialog = new ContentDialog
         {
-            Title = "DXVK Settings",
+            Title = "Настройки DXVK",
             Content = content,
-            CloseButtonText = "Close",
+            CloseButtonText = "Закрыть",
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
diff --git a/RenoDXCommander/MainWindow.Events.Install.cs b/RenoDXCommander/MainWindow.Events.Install.cs
index f389a08..6266e01 100644
--- a/RenoDXCommander/MainWindow.Events.Install.cs
+++ b/RenoDXCommander/MainWindow.Events.Install.cs
@@ -21,7 +21,7 @@ public sealed partial class MainWindow
         // Build the progress dialog
         var statusText = new TextBlock
         {
-            Text = "Preparing...",
+            Text = "Подготовка...",
             FontSize = 12,
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             TextWrapping = Microsoft.UI.Xaml.TextWrapping.Wrap,
@@ -39,7 +39,7 @@ public sealed partial class MainWindow
 
         var dialog = new ContentDialog
         {
-            Title = "Updating All Components",
+            Title = "Обновление всех компонентов",
             Content = panel,
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -52,39 +52,39 @@ public sealed partial class MainWindow
         {
             if (!ViewModel.Settings.GlobalSkipRsUpdates)
             {
-                statusText.Text = "Updating ReShade...";
+                statusText.Text = "Обновление ReShade...";
                 await ViewModel.UpdateAllReShadeAsync();
             }
             if (!ViewModel.Settings.GlobalSkipRdxUpdates)
             {
-                statusText.Text = "Updating RenoDX...";
+                statusText.Text = "Обновление RenoDX...";
                 await ViewModel.UpdateAllRenoDxAsync();
             }
             if (!ViewModel.Settings.GlobalSkipUlUpdates)
             {
-                statusText.Text = "Updating ReLimiter...";
+                statusText.Text = "Обновление ReLimiter...";
                 await ViewModel.UpdateAllUlAsync();
             }
             if (!ViewModel.Settings.GlobalSkipDcUpdates)
             {
-                statusText.Text = "Updating Display Commander...";
+                statusText.Text = "Обновление Display Commander...";
                 await ViewModel.UpdateAllDcAsync();
             }
             if (!ViewModel.Settings.GlobalSkipOsUpdates)
             {
-                statusText.Text = "Updating OptiScaler...";
+                statusText.Text = "Обновление OptiScaler...";
                 await ViewModel.UpdateAllOsAsync();
             }
             if (!ViewModel.Settings.GlobalSkipRefUpdates)
             {
-                statusText.Text = "Updating RE Framework...";
+                statusText.Text = "Обновление RE Framework...";
                 await ViewModel.UpdateAllRefAsync();
             }
-            statusText.Text = "Updating DXVK...";
+            statusText.Text = "Обновление DXVK...";
             await ViewModel.UpdateAllDxvkAsync();
-            statusText.Text = "Updating Luma...";
+            statusText.Text = "Обновление Luma...";
             await ViewModel.UpdateAllLumaAsync();
-            statusText.Text = "Updating DOF Fix...";
+            statusText.Text = "Обновление DOF Fix...";
             await ViewModel.UpdateAllDofFixAsync();
         }
         catch (Exception ex)
@@ -197,12 +197,12 @@ public sealed partial class MainWindow
         // Build DXVK info content with game-specific notes from manifest
         var content = "DXVK translates DirectX 8/9/10/11 API calls into Vulkan.\n\n"
             + "Benefits:\n"
-            + "• Enables ReShade compute shaders on older DX games\n"
-            + "• May improve performance and reduce shader stutter\n"
-            + "• Enables HDR output via dxvk.conf\n"
-            + "• Borderless fullscreen recommended over exclusive fullscreen\n\n"
-            + "⚠ Anti-cheat games may ban players using DXVK.\n"
-            + "⚠ Game overlays (Steam, NVIDIA, RTSS) may conflict.";
+            + "• Включает compute-шейдеры ReShade в старых DX-играх\n"
+            + "• Может повысить производительность и снизить фризы шейдеров\n"
+            + "• Включает HDR-вывод через dxvk.conf\n"
+            + "• Рекомендуется окно без рамки вместо исключительного полноэкранного режима\n\n"
+            + "⚠ В играх с античитом за DXVK могут забанить.\n"
+            + "⚠ Игровые оверлеи (Steam, NVIDIA, RTSS) могут конфликтовать.";
 
         // Append game-specific notes from manifest
         var manifest = ViewModel.Manifest;
@@ -215,7 +215,7 @@ public sealed partial class MainWindow
 
         var dialog = new ContentDialog
         {
-            Title = "ℹ DXVK Info",
+            Title = "ℹ Информация о DXVK",
             Content = new TextBlock
             {
                 Text = content,
@@ -301,7 +301,7 @@ public sealed partial class MainWindow
         if (card.DofFixIsInstalling || string.IsNullOrEmpty(card.InstallPath)) return;
 
         card.DofFixIsInstalling = true;
-        card.DofFixActionMessage = "Installing DOF Fix...";
+        card.DofFixActionMessage = "Установка DOF Fix...";
         card.DofFixProgress = 0;
         try
         {
@@ -315,13 +315,13 @@ public sealed partial class MainWindow
             {
                 card.DofFixInstalledVersion = _dofFixService.StagedVersion;
                 card.DofFixStatus = Models.GameStatus.Installed;
-                card.DofFixActionMessage = "✅ DOF Fix installed!";
+                card.DofFixActionMessage = "✅ DOF Fix установлен!";
                 card.NotifyAll();
                 card.FadeMessage(m => card.DofFixActionMessage = m, card.DofFixActionMessage);
             }
             else
             {
-                card.DofFixActionMessage = "❌ Install failed";
+                card.DofFixActionMessage = "❌ Не удалось установить";
             }
         }
         catch (Exception ex)
@@ -344,13 +344,13 @@ public sealed partial class MainWindow
         {
             card.DofFixStatus = Models.GameStatus.NotInstalled;
             card.DofFixInstalledVersion = null;
-            card.DofFixActionMessage = "✖ DOF Fix removed.";
+            card.DofFixActionMessage = "✖ DOF Fix удалён.";
             card.NotifyAll();
             card.FadeMessage(m => card.DofFixActionMessage = m, card.DofFixActionMessage);
         }
         else
         {
-            card.DofFixActionMessage = "❌ Uninstall failed";
+            card.DofFixActionMessage = "❌ Не удалось удалить";
         }
     }
 
@@ -367,12 +367,12 @@ public sealed partial class MainWindow
 
         var dialog = new ContentDialog
         {
-            Title = "UE DOF Fix — Release Notes",
+            Title = "UE DOF Fix — примечания к релизу",
             Content = new ScrollViewer
             {
                 Content = new TextBlock
                 {
-                    Text = notes ?? "No release notes available.",
+                    Text = notes ?? "Примечания к релизу недоступны.",
                     TextWrapping = Microsoft.UI.Xaml.TextWrapping.Wrap,
                     IsTextSelectionEnabled = true,
                 },
@@ -389,10 +389,10 @@ public sealed partial class MainWindow
     {
         var dialog = new ContentDialog
         {
-            Title = "DOF Fix Settings",
+            Title = "Настройки DOF Fix",
             Content = new TextBlock
             {
-                Text = "No configurable settings available for this component.",
+                Text = "Для этого компонента нет настраиваемых параметров.",
                 TextWrapping = Microsoft.UI.Xaml.TextWrapping.Wrap,
             },
             CloseButtonText = "OK",
@@ -492,14 +492,14 @@ public sealed partial class MainWindow
         {
             var dontShowCheck = new CheckBox
             {
-                Content = "Don't show this again",
+                Content = "Больше не показывать",
                 FontSize = 12,
                 Margin = new Thickness(0, 8, 0, 0),
             };
             var panel = new StackPanel { Spacing = 8 };
             panel.Children.Add(new TextBlock
             {
-                Text = "This toggles the engine version to Unreal Engine 5.0–5.6, making this game eligible for the DOF Fix addon.\n\nUse this when RHI cannot detect the UE version automatically (e.g. Game Pass games).",
+                Text = "Переключает версию движка на Unreal Engine 5.0–5.6, делая игру подходящей для аддона DOF Fix.\n\nИспользуйте, когда RHI не может сам определить версию UE (например, игры Game Pass).",
                 TextWrapping = Microsoft.UI.Xaml.TextWrapping.Wrap,
                 FontSize = 12,
             });
@@ -507,10 +507,10 @@ public sealed partial class MainWindow
 
             var dialog = new ContentDialog
             {
-                Title = "Engine Version Override",
+                Title = "Переопределение версии движка",
                 Content = panel,
-                PrimaryButtonText = "Continue",
-                CloseButtonText = "Cancel",
+                PrimaryButtonText = "Продолжить",
+                CloseButtonText = "Отмена",
                 XamlRoot = Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
             };
@@ -1035,14 +1035,14 @@ public sealed partial class MainWindow
                 var panel = new StackPanel { Spacing = 8 };
                 panel.Children.Add(new TextBlock
                 {
-                    Text = $"Remove {card.GameName} from RHI?",
+                    Text = $"Удалить {card.GameName} из RHI?",
                     FontSize = 13,
                     FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
                     Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
                 });
                 panel.Children.Add(new TextBlock
                 {
-                    Text = $"The following components are installed: {componentList}.",
+                    Text = $"Установлены следующие компоненты: {componentList}.",
                     TextWrapping = Microsoft.UI.Xaml.TextWrapping.Wrap,
                     FontSize = 12,
                     Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
@@ -1050,7 +1050,7 @@ public sealed partial class MainWindow
 
                 var uninstallCheck = new CheckBox
                 {
-                    Content = "Also uninstall all RHI-managed components from game folder",
+                    Content = "Также удалить все компоненты RHI из папки игры",
                     FontSize = 12,
                     IsChecked = false,
                 };
@@ -1058,10 +1058,10 @@ public sealed partial class MainWindow
 
                 var dialog = new ContentDialog
                 {
-                    Title = "Remove Game",
+                    Title = "Удалить игру",
                     Content = panel,
-                    PrimaryButtonText = "Remove",
-                    CloseButtonText = "Cancel",
+                    PrimaryButtonText = "Удалить",
+                    CloseButtonText = "Отмена",
                     XamlRoot = Content.XamlRoot,
                     RequestedTheme = ElementTheme.Dark,
                 };
@@ -1074,15 +1074,15 @@ public sealed partial class MainWindow
                 // No components installed — simple confirm
                 var dialog = new ContentDialog
                 {
-                    Title = "Remove Game",
+                    Title = "Удалить игру",
                     Content = new TextBlock
                     {
-                        Text = $"Remove {card.GameName} from RHI?",
+                        Text = $"Удалить {card.GameName} из RHI?",
                         FontSize = 12,
                         TextWrapping = Microsoft.UI.Xaml.TextWrapping.Wrap,
                     },
-                    PrimaryButtonText = "Remove",
-                    CloseButtonText = "Cancel",
+                    PrimaryButtonText = "Удалить",
+                    CloseButtonText = "Отмена",
                     XamlRoot = Content.XamlRoot,
                     RequestedTheme = ElementTheme.Dark,
                 };
@@ -1144,14 +1144,14 @@ public sealed partial class MainWindow
         var progressPanel = new StackPanel { Spacing = 8 };
         var progressRow = new StackPanel { Orientation = Microsoft.UI.Xaml.Controls.Orientation.Horizontal, Spacing = 12 };
         var progressRing = new ProgressRing { IsActive = true, Width = 20, Height = 20 };
-        var progressText = new TextBlock { Text = "Clearing caches...", FontSize = 13, Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush) };
+        var progressText = new TextBlock { Text = "Очистка кешей...", FontSize = 13, Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush) };
         progressRow.Children.Add(progressRing);
         progressRow.Children.Add(progressText);
         progressPanel.Children.Add(progressRow);
 
         var progressDialog = new ContentDialog
         {
-            Title = "Full Refresh",
+            Title = "Полное обновление",
             Content = progressPanel,
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
diff --git a/RenoDXCommander/MainWindow.Events.Settings.cs b/RenoDXCommander/MainWindow.Events.Settings.cs
index a0e5519..989fb4b 100644
--- a/RenoDXCommander/MainWindow.Events.Settings.cs
+++ b/RenoDXCommander/MainWindow.Events.Settings.cs
@@ -28,8 +28,8 @@ public sealed partial class MainWindow
             {
                 var dlg = new ContentDialog
                 {
-                    Title = "RHI is up to date",
-                    Content = $"You're running v{Services.CrashReporter.AppVersion} — no updates available.",
+                    Title = "RHI актуален",
+                    Content = $"У вас v{Services.CrashReporter.AppVersion} — обновлений нет.",
                     CloseButtonText = "OK",
                     XamlRoot = Content.XamlRoot,
                     RequestedTheme = ElementTheme.Dark,
@@ -215,7 +215,7 @@ public sealed partial class MainWindow
         {
             DlssDefaultsSummaryPanel.Children.Add(new TextBlock
             {
-                Text = "No defaults configured yet.",
+                Text = "Значения по умолчанию ещё не заданы.",
                 FontSize = 11,
                 Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush),
             });
@@ -234,7 +234,7 @@ public sealed partial class MainWindow
 
         var srCol = new StackPanel { Spacing = 2 };
         srCol.Children.Add(new TextBlock { Text = "DLSS", FontSize = 10, FontWeight = Microsoft.UI.Text.FontWeights.SemiBold, Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush) });
-        if (s.DefaultSrDriverOverride) srCol.Children.Add(MakeSummaryText("NVIDIA Override"));
+        if (s.DefaultSrDriverOverride) srCol.Children.Add(MakeSummaryText("Переопределение NVIDIA"));
         else if (!string.IsNullOrEmpty(s.DefaultDlssVersion)) srCol.Children.Add(MakeSummaryText(s.DefaultDlssVersion));
         if (s.DefaultSrPreset != 0) srCol.Children.Add(MakeSummaryText($"Preset {DlssPresetService.SrPresets.FirstOrDefault(p => p.Value == s.DefaultSrPreset).Name ?? "?"}"));
         if (s.DefaultSrRenderScale != 0) srCol.Children.Add(MakeSummaryText($"{s.DefaultSrRenderScale}%"));
@@ -245,7 +245,7 @@ public sealed partial class MainWindow
 
         var rrCol = new StackPanel { Spacing = 2 };
         rrCol.Children.Add(new TextBlock { Text = "RR", FontSize = 10, FontWeight = Microsoft.UI.Text.FontWeights.SemiBold, Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush) });
-        if (s.DefaultRrDriverOverride) rrCol.Children.Add(MakeSummaryText("NVIDIA Override"));
+        if (s.DefaultRrDriverOverride) rrCol.Children.Add(MakeSummaryText("Переопределение NVIDIA"));
         else if (!string.IsNullOrEmpty(s.DefaultDlssdVersion)) rrCol.Children.Add(MakeSummaryText(s.DefaultDlssdVersion));
         if (s.DefaultRrPreset != 0) rrCol.Children.Add(MakeSummaryText($"Preset {DlssPresetService.RrPresets.FirstOrDefault(p => p.Value == s.DefaultRrPreset).Name ?? "?"}"));
         if (s.DefaultRrRenderScale != 0) rrCol.Children.Add(MakeSummaryText($"{s.DefaultRrRenderScale}%"));
@@ -256,7 +256,7 @@ public sealed partial class MainWindow
 
         var fgCol = new StackPanel { Spacing = 2 };
         fgCol.Children.Add(new TextBlock { Text = "FG", FontSize = 10, FontWeight = Microsoft.UI.Text.FontWeights.SemiBold, Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush) });
-        if (s.DefaultFgDriverOverride) fgCol.Children.Add(MakeSummaryText("NVIDIA Override"));
+        if (s.DefaultFgDriverOverride) fgCol.Children.Add(MakeSummaryText("Переопределение NVIDIA"));
         else if (!string.IsNullOrEmpty(s.DefaultDlssgVersion)) fgCol.Children.Add(MakeSummaryText(s.DefaultDlssgVersion));
         if (s.DefaultFgPreset != 0) fgCol.Children.Add(MakeSummaryText($"Preset {DlssPresetService.FgPresets.FirstOrDefault(p => p.Value == s.DefaultFgPreset).Name ?? "?"}"));
         Grid.SetColumn(fgCol, 4);
@@ -394,10 +394,10 @@ public sealed partial class MainWindow
         var textBox = new TextBox { PlaceholderText = "20-1000", FontSize = 13 };
         var dialog = new ContentDialog
         {
-            Title = "Custom FPS Limit",
+            Title = "Свой лимит FPS",
             Content = textBox,
-            PrimaryButtonText = "Set",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Задать",
+            CloseButtonText = "Отмена",
             XamlRoot = this.Content.XamlRoot,
             RequestedTheme = Microsoft.UI.Xaml.ElementTheme.Dark,
         };
@@ -487,10 +487,10 @@ public sealed partial class MainWindow
         var textBox = new TextBox { PlaceholderText = "20-1000", FontSize = 13 };
         var dialog = new ContentDialog
         {
-            Title = "Custom DMFG Target FPS",
+            Title = "Свой целевой FPS DMFG",
             Content = textBox,
-            PrimaryButtonText = "Set",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Задать",
+            CloseButtonText = "Отмена",
             XamlRoot = this.Content.XamlRoot,
             RequestedTheme = Microsoft.UI.Xaml.ElementTheme.Dark,
         };
@@ -594,12 +594,12 @@ public sealed partial class MainWindow
         }
 
         var content = created.Count > 0
-            ? $"Created {created.Count} profile(s):\n\n• " + string.Join("\n• ", created)
-            : "All games already have NVIDIA profiles.";
+            ? $"Создано профилей: {created.Count}\n\n• " + string.Join("\n• ", created)
+            : "У всех игр уже есть профили NVIDIA.";
 
         var dialog = new ContentDialog
         {
-            Title = "Create Missing Profiles",
+            Title = "Создать отсутствующие профили",
             Content = new ScrollViewer
             {
                 Content = new TextBlock { Text = content, TextWrapping = TextWrapping.Wrap, FontSize = 12 },
@@ -630,8 +630,8 @@ public sealed partial class MainWindow
             {
                 await DialogService.ShowSafeAsync(new ContentDialog
                 {
-                    Title = "Export",
-                    Content = "No custom profile settings found to export.",
+                    Title = "Экспорт",
+                    Content = "Не найдено пользовательских настроек профиля для экспорта.",
                     CloseButtonText = "OK",
                     XamlRoot = Content.XamlRoot,
                     RequestedTheme = ElementTheme.Dark,
@@ -648,8 +648,8 @@ public sealed partial class MainWindow
 
             await DialogService.ShowSafeAsync(new ContentDialog
             {
-                Title = "Export Complete",
-                Content = $"Exported {data.Count} profile(s) to:\n{path}",
+                Title = "Экспорт завершён",
+                Content = $"Экспортировано профилей: {data.Count} в:\n{path}",
                 CloseButtonText = "OK",
                 XamlRoot = Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -660,8 +660,8 @@ public sealed partial class MainWindow
             CrashReporter.Log($"[ExportNvidiaProfiles_Click] Error: {ex.GetType().Name}: {ex.Message}");
             await DialogService.ShowSafeAsync(new ContentDialog
             {
-                Title = "Export Failed",
-                Content = $"An error occurred during export:\n{ex.Message}",
+                Title = "Не удалось экспортировать",
+                Content = $"Ошибка при экспорте:\n{ex.Message}",
                 CloseButtonText = "OK",
                 XamlRoot = Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -679,8 +679,8 @@ public sealed partial class MainWindow
         {
             await DialogService.ShowSafeAsync(new ContentDialog
             {
-                Title = "Import",
-                Content = $"No backup file found at:\n{path}\n\nBackup profiles first.",
+                Title = "Импорт",
+                Content = $"Файл резервной копии не найден:\n{path}\n\nСначала создайте копии профилей.",
                 CloseButtonText = "OK",
                 XamlRoot = Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -694,21 +694,21 @@ public sealed partial class MainWindow
 
         // Confirm before proceeding — importing overwrites driver profile settings irreversibly
         bool isAdmin = VulkanLayerService.IsRunningAsAdmin();
-        var warningText = "This will overwrite your current NVIDIA driver profile settings with the saved backup. This cannot be undone.";
+        var warningText = "Текущие настройки профиля драйвера NVIDIA будут перезаписаны резервной копией. Это нельзя отменить.";
         if (!isAdmin)
-            warningText += "\n\nYou are not running as admin. Settings that require elevated privileges (e.g. ReBAR) will not be restored. Run RHI as admin to import all settings.";
+            warningText += "\n\nВы запущены не от имени администратора. Параметры, требующие повышенных прав (например, ReBAR), не будут восстановлены. Запустите RHI от администратора, чтобы импортировать все настройки.";
 
         var confirmResult = await DialogService.ShowSafeAsync(new ContentDialog
         {
-            Title = "Restore Profiles",
+            Title = "Восстановить профили",
             Content = new TextBlock
             {
                 Text = warningText,
                 TextWrapping = TextWrapping.Wrap,
                 FontSize = 12,
             },
-            PrimaryButtonText = "Import",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Импорт",
+            CloseButtonText = "Отмена",
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         });
@@ -717,7 +717,7 @@ public sealed partial class MainWindow
         // Show progress dialog
         var progressText = new TextBlock
         {
-            Text = "Importing profiles...",
+            Text = "Импорт профилей...",
             FontSize = 12,
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             TextWrapping = TextWrapping.Wrap,
@@ -728,7 +728,7 @@ public sealed partial class MainWindow
 
         var progressDialog = new ContentDialog
         {
-            Title = "Importing...",
+            Title = "Импорт...",
             Content = progressPanel,
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -752,8 +752,8 @@ public sealed partial class MainWindow
 
         await DialogService.ShowSafeAsync(new ContentDialog
         {
-            Title = "Import Complete",
-            Content = $"Imported {count} profile(s) from backup.",
+            Title = "Импорт завершён",
+            Content = $"Импортировано профилей из резервной копии: {count}.",
             CloseButtonText = "OK",
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -770,7 +770,7 @@ public sealed partial class MainWindow
                 await DialogService.ShowSafeAsync(new ContentDialog
                 {
                     Title = "Digital Vibrance",
-                    Content = "No NVIDIA displays detected. Digital Vibrance requires an NVIDIA GPU with compatible drivers.",
+                    Content = "Дисплеи NVIDIA не обнаружены. Digital Vibrance требует GPU NVIDIA с совместимым драйвером.",
                     CloseButtonText = "OK",
                     XamlRoot = Content.XamlRoot,
                     RequestedTheme = ElementTheme.Dark,
@@ -793,7 +793,7 @@ public sealed partial class MainWindow
 
             var displayLabel = new TextBlock
             {
-                Text = "Monitor",
+                Text = "Монитор",
                 FontSize = 11,
                 Foreground = UIFactory.Brush(ResourceKeys.InlineDescriptionBrush),
             };
@@ -817,7 +817,7 @@ public sealed partial class MainWindow
 
             var sliderLabel = new TextBlock
             {
-                Text = "Digital Vibrance (0 = desaturated, 50 = neutral, 100 = maximum)",
+                Text = "Digital Vibrance (0 — обесцвечено, 50 — нейтрально, 100 — максимум)",
                 FontSize = 11,
                 Foreground = UIFactory.Brush(ResourceKeys.InlineDescriptionBrush),
                 TextWrapping = TextWrapping.Wrap,
@@ -853,7 +853,7 @@ public sealed partial class MainWindow
             // Buttons row
             var saveBtn = new Button
             {
-                Content = "Save",
+                Content = "Сохранить",
                 Background = UIFactory.Brush(ResourceKeys.AccentBlueBgBrush),
                 Foreground = UIFactory.Brush(ResourceKeys.AccentBlueBrush),
                 BorderBrush = UIFactory.Brush(ResourceKeys.AccentBlueBorderBrush),
@@ -864,7 +864,7 @@ public sealed partial class MainWindow
             };
             var resetBtn = new Button
             {
-                Content = "Reset to 50",
+                Content = "Сбросить до 50",
                 CornerRadius = new CornerRadius(6),
                 Padding = new Thickness(16, 6, 16, 6),
                 FontSize = 12,
@@ -903,7 +903,7 @@ public sealed partial class MainWindow
                     DigitalVibranceService.SetLevel(displays[idx].Index, 50);
                     settings.DigitalVibranceSettings[displays[idx].Index.ToString()] = 50;
                     ViewModel.SaveSettingsPublic();
-                    statusText.Text = $"Reset: Display {displays[idx].Name} = 50% (neutral)";
+                    statusText.Text = $"Сброс: дисплей {displays[idx].Name} = 50% (нейтрально)";
                 }
             };
 
@@ -928,7 +928,7 @@ public sealed partial class MainWindow
             {
                 Title = "Digital Vibrance",
                 Content = panel,
-                CloseButtonText = "Close",
+                CloseButtonText = "Закрыть",
                 XamlRoot = Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
             };
@@ -945,15 +945,15 @@ public sealed partial class MainWindow
     {
         var confirmDialog = new ContentDialog
         {
-            Title = "Reset all game profiles?",
+            Title = "Сбросить профили всех игр?",
             Content = new TextBlock
             {
-                Text = "This will remove ALL per-game NVIDIA driver profile overrides (DLSS/Streamline versions, presets, render scales, ReBAR, VSync, Smooth Motion, Low Latency, Power Mode — everything) AND reset global settings (Shader Cache, G-Sync, Refresh Rate, ReBAR) to defaults.\n\nAll profiles will return to NVIDIA factory defaults. This cannot be undone.",
+                Text = "Будут удалены ВСЕ переопределения профилей драйвера NVIDIA для игр (версии DLSS/Streamline, пресеты, масштабы рендеринга, ReBAR, VSync, Smooth Motion, Low Latency, режим питания — всё) И сброшены общие настройки (кеш шейдеров, G-Sync, частота обновления, ReBAR) до значений по умолчанию.\n\nВсе профили вернутся к заводским настройкам NVIDIA. Это нельзя отменить.",
                 TextWrapping = Microsoft.UI.Xaml.TextWrapping.Wrap,
                 FontSize = 13,
             },
-            PrimaryButtonText = "Reset All",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Сбросить всё",
+            CloseButtonText = "Отмена",
             DefaultButton = ContentDialogButton.Close,
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -972,7 +972,7 @@ public sealed partial class MainWindow
             // Show progress dialog
             var progressPanel = new StackPanel { Spacing = 8 };
             var progressRing = new ProgressRing { IsActive = true, Width = 20, Height = 20 };
-            var progressText = new TextBlock { Text = "Preparing...", FontSize = 13, Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush) };
+            var progressText = new TextBlock { Text = "Подготовка...", FontSize = 13, Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush) };
             var progressRow = new StackPanel { Orientation = Microsoft.UI.Xaml.Controls.Orientation.Horizontal, Spacing = 12 };
             progressRow.Children.Add(progressRing);
             progressRow.Children.Add(progressText);
@@ -980,7 +980,7 @@ public sealed partial class MainWindow
 
             var progressDialog = new ContentDialog
             {
-                Title = "Resetting...",
+                Title = "Сброс...",
                 Content = progressPanel,
                 XamlRoot = Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -1010,7 +1010,7 @@ public sealed partial class MainWindow
 
                 // Reset global profile
                 DispatcherQueue?.TryEnqueue(() =>
-                    progressText.Text = "Resetting global settings...");
+                    progressText.Text = "Сброс общих настроек...");
                 presetSvc.ResetGlobalProfile();
             });
 
@@ -1033,8 +1033,8 @@ public sealed partial class MainWindow
 
             await DialogService.ShowSafeAsync(new ContentDialog
             {
-                Title = "Profiles Reset",
-                Content = $"Reset {resetCount} game profile(s) and global settings to factory defaults.",
+                Title = "Профили сброшены",
+                Content = $"Сбросить профили игр ({resetCount}) и общие настройки к заводским значениям.",
                 CloseButtonText = "OK",
                 XamlRoot = Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -1045,7 +1045,7 @@ public sealed partial class MainWindow
             CrashReporter.Log($"[ResetAllNvidiaProfiles_Click] Error: {ex.GetType().Name}: {ex.Message}");
             await DialogService.ShowSafeAsync(new ContentDialog
             {
-                Title = "Reset Failed",
+                Title = "Не удалось сбросить",
                 Content = $"Error: {ex.Message}",
                 CloseButtonText = "OK",
                 XamlRoot = Content.XamlRoot,
@@ -1058,10 +1058,10 @@ public sealed partial class MainWindow
     {
         var confirmDialog = new ContentDialog
         {
-            Title = "Clear NVIDIA Shader Cache",
-            Content = "This will permanently delete the NVIDIA DXCache and GLCache folders. This cannot be undone.\n\nAll games will need to rebuild their shader caches on next launch, which may cause brief stuttering or longer load times the first time. This can fix shader corruption, persistent stuttering, or graphical issues after driver updates.",
-            PrimaryButtonText = "Clear",
-            CloseButtonText = "Cancel",
+            Title = "Очистить кеш шейдеров NVIDIA",
+            Content = "Папки DXCache и GLCache NVIDIA будут удалены безвозвратно.\n\nВсем играм потребуется ",
+            PrimaryButtonText = "Очистить",
+            CloseButtonText = "Отмена",
             DefaultButton = ContentDialogButton.Primary,
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -1102,12 +1102,12 @@ public sealed partial class MainWindow
         }
 
         var message = filesFailed > 0
-            ? $"Shader cache folders cleared. {filesDeleted} files deleted, {filesFailed} files skipped (in use by running games). Shaders will recompile on next game launch."
-            : $"Shader cache folders cleared. {filesDeleted} files deleted. Shaders will recompile on next game launch.";
+            ? $"Папки кеша шейдеров очищены. Удалено файлов: {filesDeleted}, пропущено: {filesFailed} (заняты запущенными играми). Шейдеры перекомпилируются при следующем запуске игры."
+            : $"Папки кеша шейдеров очищены. Удалено файлов: {filesDeleted}. Шейдеры перекомпилируются при следующем запуске игры.";
 
         await DialogService.ShowSafeAsync(new ContentDialog
         {
-            Title = "Shader Cache Cleared",
+            Title = "Кеш шейдеров очищен",
             Content = message,
             CloseButtonText = "OK",
             XamlRoot = Content.XamlRoot,
@@ -1216,7 +1216,7 @@ public sealed partial class MainWindow
 
         panel.Children.Add(new TextBlock
         {
-            Text = "Leave all unchecked to enable HDR on the primary display only.",
+            Text = "Если ничего не отмечать, HDR включится только на основном дисплее.",
             FontSize = 11,
             Foreground = UIFactory.Brush(ResourceKeys.InlineDescriptionBrush),
             Margin = new Thickness(0, 4, 0, 0),
@@ -1224,10 +1224,10 @@ public sealed partial class MainWindow
 
         var dialog = new ContentDialog
         {
-            Title = "Select HDR Monitors",
+            Title = "Выберите HDR-мониторы",
             Content = panel,
-            PrimaryButtonText = "Save",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Сохранить",
+            CloseButtonText = "Отмена",
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -1417,10 +1417,10 @@ public sealed partial class MainWindow
 
         var dialog = new ContentDialog
         {
-            Title   = "Output Colour Settings",
+            Title   = "Настройки вывода цвета",
             Content = ok
                 ? $"Applied to {display.Name}: {depthLabel}, {rangeLabel}."
-                : $"Failed to apply colour settings to {display.Name}. Check that RHI is running as administrator.",
+                : $"Не удалось применить настройки цвета к {display.Name}. Проверьте, что RHI запущен от администратора.",
             CloseButtonText = "OK",
             XamlRoot        = Content.XamlRoot,
             RequestedTheme  = ElementTheme.Dark,
@@ -1451,7 +1451,7 @@ public sealed partial class MainWindow
 
         panel.Children.Add(new TextBlock
         {
-            Text = "Leave all unchecked to change resolution on the primary display only.",
+            Text = "Если ничего не отмечать, разрешение сменится только на основном дисплее.",
             FontSize = 11,
             Foreground = UIFactory.Brush(ResourceKeys.InlineDescriptionBrush),
             Margin = new Thickness(0, 4, 0, 0),
@@ -1459,10 +1459,10 @@ public sealed partial class MainWindow
 
         var dialog = new ContentDialog
         {
-            Title = "Select Resolution Monitors",
+            Title = "Выберите мониторы для смены разрешения",
             Content = panel,
-            PrimaryButtonText = "Save",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Сохранить",
+            CloseButtonText = "Отмена",
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -1615,7 +1615,7 @@ public sealed partial class MainWindow
         var enablePanel = new StackPanel { Orientation = Orientation.Horizontal, Spacing = 10 };
         enablePanel.Children.Add(new TextBlock
         {
-            Text = "Auto-apply peak nits on deploy",
+            Text = "Автоматически применять пиковую яркость при развертывании",
             FontSize = 12,
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             VerticalAlignment = VerticalAlignment.Center,
@@ -1630,22 +1630,22 @@ public sealed partial class MainWindow
         // Preset checkboxes
         content.Children.Add(new TextBlock
         {
-            Text = "Apply to presets:",
+            Text = "Применить к пресетам:",
             FontSize = 12,
             Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             Margin = new Thickness(0, 4, 0, 0),
         });
 
-        var cb1 = new CheckBox { Content = "Preset 1", IsChecked = settings.PeakNitsPresets.Contains(1), FontSize = 12 };
-        var cb2 = new CheckBox { Content = "Preset 2", IsChecked = settings.PeakNitsPresets.Contains(2), FontSize = 12 };
-        var cb3 = new CheckBox { Content = "Preset 3", IsChecked = settings.PeakNitsPresets.Contains(3), FontSize = 12 };
+        var cb1 = new CheckBox { Content = "Пресет 1", IsChecked = settings.PeakNitsPresets.Contains(1), FontSize = 12 };
+        var cb2 = new CheckBox { Content = "Пресет 2", IsChecked = settings.PeakNitsPresets.Contains(2), FontSize = 12 };
+        var cb3 = new CheckBox { Content = "Пресет 3", IsChecked = settings.PeakNitsPresets.Contains(3), FontSize = 12 };
         content.Children.Add(cb1);
         content.Children.Add(cb2);
         content.Children.Add(cb3);
 
         content.Children.Add(new TextBlock
         {
-            Text = "Unchecked presets keep their existing per-preset values.",
+            Text = "У неотмеченных пресетов сохраняются текущие индивидуальные значения.",
             FontSize = 11,
             Foreground = UIFactory.Brush(ResourceKeys.InlineDescriptionBrush),
             TextWrapping = TextWrapping.Wrap,
@@ -1653,10 +1653,10 @@ public sealed partial class MainWindow
 
         var dialog = new ContentDialog
         {
-            Title = "Peak Nits Settings",
+            Title = "Настройки пиковой яркости",
             Content = content,
-            PrimaryButtonText = "Save",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Сохранить",
+            CloseButtonText = "Отмена",
             DefaultButton = ContentDialogButton.Primary,
             XamlRoot = (sender as FrameworkElement)?.XamlRoot ?? Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -1718,7 +1718,7 @@ public sealed partial class MainWindow
             {
                 wikiListPanel.Children.Add(new TextBlock
                 {
-                    Text = $"... and {newWikiMods.Count - 30} more",
+                    Text = $"... и ещё {newWikiMods.Count - 30}",
                     FontSize = 12,
                     FontStyle = Windows.UI.Text.FontStyle.Italic,
                     Foreground = (SolidColorBrush)Application.Current.Resources["TextTertiaryBrush"],
@@ -1759,7 +1759,7 @@ public sealed partial class MainWindow
             {
                 ultraPlusListPanel.Children.Add(new TextBlock
                 {
-                    Text = $"... and {newUltraPlusMods.Count - 30} more",
+                    Text = $"... и ещё {newUltraPlusMods.Count - 30}",
                     FontSize = 12,
                     FontStyle = Windows.UI.Text.FontStyle.Italic,
                     Foreground = (SolidColorBrush)Application.Current.Resources["TextTertiaryBrush"],
@@ -1779,7 +1779,7 @@ public sealed partial class MainWindow
             });
             ultraPlusHeader.Children.Add(new TextBlock
             {
-                Text = $"{newUltraPlusMods.Count} new Ultra+ mod{(newUltraPlusMods.Count == 1 ? "" : "s")}:",
+                Text = $"новых модов Ultra+: {newUltraPlusMods.Count}{(newUltraPlusMods.Count == 1 ? "" : "s")}:",
                 FontSize = 13,
                 FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
                 Foreground = (SolidColorBrush)Application.Current.Resources["TextPrimaryBrush"],
@@ -1812,7 +1812,7 @@ public sealed partial class MainWindow
             {
                 lumaListPanel.Children.Add(new TextBlock
                 {
-                    Text = $"... and {newLumaMods.Count - 30} more",
+                    Text = $"... и ещё {newLumaMods.Count - 30}",
                     FontSize = 12,
                     FontStyle = Windows.UI.Text.FontStyle.Italic,
                     Foreground = (SolidColorBrush)Application.Current.Resources["TextTertiaryBrush"],
@@ -1839,10 +1839,10 @@ public sealed partial class MainWindow
         var totalCount = newWikiMods.Count + newUltraPlusMods.Count + newLumaMods.Count;
         var dialog = new ContentDialog
         {
-            Title = "New Mods Available",
+            Title = "Доступны новые моды",
             Content = contentPanel,
-            PrimaryButtonText = "Dismiss",
-            CloseButtonText = "Close",
+            PrimaryButtonText = "Скрыть",
+            CloseButtonText = "Закрыть",
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
diff --git a/RenoDXCommander/MainWindow.Events.cs b/RenoDXCommander/MainWindow.Events.cs
index d5eb577..2f92842 100644
--- a/RenoDXCommander/MainWindow.Events.cs
+++ b/RenoDXCommander/MainWindow.Events.cs
@@ -28,18 +28,18 @@ public sealed partial class MainWindow
 
         var dialog = new ContentDialog
         {
-            Title = "Full Refresh",
-            Content = "This will clear all caches and re-scan everything from scratch:\n\n" +
-                      "• Re-detects all games from every storefront\n" +
-                      "• Re-scans DLSS/Streamline DLL paths\n" +
-                      "• Re-detects graphics APIs and engine types\n" +
-                      "• Rebuilds shader and addon deployment state\n\n" +
-                      "Try a normal Refresh first — it handles most issues without the full rescan. " +
-                      "Use Full Refresh as a last resort if games are missing, paths have changed, DLSS has been added to a game, or the DLSS section is missing from a game card.\n\n" +
-                      "The next couple of restarts may take a few seconds longer while caches are rebuilt.\n\n" +
-                      "Do not close RHI while the refresh is in progress — closing early will result in a missing library and the scan will need to be repeated.",
-            PrimaryButtonText = "Continue",
-            CloseButtonText = "Cancel",
+            Title = "Полное обновление",
+            Content = "Будут очищены все кеши и всё пересканировано с нуля:\n\n" +
+                      "• Заново находит игры во всех магазинах\n" +
+                      "• Заново сканирует пути DLL DLSS/Streamline\n" +
+                      "• Заново определяет графические API и типы движков\n" +
+                      "• Пересобирает состояние развертывания шейдеров и аддонов\n\n" +
+                      "Сначала попробуйте обычное «Обновить» — оно решает большинство проблем без полного пересканирования. " +
+                      "«Полное обновление» — крайняя мера: если игры пропали, пути изменились, к игре добавился DLSS или с карточки игры пропал раздел DLSS.\n\n" +
+                      "Следующие пару запусков могут занять на несколько секунд дольше — кеши будут пересоздаваться.\n\n" +
+                      "Не закрывайте RHI, пока идёт обновление — раннее закрытие приведёт к потере части библиотеки, и сканирование придётся повторить.",
+            PrimaryButtonText = "Продолжить",
+            CloseButtonText = "Отмена",
             DefaultButton = ContentDialogButton.Primary,
             XamlRoot = Content.XamlRoot,
         };
@@ -66,14 +66,14 @@ public sealed partial class MainWindow
         var progressPanel = new StackPanel { Spacing = 8 };
         var progressRow = new StackPanel { Orientation = Microsoft.UI.Xaml.Controls.Orientation.Horizontal, Spacing = 12 };
         var progressRing = new ProgressRing { IsActive = true, Width = 20, Height = 20 };
-        var progressText = new TextBlock { Text = "Fetching manifest...", FontSize = 13, Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush) };
+        var progressText = new TextBlock { Text = "Загрузка манифеста...", FontSize = 13, Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush) };
         progressRow.Children.Add(progressRing);
         progressRow.Children.Add(progressText);
         progressPanel.Children.Add(progressRow);
 
         var progressDialog = new ContentDialog
         {
-            Title = "Checking for updates...",
+            Title = "Проверка обновлений...",
             Content = progressPanel,
             XamlRoot = Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -89,14 +89,14 @@ public sealed partial class MainWindow
             ViewModel.ForceNextUpdateCheck();
 
             // Trigger a Refresh (which fetches manifests + wiki + runs update checks)
-            DispatcherQueue?.TryEnqueue(() => progressText.Text = "Checking components...");
+            DispatcherQueue?.TryEnqueue(() => progressText.Text = "Проверка компонентов...");
             await ViewModel.RefreshAsync();
 
             // Trigger silent auto-install of any updates found
             ViewModel.TriggerAutoUpdate();
 
             // Check app update
-            DispatcherQueue?.TryEnqueue(() => progressText.Text = "Checking app version...");
+            DispatcherQueue?.TryEnqueue(() => progressText.Text = "Проверка версии приложения...");
             await _dialogService.CheckForAppUpdateAsync();
         }
         catch (Exception ex)
@@ -200,7 +200,7 @@ public sealed partial class MainWindow
             // ── HDR on First Boot ──────────────────────────────────────────────
             var hdrLabel = new TextBlock
             {
-                Text = "HDR on First Boot",
+                Text = "HDR при первом запуске",
                 FontSize = 11,
                 Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
                 VerticalAlignment = VerticalAlignment.Center,
@@ -214,9 +214,9 @@ public sealed partial class MainWindow
             hdrCombo.Items.Add("On");
             ToolTipService.SetToolTip(hdrCombo,
                 "Controls EnableHDR and DisplayMode in the [Luma] section of reshade.ini.\n" +
-                "Default: leaves both keys as-is (Luma controls them).\n" +
-                "Off: sets EnableHDR=0 and DisplayMode=0.\n" +
-                "On: sets EnableHDR=1 and DisplayMode=1.");
+                "По умолчанию: оставить оба ключа как есть (ими управляет Luma).\n" +
+                "Выкл: задаёт EnableHDR=0 и DisplayMode=0.\n" +
+                "Вкл: задаёт EnableHDR=1 и DisplayMode=1.");
 
             var currentHdr = AuxInstallService.GetLumaReshadeIniValue(card.InstallPath, "EnableHDR");
             // Default = key absent or never set by RHI; Off = "0"; On = "1"
@@ -230,7 +230,7 @@ public sealed partial class MainWindow
                 {
                     AuxInstallService.RemoveLumaReshadeIniValue(card.InstallPath, "EnableHDR");
                     AuxInstallService.RemoveLumaReshadeIniValue(card.InstallPath, "DisplayMode");
-                    card.LumaActionMessage = "✅ HDR reset to Luma default.";
+                    card.LumaActionMessage = "✅ HDR сброшен к значению Luma по умолчанию.";
                 }
                 else
                 {
@@ -247,7 +247,7 @@ public sealed partial class MainWindow
             // ── TAA Settings ───────────────────────────────────────────────────
             var taaLabel = new TextBlock
             {
-                Text = "TAA Settings",
+                Text = "Настройки TAA",
                 FontSize = 11,
                 Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
                 VerticalAlignment = VerticalAlignment.Center,
@@ -260,7 +260,7 @@ public sealed partial class MainWindow
             taaCombo.Items.Add("On");
             ToolTipService.SetToolTip(taaCombo,
                 "Writes r.DefaultFeature.AntiAliasing=2 and r.PostProcessAAQuality=4 to Engine.ini.\n" +
-                "Forces TAA with high quality for Luma HDR compatibility.");
+                "Принудительно включает качественный TAA для совместимости с Luma HDR.");
 
             bool taaActive = ViewModel.IsLumaTaaEnabled(card.GameName);
             taaCombo.SelectedIndex = taaActive ? 1 : 0;
@@ -301,9 +301,9 @@ public sealed partial class MainWindow
 
         var dialog = new ContentDialog
         {
-            Title = "Luma Settings",
+            Title = "Настройки Luma",
             Content = content,
-            CloseButtonText = "Close",
+            CloseButtonText = "Закрыть",
             DefaultButton = ContentDialogButton.Close,
             XamlRoot = this.Content.XamlRoot,
         };
@@ -630,7 +630,7 @@ public sealed partial class MainWindow
         // ── Open Folder ──
         var openFolderItem = new MenuFlyoutItem
         {
-            Text = "📂 Open Folder",
+            Text = "📂 Открыть папку",
             Tag = card,
         };
         openFolderItem.Click += CardOpenFolder_Click;
@@ -652,7 +652,7 @@ public sealed partial class MainWindow
         {
             var discussionItem = new MenuFlyoutItem
             {
-                Text = "ℹ Discussion / Instructions",
+                Text = "ℹ Обсуждение / инструкции",
                 Tag = card,
             };
             discussionItem.Click += async (s, ev) =>
@@ -668,7 +668,7 @@ public sealed partial class MainWindow
         {
             var notesItem = new MenuFlyoutItem
             {
-                Text = "💬 View Notes",
+                Text = "💬 Заметки",
                 Tag = card,
             };
             notesItem.Click += async (s, ev) =>
@@ -839,7 +839,7 @@ public sealed partial class MainWindow
         var currentQuery = SearchBox.Text?.Trim() ?? "";
         if (string.IsNullOrEmpty(currentQuery)) return;
 
-        var nameBox = new TextBox { PlaceholderText = "Filter name", Text = currentQuery, Width = 350 };
+        var nameBox = new TextBox { PlaceholderText = "Имя фильтра", Text = currentQuery, Width = 350 };
         var errorText = new TextBlock
         {
             Text = "",
@@ -851,7 +851,7 @@ public sealed partial class MainWindow
 
         var dialog = new ContentDialog
         {
-            Title = "Save Custom Filter",
+            Title = "Сохранить свой фильтр",
             Content = new StackPanel
             {
                 Spacing = 8,
@@ -859,7 +859,7 @@ public sealed partial class MainWindow
                 {
                     new TextBlock
                     {
-                        Text = $"Save the current search \"{currentQuery}\" as a custom filter:",
+                        Text = $"Сохранить текущий поиск «{currentQuery}» как свой фильтр:",
                         TextWrapping = TextWrapping.Wrap,
                         Foreground = Brush(ResourceKeys.TextSecondaryBrush),
                     },
@@ -867,8 +867,8 @@ public sealed partial class MainWindow
                     errorText,
                 }
             },
-            PrimaryButtonText = "Save",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Сохранить",
+            CloseButtonText = "Отмена",
             XamlRoot = Content.XamlRoot,
             Background = Brush(ResourceKeys.SurfaceToolbarBrush),
             RequestedTheme = ElementTheme.Dark,
@@ -880,14 +880,14 @@ public sealed partial class MainWindow
             var name = nameBox.Text?.Trim() ?? "";
             if (string.IsNullOrEmpty(name))
             {
-                errorText.Text = "Please enter a filter name.";
+                errorText.Text = "Введите имя фильтра.";
                 errorText.Visibility = Visibility.Visible;
                 args.Cancel = true;
                 return;
             }
             if (ViewModel.Filter.CustomFilterNameExists(name))
             {
-                errorText.Text = $"A filter named \"{name}\" already exists.";
+                errorText.Text = $"Фильтр с именем «{name}» уже существует.";
                 errorText.Visibility = Visibility.Visible;
                 args.Cancel = true;
                 return;
@@ -942,7 +942,7 @@ public sealed partial class MainWindow
 
             // Right-click context menu with "Delete" option (Req 5.1–5.5)
             var flyout = new MenuFlyout();
-            var deleteItem = new MenuFlyoutItem { Text = "Delete" };
+            var deleteItem = new MenuFlyoutItem { Text = "Удалить" };
             deleteItem.Click += (s, args) =>
             {
                 ViewModel.Filter.RemoveCustomFilter(chipName);
@@ -1012,19 +1012,19 @@ public sealed partial class MainWindow
         nameBox.SelectAll();
         var nameDialog = new ContentDialog
         {
-            Title           = "Name This Game",
+            Title           = "Назвать игру",
             Content         = new StackPanel
             {
                 Spacing = 10,
                 Children =
                 {
                     new TextBlock { Text = $"Selected: {filePath}", TextWrapping = TextWrapping.Wrap, Foreground = Brush(ResourceKeys.TextSecondaryBrush), FontSize = 11 },
-                    new TextBlock { Text = "Enter the game name:", TextWrapping = TextWrapping.Wrap, Foreground = Brush(ResourceKeys.TextSecondaryBrush) },
+                    new TextBlock { Text = "Введите название игры:", TextWrapping = TextWrapping.Wrap, Foreground = Brush(ResourceKeys.TextSecondaryBrush) },
                     nameBox
                 }
             },
-            PrimaryButtonText   = "Add Game",
-            CloseButtonText     = "Cancel",
+            PrimaryButtonText   = "Добавить игру",
+            CloseButtonText     = "Отмена",
             XamlRoot            = Content.XamlRoot,
             Background          = Brush(ResourceKeys.SurfaceToolbarBrush),
             RequestedTheme      = ElementTheme.Dark,
@@ -1112,17 +1112,17 @@ public sealed partial class MainWindow
             {
                 var warningDialog = new ContentDialog
                 {
-                    Title = "⚠ ReShade Addons",
+                    Title = "⚠ Аддоны ReShade",
                     Content = new TextBlock
                     {
-                        Text = "ReShade addons are advanced features intended for experienced users who understand what they are.\n\n" +
-                               "Addons can modify game rendering behaviour and may cause instability. " +
-                               "Only proceed if you are comfortable managing ReShade addons.",
+                        Text = "Аддоны ReShade — продвинутая функция для опытных пользователей, понимающих, что это такое.\n\n" +
+                               "Аддоны могут менять работу рендеринга игры и способны вызывать нестабильность. " +
+                               "Продолжайте, только если уверенно управляете аддонами ReShade.",
                         TextWrapping = Microsoft.UI.Xaml.TextWrapping.Wrap,
                         MaxWidth = 450,
                     },
-                    PrimaryButtonText = "Continue",
-                    CloseButtonText = "Cancel",
+                    PrimaryButtonText = "Продолжить",
+                    CloseButtonText = "Отмена",
                     XamlRoot = Content.XamlRoot,
                     RequestedTheme = ElementTheme.Dark,
                 };
diff --git a/RenoDXCommander/MainWindow.FaqBuilder.cs b/RenoDXCommander/MainWindow.FaqBuilder.cs
index 1d43713..d799873 100644
--- a/RenoDXCommander/MainWindow.FaqBuilder.cs
+++ b/RenoDXCommander/MainWindow.FaqBuilder.cs
@@ -24,26 +24,26 @@ public sealed partial class MainWindow
 
         // Welcome section
         panel.Children.Add(BuildFaqSection(
-            null, "Welcome to RHI", "AccentTealBrush",
-            "RHI auto-detects your games and lets you install HDR mods, shaders, frame limiters, and manage NVIDIA driver settings — all from one place. Here's how to get started.",
+            null, "Добро пожаловать в RHI", "AccentTealBrush",
+            "RHI сам находит ваши игры и позволяет устанавливать HDR-моды, шейдеры, ограничители FPS и управлять настройками драйвера NVIDIA — всё в одном месте. Вот с чего начать.",
             null));
 
         // Step 1: Select a Game
         panel.Children.Add(BuildFaqStep(1,
-            "Select a Game",
-            "Your games are listed in the sidebar on the left. Click any game to see its details and available actions. Use the filter chips (All Games, Installed, Unreal, etc.) and search box to find specific games.",
+            "Выберите игру",
+            "Игры перечислены в боковой панели слева. Нажмите на игру, чтобы увидеть её подробности и доступные действия. Для поиска используйте фильтры (Все игры, Установленные, Unreal и т.д.) и строку поиска.",
             "Tip: Double-click a game to launch it directly. Drag and drop a game's .exe file onto RHI to add games not auto-detected."));
 
         // Step 2: Install ReShade
         panel.Children.Add(BuildFaqStep(2,
-            "Install ReShade",
-            "ReShade is required for RenoDX HDR mods to work. Click 'Install ReShade' on the game's detail panel. RHI automatically downloads and installs the correct version with full addon support.",
+            "Установить ReShade",
+            "Для работы HDR-модов RenoDX нужен ReShade. Нажмите «Установить ReShade» на панели игры. RHI автоматически скачает и установит нужную версию с полной поддержкой аддонов.",
             "ReShade version can be changed per-game via the game overrides section — choose Stable, Nightly, Legacy, or a custom ReShade DLL.\nVulkan Games: Vulkan games (like Doom Eternal) require admin privileges. RHI will prompt for elevation when needed.\nDrag and drop ReShade preset files (.ini) onto a game to install them automatically."));
 
         // Step 3a: RenoDX
         panel.Children.Add(BuildFaqSection(
             "3a", "RenoDX", "AccentTealBrush",
-            "RenoDX is RHI's primary HDR mod framework. The RenoDX row on each game shows what's available:\n\n• Named mods — game-specific HDR mods from the RenoDX wiki, made by the community.\n• UE-Extended — for Unreal Engine games without a named mod. Provides native HDR output via a generic UE addon.\n• Unity addon — same idea for Unity Engine games. Provides HDR output for Unity games without a named mod.\n• RTX HDR — if no mod is available, you can enable NVIDIA's driver-level SDR-to-HDR conversion via the RenoDX ⚙ cog.",
+            "RenoDX",
             "The cog icon next to RenoDX opens advanced settings: Peak Nits, UE-Extended toggle, and RTX HDR configuration.\nEngine.ini Settings (Unreal Engine games only): toggle HDR keys and LUT update frequency written to the game's Engine.ini for accurate HDR rendering.\nFor games not on the wiki, drag and drop an .addon64 file from the RenoDX Discord directly onto the game in RHI."));
 
         // Step 3b: Luma
@@ -54,87 +54,87 @@ public sealed partial class MainWindow
 
         // Step 4: Choose Shaders
         panel.Children.Add(BuildFaqStep(4,
-            "Choose Shaders (Optional)",
-            "Click the 'Shaders/Addons' button in the toolbar, then 'Global Shaders' to select shader packs. Lilium's HDR shader pack is selected by default. These apply to all games with ReShade installed.\n\nExpand any pack to pick individual shaders — the pack shows a dash when only some files are selected. Use the Profiles panel on the right to save, load, rename, and share named shader selections. Export a profile as a zip to share via Discord.",
-            "Tip: Per-game shaders can be set using the Shaders button on each game's detail card (when ReShade is installed).\nTip: Use Expand All / Collapse All to browse all packs at once. Deselect All clears the whole selection. Export copies a zip of your selected shaders to the clipboard — paste directly into Discord to share."));
+            "Выбрать шейдеры (необязательно)",
+            "Нажмите кнопку «Шейдеры/Аддоны» на панели инструментов, затем «Глобальные шейдеры», чтобы выбрать наборы шейдеров. По умолчанию выбран HDR-набор Lilium. Он применяется ко всем играм с установленным ReShade.\n\nРазверните набор, чтобы выбрать отдельные шейдеры — если отмечены лишь некоторые файлы, у набора будет прочерк. Панель «Профили» справа позволяет сохранять, загружать, переименовывать и делиться именными подборками шейдеров. Экспортируйте профиль в zip, чтобы поделиться через Discord.",
+            "Совет: шейдеры для отдельной игры задаются кнопкой «Шейдеры» на её карточке (если ReShade установлен).\nСовет: «Развернуть все» / «Свернуть все» — просмотреть все наборы сразу. «Снять всё» очищает выбор. «Экспорт» копирует zip выбранных шейдеров в буфер обмена — вставьте прямо в Discord, чтобы поделиться."));
 
         // Step 5: DOF Fix
         panel.Children.Add(BuildFaqStep(5,
-            "DOF Fix (Recommended for UE5)",
-            "DOF Fix backports a depth-of-field rendering fix from Unreal Engine 5.7 to games running on UE 5.0–5.6. It appears in the Recommended section on the game's detail panel when supported. Install it alongside RenoDX for the best result.",
-            "DOF Fix only shows on eligible UE 5.0–5.6 games — it won't appear on UE4 or UE 5.7+ titles.\nInstall and uninstall work the same as any other component."));
+            "DOF Fix (рекомендуется для UE5)",
+            "DOF Fix переносит исправление рендеринга глубины резкости из Unreal Engine 5.7 в игры на UE 5.0–5.6. Для поддерживаемых игр появляется в разделе «Рекомендуемое» на панели игры. Для лучшего результата устанавливайте вместе с RenoDX.",
+            "DOF Fix"));
 
         // Step 6: Frame Limiters
         panel.Children.Add(BuildFaqStep(6,
-            "Frame Limiters (Optional)",
-            "ReLimiter and Display Commander are ReShade addons that provide precise frame limiting for VRR displays. Install them from the game's detail panel. ReLimiter is recommended as it's developed by the same team as RHI. Set your target FPS in Settings, per-game via the cog icon, or directly in-game.",
-            "VRR cap presets by refresh rate (leave headroom below max for smooth VRR):\n• 60Hz → 59 FPS\n• 120Hz → 116 FPS\n• 144Hz → 138 FPS\n• 165Hz → 157 FPS\n• 240Hz → 224 FPS\n• 360Hz → 324 FPS\nThese values are pre-configured in RHI's FPS dropdown menus."));
+            "Ограничители FPS (необязательно)",
+            "ReLimiter и Display Commander — аддоны ReShade с точным ограничением частоты кадров для VRR-дисплеев. Устанавливаются с панели игры. Рекомендуется ReLimiter — его развивает та же команда, что и RHI. Целевой FPS задаётся в настройках, для каждой игры через значок ⚙ или прямо в игре.",
+            "Пресеты лимитов VRR по частоте обновления (с запасом ниже максимума для плавного VRR):\n• 60 Гц → 59 FPS\n• 120 Гц → 116 FPS\n• 144 Гц → 138 FPS\n• 165 Гц → 157 FPS\n• 240 Гц → 224 FPS\n• 360 Гц → 324 FPS\nЭти значения предзаданы в выпадающих списках FPS в RHI."));
 
         // Step 7: DLSS/Streamline
         panel.Children.Add(BuildFaqStep(7,
-            "Update DLSS / Streamline (Optional)",
-            "Games with DLSS or Streamline DLLs have a dedicated section on the detail panel showing version info. Click to update to the latest version. Using the newest versions is recommended for best performance and quality. RHI backs up originals automatically so you can restore anytime.",
-            "When new DLSS or Streamline versions release, they will appear in RHI automatically. Set your default DLSS preset in Settings. Per-game presets can be changed in the DLSS section on each game's detail panel."));
+            "Обновить DLSS / Streamline (необязательно)",
+            "Игры с DLL DLSS или Streamline имеют отдельный раздел на панели игры с информацией о версиях. Нажмите, чтобы обновить до последней версии. Новейшие версии рекомендуются для лучшей производительности и качества. RHI автоматически резервирует оригиналы, так что их можно вернуть в любой момент.",
+            "Новые версии DLSS и Streamline появляются в RHI автоматически. Пресет DLSS по умолчанию задаётся в настройках. Пресеты отдельных игр меняются в разделе DLSS на панели каждой игры."));
 
         // OptiScaler
         panel.Children.Add(BuildFaqSpecialSection("⚙", "AccentAmberBrush",
-            "OptiScaler (Optional)",
+            "OptiScaler (необязательно)",
             "OptiScaler replaces DLSS/XeSS with alternative upscalers (FSR, XeSS, Intel Arc) or adds/patches frame generation on any GPU. Install it from the game's detail panel when a game has OptiScaler support.\n\nThe ⚙ cog on the OptiScaler row opens per-game settings. For the Nightly build channel these include:\n• Streamline/DLSS Enabler — deploys Streamline and DLSS Enabler to the game folder for DLSS Frame Generation support.\n• Frame Generation — set FG Input, FG Output, FG Nvngx Override, and HUD Fix.\n• Additional Settings — DLSS SR/RR preset, render scale, and flip metering.\n• Presets — save and apply named setting presets across games.\n• Engine.ini Settings (Unreal Engine games) — Dilated Motion Vectors, FSR Crash Fix, FSR-FG Swapchain, Upscaler Plugin.",
             "Switch between Stable and Nightly channels per game in the cog — Nightly adds frame generation and additional settings.\nOptiScaler and ReShade can coexist. If you see crashes with both installed, try renaming ReShade to a different DLL name using DLL Naming Overrides in the Game Overrides panel.\nGPU type and DLSS input settings (AMD/Intel only) are configured in Settings → OptiScaler Settings before installing.\nThe 'Deploy OptiScaler.ini' button in the cog redeploys your configured INI template to the game folder."));
 
         // Settings Overview
         panel.Children.Add(BuildFaqInfoSection("Settings",
-            "Click 'Settings' in the toolbar to configure defaults for all games:",
+            "Нажмите «Настройки» на панели инструментов, чтобы задать значения по умолчанию для всех игр:",
             new[]
             {
-                "ReLimiter FPS: Default frame rate target",
-                "DLSS Preset: Default upscaling preset",
-                "NVIDIA Driver Settings: VSync, Low Latency, Power Mode",
-                "Peak Nits: Your display's peak brightness for HDR",
-                "ReShade Hotkeys: Customize overlay and screenshot keys"
+                "ReLimiter FPS: целевая частота кадров по умолчанию",
+                "Пресет DLSS: пресет масштабирования по умолчанию",
+                "Настройки драйвера NVIDIA: VSync, Low Latency, режим питания",
+                "Пиковая яркость: пиковая яркость вашего дисплея для HDR",
+                "Горячие клавиши ReShade: настройка клавиш оверлея и скриншотов"
             }));
 
         // NVIDIA Driver Settings
-        panel.Children.Add(BuildFaqInfoSection("NVIDIA Driver Settings",
-            "RHI can manage per-game NVIDIA driver profiles. These settings are available directly on each game's detail panel:",
+        panel.Children.Add(BuildFaqInfoSection("Настройки драйвера NVIDIA",
+            "RHI умеет управлять профилями драйвера NVIDIA для каждой игры. Эти настройки доступны прямо на панели каждой игры:",
             new[]
             {
-                "VSync: On, Off, or Adaptive (Fast Sync)",
-                "Low Latency Mode: Ultra, On, or Off",
-                "Smooth Motion: Multi Frame Generation (per-game only)",
-                "ReBAR: Resizable BAR (requires admin)"
+                "VSync: вкл, выкл или адаптивный (Fast Sync)",
+                "Режим Low Latency: Ultra, вкл или выкл",
+                "Smooth Motion: мульти-генерация кадров (только для отдельных игр)",
+                "ReBAR: Resizable BAR (нужны права администратора)"
             },
-            "Global defaults for VSync, Low Latency, and Power Mode are set in Settings. Per-game overrides are configured directly on each game's detail panel."));
+            "Значения по умолчанию для VSync, Low Latency и режима питания задаются в настройках. Переопределения для отдельных игр настраиваются прямо на панели каждой игры."));
 
         // Vulkan Games
         panel.Children.Add(BuildFaqSpecialSection("V", "AccentPurpleBrush",
-            "Vulkan Games",
+            "Vulkan-игры",
             "Vulkan games (shown with a 'Vulkan' badge) use a global ReShade layer installed to C:\\ProgramData\\ReShade. This requires administrator privileges. When you install ReShade on a Vulkan game, RHI will prompt for elevation.",
-            "All Vulkan games share the same ReShade installation. Updating ReShade on one Vulkan game updates it for all.\nPer-game RenoDX addons and shaders are still installed individually to each game folder."));
+            "Все Vulkan-игры используют общую установку ReShade. Обновление ReShade в одной Vulkan-игре обновляет его для всех.\nИндивидуальные аддоны RenoDX и шейдеры по-прежнему устанавливаются в папку каждой игры отдельно."));
 
         // Adding Games Manually
         panel.Children.Add(BuildFaqSpecialSection("+", "AccentAmberBrush",
-            "Adding Games Manually",
+            "Добавление игр вручную",
             "If a game isn't auto-detected, drag and drop its .exe file directly onto the RHI window. RHI will add it to your library and detect its engine type.",
             "You can also drag .addon64 files from the RenoDX Discord onto any game to install mods not yet on the wiki."));
 
         // Updating Everything
         panel.Children.Add(BuildFaqSpecialSection("↑", "AccentGreenBrush",
-            "Updating Everything",
-            "Click 'Update All' in the toolbar to update all installed components across all games at once. This includes ReShade, RenoDX mods, ReLimiter, Display Commander, and more.",
-            "Games with available updates show a green dot in the sidebar. Configure which components are included in 'Update All' from Settings."));
+            "Обновление всего",
+            "Нажмите «Обновить всё» на панели инструментов, чтобы разом обновить все установленные компоненты во всех играх. Это включает ReShade, моды RenoDX, ReLimiter, Display Commander и многое другое.",
+            "Игры с доступными обновлениями помечаются зелёной точкой в боковой панели. Какие компоненты входят в «Обновить всё», настраивается в параметрах."));
 
         // Troubleshooting - Full Refresh
         panel.Children.Add(BuildFaqSpecialSection("↻", "AccentBlueBrush",
-            "Troubleshooting: Full Refresh",
-            "If games are missing, install locations have changed, or DLSS/Streamline files have been added or removed, use 'Full Refresh' in Settings to rescan your entire library from scratch.",
-            "Full Refresh clears the cached game list and re-detects everything. Use it when the normal Refresh button doesn't pick up changes."));
+            "Устранение неполадок: полное обновление",
+            "Если игры пропали, изменились пути установки или файлы DLSS/Streamline добавлялись и удалялись, используйте «Полное обновление» в настройках, чтобы пересканировать всю библиотеку с нуля.",
+            "Полное обновление очищает кешированный список игр и заново определяет всё. Используйте, если обычная кнопка «Обновить» не замечает изменений."));
 
         // System Tray
         panel.Children.Add(BuildFaqSpecialSection("◰", "AccentPurpleBrush",
-            "System Tray",
-            "RHI can minimize to the system tray instead of closing. Right-click the tray icon to quickly launch recent games without opening the main window.",
-            "Enable 'Close to System Tray' in Settings to keep RHI running in the background. The tray icon provides quick access to your most recently played games. RHI automatically checks for updates every 4 hours while running, so everything stays up to date."));
+            "Системный трей",
+            "RHI может сворачиваться в системный трей вместо закрытия. Правый клик по значку в трее — быстрый запуск недавних игр без открытия главного окна.",
+            "Включите «Свёртывать в трей» в настройках, чтобы RHI продолжал работать в фоне. Значок в трее даёт быстрый доступ к недавно запускавшимся играм. Работающий RHI каждые 4 часа автоматически проверяет обновления, так что всё остаётся актуальным."));
 
         // Need More Help
         panel.Children.Add(BuildFaqLinksSection());
@@ -467,7 +467,7 @@ public sealed partial class MainWindow
 
         stack.Children.Add(new TextBlock
         {
-            Text = "Need More Help?",
+            Text = "Нужна помощь?",
             FontSize = 14,
             FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
             Foreground = (Brush)Application.Current.Resources["AccentTealBrush"]
@@ -475,7 +475,7 @@ public sealed partial class MainWindow
 
         stack.Children.Add(new TextBlock
         {
-            Text = "Support is available on Discord — join the community for help, mod updates, and discussion.",
+            Text = "Поддержка — в Discord: присоединяйтесь к сообществу за помощью, обновлениями модов и обсуждением.",
             TextWrapping = TextWrapping.Wrap,
             FontSize = 12,
             Foreground = (Brush)Application.Current.Resources["TextSecondaryBrush"],
@@ -491,7 +491,7 @@ public sealed partial class MainWindow
         };
         discordLink.Content = new TextBlock
         {
-            Text = "Join the Ultra+ Discord (main community)",
+            Text = "Discord Ultra+ (основное сообщество)",
             Foreground = (Brush)Application.Current.Resources["AccentBlueBrush"],
             FontSize = 12
         };
@@ -504,7 +504,7 @@ public sealed partial class MainWindow
         };
         renodxDiscordLink.Content = new TextBlock
         {
-            Text = "RenoDX Discord (mod development)",
+            Text = "RenoDX Discord (разработка модов)",
             Foreground = (Brush)Application.Current.Resources["AccentBlueBrush"],
             FontSize = 12
         };
@@ -517,7 +517,7 @@ public sealed partial class MainWindow
         };
         wikiLink.Content = new TextBlock
         {
-            Text = "Browse the RenoDX Mod Wiki",
+            Text = "Открыть вики модов RenoDX",
             Foreground = (Brush)Application.Current.Resources["AccentBlueBrush"],
             FontSize = 12
         };
@@ -530,7 +530,7 @@ public sealed partial class MainWindow
         };
         githubLink.Content = new TextBlock
         {
-            Text = "RHI GitHub — Report issues or request features",
+            Text = "RHI на GitHub — сообщить о проблеме или предложить функцию",
             Foreground = (Brush)Application.Current.Resources["AccentBlueBrush"],
             FontSize = 12
         };
diff --git a/RenoDXCommander/MainWindow.Skeleton.cs b/RenoDXCommander/MainWindow.Skeleton.cs
index 0ebe10c..1e50777 100644
--- a/RenoDXCommander/MainWindow.Skeleton.cs
+++ b/RenoDXCommander/MainWindow.Skeleton.cs
@@ -610,7 +610,7 @@ public sealed partial class MainWindow
         // "Components" header text (real TextBlock, not skeleton — matches real layout)
         tableContent.Children.Add(new TextBlock
         {
-            Text = "Components",
+            Text = "Компоненты",
             FontSize = 13,
             FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
             Foreground = new SolidColorBrush(ColorHelper.FromArgb(0xFF, 0xE8, 0xEC, 0xF2)),
@@ -669,7 +669,7 @@ public sealed partial class MainWindow
         // "── Frame limiters — Choose one ──" separator
         tableContent.Children.Add(new TextBlock
         {
-            Text = "——  Frame limiters — Choose one  ——",
+            Text = "——  Ограничители FPS — выберите один  ——",
             FontSize = 10,
             Foreground = new SolidColorBrush(ColorHelper.FromArgb(0xFF, 0x5A, 0x68, 0x80)),
             HorizontalAlignment = HorizontalAlignment.Center,
@@ -728,7 +728,7 @@ public sealed partial class MainWindow
         // "── Optional ──" separator (matches real detail panel separator above OptiScaler)
         tableContent.Children.Add(new TextBlock
         {
-            Text = "── Optional ──",
+            Text = "── Необязательно ──",
             FontSize = 10,
             Foreground = new SolidColorBrush(ColorHelper.FromArgb(0xFF, 0x5A, 0x68, 0x80)),
             HorizontalAlignment = HorizontalAlignment.Center,
@@ -815,7 +815,7 @@ public sealed partial class MainWindow
         // "Overrides" header text
         overridesContent.Children.Add(new TextBlock
         {
-            Text = "Overrides",
+            Text = "Переопределения",
             FontSize = 13,
             FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
             Foreground = new SolidColorBrush(ColorHelper.FromArgb(0xFF, 0xE8, 0xEC, 0xF2)),
@@ -927,7 +927,7 @@ public sealed partial class MainWindow
         // "Manage" header text
         manageContent.Children.Add(new TextBlock
         {
-            Text = "Manage",
+            Text = "Управление",
             FontSize = 13,
             FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
             Foreground = new SolidColorBrush(ColorHelper.FromArgb(0xFF, 0xE8, 0xEC, 0xF2)),
diff --git a/RenoDXCommander/MainWindow.UISync.cs b/RenoDXCommander/MainWindow.UISync.cs
index 4eb6a44..49eb97e 100644
--- a/RenoDXCommander/MainWindow.UISync.cs
+++ b/RenoDXCommander/MainWindow.UISync.cs
@@ -81,10 +81,10 @@ public sealed partial class MainWindow
                         + (string.IsNullOrEmpty(ViewModel.SubStatusText) ? "" : $"  —  {ViewModel.SubStatusText}");
                     break;
                 case nameof(ViewModel.InstalledCount):
-                    InstalledCountText.Text = $"{ViewModel.InstalledCount} installed";
+                    InstalledCountText.Text = $"установлено: {ViewModel.InstalledCount}";
                     break;
                 case nameof(ViewModel.TotalGames):
-                    GameCountText.Text = $"{ViewModel.TotalGames} shown";
+                    GameCountText.Text = $"показано: {ViewModel.TotalGames}";
                     if (ViewModel.CurrentViewLayout == ViewLayout.Compact
                         && ViewModel.SelectedGame is { } compactCard)
                     {
@@ -94,7 +94,7 @@ public sealed partial class MainWindow
                     break;
                 case nameof(ViewModel.HiddenCount):
                     HiddenCountText.Text = ViewModel.HiddenCount > 0
-                        ? $"· {ViewModel.HiddenCount} hidden" : "";
+                        ? $"· скрыто: {ViewModel.HiddenCount}" : "";
                     break;
                 case nameof(ViewModel.FilterMode):
                     RefreshFilterButtonStyles();
@@ -210,7 +210,7 @@ public sealed partial class MainWindow
 
     internal void UpdateLumaToggleStyle(bool isLumaMode)
     {
-        DetailLumaToggleText.Text = isLumaMode ? "Luma ON" : "Luma OFF";
+        DetailLumaToggleText.Text = isLumaMode ? "Luma вкл" : "Luma выкл";
         if (isLumaMode)
         {
             DetailLumaToggle.Background = Brush(ResourceKeys.AccentGreenBgBrush);
diff --git a/RenoDXCommander/MainWindow.xaml b/RenoDXCommander/MainWindow.xaml
index f8e0bf3..04e6198 100644
--- a/RenoDXCommander/MainWindow.xaml
+++ b/RenoDXCommander/MainWindow.xaml
@@ -44,44 +44,44 @@
 
                 <!-- Action buttons -->
                 <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
-                    <Button x:Name="NewModsBtn" Content="New Mods Available"
+                    <Button x:Name="NewModsBtn" Content="Доступны новые моды"
                             Click="NewModsButton_Click"
                             Visibility="{x:Bind ViewModel.NewWikiModsButtonVisibility, Mode=OneWay}"
                             Background="{StaticResource SurfaceInputBrush}" Foreground="{StaticResource AccentGreenBrush}"
                             BorderBrush="{StaticResource AccentGreenBorderBrush}" BorderThickness="1"
                             CornerRadius="8" Padding="12,7" FontSize="12"
-                            ToolTipService.ToolTip="New RenoDX mods have been added to the wiki"/>
-                    <Button x:Name="FaqBtn" Content="Quick Start"
+                            ToolTipService.ToolTip="В вики добавлены новые моды RenoDX"/>
+                    <Button x:Name="FaqBtn" Content="Быстрый старт"
                             Click="FaqButton_Click"
                             Background="{StaticResource SurfaceInputBrush}" Foreground="{StaticResource AccentTealBrush}"
                             BorderBrush="{StaticResource AccentTealBorderBrush}" BorderThickness="1"
                             CornerRadius="8" Padding="12,7" FontSize="12"
-                            ToolTipService.ToolTip="Quick Start Guide"/>
+                            ToolTipService.ToolTip="Руководство быстрого старта"/>
 
                     <Border Width="1" Height="20" Background="{StaticResource BorderSubtleBrush}" Margin="4,0"/>
 
-                    <Button x:Name="RefreshBtn" Content="Refresh"
+                    <Button x:Name="RefreshBtn" Content="Обновить"
                             Click="RefreshButton_Click"
                             Background="{StaticResource SurfaceInputBrush}" Foreground="{StaticResource AccentTealBrush}"
                             BorderBrush="{StaticResource AccentTealBorderBrush}" BorderThickness="1"
                             CornerRadius="8" Padding="12,7" FontSize="12"
-                            ToolTipService.ToolTip="Rescan game library and fetch latest mod info"/>
-                    <Button x:Name="ShadersAddonsBtn" Content="Shaders/Addons"
+                            ToolTipService.ToolTip="Пересканировать библиотеку игр и загрузить свежую информацию о модах"/>
+                    <Button x:Name="ShadersAddonsBtn" Content="Шейдеры/Аддоны"
                             Background="{StaticResource SurfaceInputBrush}" Foreground="{StaticResource AccentTealBrush}"
                             BorderBrush="{StaticResource AccentTealBorderBrush}" BorderThickness="1"
                             CornerRadius="8" Padding="12,7" FontSize="12"
-                            ToolTipService.ToolTip="Manage global shaders and ReShade addons">
+                            ToolTipService.ToolTip="Управление глобальными шейдерами и аддонами ReShade">
                         <Button.Flyout>
                             <MenuFlyout Placement="Bottom">
-                                <MenuFlyoutItem x:Name="ChooseShadersItem" Text="Global Shaders" Click="ChooseShadersButton_Click"/>
-                                <MenuFlyoutItem x:Name="ReShadeAddonsItem" Text="Global Addons" Click="ReShadeAddonsButton_Click"/>
+                                <MenuFlyoutItem x:Name="ChooseShadersItem" Text="Глобальные шейдеры" Click="ChooseShadersButton_Click"/>
+                                <MenuFlyoutItem x:Name="ReShadeAddonsItem" Text="Глобальные аддоны" Click="ReShadeAddonsButton_Click"/>
                             </MenuFlyout>
                         </Button.Flyout>
                     </Button>
 
                     <Border Width="1" Height="20" Background="{StaticResource BorderSubtleBrush}" Margin="4,0"/>
 
-                    <Button x:Name="UpdateBtn" Content="Update All"
+                    <Button x:Name="UpdateBtn" Content="Обновить всё"
                             Click="UpdateAllButton_Click"
                             Background="{StaticResource SurfaceInputBrush}"
                             Foreground="{StaticResource TextTertiaryBrush}"
@@ -92,31 +92,31 @@
 
                     <Border Width="1" Height="20" Background="{StaticResource BorderSubtleBrush}" Margin="4,0"/>
 
-                    <Button x:Name="LinksBtn" Content="Links"
+                    <Button x:Name="LinksBtn" Content="Ссылки"
                             Background="{StaticResource SurfaceInputBrush}" Foreground="{StaticResource AccentTealBrush}"
                             BorderBrush="{StaticResource AccentTealBorderBrush}" BorderThickness="1"
                             CornerRadius="8" Padding="12,7" FontSize="12"
-                            ToolTipService.ToolTip="Useful links and resources">
+                            ToolTipService.ToolTip="Полезные ссылки и ресурсы">
                         <Button.Flyout>
                             <MenuFlyout Placement="Bottom">
-                                <MenuFlyoutItem Text="RenoDX Wiki" Click="LinkRenoDxWiki_Click"/>
-                                <MenuFlyoutItem Text="Luma Wiki" Click="LinkLumaWiki_Click"/>
-                                <MenuFlyoutItem Text="RHI GitHub" Click="LinkRhiGithub_Click"/>
-                                <MenuFlyoutItem Text="ReLimiter GitHub" Click="LinkReLimiterGithub_Click"/>
+                                <MenuFlyoutItem Text="Вики RenoDX" Click="LinkRenoDxWiki_Click"/>
+                                <MenuFlyoutItem Text="Вики Luma" Click="LinkLumaWiki_Click"/>
+                                <MenuFlyoutItem Text="RHI на GitHub" Click="LinkRhiGithub_Click"/>
+                                <MenuFlyoutItem Text="ReLimiter на GitHub" Click="LinkReLimiterGithub_Click"/>
                             </MenuFlyout>
                         </Button.Flyout>
                     </Button>
-                    <Button x:Name="SupportBtn" Content="Help"
+                    <Button x:Name="SupportBtn" Content="Справка"
                             Background="{StaticResource SurfaceInputBrush}" Foreground="{StaticResource AccentTealBrush}"
                             BorderBrush="{StaticResource AccentTealBorderBrush}" BorderThickness="1"
                             CornerRadius="8" Padding="12,7" FontSize="12"
-                            ToolTipService.ToolTip="Support and help resources">
+                            ToolTipService.ToolTip="Поддержка и справочные ресурсы">
                         <Button.Flyout>
                             <MenuFlyout Placement="Bottom">
                                 <MenuFlyoutItem Text="Discord" Click="SupportDiscord_Click"/>
-                                <MenuFlyoutItem Text="Guide" Click="SupportGuide_Click"/>
+                                <MenuFlyoutItem Text="Руководство" Click="SupportGuide_Click"/>
                                 <MenuFlyoutItem Text="Ko-fi" Click="SupportKofi_Click"/>
-                                <MenuFlyoutItem Text="About" Click="AboutButton_Click"/>
+                                <MenuFlyoutItem Text="О программе" Click="AboutButton_Click"/>
                             </MenuFlyout>
                         </Button.Flyout>
                     </Button>
@@ -128,13 +128,13 @@
                             Background="{StaticResource SurfaceInputBrush}" Foreground="{StaticResource AccentTealBrush}"
                             BorderBrush="{StaticResource AccentTealBorderBrush}" BorderThickness="1"
                             CornerRadius="8" Padding="12,7" FontSize="12"
-                            ToolTipService.ToolTip="Toggle between Detail and Simple view"/>
-                    <Button x:Name="SettingsBtn" Content="Settings"
+                            ToolTipService.ToolTip="Переключить подробный и простой вид"/>
+                    <Button x:Name="SettingsBtn" Content="Настройки"
                             Click="SettingsButton_Click" IsEnabled="False"
                             Background="{StaticResource SurfaceInputBrush}" Foreground="{StaticResource AccentTealBrush}"
                             BorderBrush="{StaticResource AccentTealBorderBrush}" BorderThickness="1"
                             CornerRadius="8" Padding="12,7" FontSize="12"
-                            ToolTipService.ToolTip="Open settings"/>
+                            ToolTipService.ToolTip="Открыть настройки"/>
                 </StackPanel>
             </Grid>
         </Border>
@@ -147,7 +147,7 @@
                         Spacing="16" Visibility="Collapsed" Canvas.ZIndex="10">
                 <ProgressRing x:Name="LoadingRing" IsActive="True" Width="48" Height="48"
                               Foreground="{StaticResource AccentBlueBrush}"/>
-                <TextBlock x:Name="LoadingTitle" Text="Loading..." FontSize="16"
+                <TextBlock x:Name="LoadingTitle" Text="Загрузка..." FontSize="16"
                            Foreground="{StaticResource ChipTextBrush}" HorizontalAlignment="Center"/>
                 <TextBlock x:Name="LoadingSubtitle" Text="" FontSize="12"
                            Foreground="{StaticResource TextDisabledBrush}" HorizontalAlignment="Center"/>
@@ -179,7 +179,7 @@
                                     <ColumnDefinition Width="Auto"/>
                                 </Grid.ColumnDefinitions>
                                 <TextBox x:Name="SearchBox" Grid.Column="0"
-                                         PlaceholderText="Filter games..."
+                                         PlaceholderText="Фильтр игр..."
                                          TextChanged="SearchBox_TextChanged"
                                          Background="{StaticResource SurfaceInputBrush}" Foreground="{StaticResource TextSecondaryBrush}"
                                          BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1"
@@ -197,7 +197,7 @@
                                         MinWidth="0" MinHeight="0"
                                         FontSize="14" FontWeight="Bold"
                                         VerticalAlignment="Center"
-                                        ToolTipService.ToolTip="Save current search as a custom filter"/>
+                                        ToolTipService.ToolTip="Сохранить текущий поиск как свой фильтр"/>
                                 <Button x:Name="AddGameBtn" Grid.Column="2"
                                         Content="+"
                                         Click="AddGameButton_Click"
@@ -209,7 +209,7 @@
                                         MinWidth="0" MinHeight="0"
                                         FontSize="14" FontWeight="Bold"
                                         VerticalAlignment="Center"
-                                        ToolTipService.ToolTip="Manually add a game"/>
+                                        ToolTipService.ToolTip="Добавить игру вручную"/>
                             </Grid>
                         </Border>
 
@@ -218,19 +218,19 @@
                                 BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="0,0,0,1">
                             <StackPanel Spacing="4">
                                 <StackPanel Orientation="Horizontal" Spacing="4">
-                                    <Button x:Name="FilterDetected" Content="All Games" Tag="Detected"
+                                    <Button x:Name="FilterDetected" Content="Все игры" Tag="Detected"
                                             Click="Filter_Click"
                                             Background="{StaticResource ChipActiveBrush}" Foreground="White"
                                             BorderThickness="0" CornerRadius="7" Padding="10,5" FontSize="11"/>
-                                    <Button x:Name="FilterInstalled" Content="Installed" Tag="Installed"
+                                    <Button x:Name="FilterInstalled" Content="Установлено" Tag="Installed"
                                             Click="Filter_Click"
                                             Background="{StaticResource ChipDefaultBrush}" Foreground="{StaticResource ChipTextBrush}"
                                             BorderThickness="0" CornerRadius="7" Padding="10,5" FontSize="11"/>
-                                    <Button x:Name="FilterFavourites" Content="Favourites" Tag="Favourites"
+                                    <Button x:Name="FilterFavourites" Content="Избранное" Tag="Favourites"
                                             Click="Filter_Click"
                                             Background="{StaticResource ChipDefaultBrush}" Foreground="{StaticResource ChipTextBrush}"
                                             BorderThickness="0" CornerRadius="7" Padding="10,5" FontSize="11"/>
-                                    <Button x:Name="FilterHidden" Content="Hidden" Tag="Hidden"
+                                    <Button x:Name="FilterHidden" Content="Скрытые" Tag="Hidden"
                                             Click="Filter_Click"
                                             Background="{StaticResource ChipDefaultBrush}" Foreground="{StaticResource ChipTextBrush}"
                                             BorderThickness="0" CornerRadius="7" Padding="10,5" FontSize="11"/>
@@ -244,7 +244,7 @@
                                             Click="Filter_Click"
                                             Background="{StaticResource ChipDefaultBrush}" Foreground="{StaticResource ChipTextBrush}"
                                             BorderThickness="0" CornerRadius="7" Padding="10,5" FontSize="11"/>
-                                    <Button x:Name="FilterOther" Content="Other" Tag="Other"
+                                    <Button x:Name="FilterOther" Content="Другое" Tag="Другое"
                                             Click="Filter_Click"
                                             Background="{StaticResource ChipDefaultBrush}" Foreground="{StaticResource ChipTextBrush}"
                                             BorderThickness="0" CornerRadius="7" Padding="10,5" FontSize="11"/>
@@ -267,9 +267,9 @@
                         <Border Grid.Row="2" Padding="12,4,12,4">
                             <StackPanel Orientation="Horizontal" Spacing="10">
                                 <Ellipse x:Name="StatusDot" Width="7" Height="7" Fill="{StaticResource AccentGreenDimBrush}"/>
-                                <TextBlock x:Name="GameCountText" Text="0 shown"
+                                <TextBlock x:Name="GameCountText" Text="показано: 0"
                                            FontSize="11" Foreground="{StaticResource TextDisabledBrush}" VerticalAlignment="Center"/>
-                                <TextBlock x:Name="InstalledCountText" Text="0 installed"
+                                <TextBlock x:Name="InstalledCountText" Text="установлено: 0"
                                            FontSize="11" Foreground="{StaticResource AccentGreenDimBrush}" VerticalAlignment="Center"/>
                                 <TextBlock x:Name="HiddenCountText" Text=""
                                            FontSize="11" Foreground="{StaticResource AccentPurpleDimBrush}" VerticalAlignment="Center"/>
@@ -380,8 +380,8 @@
                                                 CornerRadius="6" Padding="10,4" MinWidth="0" MinHeight="0"
                                                 FontSize="11" FontWeight="SemiBold"
                                                 Click="LaunchGame_Click"
-                                                ToolTipService.ToolTip="Launch this game">
-                                            <TextBlock Text="▶ Launch" FontSize="11"/>
+                                                ToolTipService.ToolTip="Запустить игру">
+                                            <TextBlock Text="▶ Запустить" FontSize="11"/>
                                         </Button>
                                         <Button x:Name="DetailHdrToggleBtn" x:FieldModifier="internal"
                                                 Background="{StaticResource SurfaceOverlayBrush}"
@@ -390,7 +390,7 @@
                                                 CornerRadius="6" Padding="8,4" MinWidth="0" MinHeight="0"
                                                 FontSize="11"
                                                 Click="HdrToggle_Click"
-                                                ToolTipService.ToolTip="Toggle per-game HDR auto-switch. Purple = HDR enabled on launch, disabled on exit.">
+                                                ToolTipService.ToolTip="Автопереключение HDR для игры. Фиолетовый = HDR включается при запуске и выключается при выходе.">
                                             <TextBlock x:Name="DetailHdrToggleText" x:FieldModifier="internal" FontSize="11" Text="HDR"/>
                                         </Button>
                                         <Button x:Name="DetailResToggleBtn" x:FieldModifier="internal"
@@ -401,7 +401,7 @@
                                                 CornerRadius="6" Padding="8,4" MinWidth="0" MinHeight="0"
                                                 FontSize="11"
                                                 Click="ResToggle_Click"
-                                                ToolTipService.ToolTip="Toggle per-game resolution switch. Purple = resolution changes on launch, restores on exit.">
+                                                ToolTipService.ToolTip="Переключение разрешения для игры. Фиолетовый = разрешение меняется при запуске и восстанавливается при выходе.">
                                             <TextBlock x:Name="DetailResToggleText" x:FieldModifier="internal" FontSize="11" Text="RES"/>
                                         </Button>
                                         <Border Width="1" Background="{StaticResource BorderSubtleBrush}" Margin="4,2"/>
@@ -413,7 +413,7 @@
                                                 FontSize="11"
                                                 Click="NexusModsLink_Click"
                                                 Visibility="Collapsed"
-                                                ToolTipService.ToolTip="Open Nexus Mods page">
+                                                ToolTipService.ToolTip="Открыть страницу на Nexus Mods">
                                             <TextBlock Text="Nexus" TextDecorations="Underline"/>
                                         </Button>
                                         <Button x:Name="DetailPcgwBtn" x:FieldModifier="internal"
@@ -424,7 +424,7 @@
                                                 FontSize="11"
                                                 Click="PcgwLink_Click"
                                                 Visibility="Collapsed"
-                                                ToolTipService.ToolTip="Open PCGamingWiki page">
+                                                ToolTipService.ToolTip="Открыть страницу PCGamingWiki">
                                             <TextBlock Text="PCGW" TextDecorations="Underline"/>
                                         </Button>
                                         <Button x:Name="DetailUwFixBtn" x:FieldModifier="internal"
@@ -435,7 +435,7 @@
                                                 FontSize="11"
                                                 Click="UwFixLink_Click"
                                                 Visibility="Collapsed"
-                                                ToolTipService.ToolTip="Open ultrawide fix page">
+                                                ToolTipService.ToolTip="Открыть страницу ультраширокого исправления">
                                             <TextBlock Text="UW Fix" TextDecorations="Underline"/>
                                         </Button>
                                         <Button x:Name="DetailUltraPlusBtn" x:FieldModifier="internal"
@@ -446,7 +446,7 @@
                                                 FontSize="11"
                                                 Click="UltraPlusLink_Click"
                                                 Visibility="Collapsed"
-                                                ToolTipService.ToolTip="Open Ultra+ page for this game">
+                                                ToolTipService.ToolTip="Открыть страницу Ultra+ для этой игры">
                                             <TextBlock Text="Ultra+" TextDecorations="Underline"/>
                                         </Button>
                                     </StackPanel>
@@ -501,12 +501,12 @@
                                     <Border x:Name="Detail32BitBadge" x:FieldModifier="internal" CornerRadius="5" Padding="6,2"
                                             Background="{StaticResource AccentAmberBgBrush}" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1"
                                             VerticalAlignment="Center" Visibility="Collapsed">
-                                        <TextBlock Text="32-bit" FontSize="11" Foreground="{StaticResource AccentAmberBrush}"/>
+                                        <TextBlock Text="32-бит" FontSize="11" Foreground="{StaticResource AccentAmberBrush}"/>
                                     </Border>
                                     <Border x:Name="Detail64BitBadge" x:FieldModifier="internal" CornerRadius="5" Padding="6,2"
                                             Background="{StaticResource ChipDefaultBrush}" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1"
                                             VerticalAlignment="Center" Visibility="Collapsed">
-                                        <TextBlock Text="64-bit" FontSize="11" Foreground="{StaticResource ChipTextBrush}"/>
+                                        <TextBlock Text="64-бит" FontSize="11" Foreground="{StaticResource ChipTextBrush}"/>
                                     </Border>
 
                                     <!-- Separator: platform → status -->
@@ -543,7 +543,7 @@
                                             CornerRadius="6" Padding="8,4" MinWidth="0" MinHeight="0"
                                             FontSize="11" HorizontalAlignment="Stretch"
                                             Click="HideButton_Click"
-                                            ToolTipService.ToolTip="Hide this game from the sidebar (find it again in the Hidden filter)">
+                                            ToolTipService.ToolTip="Скрыть игру из боковой панели (найдёте её во фильтре «Скрытые»)">
                                         <TextBlock x:Name="DetailHideIcon" x:FieldModifier="internal" FontSize="11" HorizontalAlignment="Center"/>
                                     </Button>
                                     <Button Grid.Row="0" Grid.Column="1" x:Name="DetailFavBtn" x:FieldModifier="internal"
@@ -553,8 +553,8 @@
                                             CornerRadius="6" Padding="8,4" MinWidth="0" MinHeight="0"
                                             FontSize="11" HorizontalAlignment="Stretch"
                                             Click="FavouriteButton_Click"
-                                            ToolTipService.ToolTip="Toggle favourite">
-                                        <TextBlock x:Name="DetailFavIcon" x:FieldModifier="internal" FontSize="11" Text="Favourite" HorizontalAlignment="Center"/>
+                                            ToolTipService.ToolTip="Добавить/убрать из избранного">
+                                        <TextBlock x:Name="DetailFavIcon" x:FieldModifier="internal" FontSize="11" Text="В избранное" HorizontalAlignment="Center"/>
                                     </Button>
                                     <Button Grid.Row="1" Grid.Column="0" x:Name="DetailAppDataBtn" x:FieldModifier="internal"
                                             Background="{StaticResource SurfaceOverlayBrush}"
@@ -564,8 +564,8 @@
                                             FontSize="11" HorizontalAlignment="Stretch"
                                             Click="OpenAppData_Click"
                                             Visibility="Collapsed"
-                                            ToolTipService.ToolTip="Open game config folder in Explorer">
-                                        <TextBlock Text="Config" TextDecorations="Underline" HorizontalAlignment="Center"/>
+                                            ToolTipService.ToolTip="Открыть папку конфигурации игры в Проводнике">
+                                        <TextBlock Text="Конфиг" TextDecorations="Underline" HorizontalAlignment="Center"/>
                                     </Button>
                                     <Button Grid.Row="1" Grid.Column="1" x:Name="DetailFolderBtn" x:FieldModifier="internal"
                                             Background="{StaticResource SurfaceOverlayBrush}"
@@ -574,8 +574,8 @@
                                             CornerRadius="6" Padding="8,4" MinWidth="0" MinHeight="0"
                                             FontSize="11" HorizontalAlignment="Stretch"
                                             Click="OpenFolder_Click"
-                                            ToolTipService.ToolTip="Open game folder in Explorer">
-                                        <TextBlock Text="Browse" TextDecorations="Underline" HorizontalAlignment="Center"/>
+                                            ToolTipService.ToolTip="Открыть папку игры в Проводнике">
+                                        <TextBlock Text="Обзор" TextDecorations="Underline" HorizontalAlignment="Center"/>
                                     </Button>
                                 </Grid>
                             </Border>
@@ -604,7 +604,7 @@
                                                    Foreground="{StaticResource TextTertiaryBrush}"
                                                    Margin="0,0,6,0"/>
                                         <TextBlock x:Name="DetailComponentsTitle" x:FieldModifier="internal"
-                                                   Text="Components" FontSize="13" FontWeight="SemiBold"
+                                                   Text="Компоненты" FontSize="13" FontWeight="SemiBold"
                                                    Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                                     </StackPanel>
                                     <TextBlock x:Name="DetailLumaInfoText" x:FieldModifier="internal" Grid.Column="3"
@@ -638,7 +638,7 @@
                                                PointerPressed="DetailRefStatus_PointerPressed"
                                                PointerEntered="LinkText_PointerEntered" PointerExited="LinkText_PointerExited"/>
                                     <Button x:Name="DetailRefInfoBtn" x:FieldModifier="internal" Grid.Column="2"
-                                            Content="Info" FontSize="11" Padding="6,2,6,2"
+                                            Content="Инфо" FontSize="11" Padding="6,2,6,2"
                                             Background="{StaticResource SurfaceOverlayBrush}"
                                             Foreground="{StaticResource TextSecondaryBrush}"
                                             BorderBrush="{StaticResource BorderStrongBrush}" BorderThickness="1"
@@ -654,7 +654,7 @@
                                             Background="{StaticResource AccentRedBgBrush}" Foreground="{StaticResource AccentRedBrush}"
                                             BorderBrush="{StaticResource AccentPurpleBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Width="36" Height="32" Padding="0"
-                                            ToolTipService.ToolTip="Remove RE Framework" Opacity="0" IsHitTestVisible="False">
+                                            ToolTipService.ToolTip="Удалить RE Framework" Opacity="0" IsHitTestVisible="False">
                                         <TextBlock Text="✕" FontSize="12" HorizontalAlignment="Center" Foreground="{StaticResource AccentRedBrush}"/>
                                     </Button>
                                 </Grid>
@@ -676,7 +676,7 @@
                                                PointerPressed="DetailRsStatus_PointerPressed"
                                                PointerEntered="LinkText_PointerEntered" PointerExited="LinkText_PointerExited"/>
                                     <Button x:Name="DetailRsInfoBtn" x:FieldModifier="internal" Grid.Column="2"
-                                            Content="Info" FontSize="11" Padding="6,2,6,2"
+                                            Content="Инфо" FontSize="11" Padding="6,2,6,2"
                                             Background="{StaticResource SurfaceOverlayBrush}"
                                             Foreground="{StaticResource TextSecondaryBrush}"
                                             BorderBrush="{StaticResource BorderStrongBrush}" BorderThickness="1"
@@ -691,7 +691,7 @@
                                             Background="{StaticResource SurfaceOverlayBrush}" Foreground="{StaticResource TextSecondaryBrush}"
                                             BorderBrush="{StaticResource BorderStrongBrush}" BorderThickness="1"
                                             CornerRadius="8" Width="36" Height="32" Padding="0"
-                                            ToolTipService.ToolTip="ReShade Settings">
+                                            ToolTipService.ToolTip="Настройки ReShade">
                                         <TextBlock Text="⚙" FontSize="14" HorizontalAlignment="Center"/>
                                     </Button>
                                     <Button x:Name="DetailRsDeleteBtn" x:FieldModifier="internal" Grid.Column="5"
@@ -699,7 +699,7 @@
                                             Background="{StaticResource AccentRedBgBrush}" Foreground="{StaticResource AccentRedBrush}"
                                             BorderBrush="{StaticResource AccentPurpleBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Width="36" Height="32" Padding="0"
-                                            ToolTipService.ToolTip="Remove ReShade" Opacity="0" IsHitTestVisible="False">
+                                            ToolTipService.ToolTip="Удалить ReShade" Opacity="0" IsHitTestVisible="False">
                                         <TextBlock Text="✕" FontSize="12" HorizontalAlignment="Center" Foreground="{StaticResource AccentRedBrush}"/>
                                     </Button>
                                 </Grid>
@@ -714,7 +714,7 @@
                                         <ColumnDefinition Width="36"/>
                                         <ColumnDefinition Width="36"/>
                                     </Grid.ColumnDefinitions>
-                                    <TextBlock Grid.Column="3" Text="———  HDR Mods — Install one or both  ———" FontSize="11"
+                                    <TextBlock Grid.Column="3" Text="———  HDR-моды — установите один или оба  ———" FontSize="11"
                                                Foreground="{StaticResource TextSecondaryBrush}"
                                                HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                 </Grid>
@@ -736,7 +736,7 @@
                                                PointerPressed="DetailRdxStatus_PointerPressed"
                                                PointerEntered="LinkText_PointerEntered" PointerExited="LinkText_PointerExited"/>
                                     <Button x:Name="DetailRdxInfoBtn" x:FieldModifier="internal" Grid.Column="2"
-                                            Content="Info" FontSize="11" Padding="6,2,6,2"
+                                            Content="Инфо" FontSize="11" Padding="6,2,6,2"
                                             Background="{StaticResource SurfaceOverlayBrush}"
                                             Foreground="{StaticResource TextSecondaryBrush}"
                                             BorderBrush="{StaticResource BorderStrongBrush}" BorderThickness="1"
@@ -751,7 +751,7 @@
                                             Background="{StaticResource SurfaceOverlayBrush}" Foreground="{StaticResource TextSecondaryBrush}"
                                             BorderBrush="{StaticResource BorderStrongBrush}" BorderThickness="1"
                                             CornerRadius="8" Width="36" Height="32" Padding="0"
-                                            ToolTipService.ToolTip="RenoDX Settings">
+                                            ToolTipService.ToolTip="Настройки RenoDX">
                                         <TextBlock Text="⚙" FontSize="14" HorizontalAlignment="Center"/>
                                     </Button>
                                     <Button x:Name="DetailRdxDeleteBtn" x:FieldModifier="internal" Grid.Column="5"
@@ -759,7 +759,7 @@
                                             Background="{StaticResource AccentRedBgBrush}" Foreground="{StaticResource AccentRedBrush}"
                                             BorderBrush="{StaticResource AccentPurpleBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Width="36" Height="32" Padding="0"
-                                            ToolTipService.ToolTip="Remove RenoDX mod" Opacity="0" IsHitTestVisible="False">
+                                            ToolTipService.ToolTip="Удалить мод RenoDX" Opacity="0" IsHitTestVisible="False">
                                         <TextBlock Text="✕" FontSize="12" HorizontalAlignment="Center" Foreground="{StaticResource AccentRedBrush}"/>
                                     </Button>
                                 </Grid>
@@ -781,7 +781,7 @@
                                                PointerPressed="DetailLumaStatus_PointerPressed"
                                                PointerEntered="LinkText_PointerEntered" PointerExited="LinkText_PointerExited"/>
                                     <Button x:Name="DetailLumaInfoBtn" x:FieldModifier="internal" Grid.Column="2"
-                                            Content="Info" FontSize="11" Padding="6,2,6,2"
+                                            Content="Инфо" FontSize="11" Padding="6,2,6,2"
                                             Background="{StaticResource SurfaceOverlayBrush}"
                                             Foreground="{StaticResource TextSecondaryBrush}"
                                             BorderBrush="{StaticResource BorderStrongBrush}" BorderThickness="1"
@@ -797,7 +797,7 @@
                                             Background="{StaticResource SurfaceOverlayBrush}" Foreground="{StaticResource TextSecondaryBrush}"
                                             BorderBrush="{StaticResource BorderStrongBrush}" BorderThickness="1"
                                             CornerRadius="8" Width="36" Height="32" Padding="0"
-                                            ToolTipService.ToolTip="Luma Settings">
+                                            ToolTipService.ToolTip="Настройки Luma">
                                         <TextBlock Text="⚙" FontSize="12" HorizontalAlignment="Center"/>
                                     </Button>
                                     <Button x:Name="DetailLumaDeleteBtn" x:FieldModifier="internal" Grid.Column="5"
@@ -805,7 +805,7 @@
                                             Background="{StaticResource AccentRedBgBrush}" Foreground="{StaticResource AccentRedBrush}"
                                             BorderBrush="{StaticResource AccentPurpleBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Width="36" Height="32" Padding="0"
-                                            ToolTipService.ToolTip="Remove Luma mod" Opacity="0" IsHitTestVisible="False">
+                                            ToolTipService.ToolTip="Удалить мод Luma" Opacity="0" IsHitTestVisible="False">
                                         <TextBlock Text="✕" FontSize="12" HorizontalAlignment="Center" Foreground="{StaticResource AccentRedBrush}"/>
                                     </Button>
                                 </Grid>
@@ -820,7 +820,7 @@
                                         <ColumnDefinition Width="36"/>
                                         <ColumnDefinition Width="36"/>
                                     </Grid.ColumnDefinitions>
-                                    <TextBlock Grid.Column="3" Text="——  Recommended  ——" FontSize="11"
+                                    <TextBlock Grid.Column="3" Text="——  Рекомендуемое  ——" FontSize="11"
                                                Foreground="{StaticResource TextSecondaryBrush}"
                                                HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                 </Grid>
@@ -842,7 +842,7 @@
                                                PointerPressed="DetailDofFixStatus_PointerPressed"
                                                PointerEntered="LinkText_PointerEntered" PointerExited="LinkText_PointerExited"/>
                                     <Button x:Name="DetailDofFixInfoBtn" x:FieldModifier="internal" Grid.Column="2"
-                                            Content="Info" FontSize="11" Padding="6,2,6,2"
+                                            Content="Инфо" FontSize="11" Padding="6,2,6,2"
                                             Background="{StaticResource SurfaceOverlayBrush}"
                                             Foreground="{StaticResource TextSecondaryBrush}"
                                             BorderBrush="{StaticResource BorderStrongBrush}" BorderThickness="1"
@@ -858,7 +858,7 @@
                                             Background="{StaticResource SurfaceOverlayBrush}" Foreground="{StaticResource TextSecondaryBrush}"
                                             BorderBrush="{StaticResource BorderStrongBrush}" BorderThickness="1"
                                             CornerRadius="8" Width="36" Height="32" Padding="0"
-                                            ToolTipService.ToolTip="DOF Fix Settings">
+                                            ToolTipService.ToolTip="Настройки DOF Fix">
                                         <TextBlock Text="⚙" FontSize="14" HorizontalAlignment="Center"/>
                                     </Button>
                                     <Button x:Name="DetailDofFixDeleteBtn" x:FieldModifier="internal" Grid.Column="5"
@@ -866,7 +866,7 @@
                                             Background="{StaticResource AccentRedBgBrush}" Foreground="{StaticResource AccentRedBrush}"
                                             BorderBrush="{StaticResource AccentPurpleBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Width="36" Height="32" Padding="0"
-                                            ToolTipService.ToolTip="Remove DOF Fix" Opacity="0" IsHitTestVisible="False">
+                                            ToolTipService.ToolTip="Удалить DOF Fix" Opacity="0" IsHitTestVisible="False">
                                         <TextBlock Text="✕" FontSize="12" HorizontalAlignment="Center" Foreground="{StaticResource AccentRedBrush}"/>
                                     </Button>
                                 </Grid>
@@ -881,7 +881,7 @@
                                         <ColumnDefinition Width="36"/>
                                         <ColumnDefinition Width="36"/>
                                     </Grid.ColumnDefinitions>
-                                    <TextBlock Grid.Column="3" Text="———  Frame limiters — Choose one  ———" FontSize="11"
+                                    <TextBlock Grid.Column="3" Text="———  Ограничители FPS — выберите один  ———" FontSize="11"
                                                Foreground="{StaticResource TextSecondaryBrush}"
                                                HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                 </Grid>
@@ -903,7 +903,7 @@
                                                PointerPressed="DetailUlStatus_PointerPressed"
                                                PointerEntered="LinkText_PointerEntered" PointerExited="LinkText_PointerExited"/>
                                     <Button x:Name="DetailUlInfoBtn" x:FieldModifier="internal" Grid.Column="2"
-                                            Content="Info" FontSize="11" Padding="6,2,6,2"
+                                            Content="Инфо" FontSize="11" Padding="6,2,6,2"
                                             Background="{StaticResource SurfaceOverlayBrush}"
                                             Foreground="{StaticResource TextSecondaryBrush}"
                                             BorderBrush="{StaticResource BorderStrongBrush}" BorderThickness="1"
@@ -918,7 +918,7 @@
                                             Background="{StaticResource SurfaceOverlayBrush}" Foreground="{StaticResource TextSecondaryBrush}"
                                             BorderBrush="{StaticResource BorderStrongBrush}" BorderThickness="1"
                                             CornerRadius="8" Width="36" Height="32" Padding="0"
-                                            ToolTipService.ToolTip="ReLimiter Settings">
+                                            ToolTipService.ToolTip="Настройки ReLimiter">
                                         <TextBlock Text="⚙" FontSize="14" HorizontalAlignment="Center"/>
                                     </Button>
                                     <Button x:Name="DetailUlDeleteBtn" x:FieldModifier="internal" Grid.Column="5"
@@ -926,7 +926,7 @@
                                             Background="{StaticResource AccentRedBgBrush}" Foreground="{StaticResource AccentRedBrush}"
                                             BorderBrush="{StaticResource AccentPurpleBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Width="36" Height="32" Padding="0"
-                                            ToolTipService.ToolTip="Remove ReLimiter" Opacity="0" IsHitTestVisible="False">
+                                            ToolTipService.ToolTip="Удалить ReLimiter" Opacity="0" IsHitTestVisible="False">
                                         <TextBlock Text="✕" FontSize="12" HorizontalAlignment="Center" Foreground="{StaticResource AccentRedBrush}"/>
                                     </Button>
                                 </Grid>
@@ -948,7 +948,7 @@
                                                PointerPressed="DetailDcStatus_PointerPressed"
                                                PointerEntered="LinkText_PointerEntered" PointerExited="LinkText_PointerExited"/>
                                     <Button x:Name="DetailDcInfoBtn" x:FieldModifier="internal" Grid.Column="2"
-                                            Content="Info" FontSize="11" Padding="6,2,6,2"
+                                            Content="Инфо" FontSize="11" Padding="6,2,6,2"
                                             Background="{StaticResource SurfaceOverlayBrush}"
                                             Foreground="{StaticResource TextSecondaryBrush}"
                                             BorderBrush="{StaticResource BorderStrongBrush}" BorderThickness="1"
@@ -963,7 +963,7 @@
                                             Background="{StaticResource SurfaceOverlayBrush}" Foreground="{StaticResource TextSecondaryBrush}"
                                             BorderBrush="{StaticResource BorderStrongBrush}" BorderThickness="1"
                                             CornerRadius="8" Width="36" Height="32" Padding="0"
-                                            ToolTipService.ToolTip="Display Commander Settings">
+                                            ToolTipService.ToolTip="Настройки Display Commander">
                                         <TextBlock Text="⚙" FontSize="14" HorizontalAlignment="Center"/>
                                     </Button>
                                     <Button x:Name="DetailDcDeleteBtn" x:FieldModifier="internal" Grid.Column="5"
@@ -971,7 +971,7 @@
                                             Background="{StaticResource AccentRedBgBrush}" Foreground="{StaticResource AccentRedBrush}"
                                             BorderBrush="{StaticResource AccentPurpleBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Width="36" Height="32" Padding="0"
-                                            ToolTipService.ToolTip="Remove Display Commander" Opacity="0" IsHitTestVisible="False">
+                                            ToolTipService.ToolTip="Удалить Display Commander" Opacity="0" IsHitTestVisible="False">
                                         <TextBlock Text="✕" FontSize="12" HorizontalAlignment="Center" Foreground="{StaticResource AccentRedBrush}"/>
                                     </Button>
                                 </Grid>
@@ -980,7 +980,7 @@
                                 <Border x:Name="DetailNoModMsg" x:FieldModifier="internal" Visibility="Collapsed"
                                         Background="{StaticResource AccentPurpleBgBrush}" CornerRadius="8" Padding="12,8"
                                         BorderBrush="{StaticResource AccentPurpleBorderBrush}" BorderThickness="1">
-                                    <TextBlock Text="No RenoDX mod available for this game"
+                                    <TextBlock Text="Для этой игры нет мода RenoDX"
                                                FontSize="12" Foreground="{StaticResource TextDisabledBrush}" TextAlignment="Center"/>
                                 </Border>
 
@@ -1131,12 +1131,12 @@
                 <Border Grid.Row="0" Padding="24,12,24,10" Background="Transparent"
                         BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="0,0,0,1">
                     <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
-                        <Button x:Name="SettingsBackBtn" Content="← Back to Games"
+                        <Button x:Name="SettingsBackBtn" Content="← К играм"
                                 Click="SettingsBack_Click"
                                 Background="{StaticResource SurfaceOverlayBrush}" Foreground="{StaticResource TextSecondaryBrush}"
                                 BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1"
                                 CornerRadius="8" Padding="12,6" FontSize="12"/>
-                        <TextBlock Text="Settings" FontSize="20" FontWeight="Bold"
+                        <TextBlock Text="Настройки" FontSize="20" FontWeight="Bold"
                                    Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                     </StackPanel>
                 </Border>
@@ -1152,7 +1152,7 @@
                         <Border Background="{StaticResource SurfaceRaisedBrush}" CornerRadius="10" Padding="20,16"
                                 BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                             <StackPanel Spacing="12">
-                            <TextBlock Text="Component Updates" FontSize="15" FontWeight="Bold"
+                            <TextBlock Text="Обновления компонентов" FontSize="15" FontWeight="Bold"
                                        Foreground="{StaticResource TextPrimaryBrush}"/>
                             <Grid ColumnSpacing="0" RowSpacing="8">
                                 <Grid.ColumnDefinitions>
@@ -1168,18 +1168,18 @@
                                         <RowDefinition Height="Auto"/>
                                     </Grid.RowDefinitions>
                                     <StackPanel Grid.Row="0" Spacing="8">
-                                        <TextBlock Text="Check For Updates" FontSize="13" FontWeight="SemiBold"
+                                        <TextBlock Text="Проверить обновления" FontSize="13" FontWeight="SemiBold"
                                                    Foreground="{StaticResource TextPrimaryBrush}"/>
                                         <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                                   Text="Fetches the latest manifest data and checks all components for available updates. Bypasses the 4-hour cooldown."/>
+                                                   Text="Загружает свежий манифест и проверяет все компоненты на обновления. Пропускает 4-часовую задержку."/>
                                     </StackPanel>
-                                    <Button Grid.Row="1" x:Name="CheckForUpdatesBtn" Content="Check For Updates"
+                                    <Button Grid.Row="1" x:Name="CheckForUpdatesBtn" Content="Проверить обновления"
                                             Click="CheckForUpdatesButton_Click"
                                             Background="{StaticResource AccentBlueBgBrush}" Foreground="{StaticResource AccentBlueBrush}"
                                             BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Padding="12,7" FontSize="12"
                                             HorizontalAlignment="Stretch"
-                                            ToolTipService.ToolTip="Check all components and the app for available updates"/>
+                                            ToolTipService.ToolTip="Проверить наличие обновлений всех компонентов и приложения"/>
                                 </Grid>
 
                                 <!-- Divider -->
@@ -1189,16 +1189,16 @@
 
                                 <!-- Right: Automatic Updates -->
                                 <StackPanel Grid.Column="2" Spacing="8">
-                                    <TextBlock Text="Automatic Updates" FontSize="13" FontWeight="SemiBold"
+                                    <TextBlock Text="Автоматические обновления" FontSize="13" FontWeight="SemiBold"
                                                Foreground="{StaticResource TextPrimaryBrush}"/>
                                     <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                               Text="When enabled, RHI silently installs component updates in the background after each update check. Games that are running when an update is detected will be retried automatically once they close. Does not apply to app updates."/>
+                                               Text="Если включено, RHI тихо устанавливает обновления компонентов в фоне после каждой проверки. Если игра с обновлением была запущена, попытка повторится автоматически после её закрытия. На обновления самого приложения не влияет."/>
                                     <ComboBox x:Name="AutoUpdateComponentsCombo" x:FieldModifier="internal"
                                               FontSize="12" HorizontalAlignment="Stretch"
-                                              ToolTipService.ToolTip="Automatically install ReShade, RenoDX, ReLimiter, DC, OptiScaler, RE Framework, Luma, DXVK and DOF Fix updates silently in the background"
+                                              ToolTipService.ToolTip="Автоматически и незаметно устанавливать в фоне обновления ReShade, RenoDX, ReLimiter, DC, OptiScaler, RE Framework, Luma, DXVK и DOF Fix"
                                               SelectionChanged="AutoUpdateComponentsCombo_SelectionChanged">
-                                        <ComboBoxItem Content="No"/>
-                                        <ComboBoxItem Content="Yes"/>
+                                        <ComboBoxItem Content="Нет"/>
+                                        <ComboBoxItem Content="Да"/>
                                     </ComboBox>
                                 </StackPanel>
                             </Grid>
@@ -1211,7 +1211,7 @@
                         <Border Background="{StaticResource SurfaceRaisedBrush}" CornerRadius="10" Padding="20,16"
                                 BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                             <StackPanel Spacing="12">
-                            <TextBlock Text="ReShade &amp; Display" FontSize="15" FontWeight="Bold"
+                            <TextBlock Text="ReShade и дисплей" FontSize="15" FontWeight="Bold"
                                        Foreground="{StaticResource TextPrimaryBrush}"/>
                             <Grid ColumnSpacing="0" RowSpacing="8">
                                 <Grid.ColumnDefinitions>
@@ -1229,11 +1229,11 @@
 
                                 <!-- ── Left: Screenshots & Hotkeys ── -->
                                 <StackPanel Grid.Row="0" Grid.Column="0" Spacing="4">
-                                    <TextBlock Text="Screenshots &amp; Hotkeys" FontSize="14" FontWeight="SemiBold"
+                                    <TextBlock Text="Скриншоты и горячие клавиши" FontSize="14" FontWeight="SemiBold"
                                                Foreground="{StaticResource TextPrimaryBrush}"/>
                                     <TextBlock TextWrapping="Wrap" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                               Text="Screenshot save path and ReShade key bindings applied to all managed reshade.ini files."/>
+                                               Text="Путь сохранения скриншотов и назначение клавиш ReShade применяются ко всем управляемым reshade.ini."/>
                                 </StackPanel>
 
                                 <!-- Divider -->
@@ -1243,11 +1243,11 @@
 
                                 <!-- ── Right: HDR & Peak Brightness ── -->
                                 <StackPanel Grid.Row="0" Grid.Column="2" Spacing="4">
-                                    <TextBlock Text="HDR &amp; Peak Brightness" FontSize="14" FontWeight="SemiBold"
+                                    <TextBlock Text="HDR и пиковая яркость" FontSize="14" FontWeight="SemiBold"
                                                Foreground="{StaticResource TextPrimaryBrush}"/>
                                     <TextBlock TextWrapping="Wrap" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                               Text="Peak brightness (nits) written to reshade.ini presets. HDR auto-toggle enables Windows HDR on game launch and disables on exit."/>
+                                               Text="Пиковая яркость (в нит) записывается в пресеты reshade.ini. Автопереключение HDR включает Windows HDR при запуске игры и выключает при выходе."/>
                                 </StackPanel>
 
                                 <!-- Row 1 Left: Screenshot path + Open -->
@@ -1258,7 +1258,7 @@
                                     </Grid.ColumnDefinitions>
                                     <Grid Grid.Column="0">
                                         <TextBox x:Name="ScreenshotPathBox" x:FieldModifier="internal"
-                                                 PlaceholderText="Type or choose screenshot folder"
+                                                 PlaceholderText="Введите или выберите папку скриншотов"
                                                  FontSize="12" Padding="12,8,36,8"
                                                  Background="{StaticResource SurfaceInputBrush}"
                                                  Foreground="{StaticResource TextSecondaryBrush}"
@@ -1268,16 +1268,16 @@
                                                 Padding="8,6" MinWidth="0" MinHeight="0"
                                                 HorizontalAlignment="Right" VerticalAlignment="Center"
                                                 Margin="0,0,4,0"
-                                                ToolTipService.ToolTip="Pick a folder for screenshots">
+                                                ToolTipService.ToolTip="Выберите папку для скриншотов">
                                             <FontIcon Glyph="&#xED25;" FontSize="14"
                                                       Foreground="{StaticResource TextTertiaryBrush}"/>
                                         </Button>
                                     </Grid>
-                                    <Button Grid.Column="1" Content="Open" Click="OpenScreenshotFolder_Click"
+                                    <Button Grid.Column="1" Content="Открыть" Click="OpenScreenshotFolder_Click"
                                             Background="{StaticResource AccentBlueBgBrush}" Foreground="{StaticResource AccentBlueBrush}"
                                             BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Padding="12,6" FontSize="12"
-                                            ToolTipService.ToolTip="Open the screenshot folder in Explorer"/>
+                                            ToolTipService.ToolTip="Открыть папку скриншотов в Проводнике"/>
                                 </Grid>
 
                                 <!-- Row 1 Right: Peak Nits -->
@@ -1288,18 +1288,18 @@
                                         <ColumnDefinition Width="*"/>
                                         <ColumnDefinition Width="Auto"/>
                                     </Grid.ColumnDefinitions>
-                                    <TextBlock Grid.Column="0" Text="Peak Nits" FontSize="12"
+                                    <TextBlock Grid.Column="0" Text="Пиковая яркость" FontSize="12"
                                                Foreground="{StaticResource TextSecondaryBrush}"
                                                VerticalAlignment="Center"/>
                                     <Button Grid.Column="1" x:Name="PeakNitsAutoBtn"
-                                            Content="Auto" Click="PeakNitsAuto_Click"
+                                            Content="Авто" Click="PeakNitsAuto_Click"
                                             Background="{StaticResource AccentBlueBgBrush}"
                                             Foreground="{StaticResource AccentBlueBrush}"
                                             BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Padding="10,5" FontSize="12"
-                                            ToolTipService.ToolTip="Reads your monitor's peak brightness automatically."/>
+                                            ToolTipService.ToolTip="Автоматически считывает пиковую яркость монитора."/>
                                     <TextBox Grid.Column="2" x:Name="PeakNitsBox" x:FieldModifier="internal"
-                                             PlaceholderText="nits" FontSize="12"
+                                             PlaceholderText="нит" FontSize="12"
                                              VerticalContentAlignment="Center"
                                              Background="{StaticResource SurfaceInputBrush}"
                                              Foreground="{StaticResource TextSecondaryBrush}"
@@ -1311,7 +1311,7 @@
                                             BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1"
                                             CornerRadius="6" Padding="8,4" MinWidth="0" MinHeight="0"
                                             FontSize="12" VerticalAlignment="Center"
-                                            ToolTipService.ToolTip="Configure which presets receive the global peak nits value."/>
+                                            ToolTipService.ToolTip="Выберите, каким пресетам назначать общее значение пиковой яркости."/>
                                 </Grid>
 
                                 <!-- Row 2 Left: Overlay key | Screenshot key | Per-Game Folders combo -->
@@ -1327,15 +1327,15 @@
                                     </Grid.RowDefinitions>
                                     <TextBlock Grid.Row="0" Grid.Column="0" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}"
-                                               Text="Overlay key" Margin="0,0,0,3"/>
+                                               Text="Клавиша оверлея" Margin="0,0,0,3"/>
                                     <TextBlock Grid.Row="0" Grid.Column="1" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}"
-                                               Text="Screenshot key" Margin="0,0,0,3"/>
+                                               Text="Клавиша скриншота" Margin="0,0,0,3"/>
                                     <TextBlock Grid.Row="0" Grid.Column="2" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}"
-                                               Text="Subfolders" Margin="0,0,0,3"/>
+                                               Text="Подпапки" Margin="0,0,0,3"/>
                                     <TextBox Grid.Row="1" Grid.Column="0" x:Name="HotkeyBox" x:FieldModifier="internal"
-                                             PlaceholderText="Press a key..." IsReadOnly="True" FontSize="12"
+                                             PlaceholderText="Нажмите клавишу..." IsReadOnly="True" FontSize="12"
                                              VerticalContentAlignment="Center"
                                              Background="{StaticResource SurfaceInputBrush}"
                                              Foreground="{StaticResource TextSecondaryBrush}"
@@ -1347,15 +1347,15 @@
                                             <ColumnDefinition Width="Auto"/>
                                         </Grid.ColumnDefinitions>
                                         <TextBox Grid.Column="0" x:Name="ScreenshotHotkeyBox" x:FieldModifier="internal"
-                                                 PlaceholderText="Press a key..." IsReadOnly="True" FontSize="12"
+                                                 PlaceholderText="Нажмите клавишу..." IsReadOnly="True" FontSize="12"
                                                  VerticalContentAlignment="Center"
                                                  Background="{StaticResource SurfaceInputBrush}"
                                                  Foreground="{StaticResource TextSecondaryBrush}"
                                                  BorderBrush="{StaticResource BorderSubtleBrush}"
-                                                 ToolTipService.ToolTip="Press a key to assign it, or Backspace to clear it."
+                                                 ToolTipService.ToolTip="Нажмите клавишу, чтобы назначить её, или Backspace, чтобы очистить."
                                                  PreviewKeyDown="ScreenshotHotkeyBox_PreviewKeyDown"/>
                                         <Button Grid.Column="1" Content="↺" Click="ResetScreenshotHotkey_Click"
-                                                ToolTipService.ToolTip="Reset to Print Screen"
+                                                ToolTipService.ToolTip="Вернуть Print Screen"
                                                 Background="Transparent" BorderThickness="0"
                                                 Padding="6,4" MinWidth="0" MinHeight="0" FontSize="14"
                                                 Foreground="{StaticResource TextTertiaryBrush}"
@@ -1363,7 +1363,7 @@
                                     </Grid>
                                     <ComboBox Grid.Row="1" Grid.Column="2" x:Name="PerGameScreenshotCombo" x:FieldModifier="internal"
                                               FontSize="12" MinWidth="60"
-                                              ToolTipService.ToolTip="When On, each game gets its own screenshot subfolder."
+                                              ToolTipService.ToolTip="Вкл — у каждой игры своя подпапка для скриншотов."
                                               SelectionChanged="PerGameScreenshotCombo_SelectionChanged">
                                         <ComboBoxItem Content="Off"/>
                                         <ComboBoxItem Content="On"/>
@@ -1372,7 +1372,7 @@
 
                                 <!-- Row 2 Right: Apply to All Games (nits) -->
                                 <Button Grid.Row="2" Grid.Column="2" x:Name="ApplyScreenshotPathBtn"
-                                        Content="Apply to All Games"
+                                        Content="Применить ко всем играм"
                                         Click="ApplyPeakNitsToAll_Click"
                                         Background="{StaticResource AccentBlueBgBrush}"
                                         Foreground="{StaticResource AccentBlueBrush}"
@@ -1386,12 +1386,12 @@
                                         <ColumnDefinition Width="*"/>
                                         <ColumnDefinition Width="Auto"/>
                                     </Grid.ColumnDefinitions>
-                                    <TextBlock Grid.Column="0" Text="HDR Auto-Toggle" FontSize="12"
+                                    <TextBlock Grid.Column="0" Text="Автопереключение HDR" FontSize="12"
                                                Foreground="{StaticResource TextSecondaryBrush}"
                                                VerticalAlignment="Center"/>
                                     <ComboBox Grid.Column="1" x:Name="HdrAutoToggleCombo" x:FieldModifier="internal"
                                               FontSize="12" HorizontalAlignment="Stretch" VerticalAlignment="Center"
-                                              ToolTipService.ToolTip="When On, Windows HDR is automatically enabled when launching a game through RHI and disabled when the game exits."
+                                              ToolTipService.ToolTip="Вкл — Windows HDR автоматически включается при запуске игры через RHI и выключается при выходе из игры"
                                               SelectionChanged="HdrAutoToggleCombo_SelectionChanged">
                                         <ComboBoxItem Content="Off"/>
                                         <ComboBoxItem Content="On"/>
@@ -1402,7 +1402,7 @@
                                             BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1"
                                             CornerRadius="6" Padding="8,4" MinWidth="0" MinHeight="0"
                                             FontSize="12" VerticalAlignment="Center"
-                                            ToolTipService.ToolTip="Select which monitors to enable HDR on. Empty = primary display only."/>
+                                            ToolTipService.ToolTip="Выберите мониторы, на которых включать HDR. Пусто — только основной дисплей."/>
                                 </Grid>
 
                                 <!-- Row 3 Left: Effect list style -->
@@ -1411,21 +1411,21 @@
                                         <ColumnDefinition Width="Auto"/>
                                         <ColumnDefinition Width="*"/>
                                     </Grid.ColumnDefinitions>
-                                    <TextBlock Grid.Column="0" Text="Effect list style" FontSize="12"
+                                    <TextBlock Grid.Column="0" Text="Стиль списка эффектов" FontSize="12"
                                                Foreground="{StaticResource TextSecondaryBrush}"
                                                VerticalAlignment="Center"/>
                                     <ComboBox Grid.Column="1" x:Name="RsVariableListUseTabsCombo" x:FieldModifier="internal"
                                               FontSize="12" HorizontalAlignment="Stretch"
-                                              ToolTipService.ToolTip="Controls how shaders are displayed in the ReShade overlay. Tabs groups effect files into named tabs; Tree shows a collapsible hierarchy."
+                                              ToolTipService.ToolTip="Управляет отображением шейдеров в оверлее ReShade. «Вкладки» группируют файлы эффектов по именным вкладкам; «Дерево» показывает сворачиваемую иерархию."
                                               SelectionChanged="RsVariableListUseTabsCombo_SelectionChanged">
-                                        <ComboBoxItem Content="Tabs"/>
-                                        <ComboBoxItem Content="Tree"/>
+                                        <ComboBoxItem Content="Вкладки"/>
+                                        <ComboBoxItem Content="Дерево"/>
                                     </ComboBox>
                                 </Grid>
 
                                 <!-- Row 4: Apply buttons -->
                                 <Button Grid.Row="4" Grid.Column="0" x:Name="ApplyHotkeyBtn" x:FieldModifier="internal"
-                                        Content="Apply to All Games"
+                                        Content="Применить ко всем играм"
                                         Click="ApplyScreenshotPath_Click"
                                         Background="{StaticResource AccentBlueBgBrush}"
                                         Foreground="{StaticResource AccentBlueBrush}"
@@ -1444,7 +1444,7 @@
                                 Background="{StaticResource SurfaceRaisedBrush}" CornerRadius="10" Padding="20,16"
                                 BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                             <StackPanel Spacing="12">
-                                <TextBlock Text="Resolution &amp; Colour Control" FontSize="15" FontWeight="Bold"
+                                <TextBlock Text="Управление разрешением и цветом" FontSize="15" FontWeight="Bold"
                                            Foreground="{StaticResource TextPrimaryBrush}"/>
                                 <Grid ColumnSpacing="0" RowSpacing="0">
                                     <Grid.ColumnDefinitions>
@@ -1457,31 +1457,31 @@
                                     <StackPanel Grid.Column="0" Spacing="12">
                                         <!-- Target Resolution -->
                                         <StackPanel Spacing="4">
-                                            <TextBlock Text="Target Resolution" FontSize="14" FontWeight="SemiBold"
+                                            <TextBlock Text="Целевое разрешение" FontSize="14" FontWeight="SemiBold"
                                                        Foreground="{StaticResource TextPrimaryBrush}"/>
                                             <TextBlock TextWrapping="Wrap" FontSize="11"
                                                        Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                                       Text="Desktop resolution to switch to before a game launches. Restored on game exit."/>
+                                                       Text="Разрешение рабочего стола, переключаемое перед запуском игры. Восстанавливается при выходе из игры."/>
                                             <ComboBox x:Name="ResolutionTargetCombo" x:FieldModifier="internal"
                                                       FontSize="12" HorizontalAlignment="Stretch"
-                                                      ToolTipService.ToolTip="Select the target desktop resolution."
+                                                      ToolTipService.ToolTip="Выберите целевое разрешение рабочего стола."
                                                       SelectionChanged="ResolutionTargetCombo_SelectionChanged"/>
                                         </StackPanel>
 
                                         <!-- Resolution Auto-Toggle -->
                                         <StackPanel Spacing="4">
-                                            <TextBlock Text="Resolution Auto-Toggle" FontSize="14" FontWeight="SemiBold"
+                                            <TextBlock Text="Автопереключение разрешения" FontSize="14" FontWeight="SemiBold"
                                                        Foreground="{StaticResource TextPrimaryBrush}"/>
                                             <TextBlock TextWrapping="Wrap" FontSize="11"
                                                        Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                                       Text="When On, switches to the target resolution on game launch and restores it on exit."/>
+                                                       Text="Вкл — при запуске игры переключается на целевое разрешение и возвращает его при выходе."/>
                                             <Grid ColumnSpacing="6">
                                                 <Grid.ColumnDefinitions>
                                                     <ColumnDefinition Width="Auto"/>
                                                     <ColumnDefinition Width="*"/>
                                                     <ColumnDefinition Width="Auto"/>
                                                 </Grid.ColumnDefinitions>
-                                                <TextBlock Grid.Column="0" Text="Resolution Auto-Toggle" FontSize="12"
+                                                <TextBlock Grid.Column="0" Text="Автопереключение разрешения" FontSize="12"
                                                            Foreground="{StaticResource TextSecondaryBrush}"
                                                            VerticalAlignment="Center"/>
                                                 <ComboBox Grid.Column="1" x:Name="ResAutoToggleCombo" x:FieldModifier="internal"
@@ -1496,7 +1496,7 @@
                                                         BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1"
                                                         CornerRadius="6" Padding="8,4" MinWidth="0" MinHeight="0"
                                                         FontSize="12" VerticalAlignment="Center"
-                                                        ToolTipService.ToolTip="Select which monitors to change resolution on."/>
+                                                        ToolTipService.ToolTip="Выберите мониторы, у которых менять разрешение."/>
                                             </Grid>
                                         </StackPanel>
                                     </StackPanel>
@@ -1509,17 +1509,17 @@
                                     <!-- Right: Output Colour Settings -->
                                     <StackPanel Grid.Column="2" Spacing="12">
                                         <StackPanel Spacing="4">
-                                            <TextBlock Text="Output Colour Settings" FontSize="14" FontWeight="SemiBold"
+                                            <TextBlock Text="Настройки вывода цвета" FontSize="14" FontWeight="SemiBold"
                                                        Foreground="{StaticResource TextPrimaryBrush}"/>
                                             <TextBlock TextWrapping="Wrap" FontSize="11"
                                                        Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                                       Text="Set display colour depth and dynamic range without opening NVIDIA Control Panel."/>
+                                                       Text="Меняйте глубину цвета и динамический диапазон дисплея без Панели управления NVIDIA."/>
                                         </StackPanel>
 
                                         <!-- Display selector -->
                                         <ComboBox x:Name="ColorDisplayCombo" x:FieldModifier="internal"
                                                   FontSize="12" HorizontalAlignment="Stretch"
-                                                  ToolTipService.ToolTip="Select which display to configure."
+                                                  ToolTipService.ToolTip="Выберите настраиваемый дисплей."
                                                   SelectionChanged="ColorDisplayCombo_SelectionChanged"/>
 
                                         <!-- Colour Depth + Dynamic Range + Apply -->
@@ -1530,7 +1530,7 @@
                                                 <ColumnDefinition Width="Auto"/>
                                             </Grid.ColumnDefinitions>
                                             <StackPanel Grid.Column="0" Spacing="4">
-                                                <TextBlock Text="Colour Depth" FontSize="11"
+                                                <TextBlock Text="Глубина цвета" FontSize="11"
                                                            Foreground="{StaticResource TextSecondaryBrush}"/>
                                                 <ComboBox x:Name="ColorDepthCombo" x:FieldModifier="internal"
                                                           FontSize="12" HorizontalAlignment="Stretch">
@@ -1540,7 +1540,7 @@
                                                 </ComboBox>
                                             </StackPanel>
                                             <StackPanel Grid.Column="1" Spacing="4">
-                                                <TextBlock Text="Dynamic Range" FontSize="11"
+                                                <TextBlock Text="Динамический диапазон" FontSize="11"
                                                            Foreground="{StaticResource TextSecondaryBrush}"/>
                                                 <ComboBox x:Name="ColorRangeCombo" x:FieldModifier="internal"
                                                           FontSize="12" HorizontalAlignment="Stretch">
@@ -1548,7 +1548,7 @@
                                                     <ComboBoxItem Content="Limited"/>
                                                 </ComboBox>
                                             </StackPanel>
-                                            <Button Grid.Column="2" Content="Apply"
+                                            <Button Grid.Column="2" Content="Применить"
                                                     x:Name="ColorApplyBtn" x:FieldModifier="internal"
                                                     Click="ColorApplyBtn_Click"
                                                     Background="{StaticResource AccentBlueBgBrush}"
@@ -1588,11 +1588,11 @@
 
                                     <!-- Left description -->
                                     <StackPanel Grid.Row="0" Grid.Column="0" Spacing="4">
-                                        <TextBlock Text="API Key" FontSize="14" FontWeight="SemiBold"
+                                        <TextBlock Text="API-ключ" FontSize="14" FontWeight="SemiBold"
                                                    Foreground="{StaticResource TextPrimaryBrush}"/>
                                         <TextBlock TextWrapping="Wrap" FontSize="11"
                                                    Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                                   Text="Paste your Nexus Mods personal API key. Premium users get one-click downloads and silent auto-updates. Free users get one-click browser downloads."/>
+                                                   Text="Вставьте личный API-ключ Nexus Mods. Премиум-пользователям — скачивание в один клик и тихие автообновления. Остальным — скачивание в один клик через браузер."/>
                                     </StackPanel>
 
                                     <!-- Divider -->
@@ -1602,11 +1602,11 @@
 
                                     <!-- Right: connection status + NXM toggle -->
                                     <StackPanel Grid.Row="0" Grid.Column="2" Spacing="4">
-                                        <TextBlock Text="NXM Protocol Handler" FontSize="14" FontWeight="SemiBold"
+                                        <TextBlock Text="Обработчик протокола NXM" FontSize="14" FontWeight="SemiBold"
                                                    Foreground="{StaticResource TextPrimaryBrush}"/>
                                         <TextBlock TextWrapping="Wrap" FontSize="11"
                                                    Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                                   Text="Register RHI as the nxm:// handler so Nexus &quot;Mod Manager Download&quot; buttons open directly in RHI."/>
+                                                   Text="Зарегистрировать RHI как обработчик nxm://, чтобы кнопки «Mod Manager Download» на Nexus открывались прямо в RHI."/>
                                     </StackPanel>
 
                                     <!-- Row 1 Left: API key input + Connect/Disconnect buttons -->
@@ -1617,7 +1617,7 @@
                                             </Grid.ColumnDefinitions>
                                             <Button x:Name="NexusConnectBtn" x:FieldModifier="internal"
                                                     Grid.Column="0"
-                                                    Content="Connect"
+                                                    Content="Подключить"
                                                     Click="NexusConnectBtn_Click"
                                                     Background="{StaticResource AccentGreenBrush}"
                                                     Foreground="White"
@@ -1629,7 +1629,7 @@
                                         <TextBlock x:Name="NexusStatusText" x:FieldModifier="internal"
                                                    FontSize="11" TextWrapping="Wrap"
                                                    Foreground="{StaticResource TextTertiaryBrush}"
-                                                   Text="Not connected"/>
+                                                   Text="Не подключено"/>
                                     </StackPanel>
 
                                     <!-- Row 1 Right: NXM handler register/unregister -->
@@ -1643,10 +1643,10 @@
                                                        Grid.Column="0"
                                                        FontSize="12" TextWrapping="Wrap" VerticalAlignment="Center"
                                                        Foreground="{StaticResource TextSecondaryBrush}"
-                                                       Text="Not registered"/>
+                                                       Text="Не зарегистрировано"/>
                                             <Button x:Name="NxmRegisterBtn" x:FieldModifier="internal"
                                                     Grid.Column="1"
-                                                    Content="Register"
+                                                    Content="Зарегистрировать"
                                                     Click="NxmRegisterBtn_Click"
                                                     Background="{StaticResource SurfaceOverlayBrush}"
                                                     Foreground="{StaticResource ChipTextBrush}"
@@ -1658,7 +1658,7 @@
 
                                     <!-- Row 2: Disconnect row (shown only when connected) -->
                                     <StackPanel Grid.Row="2" Grid.Column="0" Spacing="4" x:Name="NexusDisconnectRow" x:FieldModifier="internal" Visibility="Collapsed">
-                                        <Button Content="Disconnect"
+                                        <Button Content="Отключить"
                                                 Click="NexusDisconnectBtn_Click"
                                                 Background="Transparent"
                                                 Foreground="{StaticResource AccentRedBrush}"
@@ -1678,24 +1678,24 @@
                                 Background="{StaticResource SurfaceRaisedBrush}" CornerRadius="10" Padding="20,16"
                                 BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                             <StackPanel Spacing="12">
-                                <TextBlock Text="RenoDX Data Source" FontSize="15" FontWeight="Bold"
+                                <TextBlock Text="Источник данных RenoDX" FontSize="15" FontWeight="Bold"
                                            Foreground="{StaticResource TextPrimaryBrush}"/>
                                 <TextBlock TextWrapping="Wrap" FontSize="11"
                                            Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                           Text="Controls where RHI fetches mod data. RHI Database uses the community-maintained RHI database (recommended). RenoDX Wiki falls back to the RenoDX wiki scraper. Changing this setting triggers an immediate refresh."/>
+                                           Text="Определяет источник данных о модах. База RHI использует поддерживаемое сообществом хранилище RHI (рекомендуется). Вики RenoDX обращается к скраперу вики RenoDX. При изменении настройки список сразу обновится."/>
                                 <Grid ColumnSpacing="12">
                                     <Grid.ColumnDefinitions>
                                         <ColumnDefinition Width="Auto"/>
                                         <ColumnDefinition Width="*"/>
                                     </Grid.ColumnDefinitions>
-                                    <TextBlock Grid.Column="0" Text="Source" FontSize="13"
+                                    <TextBlock Grid.Column="0" Text="Исходный код" FontSize="13"
                                                Foreground="{StaticResource TextPrimaryBrush}"
                                                VerticalAlignment="Center"/>
                                     <ComboBox x:Name="RenoDxDbSourceCombo" x:FieldModifier="internal"
                                               Grid.Column="1" FontSize="12"
                                               HorizontalAlignment="Left" MinWidth="160">
-                                        <ComboBoxItem Content="RHI Database" Tag="DbOnly"/>
-                                        <ComboBoxItem Content="RenoDX Wiki" Tag="WikiOnly"/>
+                                        <ComboBoxItem Content="База RHI" Tag="DbOnly"/>
+                                        <ComboBoxItem Content="Вики RenoDX" Tag="WikiOnly"/>
                                     </ComboBox>
                                 </Grid>
                             </StackPanel>
@@ -1707,7 +1707,7 @@
                         <Border Background="{StaticResource SurfaceRaisedBrush}" CornerRadius="10" Padding="20,16"
                                 BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                             <StackPanel Spacing="12">
-                            <TextBlock Text="DLSS / Streamline Settings" FontSize="15" FontWeight="Bold"
+                            <TextBlock Text="Настройки DLSS / Streamline" FontSize="15" FontWeight="Bold"
                                        Foreground="{StaticResource TextPrimaryBrush}"/>
                             <Grid ColumnSpacing="0" RowSpacing="12">
                                 <Grid.ColumnDefinitions>
@@ -1724,12 +1724,12 @@
                                         <RowDefinition Height="*"/>
                                     </Grid.RowDefinitions>
                                     <StackPanel Grid.Row="0" Spacing="8">
-                                        <TextBlock Text="Defaults" FontSize="14" FontWeight="SemiBold"
+                                        <TextBlock Text="По умолчанию" FontSize="14" FontWeight="SemiBold"
                                                    Foreground="{StaticResource TextPrimaryBrush}"/>
                                         <TextBlock TextWrapping="Wrap" FontSize="11"
                                                    Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                                   Text="Configure preferred DLSS/Streamline versions, presets, and render scales. Use 'Quick Apply' in the Nvidia Profile section to stamp these onto any game."/>
-                                        <Button Content="Configure Defaults" Click="DlssDefaults_Click"
+                                                   Text="Настройте предпочитаемые версии, пресеты и масштабы рендеринга DLSS/Streamline. Кнопка «Быстрое применение» в разделе профиля NVIDIA применит их к любой игре."/>
+                                        <Button Content="Настроить значения по умолчанию" Click="DlssDefaults_Click"
                                                 Background="{StaticResource AccentBlueBgBrush}"
                                                 Foreground="{StaticResource AccentBlueBrush}"
                                                 BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
@@ -1740,40 +1740,40 @@
 
                                     <!-- DMFG Defaults (global base profile) -->
                                     <StackPanel Grid.Row="1" Spacing="8" Margin="0,16,0,0">
-                                        <TextBlock Text="DMFG Defaults" FontSize="13" FontWeight="SemiBold"
+                                        <TextBlock Text="DMFG по умолчанию" FontSize="13" FontWeight="SemiBold"
                                                    Foreground="{StaticResource TextPrimaryBrush}"/>
                                         <TextBlock TextWrapping="Wrap" FontSize="11"
                                                    Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                                   Text="Global Dynamic Multi Frame Generation defaults. Set these once — then just enable Dynamic mode on each game's MFG dialog."/>
+                                                   Text="Общие настройки динамической мульти-генерации кадров. Задайте один раз — затем просто включайте динамический режим в диалоге MFG каждой игры."/>
                                         <Grid ColumnSpacing="12">
                                             <Grid.ColumnDefinitions>
                                                 <ColumnDefinition Width="*"/>
                                                 <ColumnDefinition Width="*"/>
                                             </Grid.ColumnDefinitions>
                                             <StackPanel Grid.Column="0" Spacing="4">
-                                                <TextBlock Text="Frame Count" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
+                                                <TextBlock Text="Количество кадров" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
                                                 <ComboBox x:Name="DmfgFrameCountCombo" x:FieldModifier="internal"
                                                           FontSize="12" HorizontalAlignment="Stretch"
-                                                          ToolTipService.ToolTip="Global DMFG max frame count. Games using Dynamic mode inherit this unless overridden per-game."
+                                                          ToolTipService.ToolTip="Общее максимальное количество кадров DMFG. Игры в динамическом режиме наследуют его, если не задано своё."
                                                           SelectionChanged="DmfgFrameCountCombo_SelectionChanged"/>
                                             </StackPanel>
                                             <StackPanel Grid.Column="1" Spacing="4">
-                                                <TextBlock Text="Target FPS" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
+                                                <TextBlock Text="Целевой FPS" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
                                                 <ComboBox x:Name="DmfgTargetFpsCombo" x:FieldModifier="internal"
                                                           FontSize="12" HorizontalAlignment="Stretch"
-                                                          ToolTipService.ToolTip="Global DMFG target frame rate. Off = no target. Max Refresh Rate = match display. Or pick a specific FPS."
+                                                          ToolTipService.ToolTip="Общая целевая частота кадров DMFG. Выкл — без цели. Max Refresh Rate — по дисплею. Либо конкретный FPS."
                                                           SelectionChanged="DmfgTargetFpsCombo_SelectionChanged"/>
                                             </StackPanel>
                                         </Grid>
                                     </StackPanel>
 
                                     <StackPanel Grid.Row="2" Spacing="8" Margin="0,16,0,0">
-                                        <TextBlock Text="Batch Deploy" FontSize="13" FontWeight="SemiBold"
+                                        <TextBlock Text="Массовое развертывание" FontSize="13" FontWeight="SemiBold"
                                                    Foreground="{StaticResource TextPrimaryBrush}"/>
                                         <TextBlock TextWrapping="Wrap" FontSize="11"
                                                    Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                                   Text="Deploy DLSS and Streamline DLL versions and DLSS presets to multiple games at once. Backs up originals automatically. Pre-populated with your configured defaults."/>
-                                        <Button Content="Batch Deploy" Click="MassDlssDeploy_Click"
+                                                   Text="Развернуть версии DLL DLSS и Streamline, а также пресеты DLSS сразу в нескольких играх. Оригиналы автоматически резервируются. Поля заранее заполнены вашими значениями по умолчанию."/>
+                                        <Button Content="Массовое развертывание" Click="MassDlssDeploy_Click"
                                                 Background="{StaticResource AccentBlueBgBrush}"
                                                 Foreground="{StaticResource AccentBlueBrush}"
                                                 BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
@@ -1788,32 +1788,32 @@
 
                                 <!-- Right: DLSS & G-Sync Indicators + Auto-Update -->
                                 <StackPanel Grid.Column="2" Spacing="8">
-                                    <TextBlock Text="DLSS On-Screen Indicator" FontSize="13" FontWeight="SemiBold"
+                                    <TextBlock Text="Экранный индикатор DLSS" FontSize="13" FontWeight="SemiBold"
                                                Foreground="{StaticResource TextPrimaryBrush}"/>
                                     <TextBlock TextWrapping="Wrap" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                               Text="Controls the DLSS text overlay NVIDIA shows in the corner of games. Global system setting — affects all games. Requires admin."/>
+                                               Text="Управляет текстовым индикатором DLSS, который драйвер NVIDIA показывает в углу игр. Общесистемная настройка — влияет на все игры. Требуются права администратора."/>
                                     <ComboBox x:Name="DlssIndicatorCombo" x:FieldModifier="internal"
                                               FontSize="12" HorizontalAlignment="Stretch"
                                               SelectionChanged="DlssIndicatorCombo_SelectionChanged">
-                                        <ComboBoxItem Content="Enabled"/>
-                                        <ComboBoxItem Content="Disabled"/>
+                                        <ComboBoxItem Content="Включено"/>
+                                        <ComboBoxItem Content="Отключено"/>
                                     </ComboBox>
 
-                                    <TextBlock Text="G-Sync On-Screen Indicator" FontSize="13" FontWeight="SemiBold"
+                                    <TextBlock Text="Экранный индикатор G-Sync" FontSize="13" FontWeight="SemiBold"
                                                Foreground="{StaticResource TextPrimaryBrush}" Margin="0,8,0,0"/>
                                     <ComboBox x:Name="GSyncIndicatorCombo" x:FieldModifier="internal"
                                               FontSize="12" HorizontalAlignment="Stretch"
                                               SelectionChanged="GSyncIndicatorCombo_SelectionChanged">
-                                        <ComboBoxItem Content="Enabled"/>
-                                        <ComboBoxItem Content="Disabled"/>
+                                        <ComboBoxItem Content="Включено"/>
+                                        <ComboBoxItem Content="Отключено"/>
                                     </ComboBox>
 
-                                    <TextBlock Text="Auto-Update" FontSize="13" FontWeight="SemiBold"
+                                    <TextBlock Text="Автообновление" FontSize="13" FontWeight="SemiBold"
                                                Foreground="{StaticResource TextPrimaryBrush}" Margin="0,8,0,0"/>
                                     <TextBlock TextWrapping="Wrap" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                               Text="Automatically swap to the newest version when a new release appears. Only affects games currently on the previous latest — manually chosen older versions are left alone."/>
+                                               Text="Автоматически переключаться на новейшую версию при выходе нового релиза. Касается только игр с последней версией — вручную выбранные старые версии не трогаются."/>
                                     <Grid ColumnSpacing="12">
                                         <Grid.ColumnDefinitions>
                                             <ColumnDefinition Width="*"/>
@@ -1849,7 +1849,7 @@
                         <Border Background="{StaticResource SurfaceRaisedBrush}" CornerRadius="10" Padding="20,16"
                                 BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                             <StackPanel Spacing="12">
-                            <TextBlock Text="Global NVIDIA Driver Settings" FontSize="15" FontWeight="Bold"
+                            <TextBlock Text="Глобальные настройки драйвера NVIDIA" FontSize="15" FontWeight="Bold"
                                        Foreground="{StaticResource TextPrimaryBrush}"/>
                             <Grid ColumnSpacing="0">
                                 <Grid.ColumnDefinitions>
@@ -1862,7 +1862,7 @@
                                 <StackPanel Grid.Column="0" Spacing="12" VerticalAlignment="Top">
                                     <TextBlock TextWrapping="Wrap" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                               Text="Driver-level settings applied globally via the base NVIDIA profile. Per-game settings are in the Nvidia Profile section of each game's overrides panel."/>
+                                               Text="Драйверные настройки, применяемые глобально через базовый профиль NVIDIA. Настройки для отдельных игр находятся в разделе «Профиль NVIDIA» на панели переопределений каждой игры."/>
 
                                     <Grid ColumnSpacing="12">
                                         <Grid.ColumnDefinitions>
@@ -1870,17 +1870,17 @@
                                             <ColumnDefinition Width="*"/>
                                         </Grid.ColumnDefinitions>
                                         <StackPanel Grid.Column="0" Spacing="4">
-                                            <TextBlock Text="Shader Cache Size" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
+                                            <TextBlock Text="Размер кеша шейдеров" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
                                             <ComboBox x:Name="ShaderCacheSizeCombo" x:FieldModifier="internal"
                                                       FontSize="12" HorizontalAlignment="Stretch"
-                                                      ToolTipService.ToolTip="Maximum disk space for compiled shader cache. Larger = fewer stutters on revisits. 16GB is the driver default."
+                                                      ToolTipService.ToolTip="Максимум места на диске для кеша скомпилированных шейдеров. Больше — меньше фризов при повторных посещениях. 16 ГБ — значение драйвера по умолчанию."
                                                       SelectionChanged="ShaderCacheSizeCombo_SelectionChanged"/>
                                         </StackPanel>
                                         <StackPanel Grid.Column="1" Spacing="4">
-                                            <TextBlock Text="Shader Pre-Compile" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
+                                            <TextBlock Text="Предварительная компиляция шейдеров" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
                                             <ComboBox x:Name="ShaderPrecompileCombo" x:FieldModifier="internal"
                                                       FontSize="12" HorizontalAlignment="Stretch"
-                                                      ToolTipService.ToolTip="Auto-compiles shaders in the background when idle. Higher = more CPU usage during idle, faster first-time game loads."
+                                                      ToolTipService.ToolTip="Компилирует шейдеры в фоне во время простоя. Выше значение — больше нагрузки на CPU в простое, но быстрее первая загрузка игр."
                                                       SelectionChanged="ShaderPrecompileCombo_SelectionChanged"/>
                                         </StackPanel>
                                     </Grid>
@@ -1894,14 +1894,14 @@
                                             <TextBlock Text="G-Sync" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
                                             <ComboBox x:Name="GSyncEnableCombo" x:FieldModifier="internal"
                                                       FontSize="12" HorizontalAlignment="Stretch"
-                                                      ToolTipService.ToolTip="Globally enables or disables G-Sync/FreeSync variable refresh rate."
+                                                      ToolTipService.ToolTip="Глобально включает или отключает адаптивную синхронизацию G-Sync/FreeSync."
                                                       SelectionChanged="GSyncEnableCombo_SelectionChanged"/>
                                         </StackPanel>
                                         <StackPanel Grid.Column="1" Spacing="4">
-                                            <TextBlock Text="G-Sync Mode" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
+                                            <TextBlock Text="Режим G-Sync" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
                                             <ComboBox x:Name="GSyncModeCombo" x:FieldModifier="internal"
                                                       FontSize="12" HorizontalAlignment="Stretch"
-                                                      ToolTipService.ToolTip="Controls G-Sync/FreeSync variable refresh rate. Fullscreen and Windowed recommended for seamless experience."
+                                                      ToolTipService.ToolTip="Управляет адаптивной синхронизацией G-Sync/FreeSync. Рекомендуются режимы «Полноэкранный» и «Оконный» для бесшовной работы."
                                                       SelectionChanged="GSyncModeCombo_SelectionChanged"/>
                                         </StackPanel>
                                     </Grid>
@@ -1912,17 +1912,17 @@
                                             <ColumnDefinition Width="*"/>
                                         </Grid.ColumnDefinitions>
                                         <StackPanel Grid.Column="0" Spacing="4">
-                                            <TextBlock Text="FPS Limit" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
+                                            <TextBlock Text="Лимит FPS" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
                                             <ComboBox x:Name="FpsLimitCombo" x:FieldModifier="internal"
                                                       FontSize="12" HorizontalAlignment="Stretch"
-                                                      ToolTipService.ToolTip="Global FPS cap via NVIDIA driver. VRR-optimal presets cap just below refresh rate to stay in VRR range."
+                                                      ToolTipService.ToolTip="Общий лимит FPS через драйвер NVIDIA. Оптимальные для VRR пресеты ставят лимит чуть ниже частоты обновления, чтобы оставаться в диапазоне VRR."
                                                       SelectionChanged="FpsLimitCombo_SelectionChanged"/>
                                         </StackPanel>
                                         <StackPanel Grid.Column="1" Spacing="4">
-                                            <TextBlock Text="Preferred Refresh Rate" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
+                                            <TextBlock Text="Предпочитаемая частота обновления" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
                                             <ComboBox x:Name="PreferredRefreshRateCombo" x:FieldModifier="internal"
                                                       FontSize="12" HorizontalAlignment="Stretch"
-                                                      ToolTipService.ToolTip="Highest available forces your max refresh rate. App Setting lets games choose their own refresh rate."
+                                                      ToolTipService.ToolTip="«Наивысшая доступная» принудительно ставит максимальную частоту обновления. «Настройка игры» позволяет играм выбирать свою."
                                                       SelectionChanged="PreferredRefreshRateCombo_SelectionChanged"/>
                                         </StackPanel>
                                     </Grid>
@@ -1936,14 +1936,14 @@
                                             <TextBlock Text="VSync" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
                                             <ComboBox x:Name="GlobalVSyncCombo" x:FieldModifier="internal"
                                                       FontSize="12" HorizontalAlignment="Stretch"
-                                                      ToolTipService.ToolTip="Global VSync mode — applies to all games without a per-game override."
+                                                      ToolTipService.ToolTip="Общий режим VSync — действует для всех игр без индивидуального переопределения."
                                                       SelectionChanged="GlobalVSyncCombo_SelectionChanged"/>
                                         </StackPanel>
                                         <StackPanel Grid.Column="1" Spacing="4">
-                                            <TextBlock Text="Power Mode" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
+                                            <TextBlock Text="Режим питания" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
                                             <ComboBox x:Name="GlobalPowerModeCombo" x:FieldModifier="internal"
                                                       FontSize="12" HorizontalAlignment="Stretch"
-                                                      ToolTipService.ToolTip="Global power management mode — applies to all games without a per-game override."
+                                                      ToolTipService.ToolTip="Общий режим управления питанием — действует для всех игр без индивидуального переопределения."
                                                       SelectionChanged="GlobalPowerModeCombo_SelectionChanged"/>
                                         </StackPanel>
                                     </Grid>
@@ -1958,20 +1958,20 @@
                                             <StackPanel Orientation="Horizontal" Spacing="6">
                                                 <TextBlock Text="ReBAR" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
                                                 <TextBlock x:Name="ReBarAdminWarning" x:FieldModifier="internal"
-                                                           Text="— Admin required" FontSize="11"
+                                                           Text="— Требуется администратор" FontSize="11"
                                                            Foreground="{StaticResource AccentAmberBrush}"
                                                            Visibility="Collapsed"/>
                                             </StackPanel>
                                             <ComboBox x:Name="GlobalReBarEnableCombo" x:FieldModifier="internal"
                                                       FontSize="12" HorizontalAlignment="Stretch"
-                                                      ToolTipService.ToolTip="Global Resizable BAR — enables large GPU memory transfers for all games. Requires admin and compatible hardware (RTX 30 Series+)."
+                                                      ToolTipService.ToolTip="Глобальный Resizable BAR — разрешает крупные передачи памяти GPU для всех игр. Требуются права администратора и совместимое железо (RTX 30-й серии и новее)."
                                                       SelectionChanged="GlobalReBarEnableCombo_SelectionChanged"/>
                                         </StackPanel>
                                         <StackPanel Grid.Column="1" Spacing="4">
-                                            <TextBlock Text="ReBAR Size Limit" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
+                                            <TextBlock Text="Лимит размера ReBAR" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
                                             <ComboBox x:Name="GlobalReBarSizeCombo" x:FieldModifier="internal"
                                                       FontSize="12" HorizontalAlignment="Stretch"
-                                                      ToolTipService.ToolTip="Maximum BAR transfer size. 1GB is optimal for most games. Decrease to 512MB if experiencing ReBAR-related stutters."
+                                                      ToolTipService.ToolTip="Максимальный размер передачи BAR. 1 ГБ оптимален для большинства игр. Уменьшите до 512 МБ, если возникают фризы, связанные с ReBAR."
                                                       SelectionChanged="GlobalReBarSizeCombo_SelectionChanged"/>
                                         </StackPanel>
                                     </Grid>
@@ -1986,10 +1986,10 @@
                                 <StackPanel Grid.Column="2" Spacing="12" VerticalAlignment="Top">
                                     <TextBlock TextWrapping="Wrap" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                               Text="Adjust NVIDIA Digital Vibrance (color saturation) per-display. Saved values are automatically restored on app startup."/>
+                                               Text="Настройка цифровой насыщенности NVIDIA (Digital Vibrance) для каждого дисплея. Сохранённые значения автоматически восстанавливаются при запуске приложения."/>
                                     <Button Content="Digital Vibrance"
                                             Click="DigitalVibrance_Click"
-                                            ToolTipService.ToolTip="Adjust Digital Vibrance (color saturation) per-display. Saved values are restored on app startup."
+                                            ToolTipService.ToolTip="Настройка Digital Vibrance (насыщенности цвета) для каждого дисплея. Сохранённые значения восстанавливаются при запуске приложения."
                                             Background="{StaticResource AccentBlueBgBrush}"
                                             Foreground="{StaticResource AccentBlueBrush}"
                                             BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
@@ -1999,17 +1999,17 @@
                                     <!-- Create Missing Profiles -->
                                     <TextBlock TextWrapping="Wrap" FontSize="11" Margin="0,8,0,0"
                                                Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                               Text="Games without an NVIDIA profile don't benefit from global driver settings. Create profiles for all games in your library to ensure global settings apply."/>
-                                    <Button Content="Create Missing Profiles"
+                                               Text="Игры без профиля NVIDIA не получают общие драйверные настройки. Создайте профили для всех игр в библиотеке, чтобы общие настройки применялись везде."/>
+                                    <Button Content="Создать отсутствующие профили"
                                             Click="CreateMissingNvidiaProfiles_Click"
-                                            ToolTipService.ToolTip="Creates NVIDIA driver profiles for all games that don't have one yet. Ensures global settings like Power Mode and VSync apply to every game."
+                                            ToolTipService.ToolTip="Создаёт профили драйвера NVIDIA для всех игр, у которых их ещё нет. Гарантирует, что общие настройки вроде режима питания и VSync действуют для каждой игры."
                                             Background="{StaticResource AccentBlueBgBrush}"
                                             Foreground="{StaticResource AccentBlueBrush}"
                                             BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Padding="12,7" FontSize="12"
                                             HorizontalAlignment="Stretch"/>
 
-                                    <TextBlock Text="Backup saves all per-game NVIDIA driver settings to a file. Restore loads them back — useful before updating drivers."
+                                    <TextBlock Text="Резервная копия сохраняет все индивидуальные настройки драйвера NVIDIA в файл. Восстановление загружает их обратно — полезно перед обновлением драйверов."
                                                TextWrapping="Wrap" FontSize="11"
                                                Foreground="{StaticResource TextSecondaryBrush}"
                                                Margin="0,8,0,0"/>
@@ -2018,17 +2018,17 @@
                                             <ColumnDefinition Width="*"/>
                                             <ColumnDefinition Width="*"/>
                                         </Grid.ColumnDefinitions>
-                                        <Button Grid.Column="0" Content="Backup Profiles"
+                                        <Button Grid.Column="0" Content="Резервные копии профилей"
                                                 Click="ExportNvidiaProfiles_Click"
-                                                ToolTipService.ToolTip="Save all per-game NVIDIA profile settings to a backup file."
+                                                ToolTipService.ToolTip="Сохранить все настройки профилей NVIDIA в резервный файл."
                                                 Background="{StaticResource AccentBlueBgBrush}"
                                                 Foreground="{StaticResource AccentBlueBrush}"
                                                 BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
                                                 CornerRadius="8" Padding="12,7" FontSize="12"
                                                 HorizontalAlignment="Stretch"/>
-                                        <Button Grid.Column="1" Content="Restore Profiles"
+                                        <Button Grid.Column="1" Content="Восстановить профили"
                                                 Click="ImportNvidiaProfiles_Click"
-                                                ToolTipService.ToolTip="Restore per-game NVIDIA profile settings from a previous export."
+                                                ToolTipService.ToolTip="Восстановить настройки профилей NVIDIA из ранее созданной резервной копии."
                                                 Background="{StaticResource AccentBlueBgBrush}"
                                                 Foreground="{StaticResource AccentBlueBrush}"
                                                 BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
@@ -2041,17 +2041,17 @@
                                             <ColumnDefinition Width="*"/>
                                             <ColumnDefinition Width="*"/>
                                         </Grid.ColumnDefinitions>
-                                        <Button Grid.Column="0" Content="Reset Nvidia Profiles"
+                                        <Button Grid.Column="0" Content="Сбросить профили NVIDIA"
                                                 Click="ResetAllNvidiaProfiles_Click"
-                                                ToolTipService.ToolTip="Removes ALL per-game NVIDIA profile overrides. The global profile will be like new."
+                                                ToolTipService.ToolTip="Удаляет ВСЕ переопределения профилей NVIDIA для игр. Глобальный профиль станет как новый."
                                                 Background="{StaticResource AccentBlueBgBrush}"
                                                 Foreground="{StaticResource AccentBlueBrush}"
                                                 BorderBrush="#CC4444" BorderThickness="1"
                                                 CornerRadius="8" Padding="12,7" FontSize="12"
                                                 HorizontalAlignment="Stretch"/>
-                                        <Button Grid.Column="1" Content="Clear Shader Cache"
+                                        <Button Grid.Column="1" Content="Очистить кеш шейдеров"
                                                 Click="ClearNvidiaShaderCache_Click"
-                                                ToolTipService.ToolTip="Deletes the NVIDIA DXCache and GLCache folders. Forces shaders to recompile on next launch."
+                                                ToolTipService.ToolTip="Удаляет папки DXCache и GLCache NVIDIA. Шейдеры будут перекомпилированы при следующем запуске."
                                                 Background="{StaticResource AccentBlueBgBrush}"
                                                 Foreground="{StaticResource AccentBlueBrush}"
                                                 BorderBrush="#CC4444" BorderThickness="1"
@@ -2069,7 +2069,7 @@
                         <Border Background="{StaticResource SurfaceRaisedBrush}" CornerRadius="10" Padding="20,16"
                                 BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                             <StackPanel Spacing="12">
-                            <TextBlock Text="Component Settings" FontSize="15" FontWeight="Bold"
+                            <TextBlock Text="Настройки компонентов" FontSize="15" FontWeight="Bold"
                                        Foreground="{StaticResource TextPrimaryBrush}"/>
                             <Grid ColumnSpacing="0" RowSpacing="8">
                                 <Grid.ColumnDefinitions>
@@ -2089,7 +2089,7 @@
                                                Foreground="{StaticResource TextPrimaryBrush}"/>
                                     <TextBlock TextWrapping="Wrap" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                               Text="ReLimiter OSD hotkey and global feature settings applied to all games."/>
+                                               Text="Горячая клавиша OSD ReLimiter и общие настройки функций применяются ко всем играм."/>
                                 </StackPanel>
 
                                 <!-- Divider -->
@@ -2103,7 +2103,7 @@
                                                Foreground="{StaticResource TextPrimaryBrush}"/>
                                     <TextBlock TextWrapping="Wrap" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                               Text="GPU type, DLSS input settings, and overlay hotkey for OptiScaler installations."/>
+                                               Text="Тип GPU, настройки DLSS-входа и горячая клавиша оверлея для установок OptiScaler."/>
                                 </StackPanel>
 
                                 <!-- Row 1 Left: ReLimiter controls -->
@@ -2120,41 +2120,41 @@
                                     </Grid.RowDefinitions>
                                     <TextBlock Grid.Row="0" Grid.Column="0" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}"
-                                               Text="OSD Key" VerticalAlignment="Bottom" Margin="0,0,0,4"/>
+                                               Text="Клавиша OSD" VerticalAlignment="Bottom" Margin="0,0,0,4"/>
                                     <TextBlock Grid.Row="0" Grid.Column="1" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}"
-                                               Text="Shared Presets" VerticalAlignment="Bottom" Margin="0,0,0,4"/>
+                                               Text="Общие пресеты" VerticalAlignment="Bottom" Margin="0,0,0,4"/>
                                     <TextBlock Grid.Row="0" Grid.Column="2" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}"
-                                               Text="DLSS Hooks" VerticalAlignment="Bottom" Margin="0,0,0,4"/>
+                                               Text="Хуки DLSS" VerticalAlignment="Bottom" Margin="0,0,0,4"/>
                                     <TextBlock Grid.Row="0" Grid.Column="3" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}"
-                                               Text="Target FPS" VerticalAlignment="Bottom" Margin="0,0,0,4"/>
+                                               Text="Целевой FPS" VerticalAlignment="Bottom" Margin="0,0,0,4"/>
                                     <TextBox Grid.Row="1" Grid.Column="0" x:Name="UlHotkeyBox" x:FieldModifier="internal"
-                                             PlaceholderText="Press a key..." IsReadOnly="True"
+                                             PlaceholderText="Нажмите клавишу..." IsReadOnly="True"
                                              FontSize="12" MinWidth="80"
                                              Background="{StaticResource SurfaceInputBrush}"
                                              Foreground="{StaticResource TextSecondaryBrush}"
                                              BorderBrush="{StaticResource BorderSubtleBrush}"
-                                             ToolTipService.ToolTip="The key that toggles the ReLimiter OSD overlay in-game."
+                                             ToolTipService.ToolTip="Клавиша включения оверлея OSD ReLimiter в игре."
                                              PreviewKeyDown="UlHotkeyBox_PreviewKeyDown"/>
                                     <ComboBox Grid.Row="1" Grid.Column="1" x:Name="UlSharedPresetsCombo" x:FieldModifier="internal"
                                               FontSize="12" HorizontalAlignment="Stretch"
-                                              ToolTipService.ToolTip="When On, all games share the same OSD presets file. When Off, each game has its own presets."
+                                              ToolTipService.ToolTip="Вкл — все игры используют общий файл пресетов OSD. Выкл — у каждой игры свои пресеты."
                                               SelectionChanged="UlSharedPresetsCombo_SelectionChanged">
                                         <ComboBoxItem Content="Off"/>
                                         <ComboBoxItem Content="On"/>
                                     </ComboBox>
                                     <ComboBox Grid.Row="1" Grid.Column="2" x:Name="UlDlssHooksCombo" x:FieldModifier="internal"
                                               FontSize="12" HorizontalAlignment="Stretch"
-                                              ToolTipService.ToolTip="Shows DLSS quality mode, render resolution, and active features on the ReLimiter OSD. Disable if experiencing crashes."
+                                              ToolTipService.ToolTip="Показывает режим качества DLSS, разрешение рендеринга и активные функции в OSD ReLimiter. Отключите при вылетах."
                                               SelectionChanged="UlDlssHooksCombo_SelectionChanged">
                                         <ComboBoxItem Content="Off"/>
                                         <ComboBoxItem Content="On"/>
                                     </ComboBox>
                                     <ComboBox Grid.Row="1" Grid.Column="3" x:Name="UlTargetFpsCombo" x:FieldModifier="internal"
                                               FontSize="12" HorizontalAlignment="Stretch"
-                                              ToolTipService.ToolTip="Global FPS cap written to all relimiter.ini files. Select a VRR preset or Custom for a manual value."
+                                              ToolTipService.ToolTip="Общий лимит FPS записывается во все relimiter.ini. Выберите пресет VRR или «Свой» для ручного значения."
                                               SelectionChanged="UlTargetFpsCombo_SelectionChanged">
                                         <!-- Items populated in code-behind -->
                                     </ComboBox>
@@ -2171,7 +2171,7 @@
                                         <RowDefinition Height="Auto"/>
                                     </Grid.RowDefinitions>
                                     <StackPanel Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Spacing="4">
-                                        <TextBlock Text="GPU Type" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
+                                        <TextBlock Text="Тип GPU" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
                                         <ComboBox x:Name="OsGpuCombo" x:FieldModifier="internal"
                                                   FontSize="12" HorizontalAlignment="Stretch"
                                                   Background="{StaticResource SurfaceInputBrush}"
@@ -2185,22 +2185,22 @@
                                     </StackPanel>
                                     <StackPanel Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" Spacing="4"
                                                 x:Name="OsDlssInputsPanel" x:FieldModifier="internal" Visibility="Collapsed">
-                                        <TextBlock Text="DLSS Inputs" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
+                                        <TextBlock Text="Входы DLSS" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}"/>
                                         <ComboBox x:Name="OsDlssInputsCombo" x:FieldModifier="internal"
                                                   FontSize="12" HorizontalAlignment="Stretch"
                                                   Background="{StaticResource SurfaceInputBrush}"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   BorderBrush="{StaticResource BorderSubtleBrush}"
                                                   SelectionChanged="OsDlssInputsCombo_SelectionChanged">
-                                            <x:String>Yes</x:String>
-                                            <x:String>No</x:String>
+                                            <x:String>Да</x:String>
+                                            <x:String>Нет</x:String>
                                         </ComboBox>
                                     </StackPanel>
                                 </Grid>
 
                                 <!-- Row 2 Left: ReLimiter Apply -->
                                 <Button Grid.Row="2" Grid.Column="0" x:Name="ApplyUlHotkeyBtn" x:FieldModifier="internal"
-                                        Content="Apply to All Games"
+                                        Content="Применить ко всем играм"
                                         Click="ApplyUlOsdHotkey_Click"
                                         Background="{StaticResource AccentBlueBgBrush}"
                                         Foreground="{StaticResource AccentBlueBrush}"
@@ -2223,7 +2223,7 @@
                                         <x:String>Insert</x:String>
                                         <x:String>Home</x:String>
                                         <x:String>End</x:String>
-                                        <x:String>Delete</x:String>
+                                        <x:String>Удалить</x:String>
                                         <x:String>Page Up</x:String>
                                         <x:String>Page Down</x:String>
                                         <x:String>F1</x:String>
@@ -2240,7 +2240,7 @@
                                         <x:String>F12</x:String>
                                     </ComboBox>
                                     <Button Grid.Column="1" x:Name="ApplyOsHotkeyBtn" x:FieldModifier="internal"
-                                            Content="Apply to All Games"
+                                            Content="Применить ко всем играм"
                                             Click="ApplyOsHotkey_Click"
                                             Background="{StaticResource AccentBlueBgBrush}"
                                             Foreground="{StaticResource AccentBlueBrush}"
@@ -2258,7 +2258,7 @@
                         <Border Background="{StaticResource SurfaceRaisedBrush}" CornerRadius="10" Padding="20,16"
                                 BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                             <StackPanel Spacing="12">
-                            <TextBlock Text="Shaders &amp; Addons" FontSize="15" FontWeight="Bold"
+                            <TextBlock Text="Шейдеры и аддоны" FontSize="15" FontWeight="Bold"
                                        Foreground="{StaticResource TextPrimaryBrush}"/>
                             <Grid ColumnSpacing="0">
                                 <Grid.ColumnDefinitions>
@@ -2270,10 +2270,10 @@
                                 <!-- Left: Shader Management + Cache -->
                                 <StackPanel Grid.Column="0" Spacing="12">
                                     <StackPanel Spacing="4">
-                                        <TextBlock Text="Shader Management" FontSize="13" FontWeight="SemiBold"
+                                        <TextBlock Text="Управление шейдерами" FontSize="13" FontWeight="SemiBold"
                                                    Foreground="{StaticResource TextPrimaryBrush}"/>
                                         <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                                   Text="Control how RHI manages shader files across your games."/>
+                                                   Text="Управление тем, как RHI обращается с файлами шейдеров в ваших играх."/>
                                     </StackPanel>
                                     <Grid ColumnSpacing="16">
                                         <Grid.ColumnDefinitions>
@@ -2284,21 +2284,21 @@
                                             <RowDefinition Height="Auto"/>
                                             <RowDefinition Height="Auto"/>
                                         </Grid.RowDefinitions>
-                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Shader Management" FontSize="11"
+                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Управление шейдерами" FontSize="11"
                                                    Foreground="{StaticResource InlineDescriptionBrush}" Margin="0,0,0,3"/>
-                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="Cache All Shaders" FontSize="11"
+                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="Кешировать все шейдеры" FontSize="11"
                                                    Foreground="{StaticResource InlineDescriptionBrush}" Margin="0,0,0,3"/>
                                         <ComboBox Grid.Row="1" Grid.Column="0" x:Name="CustomShadersCombo" x:FieldModifier="internal"
                                                   FontSize="12" HorizontalAlignment="Stretch"
-                                                  ToolTipService.ToolTip="RHI Managed: deploy built-in shader packs to all games.&#10;Custom: use your own shader directory.&#10;Off: RHI never touches shaders on any game."
+                                                  ToolTipService.ToolTip="Под управлением RHI: разворачивать встроенные наборы шейдеров во все игры.&amp;#10;Свои: использовать собственный каталог шейдеров.&amp;#10;Выкл: RHI вообще не трогает шейдеры."
                                                   SelectionChanged="CustomShadersCombo_SelectionChanged">
                                             <ComboBoxItem Content="Off"/>
-                                            <ComboBoxItem Content="RHI Managed"/>
-                                            <ComboBoxItem Content="Custom"/>
+                                            <ComboBoxItem Content="Под управлением RHI"/>
+                                            <ComboBoxItem Content="Свой"/>
                                         </ComboBox>
                                         <ComboBox Grid.Row="1" Grid.Column="1" x:Name="CacheAllShadersCombo" x:FieldModifier="internal"
                                                   FontSize="12" HorizontalAlignment="Stretch"
-                                                  ToolTipService.ToolTip="When On, all shader packs are downloaded and cached at startup. When Off, packs are downloaded on demand when needed."
+                                                  ToolTipService.ToolTip="Вкл — все наборы шейдеров скачиваются и кешируются при запуске. Выкл — наборы скачиваются по мере необходимости."
                                                   SelectionChanged="CacheAllShadersCombo_SelectionChanged">
                                             <ComboBoxItem Content="Off"/>
                                             <ComboBoxItem Content="On"/>
@@ -2314,10 +2314,10 @@
                                 <!-- Right: Addon Watch Folder -->
                                 <StackPanel Grid.Column="2" Spacing="12">
                                     <StackPanel Spacing="4">
-                                        <TextBlock Text="Addon Watch Folder" FontSize="13" FontWeight="SemiBold"
+                                        <TextBlock Text="Папка наблюдения за аддонами" FontSize="13" FontWeight="SemiBold"
                                                    Foreground="{StaticResource TextPrimaryBrush}"/>
                                         <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                                   Text="RHI watches this folder for RenoDX addon files (.addon64/.addon32) and archives. Defaults to your Downloads folder."/>
+                                                   Text="RHI следит в этой папке за файлами аддонов RenoDX (.addon64/.addon32) и архивами. По умолчанию — ваша папка загрузок."/>
                                     </StackPanel>
                                     <Grid ColumnSpacing="8">
                                         <Grid.ColumnDefinitions>
@@ -2327,16 +2327,16 @@
                                         </Grid.ColumnDefinitions>
                                         <TextBox x:Name="AddonWatchFolderBox" x:FieldModifier="internal"
                                                  Grid.Column="0"
-                                                 PlaceholderText="Downloads folder (default)"
+                                                 PlaceholderText="Папка загрузок (по умолчанию)"
                                                  FontSize="12"
                                                  Background="{StaticResource SurfaceInputBrush}"
                                                  Foreground="{StaticResource TextSecondaryBrush}"
                                                  BorderBrush="{StaticResource BorderSubtleBrush}"/>
-                                        <Button Grid.Column="1" Content="Browse" Click="BrowseAddonWatchFolder_Click"
+                                        <Button Grid.Column="1" Content="Обзор" Click="BrowseAddonWatchFolder_Click"
                                                 Background="{StaticResource AccentBlueBgBrush}" Foreground="{StaticResource AccentBlueBrush}"
                                                 BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
                                                 CornerRadius="8" Padding="12,6" FontSize="12"/>
-                                        <Button Grid.Column="2" Content="Reset" Click="ResetAddonWatchFolder_Click"
+                                        <Button Grid.Column="2" Content="Сбросить" Click="ResetAddonWatchFolder_Click"
                                                 Background="{StaticResource SurfaceInputBrush}" Foreground="{StaticResource TextTertiaryBrush}"
                                                 BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1"
                                                 CornerRadius="8" Padding="12,6" FontSize="12"/>
@@ -2352,7 +2352,7 @@
                         <Border Background="{StaticResource SurfaceRaisedBrush}" CornerRadius="10" Padding="20,16"
                                 BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                             <StackPanel Spacing="12">
-                            <TextBlock Text="Update &amp; Deployment" FontSize="15" FontWeight="Bold"
+                            <TextBlock Text="Обновление и развертывание" FontSize="15" FontWeight="Bold"
                                        Foreground="{StaticResource TextPrimaryBrush}"/>
                             <Grid ColumnSpacing="0" RowSpacing="8">
                                 <Grid.ColumnDefinitions>
@@ -2368,11 +2368,11 @@
 
                                 <!-- Left: Global Update Checks -->
                                 <StackPanel Grid.Row="0" Grid.Column="0" Spacing="4">
-                                    <TextBlock Text="Global Update Checks" FontSize="14" FontWeight="SemiBold"
+                                    <TextBlock Text="Глобальная проверка обновлений" FontSize="14" FontWeight="SemiBold"
                                                Foreground="{StaticResource TextPrimaryBrush}"/>
                                     <TextBlock TextWrapping="Wrap" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                               Text="Disable update checks for individual components. When disabled, the component will not be checked during startup or Update All."/>
+                                               Text="Отключите проверку обновлений для отдельных компонентов. Отключённый компонент не проверяется при запуске и в «Обновить всё»."/>
                                 </StackPanel>
 
                                 <!-- Divider -->
@@ -2382,16 +2382,16 @@
 
                                 <!-- Right: Mass INI Deployment -->
                                 <StackPanel Grid.Row="0" Grid.Column="2" Spacing="4">
-                                    <TextBlock Text="Mass INI Deployment" FontSize="14" FontWeight="SemiBold"
+                                    <TextBlock Text="Массовое развертывание INI" FontSize="14" FontWeight="SemiBold"
                                                Foreground="{StaticResource TextPrimaryBrush}"/>
                                     <TextBlock TextWrapping="Wrap" FontSize="11"
                                                Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                               Text="Deploy INI files or ReShade presets to multiple games at once. Custom hotkey and screenshot path settings are preserved."/>
+                                               Text="Развернуть INI-файлы или пресеты ReShade сразу в нескольких играх. Пользовательские горячие клавиши и путь к скриншотам сохраняются."/>
                                 </StackPanel>
 
                                 <!-- Row 1 Left: Update Inclusion button -->
                                 <Button Grid.Row="1" Grid.Column="0" x:Name="GlobalUpdateInclusionBtn"
-                                        Content="Update Inclusion"
+                                        Content="Включение в обновление"
                                         Click="GlobalUpdateInclusion_Click"
                                         Background="{StaticResource AccentBlueBgBrush}"
                                         Foreground="{StaticResource AccentBlueBrush}"
@@ -2426,7 +2426,7 @@
                                             Background="{StaticResource AccentBlueBgBrush}" Foreground="{StaticResource AccentBlueBrush}"
                                             BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Padding="12,7" FontSize="12" HorizontalAlignment="Stretch"/>
-                                    <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Content="Mass Preset Install"
+                                    <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Content="Массовая установка пресетов"
                                             Click="MassPresetInstall_Click"
                                             Background="{StaticResource AccentBlueBgBrush}" Foreground="{StaticResource AccentBlueBrush}"
                                             BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
@@ -2446,7 +2446,7 @@
                         <Border Background="{StaticResource SurfaceRaisedBrush}" CornerRadius="10" Padding="20,16"
                                 BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                             <StackPanel Spacing="12">
-                            <TextBlock Text="System &amp; Maintenance" FontSize="15" FontWeight="Bold"
+                            <TextBlock Text="Система и обслуживание" FontSize="15" FontWeight="Bold"
                                        Foreground="{StaticResource TextPrimaryBrush}"/>
                             <Grid ColumnSpacing="0" RowSpacing="8">
                                 <Grid.ColumnDefinitions>
@@ -2464,12 +2464,12 @@
                                         <RowDefinition Height="Auto"/>
                                     </Grid.RowDefinitions>
                                     <StackPanel Grid.Row="0" Spacing="8">
-                                        <TextBlock Text="Full Refresh" FontSize="13" FontWeight="SemiBold"
+                                        <TextBlock Text="Полное обновление" FontSize="13" FontWeight="SemiBold"
                                                    Foreground="{StaticResource TextPrimaryBrush}"/>
                                         <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                                   Text="Clears all caches and re-scans everything from disk. Use as a last resort if games are missing, paths have changed, DLSS has been added to a game, or the DLSS section is missing from a game card."/>
+                                                   Text="Очищает все кеши и заново сканирует всё с диска. Крайняя мера — если игры пропали, пути изменились, к игре добавился DLSS или с карточки игры пропал раздел DLSS."/>
                                     </StackPanel>
-                                    <Button Grid.Row="1" x:Name="FullRefreshBtn" Content="Full Refresh"
+                                    <Button Grid.Row="1" x:Name="FullRefreshBtn" Content="Полное обновление"
                                             Click="FullRefreshButton_Click"
                                             Background="{StaticResource AccentBlueBgBrush}" Foreground="{StaticResource AccentBlueBrush}"
                                             BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
@@ -2477,15 +2477,15 @@
                                             HorizontalAlignment="Stretch"/>
                                     <TextBlock Grid.Row="2" TextWrapping="Wrap" FontSize="11" Margin="0,8,0,0"
                                                Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                               Text="Deletes cached DLSS, Streamline, and component staging files to free disk space. Shaders, installed addons, and version metadata are preserved. Files are re-downloaded automatically when needed."/>
+                                               Text="Удаляет кешированные файлы DLSS, Streamline и компонентов, освобождая место на диске. Шейдеры, установленные аддоны и метаданные версий сохраняются. Файлы автоматически скачаются заново, когда понадобятся."/>
                                     <Button Grid.Row="3" x:Name="PurgeCacheBtn"
-                                            Content="Purge Staging Files"
+                                            Content="Очистить кеш загрузок"
                                             Click="PurgeCachedFiles_Click"
                                             Background="{StaticResource AccentRedBgBrush}" Foreground="{StaticResource AccentRedBrush}"
                                             BorderBrush="{StaticResource AccentPurpleBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Padding="12,7" FontSize="12"
                                             HorizontalAlignment="Stretch"
-                                            ToolTipService.ToolTip="Deletes cached DLSS, Streamline, and download files to free disk space. Shaders are preserved."/>
+                                            ToolTipService.ToolTip="Удаляет кешированные DLSS, Streamline и загруженные файлы, освобождая место на диске. Шейдеры сохраняются."/>
                                 </Grid>
 
                                 <!-- Divider -->
@@ -2504,10 +2504,10 @@
                                     </Grid.RowDefinitions>
                                     <!-- Admin Mode description + combo -->
                                     <StackPanel Grid.Row="0" Spacing="4">
-                                        <TextBlock Text="Admin Mode" FontSize="13" FontWeight="SemiBold"
+                                        <TextBlock Text="Режим администратора" FontSize="13" FontWeight="SemiBold"
                                                    Foreground="{StaticResource TextPrimaryBrush}"/>
                                         <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                                   Text="Creates a scheduled task to launch RHI with elevated privileges. Required for ReBAR, Low Latency Ultra, and Smooth Motion driver settings. The Drop Helper enables Discord drag-and-drop when running elevated."/>
+                                                   Text="Создаёт назначенное задание для запуска RHI с повышенными правами. Необходимо для драйверных настроек ReBAR, Low Latency Ultra и Smooth Motion. Drop Helper включает перетаскивание из Discord при запуске с повышенными правами."/>
                                     </StackPanel>
                                     <Grid Grid.Row="1" ColumnSpacing="12">
                                         <Grid.ColumnDefinitions>
@@ -2515,7 +2515,7 @@
                                             <ColumnDefinition Width="*"/>
                                         </Grid.ColumnDefinitions>
                                         <StackPanel Grid.Column="0" Spacing="4">
-                                            <TextBlock Text="Admin Mode" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
+                                            <TextBlock Text="Режим администратора" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                             <ComboBox x:Name="AdminModeCombo" x:FieldModifier="internal"
                                                       SelectionChanged="AdminModeCombo_SelectionChanged"
                                                       FontSize="12" Padding="8,4"
@@ -2530,7 +2530,7 @@
                                                       SelectionChanged="DropHelperCombo_SelectionChanged"
                                                       FontSize="12" Padding="8,4"
                                                       CornerRadius="8" HorizontalAlignment="Stretch"
-                                                      ToolTipService.ToolTip="The drop helper window enables Discord drag-and-drop in admin mode. Disable if you don't need it. Restart required.">
+                                                      ToolTipService.ToolTip="Окно Drop Helper включает перетаскивание из Discord в режиме администратора. Отключите, если не нужно. Требуется перезапуск.">
                                                 <ComboBoxItem Content="Off"/>
                                                 <ComboBoxItem Content="On"/>
                                             </ComboBox>
@@ -2538,10 +2538,10 @@
                                     </Grid>
                                     <!-- System Tray description + combos -->
                                     <StackPanel Grid.Row="2" Spacing="4" Margin="0,12,0,0">
-                                        <TextBlock Text="System Tray" FontSize="13" FontWeight="SemiBold"
+                                        <TextBlock Text="Системный трей" FontSize="13" FontWeight="SemiBold"
                                                    Foreground="{StaticResource TextPrimaryBrush}"/>
                                         <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                                   Text="Minimize to system tray on close. Right-click the tray icon or pinned taskbar icon to quickly launch recent games."/>
+                                                   Text="Сворачивать в системный трей при закрытии. Правый клик по значку в трее или закреплённому значку панели задач — быстрый запуск недавних игр."/>
                                     </StackPanel>
                                     <Grid Grid.Row="3" ColumnSpacing="12">
                                         <Grid.ColumnDefinitions>
@@ -2550,7 +2550,7 @@
                                             <ColumnDefinition Width="*"/>
                                         </Grid.ColumnDefinitions>
                                         <StackPanel Grid.Column="0" Spacing="4">
-                                            <TextBlock Text="Close to Tray" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
+                                            <TextBlock Text="Свёртывать в трей" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                             <ComboBox x:Name="CloseToTrayCombo" x:FieldModifier="internal"
                                                       SelectionChanged="CloseToTrayCombo_SelectionChanged"
                                                       FontSize="12" Padding="8,4" CornerRadius="8" HorizontalAlignment="Stretch">
@@ -2559,7 +2559,7 @@
                                             </ComboBox>
                                         </StackPanel>
                                         <StackPanel Grid.Column="1" Spacing="4">
-                                            <TextBlock Text="Recent Games" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
+                                            <TextBlock Text="Недавние игры" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                             <ComboBox x:Name="RecentGamesCombo" x:FieldModifier="internal"
                                                       SelectionChanged="RecentGamesCombo_SelectionChanged"
                                                       FontSize="12" Padding="8,4" CornerRadius="8" HorizontalAlignment="Stretch">
@@ -2568,7 +2568,7 @@
                                             </ComboBox>
                                         </StackPanel>
                                         <StackPanel Grid.Column="2" Spacing="4">
-                                            <TextBlock Text="Start with Windows" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
+                                            <TextBlock Text="Запускать с Windows" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                             <ComboBox x:Name="StartWithWindowsCombo" x:FieldModifier="internal"
                                                       SelectionChanged="StartWithWindowsCombo_SelectionChanged"
                                                       FontSize="12" Padding="8,4" CornerRadius="8" HorizontalAlignment="Stretch">
@@ -2588,37 +2588,37 @@
                         <Border Background="{StaticResource SurfaceRaisedBrush}" CornerRadius="10" Padding="20,16"
                                 BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                             <StackPanel Spacing="8">
-                                <TextBlock Text="Data &amp; Custom Files" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
+                                <TextBlock Text="Данные и пользовательские файлы" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                 <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="20">
-                                    All RHI data is stored in AppData. Drop custom ReShade, DLSS, or Streamline DLLs into the Custom folder to use them as per-game overrides. The Logs folder contains session usage logs useful for troubleshooting.
+                                    Все данные RHI хранятся в AppData. Перетащите свои DLL ReShade, DLSS или Streamline в папку Custom, чтобы использовать их как переопределения для отдельных игр. В папке Logs лежат журналы сеансов — они помогают при разборе проблем.
                                 </TextBlock>
                                 <StackPanel Orientation="Horizontal" Spacing="8">
-                                    <Button x:Name="OpenAppDataBtn" Content="AppData Folder" Click="OpenAppDataFolder_Click"
+                                    <Button x:Name="OpenAppDataBtn" Content="Папка AppData" Click="OpenAppDataFolder_Click"
                                             Background="{StaticResource AccentBlueBgBrush}" Foreground="{StaticResource AccentBlueBrush}"
                                             BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Padding="12,6" FontSize="12"/>
-                                    <Button x:Name="OpenDownloadsBtn" Content="Downloads Folder" Click="OpenDownloadsFolder_Click"
+                                    <Button x:Name="OpenDownloadsBtn" Content="Папка загрузок" Click="OpenDownloadsFolder_Click"
                                             Background="{StaticResource AccentBlueBgBrush}" Foreground="{StaticResource AccentBlueBrush}"
                                             BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Padding="12,6" FontSize="12"/>
-                                    <Button x:Name="OpenCustomBtn" Content="Custom Folder" Click="OpenCustomFolder_Click"
+                                    <Button x:Name="OpenCustomBtn" Content="Папка Custom" Click="OpenCustomFolder_Click"
                                             Background="{StaticResource AccentBlueBgBrush}" Foreground="{StaticResource AccentBlueBrush}"
                                             BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Padding="12,6" FontSize="12"/>
-                                    <Button x:Name="OpenLogsBtn" Content="Logs Folder" Click="OpenLogsFolder_Click"
+                                    <Button x:Name="OpenLogsBtn" Content="Папка журналов" Click="OpenLogsFolder_Click"
                                             Background="{StaticResource AccentBlueBgBrush}" Foreground="{StaticResource AccentBlueBrush}"
                                             BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Padding="12,6" FontSize="12"/>
-                                    <Button x:Name="CopyLogsArchiveBtn" Content="Copy Logs" Click="CopyLogsArchive_Click"
+                                    <Button x:Name="CopyLogsArchiveBtn" Content="Скопировать журналы" Click="CopyLogsArchive_Click"
                                             Background="{StaticResource AccentBlueBgBrush}" Foreground="{StaticResource AccentBlueBrush}"
                                             BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Padding="12,6" FontSize="12"
-                                            ToolTipService.ToolTip="Creates a zip archive of all logs and copies it to clipboard — paste directly into Discord"/>
-                                    <Button x:Name="ExportGameDataBtn" x:FieldModifier="internal" Content="Export Game Data" Click="ExportGameData_Click"
+                                            ToolTipService.ToolTip="Создаёт zip-архив всех журналов и копирует его в буфер обмена — можно вставить прямо в Discord"/>
+                                    <Button x:Name="ExportGameDataBtn" x:FieldModifier="internal" Content="Экспортировать данные об играх" Click="ExportGameData_Click"
                                             Background="{StaticResource AccentBlueBgBrush}" Foreground="{StaticResource AccentBlueBrush}"
                                             BorderBrush="{StaticResource AccentBlueBorderBrush}" BorderThickness="1"
                                             CornerRadius="8" Padding="12,6" FontSize="12"
-                                            ToolTipService.ToolTip="Gathers game library data (APIs, exe paths, engine info) and copies a JSON file to clipboard — paste into Discord to share with the RHI community database"/>
+                                            ToolTipService.ToolTip="Собирает данные игровой библиотеки (API, пути к exe, сведения о движке) и копирует JSON в буфер обмена — вставьте в Discord, чтобы поделиться с базой сообщества RHI"/>
                                 </StackPanel>
                             </StackPanel>
                         </Border>
@@ -2627,13 +2627,13 @@
                         <StackPanel Visibility="Collapsed">
                             <ComboBox x:Name="ReShadeChannelCombo" x:FieldModifier="internal"
                                       SelectionChanged="ReShadeChannelCombo_SelectionChanged">
-                                <x:String>Stable (reshade.me releases)</x:String>
-                                <x:String>Nightly (GitHub Actions builds)</x:String>
+                                <x:String>Stable (релизы с reshade.me)</x:String>
+                                <x:String>Nightly (сборки GitHub Actions)</x:String>
                             </ComboBox>
                             <ComboBox x:Name="DxvkVariantCombo" x:FieldModifier="internal"
                                       SelectionChanged="DxvkVariantCombo_SelectionChanged">
-                                <x:String>Development (nightly builds)</x:String>
-                                <x:String>Stable (tagged releases)</x:String>
+                                <x:String>Development (ночные сборки)</x:String>
+                                <x:String>Stable (тегированные релизы)</x:String>
                                 <x:String>Lilium HDR</x:String>
                             </ComboBox>
                         </StackPanel>
@@ -2655,12 +2655,12 @@
                 <Border Grid.Row="0" Padding="24,12,24,10" Background="Transparent"
                         BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="0,0,0,1">
                     <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
-                        <Button Content="← Back to Games"
+                        <Button Content="← К играм"
                                 Click="AboutBack_Click"
                                 Background="{StaticResource SurfaceOverlayBrush}" Foreground="{StaticResource TextSecondaryBrush}"
                                 BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1"
                                 CornerRadius="8" Padding="12,6" FontSize="12"/>
-                        <TextBlock Text="About" FontSize="20" FontWeight="Bold"
+                        <TextBlock Text="О программе" FontSize="20" FontWeight="Bold"
                                    Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                     </StackPanel>
                 </Border>
@@ -2685,16 +2685,16 @@
                                 </StackPanel>
 
                                 <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" LineHeight="20"
-                                           Text="The all-in-one game enhancement toolkit for PC. Auto-detects games from 8 storefronts and provides one-click management for ReShade, HDR mods, shaders, DLSS/Streamline, frame limiters, NVIDIA driver profiles, and more — all from one place."/>
+                                           Text="Универсальный набор для улучшения игр на ПК. Сам находит игры из 8 магазинов и даёт управление в один клик: ReShade, HDR-моды, шейдеры, DLSS/Streamline, ограничители FPS, профили драйвера NVIDIA и многое другое — всё в одном месте."/>
 
                                 <!-- What it does -->
                                 <Border Background="{StaticResource SurfaceToolbarBrush}" CornerRadius="8" Padding="14,10"
                                         BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                                     <StackPanel Spacing="4">
-                                        <TextBlock Text="What RHI manages" FontSize="12" FontWeight="SemiBold"
+                                        <TextBlock Text="Что управляет RHI" FontSize="12" FontWeight="SemiBold"
                                                    Foreground="{StaticResource TextPrimaryBrush}"/>
                                         <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                                   Text="ReShade &amp; shader packs · RenoDX HDR mods · Luma Framework · DLSS/Streamline version swaps &amp; presets · NVIDIA driver profile settings (VSync, Low Latency, ReBAR, Smooth Motion, Power) · OptiScaler · ReLimiter &amp; Display Commander frame limiters · RE Framework · DXVK · UE-Extended native HDR · Per-game overrides for everything."/>
+                                                   Text="ReShade и наборы шейдеров · HDR-моды RenoDX · Luma Framework · подмена версий DLSS/Streamline и пресеты · настройки профиля драйвера NVIDIA (VSync, Low Latency, ReBAR, Smooth Motion, питание) · OptiScaler · ограничители FPS ReLimiter и Display Commander · RE Framework · DXVK · нативный HDR UE-Extended · индивидуальные переопределения для всего."/>
                                     </StackPanel>
                                 </Border>
 
@@ -2703,10 +2703,10 @@
                                 <Border Background="{StaticResource AccentRedBgBrush}" CornerRadius="8" Padding="14,10"
                                         BorderBrush="{StaticResource AccentPurpleBorderBrush}" BorderThickness="1">
                                     <StackPanel Spacing="4">
-                                        <TextBlock Text="Single-player only" FontSize="12" FontWeight="SemiBold"
+                                        <TextBlock Text="Только одиночная игра" FontSize="12" FontWeight="SemiBold"
                                                    Foreground="{StaticResource AccentRedBrush}"/>
                                         <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="18"
-                                                   Text="RHI installs ReShade with full addon support, OptiScaler, and can swap DLSS/Streamline DLLs. These modifications may be flagged by anti-cheat in online or multiplayer games. Uninstall ReShade, OptiScaler, and restore DLSS/Streamline defaults before playing online."/>
+                                                   Text="RHI устанавливает ReShade с полной поддержкой аддонов и OptiScaler, а также подменяет DLL DLSS/Streamline. Эти модификации могут распознаваться античитом в сетевых и многопользовательских играх. Перед игрой онлайн удалите ReShade, OptiScaler и верните DLSS/Streamline к исходным версиям."/>
                                     </StackPanel>
                                 </Border>
                             </StackPanel>
@@ -2716,21 +2716,21 @@
                         <Border Background="{StaticResource SurfaceRaisedBrush}" CornerRadius="10" Padding="20,16"
                                 BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                             <StackPanel Spacing="10">
-                                <TextBlock Text="Credits &amp; Acknowledgements" FontSize="13" FontWeight="SemiBold"
+                                <TextBlock Text="Благодарности и авторы" FontSize="13" FontWeight="SemiBold"
                                            Foreground="{StaticResource TextPrimaryBrush}"/>
                                 <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{StaticResource InlineDescriptionBrush}" LineHeight="20"
-                                           Text="This app would not exist without the work of the following people and projects."/>
+                                           Text="Это приложение не существовало бы без труда следующих людей и проектов."/>
 
                                 <!-- RenoDX -->
                                 <Border Background="{StaticResource SurfaceToolbarBrush}" CornerRadius="8" Padding="12,10" BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                                     <StackPanel Spacing="3">
                                         <TextBlock FontSize="12">
                                             <Run Text="RenoDX" FontWeight="SemiBold" Foreground="{StaticResource AccentTealBrush}"/>
-                                            <Run Text=" by clshortfuse &amp; contributors" Foreground="{StaticResource TextTertiaryBrush}"/>
-                                            <Run Text="  ·  MIT Licence" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
+                                            <Run Text=" от clshortfuse и соавторов" Foreground="{StaticResource TextTertiaryBrush}"/>
+                                            <Run Text="  ·  Лицензия MIT" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
                                         </TextBlock>
                                         <TextBlock FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" Margin="0,0,0,0" TextWrapping="Wrap"
-                                                   Text="HDR mod framework powering 800+ games. The entire reason this app exists."/>
+                                                   Text="Фреймворк HDR-модов для 800+ игр. Главная причина существования этого приложения."/>
                                         <HyperlinkButton NavigateUri="https://github.com/clshortfuse/renodx" Padding="0,2,0,0" FontSize="11">
                                             <TextBlock Foreground="{StaticResource AccentTealDimBrush}" Text="github.com/clshortfuse/renodx"/>
                                         </HyperlinkButton>
@@ -2742,11 +2742,11 @@
                                     <StackPanel Spacing="3">
                                         <TextBlock FontSize="12">
                                             <Run Text="ReShade" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
-                                            <Run Text=" by Crosire" Foreground="{StaticResource TextTertiaryBrush}"/>
-                                            <Run Text="  ·  BSD 3-Clause Licence" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
+                                            <Run Text=" от Crosire" Foreground="{StaticResource TextTertiaryBrush}"/>
+                                            <Run Text="  ·  Лицензия BSD 3-Clause" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
                                         </TextBlock>
                                         <TextBlock FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" TextWrapping="Wrap"
-                                                   Text="Post-processing injection framework. Downloaded with full addon support from reshade.me and cached locally. Copyright © Crosire."/>
+                                                   Text="Фреймворк внедрения пост-обработки. Скачивается с reshade.me с полной поддержкой аддонов и кешируется локально. © Crosire."/>
                                         <HyperlinkButton NavigateUri="https://reshade.me" Padding="0,2,0,0" FontSize="11">
                                             <TextBlock Foreground="{StaticResource AccentTealDimBrush}" Text="reshade.me  ·  github.com/crosire/reshade"/>
                                         </HyperlinkButton>
@@ -2758,11 +2758,11 @@
                                     <StackPanel Spacing="3">
                                         <TextBlock FontSize="12">
                                             <Run Text="Luma Framework" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
-                                            <Run Text=" by Pumbo (Filoppi)" Foreground="{StaticResource TextTertiaryBrush}"/>
-                                            <Run Text="  ·  Source-available" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
+                                            <Run Text=" от Pumbo (Filoppi)" Foreground="{StaticResource TextTertiaryBrush}"/>
+                                            <Run Text="  ·  Открытый исходный код" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
                                         </TextBlock>
                                         <TextBlock FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" TextWrapping="Wrap"
-                                                   Text="DX11 modding framework adding HDR support and graphics improvements via the ReShade addon system. Mods are downloaded from official GitHub releases. Drag-and-drop install with auto-detection, update checking, and per-game toggle."/>
+                                                   Text="Мод-фреймворк для DX11, добавляющий поддержку HDR и графические улучшения через систему аддонов ReShade. Моды скачиваются с официальных релизов GitHub. Установка перетаскиванием с автоопределением, проверкой обновлений и переключателем для каждой игры."/>
                                         <HyperlinkButton NavigateUri="https://github.com/Filoppi/Luma-Framework" Padding="0,2,0,0" FontSize="11">
                                             <TextBlock Foreground="{StaticResource AccentTealDimBrush}" Text="github.com/Filoppi/Luma-Framework"/>
                                         </HyperlinkButton>
@@ -2774,11 +2774,11 @@
                                     <StackPanel Spacing="3">
                                         <TextBlock FontSize="12">
                                             <Run Text="7-Zip" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
-                                            <Run Text=" by Igor Pavlov" Foreground="{StaticResource TextTertiaryBrush}"/>
+                                            <Run Text=" от Игоря Павлова" Foreground="{StaticResource TextTertiaryBrush}"/>
                                             <Run Text="  ·  LGPL + BSD 3-Clause" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
                                         </TextBlock>
                                         <TextBlock FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" TextWrapping="Wrap"
-                                                   Text="Archive utility used to extract ReShade DLLs from the NSIS installer. 7z.exe and 7z.dll are bundled under the LGPL licence. Copyright © Igor Pavlov."/>
+                                                   Text="Архиватор, используемый для извлечения DLL ReShade из NSIS-установщика. 7z.exe и 7z.dll поставляются по лицензии LGPL. © Игорь Павлов."/>
                                         <HyperlinkButton NavigateUri="https://www.7-zip.org/" Padding="0,2,0,0" FontSize="11">
                                             <TextBlock Foreground="{StaticResource AccentTealDimBrush}" Text="7-zip.org  ·  LGPL-2.1 / BSD-3-Clause"/>
                                         </HyperlinkButton>
@@ -2790,11 +2790,11 @@
                                     <StackPanel Spacing="3">
                                         <TextBlock FontSize="12">
                                             <Run Text="HtmlAgilityPack" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
-                                            <Run Text=" by ZZZ Projects" Foreground="{StaticResource TextTertiaryBrush}"/>
-                                            <Run Text="  ·  MIT Licence" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
+                                            <Run Text=" от ZZZ Projects" Foreground="{StaticResource TextTertiaryBrush}"/>
+                                            <Run Text="  ·  Лицензия MIT" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
                                         </TextBlock>
                                         <TextBlock FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" TextWrapping="Wrap"
-                                                   Text="HTML parser used to scrape game data from the RenoDX wiki. Copyright © ZZZ Projects Inc."/>
+                                                   Text="HTML-парсер для сбора данных об играх из вики RenoDX. © ZZZ Projects Inc."/>
                                         <HyperlinkButton NavigateUri="https://github.com/zzzprojects/html-agility-pack" Padding="0,2,0,0" FontSize="11">
                                             <TextBlock Foreground="{StaticResource AccentTealDimBrush}" Text="github.com/zzzprojects/html-agility-pack"/>
                                         </HyperlinkButton>
@@ -2806,11 +2806,11 @@
                                     <StackPanel Spacing="3">
                                         <TextBlock FontSize="12">
                                             <Run Text="CommunityToolkit.Mvvm" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
-                                            <Run Text=" by Microsoft" Foreground="{StaticResource TextTertiaryBrush}"/>
-                                            <Run Text="  ·  MIT Licence" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
+                                            <Run Text=" от Microsoft" Foreground="{StaticResource TextTertiaryBrush}"/>
+                                            <Run Text="  ·  Лицензия MIT" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
                                         </TextBlock>
                                         <TextBlock FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" TextWrapping="Wrap"
-                                                   Text="MVVM helpers (ObservableObject, RelayCommand, etc.) used throughout the app. Copyright © .NET Foundation."/>
+                                                   Text="MVVM-помощники (ObservableObject, RelayCommand и др.), используемые во всём приложении. © .NET Foundation."/>
                                         <HyperlinkButton NavigateUri="https://github.com/CommunityToolkit/dotnet" Padding="0,2,0,0" FontSize="11">
                                             <TextBlock Foreground="{StaticResource AccentTealDimBrush}" Text="github.com/CommunityToolkit/dotnet"/>
                                         </HyperlinkButton>
@@ -2822,11 +2822,11 @@
                                     <StackPanel Spacing="3">
                                         <TextBlock FontSize="12">
                                             <Run Text="Display Commander" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
-                                            <Run Text=" by pmnox" Foreground="{StaticResource TextTertiaryBrush}"/>
-                                            <Run Text="  ·  Source-available" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
+                                            <Run Text=" от pmnox" Foreground="{StaticResource TextTertiaryBrush}"/>
+                                            <Run Text="  ·  Открытый исходный код" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
                                         </TextBlock>
                                         <TextBlock FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" TextWrapping="Wrap"
-                                                   Text="Frame rate limiter addon available as an alternative to ReLimiter. The LITE variant is downloaded from GitHub on demand."/>
+                                                   Text="Аддон-ограничитель частоты кадров как альтернатива ReLimiter. Вариант LITE скачивается с GitHub по мере необходимости."/>
                                         <HyperlinkButton NavigateUri="https://github.com/pmnoxx/display-commander" Padding="0,2,0,0" FontSize="11">
                                             <TextBlock Foreground="{StaticResource AccentTealDimBrush}" Text="github.com/pmnoxx/display-commander"/>
                                         </HyperlinkButton>
@@ -2838,11 +2838,11 @@
                                     <StackPanel Spacing="3">
                                         <TextBlock FontSize="12">
                                             <Run Text="RE Framework" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
-                                            <Run Text=" by praydog" Foreground="{StaticResource TextTertiaryBrush}"/>
-                                            <Run Text="  ·  MIT Licence" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
+                                            <Run Text=" от praydog" Foreground="{StaticResource TextTertiaryBrush}"/>
+                                            <Run Text="  ·  Лицензия MIT" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
                                         </TextBlock>
                                         <TextBlock FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" TextWrapping="Wrap"
-                                                   Text="Modding framework required for ReShade injection on RE Engine games. Downloaded from nightly builds on GitHub."/>
+                                                   Text="Мод-фреймворк, необходимый для инъекции ReShade в игры на RE Engine. Скачивается из ночных сборок на GitHub."/>
                                         <HyperlinkButton NavigateUri="https://github.com/praydog/REFramework-nightly" Padding="0,2,0,0" FontSize="11">
                                             <TextBlock Foreground="{StaticResource AccentTealDimBrush}" Text="github.com/praydog/REFramework-nightly"/>
                                         </HyperlinkButton>
@@ -2854,11 +2854,11 @@
                                     <StackPanel Spacing="3">
                                         <TextBlock FontSize="12">
                                             <Run Text="OptiScaler" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
-                                            <Run Text=" by OptiScaler contributors" Foreground="{StaticResource TextTertiaryBrush}"/>
-                                            <Run Text="  ·  Source-available" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
+                                            <Run Text=" от участников разработки OptiScaler" Foreground="{StaticResource TextTertiaryBrush}"/>
+                                            <Run Text="  ·  Открытый исходный код" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
                                         </TextBlock>
                                         <TextBlock FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" TextWrapping="Wrap"
-                                                   Text="Upscaler redirection layer enabling DLSS, FSR, and XeSS across different GPU vendors. Downloaded from GitHub releases."/>
+                                                   Text="Слой перенаправления апскейлеров, включающий DLSS, FSR и XeSS на GPU разных производителей. Скачивается из релизов GitHub."/>
                                         <HyperlinkButton NavigateUri="https://github.com/optiscaler/OptiScaler" Padding="0,2,0,0" FontSize="11">
                                             <TextBlock Foreground="{StaticResource AccentTealDimBrush}" Text="github.com/optiscaler/OptiScaler"/>
                                         </HyperlinkButton>
@@ -2870,11 +2870,11 @@
                                     <StackPanel Spacing="3">
                                         <TextBlock FontSize="12">
                                             <Run Text="DXVK" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
-                                            <Run Text=" by doitsujin &amp; contributors" Foreground="{StaticResource TextTertiaryBrush}"/>
-                                            <Run Text="  ·  Zlib Licence" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
+                                            <Run Text=" от doitsujin и соавторов" Foreground="{StaticResource TextTertiaryBrush}"/>
+                                            <Run Text="  ·  Лицензия Zlib" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
                                         </TextBlock>
                                         <TextBlock FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" TextWrapping="Wrap"
-                                                   Text="DirectX-to-Vulkan translation layer for DX8/DX9/DX10 games. Includes Lilium HDR fork by EndlesslyFlowering for scRGB HDR output."/>
+                                                   Text="Слой трансляции DirectX в Vulkan для игр DX8/DX9/DX10. Включает форк Lilium HDR от EndlesslyFlowering для вывода scRGB HDR."/>
                                         <HyperlinkButton NavigateUri="https://github.com/doitsujin/dxvk" Padding="0,2,0,0" FontSize="11">
                                             <TextBlock Foreground="{StaticResource AccentTealDimBrush}" Text="github.com/doitsujin/dxvk  ·  github.com/EndlesslyFlowering/dxvk"/>
                                         </HyperlinkButton>
@@ -2886,11 +2886,11 @@
                                     <StackPanel Spacing="3">
                                         <TextBlock FontSize="12">
                                             <Run Text="NvAPIWrapper.Net" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
-                                            <Run Text=" by falahati" Foreground="{StaticResource TextTertiaryBrush}"/>
-                                            <Run Text="  ·  MIT Licence" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
+                                            <Run Text=" от falahati" Foreground="{StaticResource TextTertiaryBrush}"/>
+                                            <Run Text="  ·  Лицензия MIT" Foreground="{StaticResource InlineDescriptionBrush}" FontSize="11"/>
                                         </TextBlock>
                                         <TextBlock FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" TextWrapping="Wrap"
-                                                   Text=".NET wrapper for NVIDIA's NvAPI used for all driver profile management — DLSS presets, render scale, ReBAR, VSync, Smooth Motion, and more."/>
+                                                   Text="Обёртка .NET для NvAPI NVIDIA, через которую работает всё управление профилями драйвера — пресеты DLSS, масштаб рендеринга, ReBAR, VSync, Smooth Motion и другое."/>
                                         <HyperlinkButton NavigateUri="https://github.com/falahati/NvAPIWrapper" Padding="0,2,0,0" FontSize="11">
                                             <TextBlock Foreground="{StaticResource AccentTealDimBrush}" Text="github.com/falahati/NvAPIWrapper"/>
                                         </HyperlinkButton>
@@ -2901,11 +2901,11 @@
                                 <Border Background="{StaticResource SurfaceStatusBarBrush}" CornerRadius="8" Padding="12,10" BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                                     <StackPanel Spacing="3">
                                         <TextBlock FontSize="12">
-                                            <Run Text="UI Design" FontWeight="SemiBold" Foreground="{StaticResource AccentGreenBrush}"/>
-                                            <Run Text=" by Lazorr" Foreground="{StaticResource TextTertiaryBrush}"/>
+                                            <Run Text="Дизайн интерфейса" FontWeight="SemiBold" Foreground="{StaticResource AccentGreenBrush}"/>
+                                            <Run Text=" от Lazorr" Foreground="{StaticResource TextTertiaryBrush}"/>
                                         </TextBlock>
                                         <TextBlock FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" TextWrapping="Wrap"
-                                                   Text="Somehow I have become a UI guy in multiple aspects of life lol."/>
+                                                   Text="Как-то так вышло, что я стал UI-щиком сразу в нескольких сферах жизни :)"/>
                                         <HyperlinkButton NavigateUri="https://github.com/lazorr410" Padding="0,2,0,0" FontSize="11">
                                             <TextBlock Foreground="{StaticResource AccentTealDimBrush}" Text="https://github.com/lazorr410"/>
                                         </HyperlinkButton>
@@ -2917,11 +2917,11 @@
                                 <Border Background="{StaticResource SurfaceStatusBarBrush}" CornerRadius="8" Padding="12,10" BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                                     <StackPanel Spacing="3">
                                         <TextBlock FontSize="12">
-                                            <Run Text="Universal UE DOF Fix" FontWeight="SemiBold" Foreground="{StaticResource AccentGreenBrush}"/>
-                                            <Run Text=" by Lazorr" Foreground="{StaticResource TextTertiaryBrush}"/>
+                                            <Run Text="Универсальный UE DOF Fix" FontWeight="SemiBold" Foreground="{StaticResource AccentGreenBrush}"/>
+                                            <Run Text=" от Lazorr" Foreground="{StaticResource TextTertiaryBrush}"/>
                                         </TextBlock>
                                         <TextBlock FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" TextWrapping="Wrap"
-                                                   Text="Fixes depth-of-field stepping and tiling artifacts in Unreal Engine 5 games."/>
+                                                   Text="Исправляет ступенчатость глубины резкости и тайлинг в играх на Unreal Engine 5."/>
                                         <HyperlinkButton NavigateUri="https://github.com/RankFTW/rhi-repo/releases" Padding="0,2,0,0" FontSize="11">
                                             <TextBlock Foreground="{StaticResource AccentTealDimBrush}" Text="github.com/RankFTW/rhi-repo"/>
                                         </HyperlinkButton>
@@ -2933,10 +2933,10 @@
                                     <StackPanel Spacing="3">
                                         <TextBlock FontSize="12">
                                             <Run Text="RHI" FontWeight="SemiBold" Foreground="{StaticResource AccentGreenBrush}"/>
-                                            <Run Text=" by RankFTW" Foreground="{StaticResource TextTertiaryBrush}"/>
+                                            <Run Text=" от RankFTW" Foreground="{StaticResource TextTertiaryBrush}"/>
                                         </TextBlock>
                                         <TextBlock FontSize="11" Foreground="{StaticResource InlineDescriptionBrush}" TextWrapping="Wrap"
-                                                   Text="Unofficial companion app. Not affiliated with RenoDX, Crosire, or pmnoxx."/>
+                                                   Text="Неофициальное вспомогательное приложение. Не связано с RenoDX, Crosire или pmnoxx."/>
                                         <HyperlinkButton NavigateUri="https://github.com/RankFTW" Padding="0,2,0,0" FontSize="11">
                                             <TextBlock Foreground="{StaticResource AccentTealDimBrush}" Text="github.com/RankFTW"/>
                                         </HyperlinkButton>
@@ -2949,12 +2949,12 @@
                         <Border Background="{StaticResource SurfaceRaisedBrush}" CornerRadius="10" Padding="20,16"
                                 BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                             <StackPanel Spacing="8">
-                                <TextBlock Text="Links" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
+                                <TextBlock Text="Ссылки" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                 <HyperlinkButton NavigateUri="https://github.com/clshortfuse/renodx" Padding="0">
                                     <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">RenoDX Project — github.com/clshortfuse/renodx</TextBlock>
                                 </HyperlinkButton>
                                 <HyperlinkButton NavigateUri="https://github.com/clshortfuse/renodx/wiki/Mods" Padding="0">
-                                    <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">RenoDX Mod Wiki</TextBlock>
+                                    <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">Вики модов RenoDX</TextBlock>
                                 </HyperlinkButton>
                                 <HyperlinkButton NavigateUri="https://discord.gg/gF4GRJWZ2A" Padding="0">
                                     <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">RenoDX Discord</TextBlock>
@@ -2963,7 +2963,7 @@
                                     <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">HDR Den Discord</TextBlock>
                                 </HyperlinkButton>
                                 <HyperlinkButton NavigateUri="https://github.com/Filoppi/Luma-Framework/wiki/Mods-List" Padding="0">
-                                    <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">HDR Den / Luma Wiki</TextBlock>
+                                    <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">HDR Den / вики Luma</TextBlock>
                                 </HyperlinkButton>
                                 <HyperlinkButton NavigateUri="https://reshade.me" Padding="0">
                                     <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">ReShade — reshade.me</TextBlock>
@@ -2975,13 +2975,13 @@
                                     <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">Display Commander — github.com/pmnoxx/display-commander</TextBlock>
                                 </HyperlinkButton>
                                 <HyperlinkButton NavigateUri="https://www.hdrmods.com/" Padding="0">
-                                    <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">Creepy's HDR Guides — hdrmods.com</TextBlock>
+                                    <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">HDR-руководства Creepy — hdrmods.com</TextBlock>
                                 </HyperlinkButton>
                                 <HyperlinkButton NavigateUri="https://discord.gg/pQtPYcdE" Padding="0">
-                                    <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">The Ultra Place / Ultra+ Discord</TextBlock>
+                                    <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">Discord The Ultra Place / Ultra+</TextBlock>
                                 </HyperlinkButton>
                                 <HyperlinkButton NavigateUri="https://github.com/RankFTW" Padding="0">
-                                    <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">RankFTW GitHub</TextBlock>
+                                    <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">RankFTW на GitHub</TextBlock>
                                 </HyperlinkButton>
                                 <HyperlinkButton NavigateUri="https://github.com/praydog/REFramework-nightly" Padding="0">
                                     <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">RE Framework — github.com/praydog/REFramework-nightly</TextBlock>
@@ -2990,7 +2990,7 @@
                                     <TextBlock Foreground="{StaticResource AccentBlueBrush}" FontSize="12">OptiScaler — github.com/optiscaler/OptiScaler</TextBlock>
                                 </HyperlinkButton>
                                 <HyperlinkButton NavigateUri="https://ko-fi.com/rankftw" Padding="0">
-                                    <TextBlock Foreground="{StaticResource AccentRedBrush}" FontSize="12">Support RHI on Ko-Fi</TextBlock>
+                                    <TextBlock Foreground="{StaticResource AccentRedBrush}" FontSize="12">Поддержать RHI на Ko-Fi</TextBlock>
                                 </HyperlinkButton>
                             </StackPanel>
                         </Border>
@@ -3011,12 +3011,12 @@
                 <Border Grid.Row="0" Padding="24,12,24,10" Background="Transparent"
                         BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="0,0,0,1">
                     <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
-                        <Button Content="← Back to Games"
+                        <Button Content="← К играм"
                                 Click="FaqBack_Click"
                                 Background="{StaticResource SurfaceOverlayBrush}" Foreground="{StaticResource TextSecondaryBrush}"
                                 BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1"
                                 CornerRadius="8" Padding="12,6" FontSize="12"/>
-                        <TextBlock Text="Quick Start Guide" FontSize="20" FontWeight="Bold"
+                        <TextBlock Text="Руководство быстрого старта" FontSize="20" FontWeight="Bold"
                                    Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                     </StackPanel>
                 </Border>
@@ -3049,7 +3049,7 @@
                 <TextBlock Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                            FontSize="10" Foreground="{StaticResource AccentAmberDimBrush}"
                            TextTrimming="CharacterEllipsis" MaxLines="1"
-                           Text="⚠ ReShade, OptiScaler, and DLSS/Streamline modifications may trigger anti-cheat in online/multiplayer games"/>
+                           Text="⚠ ReShade, OptiScaler и модификации DLSS/Streamline могут сработать в античите сетевых/многопользовательских игр"/>
                 <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right"
                             VerticalAlignment="Center" Spacing="8">
                     <Button x:Name="StatusBarVersionText" x:FieldModifier="internal"
@@ -3057,19 +3057,19 @@
                             Background="Transparent" BorderThickness="0"
                             Padding="0,2" VerticalAlignment="Center"
                             Click="StatusBarVersion_Click"
-                            ToolTipService.ToolTip="Check for app updates"/>
-                    <Button Content="Patch Notes"
+                            ToolTipService.ToolTip="Проверить обновления приложения"/>
+                    <Button Content="Список изменений"
                             Click="PatchNotesLink_Click"
                             Background="Transparent" BorderThickness="0"
                             Foreground="{StaticResource TextDisabledBrush}" FontSize="11"
                             Padding="8,2" VerticalAlignment="Center"
-                            ToolTipService.ToolTip="View recent patch notes"/>
+                            ToolTipService.ToolTip="Посмотреть свежие списки изменений"/>
                     <Button Content="MOTD"
                             Click="MotdButton_Click"
                             Background="Transparent" BorderThickness="0"
                             Foreground="{StaticResource TextDisabledBrush}" FontSize="11"
                             Padding="8,2" VerticalAlignment="Center"
-                            ToolTipService.ToolTip="Show message of the day"/>
+                            ToolTipService.ToolTip="Показывать сообщение дня"/>
                 </StackPanel>
             </Grid>
         </Border>
diff --git a/RenoDXCommander/MainWindow.xaml.cs b/RenoDXCommander/MainWindow.xaml.cs
index 257fc2b..9cd6fb3 100644
--- a/RenoDXCommander/MainWindow.xaml.cs
+++ b/RenoDXCommander/MainWindow.xaml.cs
@@ -500,7 +500,7 @@ public sealed partial class MainWindow : Window
                 };
                 var pickerDialog = new ContentDialog
                 {
-                    Title = "🌙 Install Luma Addon",
+                    Title = "🌙 Установить аддон Luma",
                     Content = new StackPanel
                     {
                         Spacing = 8,
@@ -510,8 +510,8 @@ public sealed partial class MainWindow : Window
                             combo,
                         }
                     },
-                    PrimaryButtonText = "Install",
-                    CloseButtonText = "Cancel",
+                    PrimaryButtonText = "Установить",
+                    CloseButtonText = "Отмена",
                     XamlRoot = Content.XamlRoot,
                     RequestedTheme = Microsoft.UI.Xaml.ElementTheme.Dark,
                 };
diff --git a/RenoDXCommander/MassDeployHandler.cs b/RenoDXCommander/MassDeployHandler.cs
index dc39582..93fe23b 100644
--- a/RenoDXCommander/MassDeployHandler.cs
+++ b/RenoDXCommander/MassDeployHandler.cs
@@ -27,10 +27,10 @@ public class MassDeployHandler
         var eligible = _window.ViewModel.AllCards.Where(c => c.RsStatus == GameStatus.Installed && !string.IsNullOrEmpty(c.InstallPath)).ToList();
         var confirmDialog = new ContentDialog
         {
-            Title = "Confirm Mass Deployment",
-            Content = $"This will deploy ReShade.ini to {eligible.Count} game(s) with ReShade installed.\n\nCustom hotkey and screenshot path settings are preserved.\n\nContinue?",
-            PrimaryButtonText = "Deploy",
-            CloseButtonText = "Cancel",
+            Title = "Подтвердить массовое развертывание",
+            Content = $"ReShade.ini будет развернут в игры ({eligible.Count}) с установленным ReShade.\n\nПользовательские горячие клавиши и путь к скриншотам сохранятся.\n\nПродолжить?",
+            PrimaryButtonText = "Развернуть",
+            CloseButtonText = "Отмена",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -64,10 +64,10 @@ public class MassDeployHandler
         var eligible = _window.ViewModel.AllCards.Where(c => c.UlStatus == GameStatus.Installed && !string.IsNullOrEmpty(c.InstallPath)).ToList();
         var confirmDialog = new ContentDialog
         {
-            Title = "Confirm Mass Deployment",
-            Content = $"This will deploy relimiter.ini to {eligible.Count} game(s) with ReLimiter installed.\n\nContinue?",
-            PrimaryButtonText = "Deploy",
-            CloseButtonText = "Cancel",
+            Title = "Подтвердить массовое развертывание",
+            Content = $"relimiter.ini будет развернут в игры ({eligible.Count}) с установленным ReLimiter.\n\nПродолжить?",
+            PrimaryButtonText = "Развернуть",
+            CloseButtonText = "Отмена",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -95,10 +95,10 @@ public class MassDeployHandler
         var eligible = _window.ViewModel.AllCards.Where(c => c.DcStatus == GameStatus.Installed && !string.IsNullOrEmpty(c.InstallPath)).ToList();
         var confirmDialog = new ContentDialog
         {
-            Title = "Confirm Mass Deployment",
-            Content = $"This will deploy DisplayCommander.ini to {eligible.Count} game(s) with Display Commander installed.\n\nContinue?",
-            PrimaryButtonText = "Deploy",
-            CloseButtonText = "Cancel",
+            Title = "Подтвердить массовое развертывание",
+            Content = $"DisplayCommander.ini будет развернут в игры ({eligible.Count}) с установленным Display Commander.\n\nПродолжить?",
+            PrimaryButtonText = "Развернуть",
+            CloseButtonText = "Отмена",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -135,10 +135,10 @@ public class MassDeployHandler
         var eligible = _window.ViewModel.AllCards.Where(c => c.OsStatus == GameStatus.Installed && !string.IsNullOrEmpty(c.InstallPath)).ToList();
         var confirmDialog = new ContentDialog
         {
-            Title = "Confirm Mass Deployment",
-            Content = $"This will deploy OptiScaler.ini to {eligible.Count} game(s) with OptiScaler installed.\n\nContinue?",
-            PrimaryButtonText = "Deploy",
-            CloseButtonText = "Cancel",
+            Title = "Подтвердить массовое развертывание",
+            Content = $"OptiScaler.ini будет развернут в игры ({eligible.Count}) с установленным OptiScaler.\n\nПродолжить?",
+            PrimaryButtonText = "Развернуть",
+            CloseButtonText = "Отмена",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -163,11 +163,11 @@ public class MassDeployHandler
     private async Task ShowDeployResult(string iniName, int count)
     {
         var message = count > 0
-            ? $"✅ Deployed {iniName} to {count} game(s)."
-            : $"No games found with the corresponding component installed.";
+            ? $"✅ Развернуто {iniName} в игры ({count})."
+            : $"Игр с установленным соответствующим компонентом не найдено.";
         var dialog = new ContentDialog
         {
-            Title = "Mass INI Deployment",
+            Title = "Массовое развертывание INI",
             Content = message,
             CloseButtonText = "OK",
             XamlRoot = _window.Content.XamlRoot,
@@ -192,8 +192,8 @@ public class MassDeployHandler
         {
             var noGamesDialog = new ContentDialog
             {
-                Title = "No Games Available",
-                Content = "No games with ReShade installed were found. Install ReShade on at least one game first.",
+                Title = "Нет доступных игр",
+                Content = "Игр с установленным ReShade не найдено. Сначала установите ReShade хотя бы в одну игру.",
                 CloseButtonText = "OK",
                 XamlRoot = xamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -208,14 +208,14 @@ public class MassDeployHandler
         // Select All / Deselect All buttons
         var selectAllBtn = new Button
         {
-            Content = "Select All",
+            Content = "Выбрать всё",
             FontSize = 11,
             Padding = new Thickness(8, 4, 8, 4),
             Margin = new Thickness(0, 0, 8, 8),
         };
         var deselectAllBtn = new Button
         {
-            Content = "Deselect All",
+            Content = "Снять всё",
             FontSize = 11,
             Padding = new Thickness(8, 4, 8, 4),
             Margin = new Thickness(0, 0, 0, 8),
@@ -253,9 +253,9 @@ public class MassDeployHandler
         {
             Title = $"Select Games — {string.Join(", ", selectedPresets)}",
             Content = gameScrollViewer,
-            PrimaryButtonText = "Deploy",
+            PrimaryButtonText = "Развернуть",
             IsPrimaryButtonEnabled = false,
-            CloseButtonText = "Cancel",
+            CloseButtonText = "Отмена",
             XamlRoot = xamlRoot,
             RequestedTheme = ElementTheme.Dark,
             MinWidth = 500,
@@ -297,10 +297,10 @@ public class MassDeployHandler
         // ── 4. Offer shader installation ─────────────────────────────────────
         var shaderDialog = new ContentDialog
         {
-            Title = "🔧 Install Shaders?",
-            Content = $"Presets deployed to {selectedGames.Count} game(s).\n\nAlso install the required shader packs for these games?",
-            PrimaryButtonText = "Yes",
-            CloseButtonText = "No",
+            Title = "🔧 Установить шейдеры?",
+            Content = $"Пресеты развернуты в игры ({selectedGames.Count}).\n\nТакже установить необходимые наборы шейдеров для этих игр?",
+            PrimaryButtonText = "Да",
+            CloseButtonText = "Нет",
             XamlRoot = xamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
diff --git a/RenoDXCommander/MassDlssDeployDialog.cs b/RenoDXCommander/MassDlssDeployDialog.cs
index 65bb2e3..7bfde3d 100644
--- a/RenoDXCommander/MassDlssDeployDialog.cs
+++ b/RenoDXCommander/MassDlssDeployDialog.cs
@@ -20,7 +20,7 @@ public class MassDlssDeployDialog
     private readonly Action? _onComplete;
 
     private const string NoneOption = "None";
-    private const string DefaultOption = "Default (Restore)";
+    private const string DefaultOption = "По умолчанию (восстановить)";
     private const string CustomOption = "Custom";
 
     public MassDlssDeployDialog(MainViewModel viewModel, IDlssStreamlineService dlssService, XamlRoot xamlRoot, Action? onComplete = null)
@@ -43,8 +43,8 @@ public class MassDlssDeployDialog
         {
             var emptyDialog = new ContentDialog
             {
-                Title = "No DLSS/Streamline Games",
-                Content = "No games with DLSS or Streamline DLLs were detected.\nRun a Full Refresh to scan for them.",
+                Title = "Нет игр с DLSS/Streamline",
+                Content = "Игры с DLL DLSS или Streamline не обнаружены.\nВыполните «Полное обновление», чтобы найти их.",
                 CloseButtonText = "OK",
                 XamlRoot = _xamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -70,14 +70,14 @@ public class MassDlssDeployDialog
                     : Windows.UI.Color.FromArgb(255, 220, 220, 220)),
             };
             if (isV1)
-                ToolTipService.SetToolTip(cb, "Skipped — v1.x DLSS/Streamline not compatible with newer versions");
+                ToolTipService.SetToolTip(cb, "Пропущено — DLSS/Streamline v1.x несовместимы с новыми версиями");
             checkBoxes.Add(cb);
             gameListPanel.Children.Add(cb);
         }
 
         var selectAllBtn = new Button
         {
-            Content = "Select All", FontSize = 11, Padding = new Thickness(8, 4, 8, 4),
+            Content = "Выбрать всё", FontSize = 11, Padding = new Thickness(8, 4, 8, 4),
             Background = new SolidColorBrush(Windows.UI.Color.FromArgb(255, 30, 40, 60)),
             Foreground = new SolidColorBrush(Windows.UI.Color.FromArgb(255, 180, 190, 220)),
             CornerRadius = new CornerRadius(6),
@@ -86,7 +86,7 @@ public class MassDlssDeployDialog
 
         var deselectAllBtn = new Button
         {
-            Content = "Deselect All", FontSize = 11, Padding = new Thickness(8, 4, 8, 4),
+            Content = "Снять всё", FontSize = 11, Padding = new Thickness(8, 4, 8, 4),
             Background = new SolidColorBrush(Windows.UI.Color.FromArgb(255, 30, 40, 60)),
             Foreground = new SolidColorBrush(Windows.UI.Color.FromArgb(255, 180, 190, 220)),
             CornerRadius = new CornerRadius(6),
@@ -126,9 +126,9 @@ public class MassDlssDeployDialog
         var fgPresetCombo = BuildPresetCombo(DlssPresetService.FgPresets, settings.DefaultFgPreset);
 
         var rightPanel = new StackPanel { Spacing = 8, Width = 320 };
-        rightPanel.Children.Add(BuildDropdownSection("DLSS Super Resolution", dlssCombo));
-        rightPanel.Children.Add(BuildDropdownSection("DLSS Ray Reconstruction", dlssdCombo));
-        rightPanel.Children.Add(BuildDropdownSection("DLSS Frame Generation", dlssgCombo));
+        rightPanel.Children.Add(BuildDropdownSection("Суперразрешение DLSS (SR)", dlssCombo));
+        rightPanel.Children.Add(BuildDropdownSection("Трассировка лучей DLSS (Ray Reconstruction)", dlssdCombo));
+        rightPanel.Children.Add(BuildDropdownSection("Генерация кадров DLSS", dlssgCombo));
         rightPanel.Children.Add(BuildDropdownSection("Streamline", slCombo));
 
         // Presets section
@@ -138,9 +138,9 @@ public class MassDlssDeployDialog
             Background = new SolidColorBrush(Windows.UI.Color.FromArgb(255, 60, 70, 90)),
             Margin = new Thickness(0, 4, 0, 4),
         });
-        rightPanel.Children.Add(BuildDropdownSection("SR Preset", srPresetCombo));
-        rightPanel.Children.Add(BuildDropdownSection("RR Preset", rrPresetCombo));
-        rightPanel.Children.Add(BuildDropdownSection("FG Preset", fgPresetCombo));
+        rightPanel.Children.Add(BuildDropdownSection("Пресет SR", srPresetCombo));
+        rightPanel.Children.Add(BuildDropdownSection("Пресет RR", rrPresetCombo));
+        rightPanel.Children.Add(BuildDropdownSection("Пресет FG", fgPresetCombo));
 
         var rightScroll = new ScrollViewer
         {
@@ -152,7 +152,7 @@ public class MassDlssDeployDialog
         // Auto-create profiles checkbox — lives outside the scroll so it's always visible
         var autoCreateCheck = new CheckBox
         {
-            Content = "Auto-create NVIDIA profiles",
+            Content = "Автосоздание профилей NVIDIA",
             IsChecked = true,
             FontSize = 11,
             Foreground = new SolidColorBrush(Windows.UI.Color.FromArgb(255, 180, 190, 220)),
@@ -196,11 +196,11 @@ public class MassDlssDeployDialog
         // ── Dialog ──
         var dialog = new ContentDialog
         {
-            Title = "Batch DLSS & Streamline Deploy",
+            Title = "Массовое развертывание DLSS и Streamline",
             Content = mainGrid,
-            PrimaryButtonText = "Deploy",
-            SecondaryButtonText = "Restore",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Развернуть",
+            SecondaryButtonText = "Восстановить",
+            CloseButtonText = "Отмена",
             XamlRoot = _xamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -258,7 +258,7 @@ public class MassDlssDeployDialog
         // Show progress
         var progressText = new TextBlock
         {
-            Text = "Deploying to selected games...",
+            Text = "Развертывание в выбранные игры...",
             FontSize = 12,
             Foreground = new SolidColorBrush(Windows.UI.Color.FromArgb(255, 200, 200, 200)),
             TextWrapping = TextWrapping.Wrap,
@@ -287,7 +287,7 @@ public class MassDlssDeployDialog
 
         var progressDialog = new ContentDialog
         {
-            Title = "Deploying...",
+            Title = "Развертывание...",
             Content = progressContainer,
             XamlRoot = _xamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -473,27 +473,27 @@ public class MassDlssDeployDialog
 
         // ── Show results ──
         var report = new System.Text.StringBuilder();
-        if (dlssCount > 0) report.AppendLine($"DLSS SR deployed to {dlssCount} game(s)");
-        if (dlssdCount > 0) report.AppendLine($"DLSS RR deployed to {dlssdCount} game(s)");
-        if (dlssgCount > 0) report.AppendLine($"DLSS FG deployed to {dlssgCount} game(s)");
-        if (slCount > 0) report.AppendLine($"Streamline deployed to {slCount} game(s)");
-        if (srPresetCount > 0) report.AppendLine($"SR Preset applied to {srPresetCount} game(s)");
-        if (rrPresetCount > 0) report.AppendLine($"RR Preset applied to {rrPresetCount} game(s)");
-        if (fgPresetCount > 0) report.AppendLine($"FG Preset applied to {fgPresetCount} game(s)");
-        if (srDriverCount > 0) report.AppendLine($"SR NVIDIA Override set on {srDriverCount} game(s)");
-        if (rrDriverCount > 0) report.AppendLine($"RR NVIDIA Override set on {rrDriverCount} game(s)");
-        if (fgDriverCount > 0) report.AppendLine($"FG NVIDIA Override set on {fgDriverCount} game(s)");
+        if (dlssCount > 0) report.AppendLine($"DLSS SR развернут в игры ({dlssCount})");
+        if (dlssdCount > 0) report.AppendLine($"DLSS RR развернут в игры ({dlssdCount})");
+        if (dlssgCount > 0) report.AppendLine($"DLSS FG развернут в игры ({dlssgCount})");
+        if (slCount > 0) report.AppendLine($"Streamline развернут в игры ({slCount})");
+        if (srPresetCount > 0) report.AppendLine($"Пресет SR применён к играм ({srPresetCount})");
+        if (rrPresetCount > 0) report.AppendLine($"Пресет RR применён к играм ({rrPresetCount})");
+        if (fgPresetCount > 0) report.AppendLine($"Пресет FG применён к играм ({fgPresetCount})");
+        if (srDriverCount > 0) report.AppendLine($"Переопределение SR NVIDIA задано для игр ({srDriverCount})");
+        if (rrDriverCount > 0) report.AppendLine($"Переопределение RR NVIDIA задано для игр ({rrDriverCount})");
+        if (fgDriverCount > 0) report.AppendLine($"Переопределение FG NVIDIA задано для игр ({fgDriverCount})");
         if (autoCreateProfiles && presetService.ProfilesCreatedCount > 0)
-            report.AppendLine($"NVIDIA profiles created: {presetService.ProfilesCreatedCount}");
-        if (skippedAlreadyAtVersion > 0) report.AppendLine($"\nSkipped: {skippedAlreadyAtVersion} (already at selected version)");
-        if (skippedNoComponent > 0) report.AppendLine($"Skipped: {skippedNoComponent} (component not present)");
-        if (presetMissedCount > 0) report.AppendLine($"Presets missed: {presetMissedCount} game(s) (no NVIDIA profile found)");
+            report.AppendLine($"Создано профилей NVIDIA: {presetService.ProfilesCreatedCount}");
+        if (skippedAlreadyAtVersion > 0) report.AppendLine($"\nПропущено: {skippedAlreadyAtVersion} (уже выбранная версия)");
+        if (skippedNoComponent > 0) report.AppendLine($"Пропущено: {skippedNoComponent} (компонент отсутствует)");
+        if (presetMissedCount > 0) report.AppendLine($"Пресеты пропущены: игр — {presetMissedCount} (не найден профиль NVIDIA)");
 
-        if (report.Length == 0) report.Append("No changes made.");
+        if (report.Length == 0) report.Append("Изменений нет.");
 
         var resultDialog = new ContentDialog
         {
-            Title = "Batch Deploy Complete",
+            Title = "Массовое развертывание завершено",
             Content = new TextBlock { Text = report.ToString().TrimEnd(), TextWrapping = TextWrapping.Wrap, FontSize = 12 },
             CloseButtonText = "OK",
             XamlRoot = _xamlRoot,
@@ -512,7 +512,7 @@ public class MassDlssDeployDialog
         // Show progress
         var progressText = new TextBlock
         {
-            Text = "Restoring selected games...",
+            Text = "Восстановление выбранных игр...",
             FontSize = 12,
             Foreground = new SolidColorBrush(Windows.UI.Color.FromArgb(255, 200, 200, 200)),
             TextWrapping = TextWrapping.Wrap,
@@ -533,7 +533,7 @@ public class MassDlssDeployDialog
 
         var progressDialog = new ContentDialog
         {
-            Title = "Restoring...",
+            Title = "Восстановление...",
             Content = progressContainer,
             XamlRoot = _xamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -582,13 +582,13 @@ public class MassDlssDeployDialog
         progressDialog.Hide();
 
         var reportText = new System.Text.StringBuilder();
-        if (restoredCount > 0) reportText.AppendLine($"Restored {restoredCount} game(s) to default DLLs.");
-        if (presetsResetCount > 0) reportText.AppendLine($"Reset presets to Default on {presetsResetCount} game(s).");
-        if (restoredCount == 0 && presetsResetCount == 0) reportText.Append("No games had backups to restore or presets to reset.");
+        if (restoredCount > 0) reportText.AppendLine($"Восстановлено игр на исходные DLL: {restoredCount}.");
+        if (presetsResetCount > 0) reportText.AppendLine($"Сбросить пресеты на «По умолчанию» в играх ({presetsResetCount}).");
+        if (restoredCount == 0 && presetsResetCount == 0) reportText.Append("Нет игр с резервными копиями для восстановления или пресетами для сброса.");
 
         var resultDialog = new ContentDialog
         {
-            Title = "Restore Complete",
+            Title = "Восстановление завершено",
             Content = new TextBlock
             {
                 Text = reportText.ToString().TrimEnd(),
@@ -612,7 +612,7 @@ public class MassDlssDeployDialog
         return false;
     }
 
-    private const string NvidiaOverrideOption = "NVIDIA Override";
+    private const string NvidiaOverrideOption = "Переопределение NVIDIA";
 
     private ComboBox BuildVersionCombo(IReadOnlyList<string> versions, string savedDefault = "", bool includeDriverOverride = false, bool currentDriverOverride = false)
     {
diff --git a/RenoDXCommander/MfgDialog.cs b/RenoDXCommander/MfgDialog.cs
index 8e2fd9e..9e0d410 100644
--- a/RenoDXCommander/MfgDialog.cs
+++ b/RenoDXCommander/MfgDialog.cs
@@ -38,11 +38,11 @@ public static class MfgDialog
             var warningPanel = new StackPanel { Spacing = 12 };
             warningPanel.Children.Add(new TextBlock
             {
-                Text = "Multi Frame Generation (MFG) and Dynamic MFG are only supported on NVIDIA 50 Series GPUs (Blackwell architecture).\n\n" +
-                       "Minimum driver requirements:\n" +
-                       "• MFG (Fixed): Driver 572.16+\n" +
-                       "• DMFG (Dynamic): Driver 595.97+\n\n" +
-                       "These settings will have no effect on 40 Series or older hardware.",
+                Text = "Мульти-генерация кадров (MFG) и динамический MFG поддерживаются только на GPU NVIDIA 50-й серии (архитектура Blackwell).\n\n" +
+                       "Минимальные требования к драйверу:\n" +
+                       "• MFG (фиксированный): драйвер 572.16+\n" +
+                       "• DMFG (динамический): драйвер 595.97+\n\n" +
+                       "Эти настройки не действуют на железе 40-й серии и старше.",
                 TextWrapping = TextWrapping.Wrap,
                 FontSize = 13,
                 Foreground = UIFactory.Brush(ResourceKeys.AccentAmberBrush),
@@ -50,7 +50,7 @@ public static class MfgDialog
 
             var dontShowCheck = new CheckBox
             {
-                Content = "Don't show this warning again",
+                Content = "Больше не показывать это предупреждение",
                 FontSize = 12,
                 Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
             };
@@ -58,10 +58,10 @@ public static class MfgDialog
 
             var warningDialog = new ContentDialog
             {
-                Title = "⚠ Hardware Requirement",
+                Title = "⚠ Требования к железу",
                 Content = warningPanel,
                 PrimaryButtonText = "OK",
-                CloseButtonText = "Cancel",
+                CloseButtonText = "Отмена",
                 XamlRoot = xamlRoot,
                 RequestedTheme = ElementTheme.Dark,
             };
@@ -98,7 +98,7 @@ public static class MfgDialog
         // ── FG Mode ──
         panel.Children.Add(new TextBlock
         {
-            Text = "FG Mode",
+            Text = "Режим FG",
             FontSize = 11,
             FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
@@ -120,13 +120,13 @@ public static class MfgDialog
             CornerRadius = new CornerRadius(6),
         };
         ToolTipService.SetToolTip(modeCombo,
-            "Default = application controls frame generation. Fixed = always generate a fixed multiplier of frames. Dynamic = generate up to a target frame rate.");
+            "По умолчанию — генерацией кадров управляет игра. Фиксированный — всегда создавать заданный множитель кадров. Динамический — создавать кадры до целевой частоты.");
         panel.Children.Add(modeCombo);
 
         // ── Frame Count ──
         panel.Children.Add(new TextBlock
         {
-            Text = "Frame Count",
+            Text = "Количество кадров",
             FontSize = 11,
             FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
@@ -140,13 +140,13 @@ public static class MfgDialog
             CornerRadius = new CornerRadius(6),
         };
         ToolTipService.SetToolTip(countCombo,
-            "Fixed: exact frame multiplier (2x-6x). Dynamic: maximum frames the driver may generate (Up to 3x-6x). 50 Series GPUs only for 3x+.");
+            "Фиксированный: точный множитель кадров (2x–6x). Динамический: максимум кадров, которые может создать драйвер (до 3x–6x). Для 3x+ нужен GPU серии 50.");
         panel.Children.Add(countCombo);
 
         // ── Target Frame Rate ──
         panel.Children.Add(new TextBlock
         {
-            Text = "Target Frame Rate",
+            Text = "Целевая частота кадров",
             FontSize = 11,
             FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
@@ -160,13 +160,13 @@ public static class MfgDialog
             CornerRadius = new CornerRadius(6),
         };
         ToolTipService.SetToolTip(fpsCombo,
-            "The target output frame rate for dynamic frame generation. Only active in Dynamic mode.");
+            "Целевая частота кадров на выходе для динамической генерации кадров. Действует только в динамическом режиме.");
         panel.Children.Add(fpsCombo);
 
         // Inline custom FPS input (shown when "Custom..." is selected)
         var customFpsPanel = new StackPanel { Orientation = Orientation.Horizontal, Spacing = 8, Visibility = Visibility.Collapsed };
         var customFpsBox = new TextBox { PlaceholderText = "20-1000", FontSize = 13, MinWidth = 100 };
-        var customFpsBtn = new Button { Content = "Set", FontSize = 12 };
+        var customFpsBtn = new Button { Content = "Задать", FontSize = 12 };
         customFpsPanel.Children.Add(customFpsBox);
         customFpsPanel.Children.Add(customFpsBtn);
         panel.Children.Add(customFpsPanel);
@@ -478,9 +478,9 @@ public static class MfgDialog
         // ── Show dialog ──
         var dialog = new ContentDialog
         {
-            Title = "Multi Frame Generation",
+            Title = "Мульти-генерация кадров",
             Content = panel,
-            CloseButtonText = "Close",
+            CloseButtonText = "Закрыть",
             XamlRoot = xamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
diff --git a/RenoDXCommander/Models/RenoDXDbUnrealEntry.cs b/RenoDXCommander/Models/RenoDXDbUnrealEntry.cs
index 6dde7e3..eeaff31 100644
--- a/RenoDXCommander/Models/RenoDXDbUnrealEntry.cs
+++ b/RenoDXCommander/Models/RenoDXDbUnrealEntry.cs
@@ -86,9 +86,9 @@ public class RenoDXDbUnrealEntry
 
             return parts[1].ToLowerInvariant() switch
             {
-                "output size"  => "1",
-                "output ratio" => "2",
-                "any size"     => "3",
+                "выходное разрешение"  => "1",
+                "выходной масштаб" => "2",
+                "любой размер"     => "3",
                 _ => null,
             };
         }
diff --git a/RenoDXCommander/PresetPopupHelper.cs b/RenoDXCommander/PresetPopupHelper.cs
index 652bb9c..cd6d550 100644
--- a/RenoDXCommander/PresetPopupHelper.cs
+++ b/RenoDXCommander/PresetPopupHelper.cs
@@ -50,7 +50,7 @@ public static class PresetPopupHelper
 
             var emptyDlg = new ContentDialog
             {
-                Title = "Select ReShade Presets",
+                Title = "Выбрать пресеты ReShade",
                 Content = new StackPanel
                 {
                     Spacing = 8,
@@ -58,7 +58,7 @@ public static class PresetPopupHelper
                     {
                         new TextBlock
                         {
-                            Text = "No preset files found.",
+                            Text = "Файлы пресетов не найдены.",
                             FontSize = 13,
                             Foreground = Brush(ResourceKeys.TextPrimaryBrush),
                         },
@@ -72,8 +72,8 @@ public static class PresetPopupHelper
                         emptyPathLink,
                     },
                 },
-                PrimaryButtonText = "Open Folder",
-                CloseButtonText = "Cancel",
+                PrimaryButtonText = "Открыть папку",
+                CloseButtonText = "Отмена",
                 XamlRoot = xamlRoot,
                 Background = Brush(ResourceKeys.SurfaceOverlayBrush),
                 RequestedTheme = ElementTheme.Dark,
@@ -100,7 +100,7 @@ public static class PresetPopupHelper
             TextWrapping = TextWrapping.Wrap,
             Margin = new Thickness(0, 0, 0, 6),
         };
-        pathLink.Inlines.Add(new Microsoft.UI.Xaml.Documents.Run { Text = "Presets from: " });
+        pathLink.Inlines.Add(new Microsoft.UI.Xaml.Documents.Run { Text = "Пресеты из: " });
         var linkRun = new Microsoft.UI.Xaml.Documents.Hyperlink();
         linkRun.Inlines.Add(new Microsoft.UI.Xaml.Documents.Run { Text = PresetsDir });
         linkRun.Click += (s, e) =>
@@ -136,11 +136,11 @@ public static class PresetPopupHelper
 
         var dlg = new ContentDialog
         {
-            Title = "Select ReShade Presets",
+            Title = "Выбрать пресеты ReShade",
             Content = scrollViewer,
-            PrimaryButtonText = "Deploy",
+            PrimaryButtonText = "Развернуть",
             IsPrimaryButtonEnabled = false,
-            CloseButtonText = "Cancel",
+            CloseButtonText = "Отмена",
             XamlRoot = xamlRoot,
             Background = Brush(ResourceKeys.SurfaceOverlayBrush),
             RequestedTheme = ElementTheme.Dark,
diff --git a/RenoDXCommander/Properties/PublishProfiles/FolderProfile.pubxml.user b/RenoDXCommander/Properties/PublishProfiles/FolderProfile.pubxml.user
index c1e4d7c..14372b8 100644
--- a/RenoDXCommander/Properties/PublishProfiles/FolderProfile.pubxml.user
+++ b/RenoDXCommander/Properties/PublishProfiles/FolderProfile.pubxml.user
@@ -1,4 +1,4 @@
-﻿<?xml version="1.0" encoding="utf-8"?>
+<?xml version="1.0" encoding="utf-8"?>
 <!-- https://go.microsoft.com/fwlink/?LinkID=208121. -->
 <Project>
   <PropertyGroup>
diff --git a/RenoDXCommander/RHI_PatchNotes.md b/RenoDXCommander/RHI_PatchNotes.md
index a27a843..45aa649 100644
--- a/RenoDXCommander/RHI_PatchNotes.md
+++ b/RenoDXCommander/RHI_PatchNotes.md
@@ -1,614 +1,613 @@
 ## v2.7.5
 
-### Bug Fixes
+### Исправления
 
-**Settings**
-- Fixed the app freezing when opening Settings after the PC had been idle or the GPU woke from sleep. NVAPI reads now run on a background thread with a 5-second timeout, so the Settings page always opens immediately.
+**Настройки**
+- Исправлено зависание приложения при открытии настроек, если ПК был неактивен или GPU выходил из сна. Чтения NVAPI теперь идут в фоновом потоке с 5-секундным таймаутом, поэтому страница настроек открывается сразу.
 
 **DLSS5 Feeder**
-- Fixed dgVoodoo2 not being deployed for DX9 games (Gothic II, Diablo, etc.) where the API scan returned an empty result set. DX9 detection now falls back to the primary detected API, so dgVoodoo2 installs correctly on all DX9 games.
-- Fixed the host64\\ folder not being deployed on 32-bit games that aren't detected as DX9 (e.g. Diablo GOG). The host64\\ folder is required for all 32-bit Feeder installs — it no longer depends on DX9 being detected.
-- Fixed LumeniteFX and DLSS5_Feed.fx disappearing from the game folder after restarting RHI. The shader selection was being saved on the UI thread and could be overwritten by a concurrent settings save — it's now written synchronously during the install.
-- Fixed the 64-bit Feeder addon being copied to the game folder on 32-bit games when a specific Feeder version was pinned. Versioned staging only stores `.addon64`, so 32-bit games now always use the AddonPackService which has the correct `.addon32`.
+- Исправлено отсутствие dgVoodoo2 для DX9-игр (Gothic II, Diablo и т.п.), где API-скан возвращал пустой результат. Определение DX9 теперь откатывается к основному обнаруженному API, так что dgVoodoo2 корректно ставится на все DX9-игры.
+- Исправлено отсутствие папки host64\\ на 32-бит играх, не определённых как DX9 (например, Diablo GOG). Папка host64\\ нужна для всех 32-бит установок Feeder — она больше не зависит от того, определён ли DX9.
+- Исправлено исчезновение LumeniteFX и DLSS5_Feed.fx из папки игры после перезапуска RHI. Выбор шейдеров сохранялся в UI-потоке и мог быть перезаписан параллельным сохранением настроек — теперь он записывается синхронно во время установки.
+- Исправлено копирование 64-бит аддона Feeder в папку 32-бит игры при зафиксированной конкретной версии Feeder. Версионное хранилище держит только `.addon64`, поэтому 32-бит игры теперь всегда идут через AddonPackService, у которого правильный `.addon32`.
 
 **Luma**
-- Fixed a crash when clicking "Install Luma" on a game where Luma was originally installed via drag-drop. These games have no download URL, so RHI now shows "Drop a Luma archive onto the card to reinstall." instead of crashing.
+- Исправлен краш при нажатии «Установить Luma» на игре, где Luma изначально был установлен через drag-drop. У таких игр нет ссылки для скачивания, поэтому RHI теперь показывает «Перетащите архив Luma на карточку для переустановки» вместо падения.
 
-### Manifest Updates
+### Обновления манифеста
 
-- Fixed Engine.ini being written to the wrong folder for Clive Barker's Hellraiser: Revival Demo (`Hellraiser` instead of `Hellraiser_Demo`).
+- Исправлена запись Engine.ini в неправильную папку для Clive Barker's Hellraiser: Revival Demo (`Hellraiser` вместо `Hellraiser_Demo`).
 
 ## v2.7.4
 
-### Bug Fixes
+### Исправления
 
-- Fixed `DLSS5_Feed.fx` not deploying when the shader staging file had been deleted — RHI now re-extracts it from the cached Feeder zip automatically. Stale registration entries that were blocking re-extraction are also cleared on startup.
+- Исправлено отсутствие развертывания `DLSS5_Feed.fx`, когда файл хранилища шейдеров был удалён — RHI теперь сам заново извлекает его из кешированного zip Feeder. Устаревшие записи регистрации, блокировавшие повторное извлечение, также чистятся при запуске.
 
-### Manifest Updates
+### Обновления манифеста
 
-- Added engine hint for Mount & Blade II: Bannerlord (Daroya Engine).
+- Добавлена подсказка движка для Mount & Blade II: Bannerlord (Daroya Engine).
 
 ## v2.7.3
 
-### New
+### Новое
 
-- UE-Extended games now show a status icon next to the addon name — a green ✓ for mods marked complete, and 🔨 for mods still in progress.
-- You can now select individual files from your Custom Shaders folder in the shader picker. Files from `%LocalAppData%\RHI\reshade\Custom\Shaders\` and `\Textures\` appear as a "Custom Shaders" section between Recommended and Extra packs, grouped by subfolder. Tick or untick individual files to control exactly what gets deployed. An "Open Custom Folder" button in the Profiles panel opens the folder directly.
-- DXVK has moved to the Extras section, under a new "API Upgrades" sub-header. The install button is always available on eligible games (DX8/9/10). Variant selection (Lilium HDR by default, Development, Stable) and the Lilium preset are now in the DXVK cog alongside the existing present method settings.
+- Играм UE-Extended теперь показывается значок статуса рядом с именем аддона — зелёный ✓ для модов, помеченных завершёнными, и 🔨 для модов в работе.
+- В окне выбора шейдеров теперь можно выбирать отдельные файлы из папки своих шейдеров. Файлы из `%LocalAppData%\RHI\reshade\Custom\Shaders\` и `\Textures\` появляются секцией «Custom Shaders» между рекомендуемыми и дополнительными наборами, сгруппированные по подпапкам. Отмечайте и снимайте отдельные файлы, чтобы точно контролировать, что разворачивается. Кнопка «Открыть свою папку» в панели профилей открывает папку напрямую.
+- DXVK переехал в секцию Extras под новым заголовком «API Upgrades». Кнопка установки всегда доступна на подходящих играх (DX8/9/10). Выбор варианта (Lilium HDR по умолчанию, Development, Stable) и пресет Lilium теперь в шестерёнке DXVK рядом с существующими настройками способа вывода.
 
 ### Neural Rendering
 
-- Feeder and Bridge version selection — you can now pin a specific release version of the Feeder or Bridge addon instead of always using the latest. The dropdown shows the full release history (28+ versions including betas).
-- ShortFuse (DLSS Tool) is now available on all 64-bit games except OpenGL — previously it only showed on games with native DLSS.
-- Fixed Feeder installing the wrong dgVoodoo2 file on 64-bit DX9 games — the 32-bit version was always used, so dgVoodoo2 did nothing and the shader failed to compile. Reinstall Feeder on any affected game to fix it.
-- Fixed global shaders not being removed from the game folder immediately when Feeder is installed.
-- Fixed `DLSS5_Feed.fx` not deploying to the game folder on install.
-- Fixed the Neural Rendering panel not building on some sessions.
+- Выбор версии Feeder и Bridge — теперь можно зафиксировать конкретную версию релиза аддона Feeder или Bridge вместо постоянного использования последней. В списке вся история релизов (28+ версий, включая беты).
+- ShortFuse (DLSS Tool) теперь доступен на всех 64-бит играх, кроме OpenGL — раньше показывался только на играх с нативным DLSS.
+- Исправлена установка Feeder'ом неверного файла dgVoodoo2 на 64-бит DX9-играх — всегда бралась 32-бит версия, из-за чего dgVoodoo2 ничего не делал, а шейдер не компилировался. Переустановите Feeder на затронутых играх.
+- Исправлено отсутствие немедленного удаления глобальных шейдеров из папки игры при установке Feeder.
+- Исправлено отсутствие развертывания `DLSS5_Feed.fx` в папку игры при установке.
+- Исправлено отсутствие сборки панели Neural Rendering в некоторых сессиях.
 
-### Game Detection
+### Определение игр
 
-- Unity games now correctly report their API based on Unity's own configuration file, rather than PE import scanning (which reads the Unity player DLL and sees every API). This fixes games like Caves of Qud showing DX12 instead of their actual runtime API.
-- Games with a trademark symbol in their name (®, ™) now correctly match against RHI database entries — Borderlands® 4 and similar were not being found.
-- Unreal Legacy (UE1/2/3) games that have a DX11 compatibility shim in their imports now correctly show DX9 as their primary API.
-- PCGamingWiki API detection now reads DX9, DX10, Vulkan, and OpenGL in addition to DX11/DX12. Games where PE scanning returns no result will now use PCGW data as the source of truth.
+- Игры Unity теперь корректно сообщают API по собственному конфигу Unity, а не по PE-анализу импортов (который читает Unity player DLL и видит все API подряд). Это чинит игры вроде Caves of Qud, показывавшие DX12 вместо фактического API времени выполнения.
+- Игры с торговым знаком в названии (®, ™) теперь корректно сопоставляются с записями базы RHI — Borderlands® 4 и подобные раньше не находились.
+- Игры Unreal Legacy (UE1/2/3), у которых в импортах есть DX11-шим, теперь корректно показывают DX9 как основной API.
+- Определение через API PCGamingWiki теперь читает DX9, DX10, Vulkan и OpenGL в дополнение к DX11/DX12. Игры, где PE-скан не даёт результата, теперь берут данные PCGW как источник истины.
 
-### Bug Fixes
+### Исправления
 
-**Crashes and freezes**
-- Fixed intermittent UI freezes affecting installs, Nexus sign-in, drag-drop, Update All, Luma installs, and app launch with `--launch`.
-- Fixed a slow memory and connection leak that built up over a long session.
-- Fixed a leak where event handlers accumulated every time a game card was opened.
+**Краши и зависания**
+- Исправлены периодические зависания UI, затрагивавшие установки, вход в Nexus, drag-drop, «Обновить всё», установки Luma и запуск приложения с `--launch`.
+- Исправлена медленная утечка памяти и соединений, накапливавшаяся за долгую сессию.
+- Исправлена утечка, при которой обработчики событий накапливались при каждом открытии карточки игры.
 
 **Luma**
-- Fixed Luma uninstall removing the shader folder even when ReShade was still installed — shaders are now kept and redeployed.
-- Fixed Luma uninstall leaving a `reshade-shaders-original` folder behind.
-- Fixed Luma uninstall leaving `reshade.ini` in a Luma-configured state — a fresh copy is now deployed with your hotkeys and settings intact.
-- Fixed Luma uninstall leaving `nvngx_dlss.dll` behind.
-- Fixed Luma mods on Nexus showing "Update Available" repeatedly — this was a false positive from comparing page edit timestamps rather than actual file releases.
+- Исправлено удаление папки шейдеров при деинсталляции Luma даже когда ReShade оставался установлен — шейдеры теперь сохраняются и разворачиваются заново.
+- Исправлено оставление папки `reshade-shaders-original` после деинсталляции Luma.
+- Исправлено оставление `reshade.ini` в сконфигурированном под Luma виде — теперь разворачивается свежая копия с вашими горячими клавишами и настройками.
+- Исправлено оставление `nvngx_dlss.dll` после деинсталляции Luma.
+- Исправлено постоянное «Доступно обновление» для модов Luma на Nexus — это был ложный результат сравнения времени правки страницы вместо фактических релизов файлов.
 
 **Game Pass**
-- Fixed ReShade being lost after a Game Pass game updates — Windows replaces the install folder with a new versioned path on update. RHI now detects this and reinstalls ReShade automatically on the next launch.
+- Исправлена потеря ReShade после обновления игры Game Pass — Windows заменяет папку установки новым версионным путём. RHI теперь замечает это и переустанавливает ReShade автоматически при следующем запуске.
 
-**Other**
-- Fixed PCGamingWiki links never appearing on game cards.
-- Fixed the render scale input box keeping keyboard focus after pressing Enter.
-- Fixed occasional incorrect DLSS scan counts when multiple games were scanned at the same time.
-- Fixed file cleanup silently stopping partway through if a file was locked by another process.
-- Window position and size are now saved whenever you finish moving or resizing the window, not only on a clean close — so your layout is preserved even if RHI is force-closed or restarted by an update.
-- The version number in the status bar is now clickable and checks for app updates.
+**Прочее**
+- Исправлено отсутствие ссылок PCGamingWiki на карточках игр.
+- Исправлено сохранение фокуса клавиатуры в поле масштаба рендеринга после Enter.
+- Исправлены периодически неверные счётчики DLSS-скана при одновременном сканировании нескольких игр.
+- Исправлена тихая остановка очистки файлов на середине, если файл был занят другим процессом.
+- Положение и размер окна теперь сохраняются при каждом завершении перемещения или изменения размера, а не только при чистом закрытии — макет сохранится даже при принудительном закрытии RHI или перезапуске обновлением.
+- Номер версии в строке состояния теперь кликабелен и проверяет обновления приложения.
 
-### Manifest Updates
+### Обновления манифеста
 
-- Added "Banishers: Ghosts of New Eden - The Wanderer Set DLC" to blacklist — was being incorrectly detected as a game.
-- Removed Elden Ring and Elden Ring: Nightreign from the external-only list — both mods now have direct download links in RHI.
-- Added engine hint for Insurgency: Sandstorm.
-- Added DX9 API override for Outlast and Outlast 2 — both were showing DX11 due to a Unreal Legacy PE import shim.
-- Updated engine hint for MGS4 and Peace Walker (Master Collection) to KojiPro Engine.
+- «Banishers: Ghosts of New Eden - The Wanderer Set DLC» добавлен в чёрный список — ошибочно определялся как игра.
+- Elden Ring и Elden Ring: Nightreign убраны из списка «только внешняя установка» — у обоих модов теперь прямые ссылки в RHI.
+- Добавлена подсказка движка для Insurgency: Sandstorm.
+- Добавлено переопределение API DX9 для Outlast и Outlast 2 — оба показывали DX11 из-за Unreal Legacy PE-шима.
+- Подсказка движка для MGS4 и Peace Walker (Master Collection) обновлена на KojiPro Engine.
 
-### Diagnostic Logging
+### Диагностическое логирование
 
-This build includes enhanced session logging to help track down a UI freeze that occurs intermittently. Every major UI rendering operation logs timing and state to the session log. If the app freezes, close it and share the log from `%LocalAppData%\RHI\Logs\` — it will help narrow down the cause.
+В этой сборке усилено логирование сессий для отслеживания периодического зависания UI. Каждая крупная операция отрисовки UI пишет тайминги и состояние в лог сессии. Если приложение зависнет, закройте его и пришлите лог из `%LocalAppData%\RHI\Logs\` — это поможет сузить причину.
 
 ## v2.7.2
 
-### New
+### Новое
 
-- **RHI Database is now the default mod data source** — RHI now uses its own community database instead of scraping the RenoDX wiki. The database is easier to maintain, more reliable, and gives RHI full control over the data without depending on third-party infrastructure. There is also a security benefit: the RenoDX wiki is publicly editable, and with RenoDX's growing userbase and by extension RHI's there is a real risk that someone could upload a malicious file disguised as a legitimate HDR mod — RHI would have no way to prevent it from being delivered to users. The RHI database is controlled and reviewed before any changes go live. Big thanks to Scrungus for the work transferring all wiki mods and details across to the new database. You can still switch back to the RenoDX Wiki scraper in Settings if needed.
+- **База данных RHI теперь основной источник данных о модах** — RHI использует собственную базу сообщества вместо парсинга вики RenoDX. Базу проще поддерживать, она надёжнее и даёт RHI полный контроль над данными без зависимости от сторонней инфраструктуры. Есть и плюс к безопасности: вики RenoDX публично редактируема, и с ростом аудитории RenoDX, а значит и RHI, есть реальный риск, что кто-то зальёт вредоносный файл под видом легитимного HDR-мода — RHI не смог бы этому помешать. База RHI контролируется, и изменения проходят проверку до публикации. Огромное спасибо Scrungus за перенос всех модов и деталей из вики в новую базу. При необходимости можно переключиться обратно на парсер вики RenoDX в настройках.
 
-### Bug Fixes
+### Исправления
 
-- Fixed UI freezing when navigating between games — RHI was doing filesystem I/O (directory scans for the AppData button) on the UI thread on every card selection. This is now pre-computed in the background at startup and cached per game.
-- Fixed UI becoming permanently unresponsive after clicking Install on Luma (and potentially other components) — shader pack settings read/write operations were using a synchronous lock that could block the UI thread when background download tasks held the lock concurrently.
-- Fixed DXVK uninstall blocking the UI thread during the ReShade mode switch that follows it.
-- Fixed the shader pack picker briefly freezing the UI when opened during an active shader pack download.
-- Fixed Settings page driver combo changes (Shader Cache, G-Sync, FPS Limit, ReBAR, VSync, Power Mode, etc.) causing brief UI hitches — all NVAPI profile writes are now dispatched to a background thread.
-- Fixed per-game NVIDIA driver setting combos (VSync, Low Latency, Smooth Motion, Power Mode, G-Sync, ReBAR) in the Game Overrides panel causing brief UI hitches on every change.
-- Fixed DLSS preset combos, render scale, and driver override toggles in the NVIDIA Profile section causing brief UI hitches on every change.
-- Fixed RTX HDR toggle and Configure RTX HDR apply button blocking the UI thread on NVAPI writes.
-- Fixed MFG dialog (FG Mode, Generation Factor, Target FPS) blocking the UI thread on NVAPI writes.
-- Fixed drag-dropping a `.addon` file (some Luma mods use this extension) showing "No Addon Found" — `.addon` files are now accepted alongside `.addon64` and `.addon32`. Luma-named addon files are now correctly routed through the Luma install flow (with ReShade deploy, dgVoodoo2, shaders, etc.) instead of the RenoDX addon flow. The install picker now shows "🌙 Install Luma Addon" and uses fuzzy filename matching to pre-select the correct game.
-- Fixed ReShade installing as `dxgi.dll` on Borderlands 2 and Borderlands: The Pre-Sequel — these are DX9 games that should use `d3d9.dll`. The `dxgi.dll` override was intended for Luma+dgVoodoo installs but was incorrectly applied to plain ReShade installs as well.
-- Fixed ReShade remaining as `dxgi.dll` after uninstalling Luma on DX9+dgVoodoo games — it now reinstalls automatically with the correct `d3d9.dll` filename.
-- Fixed dgVoodoo2 not deploying for DX9 games when installing DLSS5 Feeder — it was incorrectly gated on the Luma dgVoodoo manifest list, which only covers Luma-specific games. dgVoodoo2 is now deployed for all DX9 Feeder installs.
-- Fixed DLSS5 Feeder installing ReShade as `d3d9.dll` on DX9 games — dgVoodoo2 owns `d3d9.dll` on those games, so ReShade must be `dxgi.dll`. Existing wrong installs are corrected automatically on re-install.
-- Fixed background scan renaming ReShade from `dxgi.dll` back to `ReShade32.dll` on DX9+dgVoodoo Feeder games — the reconciliation logic now correctly skips games where dgVoodoo2 is active.
-- Fixed auto-update installing ReShade as `d3d9.dll` on DX9+dgVoodoo Feeder games — same fix as above applied to the Update All ReShade path.
+- Исправлено зависание UI при переключении между играми — RHI выполнял файловый ввод-вывод (сканирование папок для кнопки AppData) в UI-потоке при каждом выборе карточки. Теперь это считается заранее в фоне при запуске и кешируется по игре.
+- Исправлено полное зависание UI после нажатия «Установить» на Luma (и потенциально других компонентах) — операции чтения/записи настроек наборов шейдеров использовали синхронную блокировку, которая могла блокировать UI-поток, пока фоновые задачи скачивания держали её параллельно.
+- Исправлена блокировка UI-потока удалением DXVK во время следующей за ним смены режима ReShade.
+- Исправлено кратковременное замерзание UI при открытии окна выбора наборов шейдеров во время активного скачивания набора.
+- Исправлены краткие подёргивания UI при изменении комбинаций драйвера на странице настроек (кеш шейдеров, G-Sync, лимит FPS, ReBAR, VSync, режим питания и т.д.) — все записи профилей NVAPI теперь уходят в фоновый поток.
+- Исправлены краткие подёргивания UI при каждом изменении комбинаций настроек драйвера NVIDIA (VSync, Low Latency, Smooth Motion, режим питания, G-Sync, ReBAR) в панели переопределений игры.
+- Исправлены краткие подёргивания UI при каждом изменении пресетов DLSS, масштаба рендеринга и переключателей переопределений драйвера в секции профиля NVIDIA.
+- Исправлена блокировка UI-потока при записи NVAPI переключателем RTX HDR и кнопкой применения «Настроить RTX HDR».
+- Исправлена блокировка UI-потока при записи NVAPI в диалоге MFG (режим FG, фактор генерации, целевой FPS).
+- Исправлено «Аддон не найден» при перетаскивании файла `.addon` (некоторые моды Luma используют это расширение) — файлы `.addon` теперь принимаются наравне с `.addon64` и `.addon32`. Аддон-файлы с именем Luma корректно направляются в поток установки Luma (с развертыванием ReShade, dgVoodoo2, шейдеров и т.д.) вместо потока аддонов RenoDX. Окно установки теперь показывает «🌙 Установить аддон Luma» и использует нечёткое сопоставление имён, чтобы заранее выбрать правильную игру.
+- Исправлена установка ReShade как `dxgi.dll` в Borderlands 2 и Borderlands: The Pre-Sequel — это DX9-игры, которым нужен `d3d9.dll`. Переопределение `dxgi.dll` предназначалось для установок Luma+dgVoodoo, но ошибочно применялось и к обычным установкам ReShade.
+- Исправлено оставание ReShade как `dxgi.dll` после удаления Luma на играх DX9+dgVoodoo — теперь он автоматически переустанавливается с правильным именем `d3d9.dll`.
+- Исправлено отсутствие dgVoodoo2 для DX9-игр при установке DLSS5 Feeder — установщик ошибочно сверялся со списком dgVoodoo-игр Luma в манифесте, который покрывает только специфичные для Luma игры. Теперь dgVoodoo2 разворачивается для всех DX9-установок Feeder.
+- Исправлена установка DLSS5 Feeder'ом ReShade как `d3d9.dll` на DX9-играх — на них `d3d9.dll` занят dgVoodoo2, поэтому ReShade должен быть `dxgi.dll`. Существующие неверные установки исправляются автоматически при переустановке.
+- Исправлено переименование фоновым сканом ReShade с `dxgi.dll` обратно в `ReShade32.dll` на играх DX9+dgVoodoo с Feeder — логика сверки теперь корректно пропускает игры с активным dgVoodoo2.
+- Исправлена установка ReShade как `d3d9.dll` при автообновлении на играх DX9+dgVoodoo с Feeder — тот же фикс применён к пути «Обновить всё» для ReShade.
 
-### Manifest Updates
+### Обновления манифеста
 
-- Updated UltraShade (formerly Ultra ReShade by Ultra+) to the new repo URL and display name.
+- UltraShade (ранее Ultra ReShade от Ultra+) обновлён до нового URL репозитория и отображаемого имени.
 
 ## v2.7.1
 
-### New
-
-- **Export Game Data** — new button in Settings next to Copy Logs. Gathers your game library data (graphics API, exe paths, engine, store IDs) and copies a zip to clipboard. Paste into Discord to share with the community — submissions help RHI detect APIs and install paths correctly for more games.
-- **GitHub API token support** — if you're hitting rate limits (OptiScaler NR staging not available, version checks not completing), create `%LocalAppData%\RHI\github_api.txt` and paste a GitHub personal access token on a single line. No scopes needed — a free token for public repos takes about 30 seconds to generate at github.com/settings/tokens. Raises the limit from 60 to 5000 API calls per hour.
-- **Nexus Mods support for Luma** — Luma mods hosted on Nexus Mods (e.g. Mass Effect, Medal of Honor: Airborne, Borderlands 2) now show a "Get on Nexus Mods" button that takes you directly to the download page. Mods with both a GitHub and Nexus link (e.g. Prey, BioShock Remastered) now show both options. Full one-click Nexus install is wired but pending Nexus API approval.
-- **dgVoodoo2 auto-detection** — RHI now automatically detects when a Luma mod requires dgVoodoo2 (for DX9 games like Mass Effect, Borderlands 2, Medal of Honor: Airborne) by reading the Luma wiki's Special Notes column, including which specific dgVoodoo2 version the mod recommends. No longer relies on a hardcoded list.
-
-### Changes
-
-- **PCGamingWiki API detection** — RHI now uses PCGamingWiki in the background to verify DirectX versions for games it can't scan directly (Xbox/Game Pass titles, access-denied paths). Halo Infinite, Resonance, and similar titles that previously showed no API badge now correctly show DX12.
-- **PCGamingWiki config path detection** — RHI now scrapes the config file location from PCGamingWiki and uses it to place Engine.ini in the correct folder. Fixes UE-Extended installs for games that haven't been launched yet, where the config folder doesn't exist yet and RHI previously had to guess the project name.
-- **Neural Rendering: Feeder and Bridge now support addon version pinning** — the Addon Version dropdown is now active for all four NR methods.
-- **OptiScaler cog Upscaler API defaults to the game's detected API** — opens on DX12 for DX12 games instead of always defaulting to DX11.
-- **More reliable OptiScaler and Neural Rendering install records** — RHI now writes a `rhi_install.txt` file to the game folder on every OptiScaler and Neural Rendering install, recording the exact version, variant, and file list that was deployed. This is used on the next launch to show the correct version (fixes nightly builds reverting to stable version numbers on restart), and on uninstall to know exactly which files to clean up regardless of whether the staging folder has since been updated.
-
-### Bug Fixes
-
-- Fixed OptiScaler uninstall leaving DLSS files behind when a NR method (DLSS5 Tool, ShortFuse, Feeder) was also installed — RHI now tracks which component owns each shared file and only removes a file when the last component that deployed it is uninstalled.
-- Fixed `nvngx_dlss*.dll` files left in the game root (e.g. Mortal Shell II) after uninstalling DLSS5 Tool, when the game's actual DLSS lived in a plugin subfolder.
-- Fixed OptiScaler DLL naming override not correctly tracking the renamed DLL, causing uninstall to leave files behind.
-- Fixed MFG Ada Unlock being reinstalled on every Refresh after removing it via the Extras section.
-- Fixed batch DLSS/Streamline deploy getting stuck when a game's install folder no longer exists.
-- Fixed DLSS5 DX11 Bridge and MFG Ada Unlock re-downloading on every launch.
-- Fixed shader pack folder occasionally being renamed to `reshade-shaders-original` during Refresh.
-- Fixed `renodx-dlss5.addon64` reappearing in the addon picker after removing Neural Rendering.
-- Fixed RHI database (rhi-repo) changes not picking up after a standard Refresh — previously required a full restart.
-- Fixed the detail panel not refreshing for the currently selected game after a Refresh.
-- Fixed UE-Extended game notes from the rhi-repo database not appearing in the RenoDX info dialog.
-- Fixed pressing Backspace in the ReShade screenshot hotkey box being recorded as a hotkey — Backspace now clears the shortcut instead (matching ReShade's own behaviour). Thanks to @tzachbon for the contribution.
-- Fixed "Apply to All Games" in Screenshots & Hotkeys doing nothing when the screenshot path is blank — hotkeys and effect list style now apply regardless. Thanks to @tzachbon for the contribution.
-- Fixed UI freezing for a few seconds during OptiScaler downloads — progress and status message updates no longer trigger a full detail panel rebuild. Thanks to @Nypheena for the contribution.
-- Fixed an empty `reshade-shaders-original` folder being created on clean ReShade installs — on a fresh game folder with no prior `reshade-shaders`, RHI was accidentally creating the folder and immediately renaming it as a backup. On uninstall this empty folder would be restored as `reshade-shaders`, leaving just the management marker with no shaders.
-- Fixed Luma installs via drag-drop or the downloads watcher missing several post-install steps — ReShade, DLSS, dgVoodoo2, Engine.ini keys, and launch arguments are now all applied correctly regardless of how the Luma archive is installed.
-- Fixed ReShade being deployed as `d3d9.dll` on DX9 games when dgVoodoo2 is also being installed — dgVoodoo2 needs `d3d9.dll` to intercept the game's DX9 calls, so ReShade now correctly installs as `dxgi.dll` instead, hooking dgVoodoo2's DX11 output.
-
-### Manifest Updates
-
-- Fixed Engine.ini being written to the wrong AppData folder for **Solasta 2** (was using the `Brimstone` exe subfolder name instead of the game name).
-- Added **ReShade Screenshot Discord Fix** to the addon picker — strips the cICP colour chunk from HDR screenshots so Discord previews them correctly instead of showing washed-out colours.
-- Kingdom Come: Deliverance II added to the 64-bit override list.
-- Added **Ultra ReShade by Ultra+** to the shader picker — a single-shader "poor man's DLSS5" effect with bloom, contrast, haze/dehaze and saturation.
-- Five Hearts Under One Roof added to the 64-bit override list.
-- Mass Effect (2007) install path corrected to the `Binaries` subfolder.
-- 007 First Light install path corrected to the `Retail` subfolder.
+### Новое
+
+- **Экспорт данных игр** — новая кнопка в настройках рядом с «Скопировать логи». Собирает данные вашей библиотеки (графический API, пути exe, движок, ID магазинов) и кладёт zip в буфер обмена. Вставьте в Discord, чтобы поделиться с сообществом — такие заявки помогают RHI правильно определять API и пути установки для большего числа игр.
+- **Поддержка токена GitHub API** — если вы упираетесь в лимиты (не доступно хранилище OptiScaler NR, не завершаются проверки версий), создайте `%LocalAppData%\RHI\github_api.txt` и вставьте персональный токен GitHub одной строкой. Права (scopes) не нужны — бесплатный токен для публичных репозиториев генерируется за ~30 секунд на github.com/settings/tokens. Поднимает лимит с 60 до 5000 вызовов API в час.
+- **Поддержка Nexus Mods для Luma** — моды Luma, размещённые на Nexus Mods (например, Mass Effect, Medal of Honor: Airborne, Borderlands 2), теперь показывают кнопку «Забрать на Nexus Mods», ведущую прямо на страницу загрузки. Для модов с обеими ссылками GitHub и Nexus (например, Prey, BioShock Remastered) показываются оба варианта. Полная установка с Nexus в один клик подключена, но ждёт одобрения Nexus API.
+- **Автоопределение dgVoodoo2** — RHI теперь сам определяет по колонке Special Notes в вики Luma, когда мод Luma требует dgVoodoo2 (для DX9-игр вроде Mass Effect, Borderlands 2, Medal of Honor: Airborne), включая конкретную рекомендованную версию dgVoodoo2. Захардкоженный список больше не используется.
+
+### Изменения
+
+- **Определение через API PCGamingWiki** — RHI теперь использует PCGamingWiki в фоне, чтобы проверить версии DirectX для игр, которые нельзя просканировать напрямую (тайтлы Xbox/Game Pass, пути с отказом в доступе). Halo Infinite, Resonance и подобные, раньше не показывавшие значок API, теперь корректно показывают DX12.
+- **Определение пути конфига через PCGamingWiki** — RHI теперь вытягивает с PCGamingWiki расположение файла конфигурации и размещает Engine.ini в правильной папке. Чинит установки UE-Extended для ещё не запускавшихся игр, где папки конфига ещё нет и RHI раньше вынужден был угадывать имя проекта.
+- **Neural Rendering: Feeder и Bridge поддерживают фиксацию версии аддона** — список «Версия аддона» теперь активен для всех четырёх методов NR.
+- **API апскейлера в шестерёнке OptiScaler по умолчанию = определённый API игры** — открывается на DX12 для DX12-игр вместо постоянного DX11.
+- **Более надёжные записи об установке OptiScaler и Neural Rendering** — RHI теперь пишет файл `rhi_install.txt` в папку игры при каждой установке OptiScaler и Neural Rendering, фиксируя точные версию, вариант и список развёрнутых файлов. Это используется при следующем запуске, чтобы показать правильную версию (чинит откат ночных сборок к стабильным номерам после перезапуска), и при удалении — чтобы точно знать, какие файлы чистить, независимо от того, обновилось ли хранилище с момента установки.
+
+### Исправления
+
+- Исправлено оставление файлов DLSS после удаления OptiScaler, когда был установлен и NR-метод (DLSS5 Tool, ShortFuse, Feeder) — RHI теперь отслеживает, какой компонент владеет каждым общим файлом, и удаляет файл только когда удалён последний развернувший его компонент.
+- Исправлено оставание файлов `nvngx_dlss*.dll` в корне игры (например, Mortal Shell II) после удаления DLSS5 Tool, когда фактический DLSS игры лежал в подпапке плагина.
+- Исправлено некорректное отслеживание переименованной DLL при переопределении имени OptiScaler.dll, из-за чего удаление оставляло файлы.
+- Исправлена переустановка MFG Ada Unlock при каждом «Обновить» после его удаления через секцию Extras.
+- Исправлено зависание массового развертывания DLSS/Streamline, когда папка установки игры больше не существует.
+- Исправлено повторное скачивание DLSS5 DX11 Bridge и MFG Ada Unlock при каждом запуске.
+- Исправлено периодическое переименование папки набора шейдеров в `reshade-shaders-original` во время «Обновить».
+- Исправлено повторное появление `renodx-dlss5.addon64` в окне выбора аддонов после удаления Neural Rendering.
+- Исправлено отсутствие подхвата изменений базы RHI (rhi-repo) после обычного «Обновить» — раньше требовался полный перезапуск.
+- Исправлено необновление панели подробностей для текущей выбранной игры после «Обновить».
+- Исправлено отсутствие заметок по играм UE-Extended из базы rhi-repo в диалоге информации RenoDX.
+- Исправлена запись Backspace в поле горячей клавиши скриншотов ReShade как самой клавиши — теперь Backspace очищает сочетание (как в самом ReShade). Спасибо @tzachbon за вклад.
+- Исправлено бездействие «Применить ко всем играм» в «Скриншоты и горячие клавиши» при пустом пути скриншотов — горячие клавиши и стиль списка эффектов теперь применяются в любом случае. Спасибо @tzachbon за вклад.
+- Исправлено зависание UI на несколько секунд при скачиваниях OptiScaler — обновления прогресса и статуса больше не пересобирают всю панель подробностей. Спасибо @Nypheena за вклад.
+- Исправлено создание пустой папки `reshade-shaders-original` при чистых установках ReShade — в свежей папке игры без предыдущего `reshade-shaders` RHI случайно создавал папку и тут же переименовывал её как резервную копию. При удалении эта пустая папка восстанавливалась как `reshade-shaders`, оставляя один маркер управления без шейдеров.
+- Исправлено пропускание нескольких пост-шагов при установке Luma через drag-drop или наблюдатель загрузок — ReShade, DLSS, dgVoodoo2, ключи Engine.ini и аргументы запуска теперь применяются правильно независимо от способа установки архива Luma.
+- Исправлено развертывание ReShade как `d3d9.dll` на DX9-играх, когда вместе ставится dgVoodoo2 — dgVoodoo2 нужен `d3d9.dll`, чтобы перехватывать DX9-вызовы игры, поэтому ReShade теперь корректно ставится как `dxgi.dll`, перехватывая DX11-вывод dgVoodoo2.
+
+### Обновления манифеста
+
+- Исправлена запись Engine.ini в неправильную папку AppData для **Solasta 2** (использовалось имя подпапки exe `Brimstone` вместо названия игры).
+- В окно выбора аддонов добавлен **ReShade Screenshot Discord Fix** — вырезает цветовой чанк cICP из HDR-скриншотов, чтобы Discord показывал превью корректно, а не с выцветшими цветами.
+- Kingdom Come: Deliverance II добавлен в список переопределения 64-бит.
+- В окно выбора шейдеров добавлен **Ultra ReShade от Ultra+** — одношейдерный «DLSS5 для бедных» с блумом, контрастом, дымкой/антидымкой и насыщенностью.
+- Five Hearts Under One Roof добавлен в список переопределения 64-бит.
+- Путь установки Mass Effect (2007) исправлен на подпапку `Binaries`.
+- Путь установки 007 First Light исправлен на подпапку `Retail`.
 
 ---
 
 ## v2.7.0
 
-### New
+### Новое
 
-- **OptiScaler DLSS NR variant** — new third option in the OptiScaler version picker alongside Stable and Nightly. Uses a community fork with DLSS 5 Neural Rendering built in. Same features as Nightly (Streamline, FG settings, presets, etc.) plus a new Neural Rendering Settings section in the cog to tune NR behaviour without touching config files. Switch between variants in the OptiScaler cog in the Extras section.
-- **DLSS5 Feeder on 32-bit DX9 games** — the DLSS5 Feeder method in Neural Rendering now fully supports 32-bit DX9 games (Borderlands 2, The Witcher 2, and much more). RHI handles all the setup automatically — still one click.
-- **Luma support for DX9 games** — Borderlands 2, Borderlands: The Pre-Sequel, The Witcher 2, Medal of Honor: Airborne, and Vanquish now work with Luma. RHI automatically handles the DX9 compatibility layer alongside the Luma install. Still one click.
-- **Neural Rendering addon version picker** — the Neural Rendering section now has an Addon Version dropdown. You can pin a game to a specific version of DLSS5 Tool or DLSS Tool (ShortFuse) before installing — useful if a newer release causes issues with a particular game. Defaults to Latest and auto-updates as normal. The picker greys out when Neural Rendering is already installed; uninstall first to switch.
-- **ReShade HDR Metadata** — new addon in the picker. Writes HDR metadata into the swapchain alongside ReShade, fixing washed-out or badly tone-mapped HDR output on TVs and monitors that rely on it to calibrate their HDR pipeline. Works with any DX11/DX12 game running ReShade. No configuration needed.
+- **Вариант OptiScaler DLSS NR** — третий вариант в списке версий OptiScaler рядом со Stable и Nightly. Использует форк сообщества со встроенным DLSS 5 Neural Rendering. Возможности Nightly (Streamline, настройки FG, пресеты и т.д.) плюс новая секция настроек Neural Rendering в шестерёнке для тонкой настройки NR без правки конфигов. Переключение вариантов — в шестерёнке OptiScaler в секции Extras.
+- **DLSS5 Feeder на 32-бит DX9-играх** — метод DLSS5 Feeder в Neural Rendering теперь полностью поддерживает 32-бит DX9-игры (Borderlands 2, The Witcher 2 и ещё много чего). RHI делает всю настройку сам — по-прежнему один клик.
+- **Поддержка Luma для DX9-игр** — Borderlands 2, Borderlands: The Pre-Sequel, The Witcher 2, Medal of Honor: Airborne и Vanquish теперь работают с Luma. RHI автоматически сопровождает установку Luma DX9-слоем совместимости. По-прежнему один клик.
+- **Выбор версии аддона Neural Rendering** — в секции Neural Rendering появился список «Версия аддона». Можно зафиксировать игру на конкретной версии DLSS5 Tool или DLSS Tool (ShortFuse) до установки — полезно, если свежий релиз ломает конкретную игру. По умолчанию Latest с обычным автообновлением. Список грейдается, когда Neural Rendering уже установлен; сначала удалите, потом переключайте.
+- **ReShade HDR Metadata** — новый аддон в списке. Записывает HDR-метаданные в свопчейн вместе с ReShade, чиня выцветший или плохо тонмапнутый HDR-вывод на ТВ и мониторах, которые полагаются на них для калибровки HDR-конвейера. Работает с любой DX11/DX12-игрой с ReShade. Настройка не нужна.
 
-### Bug Fixes
+### Исправления
 
-- Fixed Lilium HDR Shaders (and other shader packs) not picking up new releases — updates were silently skipped if the pack was already on disk, even when a newer version was available.
-- Fixed MFG Ada Unlock install button being permanently greyed out on fresh installs — it now downloads on demand when you click Install.
-- Fixed games with manifest install path overrides (e.g. The Witcher 2) showing the wrong install path and missing mod status on first launch.
-- Fixed ReBAR Size Limit writing and reading incorrect values — values set in RHI now show correctly in NVPI, and values set in NVPI are correctly read back by RHI.
-- Fixed Output Colour Settings applying to the wrong monitor on multi-monitor setups.
+- Исправлено отсутствие подхвата новых релизов Lilium HDR Shaders (и других наборов шейдеров) — обновления тихо пропускались, если набор уже был на диске, даже когда доступна более новая версия.
+- Исправлена перманентная серость кнопки установки MFG Ada Unlock на свежих установках — теперь скачивается по требованию при нажатии «Установить».
+- Исправлены неверный путь установки и потеря статуса модов при первом запуске у игр с переопределением пути из манифеста (например, The Witcher 2).
+- Исправлена запись и чтение неверных значений лимитом размера ReBAR — значения, заданные в RHI, теперь правильно показываются в NVPI, а заданные в NVPI правильно читаются RHI.
+- Исправлено применение Output Colour Settings к не тому монитору в мультимониторных конфигурациях.
 
 ---
 
 ## v2.6.9
 
-### New
+### Новое
 
-- **RTX 40 MFG Unlock v1.3** — updated to the new standalone format. No longer requires ASI Loader — it's a single DLL that you deploy under any proxy name the game loads (version.dll, dinput8.dll, etc.). RHI shows a name picker on install, and automatically cleans up any old ASI-based install on first launch.
-- **20/30 FG Unlock** — new entry in the MFG Unlocks section. Brings DLSS Frame Generation to RTX 20 and 30 series GPUs (D3D12 games only, no ReShade or ASI Loader required). Pick your GPU generation (RTX 30 or RTX 20) in the cog before installing — RHI handles the rest and keeps it updated automatically.
-- **Resolution & Colour Control** — automatically switch to a target resolution when a game launches and restore it on exit. Also includes Output Colour Settings to control colour depth and HDR dynamic range per-display, without touching NVIDIA Control Panel.
-- **Standalone DLSS Enabler** — new row in the Extras section. Installs DLSS Enabler as a proxy DLL directly into any game folder, independent of OptiScaler. Updates automatically.
-- **MFG Ada Unlock** — new row in the Extras section. Unlocks DLSS Multi Frame Generation (3x/4x+) on RTX 40-series GPUs. Requires ReShade. Mutually exclusive with RTX 40 MFG Unlock.
+- **RTX 40 MFG Unlock v1.3** — обновлён до нового самостоятельного формата. ASI Loader больше не нужен — это одна DLL, которую вы разворачиваете под любым прокси-именем, загружаемым игрой (version.dll, dinput8.dll и т.п.). RHI показывает выбор имени при установке и автоматически чистит старую ASI-установку при первом запуске.
+- **20/30 FG Unlock** — новая запись в секции MFG Unlocks. Приносит генерацию кадров DLSS на GPU RTX 20-й и 30-й серий (только D3D12-игры, ReShade и ASI Loader не нужны). Выберите поколение GPU (RTX 30 или RTX 20) в шестерёнке перед установкой — остальное RHI сделает сам и будет поддерживать актуальность.
+- **Управление разрешением и цветом** — автоматический переход на целевое разрешение при запуске игры и возврат на выходе. Также включает Output Colour Settings для управления глубиной цвета и динамическим диапазоном HDR по каждому дисплею без панели управления NVIDIA.
+- **Самостоятельный DLSS Enabler** — новая строка в секции Extras. Устанавливает DLSS Enabler как прокси-DLL прямо в любую папку игры, независимо от OptiScaler. Обновляется автоматически.
+- **MFG Ada Unlock** — новая строка в секции Extras. Разблокирует DLSS Multi Frame Generation (3x/4x+) на GPU RTX 40-й серии. Требуется ReShade. Взаимоисключаем с RTX 40 MFG Unlock.
 
-### Changes
+### Изменения
 
-- Changing a DLSS preset, render scale, or driver override in the NVIDIA Profile Overrides section no longer flashes or rebuilds the panel — the value is written immediately and the combo stays exactly as you set it.
-- Extras section install buttons (ASI Loader, RTX 40 MFG, DLSS Enabler) now show the same blue installed style as the Components section when installed.
-- Extras section is now split into groups with separators: ASI Loader at the top, MFG Unlocks (RTX 40 MFG, MFG Ada Unlock, 20/30 FG Unlock), and Other (OptiScaler, DLSS Enabler).
-- Neural Rendering: added a note below the NR Cost Scaler toggle when the ShortFuse method is selected, explaining that Cost Scaler is now built into the addon.
+- Смена пресета DLSS, масштаба рендеринга или переопределения драйвера в секции переопределений профиля NVIDIA больше не мигает и не пересобирает панель — значение записывается сразу, а комбобокс остаётся ровно на выбранном.
+- Кнопки установки в секции Extras (ASI Loader, RTX 40 MFG, DLSS Enabler) при установке показывают тот же синий стиль «установлено», что и секция компонентов.
+- Секция Extras разделена на группы с разделителями: ASI Loader наверху, MFG Unlocks (RTX 40 MFG, MFG Ada Unlock, 20/30 FG Unlock) и «Прочее» (OptiScaler, DLSS Enabler).
+- Neural Rendering: при выбранном методе ShortFuse под переключателем NR Cost Scaler добавлена заметка о том, что Cost Scaler теперь встроен в аддон.
 
-### Maintenance
+### Обслуживание
 
-- RHI now tracks every DLL it deploys into game folders using a sentinel file. If the game already had a file at that location, the original is backed up and restored on uninstall. If there was nothing there, a 0-byte marker is written so RHI knows to clean up cleanly. This prevents game-original DLLs from being lost after uninstall, and fixes orphaned leftover files. Covers OptiScaler, DLSS version swaps, RE Framework, and Luma.
-- Added internal RenoDX database service that fetches mod and UE-Extended configuration data from the RHI repository. Includes a dev-only "RenoDX Data Source" setting (Wiki Only / DB Only / Hybrid) for testing and validation ahead of a full transition away from wiki scraping.
+- RHI теперь отслеживает каждую DLL, развёрнутую в папки игр, sentinel-файлом. Если на этом месте уже был файл, оригинал резервируется и восстанавливается при удалении. Если ничего не было, пишется 0-байтовый маркер, чтобы RHI знал, что место нужно чистить начисто. Это предотвращает потерю оригинальных DLL игры после удаления и чинит брошенные файлы. Покрывает OptiScaler, подмену версий DLSS, RE Framework и Luma.
+- Добавлен внутренний сервис базы данных RenoDX, забирающий данные о модах и конфигурации UE-Extended из репозитория RHI. Включает доступную только разработчикам настройку «RenoDX Data Source» (Wiki Only / DB Only / Hybrid) для тестирования перед полным отказом от парсинга вики.
 
-### Bug Fixes
+### Исправления
 
-- Fixed DLSS Fix writing the wrong Streamline path in `reshade.ini` — it was pointing to the wrong file, which caused DLSS Fix to fail to hook Streamline correctly.
-- Fixed DLSS Fix config not being written at all when Streamline files were added to a game after RHI had already scanned it (e.g. from a game update or another component install in the same session).
-- Fixed the NR Cost Scaler toggle staying greyed out for an entire session after a fresh RHI install — it downloads in the background at startup but the toggle wasn't updating to reflect it, requiring an install+uninstall workaround to un-grey it.
-- Fixed ASI Loader leaving its INI file behind on uninstall (e.g. uninstalling `winmm.dll` now also removes `winmm.ini`).
-- Fixed a crash when clicking "Apply Peak Nits to All" or "Apply to All Games" in Settings when a game's folder no longer exists on disk.
-- Fixed Admin Mode not being detected on certain system configurations — RHI now uses a more reliable check that correctly handles accounts with full admin rights even when the standard elevation check returns false.
-- Fixed the Neural Rendering section showing ReShade as not installed immediately after installing it — you had to navigate away and back to see the correct status.
-- Fixed RE Framework showing a stale build number on the card after an update — the version now syncs correctly on the next update check.
-- Fixed NVIDIA Profile Overrides showing Default/Off on every launch until a manual Refresh — caused by a startup timing issue where profile reads would fail silently before NVAPI finished initialising.
+- Исправлена запись DLSS Fix неверного пути Streamline в `reshade.ini` — ссылка вела на не тот файл, из-за чего DLSS Fix не мог корректно перехватить Streamline.
+- Исправлена полная незапись конфига DLSS Fix, когда файлы Streamline добавлялись в игру после того, как RHI уже просканировал её (например, из-за обновления игры или установки другого компонента в той же сессии).
+- Исправлена серость переключателя NR Cost Scaler на всю сессию после свежей установки RHI — он скачивается в фоне при запуске, но переключатель не обновлялся, и приходилось ставить+удалять, чтобы разблокировать.
+- Исправлено оставление INI-файла ASI Loader'ом при удалении (удаление `winmm.dll` теперь убирает и `winmm.ini`).
+- Исправлен краш при нажатии «Применить пиковые ниты ко всем» или «Применить ко всем играм» в настройках, когда папка игры больше не существует на диске.
+- Исправлено необнаружение режима администратора на некоторых конфигурациях системы — RHI теперь использует более надёжную проверку, правильно обрабатывающую учётки с полными правами администратора даже когда стандартная проверка повышения возвращает false.
+- Исправлено показывание «ReShade не установлен» в секции Neural Rendering сразу после его установки — приходилось уйти и вернуться, чтобы увидеть верный статус.
+- Исправлен устаревший номер сборки RE Framework на карточке после обновления — версия теперь синхронизируется при следующей проверке обновлений.
+- Исправлено показывание Default/Off в переопределениях профиля NVIDIA при каждом запуске до ручного «Обновить» — причина в стартовой гонке, когда чтения профилей тихо падали до завершения инициализации NVAPI.
 
-### Manifest Updates
+### Обновления манифеста
 
-- Onimusha: Way of the Sword: added ultrawide fix link.
-- Red Dead Redemption 2: matched to the RenoDX wiki entry (Vulkan mod).
+- Onimusha: Way of the Sword: добавлена ссылка на ультраширокое исправление.
+- Red Dead Redemption 2: привязан к записи вики RenoDX (Vulkan-мод).
 
 ---
 
 ## v2.6.8
 
-### Bug Fixes
+### Исправления
 
-- Fixed the NVIDIA Profile Overrides section not applying preset and render scale changes — a stale background scan callback was overwriting the panel after a user change, discarding the new values.
-- Fixed the driver settings section (VSync, ReBAR, Smooth Motion, etc.) not appearing after a Refresh.
-- Fixed DLSS presets and render scale showing Default/Off instead of the actual driver values — reads now go directly to the live driver state instead of a stale in-memory cache.
+- Исправлено неприменение изменений пресетов и масштаба рендеринга в секции переопределений профиля NVIDIA — устаревший колбэк фонового скана перезаписывал панель после изменения пользователем, отбрасывая новые значения.
+- Исправлено отсутствие секции настроек драйвера (VSync, ReBAR, Smooth Motion и т.д.) после «Обновить».
+- Исправлено показывание Default/Off в пресетах DLSS и масштабе рендеринга вместо фактических значений драйвера — чтения теперь идут прямо в живое состояние драйвера, а не в устаревший кеш в памяти.
 
-### Manifest Updates
+### Обновления манифеста
 
-- Added DLSS SR, RR, and FG version 310.9.1.
-- Added Streamline 2.14.1.
+- Добавлены DLSS SR, RR и FG версии 310.9.1.
+- Добавлен Streamline 2.14.1.
 
 ---
 
 ## v2.6.7
 
-### Changes
+### Изменения
 
-- ReShade uninstall now preserves `reshade.log` in the game folder.
-- ReShade config files are now deployed to game folders as `ReShade.ini` instead of `reshade.ini`.
-- Neural Rendering DLL selection simplified to 310.8.2 — ShortFuse's modified build with support for all RTX GPUs (20/30/40/50 series).
-- Added tooltips to the DLSS SR, RR, FG, and Streamline version dropdowns explaining that selecting a version copies it into the game folder, what Default and Custom do, and how NVIDIA Override works.
+- Удаление ReShade теперь сохраняет `reshade.log` в папке игры.
+- Конфиги ReShade разворачиваются в папки игр как `ReShade.ini` вместо `reshade.ini`.
+- Выбор DLL Neural Rendering упрощён до 310.8.2 — модифицированная сборка ShortFuse с поддержкой всех RTX GPU (серии 20/30/40/50).
+- В списки версий DLSS SR, RR, FG и Streamline добавлены подсказки: выбор версии копирует её в папку игры, что делают Default и Custom и как работает переопределение NVIDIA.
 
-### Bug Fixes
+### Исправления
 
-- Fixed Custom render scale in DLSS & Streamline Defaults — selecting "Custom" now shows a text box so you can type in a specific percentage (33–100%).
-- Fixed an intermittent UI freeze where the window would stay active (moveable, minimisable) but all buttons and controls stopped responding. This could happen when opening cogs, install dialogs, or other popups while a background dialog was already showing. Affects the Luma Settings cog, ASI Loader cog, ShortFuse settings, RTX 40 MFG cog, and install warning prompts.
-- Fixed a freeze that could occur when an app update was found while the "Checking for updates…" progress dialog was open — the update dialog would silently block for up to 10 seconds.
-- Fixed a brief freeze when RHI updated the taskbar jump list after launching a game or changing the Recent Games setting — the update now runs in the background.
-- Fixed the Batch Deploy DLSS dialog occasionally leaving a ghost overlay that blocked all input after finishing quickly.
-- Fixed a flicker where the entire Overrides panel disappeared and rebuilt itself when changing a DLSS version, preset, render scale, or driver override — now only the NVIDIA Profile section refreshes.
-- Fixed a freeze that could occur when changing a DLSS version and clicking a section header at the same time.
-- Fixed the NVIDIA Profile Overrides section briefly going blank when selecting a game or installing OptiScaler — the section now shows immediately using cached values and updates silently in the background.
-- Fixed changing the Vulkan ReShade channel (Stable/Nightly/Custom) blocking the UI for up to 10 seconds while copying files to `C:\ProgramData\ReShade\`.
-- Fixed the OptiScaler cog potentially rebuilding off the UI thread when switching between Stable and Nightly variants, or after applying a preset — could cause a freeze in certain timing conditions.
-- Fixed OptiScaler install and uninstall progress not showing in the Extras section — the progress bar and status message now appear directly below the OptiScaler row where they belong. *(Thanks Sapphire)*
-- Fixed installing or removing ASI Loader and RTX 40 MFG Unlock resetting the Extras panel scroll position.
-- Fixed the drop helper window appearing as a separate entry in the taskbar and Alt+Tab switcher. *(Thanks Owen)*
-- Fixed an intermittent UI freeze when rapidly clicking through games — the NVIDIA Profile section's background scans now run at low priority so they can't block pointer input, and the DLSS/driver rows are rebuilt atomically instead of element-by-element.
+- Исправлен свой масштаб рендеринга в значениях DLSS и Streamline по умолчанию — выбор «Custom» теперь показывает текстовое поле, куда можно вписать конкретный процент (33–100%).
+- Исправлено периодическое зависание UI, когда окно оставалось активным (двигалось, сворачивалось), но все кнопки и элементы переставали отвечать. Случалось при открытии шестерёнок, диалогов установки и других попапов, пока фоновый диалог уже показан. Затрагивает шестерёнку настроек Luma, шестерёнку ASI Loader, настройки ShortFuse, шестерёнку RTX 40 MFG и предупреждения установки.
+- Исправлено зависание, возможное при обнаружении обновления приложения, пока открыт диалог прогресса «Проверка обновлений…» — диалог обновления тихо блокировался до 10 секунд.
+- Исправлено краткое зависание при обновлении RHI списка переходов панели задач после запуска игры или смены настройки «Недавние игры» — обновление теперь идёт в фоне.
+- Исправлено оставление диалогом массового развертывания DLSS «призрачного» оверлея, блокировавшего ввод после быстрого завершения.
+- Исправлено мигание с полной пересборкой панели переопределений при смене версии DLSS, пресета, масштаба рендеринга или переопределения драйвера — теперь обновляется только секция профиля NVIDIA.
+- Исправлено зависание, возможное при одновременной смене версии DLSS и клике по заголовку секции.
+- Исправлено краткое побеление секции переопределений профиля NVIDIA при выборе игры или установке OptiScaler — секция теперь показывается сразу на кешированных значениях и тихо обновляется в фоне.
+- Исправлена блокировка UI до 10 секунд при смене канала Vulkan ReShade (Stable/Nightly/Custom) из-за копирования файлов в `C:\ProgramData\ReShade\`.
+- Исправлена возможная пересборка шестерёнки OptiScaler вне UI-потока при переключении Stable и Nightly или после применения пресета — при определённых таймингах могло зависать.
+- Исправлено отсутствие прогресса установки/удаления OptiScaler в секции Extras — полоса прогресса и статус теперь показываются прямо под строкой OptiScaler, где им и место. *(Спасибо Sapphire)*
+- Исправлен сброс позиции прокрутки панели Extras при установке или удалении ASI Loader и RTX 40 MFG Unlock.
+- Исправлено появление окна Drop Helper отдельной записью на панели задач и в Alt+Tab. *(Спасибо Owen)*
+- Исправлено периодическое зависание UI при быстром перещёлкивании игр — фоновые сканы секции профиля NVIDIA теперь идут с низким приоритетом и не могут блокировать ввод мыши, а строки DLSS/драйвера пересобираются атомарно, а не поэлементно.
 
-### Manifest Updates
+### Обновления манифеста
 
-- Grand Theft Auto V Enhanced: added a ReShade install warning about the DirectStorage incompatibility that causes "Unable to save configuration" errors, with a link to the DirectStorageFix and a note to use an older ReShade version as an alternative.
-- Satisfactory: added Frame Generation setup instructions — the Engine.ini keys needed to enable DLSS FG in Satisfactory are now shown in the ReShade info button.
+- Grand Theft Auto V Enhanced: добавлено предупреждение об установке ReShade из-за несовместимости DirectStorage, вызывающей ошибки «Unable to save configuration», со ссылкой на DirectStorageFix и замечанием об использовании старой версии ReShade как альтернативы.
+- Satisfactory: добавлены инструкции по настройке генерации кадров — ключи Engine.ini, нужные для включения DLSS FG в Satisfactory, теперь показываются в кнопке информации ReShade.
 
 ---
 
 ## v2.6.6
 
-### Bug Fixes
+### Исправления
 
-- Fixed DLL naming override toggle becoming unresponsive after being toggled — any in-progress drag state is now cleared when the overrides panel rebuilds, and the toggle is always re-enabled via a `finally` block even if file operations throw.
-- Fixed Recent Games Off setting not hiding games from the system tray right-click menu — all call sites now pass an empty list when the setting is off.
-- Fixed Recent Games Off setting not clearing the taskbar jump list — jump list now uses `ICustomDestinationList` instead of `SHAddToRecentDocs`, giving RHI full control to clear it when the setting is toggled off.
-- Fixed reinstalling ASI Loader with a different DLL name leaving the old DLL behind — the previous install is now cleaned up (and any Hooked backup restored) before deploying under the new name.
-- Fixed ShortFuse DLSS Tool download failing with "file in use" when both DLSS5 variants download concurrently — each variant now uses its own temp filename.
-- Fixed ShortFuse DLSS Tool zip extraction failing when the addon file inside the zip doesn't match the expected exact filename — falls back to any `.addon64` entry in the zip.
-- Fixed OptiScaler install/uninstall status messages appearing in the Components section — suppressed there since OptiScaler is in the Extras section.
+- Исправлена невосприимчивость переключателя имён DLL после переключения — незавершённое drag-состояние теперь сбрасывается при пересборке панели переопределений, а переключатель всегда разблокируется через `finally`, даже если файловые операции бросают исключение.
+- Исправлено отсутствие скрытия игр из меню правого клика в трее при выключенной настройке «Недавние игры» — все точки вызова теперь передают пустой список при выключенной настройке.
+- Исправлена неочистка списка переходов панели задач при выключенной настройке «Недавние игры» — список переходов теперь использует `ICustomDestinationList` вместо `SHAddToRecentDocs`, давая RHI полный контроль на очистку при выключении настройки.
+- Исправлено оставание старой DLL при переустановке ASI Loader с другим именем DLL — предыдущая установка чистится (и восстанавливаются резервные копии Hooked) перед развертыванием под новым именем.
+- Исправлена ошибка «файл занят» при скачивании ShortFuse DLSS Tool, когда обе версии DLSS5 скачивались параллельно — каждая версия теперь использует своё временное имя файла.
+- Исправлен сбой распаковки zip ShortFuse DLSS Tool, когда аддон-файл внутри архива не совпадал с ожидаемым точным именем — теперь откатывается к любому `.addon64` в архиве.
+- Исправлено показывание статусов установки/удаления OptiScaler в секции компонентов — там они подавлены, так как OptiScaler в секции Extras.
 
 ---
 
 ## v2.6.5
 
-### Bug Fixes
+### Исправления
 
-- Fixed OptiScaler install and uninstall not refreshing the Extras section — the installed/uninstalled state now updates immediately without needing a manual refresh.
-- Fixed DLL naming override toggle requiring a refresh to interact with after enabling or disabling — the overrides panel now rebuilds immediately after the toggle operation completes.
-- Fixed RTX 40 MFG Unlock not writing the UAL ini file (`version.ini`, `dinput8.ini`, etc.) on install — without it, UAL didn't know to load `RTX40MFG.asi` and ReShade would stop working. RHI now writes the required `[GlobalSets]` keys to the matching UAL proxy ini file automatically.
+- Исправлено необновление секции Extras при установке и удалении OptiScaler — состояние «установлено/удалено» обновляется сразу без ручного обновления.
+- Исправлена необходимость «Обновить» для взаимодействия с переключателем имён DLL после его включения или отключения — панель переопределений теперь пересобирается сразу после завершения операции.
+- Исправлена незапись UAL ini-файла (`version.ini`, `dinput8.ini` и т.п.) при установке RTX 40 MFG Unlock — без него UAL не знал, что загружать `RTX40MFG.asi`, и ReShade переставал работать. RHI теперь сам пишет нужные ключи `[GlobalSets]` в соответствующий прокси-ini UAL.
 
 ---
 
 ## v2.6.4
 
-### Changes
+### Изменения
 
-- DLSS Super Resolution, Ray Reconstruction, and Frame Generation version combos now include **NVIDIA Override** as a selectable option. Choosing it writes the driver's "Latest DLL" flag to the game's NVIDIA profile, equivalent to enabling "DLSS — Enable DLL Override" in Profile Inspector. Selecting any other version or clicking Restore DLSS/SL clears the override. NVIDIA Override is also available as a default in Configure Defaults (applies via Quick Apply) and as a selectable option in Batch Deploy.
-- Added support for per-game custom Engine.ini files, hosted in the rhi-repo `engine-files/` folder and referenced by game name in the manifest. When set, RHI fetches and merges only the keys in that file instead of the standard HDR key set — allowing precise control over what gets written for games where the full set causes issues.
+- В списки версий DLSS Super Resolution, Ray Reconstruction и генерации кадров добавлен вариант **NVIDIA Override**. Его выбор записывает флаг «Latest DLL» драйвера в профиль NVIDIA игры — эквивалент включения «DLSS — Enable DLL Override» в Profile Inspector. Выбор любой другой версии или нажатие «Восстановить DLSS/SL» снимает переопределение. NVIDIA Override также доступен как значение по умолчанию в «Настроить значения по умолчанию» (применяется через «Быстрое применение») и как вариант в «Массовом развертывании».
+- Добавлена поддержка своих файлов Engine.ini для каждой игры: они размещаются в папке `engine-files/` репозитория rhi-repo и указываются в манифесте по имени игры. Когда задано, RHI скачивает и сливает только ключи из этого файла вместо стандартного набора HDR-ключей — точный контроль над записываемым для игр, где полный набор вызывает проблемы.
 
-### Bug Fixes
+### Исправления
 
-- Fixed NR Cost Scaler and RTX 40 MFG Unlock not detecting new releases mid-session — Check for Update and the 4-hour timer now bypass the ETag cache and always fetch fresh release data.
-- Fixed NR Cost Scaler and RTX 40 MFG Unlock not auto-deploying updated files to game folders after a new version is staged.
+- Исправлено необнаружение новых релизов NR Cost Scaler и RTX 40 MFG Unlock в середине сессии — «Проверить обновления» и 4-часовой таймер теперь обходят ETag-кеш и всегда забирают свежие данные релизов.
+- Исправлено отсутствие авто-развертывания обновлённых файлов NR Cost Scaler и RTX 40 MFG Unlock в папки игр после помещения новой версии в хранилище.
 
-### Manifest Updates
+### Обновления манифеста
 
-- Black Myth: Wukong — custom Engine.ini file applied on UE-Extended install/update (writes only `r.HDR.EnableHDROutput=1` instead of the standard key set). Standard HDR and LUT controls in the UE-Extended cog are disabled for this game.
+- Black Myth: Wukong — свой файл Engine.ini применяется при установке/обновлении UE-Extended (пишется только `r.HDR.EnableHDROutput=1` вместо стандартного набора ключей). Стандартные контролы HDR и LUT в шестерёнке UE-Extended для этой игры отключены.
 
 ---
 
 ## v2.6.3
 
-### New
+### Новое
 
-- RTX 40 MFG Unlock added to the Extras section — enables DLSS Multi Frame Generation multipliers beyond 2x (up to 6x) on RTX 40 Series GPUs for games with Streamline FG support. Requires ASI Loader and ReShade installed first.
-- DLSS NR Cost Scaler added to the Neural Rendering section — proxy for nvngx_dlssnr.dll that runs the neural model at reduced resolution (default 75%) for significant GPU savings while preserving native-resolution detail. Toggle on before installing an NR method to deploy it in one click.
+- RTX 40 MFG Unlock добавлен в секцию Extras — включает множители DLSS Multi Frame Generation выше 2x (до 6x) на GPU RTX 40-й серии для игр с поддержкой Streamline FG. Сначала требуются ASI Loader и ReShade.
+- NR Cost Scaler добавлен в секцию Neural Rendering — прокси для nvngx_dlssnr.dll, запускающий нейросетевую модель в пониженном разрешении (по умолчанию 75%) ради заметной экономии GPU при сохранении детализации нативного разрешения. Включите перед установкой NR-метода, чтобы развернуть одним кликом.
 
-### Changes
+### Изменения
 
-- OptiScaler moved from the Components section to the Extras section, alongside ASI Loader.
-- RenoDX cog Compatibility Settings: Upgrade_ prefix stripped from format labels for readability (e.g. `R10G10B10A2_UNORM` instead of `Upgrade_R10G10B10A2_UNORM`).
-- RenoDX cog dialog widened so Compatibility Settings labels no longer truncate.
-- Installing a Neural Rendering method now automatically removes conflicting global addons (DLSS5 Tool, DLSS Tool (ShortFuse)) from the global set and cleans up their files immediately.
-- Neural Rendering auto-select now defaults to ShortFuse (was DLSS5 Tool) for DX12 games with native DLSS.
-- RTX 40 MFG Unlock and MFG Ada Unlock are mutually exclusive — installing one blocks the other.
-- DLSS/Streamline version combo now shows `Default (x.x.x)` as a separate top entry rather than marking a version in the list — you can now select any version including the original without triggering a restore.
-- Streamline version is now read from `sl.common.dll` instead of `sl.interposer.dll`.
-- "ReShade Addons" renamed to "Global Addons" in the Shaders/Addons dropdown.
-- Clicking "Select" in the per-game shader picker while already on Select now re-opens the picker (same behaviour as the addon picker).
+- OptiScaler переехал из секции компонентов в секцию Extras, рядом с ASI Loader.
+- В настройках совместимости шестерёнки RenoDX префикс Upgrade_ убран из подписей форматов для читаемости (например, `R10G10B10A2_UNORM` вместо `Upgrade_R10G10B10A2_UNORM`).
+- Диалог шестерёнки RenoDX расширен — подписи настроек совместимости больше не обрезаются.
+- Установка метода Neural Rendering теперь автоматически удаляет конфликтующие глобальные аддоны (DLSS5 Tool, DLSS Tool (ShortFuse)) из глобального набора и сразу чистит их файлы.
+- Автовыбор Neural Rendering по умолчанию — ShortFuse (было DLSS5 Tool) для DX12-игр с нативным DLSS.
+- RTX 40 MFG Unlock и MFG Ada Unlock взаимоисключаемы — установка одного блокирует другой.
+- Список версий DLSS/Streamline теперь показывает `Default (x.x.x)` отдельной верхней записью вместо пометки версии в списке — можно выбрать любую версию, включая исходную, не запуская восстановление.
+- Версия Streamline теперь читается из `sl.common.dll` вместо `sl.interposer.dll`.
+- «Аддоны ReShade» переименованы в «Глобальные аддоны» в выпадающем меню «Шейдеры/Аддоны».
+- Повторное нажатие «Выбрать» в поигровом окне выбора шейдеров, когда уже выбран режим «Выбрать», снова открывает окно (как у окна выбора аддонов).
 
-### Bug Fixes
+### Исправления
 
-- Fixed NR Cost Scaler and RTX 40 MFG Unlock services not respecting the session-wide GitHub API rate limit flag — they now go through the shared ETag cache.
-- Fixed RenoDX update check HEAD requests hanging the UI for up to 100 seconds when offline or rate-limited — now times out after 10 seconds.
-- Fixed `renodx-dlss.addon64` not being removed when switching per-game addons to Off on games that ship with `nvngx_dlssnr.dll` natively (e.g. Cyberpunk 2077) — the stale-removal guard now correctly distinguishes RHI-placed NR DLLs from game-native ones.
-- Fixed Neural Rendering method auto-select inferring DLSS5 Tool for games that have a backed-up NR DLL but no active install.
+- Исправлено игнорирование сервисами NR Cost Scaler и RTX 40 MFG Unlock сессионного флага лимита GitHub API — теперь они идут через общий ETag-кеш.
+- Исправлено подвешивание UI HEAD-запросами проверки обновлений RenoDX до 100 секунд при отсутствии сети или лимитах — теперь таймаут 10 секунд.
+- Исправлено необновление `renodx-dlss.addon64` при переключении поигровых аддонов в Выкл на играх с нативным `nvngx_dlssnr.dll` (например, Cyberpunk 2077) — защита от удаления устаревшего теперь отличает NR-DLL, размещённые RHI, от нативных.
+- Исправлен автовыбор метода Neural Rendering, выводивший DLSS5 Tool для игр с резервной копией NR-DLL, но без активной установки.
 
-### Known Limitations
+### Известные ограничения
 
-- DLSS5 Feeder: 32-bit game support not yet implemented.
+- DLSS5 Feeder: поддержка 32-бит игр пока не реализована.
 
-### Manifest Updates
+### Обновления манифеста
 
-- METAL GEAR SOLID 4: Guns of the Patriots - Master Collection Version linked to Luma wiki entry.
-- METAL GEAR SOLID 4: Guns of the Patriots — install warning added: switch to DX11 before installing Luma.
-- Eternal Strands — UE-Extended compat entry added (HDR keys skipped, LUT only) + INI overrides.
+- METAL GEAR SOLID 4: Guns of the Patriots - Master Collection Version привязан к записи вики Luma.
+- METAL GEAR SOLID 4: Guns of the Patriots — добавлено предупреждение об установке: переключитесь на DX11 перед установкой Luma.
+- Eternal Strands — добавлена запись совместимости UE-Extended (HDR-ключи пропускаются, только LUT) + переопределения INI.
 
 ---
 
 ## v2.6.2
 
-### Changes
+### Изменения
 
-- Switching the Neural Rendering method now automatically removes any components installed by the previous method, giving a clean slate before installing the new one.
-- ShortFuse ASI auto-config (ReShade rename + UAL install) is now opt-in rather than enabled by default.
-- Detail view is now the default layout. All users are switched to it once on first launch of this version.
+- Смена метода Neural Rendering теперь автоматически удаляет компоненты, установленные предыдущим методом, давая чистый лист перед установкой нового.
+- Автонастройка ASI для ShortFuse (переименование ReShade + установка UAL) теперь опциональна, а не включена по умолчанию.
+- Подробный вид теперь макет по умолчанию. Все пользователи переключаются на него один раз при первом запуске этой версии.
 
-### Bug Fixes
+### Исправления
 
-- Fixed switching away from DLSS5 Feeder not removing the shaders it deployed (DLSS5_Feed.fx, lumenite_Kernel.fx).
+- Исправлено отсутствие удаления шейдеров, развёрнутых DLSS5 Feeder, при переключении с него (DLSS5_Feed.fx, lumenite_Kernel.fx).
 
 ---
 
 ## v2.6.1
 
-### Changes
+### Изменения
 
-- Neural Rendering auto-select now defaults to ShortFuse (was DLSS5 Tool) for DX12 games with native DLSS.
-- ReShade Settings cog: Overlay Key and Screenshot Key fields are now side by side.
+- Автовыбор Neural Rendering по умолчанию — ShortFuse (было DLSS5 Tool) для DX12-игр с нативным DLSS.
+- Шестерёнка настроек ReShade: поля Overlay Key и Screenshot Key теперь рядом.
 
-### Bug Fixes
+### Исправления
 
-- Fixed UI hang when rapidly clicking through the game list — all synchronous NVAPI driver profile reads (DLSS presets, driver overrides, VSync/ReBAR/Smooth Motion) are now fetched off the UI thread.
-- Fixed UI hang when rapidly changing DLSS/Streamline version dropdowns.
-- Fixed toggling the DLL naming overrides switch hanging the UI.
-- Fixed games using the D3D12 Agility SDK (e.g. Onimusha: Way of the Sword) being detected as DX11.
-- Fixed Neural Rendering method incorrectly defaulting to DLSS5 Tool for games that have a backed-up NR DLL but nothing actively installed.
-- Fixed DLSS5 Feeder deploying a 0-byte nvngx_dlss.dll when the cached file was unavailable.
+- Исправлено подвешивание UI при быстром перещёлкивании списка игр — все синхронные чтения профилей драйвера NVAPI (пресеты DLSS, переопределения драйвера, VSync/ReBAR/Smooth Motion) теперь идут вне UI-потока.
+- Исправлено подвешивание UI при быстрой смене списков версий DLSS/Streamline.
+- Исправлено подвешивание UI при переключении тумблера имён DLL.
+- Исправлено определение игр на D3D12 Agility SDK (например, Onimusha: Way of the Sword) как DX11.
+- Исправлено неверное умолчание метода Neural Rendering в DLSS5 Tool для игр с резервной копией NR-DLL, но без активной установки.
+- Исправлено развертывание DLSS5 Feeder'ом 0-байтового nvngx_dlss.dll при недоступности кешированного файла.
 
-### Manifest Updates
+### Обновления манифеста
 
-- Onimusha: Way of the Sword and PRAGMATA forced to DX12 detection.
-- RoboCop: Rogue City — Unfinished Business Engine.ini config path corrected.
+- Onimusha: Way of the Sword и PRAGMATA форсированы на определение DX12.
+- Исправлен путь конфига Engine.ini для RoboCop: Rogue City — Unfinished Business.
 
 ---
 
 ## v2.6.0
 
-### New
+### Новое
 
-- Detail view sections (Components, Game Overrides, Neural Rendering, Nvidia Profile Overrides, Management) are now collapsible. Click the section heading to toggle it open or closed. Collapsed state persists across restarts.
-- Detail view sections can be reordered by dragging the ≡ handle on the left of each section header. Order persists across restarts.
-- New Extras section in detail view. Contains Ultimate ASI Loader — install the UAL proxy DLL into any game folder to enable .asi plugin loading. Choose from the full list of supported DLL names (bitness-filtered, with Recommended badges and conflict warnings). Keeps itself up to date automatically. Hooked chaining handled automatically when the chosen DLL name is already in use by a game file.
-- ShortFuse DLSS Tool now auto-configures ReShade for FrameGen on install: renames ReShade to Reshade64.asi, installs ASI Loader automatically (winmm → version → dinput8 priority), and writes HookStreamline=1 and HookDirectX=1 to reshade.ini. Controlled via the ⚙ cog next to the Neural Rendering install button — enabled by default, can be turned off per-game.
+- Секции подробного вида (Компоненты, Переопределения игры, Neural Rendering, Переопределения профиля NVIDIA, Управление) теперь сворачиваются. Клик по заголовку секции открывает и закрывает её. Свёрнутое состояние сохраняется между перезапусками.
+- Секции подробного вида можно переставлять, перетаскивая рукоятку ≡ слева от заголовка секции. Порядок сохраняется между перезапусками.
+- Новая секция Extras в подробном виде. Содержит Ultimate ASI Loader — установите прокси-DLL UAL в любую папку игры, чтобы включить загрузку плагинов .asi. Полный список поддерживаемых имён DLL (с фильтром по разрядности, значками «Рекомендуется» и предупреждениями о конфликтах). Сам обновляется автоматически. Цепочка Hooked обрабатывается автоматически, когда выбранное имя DLL уже занято файлом игры.
+- ShortFuse DLSS Tool теперь сам настраивает ReShade под FrameGen при установке: переименовывает ReShade в Reshade64.asi, автоматически ставит ASI Loader (приоритет winmm → version → dinput8) и пишет HookStreamline=1 и HookDirectX=1 в reshade.ini. Управляется шестерёнкой ⚙ рядом с кнопкой установки Neural Rendering — включено по умолчанию, отключается поигрово.
 
-### Changes
+### Изменения
 
-- RenoDX renamed to RenoDX HDR in the detail view component list.
-- Version number now shown next to MFG Ada Unlock, DLSS5 Feeder, and DX11 Bridge in the addon panel (same as DLSS5 Tool).
-- ASI Loader status now appears in the ShortFuse Neural Rendering status line alongside ReShade and DLSS versions.
+- RenoDX переименован в RenoDX HDR в списке компонентов подробного вида.
+- Номер версии теперь показывается рядом с MFG Ada Unlock, DLSS5 Feeder и DX11 Bridge в панели аддонов (как у DLSS5 Tool).
+- Статус ASI Loader теперь показывается в строке статуса Neural Rendering ShortFuse рядом с версиями ReShade и DLSS.
 
-### Bug Fixes
+### Исправления
 
-- Fixed ShortFuse DLSS Tool addon (renodx-dlss.addon64) being removed as stale on every launch and Refresh for games where it was installed via the Neural Rendering section.
-- Fixed Nvidia Profile Overrides section showing stale DLSS versions after removing a Neural Rendering method — now refreshes immediately without needing a manual Refresh.
+- Исправлено удаление аддона ShortFuse DLSS Tool (renodx-dlss.addon64) как устаревшего при каждом запуске и «Обновить» для игр, где он был установлен через секцию Neural Rendering.
+- Исправлено показывание устаревших версий DLSS в секции переопределений профиля NVIDIA после удаления метода Neural Rendering — теперь обновляется сразу без ручного «Обновить».
 
-### Manifest Updates
+### Обновления манифеста
 
-- Baldur's Gate 3 forced to 64-bit detection.
-- Hogwarts Legacy linked to Marat's UE-Extended addon.
+- Baldur's Gate 3 форсирован на определение 64-бит.
+- Hogwarts Legacy привязан к аддону UE-Extended от Marat.
 
 ---
 
 ## v2.5.9
 
-### Bug Fixes
+### Исправления
 
-- Fixed DLSS5 Feeder refresh wiping lumenite shader files — `SyncGameFolder` was deleting all managed shaders then only redeploying DLSS5_Feed.fx, losing lumenite_Kernel.fx. Fixed by persisting pack-level exclusions via SetExcludedFiles so refresh correctly deploys only the two needed files.
-- Fixed MFG Ada Unlock, DLSS5 Feeder, and DX11 Bridge missing from the per-game addon picker.
+- Исправлено стирание шейдеров lumenite при обновлении DLSS5 Feeder — `SyncGameFolder` удалял все управляемые шейдеры, а разворачивал обратно только DLSS5_Feed.fx, теряя lumenite_Kernel.fx. Исправлено сохранением исключений на уровне набора через SetExcludedFiles, так что обновление корректно разворачивает только два нужных файла.
+- Исправлено отсутствие MFG Ada Unlock, DLSS5 Feeder и DX11 Bridge в поигровом окне выбора аддонов.
 
 ---
 
 ## v2.5.8
 
-### Changes
+### Изменения
 
-- Clicking the installed version number on a UE-Extended game now opens Marat's commit history for the UE-Extended addon.
+- Клик по номеру установленной версии у игры UE-Extended теперь открывает историю коммитов Marat для аддона UE-Extended.
 
-### Bug Fixes
+### Исправления
 
-- Fixed OptiPatcher not deploying for NVIDIA users — it was incorrectly gated to AMD/Intel only.
-- Fixed MFG Ada Unlock, DLSS5 Feeder, and DX11 Bridge disappearing from the global addon manager after being switched to API-based auto-updating.
-- Fixed MFG Ada Unlock, DLSS5 Feeder, and DX11 Bridge not auto-updating — these addons use dynamic release filenames so RHI now resolves the download URL from the GitHub releases API rather than a hardcoded URL.
-- Fixed addon update check running before the manifest was applied, causing manifest-driven addons to be silently skipped on every startup.
-- Fixed normal Refresh not re-checking games previously confirmed as "no DLSS" — newly installed DLSS (e.g. a game update that adds frame generation) now shows up on a standard Refresh instead of requiring a Full Refresh., causing manifest-driven addons to be silently skipped on every startup.
+- Исправлено отсутствие развертывания OptiPatcher у пользователей NVIDIA — он ошибочно ограничивался AMD/Intel.
+- Исправлено исчезновение MFG Ada Unlock, DLSS5 Feeder и DX11 Bridge из глобального менеджера аддонов после перевода на автообновление через API.
+- Исправлено отсутствие автообновления у MFG Ada Unlock, DLSS5 Feeder и DX11 Bridge — у этих аддонов динамические имена файлов релизов, поэтому RHI теперь вычисляет ссылку для скачивания из GitHub releases API вместо захардкоженной.
+- Исправлен запуск проверки обновлений аддонов до применения манифеста, из-за чего манифестные аддоны тихо пропускались при каждом запуске.
+- Исправлено отсутствие перепроверки обычным «Обновить» игр, ранее подтверждённых как «нет DLSS» — новоустановленный DLSS (например, добавленный обновлением игры) теперь появляется при обычном обновлении без «Полного обновления»., из-за чего манифестные аддоны тихо пропускались при каждом запуске.
 
 ---
 
 ## v2.5.7
 
-### Bug Fixes
+### Исправления
 
-- Fixed DLSS5 Feeder failing to download for some users — the zip filename changes with each release so the hardcoded URL broke on updates. RHI now resolves the download URL dynamically from the GitHub releases API so Feeder auto-updates correctly going forward.
-- Fixed DOF Fix install failing — the releases API was returning only the first 30 results by default, pushing DOF Fix releases off the page as the repo grew. Now uses per_page=100.
+- Исправлено незагружание DLSS5 Feeder у части пользователей — имя zip меняется с каждым релизом, и захардкоженная ссылка ломалась при обновлениях. RHI теперь вычисляет ссылку динамически из GitHub releases API, так что Feeder дальше обновляется корректно.
+- Исправлен сбой установки DOF Fix — releases API по умолчанию возвращал только первые 30 результатов, вытесняя релизы DOF Fix со страницы по мере роста репозитория. Теперь используется per_page=100.
 
 ---
 
 ## v2.5.6
 
-### Bug Fixes
+### Исправления
 
-- Fixed `renodx-dlss5.addon64` deployed by the Neural Rendering section being removed on the next Refresh — the addon cleanup pass was treating it as stale since it wasn't deployed through the standard addon system.
+- Исправлено удаление `renodx-dlss5.addon64`, развёрнутого секцией Neural Rendering, при следующем «Обновить» — проход очистки аддонов считал его устаревшим, так как тот не был развёрнут через стандартную систему аддонов.
 
 ---
 
 ## v2.5.5
 
+### Новое
 
-### New
+- **Секция Neural Rendering** — отдельная самодостаточная секция в панели подробностей игры (между «Переопределения игры» и «Переопределения профиля NVIDIA») для установки DLSS 5 Neural Rendering. Окно выбора аддонов не нужно. Список метода из четырёх вариантов:
+  - **DLSS5 Tool** — для игр с нативным DLSS. Разворачивает `renodx-dlss5.addon64`, обновляет DLSS SR/RR/FG до последних версий и разворачивает `nvngx_dlssnr.dll`.
+  - **DLSS5 Tool + DX11 Bridge** — для DX11/Vulkan игр с нативным DLSS. То же плюс `dlss5-bridge.addon64` (всегда скачивается последняя версия).
+  - **DLSS Tool (ShortFuse)** — альтернатива для любой 64-бит игры с нативным DLSS. Разворачивает полный стек DLSS SR/RR/FG/NR и Streamline по sentinel-схеме.
+  - **DLSS5 Feeder** — по умолчанию для игр без нативного DLSS (DX11, DX12, Vulkan, OpenGL, 32-бит). Разворачивает аддон Feeder, DLSS5 Tool как нейропотребитель, `nvngx_dlss.dll`, `nvngx_dlssnr.dll` и нужные шейдеры (`DLSS5_Feed.fx` + векторы движения LumeniteFX) автоматически. Записывает `ReShadePreset.ini` с обеими включёнными техниками в правильном порядке рендеринга.
+  - ReShade устанавливается автоматически, если ещё не стоит.
+  - Выбор версии NR-DLL, статусные индикаторы по файлам с версиями, кнопки Установить/Переустановить/Удалить, автоматическое определение метода для существующих установок и описания со ссылками для каждого метода.
 
-- **Neural Rendering section** — a dedicated self-contained section in the game detail panel (between Game Overrides and NVIDIA Profile Overrides) for installing DLSS 5 Neural Rendering. No addon picker required. Method combo with four options:
-  - **DLSS5 Tool** — for native DLSS games. Deploys `renodx-dlss5.addon64`, upgrades DLSS SR/RR/FG to latest, and deploys `nvngx_dlssnr.dll`.
-  - **DLSS5 Tool + DX11 Bridge** — for DX11/Vulkan native-DLSS games. Same as above plus `dlss5-bridge.addon64` (always downloads latest).
-  - **DLSS Tool (ShortFuse)** — alternative for any 64-bit native-DLSS game. Deploys the full DLSS SR/RR/FG/NR stack and Streamline via the sentinel pattern.
-  - **DLSS5 Feeder** — default for games with no native DLSS (DX11, DX12, Vulkan, OpenGL, 32-bit). Deploys the Feeder addon, DLSS5 Tool as neural consumer, `nvngx_dlss.dll`, `nvngx_dlssnr.dll`, and the required shaders (`DLSS5_Feed.fx` + LumeniteFX motion vectors) automatically. Writes a `ReShadePreset.ini` with both techniques pre-enabled in the correct render order.
-  - ReShade is installed automatically if not already present.
-  - NR DLL version picker, per-file status indicators with versions, Install/Reinstall/Remove buttons, automatic method detection for existing installs, and descriptions with links for each method.
+### Обновления манифеста
 
-### Manifest Updates
-
-- Added a note to Ori and the Blind Forest: Definitive Edition warning that the generic Unity mod may have visual issues and the named mod is deprecated.
-- Added install path override for The Witcher 3: Wild Hunt - Complete Edition (`bin\x64_dx12`), engine hint (REDengine), and graphics API override (DX12).
-- Fixed Outlast detecting as 32-bit and resolving to the wrong path — now forced 64-bit with `Binaries\Win64` path override and engine hint set to Unreal (Legacy).
-- Fixed DLSS5 DX11 Bridge download URL — old repo was deleted; updated to `NIGos/dlss5-bridge` with correct filename `dlss5-bridge.addon64`.
+- К Ori and the Blind Forest: Definitive Edition добавлено предупреждение, что универсальный Unity-мод может иметь визуальные проблемы, а именной мод устарел.
+- Для The Witcher 3: Wild Hunt - Complete Edition добавлены переопределение пути установки (`bin\x64_dx12`), подсказка движка (REDengine) и переопределение графического API (DX12).
+- Исправлено определение Outlast как 32-бит с неверным путём — теперь форсирован 64-бит с переопределением пути `Binaries\Win64` и подсказкой движка Unreal (Legacy).
+- Исправлена ссылка загрузки DLSS5 DX11 Bridge — старый репозиторий удалён; обновлено до `NIGos/dlss5-bridge` с правильным именем `dlss5-bridge.addon64`.
 
 ---
 
 ## v2.5.4
 
-### Changes
+### Изменения
 
-- Clicking "Check For Updates" now also triggers a silent auto-install pass immediately after the check completes, so any updates found are installed without needing a separate "Update All" click (when Automatic Updates is enabled).
-- Renamed "Export Profiles" / "Import Profiles" buttons in Settings to "Backup Profiles" / "Restore Profiles" for clarity.
-- ReBAR Enable now has three options: Auto (Default), Off, and On — reflecting the new driver setting (0x000BFA21). Previously only Off and On were available. Both the global Settings page and per-game overrides panel are updated.
+- Нажатие «Проверить обновления» теперь сразу после проверки запускает тихий проход автоустановки, так что найденные обновления ставятся без отдельного нажатия «Обновить всё» (когда включены автоматические обновления).
+- Кнопки «Экспорт профилей» / «Импорт профилей» в настройках переименованы в «Резервное копирование профилей» / «Восстановление профилей» для ясности.
+- У ReBAR Enable теперь три варианта: Auto (Default), Off и On — отражает новую настройку драйвера (0x000BFA21). Раньше были только Off и On. Обновлены и глобальная страница настроек, и панель поигровых переопределений.
 
-### Bug Fixes
+### Исправления
 
-- Fixed `nvngx_dlssnr.dll` not being removed from the game folder when uninstalling DLSS5 Tool. RHI now uses a sentinel file to track whether it placed the DLL, so it only removes what it deployed.
-- Fixed Automatic Updates setting reverting to Yes on restart when set to No.
-- Fixed addon downloads aborting entirely when one URL (e.g. the 32-bit variant) returns a 404 — remaining URLs now continue independently.
-- Fixed per-game addon selection being lost when switching the addon mode to Global and back.
-- Fixed pre-selected addons not re-downloading on launch if their staging files were missing.
+- Исправлено необновление `nvngx_dlssnr.dll` из папки игры при удалении DLSS5 Tool. RHI теперь отслеживает sentinel-файлом, размещал ли он DLL, и удаляет только своё.
+- Исправлен откат настройки автоматических обновлений на «Да» при перезапуске, если задано «Нет».
+- Исправлен полный обрыв загрузок аддонов, когда одна ссылка (например, 32-бит вариант) возвращала 404 — остальные ссылки теперь продолжаются независимо.
+- Исправлена потеря поигрового выбора аддонов при переключении режима аддонов в «Глобальные» и обратно.
+- Исправлено отсутствие повторного скачивания предвыбранных аддонов при запуске, если их файлы хранилища отсутствовали.
 
 ---
 
 ## v2.5.3
 
-### Bug Fixes
+### Исправления
 
-- Fixed `RenoDX DLSS5.addon64` still being deployed to game folders after v2.5.2. Per-game addon selections stored in `settings.json` still referenced the old name (`RenoDX DLSS5`) — these are now migrated to `DLSS5 Tool` on load. This is separate from the global addon list and stale file fixes in v2.5.2.
+- Исправлено продолжение развертывания `RenoDX DLSS5.addon64` в папки игр после v2.5.2. Поигровые выборы аддонов в `settings.json` всё ещё ссылались на старое имя («RenoDX DLSS5») — теперь при загрузке они мигрируют на «DLSS5 Tool». Это отдельная история от глобального списка аддонов и фиксов устаревших файлов в v2.5.2.
 
 ---
 
 ## v2.5.2
 
-### Bug Fixes
+### Исправления
 
-- Fixed `RenoDX DLSS5.addon64` being deployed to game folders on every launch due to a stale file left over from renaming the addon to DLSS5 Tool. RHI now removes it automatically on startup and cleans it up from all affected game folders, including per-game addon selections that still referenced the old name.
-- Fixed DLSS5 Tool and DLSS Tool (ShortFuse) being deployed as `.addon32` on 32-bit games, causing a ReShade load error. Both addons now always deploy as `.addon64`.
+- Исправлено развертывание `RenoDX DLSS5.addon64` в папки игр при каждом запуске из-за устаревшего файла, оставшегося от переименования аддона в DLSS5 Tool. RHI теперь удаляет его автоматически при запуске и чистит все затронутые папки игр, включая поигровые выборы аддонов, ещё ссылающиеся на старое имя.
+- Исправлено развертывание DLSS5 Tool и DLSS Tool (ShortFuse) как `.addon32` на 32-бит играх, вызывавшее ошибку загрузки ReShade. Оба аддона теперь всегда разворачиваются как `.addon64`.
 
 ---
 
 ## v2.5.1
 
-### Bug Fixes
+### Исправления
 
-- Fixed DLSS5 Tool addon not deploying to game folders after being selected. The internal package name change from "RenoDX DLSS5" to "DLSS5 Tool" was not reflected in all deploy paths.
-- Fixed stale `RenoDX DLSS5.addon64` file from the pre-rename version being re-deployed to games on every startup. RHI now removes it automatically on launch.
-- Fixed co-deployed DLSS and Streamline files not being cleaned up when switching away from DLSS Tool (ShortFuse). Files RHI placed are now fully restored or removed on deselect.
-- Fixed mutual exclusivity between DLSS5 Tool and DLSS Tool (ShortFuse) — selecting one now greys out the other in the addon picker.
+- Исправлено отсутствие развертывания аддона DLSS5 Tool в папки игр после его выбора. Смена внутреннего имени пакета с «RenoDX DLSS5» на «DLSS5 Tool» была отражена не во всех путях развертывания.
+- Исправлено повторное развертывание устаревшего файла `RenoDX DLSS5.addon64` из до-переименования в игры при каждом запуске. RHI теперь удаляет его автоматически при запуске.
+- Исправлена неочистка совместно развёрнутых файлов DLSS и Streamline при переключении с DLSS Tool (ShortFuse). Файлы, размещённые RHI, теперь полностью восстанавливаются или удаляются при снятии выбора.
+- Исправлена взаимоисключаемость DLSS5 Tool и DLSS Tool (ShortFuse) — выбор одного грейдает другой в окне выбора аддонов.
 
 ---
 
 ## v2.5.0
 
-### New
+### Новое
 
-- Added a search bar to the shader pack picker — filter by pack name or individual shader filename.
-- **DLSS Tool (ShortFuse)** — ShortFuse's DLSS5 addon is now in the addon picker as a second option alongside DLSS5 Tool. Supports DX12, DX11 and DX9 with HDR scaling. On install, RHI automatically downloads and deploys the newest DLSS SR, RR, FG, NR and Streamline files to the game folder. Supports RTX 20-50 Series. Still WIP — fall back to DLSS5 Tool if you have issues.
-- **Updated nvngx_dlssnr.dll** to ShortFuse's latest build, now supporting RTX 20, 30, 40 and 50 Series GPUs with identical performance to the original NVIDIA build on RTX 50 Series.
+- В окно выбора наборов шейдеров добавлена строка поиска — фильтр по имени набора или имени отдельного шейдера.
+- **DLSS Tool (ShortFuse)** — аддон DLSS5 от ShortFuse теперь в окне выбора аддонов как второй вариант рядом с DLSS5 Tool. Поддерживает DX12, DX11 и DX9 с HDR-масштабированием. При установке RHI автоматически скачивает и разворачивает свежие файлы DLSS SR, RR, FG, NR и Streamline в папку игры. Поддерживает RTX 20–50 серий. Пока в разработке — при проблемах откатывайтесь на DLSS5 Tool.
+- **Обновлён nvngx_dlssnr.dll** до свежей сборки ShortFuse с поддержкой GPU RTX 20, 30, 40 и 50 серий и производительностью, идентичной оригинальной сборке NVIDIA на RTX 50.
 
-### Changes
+### Изменения
 
-- Moved the Neural Rendering column to the far right of the Nvidia Profile section, after Streamline.
-- Renamed RenoDX DLSS5 addon to DLSS5 Tool. The current version is now shown next to the name in the addon picker.
+- Колонка Neural Rendering переехала в крайнее правое положение секции профиля NVIDIA, после Streamline.
+- Аддон RenoDX DLSS5 переименован в DLSS5 Tool. Текущая версия теперь показывается рядом с именем в окне выбора аддонов.
 
 ---
 
 ## v2.4.9
 
-### New
+### Новое
 
-- **nvngx_dlssnr.dll 310.8.SF** — a modified Neural Rendering DLL by ShortFuse that extends support to RTX 20, 30, 40 and 50 Series GPUs. This is now the default version RHI deploys. Shown as `310.8.1` in Windows Explorer, `310.8.SF` in RHI.
+- **nvngx_dlssnr.dll 310.8.SF** — модифицированная ShortFuse DLL Neural Rendering, расширяющая поддержку на GPU RTX 20, 30, 40 и 50 серий. Теперь версия по умолчанию для развертывания RHI. В Проводнике Windows показывается как `310.8.1`, в RHI — как `310.8.SF`.
 
-### Changes
+### Изменения
 
-- The Neural Rendering Deploy DLL button now also deploys `nvngx_dlss.dll` to the game folder alongside `nvngx_dlssnr.dll`. Any existing `nvngx_dlss.dll` is backed up as `.original` first.
-- Added an MOTD button to the status bar next to Patch Notes — click it to re-read the current message at any time.
+- Кнопка «Развернуть DLL» в Neural Rendering теперь разворачивает в папку игры и `nvngx_dlss.dll` вместе с `nvngx_dlssnr.dll`. Существующий `nvngx_dlss.dll` сначала резервируется как `.original`.
+- В строку статуса, рядом с Patch Notes, добавлена кнопка MOTD — нажмите, чтобы перечитать текущее сообщение в любой момент.
 
-### Manifest Updates
+### Обновления манифеста
 
-- Added Reshade Motion Estimation by JakobPCoder to the shader pack library — dense real-time optical flow motion estimation.
+- В библиотеку наборов шейдеров добавлен Reshade Motion Estimation от JakobPCoder — плотная оптическая оценка движения в реальном времени.
 
 ---
 
 ## v2.4.8
 
-### Bug Fixes
+### Исправления
 
-- Fixed "How to use" link not appearing in the per-game addon picker.
-- Fixed `renodx-dlss5.addon64` triggering an install prompt when double-clicked or drag-dropped. It is managed by RHI internally and should only be installed via the addon picker or placed in the Custom Addons folder.
+- Исправлено отсутствие ссылки «Как использовать» в поигровом окне выбора аддонов.
+- Исправлено появление запроса установки `renodx-dlss5.addon64` при двойном клике или перетаскивании. Он управляется RHI внутри и должен устанавливаться только через окно выбора аддонов или размещением в папке Custom Addons.
 
-### Manifest Updates
+### Обновления манифеста
 
-- Added DLSS5 DX11 Bridge and DLSS5 Feeder to the addon picker — both enable DLSS 5 Neural Rendering in D3D11 games. Additional setup steps are required; the How To Use button on each addon links to the repo for instructions.
-- Added DLSS5 Feeder companion shader to the shader pack library.
-- Fixed Metal Gear Solid 4 (Master Collection) showing as Unreal Engine — now correctly shows MGS4 Engine.
+- В окно выбора аддонов добавлены DLSS5 DX11 Bridge и DLSS5 Feeder — оба включают DLSS 5 Neural Rendering в D3D11-играх. Нужны дополнительные шаги настройки; кнопка «Как использовать» у каждого аддона ведёт на репозиторий с инструкциями.
+- В библиотеку наборов шейдеров добавлен компаньон-шейдер DLSS5 Feeder.
+- Исправлено показывание Metal Gear Solid 4 (Master Collection) как Unreal Engine — теперь корректно MGS4 Engine.
 
 ---
 
 ## v2.4.7
 
-### Bug Fixes
+### Исправления
 
-- Fixed the Neural Rendering column not showing `nvngx_dlssnr.dll` as installed after deploying it. It now updates immediately without needing a Refresh.
-- The Neural Rendering column now clearly shows "Custom" when a custom DLL is active.
+- Исправлено отсутствие показа `nvngx_dlssnr.dll` как установленного в колонке Neural Rendering после его развертывания. Обновляется сразу, без «Обновить».
+- Колонка Neural Rendering теперь явно показывает «Custom», когда активна своя DLL.
 
 ---
 
 ## v2.4.6
 
-### Bug Fixes
+### Исправления
 
-- Fixed RenoDX DLSS5 not auto-updating to games when a new version is released. The addon now deploys the updated file directly from its own staging folder and no longer creates a redundant copy in the addons folder.
+- Исправлено отсутствие автообновления RenoDX DLSS5 в играх при выходе новой версии. Аддон теперь разворачивает обновлённый файл прямо из собственного хранилища и больше не создаёт избыточную копию в папке аддонов.
 
-### Manifest Updates
+### Обновления манифеста
 
-- Added CubeLUT3Ddith by aron7awol to the shader pack library — Cube 3D LUT shader with dithering to reduce banding.
+- В библиотеку наборов шейдеров добавлен CubeLUT3Ddith от aron7awol — шейдер Cube 3D LUT с дизерингом против бандинга.
 
 ---
 
 ## v2.4.5
 
-### Bug Fixes
+### Исправления
 
-- Fixed RenoDX DLSS5 not deploying to game folders after the addons staging folder was deleted. The addon now deploys directly from its own staging location.
+- Исправлено отсутствие развертывания RenoDX DLSS5 в папки игр после удаления папки хранилища аддонов. Аддон теперь разворачивается прямо из собственного места хранения.
 
 ---
 
 ## v2.4.4
 
-### New
+### Новое
 
-- **RenoDX DLSS5 addon** — `renodx-dlss5.addon64` is now a first-class addon in the per-game addon picker, listed above RenoDX Upgrade. Enable it per game from the Addons combo → Select. RHI downloads it automatically, keeps it updated silently alongside other components, and deploys `nvngx_dlssnr.dll` to the game folder alongside it if not already present. For 50 Series GPUs only.
+- **Аддон RenoDX DLSS5** — `renodx-dlss5.addon64` теперь полноценный аддон в поигровом окне выбора аддонов, выше RenoDX Upgrade. Включается поигрово через список «Аддоны» → «Выбрать». RHI скачивает его автоматически, тихо поддерживает актуальность вместе с остальными компонентами и, если его ещё нет, разворачивает в папку игры `nvngx_dlssnr.dll`. Только для GPU 50-й серии.
diff --git a/RenoDXCommander/ReShade.Vulkan.ini b/RenoDXCommander/ReShade.Vulkan.ini
index ad8d7c6..51ca76f 100644
--- a/RenoDXCommander/ReShade.Vulkan.ini
+++ b/RenoDXCommander/ReShade.Vulkan.ini
@@ -1,4 +1,4 @@
-﻿[ADDON]
+[ADDON]
 DisabledAddons=Generic Depth,Effect Runtime Sync
 
 [GENERAL]
diff --git a/RenoDXCommander/ReShade.ini b/RenoDXCommander/ReShade.ini
index 0b3875f..c27161a 100644
--- a/RenoDXCommander/ReShade.ini
+++ b/RenoDXCommander/ReShade.ini
@@ -1,4 +1,4 @@
-﻿[ADDON]
+[ADDON]
 DisabledAddons=Generic Depth,Effect Runtime Sync
 
 [GENERAL]
diff --git a/RenoDXCommander/RenoDXCommander.csproj.user b/RenoDXCommander/RenoDXCommander.csproj.user
index 9d2254a..74ed93f 100644
--- a/RenoDXCommander/RenoDXCommander.csproj.user
+++ b/RenoDXCommander/RenoDXCommander.csproj.user
@@ -1,4 +1,4 @@
-﻿<?xml version="1.0" encoding="utf-8"?>
+<?xml version="1.0" encoding="utf-8"?>
 <Project ToolsVersion="Current" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
   <PropertyGroup>
     <_LastSelectedProfileId>C:\Users\Mark\OneDrive\Documents\RDXC\WIP\RDXC-1.4.7\RenoDXCommander\Properties\PublishProfiles\FolderProfile.pubxml</_LastSelectedProfileId>
diff --git a/RenoDXCommander/Services/AddonInfoResolver.cs b/RenoDXCommander/Services/AddonInfoResolver.cs
index f115c89..8c109bb 100644
--- a/RenoDXCommander/Services/AddonInfoResolver.cs
+++ b/RenoDXCommander/Services/AddonInfoResolver.cs
@@ -17,28 +17,28 @@ public class AddonInfoResolver
     // ── Generic fallback text constants (Tier 3) ──────────────────────────────────
 
     public const string FallbackREFramework =
-        "RE Framework is a modding framework for RE Engine games. It enables ReShade injection and other mods by hooking into the game's rendering pipeline.";
+        "RE Framework — мод-фреймворк для игр на RE Engine. Открывает возможность инъекции ReShade и других модов через подключение к конвейеру рендеринга игры.";
 
     public const string FallbackReShade =
-        "ReShade is a post-processing injector that adds visual effects to games. It is required by RenoDX to apply HDR tone mapping and color grading.";
+        "ReShade — инъектор пост-обработки, добавляющий в игры визуальные эффекты. Нужен RenoDX для применения HDR-тонмаппинга и цветокоррекции.";
 
     public const string FallbackRenoDX =
-        "RenoDX is an HDR mod framework that upgrades SDR games to HDR using ReShade. It provides per-game tone mapping and color space conversion.";
+        "RenoDX — фреймворк HDR-модов, превращающий SDR-игры в HDR через ReShade. Даёт тональное отображение и преобразование цветового пространства для каждой игры.";
 
     public const string FallbackNativeHdr =
         "This game is set to use UE-Extended. It is automatically configured to use native HDR and an Engine.ini will be automatically deployed upon installation. If there is an ingame HDR option then enable that too.";
 
     public const string FallbackReLimiter =
-        "ReLimiter is a frame limiter that works alongside ReShade to reduce input lag and improve frame pacing in games.";
+        "ReLimiter — ограничитель частоты кадров, работающий вместе с ReShade: снижает задержку ввода и делает вывод кадров в играх более плавным.";
 
     public const string FallbackDisplayCommander =
-        "Display Commander is a frame limiter that works alongside ReShade to reduce input lag and improve frame pacing.";
+        "Display Commander — ограничитель частоты кадров, работающий вместе с ReShade: снижает задержку ввода и делает вывод кадров более плавным.";
 
     public const string FallbackOptiScaler =
-        "OptiScaler is an upscaling compatibility layer that enables FSR, XeSS, or DLSS across different GPU vendors, allowing you to use any upscaler regardless of your hardware.";
+        "OptiScaler — слой совместимости апскейлинга, включающий FSR, XeSS или DLSS на GPU любых производителей: используйте любой апскейлер независимо от вашего железа.";
 
     public const string FallbackLuma =
-        "Luma is an alternative HDR mod framework that provides native HDR output for supported games, bypassing the need for ReShade-based injection.";
+        "Luma — альтернативный HDR-фреймворк, дающий нативный HDR-вывод в поддерживаемых играх без необходимости инъекции через ReShade.";
 
     /// <summary>
     /// Returns the generic fallback text for the given addon type.
@@ -146,10 +146,10 @@ public class AddonInfoResolver
         var source = GetSourceType(card, addon, manifest, osWikiData, hdrDatabase);
         return source switch
         {
-            InfoSourceType.Manifest => "Per-game notes available",
-            InfoSourceType.Wiki     => "Wiki info available",
-            InfoSourceType.Fallback => "General addon info",
-            _ => "General addon info"
+            InfoSourceType.Manifest => "Есть заметки по игре",
+            InfoSourceType.Wiki     => "Есть информация из вики",
+            InfoSourceType.Fallback => "Общие сведения об аддоне",
+            _ => "Общие сведения об аддоне"
         };
     }
 
@@ -301,7 +301,7 @@ public class AddonInfoResolver
         {
             Content = content,
             Url = card.NameUrl,
-            UrlLabel = !string.IsNullOrEmpty(card.NameUrl) ? "View wiki page" : null,
+            UrlLabel = !string.IsNullOrEmpty(card.NameUrl) ? "Открыть страницу вики" : null,
             Source = InfoSourceType.Wiki,
             WikiStatusLabel = card.WikiStatusLabel,
             WikiStatusBadgeBg = card.WikiStatusBadgeBackground,
@@ -344,7 +344,7 @@ public class AddonInfoResolver
         {
             Content = content,
             Url = url,
-            UrlLabel = url != null ? "View wiki page" : null,
+            UrlLabel = url != null ? "Открыть страницу вики" : null,
             Source = InfoSourceType.Wiki,
             OptiScalerCompat = stdEntry,
             OptiScalerFsr4Compat = fsr4Entry
@@ -382,7 +382,7 @@ public class AddonInfoResolver
         {
             var upscalers = stdEntry.Upscalers.Count > 0
                 ? string.Join(", ", stdEntry.Upscalers)
-                : "None listed";
+                : "Не указано";
             parts.Add($"OptiScaler Compatibility: {stdEntry.Status}\nUpscalers: {upscalers}");
             if (!string.IsNullOrWhiteSpace(stdEntry.Notes))
                 parts.Add(stdEntry.Notes);
@@ -392,7 +392,7 @@ public class AddonInfoResolver
         {
             var upscalers = fsr4Entry.Upscalers.Count > 0
                 ? string.Join(", ", fsr4Entry.Upscalers)
-                : "None listed";
+                : "Не указано";
             parts.Add($"FSR4 Compatibility: {fsr4Entry.Status}\nUpscalers: {upscalers}");
             if (!string.IsNullOrWhiteSpace(fsr4Entry.Notes))
                 parts.Add(fsr4Entry.Notes);
diff --git a/RenoDXCommander/Services/AddonPackService.cs b/RenoDXCommander/Services/AddonPackService.cs
index 80fd994..b36fd96 100644
--- a/RenoDXCommander/Services/AddonPackService.cs
+++ b/RenoDXCommander/Services/AddonPackService.cs
@@ -41,7 +41,7 @@ public class AddonPackService : IAddonPackService
     private static readonly AddonEntry RenoDxDevKitEntry = new(
         SectionId: "renodx-devkit",
         PackageName: "RenoDX DevKit",
-        PackageDescription: "RenoDX development tools addon for ReShade",
+        PackageDescription: "Аддон инструментов разработки RenoDX для ReShade",
         DownloadUrl: null,
         DownloadUrl32: "https://github.com/clshortfuse/renodx/releases/download/snapshot/renodx-devkit.addon32",
         DownloadUrl64: "https://github.com/clshortfuse/renodx/releases/download/snapshot/renodx-devkit.addon64",
@@ -64,7 +64,7 @@ public class AddonPackService : IAddonPackService
     private static readonly AddonEntry Renodx5SfEntry = new(
         SectionId: "renodx-dlss-sf",
         PackageName: "DLSS Tool (ShortFuse)",
-        PackageDescription: "ShortFuse's DLSS5 addon. Supports DX12, DX11 and DX9. For non-DLSS games enable Load DLSS Libraries and set Hook Method to On Present. Supports RTX 20-50 Series. Still WIP — fall back to DLSS5 Tool if you have issues.",
+        PackageDescription: "Аддон DLSS5 от ShortFuse. Поддерживает DX12, DX11 и DX9. Для игр без DLSS включите Load DLSS Libraries и задайте Hook Method = On Present. Поддерживает RTX 20–50 серий. Пока в разработке — при проблемах используйте DLSS5 Tool.",
         DownloadUrl: null,
         DownloadUrl32: null,
         DownloadUrl64: null,
@@ -76,7 +76,7 @@ public class AddonPackService : IAddonPackService
     private static readonly AddonEntry DlssFixEntry = new(
         SectionId: "renodx-dlssfix",
         PackageName: "DLSS Fix",
-        PackageDescription: "Makes ReShade draw on native game frames instead of frame gen frames. Also hides DLSS upscaling from ReShade.",
+        PackageDescription: "Заставляет ReShade работать с нативными кадрами игры вместо сгенерированных. Также скрывает масштабирование DLSS от ReShade.",
         DownloadUrl: "https://github.com/clshortfuse/renodx/releases/download/snapshot/renodx-dlssfix.addon64",
         DownloadUrl32: null,
         DownloadUrl64: "https://github.com/clshortfuse/renodx/releases/download/snapshot/renodx-dlssfix.addon64",
@@ -441,13 +441,13 @@ public class AddonPackService : IAddonPackService
             if (entry.SectionId.Equals("renodx-dlss5", StringComparison.OrdinalIgnoreCase)
                 || entry.PackageName.Equals("DLSS5 Tool", StringComparison.OrdinalIgnoreCase))
             {
-                progress?.Report(("Downloading RenoDX DLSS5 addon...", 10));
+                progress?.Report(("Загрузка аддона RenoDX DLSS5...", 10));
                 var rdx5Service = App.Services.GetRequiredService<Renodx5AddonService>();
                 await rdx5Service.EnsureStagingAsync(progress).ConfigureAwait(false);
                 if (!rdx5Service.IsStagingReady)
                 {
                     CrashReporter.Log("[AddonPackService.DownloadAddonAsync] RenoDX DLSS5 staging not ready after EnsureStaging");
-                    progress?.Report(("RenoDX DLSS5 download failed", 0));
+                    progress?.Report(("Не удалось скачать RenoDX DLSS5", 0));
                 }
                 return;
             }
@@ -455,13 +455,13 @@ public class AddonPackService : IAddonPackService
             // SF variant — route to Renodx5AddonService.EnsureSfStagingAsync
             if (entry.SectionId.Equals("renodx-dlss-sf", StringComparison.OrdinalIgnoreCase))
             {
-                progress?.Report(("Downloading DLSS Tool (ShortFuse)...", 10));
+                progress?.Report(("Загрузка DLSS Tool (ShortFuse)...", 10));
                 var rdx5Service = App.Services.GetRequiredService<Renodx5AddonService>();
                 await rdx5Service.EnsureSfStagingAsync(progress).ConfigureAwait(false);
                 if (!rdx5Service.IsSfStagingReady)
                 {
                     CrashReporter.Log("[AddonPackService.DownloadAddonAsync] SF staging not ready after EnsureStaging");
-                    progress?.Report(("DLSS Tool (ShortFuse) download failed", 0));
+                    progress?.Report(("Не удалось скачать DLSS Tool (ShortFuse)", 0));
                 }
                 return;
             }
@@ -476,7 +476,7 @@ public class AddonPackService : IAddonPackService
                 if (resolvedUrl == null)
                 {
                     CrashReporter.Log($"[AddonPackService.DownloadAddonAsync] Could not resolve asset URL from API for '{entry.PackageName}'");
-                    progress?.Report(($"❌ Failed to resolve download URL for {entry.PackageName}", 0));
+                    progress?.Report(($"❌ Не удалось определить ссылку на скачивание для {entry.PackageName}", 0));
                     return;
                 }
                 // Use the tag as the version token unless the caller already supplied one
@@ -565,7 +565,7 @@ public class AddonPackService : IAddonPackService
 
             if (!anySucceeded)
             {
-                progress?.Report(($"❌ Download failed for {entry.PackageName}", 0));
+                progress?.Report(($"❌ Не удалось скачать {entry.PackageName}", 0));
                 return;
             }
 
@@ -573,13 +573,13 @@ public class AddonPackService : IAddonPackService
             versionToken ??= "unknown";
             SaveAddonVersion(entry.PackageName, versionToken, safeName);
 
-            progress?.Report(($"{entry.PackageName} downloaded.", 100));
+            progress?.Report(($"{entry.PackageName} скачано.", 100));
             CrashReporter.Log($"[AddonPackService.DownloadAddonAsync] '{entry.PackageName}' download complete. Version = {versionToken}");
         }
         catch (Exception ex)
         {
             CrashReporter.Log($"[AddonPackService.DownloadAddonAsync] Failed for '{entry.PackageName}' — {ex.Message}");
-            progress?.Report(($"❌ Download failed: {ex.Message}", 0));
+            progress?.Report(($"❌ Ошибка загрузки: {ex.Message}", 0));
         }
     }
 
@@ -932,7 +932,7 @@ public class AddonPackService : IAddonPackService
             results.Add(new AddonEntry(
                 SectionId: $"custom-{baseName}",
                 PackageName: baseName,
-                PackageDescription: "Custom addon (local file)",
+                PackageDescription: "Свой аддон (локальный файл)",
                 DownloadUrl: null,
                 DownloadUrl32: null,
                 DownloadUrl64: null,
diff --git a/RenoDXCommander/Services/AuxInstallService.GacSymlink.cs b/RenoDXCommander/Services/AuxInstallService.GacSymlink.cs
index dac6240..58f6b1d 100644
--- a/RenoDXCommander/Services/AuxInstallService.GacSymlink.cs
+++ b/RenoDXCommander/Services/AuxInstallService.GacSymlink.cs
@@ -29,7 +29,7 @@ public partial class AuxInstallService
 
         if (!File.Exists(rsStagedPath))
             throw new FileNotFoundException(
-                $"ReShade DLL not found in staging directory.\nExpected: {rsStagedPath}");
+                $"DLL ReShade не найдена в папке загрузок.\nОжидалась: {rsStagedPath}");
 
         File.Copy(rsStagedPath, stagedDllPath, overwrite: true);
 
diff --git a/RenoDXCommander/Services/AuxInstallService.Install.cs b/RenoDXCommander/Services/AuxInstallService.Install.cs
index 9deb505..6fa9ddc 100644
--- a/RenoDXCommander/Services/AuxInstallService.Install.cs
+++ b/RenoDXCommander/Services/AuxInstallService.Install.cs
@@ -65,7 +65,7 @@ public partial class AuxInstallService
         }
 
         // ── Ensure staged DLLs exist (downloaded from reshade.me) ────────────────
-        progress?.Report(("Preparing ReShade files...", 10));
+        progress?.Report(("Подготовка файлов ReShade...", 10));
         EnsureReShadeStaging();
 
         var effectiveChannel = channel ?? ChannelStable;
@@ -99,29 +99,29 @@ public partial class AuxInstallService
         {
             if (string.Equals(effectiveChannel, ChannelCustom, StringComparison.OrdinalIgnoreCase))
                 throw new FileNotFoundException(
-                    $"Custom ReShade DLL not found.\n" +
+                    $"Пользовательская DLL ReShade не найдена.\n" +
                     $"Expected: {rsStagedPath}\n" +
                     $"Place your {(use32Bit ? "ReShade32.dll" : "ReShade64.dll")} in:\n" +
                     $"{DlssStreamlineService.RsCustomDir}");
 
             throw new FileNotFoundException(
-                $"ReShade DLLs not found in staging directory.\n" +
+                $"DLL ReShade не найдены в папке загрузок.\n" +
                 $"Expected: {rsStagedPath}\n" +
-                $"Please restart RHI to download ReShade from reshade.me.");
+                $"Перезапустите RHI, чтобы скачать ReShade с reshade.me.");
         }
 
         // ── Back up foreign DLL at destination ──────────────────────────────────
         BackupForeignDll(destPath);
 
         // ── Copy staged DLL to game folder ────────────────────────────────────────
-        progress?.Report(("Installing ReShade...", 80));
+        progress?.Report(("Установка ReShade...", 80));
         File.Copy(rsStagedPath, destPath, overwrite: true);
 
         // Deploy reshade.ini alongside the DLL (skip if caller has locked ini updates for this game).
         if (mergeIni && File.Exists(RsIniPath))
             MergeRsIni(installPath, screenshotSavePath, overlayHotkey, screenshotHotkey, gameName);
 
-        progress?.Report(("ReShade installed!", 100));
+        progress?.Report(("ReShade установлен!", 100));
 
         // ── Shader deployment ─────────────────────────────────────────────────────
         // Build exclusions off the calling thread to avoid blocking the UI thread
diff --git a/RenoDXCommander/Services/AuxInstallService.cs b/RenoDXCommander/Services/AuxInstallService.cs
index b4d14a1..d39eb12 100644
--- a/RenoDXCommander/Services/AuxInstallService.cs
+++ b/RenoDXCommander/Services/AuxInstallService.cs
@@ -494,7 +494,7 @@ public partial class AuxInstallService : IAuxInstallService, IAuxFileService
                 return false;
             }
 
-            progress?.Report(($"ReShade {version} ready!", 100));
+            progress?.Report(($"ReShade {version} готов!", 100));
             CrashReporter.Log($"[AuxInstallService.DownloadLegacyReShadeAsync] v{version} staged successfully");
             return true;
         }
diff --git a/RenoDXCommander/Services/CrashReporter.cs b/RenoDXCommander/Services/CrashReporter.cs
index f662977..fadedc8 100644
--- a/RenoDXCommander/Services/CrashReporter.cs
+++ b/RenoDXCommander/Services/CrashReporter.cs
@@ -57,7 +57,7 @@ public static class CrashReporter
             var timestamp = DateTime.Now.ToString("yyyy-MM-dd_HH-mm-ss");
             SessionLogPath = Path.Combine(LogDir, $"session_{timestamp}.txt");
             File.WriteAllText(SessionLogPath,
-                $"═══ RHI v{AppVersion} — Session started {DateTime.Now:yyyy-MM-dd HH:mm:ss} ═══{Environment.NewLine}",
+                $"═══ RHI v{AppVersion} — сеанс начат {DateTime.Now:yyyy-MM-dd HH:mm:ss} ═══{Environment.NewLine}",
                 Encoding.UTF8);
         }
         catch { SessionLogPath = Path.Combine(LogDir, "session_fallback.txt"); }
@@ -74,7 +74,7 @@ public static class CrashReporter
         {
             _verboseLogging = value;
             if (value)
-                Log("Verbose logging enabled");
+                Log("Подробное журналирование включено");
         }
     }
 
@@ -145,14 +145,14 @@ public static class CrashReporter
             var ex = e.ExceptionObject as Exception;
             WriteCrashReport("AppDomain.UnhandledException", ex,
                 isTerminating: e.IsTerminating,
-                note: e.IsTerminating ? "Process is terminating." : null);
+                note: e.IsTerminating ? "Процесс завершается." : null);
         };
 
         // 2. Unobserved Task exceptions (async void, fire-and-forget Tasks)
         TaskScheduler.UnobservedTaskException += (_, e) =>
         {
             WriteCrashReport("TaskScheduler.UnobservedTaskException", e.Exception,
-                note: "Task exception was unobserved. Marking as observed to prevent crash.");
+                note: "Необработанное исключение задачи. Помечаю как обработанное, чтобы избежать сбоя.");
             e.SetObserved(); // Prevent the process from being killed
         };
 
@@ -164,7 +164,7 @@ public static class CrashReporter
             e.Handled = true; // Try to keep the app alive
         };
 
-        Log("CrashReporter registered.");
+        Log("Отчётчик сбоев зарегистрирован.");
     }
 
     // ── Report writer ─────────────────────────────────────────────────────────────
@@ -192,18 +192,18 @@ public static class CrashReporter
 
             // ── Header ──────────────────────────────────────────────────────────
             sb.AppendLine("═══════════════════════════════════════════════════════════════");
-            sb.AppendLine("  RenoDX Mod Manager — Error / Crash Report");
+            sb.AppendLine("  RenoDX Mod Manager — отчёт об ошибке / сбое");
             sb.AppendLine("═══════════════════════════════════════════════════════════════");
             sb.AppendLine();
 
             // ── Basic info ───────────────────────────────────────────────────────
-            sb.AppendLine($"Timestamp    : {timestamp:yyyy-MM-dd HH:mm:ss} (local)");
-            sb.AppendLine($"App version  : {AppVersion}");
+            sb.AppendLine($"Время: {timestamp:yyyy-MM-dd HH:mm:ss} (местное)");
+            sb.AppendLine($"Версия приложения: {AppVersion}");
             sb.AppendLine($"Source       : {source}");
             sb.AppendLine($"Terminating  : {isTerminating}");
             sb.AppendLine($"OS           : {Environment.OSVersion}");
             sb.AppendLine($"Architecture : {RuntimeInformation()}");
-            sb.AppendLine($".NET runtime : {Environment.Version}");
+            sb.AppendLine($"Среда .NET: {Environment.Version}");
             sb.AppendLine($"Machine      : {Environment.MachineName}");
 
             if (note != null)
@@ -215,12 +215,12 @@ public static class CrashReporter
             // ── Exception chain ──────────────────────────────────────────────────
             sb.AppendLine();
             sb.AppendLine("───────────────────────────────────────────────────────────────");
-            sb.AppendLine("  Exception Details");
+            sb.AppendLine("  Подробности исключения");
             sb.AppendLine("───────────────────────────────────────────────────────────────");
 
             if (ex == null)
             {
-                sb.AppendLine("(No exception object available)");
+                sb.AppendLine("(Объект исключения недоступен)");
             }
             else
             {
@@ -230,13 +230,13 @@ public static class CrashReporter
             // ── Breadcrumb trail ─────────────────────────────────────────────────
             sb.AppendLine();
             sb.AppendLine("───────────────────────────────────────────────────────────────");
-            sb.AppendLine("  Recent Activity Log (newest last)");
+            sb.AppendLine("  Журнал недавних событий (новые — внизу)");
             sb.AppendLine("───────────────────────────────────────────────────────────────");
 
             var crumbs = _breadcrumbs.ToArray();
             if (crumbs.Length == 0)
             {
-                sb.AppendLine("(no breadcrumbs recorded)");
+                sb.AppendLine("(трассировка не записана)");
             }
             else
             {
@@ -247,7 +247,7 @@ public static class CrashReporter
             // ── Loaded assemblies (helps detect version conflicts) ───────────────
             sb.AppendLine();
             sb.AppendLine("───────────────────────────────────────────────────────────────");
-            sb.AppendLine("  Loaded Assemblies");
+            sb.AppendLine("  Загруженные сборки");
             sb.AppendLine("───────────────────────────────────────────────────────────────");
             try
             {
@@ -258,17 +258,17 @@ public static class CrashReporter
                     sb.AppendLine($"  {name.Name,-50} {name.Version}");
                 }
             }
-            catch { sb.AppendLine("(could not enumerate assemblies)"); }
+            catch { sb.AppendLine("(не удалось перечислить сборки)"); }
 
             sb.AppendLine();
             sb.AppendLine("═══════════════════════════════════════════════════════════════");
-            sb.AppendLine($"  End of report — {fileName}");
+            sb.AppendLine($"  Конец отчёта — {fileName}");
             sb.AppendLine("═══════════════════════════════════════════════════════════════");
 
             File.WriteAllText(filePath, sb.ToString(), Encoding.UTF8);
 
             // Also log that we wrote the report so the next crash knows about this one
-            Log($"Crash report written: {fileName}");
+            Log($"Отчёт о сбое записан: {fileName}");
         }
         catch
         {
@@ -281,7 +281,7 @@ public static class CrashReporter
     private static void AppendException(StringBuilder sb, Exception ex, int depth)
     {
         var indent = new string(' ', depth * 4);
-        var label  = depth == 0 ? "Exception" : "Inner Exception";
+        var label  = depth == 0 ? "Exception" : "Внутреннее исключение";
 
         sb.AppendLine($"{indent}{label}  : {ex.GetType().FullName}");
         sb.AppendLine($"{indent}Message   : {ex.Message}");
@@ -291,14 +291,14 @@ public static class CrashReporter
 
         if (ex.StackTrace != null)
         {
-            sb.AppendLine($"{indent}Stack trace:");
+            sb.AppendLine($"{indent}Трассировка стека:");
             foreach (var line in ex.StackTrace.Split('\n'))
                 sb.AppendLine($"{indent}  {line.TrimEnd()}");
         }
 
         if (ex is AggregateException agg)
         {
-            sb.AppendLine($"{indent}Aggregate inner exceptions ({agg.InnerExceptions.Count}):");
+            sb.AppendLine($"{indent}Вложенные исключения ({agg.InnerExceptions.Count}):");
             for (int i = 0; i < agg.InnerExceptions.Count; i++)
             {
                 sb.AppendLine($"{indent}  [{i}]");
diff --git a/RenoDXCommander/Services/DlssEnablerService.cs b/RenoDXCommander/Services/DlssEnablerService.cs
index f4f73f2..962b779 100644
--- a/RenoDXCommander/Services/DlssEnablerService.cs
+++ b/RenoDXCommander/Services/DlssEnablerService.cs
@@ -94,7 +94,7 @@ public class DlssEnablerService
         }
 
         Directory.CreateDirectory(_stagingDir);
-        progress?.Report(("Downloading DLSS Enabler...", 10));
+        progress?.Report(("Загрузка DLSS Enabler...", 10));
 
         var (version, downloadUrl) = await FetchLatestReleaseInfoAsync().ConfigureAwait(false);
         if (string.IsNullOrEmpty(version) || string.IsNullOrEmpty(downloadUrl))
@@ -103,7 +103,7 @@ public class DlssEnablerService
             return;
         }
 
-        progress?.Report(("Downloading DLSS Enabler...", 30));
+        progress?.Report(("Загрузка DLSS Enabler...", 30));
 
         try
         {
@@ -144,11 +144,11 @@ public class DlssEnablerService
         catch (Exception ex)
         {
             _crashReporter.Log($"[DlssEnablerService.EnsureStagingAsync] Download failed ({downloadUrl}) — {ex.Message}");
-            progress?.Report(($"DLSS Enabler download failed: {ex.Message}", 0));
+            progress?.Report(($"Не удалось скачать DLSS Enabler: {ex.Message}", 0));
             return;
         }
 
-        progress?.Report(("DLSS Enabler ready", 90));
+        progress?.Report(("DLSS Enabler готов", 90));
 
         // ── Auto-deploy to all games where DLSS Enabler is enabled ────────────
         try
@@ -185,7 +185,7 @@ public class DlssEnablerService
             _crashReporter.Log($"[DlssEnablerService.EnsureStagingAsync] Auto-deploy loop failed — {ex.Message}");
         }
 
-        progress?.Report(("DLSS Enabler ready", 100));
+        progress?.Report(("DLSS Enabler готов", 100));
     }
 
     // ── Install / Uninstall / Detection ───────────────────────────────────────
diff --git a/RenoDXCommander/Services/DlssPresetService.DriverSettings.cs b/RenoDXCommander/Services/DlssPresetService.DriverSettings.cs
index 435a6ec..192ae07 100644
--- a/RenoDXCommander/Services/DlssPresetService.DriverSettings.cs
+++ b/RenoDXCommander/Services/DlssPresetService.DriverSettings.cs
@@ -51,9 +51,9 @@ public partial class DlssPresetService
 
     public static readonly (string Name, uint Value)[] VSyncModeOptions =
     [
-        ("App Controlled", 0x60925292),
-        ("Force Off", 0x08416747),
-        ("Force On", 0x47814940),
+        ("Управляется игрой", 0x60925292),
+        ("Принудительно выкл", 0x08416747),
+        ("Принудительно вкл", 0x47814940),
         ("Fast Sync", 0x18888888),
     ];
 
@@ -67,16 +67,16 @@ public partial class DlssPresetService
 
     public static readonly (string Name, uint Value)[] PowerManagementOptions =
     [
-        ("Optimal Performance", 0x00000005),
+        ("Оптимальная производительность", 0x00000005),
         ("Adaptive", 0x00000000),
-        ("Maximum Performance", 0x00000001),
+        ("Максимальная производительность", 0x00000001),
     ];
 
     public static readonly (string Name, uint Value)[] CpuExprModeOptions =
     [
-        ("Mode 0 (Default)", 0x00000000),
-        ("Mode 1", 0x00000001),
-        ("Mode 4", 0x00000004),
+        ("Режим 0 (по умолчанию)", 0x00000000),
+        ("Режим 1", 0x00000001),
+        ("Режим 4", 0x00000004),
     ];
 
     // ── Latency option arrays ─────────────────────────────────────────────────
@@ -94,7 +94,7 @@ public partial class DlssPresetService
     public static readonly (string Name, uint Value)[] SmoothMotionEnableOptions =
     [
         ("Off", 0x00000000),
-        ("On [40 Series+]", 0x00000001),
+        ("Вкл [40-я серия и новее]", 0x00000001),
     ];
 
     public static readonly (string Name, uint Value)[] SmoothMotionApisOptions =
@@ -106,20 +106,20 @@ public partial class DlssPresetService
         ("VK", 0x00000004),
         ("DX12, VK", 0x00000005),
         ("DX11, VK", 0x00000006),
-        ("All [DX11/12, VK]", 0x00000007),
+        ("Все [DX11/12, VK]", 0x00000007),
     ];
 
     public static readonly (string Name, uint Value)[] SmoothMotionFlipPacingFsOptions =
     [
-        ("Off [Latency]", 0x00000000),
-        ("On [Pacing]", 0xFFFFFFFF),
+        ("Выкл [Latency]", 0x00000000),
+        ("Вкл [Pacing]", 0xFFFFFFFF),
     ];
 
     public static readonly (string Name, uint Value)[] SmoothMotionFlipPacingWinOptions =
     [
-        ("Off [Latency]", 0x00000000),
-        ("On [Pacing]", 0x00000001),
-        ("Force On [Pacing]", 0xFFFFFFFF),
+        ("Выкл [Latency]", 0x00000000),
+        ("Вкл [Pacing]", 0x00000001),
+        ("Принудительно вкл [Pacing]", 0xFFFFFFFF),
     ];
 
     // ── Shader Cache option arrays ────────────────────────────────────────────
@@ -132,7 +132,7 @@ public partial class DlssPresetService
         ("5GB", 0x00001400),
         ("10GB", 0x00002800),
         ("12GB", 0x00003000),
-        ("16GB (Default)", 0x00004000),
+        ("16 ГБ (по умолчанию)", 0x00004000),
         ("24GB", 0x00006000),
         ("32GB", 0x00008000),
         ("48GB", 0x0000C000),
@@ -144,7 +144,7 @@ public partial class DlssPresetService
     public static readonly (string Name, uint Value)[] ShaderPrecompileOptions =
     [
         ("Off", 0x00000000),
-        ("Low (Default)", 0x00000001),
+        ("Низкое (по умолчанию)", 0x00000001),
         ("Medium", 0x00000002),
         ("High", 0x00000003),
     ];
@@ -154,14 +154,14 @@ public partial class DlssPresetService
     public static readonly (string Name, uint Value)[] GSyncModeOptions =
     [
         ("Off", 0x00000000),
-        ("Fullscreen only", 0x00000001),
+        ("Только полноэкранный режим", 0x00000001),
         ("Fullscreen/Windowed", 0x00000002),
     ];
 
     public static readonly (string Name, uint Value)[] PreferredRefreshRateOptions =
     [
-        ("App Setting", 0x00000000),
-        ("Highest available", 0x00000001),
+        ("Настройка приложения", 0x00000000),
+        ("Наивысшая доступная", 0x00000001),
     ];
 
     public static readonly (string Name, uint Value)[] GSyncEnableOptions =
@@ -194,16 +194,16 @@ public partial class DlssPresetService
     public static readonly (string Name, uint Value)[] DmfgFrameCountOptions =
     [
         ("Off", 0x00000000),
-        ("3x [50 Series+]", 0x00000002),
-        ("4x [50 Series+]", 0x00000003),
-        ("5x [50 Series+]", 0x00000004),
-        ("6x [50 Series+]", 0x00000005),
+        ("3x [серия 50 и выше]", 0x00000002),
+        ("4x [серия 50 и выше]", 0x00000003),
+        ("5x [серия 50 и выше]", 0x00000004),
+        ("6x [серия 50 и выше]", 0x00000005),
     ];
 
     public static readonly (string Name, uint Value)[] DmfgTargetFpsOptions =
     [
         ("Off", 0x00000000),
-        ("Max Refresh Rate", 0x01000000),
+        ("Максимальная частота обновления", 0x01000000),
         ("59 FPS (60Hz VRR Cap)", 59),
         ("73 FPS (75Hz VRR Cap)", 73),
         ("97 FPS (100Hz VRR Cap)", 97),
diff --git a/RenoDXCommander/Services/DlssPresetService.ReBar.cs b/RenoDXCommander/Services/DlssPresetService.ReBar.cs
index 126723f..6d1ea56 100644
--- a/RenoDXCommander/Services/DlssPresetService.ReBar.cs
+++ b/RenoDXCommander/Services/DlssPresetService.ReBar.cs
@@ -72,7 +72,7 @@ public partial class DlssPresetService
     public static readonly (string Name, ulong Value)[] ReBarSizeLimits =
     [
         ("512MB", 0x0000000020000000),
-        ("1GB (Default)", 0x0000000040000000),
+        ("1 ГБ (по умолчанию)", 0x0000000040000000),
         ("1.5GB", 0x0000000060000000),
         ("2GB", 0x0000000080000000),
         ("4GB", 0x0000000100000000),
diff --git a/RenoDXCommander/Services/DlssStreamlineService.cs b/RenoDXCommander/Services/DlssStreamlineService.cs
index c46e894..769306a 100644
--- a/RenoDXCommander/Services/DlssStreamlineService.cs
+++ b/RenoDXCommander/Services/DlssStreamlineService.cs
@@ -189,7 +189,7 @@ public partial class DlssStreamlineService : IDlssStreamlineService
 
                 CrashReporter.Log($"[DlssStreamlineService.FetchManifestAsync] Loaded: " +
                     $"{_manifest?.Dlss?.Count ?? 0} SR, {_manifest?.Dlssd?.Count ?? 0} RR, " +
-                    $"{_manifest?.Dlssg?.Count ?? 0} FG, {_manifest?.Dlssnr?.Count ?? 0} NR, {_manifest?.Streamline?.Count ?? 0} SL versions");
+                    $"версий: {_manifest?.Dlssg?.Count ?? 0} FG, {_manifest?.Dlssnr?.Count ?? 0} NR, {_manifest?.Streamline?.Count ?? 0} SL");
             }
         }
         catch (Exception ex)
diff --git a/RenoDXCommander/Services/Dlssg20_30Service.cs b/RenoDXCommander/Services/Dlssg20_30Service.cs
index da343e1..401dc10 100644
--- a/RenoDXCommander/Services/Dlssg20_30Service.cs
+++ b/RenoDXCommander/Services/Dlssg20_30Service.cs
@@ -27,8 +27,8 @@ public class Dlssg20_30Service
     public const string StagedDllName = "version.dll";
     public const string IniFileName   = "dlssg_sm86.ini";
 
-    public const string GpuGenRtx30 = "RTX 30 Series";
-    public const string GpuGenRtx20 = "RTX 20 Series";
+    public const string GpuGenRtx30 = "Серия RTX 30";
+    public const string GpuGenRtx20 = "Серия RTX 20";
 
     /// <summary>All valid proxy DLL names (version.dll default + altnative folder).</summary>
     public static readonly string[] ProxyNames =
@@ -100,11 +100,11 @@ public class Dlssg20_30Service
             return;
         }
 
-        progress?.Report(("Downloading 20/30 FG Unlock DLL...", 20));
+        progress?.Report(("Загрузка DLL разблокировки FG 20/30...", 20));
         if (!await DownloadFileAsync(DllRawUrl, Path.Combine(_stagingDir, StagedDllName)).ConfigureAwait(false))
             return;
 
-        progress?.Report(("Downloading INI...", 60));
+        progress?.Report(("Загрузка INI...", 60));
         if (!await DownloadFileAsync(IniRawUrl, Path.Combine(_stagingDir, IniFileName)).ConfigureAwait(false))
             return;
 
@@ -112,7 +112,7 @@ public class Dlssg20_30Service
         LatestVersion = sha;
         HasUpdate = false;
         _crashReporter.Log($"[Dlssg20_30Service.EnsureStaging] Staged commit {sha}");
-        progress?.Report(("20/30 FG Unlock ready", 100));
+        progress?.Report(("Разблокировка FG 20/30 готова", 100));
     }
 
     // ── Install ───────────────────────────────────────────────────────────────
diff --git a/RenoDXCommander/Services/DofFixService.cs b/RenoDXCommander/Services/DofFixService.cs
index 71ffb56..832ef17 100644
--- a/RenoDXCommander/Services/DofFixService.cs
+++ b/RenoDXCommander/Services/DofFixService.cs
@@ -84,7 +84,7 @@ public class DofFixService : IDofFixService
         }
 
         Directory.CreateDirectory(_stagingDir);
-        progress?.Report(("Downloading DOF Fix...", 10));
+        progress?.Report(("Загрузка DOF Fix...", 10));
 
         var (version, downloadUrl, body) = await FetchLatestReleaseInfoAsync().ConfigureAwait(false);
         if (string.IsNullOrEmpty(version) || string.IsNullOrEmpty(downloadUrl))
@@ -93,7 +93,7 @@ public class DofFixService : IDofFixService
             return;
         }
 
-        progress?.Report(("Downloading DOF Fix...", 30));
+        progress?.Report(("Загрузка DOF Fix...", 30));
 
         try
         {
@@ -110,7 +110,7 @@ public class DofFixService : IDofFixService
             _crashReporter.Log($"[DofFixService.EnsureStagingAsync] Download failed ({downloadUrl}) — {ex.Message}");
         }
 
-        progress?.Report(("DOF Fix ready", 100));
+        progress?.Report(("DOF Fix готов", 100));
     }
 
     /// <summary>
@@ -142,7 +142,7 @@ public class DofFixService : IDofFixService
         await EnsureStagingAsync(progress).ConfigureAwait(false);
         if (!IsStagingReady) return false;
 
-        progress?.Report(("Deploying DOF Fix...", 70));
+        progress?.Report(("Развертывание DOF Fix...", 70));
 
         var src = Path.Combine(_stagingDir, AddonFileName);
         var dest = Path.Combine(installPath, AddonFileName);
@@ -150,7 +150,7 @@ public class DofFixService : IDofFixService
         {
             File.Copy(src, dest, overwrite: true);
             _crashReporter.Log($"[DofFixService.InstallAsync] Deployed to {installPath}");
-            progress?.Report(("DOF Fix installed!", 100));
+            progress?.Report(("DOF Fix установлен!", 100));
             return true;
         }
         catch (Exception ex)
diff --git a/RenoDXCommander/Services/DxvkService.Install.cs b/RenoDXCommander/Services/DxvkService.Install.cs
index a719ac4..f75d942 100644
--- a/RenoDXCommander/Services/DxvkService.Install.cs
+++ b/RenoDXCommander/Services/DxvkService.Install.cs
@@ -35,7 +35,7 @@ public partial class DxvkService
     {
         try
         {
-            progress?.Report(("Preparing DXVK install...", 5));
+            progress?.Report(("Подготовка установки DXVK...", 5));
 
             // ── 1. Ensure staging is ready ───────────────────────────────
             if (!IsStagingReady)
@@ -45,7 +45,7 @@ public partial class DxvkService
                 if (!IsStagingReady)
                 {
                     CrashReporter.Log("[DxvkService.InstallAsync] Staging still not ready — aborting");
-                    progress?.Report(("DXVK staging not available", 0));
+                    progress?.Report(("DXVK ещё не загружен", 0));
                     return;
                 }
             }
@@ -57,7 +57,7 @@ public partial class DxvkService
             // ── 3. Validate deployment path ──────────────────────────────
             if (!IsValidDeploymentPath(card.InstallPath))
             {
-                var msg = $"Cannot deploy DXVK to '{card.InstallPath}' — path is under a protected system directory.";
+                var msg = $"Нельзя развернуть DXVK в «{card.InstallPath}» — путь в защищённом системном каталоге.";
                 CrashReporter.Log($"[DxvkService.InstallAsync] {msg}");
                 progress?.Report((msg, 0));
                 return;
@@ -75,13 +75,13 @@ public partial class DxvkService
             var sampleDll = Path.Combine(StagingDir, archFolder, dllNames[0]);
             if (!File.Exists(sampleDll))
             {
-                var msg = $"Staged DLL not found: '{sampleDll}' — try clearing the DXVK cache in Settings.";
+                var msg = $"Загруженная DLL не найдена: «{sampleDll}» — попробуйте очистить кеш DXVK в настройках.";
                 CrashReporter.Log($"[DxvkService.InstallAsync] {msg}");
                 progress?.Report((msg, 0));
                 return;
             }
 
-            progress?.Report(("Resolving deployment paths...", 15));
+            progress?.Report(("Определение путей развертывания...", 15));
 
             // ── DX8/DX9 Direct Mode ─────────────────────────────────────
             // All variants deploy DXVK as d3d9.dll directly for DX8/DX9 games.
@@ -127,7 +127,7 @@ public partial class DxvkService
                 CrashReporter.Log($"[DxvkService.InstallAsync] Direct DX9: deployed DXVK as d3d9.dll (variant={_selectedVariant})");
 
                 // Step 4: Deploy dxvk.conf
-                progress?.Report(("Configuring dxvk.conf...", 60));
+                progress?.Report(("Настройка dxvk.conf...", 60));
                 var dx9ConfPath = Path.Combine(card.InstallPath, "dxvk.conf");
                 bool dx9DeployedConf = false;
                 try
@@ -145,7 +145,7 @@ public partial class DxvkService
                 }
 
                 // Step 5: Deploy Vulkan reshade.ini + footprint (no [PROXY] section)
-                progress?.Report(("Configuring Vulkan ReShade...", 70));
+                progress?.Report(("Настройка Vulkan ReShade...", 70));
                 AuxInstallService.MergeRsVulkanIni(card.InstallPath, card.GameName);
                 VulkanFootprintService.Create(card.InstallPath);
                 CrashReporter.Log("[DxvkService.InstallAsync] Direct DX9: deployed Vulkan reshade.ini + footprint");
@@ -153,7 +153,7 @@ public partial class DxvkService
                 // Step 5b: Lilium HDR only — set NVIDIA profile settings for HDR present mode
                 if (_selectedVariant == DxvkVariant.LiliumHdr)
                 {
-                    progress?.Report(("Setting NVIDIA profile...", 78));
+                    progress?.Report(("Настройка профиля NVIDIA...", 78));
                     try
                     {
                         var presetSvc = App.Services.GetRequiredService<DlssPresetService>();
@@ -167,7 +167,7 @@ public partial class DxvkService
                 }
 
                 // Step 6: Save tracking record
-                progress?.Report(("Saving install record...", 85));
+                progress?.Report(("Сохранение записи об установке...", 85));
                 var dx9Record = new DxvkInstalledRecord
                 {
                     GameName = card.GameName,
@@ -199,7 +199,7 @@ public partial class DxvkService
                 card.RsStatus = GameStatus.Installed;
                 card.NotifyAll();
 
-                progress?.Report(("DXVK installed!", 100));
+                progress?.Report(("DXVK установлен!", 100));
                 CrashReporter.Log($"[DxvkService.InstallAsync] Direct DX9 install complete for {card.GameName} (variant={_selectedVariant})");
                 return;
             }
@@ -216,10 +216,10 @@ public partial class DxvkService
             // - DXVK also needs to deploy dxgi.dll for DX10/DX11 games
             // - If we deploy DXVK's dxgi.dll first, then the ReShade uninstall
             //   deletes it thinking it's removing the old ReShade DLL
-            progress?.Report(("Switching ReShade mode...", 25));
+            progress?.Report(("Переключение режима ReShade...", 25));
             await SwitchReShadeForDxvkAsync(card, dxvkEnabled: true);
 
-            progress?.Report(("Deploying DXVK DLLs...", 35));
+            progress?.Report(("Развертывание DLL DXVK...", 35));
 
             var backedUpFiles = new List<string>();
 
@@ -251,7 +251,7 @@ public partial class DxvkService
                 }
             }
 
-            progress?.Report(("Configuring dxvk.conf...", 65));
+            progress?.Report(("Настройка dxvk.conf...", 65));
 
             // ── 9. Deploy default dxvk.conf if none exists ───────────────
             var confPath = Path.Combine(card.InstallPath, "dxvk.conf");
@@ -287,7 +287,7 @@ public partial class DxvkService
                 CrashReporter.Log($"[DxvkService.InstallAsync] Failed to deploy dxvk.conf — {ex.Message}");
             }
 
-            progress?.Report(("Saving install record...", 80));
+            progress?.Report(("Сохранение записи об установке...", 80));
 
             // ── 10. Create and persist DxvkInstalledRecord ───────────────
             var record = new DxvkInstalledRecord
@@ -312,12 +312,12 @@ public partial class DxvkService
             card.DxvkRecord = record;
             HasUpdate = false;
 
-            progress?.Report(("DXVK installed!", 100));
+            progress?.Report(("DXVK установлен!", 100));
             CrashReporter.Log($"[DxvkService.InstallAsync] Install complete for {card.GameName}");
         }
         catch (IOException ioEx) when (IsFileLockedException(ioEx))
         {
-            var msg = $"File is in use — close {card.GameName} and retry.";
+            var msg = $"Файл занят — закройте {card.GameName} и повторите.";
             CrashReporter.Log($"[DxvkService.InstallAsync] {msg} — {ioEx.Message}");
             progress?.Report(($"❌ {msg}", 0));
         }
@@ -326,7 +326,7 @@ public partial class DxvkService
             CrashReporter.Log($"[DxvkService.InstallAsync] {card.GameName} — {ex.Message}");
             CrashReporter.WriteCrashReport("DxvkService.InstallAsync", ex,
                 note: $"Game: {card.GameName}, Path: {card.InstallPath}");
-            progress?.Report(($"❌ Install failed: {ex.Message}", 0));
+            progress?.Report(($"❌ Не удалось установить: {ex.Message}", 0));
         }
     }
 
@@ -546,7 +546,7 @@ public partial class DxvkService
     {
         try
         {
-            progress?.Report(("Preparing DXVK update...", 5));
+            progress?.Report(("Подготовка обновления DXVK...", 5));
 
             // ── 1. Re-stage latest DXVK if needed ────────────────────────
             if (HasUpdate)
@@ -561,7 +561,7 @@ public partial class DxvkService
                 if (!IsStagingReady)
                 {
                     CrashReporter.Log("[DxvkService.UpdateAsync] Staging not ready after download — aborting");
-                    progress?.Report(("DXVK staging not available", 0));
+                    progress?.Report(("DXVK ещё не загружен", 0));
                     return;
                 }
             }
@@ -574,7 +574,7 @@ public partial class DxvkService
                 return;
             }
 
-            progress?.Report(("Updating DXVK DLLs...", 30));
+            progress?.Report(("Обновление DLL DXVK...", 30));
 
             // ── 2. Determine arch folder for staging ─────────────────────
             // For direct DX9 mode (any variant), card.GraphicsApi is Vulkan (runtime override) — use the
@@ -613,7 +613,7 @@ public partial class DxvkService
                 }
             }
 
-            progress?.Report(("Saving updated record...", 80));
+            progress?.Report(("Сохранение обновлённой записи...", 80));
 
             // ── 5. Rewrite dxvk.conf for direct DX9 installs ────────────
             if (isDx9DirectInstall)
@@ -648,12 +648,12 @@ public partial class DxvkService
             card.DxvkRecord = existingRecord;
             HasUpdate = false;
 
-            progress?.Report(("DXVK updated!", 100));
+            progress?.Report(("DXVK обновлён!", 100));
             CrashReporter.Log($"[DxvkService.UpdateAsync] Update complete for {card.GameName}");
         }
         catch (IOException ioEx) when (IsFileLockedException(ioEx))
         {
-            var msg = $"File is in use — close {card.GameName} and retry.";
+            var msg = $"Файл занят — закройте {card.GameName} и повторите.";
             CrashReporter.Log($"[DxvkService.UpdateAsync] {msg} — {ioEx.Message}");
             progress?.Report(($"❌ {msg}", 0));
         }
@@ -662,7 +662,7 @@ public partial class DxvkService
             CrashReporter.Log($"[DxvkService.UpdateAsync] {card.GameName} — {ex.Message}");
             CrashReporter.WriteCrashReport("DxvkService.UpdateAsync", ex,
                 note: $"Game: {card.GameName}, Path: {card.InstallPath}");
-            progress?.Report(($"❌ Update failed: {ex.Message}", 0));
+            progress?.Report(($"❌ Не удалось обновить: {ex.Message}", 0));
         }
     }
 
@@ -736,7 +736,7 @@ public partial class DxvkService
                     }
 
                     // Unmanaged DXVK detected
-                    return "Detected (unmanaged)";
+                    return "Определено (не управляется)";
                 }
             }
 
@@ -750,7 +750,7 @@ public partial class DxvkService
                     if (File.Exists(dllPath) && IsDxvkFile(dllPath))
                     {
                         var record = FindRecord(Path.GetFileName(installPath), installPath);
-                        return record?.DxvkVersion ?? "Detected (unmanaged)";
+                        return record?.DxvkVersion ?? "Определено (не управляется)";
                     }
                 }
             }
@@ -832,7 +832,7 @@ public partial class DxvkService
         catch (IOException ioEx) when (IsFileLockedException(ioEx))
         {
             throw new IOException(
-                $"Cannot copy '{dllName}' — the file is in use. Close {gameName} and retry.", ioEx);
+                $"Не удалось скопировать «{dllName}» — файл занят. Закройте {gameName} и повторите.", ioEx);
         }
     }
 
@@ -903,8 +903,8 @@ public partial class DxvkService
                 {
                     CrashReporter.Log(
                         $"[DxvkService.SwitchReShadeForDxvk] WARNING: {card.GameName}: " +
-                        "Global Vulkan ReShade layer is not installed. DXVK games present as Vulkan " +
-                        "and require the layer. Install ReShade on a Vulkan game via the UI to set up the layer.");
+                        "Глобальный слой Vulkan ReShade не установлен. DXVK-игры определяются как Vulkan " +
+                        "и требуют этот слой. Установите ReShade в какую-нибудь Vulkan-игру через интерфейс, чтобы настроить слой.");
                 }
 
                 CrashReporter.Log($"[DxvkService.SwitchReShadeForDxvk] {card.GameName}: switched ReShade to Vulkan layer mode");
diff --git a/RenoDXCommander/Services/DxvkService.Staging.cs b/RenoDXCommander/Services/DxvkService.Staging.cs
index faa7098..d4d241a 100644
--- a/RenoDXCommander/Services/DxvkService.Staging.cs
+++ b/RenoDXCommander/Services/DxvkService.Staging.cs
@@ -79,11 +79,11 @@ public partial class DxvkService
             if (IsStagingReady && !HasUpdate)
             {
                 CrashReporter.Log("[DxvkService.EnsureStagingAsync] Staging already valid — skipping download");
-                progress?.Report(("DXVK staging ready", 100));
+                progress?.Report(("DXVK загружен и готов", 100));
                 return;
             }
 
-            progress?.Report(("Checking DXVK release...", 5));
+            progress?.Report(("Проверка релиза DXVK...", 5));
 
             // ── 2. Route based on variant ────────────────────────────────────
             if (_selectedVariant == DxvkVariant.LiliumHdr)
@@ -128,12 +128,12 @@ public partial class DxvkService
             && IsStagingReady)
         {
             CrashReporter.Log($"[DxvkService.EnsureStagingNightlyAsync] Already up to date ({versionTag})");
-            progress?.Report(("DXVK up to date", 100));
+            progress?.Report(("DXVK актуален", 100));
             return;
         }
 
         var assetName = $"dxvk-master-{versionTag}.zip";
-        progress?.Report(($"Downloading DXVK nightly ({assetName})...", 10));
+        progress?.Report(($"Загрузка DXVK nightly ({assetName})...", 10));
         CrashReporter.Log($"[DxvkService.EnsureStagingNightlyAsync] Downloading {assetName} from {downloadUrl}");
 
         // ── Download the .zip archive to a temp file ─────────────────────
@@ -153,7 +153,7 @@ public partial class DxvkService
         }
 
         // ── Extract the .zip archive (single pass) ──────────────────────
-        progress?.Report(("Extracting DXVK...", 75));
+        progress?.Report(("Распаковка DXVK...", 75));
         try
         {
             if (!await ExtractZipToStagingAsync(tempArchive).ConfigureAwait(false))
@@ -167,7 +167,7 @@ public partial class DxvkService
         // ── Write version tag ────────────────────────────────────────────
         WriteVersionTag(versionTag);
 
-        progress?.Report(("DXVK staging ready", 100));
+        progress?.Report(("DXVK загружен и готов", 100));
         CrashReporter.Log("[DxvkService.EnsureStagingNightlyAsync] Staging complete");
     }
 
@@ -240,7 +240,7 @@ public partial class DxvkService
             && IsStagingReady)
         {
             CrashReporter.Log($"[DxvkService.EnsureStagingGitHubAsync] Already up to date ({tagName})");
-            progress?.Report(("DXVK up to date", 100));
+            progress?.Report(("DXVK актуален", 100));
             return;
         }
 
@@ -267,7 +267,7 @@ public partial class DxvkService
         // 7-Zip requires two passes for .tar.gz:
         //   Pass 1: 7z x archive.tar.gz → produces archive.tar
         //   Pass 2: 7z x archive.tar    → produces the directory tree
-        progress?.Report(("Extracting DXVK...", 75));
+        progress?.Report(("Распаковка DXVK...", 75));
         try
         {
             if (!await ExtractTarGzToStagingAsync(tempArchive).ConfigureAwait(false))
@@ -281,7 +281,7 @@ public partial class DxvkService
         // ── Write version tag to version.txt ─────────────────────────────
         WriteVersionTag(tagName ?? "unknown");
 
-        progress?.Report(("DXVK staging ready", 100));
+        progress?.Report(("DXVK загружен и готов", 100));
         CrashReporter.Log("[DxvkService.EnsureStagingGitHubAsync] Staging complete");
     }
 
@@ -356,7 +356,7 @@ public partial class DxvkService
             && IsStagingReadyForVariant(DxvkVariant.LiliumHdr))
         {
             CrashReporter.Log($"[DxvkService.EnsureStagingLiliumAsync] Already up to date ({tagName})");
-            progress?.Report(("DXVK Lilium HDR up to date", 100));
+            progress?.Report(("DXVK Lilium HDR актуален", 100));
             return;
         }
 
@@ -383,7 +383,7 @@ public partial class DxvkService
         // ── Extract the .7z archive ──────────────────────────────────────
         // Lilium's archive has structure: normal/x64/*.dll and normal/x32/*.dll
         // We need to extract and remap to x64/ and x32/ in the staging dir.
-        progress?.Report(("Extracting DXVK Lilium HDR...", 75));
+        progress?.Report(("Распаковка DXVK Lilium HDR...", 75));
         try
         {
             if (!await ExtractLilium7zToStagingAsync(tempArchive, liliumDir).ConfigureAwait(false))
@@ -400,7 +400,7 @@ public partial class DxvkService
         WriteVersionTag(tagName ?? "unknown");
         _selectedVariant = savedVariant;
 
-        progress?.Report(("DXVK Lilium HDR staging ready", 100));
+        progress?.Report(("DXVK Lilium HDR загружен и готов", 100));
         CrashReporter.Log("[DxvkService.EnsureStagingLiliumAsync] Staging complete");
     }
 
diff --git a/RenoDXCommander/Services/DxvkService.cs b/RenoDXCommander/Services/DxvkService.cs
index 2fc6189..8cc1ccd 100644
--- a/RenoDXCommander/Services/DxvkService.cs
+++ b/RenoDXCommander/Services/DxvkService.cs
@@ -161,7 +161,7 @@ public partial class DxvkService : IDxvkService
             GraphicsApiType.DirectX10 => new List<string> { "d3d10core.dll", "dxgi.dll" },
             GraphicsApiType.DirectX11 => new List<string> { "d3d11.dll", "dxgi.dll" },
             _ => throw new InvalidOperationException(
-                $"DXVK does not support {api}. Only DX8/9/10/11 are supported.")
+                $"DXVK не поддерживает {api}. Поддерживаются только DX8/9/10/11.")
         };
         return (arch, dlls);
     }
@@ -219,7 +219,7 @@ public partial class DxvkService : IDxvkService
         d3d9.upgradeSwapChainColorSpaceTo         = scRGB
         d3d9.enforceWindowModeInternally          = disabled
         """),
-        ("2nd Safest", """
+        ("2-й по безопасности", """
         dxvk.enableAsync                          = true
         dxvk.gplAsyncCache                        = true
         d3d9.enableBackBufferUpgrade              = true
@@ -229,7 +229,7 @@ public partial class DxvkService : IDxvkService
         d3d9.upgradeSwapChainColorSpaceTo         = scRGB
         d3d9.enforceWindowModeInternally          = disabled
         """),
-        ("Slightly Unsafe", """
+        ("Слегка рискованно", """
         dxvk.enableAsync                          = true
         dxvk.gplAsyncCache                        = true
         d3d9.enableSwapChainUpgrade               = true
@@ -262,7 +262,7 @@ public partial class DxvkService : IDxvkService
         d3d9.upgradeSwapChainColorSpaceTo         = scRGB
         d3d9.enforceWindowModeInternally          = disabled
         """),
-        ("Even Unsafer", """
+        ("Ещё рискованнее", """
         dxvk.enableAsync                          = true
         dxvk.gplAsyncCache                        = true
         d3d9.enableRenderTargetUpgrades           = true
@@ -346,7 +346,7 @@ public partial class DxvkService : IDxvkService
         d3d11.upgradeSwapChainFormatTo                = rgba16_sfloat
         d3d11.upgradeSwapChainColorSpaceTo            = scRGB
         """),
-        ("2nd Safest", """
+        ("2-й по безопасности", """
         dxvk.enableAsync                              = true
         dxvk.gplAsyncCache                            = true
         d3d11.enableBackBufferUpgrade                 = true
@@ -355,7 +355,7 @@ public partial class DxvkService : IDxvkService
         d3d11.upgradeSwapChainFormatTo                = rgba16_sfloat
         d3d11.upgradeSwapChainColorSpaceTo            = scRGB
         """),
-        ("Slightly Unsafe", """
+        ("Слегка рискованно", """
         dxvk.enableAsync                              = true
         dxvk.gplAsyncCache                            = true
         d3d11.enableBackBufferUpgrade                 = true
@@ -385,7 +385,7 @@ public partial class DxvkService : IDxvkService
         d3d11.upgradeSwapChainFormatTo                = rgba16_sfloat
         d3d11.upgradeSwapChainColorSpaceTo            = scRGB
         """),
-        ("Even Unsafer", """
+        ("Ещё рискованнее", """
         dxvk.enableAsync                              = true
         dxvk.gplAsyncCache                            = true
         d3d11.enableRenderTargetUpgrades              = true
@@ -406,7 +406,7 @@ public partial class DxvkService : IDxvkService
         d3d11.upgradeSwapChainFormatTo                = rgba16_sfloat
         d3d11.upgradeSwapChainColorSpaceTo            = scRGB
         """),
-        ("Slightly Experimental", """
+        ("Слегка экспериментально", """
         dxvk.enableAsync                              = true
         dxvk.gplAsyncCache                            = true
         d3d11.enableRenderTargetUpgrades              = true
@@ -427,7 +427,7 @@ public partial class DxvkService : IDxvkService
         d3d11.upgradeSwapChainFormatTo                = rgba16_sfloat
         d3d11.upgradeSwapChainColorSpaceTo            = scRGB
         """),
-        ("Fully Experimental", """
+        ("Полностью экспериментально", """
         dxvk.enableAsync                              = true
         dxvk.gplAsyncCache                            = true
         d3d11.enableRenderTargetUpgrades              = true
diff --git a/RenoDXCommander/Services/FileAssociationService.cs b/RenoDXCommander/Services/FileAssociationService.cs
index 5027e4b..281d4f9 100644
--- a/RenoDXCommander/Services/FileAssociationService.cs
+++ b/RenoDXCommander/Services/FileAssociationService.cs
@@ -20,7 +20,7 @@ public static class FileAssociationService
 
             // Create ProgId: HKCU\Software\Classes\RenoDXCommander.Addon
             using var progKey = Registry.CurrentUser.CreateSubKey($@"Software\Classes\{ProgId}");
-            progKey?.SetValue("", "RenoDX Addon");
+            progKey?.SetValue("", "Аддон RenoDX");
 
             // Icon
             using var iconKey = progKey?.CreateSubKey("DefaultIcon");
diff --git a/RenoDXCommander/Services/GameDetectionService.Platform.cs b/RenoDXCommander/Services/GameDetectionService.Platform.cs
index 2c07924..f4ab914 100644
--- a/RenoDXCommander/Services/GameDetectionService.Platform.cs
+++ b/RenoDXCommander/Services/GameDetectionService.Platform.cs
@@ -160,9 +160,9 @@ public partial class GameDetectionService
                     {
                         using var gameKey = parentKey.OpenSubKey(subName);
                         if (gameKey == null) continue;
-                        var installDir = gameKey.GetValue("Install Dir") as string
+                        var installDir = gameKey.GetValue("Папка установки") as string
                                       ?? gameKey.GetValue("InstallDir") as string
-                                      ?? gameKey.GetValue("Install Directory") as string;
+                                      ?? gameKey.GetValue("Папка установки") as string;
                         if (string.IsNullOrEmpty(installDir) || !Directory.Exists(installDir)) continue;
                         if (!seen.Add(installDir)) continue;
 
diff --git a/RenoDXCommander/Services/GameDetectionService.Xbox.cs b/RenoDXCommander/Services/GameDetectionService.Xbox.cs
index 2083e15..49bf4c3 100644
--- a/RenoDXCommander/Services/GameDetectionService.Xbox.cs
+++ b/RenoDXCommander/Services/GameDetectionService.Xbox.cs
@@ -214,7 +214,7 @@ public partial class GameDetectionService
         {
             foreach (var drive in DriveInfo.GetDrives()
                 .Where(d => d.DriveType == DriveType.Fixed && d.IsReady))
-                foreach (var name in new[] { "XboxGames", "Xbox Games" })
+                foreach (var name in new[] { "XboxGames", "Игры Xbox" })
                 {
                     var dir = Path.Combine(drive.RootDirectory.FullName, name);
                     if (Directory.Exists(dir)) searchRoots.Add(dir);
diff --git a/RenoDXCommander/Services/GameDetectionService.cs b/RenoDXCommander/Services/GameDetectionService.cs
index 23df383..9617fd4 100644
--- a/RenoDXCommander/Services/GameDetectionService.cs
+++ b/RenoDXCommander/Services/GameDetectionService.cs
@@ -425,10 +425,10 @@ public partial class GameDetectionService : IGameDetectionService
     /// that can carry their own engine DLLs and exes.</summary>
     private readonly HashSet<string> _skipFolders = new(StringComparer.OrdinalIgnoreCase)
     {
-        "artbook", "art book", "artbooks", "digitalartbook",
+        "artbook", "артбук", "artbooks", "digitalartbook",
         "soundtrack", "ost", "music",
         "manual", "manuals", "docs", "documentation",
-        "bonus", "bonuscontent", "bonus content", "extras",
+        "bonus", "bonuscontent", "бонусный контент", "extras",
         "wallpapers", "wallpaper",
     };
 
diff --git a/RenoDXCommander/Services/GameInitializationService.cs b/RenoDXCommander/Services/GameInitializationService.cs
index 5cb36f6..6aefba3 100644
--- a/RenoDXCommander/Services/GameInitializationService.cs
+++ b/RenoDXCommander/Services/GameInitializationService.cs
@@ -314,7 +314,7 @@ public class GameInitializationService : IGameInitializationService
                     card.Mod.SnapshotUrl = null;
                 card.IsExternalOnly = true;
                 card.ExternalUrl    = ext.Url ?? "https://discord.gg/gF4GRJWZ2A";
-                card.ExternalLabel  = ext.Label ?? "Download from Discord";
+                card.ExternalLabel  = ext.Label ?? "Скачать с Discord";
                 // Set WikiStatus based on the actual download source
                 bool isNexus = ext.Url != null && ext.Url.Contains("nexusmods", StringComparison.OrdinalIgnoreCase);
                 card.WikiStatus     = isNexus ? "🌐" : "💬";
@@ -377,10 +377,10 @@ public class GameInitializationService : IGameInitializationService
         var overrides = new Dictionary<string, CardOverride>(StringComparer.OrdinalIgnoreCase)
         {
             ["Cyberpunk 2077"] = new CardOverride(
-                Notes: "⚠️ The RenoDX mod for Cyberpunk 2077 is a WIP. " +
-                       "Always get the latest build directly from the RenoDX Discord — " +
-                       "it is updated more frequently than any wiki download.\n\n" +
-                       "See Creepy's Cyberpunk RenoDX Guide for setup instructions:",
+                Notes: "⚠️ Мод RenoDX для Cyberpunk 2077 в разработке. " +
+                       "Всегда берите свежую сборку прямо из RenoDX Discord — " +
+                       "он обновляется чаще, чем любая выгрузка из вики.\n\n" +
+                       "Инструкции по настройке — в руководстве Creepy по Cyberpunk (RenoDX):",
                 DiscordUrl: "https://discord.gg/gF4GRJWZ2A",
                 ForceDiscord: true,
                 NameUrl:       "https://www.hdrmods.com/Cyberpunk",
@@ -388,9 +388,9 @@ public class GameInitializationService : IGameInitializationService
                 NotesUrlLabel: "Creepy's Cyberpunk RenoDX Guide"),
 
             ["Dishonored"] = new CardOverride(
-                Notes: "ℹ️ This game has both 32-bit and 64-bit RenoDX builds.\n\n" +
-                       "• 64-bit (default) — for the Microsoft Store and Epic Games Store versions.\n" +
-                       "• 32-bit — for the Steam version. Enable 32-bit mode in 🎯 Overrides to use this.",
+                Notes: "ℹ️ У этой игры есть сборки RenoDX и 32-, и 64-бит.\n\n" +
+                       "• 64-бит (по умолчанию) — для версий из Microsoft Store и Epic Games Store.\n" +
+                       "• 32-бит — для версии из Steam. Включите 32-битный режим в 🎯 «Переопределения».",
                 DiscordUrl: null, ForceDiscord: false),
         };
 
@@ -404,7 +404,7 @@ public class GameInitializationService : IGameInitializationService
                     card.Mod.SnapshotUrl = null;
                 card.IsExternalOnly  = true;
                 card.ExternalUrl     = ov.DiscordUrl ?? "https://discord.gg/gF4GRJWZ2A";
-                card.ExternalLabel   = "Download from Discord";
+                card.ExternalLabel   = "Скачать с Discord";
                 card.DiscordUrl      = ov.DiscordUrl ?? "https://discord.gg/gF4GRJWZ2A";
                 card.WikiStatus      = "💬";
             }
diff --git a/RenoDXCommander/Services/HdrDatabaseService.cs b/RenoDXCommander/Services/HdrDatabaseService.cs
index 0a46b5e..b47fc7d 100644
--- a/RenoDXCommander/Services/HdrDatabaseService.cs
+++ b/RenoDXCommander/Services/HdrDatabaseService.cs
@@ -42,7 +42,7 @@ public class HdrDatabaseService : IHdrDatabaseService
         if (CachedData != null)
             return CachedData;
 
-        progress?.Report("Fetching HDR Gaming Database...");
+        progress?.Report("Загрузка HDR Gaming Database...");
 
         var result = new Dictionary<string, string>(StringComparer.OrdinalIgnoreCase);
 
@@ -72,7 +72,7 @@ public class HdrDatabaseService : IHdrDatabaseService
         }
 
         CachedData = result;
-        progress?.Report($"HDR database: {result.Count} entries");
+        progress?.Report($"База HDR: записей — {result.Count}");
         return result;
     }
 
diff --git a/RenoDXCommander/Services/LiliumShaderService.cs b/RenoDXCommander/Services/LiliumShaderService.cs
index 8fdb745..584c790 100644
--- a/RenoDXCommander/Services/LiliumShaderService.cs
+++ b/RenoDXCommander/Services/LiliumShaderService.cs
@@ -62,7 +62,7 @@ public class LiliumShaderService : ILiliumShaderService
     {
         try
         {
-            progress?.Report("Checking Lilium HDR shaders...");
+            progress?.Report("Проверка шейдеров Lilium HDR...");
 
             // ── 1. Fetch latest release metadata ─────────────────────────────────
             string? json;
@@ -117,11 +117,11 @@ public class LiliumShaderService : ILiliumShaderService
                 && ShadersAvailable)
             {
                 CrashReporter.Log($"[LiliumShaderService.EnsureLatestAsync] Already up to date ({assetName})");
-                progress?.Report($"Lilium shaders up to date ({assetName})");
+                progress?.Report($"Шейдеры Lilium актуальны ({assetName})");
                 return;
             }
 
-            progress?.Report($"Downloading Lilium shaders ({assetName})...");
+            progress?.Report($"Загрузка шейдеров Lilium ({assetName})...");
             CrashReporter.Log($"[LiliumShaderService.EnsureLatestAsync] Downloading {assetName} from {downloadUrl}");
 
             // ── 4. Download the .7z into the downloads cache ─────────────────────
@@ -151,7 +151,7 @@ public class LiliumShaderService : ILiliumShaderService
                         await file.WriteAsync(buf.AsMemory(0, read));
                         downloaded += read;
                         if (total > 0)
-                            progress?.Report($"Downloading Lilium shaders... {downloaded / 1024} KB / {total / 1024} KB");
+                            progress?.Report($"Загрузка шейдеров Lilium... {downloaded / 1024} КБ / {total / 1024} КБ");
                     }
                 }
 
@@ -166,7 +166,7 @@ public class LiliumShaderService : ILiliumShaderService
             }
 
             // ── 5. Extract Shaders/ and Textures/ from the .7z ──────────────────
-            progress?.Report("Extracting Lilium shaders...");
+            progress?.Report("Распаковка шейдеров Lilium...");
             try
             {
                 Directory.CreateDirectory(ShadersDir);
@@ -230,7 +230,7 @@ public class LiliumShaderService : ILiliumShaderService
 
             // ── 6. Store the version so we skip next time ─────────────────────────
             SaveStoredVersion(assetName);
-            progress?.Report($"Lilium shaders updated ({assetName})");
+            progress?.Report($"Шейдеры Lilium обновлены ({assetName})");
             CrashReporter.Log($"[LiliumShaderService.EnsureLatestAsync] Done. Version saved as {assetName}");
         }
         catch (Exception ex)
diff --git a/RenoDXCommander/Services/LumaService.cs b/RenoDXCommander/Services/LumaService.cs
index 60c1d15..6bceb72 100644
--- a/RenoDXCommander/Services/LumaService.cs
+++ b/RenoDXCommander/Services/LumaService.cs
@@ -37,7 +37,7 @@ public class LumaService : ILumaService
     /// </summary>
     public async Task<List<LumaMod>> FetchCompletedModsAsync(IProgress<string>? progress = null)
     {
-        progress?.Report("Fetching Luma wiki...");
+        progress?.Report("Загрузка вики Luma...");
         var html = await _http.GetStringAsync(WikiUrl);
         var doc = new HtmlDocument();
         doc.LoadHtml(html);
@@ -171,7 +171,7 @@ public class LumaService : ILumaService
                 mods[i].FeatureNotes = ExtractAnchorSection(doc, anchor, allAnchors);
         }
 
-        progress?.Report($"Found {mods.Count} Luma mods");
+        progress?.Report($"Найдено модов Luma: {mods.Count}");
         return mods;
     }
 
@@ -182,7 +182,7 @@ public class LumaService : ILumaService
     /// </summary>
     public async Task<Dictionary<string, LumaGenericGameEntry>> FetchGenericUeTableAsync(IProgress<string>? progress = null)
     {
-        progress?.Report("Fetching Luma UE wiki...");
+        progress?.Report("Загрузка UE-вики Luma...");
         var result = new Dictionary<string, LumaGenericGameEntry>(StringComparer.OrdinalIgnoreCase);
 
         try
@@ -561,7 +561,7 @@ public class LumaService : ILumaService
         string? store = null)
     {
         if (mod.DownloadUrl == null)
-            throw new InvalidOperationException($"{mod.Name} has no download URL.");
+            throw new InvalidOperationException($"У {mod.Name} нет ссылки на скачивание.");
 
         // Only allow downloads from Filoppi's GitHub to prevent untrusted sources.
         if (!mod.DownloadUrl.StartsWith("https://github.com/Filoppi/", StringComparison.OrdinalIgnoreCase))
@@ -601,7 +601,7 @@ public class LumaService : ILumaService
         // Download
         if (needsDownload)
         {
-        progress?.Report(("Downloading Luma mod...", 0));
+        progress?.Report(("Загрузка мода Luma...", 0));
         HttpResponseMessage? response = null;
         try
         {
@@ -637,7 +637,7 @@ public class LumaService : ILumaService
         }
         catch (Exception ex)
         {
-            throw new HttpRequestException($"Failed to download Luma mod: {ex.Message}");
+            throw new HttpRequestException($"Не удалось скачать мод Luma: {ex.Message}");
         }
         finally
         {
@@ -646,13 +646,13 @@ public class LumaService : ILumaService
         } // end if (needsDownload)
 
         // Extract zip to game folder, tracking all extracted file names
-        progress?.Report(("Extracting Luma files...", 80));
+        progress?.Report(("Распаковка файлов Luma...", 80));
         var installedFiles = new List<string>();
 
         // ── Deploy reshade.ini FIRST ──────────────────────────────────────────────
         // Must happen before zip extraction so that the AddonPath setting in
         // reshade.ini is available when resolving where .addon files should go.
-        progress?.Report(("Deploying ReShade config...", 75));
+        progress?.Report(("Развертывание конфигурации ReShade...", 75));
         try
         {
             _auxFileService.EnsureInisDir();
@@ -665,7 +665,7 @@ public class LumaService : ILumaService
         catch (Exception ex) { CrashReporter.Log($"[LumaService.Install] reshade.ini deploy failed — {ex.Message}"); }
 
         // ── Extract zip ───────────────────────────────────────────────────────────
-        progress?.Report(("Extracting Luma files...", 80));
+        progress?.Report(("Распаковка файлов Luma...", 80));
 
         if (cachePath.EndsWith(".zip", StringComparison.OrdinalIgnoreCase))
         {
@@ -720,7 +720,7 @@ public class LumaService : ILumaService
         }
 
         // ── Deploy shaders (same as normal ReShade — respects global/per-game selection) ──
-        progress?.Report(("Deploying shaders...", 95));
+        progress?.Report(("Развертывание шейдеров...", 95));
         try
         {
             var exclLuma1 = selectedShaderPacks == null ? null : await Task.Run(() =>
@@ -755,7 +755,7 @@ public class LumaService : ILumaService
             InstalledBuildNumber = await GetLatestBuildNumberAsync().ConfigureAwait(false),
         };
         SaveRecord(record);
-        progress?.Report(("Luma installed!", 100));
+        progress?.Report(("Luma установлен!", 100));
         return record;
     }
 
diff --git a/RenoDXCommander/Services/ManifestService.cs b/RenoDXCommander/Services/ManifestService.cs
index 5fef6eb..f9ad124 100644
--- a/RenoDXCommander/Services/ManifestService.cs
+++ b/RenoDXCommander/Services/ManifestService.cs
@@ -91,7 +91,7 @@ public class ManifestService : IManifestService
             }
             CrashReporter.Log($"[ManifestService.FetchAsync] Fetched: v{manifest?.Version}, " +
                 $"{manifest?.WikiNameOverrides?.Count ?? 0} name overrides, " +
-                $"{manifest?.GameNotes?.Count ?? 0} game notes");
+                $"заметок по играм: {manifest?.GameNotes?.Count ?? 0}");
             return manifest;
         }
         catch (Exception ex)
@@ -109,13 +109,13 @@ public class ManifestService : IManifestService
     {
         var apiJson = await _etagCache.GetWithETagAsync(_http, GitHubApiUrl).ConfigureAwait(false);
         if (apiJson == null)
-            throw new HttpRequestException("GitHub API returned error for manifest");
+            throw new HttpRequestException("GitHub API вернул ошибку для манифеста");
 
         using var doc = JsonDocument.Parse(apiJson);
         var root = doc.RootElement;
 
         var contentBase64 = root.GetProperty("content").GetString()
-            ?? throw new InvalidOperationException("GitHub API response missing 'content' field");
+            ?? throw new InvalidOperationException("В ответе GitHub API отсутствует поле 'content'");
 
         // GitHub returns base64 with embedded newlines — strip them before decoding
         contentBase64 = contentBase64.Replace("\n", "").Replace("\r", "");
diff --git a/RenoDXCommander/Services/ModInstallService.cs b/RenoDXCommander/Services/ModInstallService.cs
index 722f9dd..b8a91f2 100644
--- a/RenoDXCommander/Services/ModInstallService.cs
+++ b/RenoDXCommander/Services/ModInstallService.cs
@@ -69,7 +69,7 @@ public class ModInstallService : IModInstallService
         string? store = null)
     {
         if (mod.SnapshotUrl == null)
-            throw new InvalidOperationException($"{mod.Name} has no Snapshot download URL.");
+            throw new InvalidOperationException($"У {mod.Name} нет ссылки на скачивание снапшота.");
 
         // Apply URL override before anything else — this ensures the correct CDN is
         // used for both the HEAD size check and the actual download.
@@ -86,7 +86,7 @@ public class ModInstallService : IModInstallService
          && !ext.Equals(".addon32", StringComparison.OrdinalIgnoreCase))
         {
             throw new InvalidOperationException(
-                $"Unsupported file type '{ext}' for {mod.Name}. Only .addon64 and .addon32 files are supported.");
+                $"Неподдерживаемый тип файла «{ext}» для {mod.Name}. Поддерживаются только .addon64 и .addon32.");
         }
 
         var destPath  = Path.Combine(GetAddonDeployPath(gameInstallPath), fileName);
@@ -117,10 +117,10 @@ public class ModInstallService : IModInstallService
             bool sizeOk   = remoteSize.HasValue && remoteSize.Value == cacheSize;
             if (sizeOk && HasPeSignature(cachePath))
             {
-                progress?.Report(("Installing from cache...", 50));
+                progress?.Report(("Установка из кеша...", 50));
                 File.Copy(cachePath, destPath, overwrite: true);
                 usedCache = true;
-                progress?.Report(("✅ Installed from cache!", 100));
+                progress?.Report(("✅ Установлено из кеша!", 100));
             }
             else if (!HasPeSignature(cachePath))
             {
@@ -181,7 +181,7 @@ public class ModInstallService : IModInstallService
 
             if (response == null || !response.IsSuccessStatusCode)
                 throw new HttpRequestException(
-                    $"Failed to download snapshot. Tried: {string.Join(", ", tried)}");
+                    $"Не удалось скачать снапшот. Пробовал: {string.Join(", ", tried)}");
 
             // Capture size from actual download response if HEAD didn't return one
             if (!remoteSize.HasValue)
@@ -217,7 +217,7 @@ public class ModInstallService : IModInstallService
                 var badSize = new FileInfo(cachePath).Length;
                 File.Delete(cachePath);
                 throw new InvalidOperationException(
-                    $"Downloaded file from '{resolvedUrl}' is not a valid addon ({badSize} bytes). The server may have returned an error page. Please try again.");
+                    $"Скачанный файл с «{resolvedUrl}» — не аддон ({badSize} байт). Возможно, сервер вернул страницу с ошибкой. Попробуйте снова.");
             }
 
             File.Copy(cachePath, destPath, overwrite: true);
diff --git a/RenoDXCommander/Services/NexusDownloadService.cs b/RenoDXCommander/Services/NexusDownloadService.cs
index b329e1a..63c47af 100644
--- a/RenoDXCommander/Services/NexusDownloadService.cs
+++ b/RenoDXCommander/Services/NexusDownloadService.cs
@@ -203,13 +203,13 @@ public class NexusDownloadService
     {
         try
         {
-            progress?.Report(("Starting download...", 0));
+            progress?.Report(("Начинаю загрузку...", 0));
 
             using var response = await _http.GetAsync(uri, HttpCompletionOption.ResponseHeadersRead).ConfigureAwait(false);
             if (!response.IsSuccessStatusCode)
             {
                 CrashReporter.Log($"[NexusDownloadService.DownloadToTempAsync] HTTP {(int)response.StatusCode}");
-                progress?.Report(($"Download failed: HTTP {(int)response.StatusCode}", 0));
+                progress?.Report(($"Ошибка загрузки: HTTP {(int)response.StatusCode}", 0));
                 return null;
             }
 
@@ -247,13 +247,13 @@ public class NexusDownloadService
             }
 
             CrashReporter.Log($"[NexusDownloadService.DownloadToTempAsync] Downloaded {downloaded:N0} bytes to {tempPath}");
-            progress?.Report(("Download complete", 100));
+            progress?.Report(("Загрузка завершена", 100));
             return tempPath;
         }
         catch (Exception ex)
         {
             CrashReporter.Log($"[NexusDownloadService.DownloadToTempAsync] Failed — {ex.Message}");
-            progress?.Report(($"Download failed: {ex.Message}", 0));
+            progress?.Report(($"Ошибка загрузки: {ex.Message}", 0));
             return null;
         }
     }
diff --git a/RenoDXCommander/Services/NormalReShadeUpdateService.cs b/RenoDXCommander/Services/NormalReShadeUpdateService.cs
index c77e9c8..74dcf1d 100644
--- a/RenoDXCommander/Services/NormalReShadeUpdateService.cs
+++ b/RenoDXCommander/Services/NormalReShadeUpdateService.cs
@@ -87,7 +87,7 @@ public class NormalReShadeUpdateService : INormalReShadeUpdateService
         CrashReporter.Log("[NormalReShadeUpdateService.EnsureLatestAsync] Started");
         Directory.CreateDirectory(CacheDir);
 
-        progress?.Report(("Checking for Normal ReShade updates...", 5));
+        progress?.Report(("Проверка обновлений обычного ReShade...", 5));
         var latest = await CheckLatestVersionAsync();
         if (latest == null)
         {
@@ -108,7 +108,7 @@ public class NormalReShadeUpdateService : INormalReShadeUpdateService
             && new FileInfo(stagedPath32).Length > 1_000_000)
         {
             CrashReporter.Log($"[NormalReShadeUpdateService.EnsureLatestAsync] Already have v{version}");
-            progress?.Report(($"Normal ReShade {version} is current", 100));
+            progress?.Report(($"Обычный ReShade {version} актуален", 100));
             return false;
         }
 
@@ -211,7 +211,7 @@ public class NormalReShadeUpdateService : INormalReShadeUpdateService
         }
 
         // Extract DLLs from the exe (it has an appended ZIP archive)
-        progress?.Report(("Extracting Normal ReShade DLLs...", 80));
+        progress?.Report(("Распаковка DLL обычного ReShade...", 80));
         try
         {
             _extractor.ExtractFile(exePath, "ReShade64.dll", stagedPath64);
@@ -249,7 +249,7 @@ public class NormalReShadeUpdateService : INormalReShadeUpdateService
         }
         catch (Exception ex) { CrashReporter.Log($"[NormalReShadeUpdateService.EnsureLatestAsync] Failed to clean up old installers — {ex.Message}"); }
 
-        progress?.Report(($"Normal ReShade {version} ready!", 100));
+        progress?.Report(($"Обычный ReShade {version} готов!", 100));
         CrashReporter.Log($"[NormalReShadeUpdateService.EnsureLatestAsync] Staged v{version} successfully");
         return true;
     }
diff --git a/RenoDXCommander/Services/NxmProtocolHandler.cs b/RenoDXCommander/Services/NxmProtocolHandler.cs
index fa3ee3f..fe52e40 100644
--- a/RenoDXCommander/Services/NxmProtocolHandler.cs
+++ b/RenoDXCommander/Services/NxmProtocolHandler.cs
@@ -116,7 +116,7 @@ public static class NxmProtocolHandler
     public static void RegisterProtocolHandler()
     {
         var exePath = Environment.ProcessPath
-            ?? throw new InvalidOperationException("Cannot determine RHI executable path");
+            ?? throw new InvalidOperationException("Не удалось определить путь к исполняемому файлу RHI");
 
         // Normalize to backslashes (Win32 GetOpenFileName pitfall)
         exePath = exePath.Replace('/', '\\');
diff --git a/RenoDXCommander/Services/OptiScalerService.Install.cs b/RenoDXCommander/Services/OptiScalerService.Install.cs
index 2bf7001..ab8a979 100644
--- a/RenoDXCommander/Services/OptiScalerService.Install.cs
+++ b/RenoDXCommander/Services/OptiScalerService.Install.cs
@@ -45,7 +45,7 @@ public partial class OptiScalerService
                 FirstTimeWarningAcknowledged = true;
             }
 
-            progress?.Report(("Preparing OptiScaler install...", 5));
+            progress?.Report(("Подготовка установки OptiScaler...", 5));
 
             // ── 2. Resolve effective staging dir based on variant ────────────
             bool isNightly = variant.Equals("Nightly", StringComparison.OrdinalIgnoreCase);
@@ -76,7 +76,7 @@ public partial class OptiScalerService
                 if (!stagingReady)
                 {
                     CrashReporter.Log($"[OptiScalerService.InstallAsync] {variant} staging still not ready after download attempt — aborting");
-                    progress?.Report(($"OptiScaler {variant} staging not available", 0));
+                    progress?.Report(($"OptiScaler {variant} ещё не загружен", 0));
                     return null;
                 }
             }
@@ -95,7 +95,7 @@ public partial class OptiScalerService
 
             CrashReporter.Log($"[OptiScalerService.InstallAsync] {card.GameName}: effective DLL name = {effectiveDllName}");
 
-            progress?.Report(("Copying OptiScaler files...", 20));
+            progress?.Report(("Копирование файлов OptiScaler...", 20));
 
             // ── 4. ReShade coexistence — rename RS DLL to ReShade64.dll BEFORE deploying files ──
             // This MUST happen before file deployment because OptiScaler may use the same
@@ -248,7 +248,7 @@ public partial class OptiScalerService
                 }
             }
 
-            progress?.Report(("Configuring OptiScaler INI...", 60));
+            progress?.Report(("Настройка INI OptiScaler...", 60));
 
             // ── 5. INI seeding and deployment ────────────────────────────────
             Directory.CreateDirectory(AuxInstallService.InisDir);
@@ -336,7 +336,7 @@ public partial class OptiScalerService
             // ── 5b. DlssNr variant: deploy forwarder + nvngx_dlssnr.dll ─────
             if (isDlssNr)
             {
-                progress?.Report(("Deploying DLSS NR runtime...", 72));
+                progress?.Report(("Развертывание среды выполнения DLSS NR...", 72));
                 try
                 {
                     var dlssStreamlineSvc = _dlssStreamlineServiceLazy.Value;
@@ -360,7 +360,7 @@ public partial class OptiScalerService
                 }
             }
 
-            progress?.Report(("Saving install record...", 80));
+            progress?.Report(("Сохранение записи об установке...", 80));
 
             // ── 6. Create/update AuxInstalledRecord ──────────────────────────
             var record = new AuxInstalledRecord
@@ -382,7 +382,7 @@ public partial class OptiScalerService
             {
                 try
                 {
-                    progress?.Report(("Downloading OptiPatcher...", 85));
+                    progress?.Report(("Загрузка OptiPatcher...", 85));
                     await EnsureOptiPatcherStagingAsync(progress);
 
                     var stagedAsi = Path.Combine(OptiPatcherStagingDir, OptiPatcherFileName);
@@ -393,7 +393,7 @@ public partial class OptiScalerService
                         var destAsi = Path.Combine(pluginsDir, OptiPatcherFileName);
                         File.Copy(stagedAsi, destAsi, overwrite: true);
                         CrashReporter.Log($"[OptiScalerService.InstallAsync] Deployed OptiPatcher.asi to plugins folder");
-                        progress?.Report(("OptiPatcher deployed", 90));
+                        progress?.Report(("OptiPatcher развернут", 90));
                     }
                     else
                     {
@@ -525,7 +525,7 @@ public partial class OptiScalerService
                 });
             }
 
-            progress?.Report(("OptiScaler installed!", 100));
+            progress?.Report(("OptiScaler установлен!", 100));
             CrashReporter.Log($"[OptiScalerService.InstallAsync] Install complete for {card.GameName}");
 
             return record;
@@ -533,7 +533,7 @@ public partial class OptiScalerService
         catch (Exception ex)
         {
             CrashReporter.Log($"[OptiScalerService.InstallAsync] {card.GameName} — {ex.Message}");
-            progress?.Report(($"Install failed: {ex.Message}", 0));
+            progress?.Report(($"Не удалось установить: {ex.Message}", 0));
             return null;
         }
     }
@@ -967,7 +967,7 @@ public partial class OptiScalerService
     {
         try
         {
-            progress?.Report(("Preparing OptiScaler update...", 5));
+            progress?.Report(("Подготовка обновления OptiScaler...", 5));
 
             // ── Read variant from tracking record ─────────────────────────
             var record = _auxInstaller.FindRecord(card.GameName, card.InstallPath, AddonType);
@@ -999,7 +999,7 @@ public partial class OptiScalerService
                 if (!stagingReady)
                 {
                     CrashReporter.Log($"[OptiScalerService.UpdateAsync] {variant} staging still not ready after download attempt — aborting");
-                    progress?.Report(($"OptiScaler {variant} staging not available", 0));
+                    progress?.Report(($"OptiScaler {variant} ещё не загружен", 0));
                     return;
                 }
             }
@@ -1011,11 +1011,11 @@ public partial class OptiScalerService
             if (string.IsNullOrEmpty(installedDll))
             {
                 CrashReporter.Log($"[OptiScalerService.UpdateAsync] No installed DLL filename found for {card.GameName} — aborting");
-                progress?.Report(("Update failed: no installed DLL found", 0));
+                progress?.Report(("Обновление не удалось: установленная DLL не найдена", 0));
                 return;
             }
 
-            progress?.Report(("Updating OptiScaler files...", 20));
+            progress?.Report(("Обновление файлов OptiScaler...", 20));
 
             // ── 3. Clean up old deployed files before deploying new ones ────
             // Delete all previously deployed companion files and subdirectories
@@ -1221,7 +1221,7 @@ public partial class OptiScalerService
                 }
             }
 
-            progress?.Report(("Updating tracking record...", 80));
+            progress?.Report(("Обновление записи учёта...", 80));
 
             // ── 5. Update tracking record with new version ───────────────────
             if (record != null)
@@ -1300,13 +1300,13 @@ public partial class OptiScalerService
                 });
             }
 
-            progress?.Report(("OptiScaler updated!", 100));
+            progress?.Report(("OptiScaler обновлён!", 100));
             CrashReporter.Log($"[OptiScalerService.UpdateAsync] Update complete for {card.GameName}");
         }
         catch (Exception ex)
         {
             CrashReporter.Log($"[OptiScalerService.UpdateAsync] {card.GameName} — {ex.Message}");
-            progress?.Report(($"Update failed: {ex.Message}", 0));
+            progress?.Report(($"Не удалось обновить: {ex.Message}", 0));
         }
     }
 
diff --git a/RenoDXCommander/Services/OptiScalerService.Staging.cs b/RenoDXCommander/Services/OptiScalerService.Staging.cs
index 106ec6d..a453e1c 100644
--- a/RenoDXCommander/Services/OptiScalerService.Staging.cs
+++ b/RenoDXCommander/Services/OptiScalerService.Staging.cs
@@ -16,11 +16,11 @@ public partial class OptiScalerService
             if (IsStagingReady && !HasUpdate)
             {
                 CrashReporter.Log("[OptiScalerService.EnsureStagingAsync] Staging already valid — skipping download");
-                progress?.Report(("OptiScaler staging ready", 100));
+                progress?.Report(("OptiScaler загружен и готов", 100));
                 return;
             }
 
-            progress?.Report(("Checking OptiScaler release...", 5));
+            progress?.Report(("Проверка релиза OptiScaler...", 5));
 
             // ── 2. Fetch latest release metadata from GitHub API ─────────────────
             string? json;
@@ -83,7 +83,7 @@ public partial class OptiScalerService
                 && IsStagingReady)
             {
                 CrashReporter.Log($"[OptiScalerService.EnsureStagingAsync] Already up to date ({tagName})");
-                progress?.Report(("OptiScaler up to date", 100));
+                progress?.Report(("OptiScaler актуален", 100));
                 return;
             }
 
@@ -133,7 +133,7 @@ public partial class OptiScalerService
             }
 
             // ── 6. Extract the .7z archive to staging using bundled 7z.exe ──────
-            progress?.Report(("Extracting OptiScaler...", 75));
+            progress?.Report(("Распаковка OptiScaler...", 75));
             try
             {
                 var sevenZipExe = Find7ZipExe();
@@ -237,7 +237,7 @@ public partial class OptiScalerService
                 CrashReporter.Log($"[OptiScalerService.EnsureStagingAsync] Failed to write version file — {ex.Message}");
             }
 
-            progress?.Report(("OptiScaler staging ready", 100));
+            progress?.Report(("OptiScaler загружен и готов", 100));
             CrashReporter.Log("[OptiScalerService.EnsureStagingAsync] Staging complete");
         }
         catch (Exception ex)
@@ -395,7 +395,7 @@ public partial class OptiScalerService
     {
         try
         {
-            progress?.Report(("Checking OptiPatcher release...", 0));
+            progress?.Report(("Проверка релиза OptiPatcher...", 0));
 
             // ── 1. Fetch rolling release metadata from GitHub API ────────────
             string? json;
@@ -468,7 +468,7 @@ public partial class OptiScalerService
                 && File.Exists(stagedAsiPath))
             {
                 CrashReporter.Log($"[OptiScalerService.EnsureOptiPatcherStagingAsync] Already up to date ({version})");
-                progress?.Report(("OptiPatcher up to date", 100));
+                progress?.Report(("OptiPatcher актуален", 100));
                 return;
             }
 
@@ -540,7 +540,7 @@ public partial class OptiScalerService
                 CrashReporter.Log($"[OptiScalerService.EnsureOptiPatcherStagingAsync] Auto-deploy pass failed — {ex.Message}");
             }
 
-            progress?.Report(("OptiPatcher staging ready", 100));
+            progress?.Report(("OptiPatcher загружен и готов", 100));
             CrashReporter.Log("[OptiScalerService.EnsureOptiPatcherStagingAsync] Staging complete");
         }
         catch (Exception ex)
@@ -622,7 +622,7 @@ public partial class OptiScalerService
     {
         try
         {
-            progress?.Report(("Checking DLSS cache...", 0));
+            progress?.Report(("Проверка кеша DLSS...", 0));
 
             var dlssSvc = _dlssStreamlineServiceLazy.Value;
 
@@ -630,16 +630,16 @@ public partial class OptiScalerService
             if (dlssSvc.DlssVersions.Count == 0)
                 await dlssSvc.FetchManifestAsync().ConfigureAwait(false);
 
-            progress?.Report(("Caching latest DLSS SR...", 20));
+            progress?.Report(("Кеширование свежей DLSS SR...", 20));
             await dlssSvc.EnsureNewestDlssCachedAsync().ConfigureAwait(false);
 
-            progress?.Report(("Caching latest DLSS RR...", 50));
+            progress?.Report(("Кеширование свежей DLSS RR...", 50));
             await dlssSvc.EnsureNewestDlssdCachedAsync().ConfigureAwait(false);
 
-            progress?.Report(("Caching latest DLSS FG...", 80));
+            progress?.Report(("Кеширование свежей DLSS FG...", 80));
             await dlssSvc.EnsureNewestDlssgCachedAsync().ConfigureAwait(false);
 
-            progress?.Report(("DLSS cache ready", 100));
+            progress?.Report(("Кеш DLSS готов", 100));
             CrashReporter.Log("[OptiScalerService.EnsureDlssStagingAsync] All DLSS DLLs cached from dlss_manifest.json");
         }
         catch (Exception ex)
@@ -804,11 +804,11 @@ public partial class OptiScalerService
             if (IsStagingReadyNightly && !HasUpdateNightly)
             {
                 CrashReporter.Log("[OptiScalerService.EnsureNightlyStagingAsync] Staging already valid — skipping");
-                progress?.Report(("OptiScaler Nightly staging ready", 100));
+                progress?.Report(("OptiScaler Nightly загружен и готов", 100));
                 return;
             }
 
-            progress?.Report(("Checking OptiScaler Nightly release...", 5));
+            progress?.Report(("Проверка релиза OptiScaler Nightly...", 5));
             string? json;
             try { json = await _etagCache.GetWithETagAsync(_http, NightlyReleasesApi).ConfigureAwait(false); }
             catch (Exception ex)
@@ -867,7 +867,7 @@ public partial class OptiScalerService
             if (cachedVersion != null && string.Equals(cachedVersion, tagName, StringComparison.Ordinal) && IsStagingReadyNightly)
             {
                 CrashReporter.Log($"[OptiScalerService.EnsureNightlyStagingAsync] Already up to date ({tagName})");
-                progress?.Report(("OptiScaler Nightly up to date", 100));
+                progress?.Report(("OptiScaler Nightly актуален", 100));
                 return;
             }
 
@@ -915,7 +915,7 @@ public partial class OptiScalerService
                 return;
             }
 
-            progress?.Report(("Extracting OptiScaler Nightly...", 75));
+            progress?.Report(("Распаковка OptiScaler Nightly...", 75));
             try
             {
                 var sevenZipExe = Find7ZipExe();
@@ -1014,7 +1014,7 @@ public partial class OptiScalerService
             }
 
             HasUpdateNightly = false;
-            progress?.Report(("OptiScaler Nightly staging ready", 100));
+            progress?.Report(("OptiScaler Nightly загружен и готов", 100));
             CrashReporter.Log("[OptiScalerService.EnsureNightlyStagingAsync] Staging complete");
         }
         catch (Exception ex)
@@ -1071,11 +1071,11 @@ public partial class OptiScalerService
             if (IsStagingReadyDlssNr && !HasUpdateDlssNr)
             {
                 CrashReporter.Log("[OptiScalerService.EnsureDlssNrStagingAsync] Staging already valid — skipping");
-                progress?.Report(("OptiScaler DLSS NR staging ready", 100));
+                progress?.Report(("OptiScaler DLSS NR загружен и готов", 100));
                 return;
             }
 
-            progress?.Report(("Checking OptiScaler DLSS NR release...", 5));
+            progress?.Report(("Проверка релиза OptiScaler DLSS NR...", 5));
             string? json;
             try { json = await _etagCache.GetWithETagAsync(_http, DlssNrReleasesApi).ConfigureAwait(false); }
             catch (Exception ex)
@@ -1142,7 +1142,7 @@ public partial class OptiScalerService
             if (cachedVersion != null && string.Equals(cachedVersion, tagName, StringComparison.Ordinal) && IsStagingReadyDlssNr)
             {
                 CrashReporter.Log($"[OptiScalerService.EnsureDlssNrStagingAsync] Already up to date ({tagName})");
-                progress?.Report(("OptiScaler DLSS NR up to date", 100));
+                progress?.Report(("OptiScaler DLSS NR актуален", 100));
                 return;
             }
 
@@ -1190,7 +1190,7 @@ public partial class OptiScalerService
                 return;
             }
 
-            progress?.Report(("Extracting OptiScaler DLSS NR...", 75));
+            progress?.Report(("Распаковка OptiScaler DLSS NR...", 75));
             try
             {
                 var tempExtractDir = Path.Combine(Path.GetTempPath(), $"RHI_optiscaler_dlssnr_{Guid.NewGuid():N}");
@@ -1257,7 +1257,7 @@ public partial class OptiScalerService
             }
 
             HasUpdateDlssNr = false;
-            progress?.Report(("OptiScaler DLSS NR staging ready", 100));
+            progress?.Report(("OptiScaler DLSS NR загружен и готов", 100));
             CrashReporter.Log("[OptiScalerService.EnsureDlssNrStagingAsync] Staging complete");
         }
         catch (Exception ex)
diff --git a/RenoDXCommander/Services/OptiScalerWikiService.cs b/RenoDXCommander/Services/OptiScalerWikiService.cs
index 2a06cc6..66d806c 100644
--- a/RenoDXCommander/Services/OptiScalerWikiService.cs
+++ b/RenoDXCommander/Services/OptiScalerWikiService.cs
@@ -41,7 +41,7 @@ public class OptiScalerWikiService : IOptiScalerWikiService
         if (CachedData != null)
             return CachedData;
 
-        progress?.Report("Fetching OptiScaler wiki...");
+        progress?.Report("Загрузка вики OptiScaler...");
 
         var standardCompat = new Dictionary<string, OptiScalerCompatEntry>(StringComparer.OrdinalIgnoreCase);
         var fsr4Compat = new Dictionary<string, OptiScalerCompatEntry>(StringComparer.OrdinalIgnoreCase);
@@ -90,7 +90,7 @@ public class OptiScalerWikiService : IOptiScalerWikiService
         };
 
         CachedData = data;
-        progress?.Report($"OptiScaler wiki: {standardCompat.Count} standard + {fsr4Compat.Count} FSR4 entries");
+        progress?.Report($"Вики OptiScaler: {standardCompat.Count} стандартных + {fsr4Compat.Count} записей FSR4");
         return data;
     }
 
@@ -183,7 +183,7 @@ public class OptiScalerWikiService : IOptiScalerWikiService
                     if (statusText.Contains("✔") || statusText.Contains("✔️"))
                         status = "Working";
                     else if (statusText.Contains("❌"))
-                        status = "Not Working";
+                        status = "Не работает";
                 }
 
                 // Column 2: FSR4 status (✅ = confirmed working with FSR4)
diff --git a/RenoDXCommander/Services/REFrameworkService.cs b/RenoDXCommander/Services/REFrameworkService.cs
index 5ce3deb..2c4d19a 100644
--- a/RenoDXCommander/Services/REFrameworkService.cs
+++ b/RenoDXCommander/Services/REFrameworkService.cs
@@ -72,7 +72,7 @@ public class REFrameworkService : IREFrameworkService
         try
         {
             // ── Download game-specific ZIP ────────────────────────────────────
-            progress?.Report(("Downloading RE Framework...", 10));
+            progress?.Report(("Загрузка RE Framework...", 10));
             CrashReporter.Log($"[REFrameworkService.InstallAsync] Downloading {downloadUrl}");
 
             using var response = await _http.GetAsync(downloadUrl, HttpCompletionOption.ResponseHeadersRead);
@@ -103,7 +103,7 @@ public class REFrameworkService : IREFrameworkService
             // ── Fetch version tag ─────────────────────────────────────────────
             var version = await GetLatestVersionAsync() ?? "unknown";
 
-            progress?.Report(("RE Framework installed!", 100));
+            progress?.Report(("RE Framework установлен!", 100));
 
             // ── Persist install record ────────────────────────────────────────
             var record = new REFrameworkInstalledRecord
@@ -141,7 +141,7 @@ public class REFrameworkService : IREFrameworkService
             e.Name.Equals(DllFileName, StringComparison.OrdinalIgnoreCase));
 
         if (entry == null)
-            throw new FileNotFoundException($"{DllFileName} not found inside {Path.GetFileName(zipPath)}");
+            throw new FileNotFoundException($"{DllFileName} не найден внутри {Path.GetFileName(zipPath)}");
 
         // Extract to destination, overwriting any existing cached copy
         entry.ExtractToFile(destDllPath, overwrite: true);
@@ -269,7 +269,7 @@ public class REFrameworkService : IREFrameworkService
             Directory.CreateDirectory(tempDir);
 
             // ── Download outer ZIP from nightly.link ──────────────────────────
-            progress?.Report(("Downloading PD-Upscaler REFramework...", 10));
+            progress?.Report(("Загрузка PD-Upscaler REFramework...", 10));
             CrashReporter.Log($"[REFrameworkService.InstallPdUpscalerAsync] Downloading {downloadUrl}");
 
             var outerZipPath = Path.Combine(tempDir, $"{artifactName}_outer.zip");
@@ -280,7 +280,7 @@ public class REFrameworkService : IREFrameworkService
                 await response.Content.CopyToAsync(fs);
             }
 
-            progress?.Report(("Extracting PD-Upscaler REFramework...", 40));
+            progress?.Report(("Распаковка PD-Upscaler REFramework...", 40));
 
             // ── Extract outer ZIP → inner {artifactName}.zip ──────────────────
             var outerExtractDir = Path.Combine(tempDir, "outer");
@@ -292,7 +292,7 @@ public class REFrameworkService : IREFrameworkService
                 // Fallback: look for any .zip inside the outer archive
                 innerZipPath = Directory.GetFiles(outerExtractDir, "*.zip").FirstOrDefault()
                     ?? throw new FileNotFoundException(
-                        $"Inner ZIP not found in pd-upscaler download for {artifactName}");
+                        $"Внутренний ZIP не найден в загрузке pd-upscaler для {artifactName}");
             }
 
             // ── Extract inner ZIP → dinput8.dll ───────────────────────────────
@@ -302,9 +302,9 @@ public class REFrameworkService : IREFrameworkService
             var extractedDll = Path.Combine(innerExtractDir, DllFileName);
             if (!File.Exists(extractedDll))
                 throw new FileNotFoundException(
-                    $"{DllFileName} not found inside pd-upscaler inner ZIP for {artifactName}");
+                    $"{DllFileName} не найден во внутреннем ZIP пакета pd-upscaler для {artifactName}");
 
-            progress?.Report(("Backing up standard REFramework...", 60));
+            progress?.Report(("Резервное копирование стандартного REFramework...", 60));
 
             // ── Back up existing standard dinput8.dll ─────────────────────────
             if (File.Exists(destDll) && !File.Exists(backupPath))
@@ -314,7 +314,7 @@ public class REFrameworkService : IREFrameworkService
             }
 
             // ── Copy pd-upscaler DLL to game folder ──────────────────────────
-            progress?.Report(("Installing PD-Upscaler REFramework...", 80));
+            progress?.Report(("Установка PD-Upscaler REFramework...", 80));
             File.Copy(extractedDll, destDll, overwrite: true);
 
             // ── Update install record with PD-Upscaler version ───────────────
@@ -340,7 +340,7 @@ public class REFrameworkService : IREFrameworkService
                 });
             }
 
-            progress?.Report(("PD-Upscaler REFramework installed!", 100));
+            progress?.Report(("PD-Upscaler REFramework установлен!", 100));
             CrashReporter.Log($"[REFrameworkService.InstallPdUpscalerAsync] PD-Upscaler installed for '{gameName}'");
         }
         catch (Exception ex)
diff --git a/RenoDXCommander/Services/ReShadeNightlyService.cs b/RenoDXCommander/Services/ReShadeNightlyService.cs
index 6d10f60..de435b5 100644
--- a/RenoDXCommander/Services/ReShadeNightlyService.cs
+++ b/RenoDXCommander/Services/ReShadeNightlyService.cs
@@ -35,7 +35,7 @@ public class ReShadeNightlyService
         CrashReporter.Log("[ReShadeNightlyService.EnsureLatestAsync] Started");
         Directory.CreateDirectory(CacheDir);
 
-        progress?.Report(("Checking for nightly ReShade builds...", 5));
+        progress?.Report(("Проверка ночных сборок ReShade...", 5));
 
         // Get the remote Content-Length to compare against local staged DLL size.
         // If the remote zip size differs from what we last downloaded, there's a new build.
@@ -50,12 +50,12 @@ public class ReShadeNightlyService
             && new FileInfo(AuxInstallService.RsNightlyStagedPath32).Length > AuxInstallService.MinReShadeSize)
         {
             CrashReporter.Log($"[ReShadeNightlyService.EnsureLatestAsync] Remote zip size unchanged ({remote64Size} bytes) — skipping");
-            progress?.Report(("ReShade nightly is current", 100));
+            progress?.Report(("ReShade nightly актуален", 100));
             return false;
         }
 
         // Download 64-bit artifact
-        progress?.Report(("Downloading ReShade nightly (64-bit)...", 15));
+        progress?.Report(("Загрузка ReShade nightly (64 бита)...", 15));
         var (success64, downloadedZipSize) = await DownloadAndExtractAsync(Nightly64Url, AuxInstallService.RsNightlyStagedPath64, "ReShade64.dll");
         if (!success64)
         {
@@ -64,7 +64,7 @@ public class ReShadeNightlyService
         }
 
         // Download 32-bit artifact
-        progress?.Report(("Downloading ReShade nightly (32-bit)...", 55));
+        progress?.Report(("Загрузка ReShade nightly (32 бита)...", 55));
         var (success32, _) = await DownloadAndExtractAsync(Nightly32Url, AuxInstallService.RsNightlyStagedPath32, "ReShade32.dll");
         if (!success32)
         {
@@ -88,7 +88,7 @@ public class ReShadeNightlyService
         }
         catch { }
 
-        progress?.Report(($"ReShade nightly ({versionTag}) ready!", 100));
+        progress?.Report(($"ReShade nightly ({versionTag}) готов!", 100));
         CrashReporter.Log($"[ReShadeNightlyService.EnsureLatestAsync] Staged {versionTag} successfully (zip size={downloadedZipSize})");
         return true;
     }
diff --git a/RenoDXCommander/Services/ReShadeUpdateService.cs b/RenoDXCommander/Services/ReShadeUpdateService.cs
index 374ce3c..45e2b5e 100644
--- a/RenoDXCommander/Services/ReShadeUpdateService.cs
+++ b/RenoDXCommander/Services/ReShadeUpdateService.cs
@@ -80,7 +80,7 @@ public class ReShadeUpdateService : IReShadeUpdateService
         CrashReporter.Log("[ReShadeUpdateService.EnsureLatestAsync] Started");
         Directory.CreateDirectory(CacheDir);
 
-        progress?.Report(("Checking for ReShade updates...", 5));
+        progress?.Report(("Проверка обновлений ReShade...", 5));
         var latest = await CheckLatestVersionAsync();
         if (latest == null)
         {
@@ -99,7 +99,7 @@ public class ReShadeUpdateService : IReShadeUpdateService
             && new FileInfo(AuxInstallService.RsStagedPath32).Length > 1_000_000)
         {
             CrashReporter.Log($"[ReShadeUpdateService.EnsureLatestAsync] Already have v{version}");
-            progress?.Report(($"ReShade {version} is current", 100));
+            progress?.Report(($"ReShade {version} актуален", 100));
             return false;
         }
 
@@ -202,7 +202,7 @@ public class ReShadeUpdateService : IReShadeUpdateService
         }
 
         // Extract DLLs from the exe (it has an appended ZIP archive)
-        progress?.Report(("Extracting ReShade DLLs...", 80));
+        progress?.Report(("Распаковка DLL ReShade...", 80));
         try
         {
             _extractor.ExtractFile(exePath, "ReShade64.dll", AuxInstallService.RsStagedPath64);
@@ -236,7 +236,7 @@ public class ReShadeUpdateService : IReShadeUpdateService
         }
         catch (Exception ex) { CrashReporter.Log($"[ReShadeUpdateService.EnsureLatestAsync] Failed to clean up old installers — {ex.Message}"); }
 
-        progress?.Report(($"ReShade {version} ready!", 100));
+        progress?.Report(($"ReShade {version} готов!", 100));
         CrashReporter.Log($"[ReShadeUpdateService.EnsureLatestAsync] Staged v{version} successfully");
         return true;
     }
diff --git a/RenoDXCommander/Services/Renodx5AddonService.cs b/RenoDXCommander/Services/Renodx5AddonService.cs
index 5928568..e0293e9 100644
--- a/RenoDXCommander/Services/Renodx5AddonService.cs
+++ b/RenoDXCommander/Services/Renodx5AddonService.cs
@@ -100,7 +100,7 @@ public class Renodx5AddonService
             return;
         }
         await DownloadAndStageAsync(TagPrefix, StagedFileName, "renodx-dlss5", _versionFile,
-            "RenoDX DLSS5 addon", progress,
+            "Аддон RenoDX DLSS5", progress,
             v => { HasUpdate = false; LatestVersion = v; },
             () => AutoRedeployAsync(StagedFileName, DeployFileName, "Renodx5AddonService.AutoRedeployAsync")).ConfigureAwait(false);
     }
@@ -587,9 +587,9 @@ public class Renodx5AddonService
             return;
         }
 
-        progress?.Report(($"{displayName} ready", 90));
+        progress?.Report(($"{displayName} готов", 90));
         await autoRedeploy().ConfigureAwait(false);
-        progress?.Report(($"{displayName} ready", 100));
+        progress?.Report(($"{displayName} готов", 100));
     }
 
     private async Task<(string? version, string? downloadUrl)> FetchLatestReleaseInfoAsync(
@@ -835,7 +835,7 @@ public class Renodx5AddonService
 
             _crashReporter.Log($"[Renodx5AddonService.FetchAndCacheAvailableVersionsAsync] " +
                 $"Cached {_dlss5ToolVersions.Count} DLSS5 Tool, {_dlssToolVersions.Count} ShortFuse, " +
-                $"{_feederVersions.Count} Feeder, {_bridgeVersions.Count} Bridge versions");
+                $"версий: {_feederVersions.Count} Feeder, {_bridgeVersions.Count} Bridge");
         }
         catch (Exception ex)
         {
diff --git a/RenoDXCommander/Services/SevenZipExtractor.cs b/RenoDXCommander/Services/SevenZipExtractor.cs
index 7024a05..18e97e3 100644
--- a/RenoDXCommander/Services/SevenZipExtractor.cs
+++ b/RenoDXCommander/Services/SevenZipExtractor.cs
@@ -19,7 +19,7 @@ public class ReShadeExtractor : ISevenZipExtractor
     public void ExtractFile(string exePath, string entryName, string outputPath)
     {
         if (!File.Exists(exePath))
-            throw new FileNotFoundException($"ReShade installer not found: {exePath}");
+            throw new FileNotFoundException($"Установщик ReShade не найден: {exePath}");
 
         Directory.CreateDirectory(Path.GetDirectoryName(outputPath)!);
 
@@ -64,8 +64,8 @@ public class ReShadeExtractor : ISevenZipExtractor
         }
 
         throw new FileNotFoundException(
-            $"Could not extract '{entryName}' from '{Path.GetFileName(exePath)}'.\n" +
-            "The ReShade installer is an NSIS archive which requires 7-Zip to extract.\n" +
+            $"Не удалось извлечь «{entryName}» из «{Path.GetFileName(exePath)}».\n" +
+            "Установщик ReShade — NSIS-архив, для распаковки нужен 7-Zip.\n" +
             "Please install 7-Zip from https://www.7-zip.org/ and restart RDXC.");
     }
 
diff --git a/RenoDXCommander/Services/ShaderPackService.Download.cs b/RenoDXCommander/Services/ShaderPackService.Download.cs
index 9726a4f..d3081e4 100644
--- a/RenoDXCommander/Services/ShaderPackService.Download.cs
+++ b/RenoDXCommander/Services/ShaderPackService.Download.cs
@@ -302,7 +302,7 @@ public partial class ShaderPackService
 
         await SaveStoredVersionAsync(pack.Id, versionToken);
         ClearIncludeCache();
-        progress?.Report($"{pack.DisplayName} updated.");
+        progress?.Report($"{pack.DisplayName} обновлено.");
         CrashReporter.Log($"[ShaderPackService.EnsurePackAsync] [{pack.Id}] Done. Version = {versionToken}");
         }
         finally { packLock.Release(); }
diff --git a/RenoDXCommander/Services/ShaderPackService.cs b/RenoDXCommander/Services/ShaderPackService.cs
index a735158..46e5640 100644
--- a/RenoDXCommander/Services/ShaderPackService.cs
+++ b/RenoDXCommander/Services/ShaderPackService.cs
@@ -68,8 +68,8 @@ public partial class ShaderPackService : IShaderPackService
     public const string GameReShadeShaders = "reshade-shaders";
     public const string GameReShadeOriginal = "reshade-shaders-original";
     internal const string ManagedMarkerFileName = "Managed by RDXC.txt";
-    private const string ManagedMarkerContent = "This folder is managed by RenoDXCommander. Do not edit manually.\n"
-                                                  + "Deleting this file will cause RDXC to treat the folder as user-managed.";
+    private const string ManagedMarkerContent = "Эта папка управляется RenoDXCommander. Не редактируйте вручную.\n"
+                                                  + "Если удалить этот файл, RDXC будет считать папку управляемой пользователем.";
 
     // ── Pack definitions ──────────────────────────────────────────────────────────
 
@@ -113,30 +113,30 @@ public partial class ShaderPackService : IShaderPackService
     {
         new(
             Id          : "Lilium",
-            DisplayName : "Lilium HDR Shaders",
+            DisplayName : "Шейдеры Lilium HDR",
             Kind        : SourceKind.GhRelease,
             Url         : "https://api.github.com/repos/EndlesslyFlowering/ReShade_HDR_shaders/releases/latest",
             IsMinimum   : true,
             AssetExt    : ".7z",
-            Description : "HDR tone mapping and inverse tone mapping shaders",
+            Description : "Шейдеры HDR-тонмаппинга и обратного тонмаппинга",
             Category    : PackCategory.Essential
         ),
         new(
             Id          : "CrosireMaster",
-            DisplayName : "crosire reshade-shaders (master)",
+            DisplayName : "reshade-shaders от crosire (master)",
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/crosire/reshade-shaders/archive/refs/heads/master.zip",
             IsMinimum   : true,
-            Description : "Official ReShade standard effects — full master branch",
+            Description : "Официальные стандартные эффекты ReShade — полная ветка master",
             Category    : PackCategory.Recommended
         ),
         new(
             Id          : "CrosireLegacy",
-            DisplayName : "crosire reshade-shaders (legacy)",
+            DisplayName : "reshade-shaders от crosire (legacy)",
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/crosire/reshade-shaders/archive/refs/heads/legacy.zip",
             IsMinimum   : false,
-            Description : "Legacy ReShade effects (older versions removed from master)",
+            Description : "Устаревшие эффекты ReShade (старые версии, удалённые из master)",
             Category    : PackCategory.Extra
         ),
         new(
@@ -146,16 +146,16 @@ public partial class ShaderPackService : IShaderPackService
             Url         : "https://api.github.com/repos/Filoppi/PumboAutoHDR/releases/latest",
             IsMinimum   : false,
             AssetExt    : ".zip",
-            Description : "Automatic HDR conversion for SDR games",
+            Description : "Автоматическое преобразование SDR-игр в HDR",
             Category    : PackCategory.Recommended
         ),
         new(
             Id          : "SmolbbsoopShaders",
-            DisplayName : "smolbbsoop shaders",
+            DisplayName : "шейдеры smolbbsoop",
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/smolbbsoop/smolbbsoopshaders/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "HDR utility shaders and effects",
+            Description : "Служебные HDR-шейдеры и эффекты",
             Category    : PackCategory.Extra
         ),
         new(
@@ -164,16 +164,16 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/MaxG2D/ReshadeSimpleHDRShaders/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Simple HDR bloom, lens flare, and tone mapping",
+            Description : "Простой HDR-блум, блики и тонмаппинг",
             Category    : PackCategory.Recommended
         ),
         new(
             Id          : "ClshortfuseShaders",
-            DisplayName : "clshortfuse ReShade shaders",
+            DisplayName : "Шейдеры ReShade от clshortfuse",
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/clshortfuse/reshade-shaders/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "HDR and color correction shaders for RenoDX",
+            Description : "HDR- и цветокорректирующие шейдеры для RenoDX",
             Category    : PackCategory.Recommended
         ),
         new(
@@ -182,7 +182,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/CreepySasquatch/potatoFX/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Lightweight post-processing effects for low-end hardware",
+            Description : "Лёгкие эффекты пост-обработки для слабого железа",
             Category    : PackCategory.Extra
         ),
         new(
@@ -191,7 +191,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/Zenteon/Azen/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Zenteon's casual shader collection — experimental effects",
+            Description : "Любительская коллекция шейдеров Zenteon — экспериментальные эффекты",
             Requires    : new[] { "SmolbbsoopShaders" }
         ),
         new(
@@ -200,7 +200,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/CeeJayDK/SweetFX/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "Classic color grading, sharpening, and bloom effects"
+            Description : "Классическая цветокоррекция, резкость и блум"
         ),
         new(
             Id          : "OtisFX",
@@ -208,7 +208,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/FransBouma/OtisFX/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "Cinematic depth of field, light rays, and camera effects"
+            Description : "Кинематографическая глубина резкости, лучи света и эффекты камеры"
         ),
         new(
             Id          : "Depth3D",
@@ -216,7 +216,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/BlueSkyDefender/Depth3D/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "Stereoscopic 3D and depth-based visual effects"
+            Description : "Стереоскопическое 3D и эффекты на основе глубины"
         ),
         new(
             Id          : "DaodanShaders",
@@ -224,7 +224,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/Daodan317081/reshade-shaders/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "Comic, crosshatch, and artistic style effects"
+            Description : "Комиксные, штриховые и художественные стилизации"
         ),
         new(
             Id          : "BrussellShaders",
@@ -232,7 +232,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/brussell1/Shaders/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "Halftone, sketch, and stylized rendering effects"
+            Description : "Полутоновые, эскизные и стилизованные эффекты рендеринга"
         ),
         new(
             Id          : "FubaxShaders",
@@ -240,7 +240,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/Fubaxiusz/fubax-shaders/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "VR-friendly lens distortion and chromatic aberration"
+            Description : "Дисторсия и хроматическая аберрация для VR"
         ),
         new(
             Id          : "qUINT",
@@ -248,7 +248,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/martymcmodding/qUINT/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "MXAO, ADOF, lightroom, and screen-space reflections"
+            Description : "MXAO, ADOF, lightroom и отражения в экранном пространстве"
         ),
         new(
             Id          : "AlucardDH",
@@ -256,7 +256,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/AlucardDH/dh-reshade-shaders/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "Ambient occlusion, undither, and color enhancement"
+            Description : "Ambient occlusion, устранение дизеринга и улучшение цвета"
         ),
         new(
             Id          : "WarpFX",
@@ -264,15 +264,15 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/Radegast-FFXIV/Warp-FX/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "Screen warp, swirl, and distortion effects"
+            Description : "Искажение, завихрение и деформация экрана"
         ),
         new(
             Id          : "Prod80",
-            DisplayName : "Color effects by prod80",
+            DisplayName : "Цветовые эффекты от prod80",
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/prod80/prod80-ReShade-Repository/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "Professional color grading, curves, and tone tools"
+            Description : "Профессиональная цветокоррекция, кривые и тон-инструменты"
         ),
         new(
             Id          : "CorgiFX",
@@ -280,7 +280,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/originalnicodr/CorgiFX/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "Screenshot and virtual photography tools"
+            Description : "Инструменты для скриншотов и виртуальной фотографии"
         ),
         new(
             Id          : "InsaneShaders",
@@ -288,7 +288,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/LordOfLunacy/Insane-Shaders/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "Advanced dithering, fog removal, and edge detection"
+            Description : "Продвинутое дизеринг, удаление тумана и выделение границ"
         ),
         new(
             Id          : "CobraFX",
@@ -296,7 +296,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/LordKobra/CobraFX/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "Gravity, auto-focus, and real-time ray tracing effects"
+            Description : "Гравитация, автофокус и эффекты трассировки лучей в реальном времени"
         ),
         new(
             Id          : "AstrayFX",
@@ -304,7 +304,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/BlueSkyDefender/AstrayFX/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "Depth-based fog, haze, and atmospheric effects"
+            Description : "Туман, дымка и атмосферные эффекты на основе глубины"
         ),
         new(
             Id          : "CRTRoyale",
@@ -312,7 +312,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/akgunter/crt-royale-reshade/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "CRT monitor simulation with phosphor and scanline emulation"
+            Description : "Имитация ЭЛТ-монитора с эмуляцией люминофора и строк развёртки"
         ),
         new(
             Id          : "RSRetroArch",
@@ -320,7 +320,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/Matsilagi/RSRetroArch/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "RetroArch shader ports — CRT, LCD, and retro filters"
+            Description : "Порты шейдеров RetroArch — фильтры CRT, LCD и ретро"
         ),
         new(
             Id          : "VRToolkit",
@@ -328,7 +328,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/retroluxfilm/reshade-vrtoolkit/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Sharpening and clarity tools optimized for VR headsets"
+            Description : "Инструменты резкости и чёткости, оптимизированные для VR-гарнитур"
         ),
         new(
             Id          : "FGFX",
@@ -336,7 +336,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/AlexTuduran/FGFX/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Film grain, multi-LUT, and cinematic post-processing"
+            Description : "Плёночное зерно, мульты-LUT и кинематографичный пост-процессинг"
         ),
         new(
             Id          : "CShade",
@@ -344,7 +344,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/papadanku/CShade/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Optical flow, motion blur, and convolution effects"
+            Description : "Оптический поток, размытие в движении и свёрточные эффекты"
         ),
         new(
             Id          : "iMMERSE",
@@ -352,7 +352,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/martymcmodding/iMMERSE/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Next-gen RTGI, MXAO, and anti-aliasing suite"
+            Description : "RTGI нового поколения, MXAO и набор сглаживания"
         ),
         new(
             Id          : "VortShaders",
@@ -360,7 +360,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/vortigern11/vort_Shaders/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Sharpening, color correction, and depth effects"
+            Description : "Резкость, цветокоррекция и эффекты глубины"
         ),
         new(
             Id          : "BXShade",
@@ -368,7 +368,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/liuxd17thu/BX-Shade/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Bloom, exposure, and color enhancement effects"
+            Description : "Эффекты блума, экспозиции и улучшения цвета"
         ),
         new(
             Id          : "SHADERDECK",
@@ -376,7 +376,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/IAmTreyM/SHADERDECK/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Curated collection of color and lighting effects"
+            Description : "Отобранная коллекция цветовых эффектов и освещения"
         ),
         new(
             Id          : "METEOR",
@@ -384,7 +384,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/martymcmodding/METEOR/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Advanced denoiser and image reconstruction"
+            Description : "Продвинутый шумоподавитель и реконструкция изображения"
         ),
         new(
             Id          : "AnnReShade",
@@ -392,7 +392,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/AnastasiaGals/Ann-ReShade/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Soft bloom, color grading, and ambient light presets"
+            Description : "Мягкий блум, цветокоррекция и пресеты окружающего света"
         ),
         new(
             Id          : "ZenteonFX",
@@ -400,7 +400,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/Zenteon/ZenteonFX/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Global illumination, SSR, and path tracing effects"
+            Description : "Глобальное освещение, SSR и трассировка пути"
         ),
         new(
             Id          : "GShadeShaders",
@@ -408,7 +408,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/Mortalitas/GShade-Shaders/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "Large collection of community shaders from GShade"
+            Description : "Большая коллекция общественных шейдеров из GShade"
         ),
         new(
             Id          : "PthoFX",
@@ -416,7 +416,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/PthoEastCoast/Ptho-FX/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Cinematic color grading and film emulation"
+            Description : "Кинематографическая цветокоррекция и имитация плёнки"
         ),
         new(
             Id          : "Anagrama",
@@ -424,7 +424,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/nullfrctl/reshade-shaders/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Artistic and experimental visual effects"
+            Description : "Художественные и экспериментальные визуальные эффекты"
         ),
         new(
             Id          : "BarbatosShaders",
@@ -432,7 +432,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/BarbatosBachiko/Reshade-Shaders/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Ambient occlusion, bloom, and color effects"
+            Description : "Ambient occlusion, блум и цветовые эффекты"
         ),
         new(
             Id          : "BFBFX",
@@ -440,7 +440,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/yplebedev/BFBFX/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Stylized and artistic post-processing effects"
+            Description : "Стилизованные и художественные эффекты пост-обработки"
         ),
         new(
             Id          : "Rendepth",
@@ -448,7 +448,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/outmode/rendepth-reshade/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Depth-based 3D rendering and stereo effects"
+            Description : "Объёмный 3D-рендеринг на основе глубины и стереоэффекты"
         ),
         new(
             Id          : "CropAndResize",
@@ -456,7 +456,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/P0NYSLAYSTATION/Scaling-Shaders/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Screen cropping, scaling, and aspect ratio tools"
+            Description : "Обрезка экрана, масштабирование и инструменты пропорций"
         ),
         new(
             Id          : "FXShaders",
@@ -464,7 +464,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/luluco250/FXShaders/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "Bloom, grain, dithering, and utility shader library"
+            Description : "Блум, зерно, дизеринг и библиотека служебных шейдеров"
         ),
         new(
             Id          : "LumeniteFX",
@@ -472,7 +472,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/umar-afzaal/LumeniteFX/archive/refs/heads/mainline.zip",
             IsMinimum   : false,
-            Description : "Lighting, bloom, and atmospheric glow effects"
+            Description : "Освещение, блум и атмосферное свечение"
         ),
         new(
             Id          : "NNShaders",
@@ -480,7 +480,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/Sarenya/NN-Shaders/archive/refs/heads/master.zip",
             IsMinimum   : false,
-            Description : "Neural network-based image processing shaders"
+            Description : "Шейдеры обработки изображений на нейросетях"
         ),
         new(
             Id          : "QdOledAplFixer",
@@ -488,7 +488,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/mspeedo/QD-OLED-APL-FIXER/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "HDR brightness boost to compensate for QD-OLED ABL dimming"
+            Description : "Повышение яркости HDR для компенсации затемнения ABL на QD-OLED"
         ),
         new(
             Id          : "GlamaryeFX",
@@ -496,7 +496,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/rj200/Glamarye_Fast_Effects_for_ReShade/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "Lightweight all-in-one: sharpening, AO, indirect lighting, and color correction in a single pass"
+            Description : "Лёгкий универсальный набор: резкость, AO, непрямое освещение и цветокоррекция за один проход"
         ),
         new(
             Id          : "LumaBoost",
@@ -504,15 +504,15 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/Valadore/LumaBoost/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "OLED ABL compensation — dynamically lifts midtones to counteract auto brightness limiting"
+            Description : "Компенсация ABL для OLED — динамически поднимает полутона против автоограничения яркости"
         ),
         new(
             Id          : "DLSS5Feeder",
-            DisplayName : "DLSS5 Feeder shader",
+            DisplayName : "Шейдер DLSS5 Feeder",
             Kind        : SourceKind.DirectUrl,
             Url         : "",   // no URL — seeded from the Feeder addon zip by RHI, never downloaded
             IsMinimum   : false,
-            Description : "DLSS5_Feed.fx — seeded from the Feeder addon zip by RHI. Not shown in the shader picker.",
+            Description : "DLSS5_Feed.fx — распаковывается RHI из zip аддона Feeder. В списке шейдеров не показывается.",
             Category    : PackCategory.Extra
         ),
         new(
@@ -521,7 +521,7 @@ public partial class ShaderPackService : IShaderPackService
             Kind        : SourceKind.DirectUrl,
             Url         : "https://github.com/clshortfuse/renofx/archive/refs/heads/main.zip",
             IsMinimum   : false,
-            Description : "SDR to HDR conversion, tone mapping, and color grading for games without a RenoDX mod",
+            Description : "Преобразование SDR в HDR, тонмаппинг и цветокоррекция для игр без мода RenoDX",
             Category    : PackCategory.Recommended
         ),
     };
diff --git a/RenoDXCommander/Services/TrayIconService.cs b/RenoDXCommander/Services/TrayIconService.cs
index 861fa4b..4bb9af4 100644
--- a/RenoDXCommander/Services/TrayIconService.cs
+++ b/RenoDXCommander/Services/TrayIconService.cs
@@ -110,7 +110,7 @@ public static class TrayIconService
             AppendMenu(hMenu, MF_SEPARATOR, 0, null);
         }
 
-        AppendMenu(hMenu, MF_STRING, 1, "Open RHI");
+        AppendMenu(hMenu, MF_STRING, 1, "Открыть RHI");
         AppendMenu(hMenu, MF_STRING, 2, "Exit");
 
         // Required for the menu to close when clicking away
diff --git a/RenoDXCommander/Services/UltimateAsiLoaderService.cs b/RenoDXCommander/Services/UltimateAsiLoaderService.cs
index f479435..09b57c6 100644
--- a/RenoDXCommander/Services/UltimateAsiLoaderService.cs
+++ b/RenoDXCommander/Services/UltimateAsiLoaderService.cs
@@ -130,7 +130,7 @@ public class UltimateAsiLoaderService
         }
 
         Directory.CreateDirectory(stagingDir);
-        progress?.Report(("Downloading ASI Loader...", 10));
+        progress?.Report(("Загрузка ASI Loader...", 10));
 
         var (version, assets, body) = await FetchLatestReleaseInfoAsync().ConfigureAwait(false);
         if (string.IsNullOrEmpty(version) || assets == null || !assets.TryGetValue(assetName, out var downloadUrl))
@@ -139,7 +139,7 @@ public class UltimateAsiLoaderService
             return;
         }
 
-        progress?.Report(("Downloading ASI Loader...", 30));
+        progress?.Report(("Загрузка ASI Loader...", 30));
 
         try
         {
@@ -172,7 +172,7 @@ public class UltimateAsiLoaderService
             _crashReporter.Log($"[UltimateAsiLoaderService.EnsureStagingAsync] Failed — {ex.Message}");
         }
 
-        progress?.Report(("ASI Loader ready", 100));
+        progress?.Report(("ASI Loader готов", 100));
     }
 
     // ── Install / Uninstall ───────────────────────────────────────────────────
@@ -248,7 +248,7 @@ public class UltimateAsiLoaderService
             }
         }
 
-        progress?.Report(("Deploying ASI Loader...", 80));
+        progress?.Report(("Развертывание ASI Loader...", 80));
 
         try
         {
@@ -280,7 +280,7 @@ public class UltimateAsiLoaderService
         };
         _auxInstaller.SaveAuxRecord(record);
 
-        progress?.Report(("ASI Loader installed!", 100));
+        progress?.Report(("ASI Loader установлен!", 100));
         return (true, hookedOriginal);
     }
 
diff --git a/RenoDXCommander/Services/UpdateOrchestrationService.cs b/RenoDXCommander/Services/UpdateOrchestrationService.cs
index fa5e449..ae17d85 100644
--- a/RenoDXCommander/Services/UpdateOrchestrationService.cs
+++ b/RenoDXCommander/Services/UpdateOrchestrationService.cs
@@ -134,7 +134,7 @@ public class UpdateOrchestrationService : IUpdateOrchestrationService
                     card.InstalledAddonFileName = record.AddonFileName;
                     card.RdxInstalledVersion    = AuxInstallService.ReadInstalledVersion(record.InstallPath, record.AddonFileName);
                     card.Status                 = GameStatus.Installed;
-                    card.ActionMessage          = "✅ Updated!";
+                    card.ActionMessage          = "✅ Обновлено!";
                     card.NotifyAll();
                     card.FadeMessage(m => card.ActionMessage = m, card.ActionMessage);
                 });
@@ -238,7 +238,7 @@ public class UpdateOrchestrationService : IUpdateOrchestrationService
                     card.RsInstalledFile    = record.InstalledAs;
                     card.RsInstalledVersion = _auxFileService.ReadInstalledVersion(record.InstallPath, record.InstalledAs);
                     card.RsStatus           = GameStatus.Installed;
-                    card.RsActionMessage    = "✅ Updated!";
+                    card.RsActionMessage    = "✅ Обновлено!";
                     card.NotifyAll();
                     card.FadeMessage(m => card.RsActionMessage = m, card.RsActionMessage);
                 });
@@ -382,7 +382,7 @@ public class UpdateOrchestrationService : IUpdateOrchestrationService
             foreach (var vCard in vulkanTargets)
             {
                 vCard.RsIsInstalling = true;
-                vCard.RsActionMessage = "Updating Vulkan ReShade...";
+                vCard.RsActionMessage = "Обновление Vulkan ReShade...";
                 try
                 {
                     if (keepRsIniUpdatedResolver == null || keepRsIniUpdatedResolver(vCard.GameName, vCard.Source ?? ""))
@@ -394,7 +394,7 @@ public class UpdateOrchestrationService : IUpdateOrchestrationService
                     {
                         vCard.RsInstalledVersion = vulkanVersion;
                         vCard.RsStatus = GameStatus.Installed;
-                        vCard.RsActionMessage = layerUpdated ? "✅ Updated!" : "✅ Up to date";
+                        vCard.RsActionMessage = layerUpdated ? "✅ Обновлено!" : "✅ Актуально";
                         vCard.NotifyAll();
                         vCard.FadeMessage(m => vCard.RsActionMessage = m, vCard.RsActionMessage);
                     });
@@ -460,7 +460,7 @@ public class UpdateOrchestrationService : IUpdateOrchestrationService
                     card.RefRecord = record;
                     card.RefInstalledVersion = record.InstalledVersion;
                     card.RefStatus = GameStatus.Installed;
-                    card.RefActionMessage = "✅ Updated!";
+                    card.RefActionMessage = "✅ Обновлено!";
                     card.NotifyAll();
                     card.FadeMessage(m => card.RefActionMessage = m, card.RefActionMessage);
                 });
@@ -506,13 +506,13 @@ public class UpdateOrchestrationService : IUpdateOrchestrationService
                     {
                         card.DofFixInstalledVersion = dofFixService.StagedVersion;
                         card.DofFixStatus = GameStatus.Installed;
-                        card.DofFixActionMessage = "✅ Updated!";
+                        card.DofFixActionMessage = "✅ Обновлено!";
                         card.NotifyAll();
                         card.FadeMessage(m => card.DofFixActionMessage = m, card.DofFixActionMessage);
                     }
                     else
                     {
-                        card.DofFixActionMessage = "❌ Update failed";
+                        card.DofFixActionMessage = "❌ Не удалось обновить";
                     }
                 });
             }
diff --git a/RenoDXCommander/Services/UpdateService.cs b/RenoDXCommander/Services/UpdateService.cs
index 74ab3aa..45a40e6 100644
--- a/RenoDXCommander/Services/UpdateService.cs
+++ b/RenoDXCommander/Services/UpdateService.cs
@@ -197,7 +197,7 @@ public class UpdateService : IUpdateService
     {
         try
         {
-            progress?.Report(("Downloading update...", 0));
+            progress?.Report(("Загрузка обновления...", 0));
 
             var request = new HttpRequestMessage(HttpMethod.Get, downloadUrl);
             request.Headers.UserAgent.Add(new ProductInfoHeaderValue("RHI", CurrentVersion.ToString()));
@@ -228,18 +228,18 @@ public class UpdateService : IUpdateService
                 if (totalBytes > 0)
                 {
                     var pct = (double)totalRead / totalBytes * 100;
-                    progress?.Report(($"Downloading update... {pct:F0}%", pct));
+                    progress?.Report(($"Загрузка обновления... {pct:F0}%", pct));
                 }
             }
 
-            progress?.Report(("Download complete.", 100));
+            progress?.Report(("Загрузка завершена.", 100));
             CrashReporter.Log($"[UpdateService.DownloadInstallerAsync] Downloaded installer to {tempPath} ({totalRead:N0} bytes)");
             return tempPath;
         }
         catch (Exception ex)
         {
             CrashReporter.Log($"[UpdateService.DownloadInstallerAsync] Download failed — {ex.Message}");
-            progress?.Report(($"Download failed: {ex.Message}", 0));
+            progress?.Report(($"Ошибка загрузки: {ex.Message}", 0));
             return null;
         }
     }
diff --git a/RenoDXCommander/Services/VulkanFootprintService.cs b/RenoDXCommander/Services/VulkanFootprintService.cs
index 4351e23..2eed25c 100644
--- a/RenoDXCommander/Services/VulkanFootprintService.cs
+++ b/RenoDXCommander/Services/VulkanFootprintService.cs
@@ -10,8 +10,8 @@ public static class VulkanFootprintService
 {
     public const string FootprintFileName = "RDXC_VULKAN_FOOTPRINT";
     public const string FootprintContent =
-        "Managed by RHI — Vulkan ReShade footprint.\n"
-      + "Do not delete this file while Vulkan ReShade is in use.";
+        "Управляется RHI — след Vulkan ReShade.\n"
+      + "Не удаляйте этот файл, пока используется Vulkan ReShade.";
 
     /// <summary>
     /// Creates the footprint file in the game directory.
diff --git a/RenoDXCommander/Services/VulkanLayerService.cs b/RenoDXCommander/Services/VulkanLayerService.cs
index 5a11e2a..3328ce0 100644
--- a/RenoDXCommander/Services/VulkanLayerService.cs
+++ b/RenoDXCommander/Services/VulkanLayerService.cs
@@ -107,7 +107,7 @@ public static class VulkanLayerService
     {
         if (!IsRunningAsAdmin())
             throw new UnauthorizedAccessException(
-                "Administrator privileges are required to install the Vulkan ReShade layer.");
+                "Для установки слоя Vulkan ReShade требуются права администратора.");
 
         var stagedDll = AuxInstallService.RsStagedPath64;
         if (!File.Exists(stagedDll))
@@ -163,7 +163,7 @@ public static class VulkanLayerService
     {
         if (!IsRunningAsAdmin())
             throw new UnauthorizedAccessException(
-                "Administrator privileges are required to uninstall the Vulkan ReShade layer.");
+                "Для удаления слоя Vulkan ReShade требуются права администратора.");
 
         UninstallLayer(Registry.LocalMachine, RegistryKeyPath, LayerManifestPath, LayerDllPath);
     }
diff --git a/RenoDXCommander/Services/WikiService.cs b/RenoDXCommander/Services/WikiService.cs
index 2ee15af..04d5890 100644
--- a/RenoDXCommander/Services/WikiService.cs
+++ b/RenoDXCommander/Services/WikiService.cs
@@ -27,7 +27,7 @@ public class WikiService : IWikiService
     public async Task<(List<GameMod> Mods, Dictionary<string, string> GenericNotes)>
         FetchAllAsync(IProgress<string>? progress = null)
     {
-        progress?.Report("Fetching wiki...");
+        progress?.Report("Загрузка вики...");
         var html = await _http.GetStringAsync(WikiUrl).ConfigureAwait(false);
         var doc  = new HtmlDocument();
         doc.LoadHtml(html);
@@ -120,7 +120,7 @@ public class WikiService : IWikiService
             CrashReporter.Log($"[WikiService.FetchAllAsync] Normalized names ({allNorms.Count}): [{string.Join(", ", allNorms)}]");
         }
 
-        progress?.Report($"Found {mods.Count} mods, {genericNotes.Count} generic game notes");
+        progress?.Report($"Найдено модов: {mods.Count}, общих заметок по играм: {genericNotes.Count}");
         return (mods, genericNotes);
     }
 
diff --git a/RenoDXCommander/SettingsHandler.cs b/RenoDXCommander/SettingsHandler.cs
index 48d9e06..622b491 100644
--- a/RenoDXCommander/SettingsHandler.cs
+++ b/RenoDXCommander/SettingsHandler.cs
@@ -300,7 +300,7 @@ public class SettingsHandler
 
         // Global ReBAR Enable
         var isAdminForReBar = VulkanLayerService.IsRunningAsAdmin();
-        _window.GlobalReBarEnableCombo.ItemsSource = new[] { "Auto (Default)", "Off", "On" };
+        _window.GlobalReBarEnableCombo.ItemsSource = new[] { "Авто (по умолчанию)", "Off", "On" };
         var globalReBarMode = snapshot.ReBarEnableMode ?? 1; // 0=Off, 1=Auto, 2=On
         _window.GlobalReBarEnableCombo.SelectedIndex = globalReBarMode == 0 ? 1 : globalReBarMode == 2 ? 2 : 0;
         _window.GlobalReBarEnableCombo.IsEnabled = isAdminForReBar;
@@ -466,12 +466,12 @@ public class SettingsHandler
 
         // Show confirmation dialog
         var appliedSettings = string.IsNullOrEmpty(screenshotPath)
-            ? "ReShade hotkeys and effect list style"
-            : "Screenshot path, ReShade hotkeys, and effect list style";
+            ? "Горячие клавиши ReShade и стиль списка эффектов"
+            : "Путь к скриншотам, горячие клавиши ReShade и стиль списка эффектов";
         var dialog = new ContentDialog
         {
-            Title = "Screenshots & Hotkeys",
-            Content = $"{appliedSettings} applied to {updatedCount} game{(updatedCount == 1 ? "" : "s")}.",
+            Title = "Скриншоты и горячие клавиши",
+            Content = $"{appliedSettings} применено к играм: {updatedCount}{(updatedCount == 1 ? "" : "s")}.",
             CloseButtonText = "OK",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -522,8 +522,8 @@ public class SettingsHandler
         {
             var emptyDialog = new ContentDialog
             {
-                Title = "Peak Nits",
-                Content = "Peak nits is not configured or is disabled.",
+                Title = "Пиковая яркость",
+                Content = "Пиковая яркость не настроена или отключена.",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -562,8 +562,8 @@ public class SettingsHandler
 
         var dialog = new ContentDialog
         {
-            Title = "Peak Nits",
-            Content = $"Applied peak nits ({peakNits}) to {updatedCount} reshade.ini file{(updatedCount == 1 ? "" : "s")}.",
+            Title = "Пиковая яркость",
+            Content = $"Пиковая яркость ({peakNits}) применена к файлам reshade.ini ({updatedCount}){(updatedCount == 1 ? "" : "s")}.",
             CloseButtonText = "OK",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -581,7 +581,7 @@ public class SettingsHandler
         System.Diagnostics.Process.Start(new System.Diagnostics.ProcessStartInfo(logsDir) { UseShellExecute = true });
     }
 
-    private const string AdminTaskName = "RHI Admin Mode";
+    private const string AdminTaskName = "Режим администратора RHI";
     private bool _adminComboInit = true;
 
     /// <summary>
@@ -612,8 +612,8 @@ public class SettingsHandler
         {
             await DialogService.ShowSafeAsync(new ContentDialog
             {
-                Title = "Admin Mode",
-                Content = "UAC is disabled on this system — RHI always runs as administrator.",
+                Title = "Режим администратора",
+                Content = "UAC в этой системе отключён — RHI всегда работает от администратора.",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -634,10 +634,10 @@ public class SettingsHandler
             // Show restart notice
             await DialogService.ShowSafeAsync(new ContentDialog
             {
-                Title = "Admin Mode",
+                Title = "Режим администратора",
                 Content = enable
-                    ? "Admin Mode enabled. Restart RHI for it to take effect."
-                    : "Admin Mode disabled. RHI will launch normally on next start.",
+                    ? "Режим администратора включён. Перезапустите RHI, чтобы он вступил в силу."
+                    : "Режим администратора отключён. RHI запустится в обычном режиме в следующий раз.",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -792,8 +792,8 @@ public class SettingsHandler
             {
                 await DialogService.ShowSafeAsync(new ContentDialog
                 {
-                    Title = "Game Data Copied",
-                    Content = $"Data for {ViewModel.AllCards.Count} games has been gathered and copied to your clipboard. Paste directly into Discord to share.",
+                    Title = "Данные об игре скопированы",
+                    Content = $"Данные по играм ({ViewModel.AllCards.Count}) собраны и скопированы в буфер обмена. Вставьте прямо в Discord, чтобы поделиться.",
                     CloseButtonText = "OK",
                     XamlRoot = fe.XamlRoot,
                     RequestedTheme = Microsoft.UI.Xaml.ElementTheme.Dark,
@@ -957,8 +957,8 @@ public class SettingsHandler
             {
                 await DialogService.ShowSafeAsync(new ContentDialog
                 {
-                    Title = "Logs Copied",
-                    Content = "All session logs have been archived and copied to your clipboard. Paste directly into Discord to share.",
+                    Title = "Журналы скопированы",
+                    Content = "Все журналы сеанса заархивированы и скопированы в буфер обмена. Вставьте их прямо в Discord, чтобы поделиться.",
                     CloseButtonText = "OK",
                     XamlRoot = fe.XamlRoot,
                 });
@@ -975,10 +975,10 @@ public class SettingsHandler
         // Show warning dialog
         var warningDialog = new ContentDialog
         {
-            Title = "⚠ Purge Staging Files",
-            Content = "This will delete cached DLSS, Streamline, and component staging files to free disk space.\n\nShaders, installed RenoDX addons, and version metadata are preserved.\n\nThese files will be re-downloaded automatically when needed.\n\nContinue?",
-            PrimaryButtonText = "Purge",
-            CloseButtonText = "Cancel",
+            Title = "⚠ Очистить кеш загрузок",
+            Content = "Будут удалены кешированные файлы DLSS, Streamline и компонентов, чтобы освободить место.\n\nШейдеры, установленные ",
+            PrimaryButtonText = "Очистить",
+            CloseButtonText = "Отмена",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -1090,15 +1090,15 @@ public class SettingsHandler
                 >= 1_073_741_824 => $"{bytesFreed / 1_073_741_824.0:F1} GB",
                 >= 1_048_576 => $"{bytesFreed / 1_048_576.0:F1} MB",
                 >= 1024 => $"{bytesFreed / 1024.0:F1} KB",
-                _ => $"{bytesFreed} bytes"
+                _ => $"{bytesFreed} байт"
             };
 
             CrashReporter.Log($"[SettingsHandler.PurgeCachedFiles_Click] Purged {filesDeleted} files, freed {sizeStr}");
 
             var resultDialog = new ContentDialog
             {
-                Title = "✅ Cache Purged",
-                Content = $"Deleted {filesDeleted} files, freed {sizeStr} of disk space.",
+                Title = "✅ Кеш очищен",
+                Content = $"Удалено файлов: {filesDeleted}, освобождено места: {sizeStr}.",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -1110,8 +1110,8 @@ public class SettingsHandler
             CrashReporter.Log($"[SettingsHandler.PurgeCachedFiles_Click] Failed: {ex.Message}");
             var errDialog = new ContentDialog
             {
-                Title = "❌ Purge Failed",
-                Content = $"An error occurred: {ex.Message}",
+                Title = "❌ Не удалось очистить",
+                Content = $"Произошла ошибка: {ex.Message}",
                 CloseButtonText = "OK",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
@@ -1223,8 +1223,8 @@ public class SettingsHandler
         // Req 4.5: Show confirmation dialog with count of updated files
         var dialog = new ContentDialog
         {
-            Title = "ReShade UI Hotkey",
-            Content = $"Updated {updatedCount} reshade.ini file{(updatedCount == 1 ? "" : "s")}.",
+            Title = "Горячая клавиша интерфейса ReShade",
+            Content = $"Обновлено файлов reshade.ini: {updatedCount}{(updatedCount == 1 ? "" : "s")}.",
             CloseButtonText = "OK",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -1278,8 +1278,8 @@ public class SettingsHandler
 
         var dialog = new ContentDialog
         {
-            Title = "ReShade Hotkeys",
-            Content = $"Updated {updatedCount} reshade.ini file{(updatedCount == 1 ? "" : "s")}.",
+            Title = "Горячие клавиши ReShade",
+            Content = $"Обновлено файлов reshade.ini: {updatedCount}{(updatedCount == 1 ? "" : "s")}.",
             CloseButtonText = "OK",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -1371,8 +1371,8 @@ public class SettingsHandler
 
         var dialog = new ContentDialog
         {
-            Title = "ReShade Screenshot Hotkey",
-            Content = $"Updated {updatedCount} reshade.ini file{(updatedCount == 1 ? "" : "s")}.",
+            Title = "Горячая клавиша скриншотов ReShade",
+            Content = $"Обновлено файлов reshade.ini: {updatedCount}{(updatedCount == 1 ? "" : "s")}.",
             CloseButtonText = "OK",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -1493,8 +1493,8 @@ public class SettingsHandler
 
         var dialog = new ContentDialog
         {
-            Title = "ReLimiter Settings",
-            Content = $"Updated {updatedCount} relimiter.ini file{(updatedCount == 1 ? "" : "s")}.",
+            Title = "Настройки ReLimiter",
+            Content = $"Обновлено файлов relimiter.ini: {updatedCount}{(updatedCount == 1 ? "" : "s")}.",
             CloseButtonText = "OK",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -1594,7 +1594,7 @@ public class SettingsHandler
             var panel = new StackPanel { Spacing = 8 };
             panel.Children.Add(new TextBlock
             {
-                Text = "Enter a custom FPS value (20-1000):",
+                Text = "Введите своё значение FPS (20–1000):",
                 FontSize = 12,
             });
             var inputBox = new TextBox
@@ -1606,10 +1606,10 @@ public class SettingsHandler
 
             var dialog = new ContentDialog
             {
-                Title = "Custom Target FPS",
+                Title = "Свой целевой FPS",
                 Content = panel,
-                PrimaryButtonText = "Set",
-                CloseButtonText = "Cancel",
+                PrimaryButtonText = "Задать",
+                CloseButtonText = "Отмена",
                 XamlRoot = _window.Content.XamlRoot,
                 RequestedTheme = ElementTheme.Dark,
             };
@@ -1682,7 +1682,7 @@ public class SettingsHandler
         var refCheck = new CheckBox { Content = "RE Framework", IsChecked = !settings.GlobalSkipRefUpdates, FontSize = 12, Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush), Margin = new Thickness(0, 4, 0, 4) };
 
         var checkPanel = new StackPanel { Spacing = 0 };
-        checkPanel.Children.Add(new TextBlock { Text = "Include components in Update All globally:", FontSize = 12, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush), Margin = new Thickness(0, 0, 0, 8) });
+        checkPanel.Children.Add(new TextBlock { Text = "Какие компоненты включать в «Обновить всё» глобально:", FontSize = 12, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush), Margin = new Thickness(0, 0, 0, 8) });
         checkPanel.Children.Add(rsCheck);
         checkPanel.Children.Add(rdxCheck);
         checkPanel.Children.Add(ulCheck);
@@ -1692,10 +1692,10 @@ public class SettingsHandler
 
         var dialog = new ContentDialog
         {
-            Title = "Global Update Inclusion",
+            Title = "Включение в глобальное обновление",
             Content = checkPanel,
-            PrimaryButtonText = "Save",
-            CloseButtonText = "Cancel",
+            PrimaryButtonText = "Сохранить",
+            CloseButtonText = "Отмена",
             XamlRoot = xamlRoot,
             RequestedTheme = ElementTheme.Dark,
         };
@@ -1824,8 +1824,8 @@ public class SettingsHandler
 
         var dialog = new ContentDialog
         {
-            Title = "OptiScaler Hotkey",
-            Content = $"Updated {updatedCount} OptiScaler.ini file{(updatedCount == 1 ? "" : "s")}.",
+            Title = "Горячая клавиша OptiScaler",
+            Content = $"Обновлено файлов OptiScaler.ini: {updatedCount}{(updatedCount == 1 ? "" : "s")}.",
             CloseButtonText = "OK",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -1896,11 +1896,11 @@ public class SettingsHandler
 
         var dialog = new ContentDialog
         {
-            Title = "DXVK Variant Changed",
-            Content = $"DXVK variant changed to {variantLabel}."
+            Title = "Изменён вариант DXVK",
+            Content = $"Вариант DXVK изменён на {variantLabel}."
                 + (gamesWithDxvk.Count > 0
-                    ? $"\n\nSwitching {gamesWithDxvk.Count} game(s) to the {variantLabel} build."
-                    : "\n\nNo games currently have DXVK installed."),
+                    ? $"\n\nПереключение {gamesWithDxvk.Count} игры(игр) на сборку {variantLabel}."
+                    : "\n\nСейчас DXVK не установлен ни в одной игре."),
             CloseButtonText = "OK",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -2050,12 +2050,12 @@ public class SettingsHandler
 
         var dialog = new ContentDialog
         {
-            Title = "ReShade Build Channel Changed",
-            Content = $"ReShade build channel changed to {channelLabel}.\n\n"
+            Title = "Изменён канал сборки ReShade",
+            Content = $"Канал сборки ReShade изменён на {channelLabel}.\n\n"
                 + (totalCount > 0
                     ? $"Switching {totalCount} game(s) to the {channelLabel} build."
-                      + (vulkanCount > 0 ? $"\n{vulkanCount} Vulkan game(s) updated via global layer." : "")
-                    : "No games currently have ReShade installed."),
+                      + (vulkanCount > 0 ? $"\nОбновлено Vulkan-игр через глобальный слой: {vulkanCount}." : "")
+                    : "Сейчас ReShade не установлен ни в одной игре."),
             CloseButtonText = "OK",
             XamlRoot = _window.Content.XamlRoot,
             RequestedTheme = ElementTheme.Dark,
@@ -2109,7 +2109,7 @@ public class SettingsHandler
         }
         else
         {
-            _window.NexusStatusText.Text = "Not connected";
+            _window.NexusStatusText.Text = "Не подключено";
             _window.NexusStatusText.Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush);
             _window.NexusDisconnectRow.Visibility = Microsoft.UI.Xaml.Visibility.Collapsed;
             _window.NexusConnectBtn.Content = "Connect";
@@ -2117,7 +2117,7 @@ public class SettingsHandler
 
         // NXM handler status
         bool nxmRegistered = NxmProtocolHandler.IsRegistered();
-        _window.NxmStatusText.Text = nxmRegistered ? "RHI is the active nxm:// handler" : "Not registered";
+        _window.NxmStatusText.Text = nxmRegistered ? "RHI — активный обработчик nxm://" : "Не зарегистрировано";
         _window.NxmStatusText.Foreground = UIFactory.Brush(
             nxmRegistered ? ResourceKeys.AccentGreenBrush : ResourceKeys.TextSecondaryBrush);
         _window.NxmRegisterBtn.Content = nxmRegistered ? "Unregister" : "Register";
@@ -2129,7 +2129,7 @@ public class SettingsHandler
         var nexusSso = App.Services.GetRequiredService<NexusSsoService>();
 
         _window.NexusConnectBtn.IsEnabled = false;
-        _window.NexusStatusText.Text = "Opening browser for authorisation...";
+        _window.NexusStatusText.Text = "Открываю браузер для авторизации...";
         _window.NexusStatusText.Foreground = UIFactory.Brush(ResourceKeys.TextTertiaryBrush);
 
         using var cts = new System.Threading.CancellationTokenSource(TimeSpan.FromSeconds(120));
@@ -2142,7 +2142,7 @@ public class SettingsHandler
             catch (Exception ex) { CrashReporter.Log($"[SettingsHandler.NexusConnectBtn_Click] Failed to open browser — {ex.Message}"); }
 
             _window.DispatcherQueue?.TryEnqueue(() =>
-                _window.NexusStatusText.Text = "Waiting for authorisation in browser...");
+                _window.NexusStatusText.Text = "Ожидание авторизации в браузере...");
         };
 
         try
@@ -2167,14 +2167,14 @@ public class SettingsHandler
 
                 if (string.IsNullOrEmpty(receivedKey))
                 {
-                    _window.NexusStatusText.Text = "Authorisation timed out or was cancelled.";
+                    _window.NexusStatusText.Text = "Авторизация не дождалась ответа или была отменена.";
                     _window.NexusStatusText.Foreground = UIFactory.Brush(ResourceKeys.AccentRedBrush);
                     return;
                 }
 
                 if (info == null)
                 {
-                    _window.NexusStatusText.Text = "Received key was invalid. Please try again.";
+                    _window.NexusStatusText.Text = "Полученный ключ недействителен. Попробуйте ещё раз.";
                     _window.NexusStatusText.Foreground = UIFactory.Brush(ResourceKeys.AccentRedBrush);
                     return;
                 }
@@ -2216,10 +2216,10 @@ public class SettingsHandler
             {
                 var result = await DialogService.ShowSafeAsync(new ContentDialog
                 {
-                    Title = "NXM Protocol Handler",
-                    Content = "Another application (e.g. Vortex or MO2) is already registered as the nxm:// handler. Registering RHI will replace it. Continue?",
-                    PrimaryButtonText = "Register RHI",
-                    CloseButtonText = "Cancel",
+                    Title = "Обработчик протокола NXM",
+                    Content = "Другое приложение (например, Vortex или MO2) уже зарегистрировано как обработчик nxm://. Регистрация RHI заменит его. Продолжить?",
+                    PrimaryButtonText = "Зарегистрировать RHI",
+                    CloseButtonText = "Отмена",
                     XamlRoot = _window.Content.XamlRoot,
                     RequestedTheme = Microsoft.UI.Xaml.ElementTheme.Dark,
                 });
@@ -2235,8 +2235,8 @@ public class SettingsHandler
             {
                 await DialogService.ShowSafeAsync(new ContentDialog
                 {
-                    Title = "NXM Registration Failed",
-                    Content = $"Could not register the nxm:// handler: {ex.Message}",
+                    Title = "Не удалось зарегистрировать NXM",
+                    Content = $"Не удалось зарегистрировать обработчик nxm://: {ex.Message}",
                     CloseButtonText = "OK",
                     XamlRoot = _window.Content.XamlRoot,
                     RequestedTheme = Microsoft.UI.Xaml.ElementTheme.Dark,
diff --git a/RenoDXCommander/SetupWindow.xaml.cs b/RenoDXCommander/SetupWindow.xaml.cs
index 0a9d5b7..a31c229 100644
--- a/RenoDXCommander/SetupWindow.xaml.cs
+++ b/RenoDXCommander/SetupWindow.xaml.cs
@@ -27,7 +27,7 @@ public sealed partial class SetupWindow : Window
     {
         InitializeComponent();
 
-        Title = "RHI Setup";
+        Title = "Установка RHI";
 
         // Size and position — scale by display DPI so window is correct at any Windows scaling setting
         var hwndForDpi = WindowNative.GetWindowHandle(this);
@@ -97,7 +97,7 @@ public sealed partial class SetupWindow : Window
         // ── Title ──
         root.Children.Add(new TextBlock
         {
-            Text = "Welcome to RHI",
+            Text = "Добро пожаловать в RHI",
             FontSize = 22,
             FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
             Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
@@ -105,14 +105,14 @@ public sealed partial class SetupWindow : Window
 
         // ── Description ──
         const string description =
-            "RHI can manage ReShade across all your games automatically. Here's what that includes:\n\n" +
-            "  - Installs and updates ReShade for each game with one click\n" +
-            "  - Keeps the correct DLL name per game based on what the game needs\n" +
-            "  - Manages shader packs globally - install once, deployed to every game automatically\n" +
-            "  - Backs up any shaders already in your game folders before taking over\n" +
-            "  - Keeps ReShade in sync when you install RenoDX, OptiScaler, or other components\n\n" +
-            "If you already have a custom ReShade setup - specific shader collections, hand-tuned configs, " +
-            "or a version you prefer - choose \"I'll manage it myself\" and RHI will leave ReShade completely alone.";
+            "RHI умеет автоматически управлять ReShade во всех ваших играх. Это включает:\n\n" +
+            "  - Устанавливает и обновляет ReShade для каждой игры в один клик\n" +
+            "  - Подбирает правильное имя DLL для каждой игры в зависимости от её требований\n" +
+            "  - Управляет наборами шейдеров глобально — установите один раз, они развернутся во все игры автоматически\n" +
+            "  - Создаёт резервные копии шейдеров в папках игр перед тем, как взять их под контроль\n" +
+            "  - Синхронизирует ReShade при установке RenoDX, OptiScaler и других компонентов\n\n" +
+            "Если у вас уже своя настройка ReShade — особые наборы шейдеров, вручную подобранные конфиги, " +
+            "или любую удобную версию — выберите «Настрою сам», и RHI вообще не будет трогать ReShade.";
 
         root.Children.Add(new TextBlock
         {
@@ -140,7 +140,7 @@ public sealed partial class SetupWindow : Window
         // "Manage ReShade for me" — accent blue style
         var manageBtn = new Button
         {
-            Content = "Manage ReShade for me",
+            Content = "Пусть ReShade управляет RHI",
             HorizontalAlignment = HorizontalAlignment.Stretch,
             FontSize = 14,
             Padding = new Thickness(12, 10, 12, 10),
@@ -153,7 +153,7 @@ public sealed partial class SetupWindow : Window
 
         var selfBtn = new Button
         {
-            Content = "I'll manage it myself",
+            Content = "Настрою сам",
             HorizontalAlignment = HorizontalAlignment.Stretch,
             FontSize = 14,
             Padding = new Thickness(12, 10, 12, 10),
diff --git a/RenoDXCommander/ShaderPopupHelper.cs b/RenoDXCommander/ShaderPopupHelper.cs
index 4e12073..792fcd6 100644
--- a/RenoDXCommander/ShaderPopupHelper.cs
+++ b/RenoDXCommander/ShaderPopupHelper.cs
@@ -34,16 +34,16 @@ public static class ShaderPopupHelper
         {
             var emptyDlg = new ContentDialog
             {
-                Title             = "Select Shader Packs",
+                Title             = "Выбрать наборы шейдеров",
                 Content           = new TextBlock
                 {
-                    Text       = "No shader packs available.",
+                    Text       = "Доступных наборов шейдеров нет.",
                     FontSize   = 13,
                     Foreground = Brush(ResourceKeys.TextPrimaryBrush),
                 },
                 PrimaryButtonText      = primaryButtonText,
                 IsPrimaryButtonEnabled = false,
-                CloseButtonText        = "Cancel",
+                CloseButtonText        = "Отмена",
                 XamlRoot               = xamlRoot,
                 Background             = Brush(ResourceKeys.SurfaceOverlayBrush),
                 RequestedTheme         = ElementTheme.Dark,
@@ -102,14 +102,14 @@ public static class ShaderPopupHelper
         bool allExpanded = false;
         var expandAllBtn = new Button
         {
-            Content             = "Expand All",
+            Content             = "Развернуть все",
             FontSize            = 12,
             Padding             = new Thickness(8, 4, 8, 4),
             HorizontalAlignment = HorizontalAlignment.Left,
         };
         var deselectAllBtn = new Button
         {
-            Content             = "Deselect All",
+            Content             = "Снять всё",
             FontSize            = 12,
             Padding             = new Thickness(8, 4, 8, 4),
             HorizontalAlignment = HorizontalAlignment.Left,
@@ -124,7 +124,7 @@ public static class ShaderPopupHelper
                 if (expandButtons.TryGetValue(pid, out var eb))
                     eb.Content = allExpanded ? "▼" : "▶";
             }
-            expandAllBtn.Content = allExpanded ? "Collapse All" : "Expand All";
+            expandAllBtn.Content = allExpanded ? "Свернуть все" : "Развернуть все";
         };
         deselectAllBtn.Click += (s, ev) =>
         {
@@ -143,7 +143,7 @@ public static class ShaderPopupHelper
                         eb.Content = "▶";
                 }
                 allExpanded = false;
-                expandAllBtn.Content = "Expand All";
+                expandAllBtn.Content = "Развернуть все";
             }
             finally { profileLoading = false; }
         };
@@ -159,7 +159,7 @@ public static class ShaderPopupHelper
         // ── Search box (right of Deselect All) ───────────────────────────────
         var searchBox = new TextBox
         {
-            PlaceholderText = "Search packs or shaders...",
+            PlaceholderText = "Поиск по наборам и шейдерам...",
             FontSize        = 12,
             MinWidth        = 220,
             Background      = Brush(ResourceKeys.SurfaceInputBrush),
@@ -206,7 +206,7 @@ public static class ShaderPopupHelper
             // ── "Custom Shaders" plain section header (no checkbox, no expand) ──
             panel.Children.Add(new TextBlock
             {
-                Text       = "Custom Shaders",
+                Text       = "Свои шейдеры",
                 FontSize   = 14,
                 FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
                 Foreground = Brush(ResourceKeys.TextPrimaryBrush),
@@ -714,7 +714,7 @@ public static class ShaderPopupHelper
         // Header
         profilePanel.Children.Add(new TextBlock
         {
-            Text       = "Profiles",
+            Text       = "Профили",
             FontSize   = 13,
             FontWeight = Microsoft.UI.Text.FontWeights.SemiBold,
             Foreground = Brush(ResourceKeys.TextPrimaryBrush),
@@ -734,7 +734,7 @@ public static class ShaderPopupHelper
         // Inline rename TextBox for new profile (hidden until "New" is clicked)
         var newProfileBox = new TextBox
         {
-            PlaceholderText = "Profile name",
+            PlaceholderText = "Имя профиля",
             FontSize        = 12,
             Margin          = new Thickness(0, 2, 0, 2),
             Visibility      = Visibility.Collapsed,
@@ -993,7 +993,7 @@ public static class ShaderPopupHelper
             // Save button
             var saveBtn = new Button
             {
-                Content  = "Save",
+                Content  = "Сохранить",
                 FontSize = 12,
                 Padding  = new Thickness(8, 4, 8, 4),
                 Margin   = new Thickness(0, 6, 0, 2),
@@ -1032,10 +1032,10 @@ public static class ShaderPopupHelper
                 rebuildProfileList!();
             };
             profilePanel.Children.Add(saveBtn);
-            ToolTipService.SetToolTip(saveBtn, "Save the current shader selection into the highlighted profile. If no profile is selected, a new one is created automatically.");
+            ToolTipService.SetToolTip(saveBtn, "Сохранить текущий набор шейдеров в выделенный профиль. Если профиль не выбран, будет создан новый.");
             var newBtn = new Button
             {
-                Content  = "New",
+                Content  = "Новое",
                 FontSize = 12,
                 Padding  = new Thickness(8, 4, 8, 4),
                 Margin   = new Thickness(0, 2, 0, 2),
@@ -1053,7 +1053,7 @@ public static class ShaderPopupHelper
                 newProfileBox.SelectAll();
             };
             profilePanel.Children.Add(newBtn);
-            ToolTipService.SetToolTip(newBtn, "Create a new profile from the current shader selection. You'll be prompted to enter a name.");
+            ToolTipService.SetToolTip(newBtn, "Создать новый профиль из текущего набора шейдеров. Потребуется ввести имя.");
             profilePanel.Children.Add(newProfileBox);
 
             // Confirm new profile on Enter or focus lost
@@ -1100,7 +1100,7 @@ public static class ShaderPopupHelper
             // Export button
             var exportBtn = new Button
             {
-                Content  = "Export",
+                Content  = "Экспорт",
                 FontSize = 12,
                 Padding  = new Thickness(8, 4, 8, 4),
                 HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -1135,7 +1135,7 @@ public static class ShaderPopupHelper
 
                     CrashReporter.Log($"[ShaderPopupHelper.ShowAsync] Exported shaders zip to clipboard: {zipPath}");
 
-                    exportStatusLabel.Text       = "Copied to clipboard";
+                    exportStatusLabel.Text       = "Скопировано в буфер обмена";
                     exportStatusLabel.Visibility = Visibility.Visible;
 
                     // Clear after 3 seconds
@@ -1157,13 +1157,13 @@ public static class ShaderPopupHelper
                 }
             };
             profilePanel.Children.Add(exportBtn);
-            ToolTipService.SetToolTip(exportBtn, "Zip the currently selected shader files and copy the archive to your clipboard. Paste directly into Discord to share.");
+            ToolTipService.SetToolTip(exportBtn, "Упакует выбранные файлы шейдеров в zip и скопирует архив в буфер обмена. Вставьте прямо в Discord, чтобы поделиться.");
             profilePanel.Children.Add(exportStatusLabel);
 
             // Import button
             var importBtn = new Button
             {
-                Content  = "Import",
+                Content  = "Импорт",
                 FontSize = 12,
                 Padding  = new Thickness(8, 4, 8, 4),
                 Margin   = new Thickness(0, 2, 0, 0),
@@ -1211,7 +1211,7 @@ public static class ShaderPopupHelper
 
                     if (result == null)
                     {
-                        importStatusLabel.Text       = "Invalid archive — not an RHI shader profile.";
+                        importStatusLabel.Text       = "Неверный архив — это не профиль шейдеров RHI.";
                         importStatusLabel.Foreground = Brush(ResourceKeys.AccentRedBrush);
                         importStatusLabel.Visibility = Visibility.Visible;
                     }
@@ -1233,7 +1233,7 @@ public static class ShaderPopupHelper
                         ApplyProfileToPanel(importedProfile);
 
                         var msg = extractedPackIds.Count > 0
-                            ? $"Imported — {extractedPackIds.Count} pack(s) extracted from archive."
+                            ? $"Импортировано — наборов извлечено из архива: {extractedPackIds.Count}."
                             : "Imported.";
                         importStatusLabel.Text       = msg;
                         importStatusLabel.Foreground = Brush(ResourceKeys.AccentGreenBrush);
@@ -1265,7 +1265,7 @@ public static class ShaderPopupHelper
             profilePanel.Children.Add(new Border { Height = 1, Background = Brush(ResourceKeys.BorderDefaultBrush), Margin = new Thickness(0, 10, 0, 10) });
             var openCustomBtn = new Button
             {
-                Content             = "Open Custom Folder",
+                Content             = "Открыть папку Custom",
                 FontSize            = 12,
                 Padding             = new Thickness(8, 4, 8, 4),
                 HorizontalAlignment = HorizontalAlignment.Stretch,
@@ -1313,10 +1313,10 @@ public static class ShaderPopupHelper
 
         var dlg = new ContentDialog
         {
-            Title             = "Select Shader Packs",
+            Title             = "Выбрать наборы шейдеров",
             Content           = contentGrid,
             PrimaryButtonText = primaryButtonText,
-            CloseButtonText   = "Cancel",
+            CloseButtonText   = "Отмена",
             XamlRoot          = xamlRoot,
             Background        = Brush(ResourceKeys.SurfaceOverlayBrush),
             RequestedTheme    = ElementTheme.Dark,
diff --git a/RenoDXCommander/UpdateInclusionHelper.cs b/RenoDXCommander/UpdateInclusionHelper.cs
index dba15c4..99dbcff 100644
--- a/RenoDXCommander/UpdateInclusionHelper.cs
+++ b/RenoDXCommander/UpdateInclusionHelper.cs
@@ -87,7 +87,7 @@ public static class UpdateInclusionHelper
 
         var button = new Button
         {
-            Content = "Update Inclusion",
+            Content = "Включение в обновление",
             Background = UIFactory.Brush(ResourceKeys.AccentBlueBgBrush),
             Foreground = UIFactory.Brush(ResourceKeys.AccentBlueBrush),
             BorderBrush = UIFactory.Brush(ResourceKeys.AccentBlueBorderBrush),
@@ -121,7 +121,7 @@ public static class UpdateInclusionHelper
                 : null;
 
             var checkPanel = new StackPanel { Spacing = 0 };
-            checkPanel.Children.Add(new TextBlock { Text = "Include this game in Update All for:", FontSize = 12, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush), Margin = new Thickness(0, 0, 0, 8) });
+            checkPanel.Children.Add(new TextBlock { Text = "Включать эту игру в «Обновить всё» для:", FontSize = 12, Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush), Margin = new Thickness(0, 0, 0, 8) });
             checkPanel.Children.Add(rsCheck);
             checkPanel.Children.Add(rdxCheck);
             checkPanel.Children.Add(ulCheck);
@@ -132,10 +132,10 @@ public static class UpdateInclusionHelper
 
             var dialog = new ContentDialog
             {
-                Title = "Global Update Inclusion",
+                Title = "Включение в глобальное обновление",
                 Content = checkPanel,
-                PrimaryButtonText = "Save",
-                CloseButtonText = "Cancel",
+                PrimaryButtonText = "Сохранить",
+                CloseButtonText = "Отмена",
                 XamlRoot = effectiveRoot,
                 RequestedTheme = ElementTheme.Dark,
             };
diff --git a/RenoDXCommander/ViewModels/GameCardViewModel.DisplayCommander.cs b/RenoDXCommander/ViewModels/GameCardViewModel.DisplayCommander.cs
index fb69a8c..756d418 100644
--- a/RenoDXCommander/ViewModels/GameCardViewModel.DisplayCommander.cs
+++ b/RenoDXCommander/ViewModels/GameCardViewModel.DisplayCommander.cs
@@ -13,10 +13,10 @@ public partial class GameCardViewModel
         : DcStatus == GameStatus.Installed ? "🟢" : "⚪";
 
     public string DcActionLabel => DcIsInstalling ? "Installing..."
-        : (!IsRsInstalled && !ExcludeFromUpdateAllReShade) ? "⚠  ReShade required"
-        : DcStatus == GameStatus.UpdateAvailable ? "⬆  Update DC"
-        : DcStatus == GameStatus.Installed ? "↺  Reinstall DC"
-        : "⬇  Install DC";
+        : (!IsRsInstalled && !ExcludeFromUpdateAllReShade) ? "⚠  Требуется ReShade"
+        : DcStatus == GameStatus.UpdateAvailable ? "⬆  Обновить DC"
+        : DcStatus == GameStatus.Installed ? "↺  Переустановить DC"
+        : "⬇  Установить DC";
 
     public string DcBtnBackground  => DcStatus == GameStatus.UpdateAvailable ? "#201838" : "#182840";
     public string DcBtnForeground  => DcStatus == GameStatus.UpdateAvailable ? "#B898E8" : "#7AACDD";
@@ -36,9 +36,9 @@ public partial class GameCardViewModel
         : DcStatus == GameStatus.Installed ? "#5ECB7D"
         : "#A0AABB";
     public string DcShortAction => DcIsInstalling ? "…"
-        : DcStatus == GameStatus.UpdateAvailable ? "⬆ Update"
-        : DcStatus == GameStatus.Installed ? "↺ Reinstall"
-        : "⬇ Install";
+        : DcStatus == GameStatus.UpdateAvailable ? "⬆ Обновить"
+        : DcStatus == GameStatus.Installed ? "↺ Переустановить"
+        : "⬇ Установить";
 
     public bool IsDcNotInstalling => !DcIsInstalling;
     public bool IsDcInstalled => DcStatus == GameStatus.Installed || DcStatus == GameStatus.UpdateAvailable;
diff --git a/RenoDXCommander/ViewModels/GameCardViewModel.DofFix.cs b/RenoDXCommander/ViewModels/GameCardViewModel.DofFix.cs
index 0f7a56d..5a88c45 100644
--- a/RenoDXCommander/ViewModels/GameCardViewModel.DofFix.cs
+++ b/RenoDXCommander/ViewModels/GameCardViewModel.DofFix.cs
@@ -27,9 +27,9 @@ public partial class GameCardViewModel
         IsDofFixEligible ? Visibility.Visible : Visibility.Collapsed;
 
     public string DofFixActionLabel => DofFixIsInstalling ? "Installing..."
-        : DofFixStatus == GameStatus.UpdateAvailable ? "⬆  Update DOF Fix"
-        : DofFixStatus == GameStatus.Installed ? "↺  Reinstall DOF Fix"
-        : "⬇  Install DOF Fix";
+        : DofFixStatus == GameStatus.UpdateAvailable ? "⬆  Обновить DOF Fix"
+        : DofFixStatus == GameStatus.Installed ? "↺  Переустановить DOF Fix"
+        : "⬇  Установить DOF Fix";
 
     public string DofFixBtnBackground  => DofFixStatus == GameStatus.UpdateAvailable ? "#201838" : "#182840";
     public string DofFixBtnForeground  => DofFixStatus == GameStatus.UpdateAvailable ? "#B898E8" : "#7AACDD";
diff --git a/RenoDXCommander/ViewModels/GameCardViewModel.Dxvk.cs b/RenoDXCommander/ViewModels/GameCardViewModel.Dxvk.cs
index e968084..d9798b9 100644
--- a/RenoDXCommander/ViewModels/GameCardViewModel.Dxvk.cs
+++ b/RenoDXCommander/ViewModels/GameCardViewModel.Dxvk.cs
@@ -70,11 +70,11 @@ public partial class GameCardViewModel
     /// </summary>
     public string? DxvkToggleTooltip =>
         IsDxvkToggleEnabled ? null
-        : IsDxvkBlacklisted ? "DXVK is blocked for this game due to anti-cheat software."
+        : IsDxvkBlacklisted ? "DXVK заблокирован для этой игры из-за античита."
         : GraphicsApi == GraphicsApiType.Unknown && !HasDxvkApiOverride
-            ? "DXVK cannot be enabled because the game's DirectX version could not be determined."
+            ? "DXVK нельзя включить: не удалось определить версию DirectX игры."
         : GraphicsApi is GraphicsApiType.DirectX12 or GraphicsApiType.Vulkan or GraphicsApiType.OpenGL
-            ? $"DXVK does not support {GraphicsApi}. It only translates DirectX 8/9/10/11 to Vulkan."
+            ? $"DXVK не поддерживает {GraphicsApi}. Он транслирует в Vulkan только DirectX 8/9/10/11."
         : null;
 
     // ── DXVK computed properties ──────────────────────────────────────────────────
@@ -84,9 +84,9 @@ public partial class GameCardViewModel
         : DxvkStatus == GameStatus.Installed ? "🟢" : "⚪";
 
     public string DxvkActionLabel => DxvkIsInstalling ? "Installing..."
-        : DxvkStatus == GameStatus.UpdateAvailable ? "⬆  Update DXVK"
-        : DxvkStatus == GameStatus.Installed ? "↺  Reinstall DXVK"
-        : "⬇  Install DXVK";
+        : DxvkStatus == GameStatus.UpdateAvailable ? "⬆  Обновить DXVK"
+        : DxvkStatus == GameStatus.Installed ? "↺  Переустановить DXVK"
+        : "⬇  Установить DXVK";
 
     public string DxvkBtnBackground  => DxvkStatus == GameStatus.UpdateAvailable ? "#201838" : "#182840";
     public string DxvkBtnForeground  => DxvkStatus == GameStatus.UpdateAvailable ? "#B898E8" : "#7AACDD";
@@ -105,9 +105,9 @@ public partial class GameCardViewModel
         : DxvkStatus == GameStatus.Installed ? "#5ECB7D"
         : "#A0AABB";
     public string DxvkShortAction => DxvkIsInstalling ? "…"
-        : DxvkStatus == GameStatus.UpdateAvailable ? "⬆ Update"
-        : DxvkStatus == GameStatus.Installed ? "↺ Reinstall"
-        : "⬇ Install";
+        : DxvkStatus == GameStatus.UpdateAvailable ? "⬆ Обновить"
+        : DxvkStatus == GameStatus.Installed ? "↺ Переустановить"
+        : "⬇ Установить";
 
     public bool IsDxvkNotInstalling => !DxvkIsInstalling;
     public bool IsDxvkInstalled => DxvkStatus == GameStatus.Installed || DxvkStatus == GameStatus.UpdateAvailable;
diff --git a/RenoDXCommander/ViewModels/GameCardViewModel.Luma.cs b/RenoDXCommander/ViewModels/GameCardViewModel.Luma.cs
index 292d43b..16e4d7d 100644
--- a/RenoDXCommander/ViewModels/GameCardViewModel.Luma.cs
+++ b/RenoDXCommander/ViewModels/GameCardViewModel.Luma.cs
@@ -41,11 +41,11 @@ public partial class GameCardViewModel
     public Visibility LumaProgressVisibility => IsLumaInstalling ? Visibility.Visible : Visibility.Collapsed;
     public Visibility LumaMessageVisibility => string.IsNullOrEmpty(LumaActionMessage) ? Visibility.Collapsed : Visibility.Visible;
     public string LumaActionLabel => IsLumaInstalling ? "Installing..."
-        : (!IsRsInstalled && !ExcludeFromUpdateAllReShade && LumaStatus == GameStatus.NotInstalled) ? "⚠  ReShade required"
-        : LumaStatus == GameStatus.UpdateAvailable ? "⬆  Update Luma"
-        : LumaStatus == GameStatus.Installed ? "↺  Reinstall Luma"
-        : LumaIsExternalOnly ? "Get on Nexus Mods"
-        : "⬇  Install Luma";
+        : (!IsRsInstalled && !ExcludeFromUpdateAllReShade && LumaStatus == GameStatus.NotInstalled) ? "⚠  Требуется ReShade"
+        : LumaStatus == GameStatus.UpdateAvailable ? "⬆  Обновить Luma"
+        : LumaStatus == GameStatus.Installed ? "↺  Переустановить Luma"
+        : LumaIsExternalOnly ? "Открыть на Nexus Mods"
+        : "⬇  Установить Luma";
 
     // Component table: Luma short status/action (consistent with RS/DC/RDX)
     public string LumaStatusText => IsLumaInstalling ? "Installing…"
@@ -58,9 +58,9 @@ public partial class GameCardViewModel
         : LumaStatus == GameStatus.Installed       ? "#5ECB7D"
         : "#A0AABB";
     public string LumaShortAction => IsLumaInstalling ? "…"
-        : LumaStatus == GameStatus.UpdateAvailable ? "⬆ Update"
-        : LumaStatus == GameStatus.Installed       ? "↺ Reinstall"
-        : "⬇ Install";
+        : LumaStatus == GameStatus.UpdateAvailable ? "⬆ Обновить"
+        : LumaStatus == GameStatus.Installed       ? "↺ Переустановить"
+        : "⬇ Установить";
 
     public string LumaBtnBackground  => LumaStatus == GameStatus.UpdateAvailable ? "#201838" : "#182840";
     public string LumaBtnForeground  => LumaStatus == GameStatus.UpdateAvailable ? "#B898E8" : "#7AACDD";
diff --git a/RenoDXCommander/ViewModels/GameCardViewModel.OptiScaler.cs b/RenoDXCommander/ViewModels/GameCardViewModel.OptiScaler.cs
index a8cb09d..4aa7168 100644
--- a/RenoDXCommander/ViewModels/GameCardViewModel.OptiScaler.cs
+++ b/RenoDXCommander/ViewModels/GameCardViewModel.OptiScaler.cs
@@ -25,9 +25,9 @@ public partial class GameCardViewModel
         : OsStatus == GameStatus.Installed ? "🟢" : "⚪";
 
     public string OsActionLabel => OsIsInstalling ? "Installing..."
-        : OsStatus == GameStatus.UpdateAvailable ? "⬆  Update OptiScaler"
-        : OsStatus == GameStatus.Installed ? "↺  Reinstall OptiScaler"
-        : "⬇  Install OptiScaler";
+        : OsStatus == GameStatus.UpdateAvailable ? "⬆  Обновить OptiScaler"
+        : OsStatus == GameStatus.Installed ? "↺  Переустановить OptiScaler"
+        : "⬇  Установить OptiScaler";
 
     public string OsBtnBackground  => OsStatus == GameStatus.UpdateAvailable ? "#201838" : "#182840";
     public string OsBtnForeground  => OsStatus == GameStatus.UpdateAvailable ? "#B898E8" : "#7AACDD";
@@ -48,9 +48,9 @@ public partial class GameCardViewModel
         : OsStatus == GameStatus.Installed ? "#5ECB7D"
         : "#A0AABB";
     public string OsShortAction => OsIsInstalling ? "…"
-        : OsStatus == GameStatus.UpdateAvailable ? "⬆ Update"
-        : OsStatus == GameStatus.Installed ? "↺ Reinstall"
-        : "⬇ Install";
+        : OsStatus == GameStatus.UpdateAvailable ? "⬆ Обновить"
+        : OsStatus == GameStatus.Installed ? "↺ Переустановить"
+        : "⬇ Установить";
 
     public bool IsOsNotInstalling => !OsIsInstalling;
     public bool IsOsInstalled => OsStatus == GameStatus.Installed
diff --git a/RenoDXCommander/ViewModels/GameCardViewModel.REFramework.cs b/RenoDXCommander/ViewModels/GameCardViewModel.REFramework.cs
index 04ee641..a85cbf0 100644
--- a/RenoDXCommander/ViewModels/GameCardViewModel.REFramework.cs
+++ b/RenoDXCommander/ViewModels/GameCardViewModel.REFramework.cs
@@ -14,9 +14,9 @@ public partial class GameCardViewModel
     // ── REF computed properties ───────────────────────────────────────────────────
 
     public string RefActionLabel => RefIsInstalling ? "Installing..."
-        : RefStatus == GameStatus.UpdateAvailable ? "⬆  Update RE Framework"
-        : RefStatus == GameStatus.Installed ? "↺  Reinstall RE Framework"
-        : "⬇  Install RE Framework";
+        : RefStatus == GameStatus.UpdateAvailable ? "⬆  Обновить RE Framework"
+        : RefStatus == GameStatus.Installed ? "↺  Переустановить RE Framework"
+        : "⬇  Установить RE Framework";
 
     public string RefBtnBackground  => RefStatus == GameStatus.UpdateAvailable ? "#201838" : "#182840";
     public string RefBtnForeground  => RefStatus == GameStatus.UpdateAvailable ? "#B898E8" : "#7AACDD";
@@ -35,9 +35,9 @@ public partial class GameCardViewModel
         : RefStatus == GameStatus.Installed ? "#5ECB7D"
         : "#A0AABB";
     public string RefShortAction => RefIsInstalling ? "…"
-        : RefStatus == GameStatus.UpdateAvailable ? "⬆ Update"
-        : RefStatus == GameStatus.Installed ? "↺ Reinstall"
-        : "⬇ Install";
+        : RefStatus == GameStatus.UpdateAvailable ? "⬆ Обновить"
+        : RefStatus == GameStatus.Installed ? "↺ Переустановить"
+        : "⬇ Установить";
 
     public bool IsRefNotInstalling => !RefIsInstalling;
     public bool IsRefInstalled => RefStatus == GameStatus.Installed || RefStatus == GameStatus.UpdateAvailable;
diff --git a/RenoDXCommander/ViewModels/GameCardViewModel.ReShade.cs b/RenoDXCommander/ViewModels/GameCardViewModel.ReShade.cs
index d20d077..a5846f7 100644
--- a/RenoDXCommander/ViewModels/GameCardViewModel.ReShade.cs
+++ b/RenoDXCommander/ViewModels/GameCardViewModel.ReShade.cs
@@ -30,19 +30,19 @@ public partial class GameCardViewModel
             // RE Engine games require REFramework before ReShade can be installed
             // (unless user has excluded REF via Update Inclusion toggle)
             if (IsREEngineGame && !IsRefInstalled && !EffectiveLumaMode && !ExcludeFromUpdateAllRef)
-                return "⚠  RE Framework required";
+                return "⚠  Требуется RE Framework";
             if (RequiresVulkanInstall)
             {
                 bool layerInstalled = IsLayerInstalledFunc();
                 if (RsStatus == GameStatus.UpdateAvailable && layerInstalled && IsVulkanRsActive)
-                    return "⬆  Update Vulkan ReShade";
-                if (layerInstalled && IsVulkanRsActive) return "↺  Reinstall Vulkan ReShade";
-                if (layerInstalled) return "⬇  Install Vulkan ReShade";
-                return "⬇  Install Vulkan Layer";
+                    return "⬆  Обновить Vulkan ReShade";
+                if (layerInstalled && IsVulkanRsActive) return "↺  Переустановить Vulkan ReShade";
+                if (layerInstalled) return "⬇  Установить Vulkan ReShade";
+                return "⬇  Установить слой Vulkan";
             }
-            return RsStatus == GameStatus.UpdateAvailable ? "⬆  Update ReShade"
-                 : RsStatus == GameStatus.Installed       ? "↺  Reinstall ReShade"
-                 : "⬇  Install ReShade";
+            return RsStatus == GameStatus.UpdateAvailable ? "⬆  Обновить ReShade"
+                 : RsStatus == GameStatus.Installed       ? "↺  Переустановить ReShade"
+                 : "⬇  Установить ReShade";
         }
     }
 
@@ -79,14 +79,14 @@ public partial class GameCardViewModel
             {
                 bool layerInstalled = IsLayerInstalledFunc();
                 if (RsStatus == GameStatus.UpdateAvailable && layerInstalled && IsVulkanRsActive)
-                    return "⬆ Update";
-                if (layerInstalled && IsVulkanRsActive) return "↺ Reinstall";
+                    return "⬆ Обновить";
+                if (layerInstalled && IsVulkanRsActive) return "↺ Переустановить";
                 if (layerInstalled) return "⬇ Vulkan RS";
-                return "⬇ Install";
+                return "⬇ Установить";
             }
-            return RsStatus == GameStatus.UpdateAvailable ? "⬆ Update"
-                 : RsStatus == GameStatus.Installed       ? "↺ Reinstall"
-                 : "⬇ Install";
+            return RsStatus == GameStatus.UpdateAvailable ? "⬆ Обновить"
+                 : RsStatus == GameStatus.Installed       ? "↺ Переустановить"
+                 : "⬇ Установить";
         }
     }
 
diff --git a/RenoDXCommander/ViewModels/GameCardViewModel.RenoDX.cs b/RenoDXCommander/ViewModels/GameCardViewModel.RenoDX.cs
index bcc574c..d26fe8a 100644
--- a/RenoDXCommander/ViewModels/GameCardViewModel.RenoDX.cs
+++ b/RenoDXCommander/ViewModels/GameCardViewModel.RenoDX.cs
@@ -14,12 +14,12 @@ public partial class GameCardViewModel
         get
         {
             if (IsInstalling) return "Installing...";
-            if (IsRtxHdrEnabled) return "Configure RTX HDR";
+            if (IsRtxHdrEnabled) return "Настроить RTX HDR";
             if (!IsRsInstalled && !ExcludeFromUpdateAllReShade && Mod?.SnapshotUrl != null && !IsExternalOnly)
-                return "⚠  ReShade required";
+                return "⚠  Требуется ReShade";
             // No mod available and nothing manually installed
             if (Mod?.SnapshotUrl == null && !IsExternalOnly && string.IsNullOrEmpty(InstalledAddonFileName))
-                return "No RenoDX mod available";
+                return "Мод RenoDX недоступен";
             var name = UseUeExtended ? "UE-Extended" : "RenoDX";
             return Status == GameStatus.UpdateAvailable ? $"⬆  Update {name}"
                  : Status == GameStatus.Installed       ? $"↺  Reinstall {name}"
@@ -31,10 +31,10 @@ public partial class GameCardViewModel
 
     public string GenericModLabel => IsGenericMod
         ? (EngineHint.Contains("Unity")
-           ? "Generic Unity"
-           : (IsNativeHdrGame ? "UE Extended Native HDR"
+           ? "Универсальный Unity"
+           : (IsNativeHdrGame ? "UE Extended нативный HDR"
               : (IsManifestUeExtended || UseUeExtended) ? "UE Extended"
-              : "Generic UE"))
+              : "Универсальный UE"))
         : "";
 
     // Update button colours — purple when an update is available, normal blue otherwise
@@ -43,7 +43,7 @@ public partial class GameCardViewModel
     public string InstallBtnBorderBrush => Status == GameStatus.UpdateAvailable ? "#3A2860" : "#2A4468";
 
     // UE-Extended toggle label and styling
-    public string UeExtendedLabel      => UseUeExtended ? "⚡ UE Extended" : "⚡ Standard UE";
+    public string UeExtendedLabel      => UseUeExtended ? "⚡ UE Extended" : "⚡ Обычный UE";
     public string UeExtendedBackground => UseUeExtended ? "#201838" : "#1E242C";
     public string UeExtendedForeground => UseUeExtended ? "#B898E8" : "#6B7A8E";
     public string UeExtendedBorderBrush => UseUeExtended ? "#3A2860" : "#283240";
@@ -73,13 +73,13 @@ public partial class GameCardViewModel
             if (IsExternalOnly)
             {
                 // External-only: no RenoDX to install, only ReShade
-                if (RsStatus == GameStatus.UpdateAvailable) return "⬆  Update ReShade";
-                if (RsStatus == GameStatus.Installed) return "↺  Reinstall ReShade";
-                return "⬇  Install ReShade";
+                if (RsStatus == GameStatus.UpdateAvailable) return "⬆  Обновить ReShade";
+                if (RsStatus == GameStatus.Installed) return "↺  Переустановить ReShade";
+                return "⬇  Установить ReShade";
             }
-            if (AnyUpdateAvailable) return "⬆  Update All";
-            if (Status == GameStatus.Installed) return "↺  Reinstall All";
-            return "⬇  Install All";
+            if (AnyUpdateAvailable) return "⬆  Обновить всё";
+            if (Status == GameStatus.Installed) return "↺  Переустановить всё";
+            return "⬇  Установить всё";
         }
     }
 
@@ -114,9 +114,9 @@ public partial class GameCardViewModel
         : Status == GameStatus.Installed       ? "#5ECB7D"
         : Mod?.SnapshotUrl != null             ? "#A0AABB" : "#404858";
     public string RdxShortAction => IsInstalling ? "…"
-        : Status == GameStatus.UpdateAvailable ? "⬆ Update"
-        : Status == GameStatus.Installed       ? "↺ Reinstall"
-        : "⬇ Install";
+        : Status == GameStatus.UpdateAvailable ? "⬆ Обновить"
+        : Status == GameStatus.Installed       ? "↺ Переустановить"
+        : "⬇ Установить";
 
     // Negated installing flag — used for IsEnabled bindings to avoid converter in DataTemplate
     public bool IsNotInstalling => !IsInstalling;
@@ -145,7 +145,7 @@ public partial class GameCardViewModel
     // ── External link label: "Update" when Nexus update available, "Redownload" when installed ──
     public string ExternalDisplayLabel =>
         Status == GameStatus.UpdateAvailable && IsRdxInstalled
-            ? "⬆  Update RenoDX"
+            ? "⬆  Обновить RenoDX"
             : IsRdxInstalled && !string.IsNullOrEmpty(ExternalLabel)
                 ? "↺  " + ExternalLabel.Replace("Download", "Redownload")
                 : "⬇  " + ExternalLabel;
diff --git a/RenoDXCommander/ViewModels/GameCardViewModel.UI.cs b/RenoDXCommander/ViewModels/GameCardViewModel.UI.cs
index ff6a78d..2e6ae9c 100644
--- a/RenoDXCommander/ViewModels/GameCardViewModel.UI.cs
+++ b/RenoDXCommander/ViewModels/GameCardViewModel.UI.cs
@@ -52,10 +52,10 @@ public partial class GameCardViewModel
                 if (effectiveStatus == GameStatus.UpdateAvailable
                     || RsStatus == GameStatus.UpdateAvailable
                     || LumaStatus == GameStatus.UpdateAvailable)
-                    return "⬆  Manage";
-                return "↺  Manage";
+                    return "⬆  Управление";
+                return "↺  Управление";
             }
-            return "⬇  Install";
+            return "⬇  Установить";
         }
     }
 
@@ -100,12 +100,12 @@ public partial class GameCardViewModel
 
     // ── Derived display ───────────────────────────────────────────────────────────
 
-    public string WikiStatusLabel => WikiStatus == "✅" ? "✅ Working"
-                                   : WikiStatus == "🚧" ? "🚧 In Progress"
-                                   : WikiStatus == "?"  ? "⚠️ May Work"
+    public string WikiStatusLabel => WikiStatus == "✅" ? "✅ Работает"
+                                   : WikiStatus == "🚧" ? "🚧 В работе"
+                                   : WikiStatus == "?"  ? "⚠️ Может работать"
                                    : WikiStatus == "💬" ? "💬 Discord"
                                    : WikiStatus == "🌐" ? "🌐 Nexus"
-                                   : WikiStatus == "—" && IsGenericMod ? "⚠️ May Work"
+                                   : WikiStatus == "—" && IsGenericMod ? "⚠️ Может работать"
                                    : "";
 
     /// <summary>
@@ -199,7 +199,7 @@ public partial class GameCardViewModel
     public bool HasUwFixUrl        => !string.IsNullOrEmpty(UwFixUrl);
     public bool HasUltraPlusUrl    => !string.IsNullOrEmpty(UltraPlusUrl);
     public bool HasNameUrl            => !string.IsNullOrEmpty(NameUrl);
-    public string HideButtonLabel     => IsHidden ? "👁 Show" : "🚫 Hide";
+    public string HideButtonLabel     => IsHidden ? "👁 Показать" : "🚫 Скрыть";
     public string StarForeground       => IsFavourite ? "#FFD700" : "#282840";
     public Visibility IsFavouriteVisibility      => IsFavourite ? Visibility.Visible : Visibility.Collapsed;
     public Visibility IsNotFavouriteVisibility   => IsFavourite ? Visibility.Collapsed : Visibility.Visible;
diff --git a/RenoDXCommander/ViewModels/GameCardViewModel.UltraLimiter.cs b/RenoDXCommander/ViewModels/GameCardViewModel.UltraLimiter.cs
index 916d6f8..78b9678 100644
--- a/RenoDXCommander/ViewModels/GameCardViewModel.UltraLimiter.cs
+++ b/RenoDXCommander/ViewModels/GameCardViewModel.UltraLimiter.cs
@@ -13,10 +13,10 @@ public partial class GameCardViewModel
         : UlStatus == GameStatus.Installed ? "🟢" : "⚪";
 
     public string UlActionLabel => UlIsInstalling ? "Installing..."
-        : (!IsRsInstalled && !ExcludeFromUpdateAllReShade) ? "⚠  ReShade required"
-        : UlStatus == GameStatus.UpdateAvailable ? "⬆  Update ReLimiter"
-        : UlStatus == GameStatus.Installed ? "↺  Reinstall ReLimiter"
-        : "⬇  Install ReLimiter";
+        : (!IsRsInstalled && !ExcludeFromUpdateAllReShade) ? "⚠  Требуется ReShade"
+        : UlStatus == GameStatus.UpdateAvailable ? "⬆  Обновить ReLimiter"
+        : UlStatus == GameStatus.Installed ? "↺  Переустановить ReLimiter"
+        : "⬇  Установить ReLimiter";
 
     public string UlBtnBackground  => UlStatus == GameStatus.UpdateAvailable ? "#201838" : "#182840";
     public string UlBtnForeground  => UlStatus == GameStatus.UpdateAvailable ? "#B898E8" : "#7AACDD";
@@ -35,9 +35,9 @@ public partial class GameCardViewModel
         : UlStatus == GameStatus.Installed ? "#5ECB7D"
         : "#A0AABB";
     public string UlShortAction => UlIsInstalling ? "…"
-        : UlStatus == GameStatus.UpdateAvailable ? "⬆ Update"
-        : UlStatus == GameStatus.Installed ? "↺ Reinstall"
-        : "⬇ Install";
+        : UlStatus == GameStatus.UpdateAvailable ? "⬆ Обновить"
+        : UlStatus == GameStatus.Installed ? "↺ Переустановить"
+        : "⬇ Установить";
 
     public bool IsUlNotInstalling => !UlIsInstalling;
     public bool IsUlInstalled => UlStatus == GameStatus.Installed || UlStatus == GameStatus.UpdateAvailable;
diff --git a/RenoDXCommander/ViewModels/MainViewModel.BackgroundScan.cs b/RenoDXCommander/ViewModels/MainViewModel.BackgroundScan.cs
index 324b2a9..c31a750 100644
--- a/RenoDXCommander/ViewModels/MainViewModel.BackgroundScan.cs
+++ b/RenoDXCommander/ViewModels/MainViewModel.BackgroundScan.cs
@@ -22,7 +22,7 @@ public partial class MainViewModel
     private async Task RunBackgroundScanAndMergeAsync(SavedGameLibrary savedLib, bool isStartup = false)
     {
         IsBackgroundScanning = true;
-        BackgroundScanStatusText = "Scanning for changes...";
+        BackgroundScanStatusText = "Поиск изменений...";
         _crashReporter.Log("[MainViewModel.RunBackgroundScanAndMergeAsync] Starting background scan...");
 
         try
@@ -472,8 +472,8 @@ public partial class MainViewModel
             DispatcherQueue?.TryEnqueue(() =>
             {
                 StatusText = offlineMode
-                    ? $"{detectedGames.Count} games detected · offline mode (mod info unavailable)"
-                    : $"{detectedGames.Count} games detected · {InstalledCount} mods installed";
+                    ? $"игр обнаружено: {detectedGames.Count} · автономный режим (данные о модах недоступны)"
+                    : $"игр обнаружено: {detectedGames.Count} · модов установлено: {InstalledCount}";
                 SubStatusText = "";
 
                 // Re-scroll to selected game after merge (cards may have shifted)
@@ -799,10 +799,10 @@ public partial class MainViewModel
     private static string FormatAge(DateTime utc)
     {
         var age = DateTime.UtcNow - utc;
-        if (age.TotalMinutes < 1) return "just now";
-        if (age.TotalHours   < 1) return $"{(int)age.TotalMinutes}m ago";
-        if (age.TotalDays    < 1) return $"{(int)age.TotalHours}h ago";
-        return $"{(int)age.TotalDays}d ago";
+        if (age.TotalMinutes < 1) return "только что";
+        if (age.TotalHours   < 1) return $"{(int)age.TotalMinutes} мин назад";
+        if (age.TotalDays    < 1) return $"{(int)age.TotalHours} ч назад";
+        return $"{(int)age.TotalDays} дн назад";
     }
 
     /// <summary>
diff --git a/RenoDXCommander/ViewModels/MainViewModel.BuildCards.cs b/RenoDXCommander/ViewModels/MainViewModel.BuildCards.cs
index 9f946a1..60fad12 100644
--- a/RenoDXCommander/ViewModels/MainViewModel.BuildCards.cs
+++ b/RenoDXCommander/ViewModels/MainViewModel.BuildCards.cs
@@ -695,7 +695,7 @@ public partial class MainViewModel
                                          : effectiveMod?.NexusUrl ?? effectiveMod?.DiscordUrl ?? "",
                 ExternalLabel          = _wikiExclusions.Contains(game.Name)
                                          ? ""
-                                         : effectiveMod?.NexusUrl != null ? "Download from Nexus Mods" : "Download from Discord",
+                                         : effectiveMod?.NexusUrl != null ? "Скачать с Nexus Mods" : "Скачать с Discord",
                 NexusUrl               = effectiveMod?.NexusUrl,
                 DiscordUrl             = _wikiExclusions.Contains(game.Name)
                                          ? null
@@ -755,7 +755,7 @@ public partial class MainViewModel
                     if (emuFoundCount > 0)
                     {
                         newCard.Status = GameStatus.Installed;
-                        newCard.InstalledAddonFileName = $"{emuFoundCount} addons";
+                        newCard.InstalledAddonFileName = $"аддонов: {emuFoundCount}";
                     }
                 }
             }
diff --git a/RenoDXCommander/ViewModels/MainViewModel.CacheLoad.cs b/RenoDXCommander/ViewModels/MainViewModel.CacheLoad.cs
index aa76d83..854ea53 100644
--- a/RenoDXCommander/ViewModels/MainViewModel.CacheLoad.cs
+++ b/RenoDXCommander/ViewModels/MainViewModel.CacheLoad.cs
@@ -1054,7 +1054,7 @@ public partial class MainViewModel
         }
 
         // 14. Set StatusText to show cached game count
-        StatusText    = $"{_allCards.Count} games";
+        StatusText    = $"игр: {_allCards.Count}";
         SubStatusText = "";
 
         _crashReporter.Log($"[MainViewModel.LoadCacheAndBuildCardsAsync] Cache phase complete — {_allCards.Count} cards displayed");
diff --git a/RenoDXCommander/ViewModels/MainViewModel.Dxvk.cs b/RenoDXCommander/ViewModels/MainViewModel.Dxvk.cs
index 5586a6a..08064f3 100644
--- a/RenoDXCommander/ViewModels/MainViewModel.Dxvk.cs
+++ b/RenoDXCommander/ViewModels/MainViewModel.Dxvk.cs
@@ -30,7 +30,7 @@ public partial class MainViewModel
         {
             var dontShowAgain = new CheckBox
             {
-                Content = "Don't show this warning again",
+                Content = "Больше не показывать это предупреждение",
                 Foreground = UIFactory.Brush(ResourceKeys.TextSecondaryBrush),
                 Margin = new Microsoft.UI.Xaml.Thickness(0, 8, 0, 0),
             };
@@ -38,20 +38,20 @@ public partial class MainViewModel
             var contentPanel = new StackPanel();
             contentPanel.Children.Add(new TextBlock
             {
-                Text = "⚠ ADVANCED FEATURE — USE AT YOUR OWN RISK\n\n"
-                    + "DXVK is an unofficial DirectX-to-Vulkan translation layer.\n"
-                    + "No support will be provided if a game is not compatible.\n\n"
-                    + "WHO SHOULD USE THIS:\n"
-                    + "• Primarily benefits older DX8/DX9 games (e.g. FFXIV, Morrowind)\n"
-                    + "• Enables ReShade compute shaders on games that don't support them natively\n"
-                    + "• Can reduce CPU-bound stuttering in older titles\n\n"
-                    + "IMPORTANT WARNINGS:\n"
-                    + "• Anti-cheat games may ban players using DXVK\n"
-                    + "• Game overlays (Steam, NVIDIA, RTSS) may conflict or stop working\n"
-                    + "• Exclusive fullscreen is blocked — use borderless windowed\n"
-                    + "• First launch will be slow due to shader compilation (improves on subsequent runs)\n"
-                    + "• Some games may crash or have graphical glitches with DXVK\n\n"
-                    + "Do you want to continue?",
+                Text = "⚠ ПРОДВИНУТАЯ ФУНКЦИЯ — ИСПОЛЬЗУЙТЕ НА СВОЙ СТРАХ И РИСК\n\n"
+                    + "DXVK — неофициальный слой трансляции DirectX в Vulkan.\n"
+                    + "Для несовместимых игр поддержка не оказывается.\n\n"
+                    + "КОМУ ЭТО НУЖНО:\n"
+                    + "• В первую очередь помогает старым играм DX8/DX9 (например, FFXIV, Morrowind)\n"
+                    + "• Включает compute-шейдеры ReShade в играх без нативной поддержки\n"
+                    + "• Может снизить фризы из-за процессора в старых играх\n\n"
+                    + "ВАЖНЫЕ ПРЕДУПРЕЖДЕНИЯ:\n"
+                    + "• В играх с античитом за DXVK могут забанить\n"
+                    + "• Игровые оверлеи (Steam, NVIDIA, RTSS) могут конфликтовать или перестать работать\n"
+                    + "• Исключительный полноэкранный режим заблокирован — используйте окно без рамки\n"
+                    + "• Первый запуск будет медленным из-за компиляции шейдеров (дальше быстрее)\n"
+                    + "• В некоторых играх возможны вылеты или графические сбои с DXVK\n\n"
+                    + "Продолжить?",
                 TextWrapping = Microsoft.UI.Xaml.TextWrapping.Wrap,
                 Foreground = UIFactory.Brush(ResourceKeys.TextPrimaryBrush),
                 FontSize = 13,
@@ -60,10 +60,10 @@ public partial class MainViewModel
 
             var warningDialog = new ContentDialog
             {
-                Title = "⚠ DXVK Warning",
+                Title = "⚠ Предупреждение DXVK",
                 Content = contentPanel,
-                PrimaryButtonText = "Continue",
-                CloseButtonText = "Cancel",
+                PrimaryButtonText = "Продолжить",
+                CloseButtonText = "Отмена",
                 XamlRoot = xamlRoot,
                 RequestedTheme = Microsoft.UI.Xaml.ElementTheme.Dark,
             };
@@ -86,7 +86,7 @@ public partial class MainViewModel
         _dxvkService.LiliumPresetIndex = GetLiliumPreset(card.GameName, card.Source ?? "");
 
         card.DxvkIsInstalling = true;
-        card.DxvkActionMessage = "Installing DXVK...";
+        card.DxvkActionMessage = "Установка DXVK...";
         card.DxvkProgress = 0;
         try
         {
@@ -101,7 +101,7 @@ public partial class MainViewModel
                     card.DxvkProgress = p.percent;
                 }));
 
-            card.DxvkActionMessage = "✅ DXVK installed!";
+            card.DxvkActionMessage = "✅ DXVK установлен!";
             card.NotifyAll();
             card.FadeMessage(m => card.DxvkActionMessage = m, card.DxvkActionMessage);
             SaveLibrary();
@@ -112,7 +112,7 @@ public partial class MainViewModel
         }
         catch (Exception ex)
         {
-            card.DxvkActionMessage = $"❌ Install failed: {ex.Message}";
+            card.DxvkActionMessage = $"❌ Не удалось установить: {ex.Message}";
             _crashReporter.WriteCrashReport("InstallDxvk", ex, note: $"Game: {card.GameName}");
         }
         finally
@@ -133,7 +133,7 @@ public partial class MainViewModel
         if (string.IsNullOrEmpty(card.InstallPath)) return;
 
         card.DxvkIsInstalling = true;
-        card.DxvkActionMessage = "Removing DXVK...";
+        card.DxvkActionMessage = "Удаление DXVK...";
         try
         {
             await _dxvkService.UninstallAsync(card);
@@ -141,14 +141,14 @@ public partial class MainViewModel
             // Clear persisted Vulkan rendering path — Lilium HDR uninstall resets to DirectX
             SetVulkanRenderingPath(card.GameName, "DirectX", card.Source ?? "");
             
-            card.DxvkActionMessage = "✖ DXVK removed.";
+            card.DxvkActionMessage = "✖ DXVK удалён.";
             card.NotifyAll();
             card.FadeMessage(m => card.DxvkActionMessage = m, card.DxvkActionMessage);
             SaveLibrary();
         }
         catch (Exception ex)
         {
-            card.DxvkActionMessage = $"❌ Uninstall failed: {ex.Message}";
+            card.DxvkActionMessage = $"❌ Не удалось удалить: {ex.Message}";
             _crashReporter.WriteCrashReport("UninstallDxvk", ex, note: $"Game: {card.GameName}");
         }
         finally
@@ -171,7 +171,7 @@ public partial class MainViewModel
         if (string.IsNullOrEmpty(card.InstallPath)) return;
 
         card.DxvkIsInstalling = true;
-        card.DxvkActionMessage = "Updating DXVK...";
+        card.DxvkActionMessage = "Обновление DXVK...";
         card.DxvkProgress = 0;
         try
         {
@@ -195,7 +195,7 @@ public partial class MainViewModel
 
             _dxvkService.SelectedVariant = savedVariant;
 
-            card.DxvkActionMessage = "✅ DXVK updated!";
+            card.DxvkActionMessage = "✅ DXVK обновлён!";
             card.DxvkStatus = GameStatus.Installed;
             card.NotifyAll();
             card.FadeMessage(m => card.DxvkActionMessage = m, card.DxvkActionMessage);
@@ -203,7 +203,7 @@ public partial class MainViewModel
         }
         catch (Exception ex)
         {
-            card.DxvkActionMessage = $"❌ Update failed: {ex.Message}";
+            card.DxvkActionMessage = $"❌ Не удалось обновить: {ex.Message}";
             _crashReporter.WriteCrashReport("UpdateDxvk", ex, note: $"Game: {card.GameName}");
         }
         finally
@@ -223,7 +223,7 @@ public partial class MainViewModel
         try
         {
             _dxvkService.CopyConfToGame(card);
-            card.DxvkActionMessage = "✅ dxvk.conf copied to game folder.";
+            card.DxvkActionMessage = "✅ dxvk.conf скопирован в папку игры.";
             card.FadeMessage(m => card.DxvkActionMessage = m, card.DxvkActionMessage);
         }
         catch (Exception ex)
diff --git a/RenoDXCommander/ViewModels/MainViewModel.Init.cs b/RenoDXCommander/ViewModels/MainViewModel.Init.cs
index 28c1747..7ef4bf8 100644
--- a/RenoDXCommander/ViewModels/MainViewModel.Init.cs
+++ b/RenoDXCommander/ViewModels/MainViewModel.Init.cs
@@ -127,7 +127,7 @@ public partial class MainViewModel
     public async Task FullRefreshAsync(IProgress<string>? progress = null)
     {
         // Clear all caches so every game is re-scanned from disk.
-        progress?.Report("Clearing caches...");
+        progress?.Report("Очистка кешей...");
         _engineTypeCache.Clear();
         _resolvedPathCache.Clear();
         _addonFileCache.Clear();
@@ -135,7 +135,7 @@ public partial class MainViewModel
         _dlssStreamlineService.ClearScanCaches();
 
         // Validate installed.json — remove records where the addon file no longer exists on disk
-        progress?.Report("Validating install records...");
+        progress?.Report("Проверка записей об установке...");
         try
         {
             var records = _installer.LoadAll();
@@ -260,14 +260,14 @@ public partial class MainViewModel
             bool hasCachedLibrary = savedLib != null && !forceRescan;
             if (hasCachedLibrary)
             {
-                StatusText    = $"Library loaded ({savedLib!.Games.Count} games, scanned {FormatAge(savedLib.LastScanned)})";
-                SubStatusText = "Checking for new games and fetching latest mod info...";
+                StatusText    = $"Библиотека загружена (игр: {savedLib!.Games.Count}, просканировано: {FormatAge(savedLib.LastScanned)})";
+                SubStatusText = "Поиск новых игр и загрузка свежей информации о модах...";
                 addonCache    = savedLib.AddonScanCache;
             }
             else
             {
-                StatusText    = "Scanning game library...";
-                SubStatusText = "Running store scans + wiki fetch simultaneously...";
+                StatusText    = "Сканирование библиотеки игр...";
+                SubStatusText = "Сканирую магазины и параллельно загружаю вики...";
                 addonCache    = new Dictionary<string, bool>(StringComparer.OrdinalIgnoreCase);
             }
 
@@ -427,7 +427,7 @@ public partial class MainViewModel
             });
 
             // 3. Await detection first — this never needs network
-            progress?.Report("Detecting games...");
+            progress?.Report("Поиск игр...");
             var freshGames = await detectTask;
 
             // 4. Await network tasks individually so failures don't block game display
@@ -618,7 +618,7 @@ public partial class MainViewModel
             // a visual gap where the update badge disappears until the network check completes.
             // (prevUpdateStatus was captured at the top of InitializeAsync before _allCards.Clear())
 
-            SubStatusText = "Matching mods and checking install status...";
+            SubStatusText = "Подбор модов и проверка статуса установки...";
 
             // Ensure Nexus Mods dictionary and PCGW AppID cache are ready before building cards
             await nexusInitTask;
@@ -627,7 +627,7 @@ public partial class MainViewModel
             await ultraPlusInitTask;
 
             _crashReporter.Log($"[MainViewModel.InitializeAsync] Building cards for {allGames.Count} games...");
-            progress?.Report($"Building cards for {allGames.Count} games...");
+            progress?.Report($"Построение карточек для игр ({allGames.Count})...");
             _allCards = await Task.Run(() => BuildCards(allGames, records, auxRecords, addonCache, _genericNotes));
             _crashReporter.Log($"[MainViewModel.InitializeAsync] BuildCards complete: {_allCards.Count} cards");
             GraphicsApiDetector.SaveCache();
@@ -834,13 +834,13 @@ public partial class MainViewModel
 
             var offlineMode = wikiFetchFailed;
             StatusText    = offlineMode
-                ? $"{detectedGames.Count} games detected · offline mode (mod info unavailable)"
-                : $"{detectedGames.Count} games detected · {InstalledCount} mods installed";
+                ? $"игр обнаружено: {detectedGames.Count} · автономный режим (данные о модах недоступны)"
+                : $"игр обнаружено: {detectedGames.Count} · модов установлено: {InstalledCount}";
             SubStatusText = "";
         }
         catch (Exception ex)
         {
-            StatusText = "Error loading";
+            StatusText = "Ошибка загрузки";
             SubStatusText = ex.Message;
             _crashReporter.WriteCrashReport("InitializeAsync", ex);
         }
diff --git a/RenoDXCommander/ViewModels/MainViewModel.Install.Components.cs b/RenoDXCommander/ViewModels/MainViewModel.Install.Components.cs
index f8a0597..e9381a8 100644
--- a/RenoDXCommander/ViewModels/MainViewModel.Install.Components.cs
+++ b/RenoDXCommander/ViewModels/MainViewModel.Install.Components.cs
@@ -149,7 +149,7 @@ public partial class MainViewModel
             return;
 
         card.IsInstalling = true;
-        card.ActionMessage = "Installing emulator addons...";
+        card.ActionMessage = "Установка аддонов-эмуляторов...";
         _crashReporter.Log($"[MainViewModel.InstallEmulatorAddonsAsync] Starting bundle install for {card.GameName}");
 
         int installed = 0;
@@ -223,10 +223,10 @@ public partial class MainViewModel
             DispatcherQueue?.TryEnqueue(() =>
             {
                 card.Status = installed > 0 ? GameStatus.Installed : GameStatus.Available;
-                card.InstalledAddonFileName = $"{installed} addons";
+                card.InstalledAddonFileName = $"аддонов: {installed}";
                 card.ActionMessage = failed == 0
-                    ? $"✅ {installed} addons installed!"
-                    : $"✅ {installed} installed, {failed} failed.";
+                    ? $"✅ Аддонов установлено: {installed}!"
+                    : $"✅ Установлено: {installed}, с ошибками: {failed}.";
                 card.FadeMessage(m => card.ActionMessage = m, card.ActionMessage);
                 card.NotifyAll();
                 SaveLibrary();
@@ -281,7 +281,7 @@ public partial class MainViewModel
 
         card.Status = GameStatus.Available;
         card.InstalledAddonFileName = null;
-        card.ActionMessage = $"✖ {removed} addons removed.";
+        card.ActionMessage = $"✖ Аддонов удалено: {removed}.";
         card.FadeMessage(m => card.ActionMessage = m, card.ActionMessage);
         card.NotifyAll();
         SaveLibrary();
@@ -322,7 +322,7 @@ public partial class MainViewModel
         if (!await CheckInstallWarningAsync(card.GameName, "relimiter")) return;
 
         card.UlIsInstalling = true;
-        card.UlActionMessage = "Downloading ReLimiter...";
+        card.UlActionMessage = "Загрузка ReLimiter...";
         card.UlProgress = 0;
         try
         {
@@ -401,7 +401,7 @@ public partial class MainViewModel
                 card.UlInstalledVersion = _latestUlVersion?.TrimStart('v', 'V')
                     ?? ReadUlInstalledVersion(card.Is32Bit);
                 card.UlStatus = GameStatus.Installed;
-                card.UlActionMessage = "✅ ReLimiter installed!";
+                card.UlActionMessage = "✅ ReLimiter установлен!";
                 card.UlIsInstalling = false;
                 card.NotifyAll();
                 card.FadeMessage(m => card.UlActionMessage = m, card.UlActionMessage);
@@ -411,7 +411,7 @@ public partial class MainViewModel
         {
             DispatcherQueue?.TryEnqueue(() =>
             {
-                card.UlActionMessage = $"❌ Install failed: {ex.Message}";
+                card.UlActionMessage = $"❌ Не удалось установить: {ex.Message}";
                 card.UlIsInstalling = false;
                 card.NotifyAll();
             });
@@ -427,7 +427,7 @@ public partial class MainViewModel
     {
         if (File.Exists(GetUlCachePath(is32Bit)))
         {
-            progress?.Report(("Installing from cache...", 50));
+            progress?.Report(("Установка из кеша...", 50));
             return;
         }
 
@@ -442,7 +442,7 @@ public partial class MainViewModel
         var url = currentUrl;
         if (string.IsNullOrEmpty(url))
         {
-            throw new InvalidOperationException("Could not determine ReLimiter download URL from GitHub releases.");
+            throw new InvalidOperationException("Не удалось определить ссылку на скачивание ReLimiter из релизов GitHub.");
         }
 
         Directory.CreateDirectory(DownloadPaths.FrameLimiter);
@@ -565,13 +565,13 @@ public partial class MainViewModel
             card.UlInstalledFile = null;
             card.UlInstalledVersion = null;
             card.UlStatus = GameStatus.NotInstalled;
-            card.UlActionMessage = "✖ ReLimiter removed.";
+            card.UlActionMessage = "✖ ReLimiter удалён.";
             card.NotifyAll();
             card.FadeMessage(m => card.UlActionMessage = m, card.UlActionMessage);
         }
         catch (Exception ex)
         {
-            card.UlActionMessage = $"❌ Uninstall failed: {ex.Message}";
+            card.UlActionMessage = $"❌ Не удалось удалить: {ex.Message}";
             _crashReporter.WriteCrashReport("UninstallUl", ex, note: $"Game: {card.GameName}");
         }
     }
@@ -641,7 +641,7 @@ public partial class MainViewModel
 
         if (File.Exists(GetDcCachePath(is32Bit)))
         {
-            progress?.Report(("Installing from cache...", 50));
+            progress?.Report(("Установка из кеша...", 50));
             return;
         }
 
@@ -656,7 +656,7 @@ public partial class MainViewModel
         var url = currentUrl;
         if (string.IsNullOrEmpty(url))
         {
-            throw new InvalidOperationException("Could not determine Display Commander download URL from GitHub releases.");
+            throw new InvalidOperationException("Не удалось определить ссылку на скачивание Display Commander из релизов GitHub.");
         }
 
         Directory.CreateDirectory(DownloadPaths.FrameLimiter);
@@ -766,7 +766,7 @@ public partial class MainViewModel
         if (!await CheckInstallWarningAsync(card.GameName, "dc")) return;
 
         card.DcIsInstalling = true;
-        card.DcActionMessage = "Downloading Display Commander...";
+        card.DcActionMessage = "Загрузка Display Commander...";
         card.DcProgress = 0;
         try
         {
@@ -847,7 +847,7 @@ public partial class MainViewModel
                 if (cachedVersion == "latest_build") cachedVersion = null;
                 card.DcInstalledVersion = peVersion ?? cachedVersion ?? ReadDcInstalledVersion(card.Is32Bit);
                 card.DcStatus = GameStatus.Installed;
-                card.DcActionMessage = "✅ Display Commander installed!";
+                card.DcActionMessage = "✅ Display Commander установлен!";
                 card.DcIsInstalling = false;
                 card.NotifyAll();
                 card.FadeMessage(m => card.DcActionMessage = m, card.DcActionMessage);
@@ -857,7 +857,7 @@ public partial class MainViewModel
         {
             DispatcherQueue?.TryEnqueue(() =>
             {
-                card.DcActionMessage = $"❌ Install failed: {ex.Message}";
+                card.DcActionMessage = $"❌ Не удалось установить: {ex.Message}";
                 card.DcIsInstalling = false;
                 card.NotifyAll();
             });
@@ -896,13 +896,13 @@ public partial class MainViewModel
             card.DcInstalledFile = null;
             card.DcInstalledVersion = null;
             card.DcStatus = GameStatus.NotInstalled;
-            card.DcActionMessage = "✖ Display Commander removed.";
+            card.DcActionMessage = "✖ Display Commander удалён.";
             card.NotifyAll();
             card.FadeMessage(m => card.DcActionMessage = m, card.DcActionMessage);
         }
         catch (Exception ex)
         {
-            card.DcActionMessage = $"❌ Uninstall failed: {ex.Message}";
+            card.DcActionMessage = $"❌ Не удалось удалить: {ex.Message}";
             _crashReporter.WriteCrashReport("UninstallDc", ex, note: $"Game: {card.GameName}");
         }
     }
@@ -1001,7 +1001,7 @@ public partial class MainViewModel
             sections.Add((currentHeader, currentBody.ToString().TrimEnd()));
 
         if (sections.Count == 0)
-            return "(No changelog entries found)";
+            return "(Список изменений пуст)";
 
         // Find the section matching the installed version
         int startIndex = 0;
diff --git a/RenoDXCommander/ViewModels/MainViewModel.Install.Luma.cs b/RenoDXCommander/ViewModels/MainViewModel.Install.Luma.cs
index 8273896..14cf500 100644
--- a/RenoDXCommander/ViewModels/MainViewModel.Install.Luma.cs
+++ b/RenoDXCommander/ViewModels/MainViewModel.Install.Luma.cs
@@ -44,7 +44,7 @@ public partial class MainViewModel
 
         if (string.IsNullOrEmpty(card.InstallPath) || !Directory.Exists(card.InstallPath))
         {
-            card.RsActionMessage = "No install path — use 📁 to pick the game folder.";
+            card.RsActionMessage = "Путь установки не задан — выберите папку игры кнопкой 📁.";
             return;
         }
 
@@ -54,7 +54,7 @@ public partial class MainViewModel
             && !(card.LumaFeatureEnabled && card.IsLumaMode)
             && !card.ExcludeFromUpdateAllRef)
         {
-            card.RsActionMessage = "⚠ Install RE Framework first.";
+            card.RsActionMessage = "⚠ Сначала установите RE Framework.";
             return;
         }
 
@@ -110,7 +110,7 @@ public partial class MainViewModel
         }
 
         card.RsIsInstalling  = true;
-        card.RsActionMessage = "Starting ReShade download...";
+        card.RsActionMessage = "Начинаю загрузку ReShade...";
         try
         {
             var progress = new Progress<(string msg, double pct)>(p =>
@@ -159,7 +159,7 @@ public partial class MainViewModel
                 card.RsInstalledFile    = record.InstalledAs;
                 card.RsInstalledVersion = AuxInstallService.ReadInstalledVersion(record.InstallPath, record.InstalledAs);
                 card.RsStatus           = GameStatus.Installed;
-                card.RsActionMessage    = "✅ ReShade installed!";
+                card.RsActionMessage    = "✅ ReShade установлен!";
                 card.NotifyAll();
                 card.FadeMessage(m => card.RsActionMessage = m, card.RsActionMessage);
 
@@ -189,7 +189,7 @@ public partial class MainViewModel
         if (IsVulkanLayerInstalledFunc())
         {
             card.RsIsInstalling  = true;
-            card.RsActionMessage = "Installing Vulkan ReShade...";
+            card.RsActionMessage = "Установка Vulkan ReShade...";
             try
             {
                 AuxInstallService.MergeRsVulkanIni(card.InstallPath, card.GameName, BuildScreenshotSavePath(card.GameName), _settingsViewModel.OverlayHotkey, _settingsViewModel.ScreenshotHotkey);
@@ -205,7 +205,7 @@ public partial class MainViewModel
                 {
                     card.RsInstalledVersion = vulkanVersion;
                     card.RsStatus        = GameStatus.Installed;
-                    card.RsActionMessage = "✅ Vulkan ReShade installed!";
+                    card.RsActionMessage = "✅ Vulkan ReShade установлен!";
                     card.NotifyAll();
                     card.FadeMessage(m => card.RsActionMessage = m, card.RsActionMessage);
 
@@ -236,7 +236,7 @@ public partial class MainViewModel
             if (ShowVulkanAdminRequiredDialog != null)
                 await ShowVulkanAdminRequiredDialog();
             else
-                card.RsActionMessage = "⚠ Administrator privileges are required for Vulkan layer installation. Restart RHI as admin.";
+                card.RsActionMessage = "⚠ Для установки слоя Vulkan нужны права администратора. Перезапустите RHI от администратора.";
             return;
         }
 
@@ -248,14 +248,14 @@ public partial class MainViewModel
                 var proceed = await ShowVulkanLayerWarningDialog();
                 if (!proceed)
                 {
-                    card.RsActionMessage = "Vulkan layer install cancelled.";
+                    card.RsActionMessage = "Установка слоя Vulkan отменена.";
                     return;
                 }
             }
         }
 
         card.RsIsInstalling  = true;
-        card.RsActionMessage = "Installing Vulkan ReShade layer...";
+        card.RsActionMessage = "Установка слоя Vulkan ReShade...";
         try
         {
             // 3. Install the global Vulkan layer (copies DLL, writes manifest, registers in registry)
@@ -283,7 +283,7 @@ public partial class MainViewModel
             {
                 card.RsInstalledVersion = vulkanVersion;
                 card.RsStatus        = GameStatus.Installed;
-                card.RsActionMessage = "✅ ReShade installed (Vulkan Layer)!";
+                card.RsActionMessage = "✅ ReShade установлен (слой Vulkan)!";
                 card.NotifyAll();
                 card.FadeMessage(m => card.RsActionMessage = m, card.RsActionMessage);
 
@@ -318,7 +318,7 @@ public partial class MainViewModel
             if (ShowVulkanAdminRequiredDialog != null)
                 await ShowVulkanAdminRequiredDialog();
             else
-                card.RsActionMessage = "⚠ Administrator privileges are required for GAC symlink installation. Restart RHI as admin.";
+                card.RsActionMessage = "⚠ Для установки символической ссылки в GAC нужны права администратора. Перезапустите RHI от администратора.";
             return;
         }
 
@@ -331,7 +331,7 @@ public partial class MainViewModel
         dllFileName ??= "dxgi.dll"; // default — DX11/DX12 games use dxgi.dll
 
         card.RsIsInstalling = true;
-        card.RsActionMessage = "Installing ReShade (GAC symlink)...";
+        card.RsActionMessage = "Установка ReShade (ссылка GAC)...";
         try
         {
             AuxInstallService.EnsureReShadeStaging();
@@ -361,7 +361,7 @@ public partial class MainViewModel
                 card.RsInstalledFile = dllFileName;
                 card.RsInstalledVersion = version;
                 card.RsStatus = GameStatus.Installed;
-                card.RsActionMessage = "✅ ReShade installed (GAC symlink)!";
+                card.RsActionMessage = "✅ ReShade установлен (ссылка GAC)!";
                 card.NotifyAll();
                 card.FadeMessage(m => card.RsActionMessage = m, card.RsActionMessage);
                 DeployAddonsForCard(card.GameName);
@@ -396,7 +396,7 @@ public partial class MainViewModel
         // GAC symlink removal requires admin (files are in C:\Windows\...)
         if (isGacGame && !IsRunningAsAdminFunc())
         {
-            card.RsActionMessage = "⚠ Administrator privileges required to remove GAC symlinks. Enable Admin Mode in Settings.";
+            card.RsActionMessage = "⚠ Для удаления ссылок GAC нужны права администратора. Включите режим администратора в настройках.";
             card.NotifyAll();
             return;
         }
@@ -439,13 +439,13 @@ public partial class MainViewModel
             card.RsInstalledFile    = null;
             card.RsInstalledVersion = null;
             card.RsStatus           = GameStatus.NotInstalled;
-            card.RsActionMessage    = "✖ ReShade removed.";
+            card.RsActionMessage    = "✖ ReShade удалён.";
             card.NotifyAll();
             card.FadeMessage(m => card.RsActionMessage = m, card.RsActionMessage);
         }
         catch (Exception ex)
         {
-            card.RsActionMessage = $"❌ Uninstall failed: {ex.Message}";
+            card.RsActionMessage = $"❌ Не удалось удалить: {ex.Message}";
             _crashReporter.WriteCrashReport("UninstallReShade", ex, note: $"Game: {card.GameName}");
         }
     }
@@ -484,13 +484,13 @@ public partial class MainViewModel
 
             // 6. Update card status — do NOT touch the global Vulkan layer
             card.RsStatus        = GameStatus.NotInstalled;
-            card.RsActionMessage = "✖ Vulkan ReShade removed.";
+            card.RsActionMessage = "✖ Vulkan ReShade удалён.";
             card.NotifyAll();
             card.FadeMessage(m => card.RsActionMessage = m, card.RsActionMessage);
         }
         catch (Exception ex)
         {
-            card.RsActionMessage = $"❌ Uninstall failed: {ex.Message}";
+            card.RsActionMessage = $"❌ Не удалось удалить: {ex.Message}";
             _crashReporter.WriteCrashReport("UninstallVulkanReShade", ex, note: $"Game: {card.GameName}");
         }
     }
@@ -504,7 +504,7 @@ public partial class MainViewModel
 
         if (string.IsNullOrEmpty(card.InstallPath) || !Directory.Exists(card.InstallPath))
         {
-            card.RefActionMessage = "No install path — use 📁 to pick the game folder.";
+            card.RefActionMessage = "Путь установки не задан — выберите папку игры кнопкой 📁.";
             return;
         }
 
@@ -512,7 +512,7 @@ public partial class MainViewModel
         if (!await CheckInstallWarningAsync(card.GameName, "reframework")) return;
 
         card.RefIsInstalling = true;
-        card.RefActionMessage = "Starting RE Framework download...";
+        card.RefActionMessage = "Начинаю загрузку RE Framework...";
         try
         {
             var progress = new Progress<(string msg, double pct)>(p =>
@@ -526,7 +526,7 @@ public partial class MainViewModel
                 card.RefRecord = record;
                 card.RefInstalledVersion = record.InstalledVersion;
                 card.RefStatus = GameStatus.Installed;
-                card.RefActionMessage = "✅ RE Framework installed!";
+                card.RefActionMessage = "✅ RE Framework установлен!";
                 card.NotifyAll();
                 card.FadeMessage(m => card.RefActionMessage = m, card.RefActionMessage);
             });
@@ -553,13 +553,13 @@ public partial class MainViewModel
             card.RefRecord = null;
             card.RefInstalledVersion = null;
             card.RefStatus = GameStatus.NotInstalled;
-            card.RefActionMessage = "✖ RE Framework removed.";
+            card.RefActionMessage = "✖ RE Framework удалён.";
             card.NotifyAll();
             card.FadeMessage(m => card.RefActionMessage = m, card.RefActionMessage);
         }
         catch (Exception ex)
         {
-            card.RefActionMessage = $"❌ Uninstall failed: {ex.Message}";
+            card.RefActionMessage = $"❌ Не удалось удалить: {ex.Message}";
             _crashReporter.WriteCrashReport("UninstallREFramework", ex, note: $"Game: {card.GameName}");
         }
     }
@@ -893,7 +893,7 @@ public partial class MainViewModel
         if (!skipWarning && !await CheckInstallWarningAsync(card.GameName, "luma")) return;
 
         card.IsLumaInstalling = true;
-        card.LumaActionMessage = "Installing Luma...";
+        card.LumaActionMessage = "Установка Luma...";
         try
         {
             LumaInstalledRecord record;
@@ -906,16 +906,16 @@ public partial class MainViewModel
                 var parsed = NexusUpdateService.ParseNexusUrl(mod.NexusUrl);
                 if (parsed == null)
                 {
-                    card.LumaActionMessage = "❌ Invalid Nexus URL.";
+                    card.LumaActionMessage = "❌ Неверная ссылка Nexus.";
                     return;
                 }
 
-                card.LumaActionMessage = "Fetching mod info...";
+                card.LumaActionMessage = "Загрузка информации о модах...";
                 var latestFile = await nexusDl.GetLatestMainFileAsync(parsed.Value.Domain, parsed.Value.ModId).ConfigureAwait(false);
                 if (latestFile == null)
                 {
                     _crashReporter.Log($"[InstallLumaAsync] Nexus — no MAIN file for '{card.GameName}'");
-                    DispatcherQueue?.TryEnqueue(() => card.LumaActionMessage = "No downloadable file found on Nexus.");
+                    DispatcherQueue?.TryEnqueue(() => card.LumaActionMessage = "На Nexus не найдено файлов для скачивания.");
                     return;
                 }
 
@@ -923,7 +923,7 @@ public partial class MainViewModel
                 if (uri == null)
                 {
                     _crashReporter.Log($"[InstallLumaAsync] Nexus — could not resolve CDN URI for '{card.GameName}'");
-                    DispatcherQueue?.TryEnqueue(() => card.LumaActionMessage = "Could not resolve Nexus download link.");
+                    DispatcherQueue?.TryEnqueue(() => card.LumaActionMessage = "Не удалось определить ссылку на скачивание с Nexus.");
                     return;
                 }
 
@@ -933,7 +933,7 @@ public partial class MainViewModel
                 var tempPath = await nexusDl.DownloadToTempAsync(uri, progress).ConfigureAwait(false);
                 if (tempPath == null)
                 {
-                    DispatcherQueue?.TryEnqueue(() => card.LumaActionMessage = "Download failed.");
+                    DispatcherQueue?.TryEnqueue(() => card.LumaActionMessage = "Не удалось скачать.");
                     return;
                 }
 
@@ -964,7 +964,7 @@ public partial class MainViewModel
             {
                 // Bespoke drag-drop install — no download URL available
                 _crashReporter.Log($"[InstallLumaAsync] '{card.GameName}' Luma mod has no download URL (bespoke drag-drop install) — cannot reinstall automatically");
-                DispatcherQueue?.TryEnqueue(() => card.LumaActionMessage = "Drop a Luma archive onto the card to reinstall.");
+                DispatcherQueue?.TryEnqueue(() => card.LumaActionMessage = "Перетащите архив Luma на карточку, чтобы переустановить.");
                 return;
             }
             else
@@ -994,7 +994,7 @@ public partial class MainViewModel
             {
                 card.LumaRecord = record;
                 card.LumaStatus = GameStatus.Installed;
-                card.LumaActionMessage = "Luma installed!";
+                card.LumaActionMessage = "Luma установлен!";
                 card.FadeMessage(m => card.LumaActionMessage = m, card.LumaActionMessage);
             });
 
@@ -1002,7 +1002,7 @@ public partial class MainViewModel
         }
         catch (Exception ex)
         {
-            DispatcherQueue?.TryEnqueue(() => card.LumaActionMessage = $"❌ Install failed: {ex.Message}");
+            DispatcherQueue?.TryEnqueue(() => card.LumaActionMessage = $"❌ Не удалось установить: {ex.Message}");
             _crashReporter.WriteCrashReport("InstallLuma", ex, note: $"Game: {card.GameName}");
         }
         finally
@@ -1026,7 +1026,7 @@ public partial class MainViewModel
         // Deploy RHI's newest DLSS version (Luma bundles its own — RHI manages it instead)
         try
         {
-            DispatcherQueue?.TryEnqueue(() => card.LumaActionMessage = "Updating DLSS...");
+            DispatcherQueue?.TryEnqueue(() => card.LumaActionMessage = "Обновление DLSS...");
             var newestDlssPath = await _dlssStreamlineService.EnsureNewestDlssCachedAsync();
             if (newestDlssPath != null && File.Exists(newestDlssPath))
             {
@@ -1053,7 +1053,7 @@ public partial class MainViewModel
         // Luma's bundled ReShade DLL was excluded from the zip — RHI manages ReShade.
         // When dgVoodoo2 is being deployed: force ReShade to dxgi.dll so it hooks dgVoodoo's
         // DX11 output rather than competing with dgVoodoo2 for the d3d9.dll slot.
-        DispatcherQueue?.TryEnqueue(() => card.LumaActionMessage = "Installing ReShade...");
+        DispatcherQueue?.TryEnqueue(() => card.LumaActionMessage = "Установка ReShade...");
         await InstallReShadeInternalAsync(card, needsDgVoodoo ? "dxgi.dll" : null);
 
         // ── dgVoodoo2 (DX9→DX11 translation layer — required for some legacy games) ────
@@ -1064,7 +1064,7 @@ public partial class MainViewModel
         {
             try
             {
-                DispatcherQueue?.TryEnqueue(() => card.LumaActionMessage = "Installing dgVoodoo2...");
+                DispatcherQueue?.TryEnqueue(() => card.LumaActionMessage = "Установка dgVoodoo2...");
                 var dgVoodooSvc = App.Services.GetRequiredService<DgVoodooService>();
 
                 // Prefer the version recommended by the wiki for this specific mod.
@@ -1155,7 +1155,7 @@ public partial class MainViewModel
         }
 
         // Clear the action message — drag-drop path has no subsequent step to overwrite it
-        card.FadeMessage(m => card.LumaActionMessage = m, "✅ Luma installed!");
+        card.FadeMessage(m => card.LumaActionMessage = m, "✅ Luma установлен!");
     }
 
     [RelayCommand]
@@ -1185,7 +1185,7 @@ public partial class MainViewModel
             _lumaService.Uninstall(card.LumaRecord);
             card.LumaRecord = null;
             card.LumaStatus = GameStatus.NotInstalled;
-            card.LumaActionMessage = "✖ Luma removed.";
+            card.LumaActionMessage = "✖ Luma удалён.";
 
             // Clean up nvngx_dlss.dll deployed by ApplyLumaPostInstallAsync.
             // It's not in InstalledFiles (deployed after record was saved), so handle it here.
@@ -1218,7 +1218,7 @@ public partial class MainViewModel
             bool wasDgVoodooGame = wasDgVoodoo;
             if (wasDgVoodooGame && card.IsRsInstalled)
             {
-                card.LumaActionMessage = "Restoring ReShade...";
+                card.LumaActionMessage = "Восстановление ReShade...";
                 _ = InstallReShadeInternalAsync(card, forceFilename: null);
             }
             else if (card.IsRsInstalled)
@@ -1320,7 +1320,7 @@ public partial class MainViewModel
         }
         catch (Exception ex)
         {
-            card.LumaActionMessage = $"❌ Uninstall failed: {ex.Message}";
+            card.LumaActionMessage = $"❌ Не удалось удалить: {ex.Message}";
             _crashReporter.WriteCrashReport("UninstallLuma", ex, note: $"Game: {card.GameName}");
         }
     }
@@ -1353,8 +1353,8 @@ public partial class MainViewModel
             {
                 Title = $"⚠ Install Note — {gameName}",
                 Content = message,
-                PrimaryButtonText = "Continue",
-                CloseButtonText = "Cancel",
+                PrimaryButtonText = "Продолжить",
+                CloseButtonText = "Отмена",
                 DefaultButton = Microsoft.UI.Xaml.Controls.ContentDialogButton.Primary,
                 XamlRoot = xamlRoot,
             };
@@ -1440,7 +1440,7 @@ public partial class MainViewModel
 
             card.UseNormalReShade = true;
             card.RsStatus = GameStatus.NotInstalled;
-            card.RsActionMessage = "Normal ReShade selected — click Install to deploy.";
+            card.RsActionMessage = "Выбран обычный ReShade — нажмите «Установить» для развертывания.";
             card.NotifyAll();
             card.FadeMessage(m => card.RsActionMessage = m, card.RsActionMessage);
             _crashReporter.Log($"[SetUseNormalReShade] '{card.GameName}' flagged for normal ReShade (not installed yet)");
@@ -1476,7 +1476,7 @@ public partial class MainViewModel
 
             card.UseNormalReShade = false;
             card.RsStatus = GameStatus.NotInstalled;
-            card.RsActionMessage = "Addon ReShade selected — click Install to deploy.";
+            card.RsActionMessage = "Выбран ReShade с аддонами — нажмите «Установить» для развертывания.";
             card.NotifyAll();
             card.FadeMessage(m => card.RsActionMessage = m, card.RsActionMessage);
             _crashReporter.Log($"[SetUseNormalReShade] '{card.GameName}' flagged for addon ReShade (not installed yet)");
diff --git a/RenoDXCommander/ViewModels/MainViewModel.Install.Nexus.cs b/RenoDXCommander/ViewModels/MainViewModel.Install.Nexus.cs
index e5747ca..09cbe23 100644
--- a/RenoDXCommander/ViewModels/MainViewModel.Install.Nexus.cs
+++ b/RenoDXCommander/ViewModels/MainViewModel.Install.Nexus.cs
@@ -33,7 +33,7 @@ public partial class MainViewModel
         if (!await CheckInstallWarningAsync(card.GameName, "renodx")) return;
 
         card.IsInstalling = true;
-        card.ActionMessage = "Fetching mod info...";
+        card.ActionMessage = "Загрузка информации о модах...";
 
         try
         {
@@ -41,7 +41,7 @@ public partial class MainViewModel
             if (latestFile == null)
             {
                 _crashReporter.Log($"[MainViewModel.InstallNexusModAsync] No MAIN file found for '{card.GameName}'");
-                DispatcherQueue?.TryEnqueue(() => { card.ActionMessage = "No downloadable file found."; card.IsInstalling = false; });
+                DispatcherQueue?.TryEnqueue(() => { card.ActionMessage = "Файлы для скачивания не найдены."; card.IsInstalling = false; });
                 return;
             }
 
@@ -50,7 +50,7 @@ public partial class MainViewModel
         catch (Exception ex)
         {
             _crashReporter.Log($"[MainViewModel.InstallNexusModAsync] '{card.GameName}' — {ex.Message}");
-            DispatcherQueue?.TryEnqueue(() => { card.ActionMessage = $"Install failed: {ex.Message}"; card.IsInstalling = false; });
+            DispatcherQueue?.TryEnqueue(() => { card.ActionMessage = $"Не удалось установить: {ex.Message}"; card.IsInstalling = false; });
         }
     }
 
@@ -82,7 +82,7 @@ public partial class MainViewModel
         if (!await CheckInstallWarningAsync(card.GameName, "renodx")) return;
 
         card.IsInstalling = true;
-        card.ActionMessage = "Resolving download link...";
+        card.ActionMessage = "Определение ссылки на скачивание...";
 
         try
         {
@@ -107,7 +107,7 @@ public partial class MainViewModel
             if (uri == null)
             {
                 _crashReporter.Log($"[MainViewModel.HandleNxmLinkAsync] Could not resolve download URI for '{card.GameName}'");
-                DispatcherQueue?.TryEnqueue(() => { card.ActionMessage = "Could not resolve download link."; card.IsInstalling = false; });
+                DispatcherQueue?.TryEnqueue(() => { card.ActionMessage = "Не удалось определить ссылку на скачивание."; card.IsInstalling = false; });
                 return;
             }
 
@@ -133,7 +133,7 @@ public partial class MainViewModel
         catch (Exception ex)
         {
             _crashReporter.Log($"[MainViewModel.HandleNxmLinkAsync] '{card.GameName}' — {ex.Message}");
-            DispatcherQueue?.TryEnqueue(() => { card.ActionMessage = $"Install failed: {ex.Message}"; card.IsInstalling = false; });
+            DispatcherQueue?.TryEnqueue(() => { card.ActionMessage = $"Не удалось установить: {ex.Message}"; card.IsInstalling = false; });
         }
     }
 
@@ -160,13 +160,13 @@ public partial class MainViewModel
     {
         var nexusDl = App.Services.GetRequiredService<NexusDownloadService>();
 
-        DispatcherQueue?.TryEnqueue(() => card.ActionMessage = "Resolving download link...");
+        DispatcherQueue?.TryEnqueue(() => card.ActionMessage = "Определение ссылки на скачивание...");
 
         var uri = await nexusDl.GetDownloadUriAsync(domain, modId, file.FileId).ConfigureAwait(false);
         if (uri == null)
         {
             _crashReporter.Log($"[MainViewModel.InstallNexusFileAsync] Could not get CDN URI for '{card.GameName}' file {file.FileId}");
-            DispatcherQueue?.TryEnqueue(() => { card.ActionMessage = "Download link unavailable."; card.IsInstalling = false; });
+            DispatcherQueue?.TryEnqueue(() => { card.ActionMessage = "Ссылка на скачивание недоступна."; card.IsInstalling = false; });
             return;
         }
 
@@ -181,7 +181,7 @@ public partial class MainViewModel
     {
         if (string.IsNullOrEmpty(card.InstallPath))
         {
-            DispatcherQueue?.TryEnqueue(() => { card.ActionMessage = "No install path set."; card.IsInstalling = false; });
+            DispatcherQueue?.TryEnqueue(() => { card.ActionMessage = "Путь установки не задан."; card.IsInstalling = false; });
             return;
         }
 
@@ -194,7 +194,7 @@ public partial class MainViewModel
         var tempPath = await nexusDl.DownloadToTempAsync(uri, progress).ConfigureAwait(false);
         if (tempPath == null)
         {
-            DispatcherQueue?.TryEnqueue(() => { card.ActionMessage = "Download failed."; card.IsInstalling = false; });
+            DispatcherQueue?.TryEnqueue(() => { card.ActionMessage = "Не удалось скачать."; card.IsInstalling = false; });
             return;
         }
 
@@ -239,7 +239,7 @@ public partial class MainViewModel
                 if (addonFiles.Count == 0)
                 {
                     _crashReporter.Log($"[MainViewModel.DownloadAndDeployNexusFileAsync] No renodx-*.addon64/32 found in archive for '{card.GameName}'");
-                    DispatcherQueue?.TryEnqueue(() => { card.ActionMessage = "No addon file found in archive."; card.IsInstalling = false; });
+                    DispatcherQueue?.TryEnqueue(() => { card.ActionMessage = "В архиве не найден файл аддона."; card.IsInstalling = false; });
                     return;
                 }
 
@@ -325,7 +325,7 @@ public partial class MainViewModel
                     card.Status                 = GameStatus.Installed;
                     card.IsInstalling           = false;
                     card.InstallProgress        = 0;
-                    card.FadeMessage(m => card.ActionMessage = m, "✅ Installed!");
+                    card.FadeMessage(m => card.ActionMessage = m, "✅ Установлено!");
                     _crashReporter.Log($"[MainViewModel.DownloadAndDeployNexusFileAsync] Complete: '{card.GameName}' — {addonFileName}");
 
                     if (!string.IsNullOrEmpty(card.InstallPath))
diff --git a/RenoDXCommander/ViewModels/MainViewModel.Install.cs b/RenoDXCommander/ViewModels/MainViewModel.Install.cs
index cf27e14..dd6b40a 100644
--- a/RenoDXCommander/ViewModels/MainViewModel.Install.cs
+++ b/RenoDXCommander/ViewModels/MainViewModel.Install.cs
@@ -119,7 +119,7 @@ public partial class MainViewModel
             card.IsExternalOnly = effectiveMod?.SnapshotUrl == null &&
                                   (effectiveMod?.NexusUrl != null || effectiveMod?.DiscordUrl != null);
             card.ExternalUrl    = effectiveMod?.NexusUrl ?? effectiveMod?.DiscordUrl ?? "";
-            card.ExternalLabel  = effectiveMod?.NexusUrl != null ? "Download from Nexus Mods" : "Download from Discord";
+            card.ExternalLabel  = effectiveMod?.NexusUrl != null ? "Скачать с Nexus Mods" : "Скачать с Discord";
             card.NexusUrl       = effectiveMod?.NexusUrl;
             card.DiscordUrl     = effectiveMod?.DiscordUrl;
             card.WikiStatus     = (mod == null && fallback != null && !card.UseUeExtended && !card.IsNativeHdrGame)
@@ -679,7 +679,7 @@ public partial class MainViewModel
                               : effectiveMod?.NexusUrl ?? effectiveMod?.DiscordUrl ?? "",
             ExternalLabel   = _wikiExclusions.Contains(game.Name)
                               ? ""
-                              : effectiveMod?.NexusUrl != null ? "Download from Nexus Mods" : "Download from Discord",
+                              : effectiveMod?.NexusUrl != null ? "Скачать с Nexus Mods" : "Скачать с Discord",
             NexusUrl        = effectiveMod?.NexusUrl,
             DiscordUrl      = _wikiExclusions.Contains(game.Name)
                               ? null
@@ -742,7 +742,7 @@ public partial class MainViewModel
                 if (foundCount > 0)
                 {
                     card.Status = GameStatus.Installed;
-                    card.InstalledAddonFileName = $"{foundCount} addons";
+                    card.InstalledAddonFileName = $"аддонов: {foundCount}";
                 }
             }
         }
@@ -832,7 +832,7 @@ public partial class MainViewModel
             card.Mod.SnapshotUrl = card.Mod.SnapshotUrl32;
         if (string.IsNullOrEmpty(card.InstallPath))
         {
-            card.ActionMessage = "No install path — use 📁 to pick the game folder.";
+            card.ActionMessage = "Путь установки не задан — выберите папку игры кнопкой 📁.";
             return;
         }
 
@@ -844,7 +844,7 @@ public partial class MainViewModel
         }
 
         card.IsInstalling = true;
-        card.ActionMessage = "Starting download...";
+        card.ActionMessage = "Начинаю загрузку...";
         _crashReporter.Log($"[MainViewModel.InstallModAsync] Install started: {card.GameName} → {card.InstallPath}");
         try
         {
@@ -961,7 +961,7 @@ public partial class MainViewModel
                 card.InstalledAddonFileName = record.AddonFileName;
                 card.RdxInstalledVersion    = AuxInstallService.ReadInstalledVersion(record.InstallPath, record.AddonFileName);
                 card.Status                 = GameStatus.Installed;
-                card.FadeMessage(m => card.ActionMessage = m, "✅ Installed! Press Home in-game to open ReShade.");
+                card.FadeMessage(m => card.ActionMessage = m, "✅ Установлено! Нажмите Home в игре, чтобы открыть ReShade.");
                 _crashReporter.Log($"[MainViewModel.InstallModAsync] Install complete: {card.GameName} — {record.AddonFileName}");
                 // Reset Nexus baseline so update indicator clears after install
                 _nexusUpdateService.ResetBaseline(card.GameName);
@@ -1020,7 +1020,7 @@ public partial class MainViewModel
         card.InstalledAddonFileName = null;
         card.RdxInstalledVersion    = null;
         card.Status                 = GameStatus.Available;
-        card.ActionMessage          = "✖ Mod removed.";
+        card.ActionMessage          = "✖ Мод удалён.";
         card.FadeMessage(m => card.ActionMessage = m, card.ActionMessage);
         // Clear the addon file cache so the next Refresh doesn't think a file is still there.
         if (!string.IsNullOrEmpty(card.InstallPath))
diff --git a/RenoDXCommander/ViewModels/MainViewModel.Settings.cs b/RenoDXCommander/ViewModels/MainViewModel.Settings.cs
index 35721ae..dd75a4b 100644
--- a/RenoDXCommander/ViewModels/MainViewModel.Settings.cs
+++ b/RenoDXCommander/ViewModels/MainViewModel.Settings.cs
@@ -783,7 +783,7 @@ public partial class MainViewModel
                     {
                         SetUalInstalledAs(gameName, chosenName, store);
                         _crashReporter.Log($"[SfAutoConfig] Installed UAL as '{chosenName}' for '{gameName}'" +
-                            (hookedOriginal != null ? $" (chained '{hookedOriginal}')" : ""));
+                            (hookedOriginal != null ? $" (в цепочке: «{hookedOriginal}»)" : ""));
                     }
                 }
                 catch (Exception ex) { _crashReporter.Log($"[SfAutoConfig] UAL install failed — {ex.Message}"); }
diff --git a/RenoDXCommander/ViewModels/MainViewModel.Update.cs b/RenoDXCommander/ViewModels/MainViewModel.Update.cs
index 8343e37..abb06ae 100644
--- a/RenoDXCommander/ViewModels/MainViewModel.Update.cs
+++ b/RenoDXCommander/ViewModels/MainViewModel.Update.cs
@@ -221,7 +221,7 @@ public partial class MainViewModel
         try
         {
             var path = Path.Combine(AppContext.BaseDirectory, "RHI_PatchNotes.md");
-            if (!File.Exists(path)) return "Patch notes file not found.";
+            if (!File.Exists(path)) return "Файл списка изменений не найден.";
 
             var lines = File.ReadAllLines(path);
             var sections = new List<string>();
@@ -264,11 +264,11 @@ public partial class MainViewModel
 
             return sections.Count > 0
                 ? string.Join("\n\n---\n\n", sections)
-                : "No patch notes available.";
+                : "Список изменений недоступен.";
         }
         catch (Exception ex)
         {
-            return $"Error reading patch notes: {ex.Message}";
+            return $"Ошибка чтения списка изменений: {ex.Message}";
         }
     }
 
diff --git a/RenoDXCommander/ViewModels/MainViewModel.cs b/RenoDXCommander/ViewModels/MainViewModel.cs
index b74ba78..1e82fa3 100644
--- a/RenoDXCommander/ViewModels/MainViewModel.cs
+++ b/RenoDXCommander/ViewModels/MainViewModel.cs
@@ -89,7 +89,7 @@ public partial class MainViewModel : ObservableObject
         set => _settingsViewModel.LastSeenVersion = value;
     }
 
-    public string UpdateButtonTooltip => "Update ReShade, RenoDX, ReLimiter, Display Commander, and RE Framework for all games";
+    public string UpdateButtonTooltip => "Обновить ReShade, RenoDX, ReLimiter, Display Commander и RE Framework во всех играх";
 
     /// <summary>
     /// The global shader picker button is disabled while custom shaders are active.
@@ -144,9 +144,9 @@ public partial class MainViewModel : ObservableObject
 
     public string LayoutToggleLabel => CurrentViewLayout switch
     {
-        ViewLayout.Detail => "Detail View",
-        ViewLayout.Compact => "Simple View",
-        _ => "Detail View",
+        ViewLayout.Detail => "Подробный вид",
+        ViewLayout.Compact => "Простой вид",
+        _ => "Подробный вид",
     };
 
     partial void OnCurrentViewLayoutChanged(ViewLayout value)
@@ -577,12 +577,12 @@ public partial class MainViewModel : ObservableObject
 
     // UE common warnings shown at bottom of every generic UE info dialog
     private const string UnrealWarnings =
-        "\n\n⚠ COMMON UNREAL ENGINE MOD WARNINGS\n\n" +
-        "🖥 Black Screen on Launch\n" +
-        "Upgrade `R10G10B10A2_UNORM` → `output size`\n" +
-        "Unlock upgrade sliders: Settings Mode → Advanced, then restart game.\n\n" +
-        "🖥 DLSS FG Flickering\n" +
-        "Replace DLSSG DLL with older 3.8.x (locks FG x2) or use DLSS FIX (beta) from Discord.";
+        "\n\n⚠ ОБЩИЕ ПРЕДУПРЕЖДЕНИЯ ДЛЯ МОДОВ UNREAL ENGINE\n\n" +
+        "🖥 Чёрный экран при запуске\n" +
+        "Замена `R10G10B10A2_UNORM` → `output size`\n" +
+        "Откройте ползунки апгрейдов: Settings Mode → Advanced, затем перезапустите игру.\n\n" +
+        "🖥 Мерцание DLSS FG\n" +
+        "Замените DLL DLSSG на старую 3.8.x (фиксирует FG x2) или используйте DLSS FIX (бета) из Discord.";
 
     public MainViewModel(
         HttpClient http,
diff --git a/build-setup.bat b/build-setup.bat
new file mode 100644
index 0000000..54c7b05
--- /dev/null
+++ b/build-setup.bat
@@ -0,0 +1,71 @@
+@echo off
+rem ============================================================
+rem  Сборка русского инсталлятора RHI в один клик.
+rem  Требуется: Windows, .NET 8 SDK, Inno Setup 6 (в C:\Program Files (x86)\Inno Setup 6).
+rem  Результат: Installers\RHI-Setup.exe
+rem ============================================================
+setlocal
+cd /d "%~dp0"
+
+set OUT=%~dp0publish\RHI
+set ISCC=%ProgramFiles(x86)%\Inno Setup 6\ISCC.exe
+
+if not exist "%ISCC%" (
+  echo [ОШИБКА] Inno Setup 6 не найден: %ISCC%
+  echo Установите: https://jrsoftware.org/isdl.php
+  exit /b 1
+)
+
+rem Закрываем RHI и хелперы, чтобы публикация смогла заменить exe
+taskkill /F /IM RHI.exe >nul 2>nul
+taskkill /F /IM RHI-Stats.exe >nul 2>nul
+taskkill /F /IM RHI.DropHelper.exe >nul 2>nul
+timeout /t 2 /nobreak >nul
+
+echo [1/4] Публикация RHI (WinUI 3)...
+dotnet publish RenoDXCommander\RenoDXCommander.csproj -c Release -r win-x64 -p:PublishSingleFile=true -p:Platform=x64 --self-contained false -o "%OUT%"
+if errorlevel 1 goto :err
+
+echo [2/4] Сборка DropHelper...
+dotnet build RHI.DropHelper\RHI.DropHelper.csproj -c Release -v quiet
+if errorlevel 1 goto :err
+copy /y "RHI.DropHelper\bin\x64\Release\net8.0-windows\RHI.DropHelper.exe" "%OUT%\" >nul
+copy /y "RHI.DropHelper\bin\x64\Release\net8.0-windows\RHI.DropHelper.dll" "%OUT%\" >nul 2>nul
+copy /y "RHI.DropHelper\bin\x64\Release\net8.0-windows\RHI.DropHelper.runtimeconfig.json" "%OUT%\" >nul
+
+echo [3/4] Копирование сопутствующих файлов...
+copy /y "RenoDXCommander\icon.ico" "%OUT%\" >nul
+copy /y "RenoDXCommander\7z.exe" "%OUT%\" >nul
+copy /y "RenoDXCommander\7z.dll" "%OUT%\" >nul
+copy /y "RenoDXCommander\ReShade.ini" "%OUT%\" >nul
+copy /y "RenoDXCommander\ReShade.Vulkan.ini" "%OUT%\" >nul
+copy /y "RenoDXCommander\ReShade64.json" "%OUT%\" >nul
+copy /y "RenoDXCommander\RHI_PatchNotes.md" "%OUT%\" >nul
+copy /y "RenoDXCommander\relimiter.ini" "%OUT%\" >nul
+copy /y "RenoDXCommander\reshade.rdr2.ini" "%OUT%\" >nul
+if not exist "%OUT%\Assets\icons" mkdir "%OUT%\Assets\icons"
+copy /y "RenoDXCommander\Assets\icons\*.ico" "%OUT%\Assets\icons\" >nul
+copy /y "RenoDXCommander\Assets\icons\*.png" "%OUT%\Assets\icons\" >nul
+copy /y "RenoDXCommander\OptiScaler_nightly.nvidia.ini" "%OUT%\" >nul
+copy /y "RenoDXCommander\OptiScaler_nightly.amd-dlss.ini" "%OUT%\" >nul
+copy /y "RenoDXCommander\OptiScaler_nightly.amd-nodlss.ini" "%OUT%\" >nul
+copy /y "RenoDXCommander\OptiScaler_dlssnr.nvidia.ini" "%OUT%\" >nul
+copy /y "RenoDXCommander\OptiScaler_dlssnr.amd-dlss.ini" "%OUT%\" >nul
+copy /y "RenoDXCommander\OptiScaler_dlssnr.amd-nodlss.ini" "%OUT%\" >nul
+copy /y "RenoDXCommander\OptiScaler.nvidia.ini" "%OUT%\" >nul
+copy /y "RenoDXCommander\OptiScaler.amd-dlss.ini" "%OUT%\" >nul
+copy /y "RenoDXCommander\OptiScaler.amd-nodlss.ini" "%OUT%\" >nul
+
+echo [4/4] Компиляция инсталлятора (Inno Setup, русский)...
+if not exist "Installers" mkdir "Installers"
+"%ISCC%" /DPublishDir="%OUT%" /DIconFile="%~dp0RenoDXCommander\icon.ico" /DInstallerOutDir="%~dp0Installers" "RHI Setup.iss"
+if errorlevel 1 goto :err
+
+echo.
+echo Готово: %~dp0Installers\RHI-Setup.exe
+exit /b 0
+
+:err
+echo.
+echo [ОШИБКА] Сборка не удалась — см. сообщения выше.
+exit /b 1
diff --git a/docs/DETAILED_GUIDE.md b/docs/DETAILED_GUIDE.md
index 17b4b89..22900e7 100644
--- a/docs/DETAILED_GUIDE.md
+++ b/docs/DETAILED_GUIDE.md
@@ -1,1075 +1,1075 @@
-# RHI — Detailed Guide
+# RHI — Подробное руководство
 
-This document covers every feature in RHI. For a quick overview, see the [README](../README.md).
+В этом документе описаны все возможности RHI. Для быстрого обзора см. [README](../README.md).
 
 ---
 
-## Table of Contents
+## Содержание
 
-- [Layout and Views](#layout-and-views)
-- [Settings Page](#settings-page)
-- [Game Detection](#game-detection)
-- [Graphics API Detection](#graphics-api-detection)
-- [Components](#components)
+- [Макет и представления](#макет-и-представления)
+- [Страница настроек](#страница-настроек)
+- [Определение игр](#определение-игр)
+- [Определение графического API](#определение-графического-api)
+- [Компоненты](#компоненты)
 - [ReShade](#reshade)
 - [RenoDX](#renodx)
 - [RE Framework](#re-framework)
 - [Luma Framework](#luma-framework)
-- [UE-Extended Auto-Configuration](#ue-extended-auto-configuration)
-- [Ryubing Emulator Support](#ryubing-emulator-support)
-- [Frame Rate Limiters](#frame-rate-limiters)
-- [DLSS & Streamline Manager](#dlss--streamline-manager)
-- [DLSS & Streamline Defaults](#dlss--streamline-defaults)
-- [Nvidia Profile Overrides](#nvidia-profile-overrides)
-- [Multi Frame Generation](#multi-frame-generation)
-- [Global Nvidia Settings](#global-nvidia-settings)
-- [Admin Mode](#admin-mode)
-- [Profile Export and Import](#profile-export-and-import)
+- [Автонастройка UE-Extended](#автонастройка-ue-extended)
+- [Поддержка эмулятора Ryubing](#поддержка-эмулятора-ryubing)
+- [Ограничители частоты кадров](#ограничители-частоты-кадров)
+- [Менеджер DLSS и Streamline](#менеджер-dlss-и-streamline)
+- [DLSS и Streamline по умолчанию](#dlss-и-streamline-по-умолчанию)
+- [Переопределения профиля NVIDIA](#переопределения-профиля-nvidia)
+- [Мульти-генерация кадров](#мульти-генерация-кадров)
+- [Глобальные настройки NVIDIA](#глобальные-настройки-nvidia)
+- [Режим администратора](#режим-администратора)
+- [Экспорт и импорт профилей](#экспорт-и-импорт-профилей)
 - [OptiScaler](#optiscaler)
-- [Shader Packs](#shader-packs)
-- [ReShade Addon Management](#reshade-addon-management)
-- [Game Launch](#game-launch)
-- [HDR Auto-Toggle](#hdr-auto-toggle)
-- [Running Game Indicator](#running-game-indicator)
-- [System Tray & Jump List](#system-tray--jump-list)
+- [Наборы шейдеров](#наборы-шейдеров)
+- [Управление аддонами ReShade](#управление-аддонами-reshade)
+- [Запуск игр](#запуск-игр)
+- [Автопереключение HDR](#автопереключение-hdr)
+- [Индикатор запущенной игры](#индикатор-запущенной-игры)
+- [Системный трей и список переходов](#системный-трей-и-список-переходов)
 - [Digital Vibrance](#digital-vibrance)
-- [Peak Brightness (Nits)](#peak-brightness-nits)
+- [Пиковая яркость (ниты)](#пиковая-яркость-ниты)
 - [DOF Fix](#dof-fix)
-- [Per-Game Overrides](#per-game-overrides)
-- [ReShade Presets](#reshade-presets)
-- [Nexus Mods and PCGamingWiki Links](#nexus-mods-and-pcgamingwiki-links)
-- [UW Fix and Ultra+ Links](#uw-fix-and-ultra-links)
-- [Vulkan ReShade Support](#vulkan-reshade-support)
+- [Переопределения для каждой игры](#переопределения-для-каждой-игры)
+- [Пресеты ReShade](#пресеты-reshade)
+- [Ссылки Nexus Mods и PCGamingWiki](#ссылки-nexus-mods-и-pcgamingwiki)
+- [UW Fix и ссылки Ultra+](#uw-fix-и-ссылки-ultra)
+- [Поддержка Vulkan в ReShade](#поддержка-vulkan-в-reshade)
 - [DXVK](#dxvk)
-- [Foreign DLL Protection](#foreign-dll-protection)
-- [Drag-and-Drop](#drag-and-drop)
-- [Addon Auto-Detection](#addon-auto-detection)
-- [Update All](#update-all)
-- [Auto-Update](#auto-update)
-- [Remote Manifest](#remote-manifest)
-- [Message of the Day](#message-of-the-day)
-- [Performance](#performance)
-- [Data Storage](#data-storage)
-- [Troubleshooting](#troubleshooting)
-- [Third-Party Components](#third-party-components)
+- [Защита от чужих DLL](#защита-от-чужих-dll)
+- [Drag-and-drop](#drag-and-drop)
+- [Автоопределение аддонов](#автоопределение-аддонов)
+- [Обновить всё](#обновить-всё)
+- [Автообновление](#автообновление)
+- [Удалённый манифест](#удалённый-манифест)
+- [Сообщение дня](#сообщение-дня)
+- [Производительность](#производительность)
+- [Хранение данных](#хранение-данных)
+- [Решение проблем](#решение-проблем)
+- [Сторонние компоненты](#сторонние-компоненты)
 
 ---
 
-## Layout and Views
+## Макет и представления
 
-RHI has two view modes, a Settings page, and an About page. Your chosen view, window size, and window position are remembered across restarts. Fresh installs default to Compact View.
+В RHI два режима просмотра, страница настроек и страница «О программе». Выбранный вид, размер и положение окна сохраняются между перезапусками. Свежая установка по умолчанию открывается в компактном виде.
 
-### Detail View
+### Подробный вид
 
-A game list sidebar on the left, and a multi-section detail panel on the right. Selecting a game shows four sections:
+Слева — боковая панель со списком игр, справа — многосекционная панель подробностей. При выборе игры показываются четыре секции:
 
-1. **Components** — game info badges, install/update/uninstall buttons for each component
-2. **Game Overrides** — per-game settings (DLL naming, shaders, addons, DXVK, RS channel, bitness, API)
-3. **Nvidia Profile Overrides** — DLSS/Streamline management and driver profile settings (VSync, Latency, Smooth Motion, Power, ReBAR)
-4. **Management** — change install folder, reset overrides, copy diagnostic report
+1. **Компоненты** — значки информации об игре, кнопки установки/обновления/удаления для каждого компонента
+2. **Переопределения игры** — настройки для каждой игры (имена DLL, шейдеры, аддоны, DXVK, канал RS, разрядность, API)
+3. **Переопределения профиля NVIDIA** — управление DLSS/Streamline и настройки профиля драйвера (VSync, Latency, Smooth Motion, питание, ReBAR)
+4. **Управление** — смена папки установки, сброс переопределений, копирование диагностического отчёта
 
-### Compact View
+### Компактный вид
 
-A paged layout showing the same content as Detail View, split across three navigable pages:
-- **Page 1** — Components (game info, install buttons)
-- **Page 2** — Game Overrides
-- **Page 3** — Nvidia Profile Overrides + Management
+Постраничный макет с тем же содержимым, что и в подробном виде, разбитым на три страницы навигации:
+- **Страница 1** — Компоненты (информация об игре, кнопки установки)
+- **Страница 2** — Переопределения игры
+- **Страница 3** — Переопределения профиля NVIDIA + Управление
 
-Use the arrow buttons on the sides to cycle between pages. The window locks to a fixed compact size in Compact View and restores your previous size when you switch back.
+Переключайтесь между страницами стрелками по бокам. В компактном виде окно фиксируется на небольшом размере, а при возврате восстанавливается прежний размер.
 
-### Toolbar
+### Панель инструментов
 
-| Button | What it does |
+| Кнопка | Что делает |
 |--------|-------------|
-| Refresh | Re-scans your game library and fetches the latest mod info from all sources. |
-| Shaders/Addons | Dropdown: Global Shaders (choose shader packs) and ReShade Addons (manage addon toggles). |
-| Update All | Updates ReShade, RenoDX, ReLimiter, Display Commander, OptiScaler, and RE Framework across all eligible games. Lights up purple when updates are available. |
-| Links | Dropdown with quick links: RenoDX Wiki, Luma Wiki, RHI GitHub, ReLimiter GitHub, Display Commander GitHub. |
-| Help | Dropdown: Discord support channel, this guide, Ko-fi, and About page. |
-| Views | Toggle button: switches between Detail and Compact view. |
-| Settings | Opens the Settings page. |
+| Обновить | Пересканирует библиотеку игр и загружает свежие данные о модах из всех источников. |
+| Шейдеры/Аддоны | Выпадающее меню: Глобальные шейдеры (выбор наборов шейдеров) и Аддоны ReShade (управление переключателями аддонов). |
+| Обновить всё | Обновляет ReShade, RenoDX, ReLimiter, Display Commander, OptiScaler и RE Framework во всех подходящих играх. Загорается фиолетовым, когда есть обновления. |
+| Ссылки | Выпадающее меню с быстрыми ссылками: вики RenoDX, вики Luma, GitHub RHI, GitHub ReLimiter, GitHub Display Commander. |
+| Помощь | Выпадающее меню: канал поддержки в Discord, это руководство, Ko-fi и страница «О программе». |
+| Виды | Кнопка-переключатель: меняет подробный и компактный вид. |
+| Настройки | Открывает страницу настроек. |
 
-### Sidebar (Detail View)
+### Боковая панель (подробный вид)
 
-- **Search box** — filters games in real time. Matches across game name, store, engine, graphics API, bitness, mod name, mod author, and more. Type "UW Fix" or "Ultra+" to filter to games with those links.
-- **Filter chips** — All Games, Favourites, Installed, Unreal, Unity, Other, RenoDX, Luma, Hidden. Your selected filter is saved and restored on reopen.
-- **Custom filter chips** — click the "+" button next to the search bar to save any search query as a named chip. Custom chips use a teal colour scheme. Right-click to delete.
-- **Game/installed counts** — shows how many games are visible and how many have mods installed.
-- **Game entries** — each row shows a platform icon, game name, and a green dot when updates are available.
+- **Поле поиска** — фильтрует игры в реальном времени. Ищет по названию игры, магазину, движку, графическому API, разрядности, имени мода, автору мода и не только. Введите «UW Fix» или «Ultra+», чтобы отобрать игры с такими ссылками.
+- **Фильтры-чипы** — Все игры, Избранное, Установлено, Unreal, Unity, Прочее, RenoDX, Luma, Скрытые. Выбранный фильтр сохраняется и восстанавливается при повторном открытии.
+- **Свои фильтры-чипы** — нажмите кнопку «+» рядом с поиском, чтобы сохранить любой поисковый запрос как именной чип. Свои чипы выделены бирюзовым. Удаляются правым кликом.
+- **Счётчики игр/установок** — сколько игр видно и в скольких установлены моды.
+- **Строки игр** — в каждой строке значок платформы, название игры и зелёная точка при наличии обновлений.
 
-### Detail Panel
+### Панель подробностей
 
-When a game is selected, the detail panel shows:
+Когда игра выбрана, панель подробностей показывает:
 
-- **Header** — game name, Launch button (green ▶), Luma toggle, mod author badge(s) linking to Ko-fi where available.
-- **Info card** — action buttons (Nexus Mods, PCGW, UW Fix, Ultra+ on the left; Hide, Favourite on the right) and badges for platform, engine, wiki status, graphics API, UE-Extended/Native HDR, and bitness.
-- **Install path** — monospace text showing the resolved game directory.
-- **Components section** — install/update/uninstall buttons for each component, with per-addon Info buttons.
-- **Game Overrides section** — all per-game settings inline.
-- **Nvidia Profile Overrides section** — DLSS/Streamline row and driver settings row.
-- **Management section** — Change install folder, Reset folder, Reset Overrides, Copy Report.
+- **Заголовок** — название игры, кнопка «Запустить» (зелёный ▶), переключатель Luma, значки авторов модов со ссылками на Ko-fi, где доступны.
+- **Карточка информации** — кнопки действий (Nexus Mods, PCGW, UW Fix, Ultra+ слева; Скрыть, Избранное справа) и значки платформы, движка, статуса вики, графического API, UE-Extended/Native HDR и разрядности.
+- **Путь установки** — моноширинный текст с итоговым каталогом игры.
+- **Секция «Компоненты»** — кнопки установки/обновления/удаления для каждого компонента с кнопками «Инфо» по аддонам.
+- **Секция «Переопределения игры»** — все индивидуальные настройки в одном месте.
+- **Секция «Переопределения профиля NVIDIA»** — строка DLSS/Streamline и строка настроек драйвера.
+- **Секция «Управление»** — сменить папку установки, сбросить папку, сбросить переопределения, скопировать отчёт.
 
-### Status Bar
+### Строка состояния
 
-The bottom bar shows the game count and current operation on the left, a single-player warning in the centre, and the app version number with a Patch Notes link on the right.
+Внизу слева — число игр и текущая операция, в центре — предупреждение об одиночных играх, справа — номер версии приложения со ссылкой на Patch Notes.
 
 ---
 
-## Settings Page
+## Страница настроек
 
-Click **Settings** in the toolbar. Click **Back to Games** to return. The page is organized into 9 labelled cards.
+Нажмите **Настройки** на панели инструментов. Кнопка **Назад к играм** возвращает обратно. Страница состоит из 9 подписанных карточек.
 
-| Card | What's in it |
+| Карточка | Что внутри |
 |------|-------------|
-| Game Library | Add Game (manual detection), Check For Updates (bypass 4-hour cooldown). |
-| ReShade & Display | Left: Screenshot path + subfolder combo + hotkeys + Apply to All. Right: Peak Brightness (Auto + nits) + HDR Auto-Toggle (Off/On) + Apply to All. |
-| DLSS / Streamline Settings | Batch Deploy, Configure Defaults (versions/presets/render scales), On-Screen Indicator (Enabled/Disabled), Auto-Update DLSS (Off/On), Auto-Update Streamline (Off/On). |
-| Global NVIDIA Driver Settings | Shader Cache Size, Shader Pre-Compile, G-Sync Mode, Preferred Refresh Rate, VSync, ReBAR + Size Limit, Export/Import/Reset Profiles, Clear Shader Cache. |
-| Component Settings | Left: ReLimiter OSD hotkey + Shared Presets + DLSS Hooks + Apply. Right: OptiScaler GPU type + DLSS inputs + hotkey + Apply. |
-| Shaders & Addons | Custom Shaders toggle, Cache All Shaders toggle, Addon Watch Folder + Browse + Reset. |
-| Update & Deployment | Global Update Inclusion (per-component toggles), Mass INI Deployment (reshade.ini, relimiter.ini, DC.ini, OptiScaler.ini, Mass Preset Install). |
-| System & Maintenance | Full Refresh, Purge Cache, Admin Mode (Off/On), Drop Helper (Off/On — disables the Discord drag-drop overlay, restart required). |
-| Data & Folders | AppData Folder, Downloads Folder, Custom Folder, Logs Folder, Copy Logs. |
+| Библиотека игр | Добавить игру (ручное определение), Проверить обновления (обход 4-часовой задержки). |
+| ReShade и дисплей | Слева: путь к скриншотам + подпапка + горячие клавиши + Применить ко всем. Справа: пиковая яркость (Авто + ниты) + автопереключение HDR (Выкл/Вкл) + Применить ко всем. |
+| Настройки DLSS / Streamline | Массовое развертывание, настройка значений по умолчанию (версии/пресеты/масштабы рендеринга), экранный индикатор (Вкл/Выкл), автообновление DLSS (Выкл/Вкл), автообновление Streamline (Выкл/Вкл). |
+| Глобальные настройки драйвера NVIDIA | Размер кеша шейдеров, предкомпиляция шейдеров, режим G-Sync, предпочитаемая частота обновления, VSync, ReBAR + лимит размера, экспорт/импорт/сброс профилей, очистка кеша шейдеров. |
+| Настройки компонентов | Слева: горячая клавиша OSD ReLimiter + общие пресеты + DLSS Hooks + Применить. Справа: тип GPU OptiScaler + входы DLSS + горячая клавиша + Применить. |
+| Шейдеры и аддоны | Переключатель своих шейдеров, переключатель кеширования всех шейдеров, папка наблюдения аддонов + Обзор + Сброс. |
+| Обновление и развертывание | Глобальное включение в обновления (переключатели по компонентам), массовое развертывание INI (reshade.ini, relimiter.ini, DC.ini, OptiScaler.ini, массовая установка пресетов). |
+| Система и обслуживание | Полное обновление, очистка кеша, режим администратора (Выкл/Вкл), Drop Helper (Выкл/Вкл — отключает оверлей drag-and-drop из Discord, нужен перезапуск). |
+| Данные и папки | Папка AppData, папка загрузок, папка Custom, папка логов, скопировать логи. |
 
 ---
 
-## Game Detection
+## Определение игр
 
-RHI scans all supported stores on every launch and merges newly installed games into its cached library. Games on a disconnected drive are preserved in cache until reconnected. Per-store detection failures are isolated — one store failing won't block others.
+При каждом запуске RHI сканирует все поддерживаемые магазины и вливает вновь установленные игры в кешированную библиотеку. Игры на отключённом диске сохраняются в кеше до повторного подключения. Сбои определения по каждому магазину изолированы — отказ одного не блокирует остальные.
 
-### Supported Stores
+### Поддерживаемые магазины
 
-| Store | How RHI finds games |
+| Магазин | Как RHI находит игры |
 |-------|-------------------|
-| Steam | Reads `libraryfolders.vdf` and `appmanifest_*.acf` files across all library folders. |
-| GOG | Registry keys under `HKLM\SOFTWARE\GOG.com\Games`. |
-| Epic Games | Manifest `.item` files in `ProgramData\Epic\EpicGamesLauncher\Data\Manifests`. |
-| EA App | `installerdata.xml` manifests, registry keys, default EA Games folders, EA Desktop config. |
-| Ubisoft Connect | Registry keys under `HKLM\SOFTWARE\Ubisoft\Launcher\Installs`, `settings.yml`, default folder. |
-| Xbox / Game Pass | Windows `PackageManager` API with `MicrosoftGame.config` detection. Falls back to `.GamingRoot`, registry, and folder scanning. |
-| Battle.net | Uninstall registry entries, `Battle.net.config`, default folder scanning. |
-| Rockstar Games | Uninstall registry entries, launcher `titles.dat`, default folder scanning. |
-
-### Engine Detection
-
-| Engine | How it's detected |
+| Steam | Читает `libraryfolders.vdf` и файлы `appmanifest_*.acf` во всех папках библиотеки. |
+| GOG | Ключи реестра в `HKLM\SOFTWARE\GOG.com\Games`. |
+| Epic Games | Манифесты `.item` в `ProgramData\Epic\EpicGamesLauncher\Data\Manifests`. |
+| EA App | Манифесты `installerdata.xml`, ключи реестра, папки EA Games по умолчанию, конфиг EA Desktop. |
+| Ubisoft Connect | Ключи реестра в `HKLM\SOFTWARE\Ubisoft\Launcher\Installs`, `settings.yml`, папка по умолчанию. |
+| Xbox / Game Pass | Windows API `PackageManager` с определением `MicrosoftGame.config`. Запасные варианты: `.GamingRoot`, реестр, сканирование папок. |
+| Battle.net | Записи удаления в реестре, `Battle.net.config`, сканирование папок по умолчанию. |
+| Rockstar Games | Записи удаления в реестре, `titles.dat` лаунчера, сканирование папок по умолчанию. |
+
+### Определение движка
+
+| Движок | Как определяется |
 |--------|------------------|
-| Unreal Engine | Unreal-specific files and folder structures. Version detected from CrashReportClient or Build.version when available. |
-| Unreal (Legacy) | Unreal Engine 3 games identified by legacy folder layouts. |
+| Unreal Engine | Специфичные для Unreal файлы и структура папок. Версия берётся из CrashReportClient или Build.version, если доступны. |
+| Unreal (Legacy) | Игры на Unreal Engine 3 определяются по устаревшей структуре папок. |
 | Unity | `UnityPlayer.dll`, `Mono`, `MonoBleedingEdge`, `il2cpp`, `GameAssembly.dll`. |
-| RE Engine | `re_chunk_000.pak` in the game directory. |
-| Custom | Engine names from the remote manifest (e.g. "Silk Engine", "Frostbite"). |
+| RE Engine | `re_chunk_000.pak` в каталоге игры. |
+| Свои | Названия движков из удалённого манифеста (например, «Silk Engine», «Frostbite»). |
 
-### Adding Games Manually
+### Добавление игр вручную
 
-- **Add Game** (Settings page) — click the button, pick the game's exe, then name it.
-- **Drag and drop** — drag a game's `.exe` onto the RHI window. Engine and game root are auto-detected.
+- **Добавить игру** (страница настроек) — нажмите кнопку, выберите exe игры и задайте название.
+- **Drag-and-drop** — перетащите `.exe` игры на окно RHI. Движок и корень игры определяются автоматически.
 
-### Multi-Game Split
+### Разделение нескольких игр
 
-Games containing multiple titles in one folder (e.g. Mass Effect Legendary Edition) are split into separate entries via the remote manifest. Each sub-game gets independent mod management.
+Игры, содержащие несколько тайтлов в одной папке (например, Mass Effect Legendary Edition), через удалённый манифест разбиваются на отдельные записи. Каждая суб-игра получает независимое управление модами.
 
 ---
 
-## Graphics API Detection
+## Определение графического API
 
-RHI scans game executables using PE header import table analysis. Results are cached to disk.
+RHI сканирует исполняемые файлы игр, анализируя таблицу импорта PE-заголовка. Результаты кешируются на диск.
 
-| API | Badge | What RHI looks for |
+| API | Значок | Что ищет RHI |
 |-----|-------|--------------------|
-| DirectX 8 | DX8 | `d3d8.dll` import |
-| DirectX 9 | DX9 | `d3d9.dll` import |
-| DirectX 10 | DX10 | `d3d10.dll` / `d3d10_1.dll` import |
-| DirectX 11/12 | DX11/12 | `d3d11.dll` / `d3d12.dll` import |
-| Vulkan | VLK | `vulkan-1.dll` import |
-| OpenGL | OGL | `opengl32.dll` import |
-
-The detected API drives automatic ReShade DLL naming:
+| DirectX 8 | DX8 | импорт `d3d8.dll` |
+| DirectX 9 | DX9 | импорт `d3d9.dll` |
+| DirectX 10 | DX10 | импорт `d3d10.dll` / `d3d10_1.dll` |
+| DirectX 11/12 | DX11/12 | импорт `d3d11.dll` / `d3d12.dll` |
+| Vulkan | VLK | импорт `vulkan-1.dll` |
+| OpenGL | OGL | импорт `opengl32.dll` |
+
+Определённый API задаёт автоматическое имя DLL ReShade:
 - DX9 → `d3d9.dll`
 - OpenGL → `opengl32.dll`
-- Default → `dxgi.dll`
+- По умолчанию → `dxgi.dll`
 
-Dual-API games show both APIs (e.g. `DX11/12 / VLK`). Per-game API overrides and manifest overrides are available for games where PE detection fails.
+Игры с двумя API показывают оба (например, `DX11/12 / VLK`). Для игр, где PE-анализ не срабатывает, доступны переопределения API по игре и переопределения из манифеста.
 
 ---
 
-## Components
+## Компоненты
 
-The detail panel Components section shows rows for each managed mod:
+Секция «Компоненты» панели подробностей показывает строки для каждого управляемого мода:
 
-| Component | Description |
+| Компонент | Описание |
 |-----------|-------------|
-| RE Framework | Required for RE Engine games. Shown only for those games. |
-| ReShade | Core injection framework. Install/Reinstall/Update, Copy INI, Uninstall. |
-| RenoDX | HDR mod addon. Install/Update, Info, Uninstall. |
-| Luma | Luma Framework. Shown only in Luma mode. |
-| ReLimiter | Frame pacing addon. Install, Info, Copy INI, Uninstall. |
-| Display Commander | Alternative frame limiter. Install, Info, Copy INI, Uninstall. |
-| OptiScaler | Upscaler redirection. Install, Info, Copy INI, Uninstall. |
-| DXVK | DirectX-to-Vulkan. Managed via the DXVK dropdown in Overrides. |
+| RE Framework | Нужен для игр на RE Engine. Показывается только для таких игр. |
+| ReShade | Основной фреймворк внедрения. Установка/переустановка/обновление, копировать INI, удалить. |
+| RenoDX | HDR-аддон. Установка/обновление, Инфо, удаление. |
+| Luma | Luma Framework. Показывается только в режиме Luma. |
+| ReLimiter | Аддон вывода кадров. Установка, Инфо, копировать INI, удаление. |
+| Display Commander | Альтернативный ограничитель FPS. Установка, Инфо, копировать INI, удаление. |
+| OptiScaler | Перенаправление апскейлеров. Установка, Инфо, копировать INI, удаление. |
+| DXVK | DirectX-в-Vulkan. Управляется выпадающим списком DXVK в переопределениях. |
 
-### Per-Addon Info Buttons
+### Кнопки «Инфо» по аддонам
 
-Every component has an **Info** button showing game-specific context:
+У каждого компонента есть кнопка **Инфо** с игровым контекстом:
 
-1. **Manifest notes** — game-specific notes from the remote manifest
-2. **Wiki content** — compatibility data from the relevant wiki
-3. **Generic description** — what the addon does in general
+1. **Заметки из манифеста** — игровые заметки из удалённого манифеста
+2. **Содержимое вики** — данные о совместимости из соответствующей вики
+3. **Общее описание** — что аддон делает в целом
 
-Info buttons are highlighted **blue** when they have content. ReLimiter and Display Commander also show changelogs.
+Кнопки «Инфо» подсвечиваются **синим**, когда для них есть содержимое. У ReLimiter и Display Commander также показываются списки изменений.
 
-### Version Display
+### Отображение версии
 
-Each component shows its installed version number. Purple text indicates an available update.
+Каждый компонент показывает номер установленной версии. Фиолетовый текст означает доступное обновление.
 
-### Dependency Enforcement
+### Контроль зависимостей
 
-- **ReShade required** — RenoDX, ReLimiter, Display Commander require ReShade first
-- **RE Framework required** — RE Engine games need RE Framework before ReShade
+- **Сначала ReShade** — RenoDX, ReLimiter и Display Commander требуют сначала установить ReShade
+- **Сначала RE Framework** — играм на RE Engine нужен RE Framework до ReShade
 
 ---
 
 ## ReShade
 
-[ReShade](https://reshade.me) is the core injection framework. RHI downloads the latest build on startup and stages it locally.
+[ReShade](https://reshade.me) — основной фреймворк внедрения. RHI скачивает свежую сборку при запуске и складывает её локально.
 
-### Build Channels
+### Каналы сборок
 
-The RS Channel dropdown in per-game overrides offers:
+Выпадающий список «Канал RS» в переопределениях игры предлагает:
 
-| Channel | Description |
+| Канал | Описание |
 |---------|-------------|
-| Global | Uses the global default (Stable or Nightly from Settings). |
-| Stable | Official reshade.me releases. |
-| Nightly | GitHub Actions nightly builds. |
-| Custom | Your own ReShade DLLs from `%LocalAppData%\RHI\Custom\ReShade\`. |
-| Legacy | Pin to a specific older version (6.0.0+). Opens a version picker. |
-| No Addons | Standard ReShade without addon support. Disables all addon rows. |
+| Глобальный | Использует глобальное значение по умолчанию (Stable или Nightly из настроек). |
+| Stable | Официальные релизы с reshade.me. |
+| Nightly | Ночные сборки GitHub Actions. |
+| Custom | Ваши собственные DLL ReShade из `%LocalAppData%\RHI\Custom\ReShade\`. |
+| Legacy | Фиксация на конкретной старой версии (6.0.0+). Открывает выбор версии. |
+| No Addons | Стандартный ReShade без поддержки аддонов. Отключает все строки аддонов. |
 
-Per-game channel overrides let you mix channels across your library. Legacy and Custom exclude games from ReShade update checks automatically.
+Переопределения канала по игре позволяют смешивать каналы по всей библиотеке. Каналы Legacy и Custom автоматически исключают игры из проверок обновлений ReShade.
 
-### Custom ReShade Auto-Redeploy
+### Автоперенос своего ReShade
 
-When you update a DLL in `%LocalAppData%\RHI\Custom\ReShade\`, RHI detects the change and automatically redeploys it to all games using that specific DLL.
+Когда вы обновляете DLL в `%LocalAppData%\RHI\Custom\ReShade\`, RHI замечает изменение и автоматически переносит её во все игры, использующие именно эту DLL.
 
-- Checked on every **Refresh** and every **4 hours** (background timer)
-- Hash file (`custom_reshade_hashes.json`) stored alongside the DLLs in the Custom folder
-- DX games: file copied directly to the game folder
-- Vulkan games: copied to the global layer (`C:\ProgramData\ReShade\`) via elevation
+- Проверка при каждом нажатии **Обновить** и каждые **4 часа** (фоновый таймер)
+- Хеш-файл (`custom_reshade_hashes.json`) хранится рядом с DLL в папке Custom
+- DX-игры: файл копируется прямо в папку игры
+- Vulkan-игры: копируется в глобальный слой (`C:\ProgramData\ReShade\`) через повышение прав
 
-### Vulkan Games
+### Vulkan-игры
 
-Vulkan games use a global implicit layer instead of a per-game DLL. See [Vulkan ReShade Support](#vulkan-reshade-support).
+Vulkan-игры используют глобальный неявный слой вместо DLL на игру. См. [Поддержка Vulkan в ReShade](#поддержка-vulkan-в-reshade).
 
-### Copy INI
+### Копировать INI
 
-Copies `reshade.ini` from the template folder to the game directory, preserving existing game-specific settings.
+Копирует `reshade.ini` из папки шаблонов в каталог игры, сохраняя существующие настройки конкретной игры.
 
-### Keep ReShade.ini Updated
+### Поддержание reshade.ini в актуальном виде
 
-A per-game toggle in the ReShade ⚙ cog. Set to **No** to prevent RHI from touching the game's `reshade.ini` automatically. When off, ReShade installs, updates, Apply to All Games (hotkeys, peak nits, effect list style), and the "Keep ReShade.ini Updated" resolver in Update All will all skip this game. Use this when you've made manual edits to a reshade.ini that you don't want overwritten.
+Переключатель в шестерёнке ⚙ ReShade. Поставьте **Нет**, чтобы RHI не трогал `reshade.ini` игры автоматически. Когда выключено, установка и обновление ReShade, «Применить ко всем играм» (горячие клавиши, пиковые ниты, стиль списка эффектов) и решатель «Поддерживать reshade.ini» в «Обновить всё» пропускают эту игру. Используйте, если вы вручную редактировали reshade.ini и не хотите, чтобы его перезаписали.
 
-### Foreign DLL Detection
+### Определение чужих DLL
 
-Before installing, RHI checks if an existing file belongs to another tool (DXVK, Special K, ENB) via binary signature scanning. A confirmation dialog appears before overwriting.
+Перед установкой RHI проверяет по бинарным сигнатурам, не принадлежит ли существующий файл другому инструменту (DXVK, Special K, ENB). Перед перезаписью показывается диалог подтверждения.
 
 ---
 
 ## RenoDX
 
-[RenoDX](https://github.com/clshortfuse/renodx) is an HDR mod framework running as a ReShade addon.
+[RenoDX](https://github.com/clshortfuse/renodx) — фреймворк HDR-модов, работающий как аддон ReShade.
 
-### How Mods Are Matched
+### Как подбираются моды
 
-RHI fetches the RenoDX wiki on startup and matches detected games by name. Games without a named mod fall back to generic engine addons (Unreal Engine, Unity, UE-Extended). The manifest can override name matching.
+RHI загружает вики RenoDX при запуске и сопоставляет найденные игры по имени. Игры без именного мода получают запасной универсальный аддон движка (Unreal Engine, Unity, UE-Extended). Манифест может переопределять сопоставление по имени.
 
-### UE-Extended Toggle
+### Переключатель UE-Extended
 
-Unreal Engine games show a toggle to switch between the game's specific named mod and the generic UE-Extended addon. Toggling uninstalls the current addon and switches mode — the user clicks Install to get the new one.
+Играм на Unreal Engine показывается переключатель между именным модом игры и универсальным аддоном UE-Extended. Переключение удаляет текущий аддон и меняет режим — пользователь нажимает «Установить», чтобы получить новый.
 
-### External-Only Games
+### Игры с внешней установкой
 
-Some games are redirected to Nexus Mods or Discord via the manifest. The install button opens the external link.
+Некоторые игры через манифест перенаправляются на Nexus Mods или Discord. Кнопка установки открывает внешнюю ссылку.
 
 ### HDR Gaming Database
 
-The RenoDX Info button links to [hdrmods.com](https://www.hdrmods.com) entries where available.
+Кнопка «Инфо» RenoDX ведёт на записи [hdrmods.com](https://www.hdrmods.com), где они доступны.
 
 ---
 
 ## RTX HDR
 
-NVIDIA's driver-level HDR injection, available for any game via the RenoDX cog dialog.
+HDR-внедрение на уровне драйвера NVIDIA, доступно для любой игры через диалог шестерёнки RenoDX.
 
-### Requirements
+### Требования
 
-- NVIDIA App installed (not legacy GeForce Experience)
-- NVIDIA Overlay enabled in NVIDIA App settings
-- Game Filters and Photo Mode enabled in NVIDIA App settings
-- RTX series GPU
-- Driver 550+
+- Установленный NVIDIA App (не старый GeForce Experience)
+- Включённый NVIDIA Overlay в настройках NVIDIA App
+- Включённые Game Filters и Photo Mode в настройках NVIDIA App
+- GPU серии RTX
+- Драйвер 550+
 
-### Usage
+### Использование
 
-1. Open the RenoDX cog (⚙) for any game
-2. Toggle "Enable RTX HDR" to On at the bottom of the dialog
-3. Click "Configure RTX HDR" to adjust settings
+1. Откройте шестерёнку (⚙) RenoDX у любой игры
+2. Включите «Включить RTX HDR» внизу диалога
+3. Нажмите «Настроить RTX HDR», чтобы изменить параметры
 
-### Settings
+### Параметры
 
-| Setting | Range | Default | Notes |
+| Параметр | Диапазон | По умолчанию | Примечания |
 |---------|-------|---------|-------|
-| Peak Brightness | 400–2000 nits | Your global peak nits | Pre-populated from RHI Settings |
-| Contrast | -100 to +100 | 0 (Gamma 2.0) | +25 = Gamma 2.2, +50 = Gamma 2.4 |
-| Saturation | -100 to +100 | 0 | -25 = Neutral Saturation |
-| Middle Grey | 10–100 | 50 | Dropdown in steps of 5 |
-| Debanding | Off/Low/High | No Debanding | Indicator and Debug variants available |
+| Пиковая яркость | 400–2000 нит | Ваш глобальный пик | Подставляется из настроек RHI |
+| Контраст | от -100 до +100 | 0 (гамма 2.0) | +25 = гамма 2.2, +50 = гамма 2.4 |
+| Насыщенность | от -100 до +100 | 0 | -25 = нейтральная насыщенность |
+| Средний серый | 10–100 | 50 | Список с шагом 5 |
+| Устранение бандинга | Выкл/Слабое/Сильное | Без устранения | Доступны варианты Indicator и Debug |
 
-### Behaviour
+### Поведение
 
-- Enabling RTX HDR **uninstalls** any active RenoDX mod (mutually exclusive — both inject HDR)
-- Disabling RTX HDR does NOT reinstall RenoDX
-- Settings are written to the game's NVIDIA driver profile
-- When no RenoDX mod is available, the cog dialog only shows the RTX HDR toggle (nits/presets hidden)
-- The install button changes to "Configure RTX HDR" when active
+- Включение RTX HDR **удаляет** любой активный мод RenoDX (взаимоисключаемы — оба внедряют HDR)
+- Выключение RTX HDR НЕ переустанавливает RenoDX
+- Параметры записываются в профиль драйвера NVIDIA для игры
+- Когда мод RenoDX недоступен, в диалоге шестерёнки показывается только переключатель RTX HDR (ниты/пресеты скрыты)
+- При активности кнопка установки меняется на «Настроить RTX HDR»
 
 ---
 
 ## RE Framework
 
-[RE Framework](https://github.com/praydog/REFramework-nightly) is required for ReShade injection on RE Engine games.
+[RE Framework](https://github.com/praydog/REFramework-nightly) нужен для внедрения ReShade в играх на RE Engine.
 
-One-click install with game-specific builds. Version tracking and auto-update included. When OptiScaler is active on supported RE Engine games, the pd-upscaler branch is automatically used instead.
+Установка в один клик со сборками под конкретные игры. Отслеживание версий и автообновление включены. Когда в поддерживаемых играх RE Engine активен OptiScaler, вместо него автоматически используется ветка pd-upscaler.
 
 ---
 
 ## Luma Framework
 
-[Luma Framework](https://github.com/Filoppi/Luma-Framework) is a DX11 HDR modding framework.
+[Luma Framework](https://github.com/Filoppi/Luma-Framework) — фреймворк HDR-моддинга для DX11.
 
-### Named Mods
+### Именные моды
 
-Game-specific Luma mods for supported titles, shown on the Luma row. Check the Info button for details on what each mod adds.
+Игровые моды Luma для поддерживаемых тайтлов — показываются на строке Luma. Подробности о том, что добавляет каждый мод, смотрите в кнопке «Инфо».
 
-### Generic Luma for Unreal Engine Games
+### Универсальный Luma для игр на Unreal Engine
 
-Every DX11 Unreal Engine game in your library shows a Luma row. RHI applies any game-specific Engine.ini tweaks or launch arguments listed on the Luma wiki automatically on install.
+Каждая игра на DX11 Unreal Engine в библиотеке показывает строку Luma. RHI автоматически применяет при установке любые перечисленные в вики Luma игровые правки Engine.ini или аргументы запуска.
 
-- Games with both DX11 and DX12 APIs show the Luma row and get `-dx11` set automatically on install and removed on uninstall.
-- A TAA settings toggle in the Luma ⚙ cog applies wiki-recommended TAA Engine.ini keys where available.
-- UE5 games use DX12 by default and don't show a Luma row. Set the Graphics API override to DirectX11 in Game Overrides first if needed.
+- Игры с API и DX11, и DX12 показывают строку Luma и получают `-dx11` автоматически при установке; при удалении флаг снимается.
+- Переключатель настроек TAA в шестерёнке ⚙ Luma применяет рекомендованные вики ключи TAA для Engine.ini, где доступны.
+- Игры на UE5 по умолчанию используют DX12 и не показывают строку Luma. При необходимости сначала задайте переопределение графического API на DirectX11 в переопределениях игры.
 
-### Luma + RenoDX Coexistence
+### Сосуществование Luma + RenoDX
 
-Games in the manifest `lumaRenodxCompat` list can run both simultaneously. A one-time compatibility warning appears the first time you install both on a game.
+Игры из списка `lumaRenodxCompat` в манифесте могут запускать оба фреймворка одновременно. Однократное предупреждение о совместимости появляется при первой совместной установке на игру.
 
-### ReShade and DLSS
+### ReShade и DLSS
 
-RHI manages both ReShade and DLSS on all Luma games. Uninstalling Luma does not remove ReShade or DLSS — all three are managed independently.
+RHI управляет и ReShade, и DLSS во всех играх Luma. Удаление Luma не удаляет ReShade или DLSS — все три компонента управляются независимо.
 
-### Luma ⚙ Cog
+### Шестерёнка ⚙ Luma
 
-- **TAA Settings** — On/Off toggle. Applies or removes standard TAA Engine.ini keys.
-- **Luma reshade.ini settings** — `EnableHDR` toggle for games that expose it.
+- **Настройки TAA** — переключатель Вкл/Выкл. Применяет или удаляет стандартные ключи TAA в Engine.ini.
+- **Настройки reshade.ini Luma** — переключатель `EnableHDR` для игр, которые его предоставляют.
 
-### Drag-and-Drop Install
+### Установка drag-and-drop
 
-Drag a Luma mod archive (zip/7z) onto the window. RHI detects it by filename (contains "Luma", not "addon") or by the `d3dcompiler_47.dll` marker inside. A game picker opens, and the mod is extracted with shader deployment and reshade.ini configuration.
+Перетащите архив мода Luma (zip/7z) на окно. RHI распознаёт его по имени файла (содержит «Luma», но не «addon») или по маркеру `d3dcompiler_47.dll` внутри. Открывается выбор игры, мод извлекается с развертыванием шейдеров и настройкой reshade.ini.
 
-### Luma Updates
+### Обновления Luma
 
-Luma mods are checked for updates and included in Update All.
+Моды Luma проверяются на обновления и участвуют в «Обновить всё».
 
 ---
 
-## UE-Extended Auto-Configuration
+## Автонастройка UE-Extended
 
-When installing UE-Extended (generic Unreal Engine RenoDX), two files are configured automatically:
+При установке UE-Extended (универсальный RenoDX для Unreal Engine) два файла настраиваются автоматически:
 
 ### reshade.ini
 
-A `[renodx]` section with `Set_Path=0` and all `Upgrade_*=0` keys. Tells RenoDX to upgrade the game's native HDR path. Only adds missing keys — never overwrites existing values. Removed on UE-Extended uninstall.
+Секция `[renodx]` с `Set_Path=0` и всеми ключами `Upgrade_*=0`. Она указывает RenoDX обновлять нативный HDR-путь игры. Добавляются только недостающие ключи — существующие значения никогда не перезаписываются. Удаляется при удалении UE-Extended.
 
 ### Engine.ini
 
-HDR settings (`r.AllowHDR=1`, `r.HDR.EnableHDROutput=1`, etc.) deployed to the game's config folder (`%LocalAppData%\{ProjectName}\Saved\Config\{Platform}\`). File set read-only to prevent the engine from overwriting. The Config button in the detail panel opens the resolved folder.
+HDR-настройки (`r.AllowHDR=1`, `r.HDR.EnableHDROutput=1` и т.д.) разворачиваются в папку конфигурации игры (`%LocalAppData%\{ProjectName}\Saved\Config\{Platform}\`). Набору файлов ставится атрибут «только чтение», чтобы движок их не перезаписал. Кнопка «Конфиг» в панели подробностей открывает итоговую папку.
 
 ---
 
-## Ryubing Emulator Support
+## Поддержка эмулятора Ryubing
 
-Drag `Ryujinx.exe` into RHI to add Ryubing. It appears as a single card managing Switch game addons.
+Перетащите `Ryujinx.exe` в RHI, чтобы добавить Ryubing. Он появится одной карточкой, управляющей аддонами Switch-игр.
 
-### Setup
+### Настройка
 
-1. Place RenoVK DLL in `%LocalAppData%\RHI\Custom\ReShade\` as `ReShade64.dll`
-2. Set RS Channel to Custom in Overrides
-3. Install ReShade (Vulkan layer — requires admin)
-4. Click Install RenoDX — downloads all 9 Souperman9 addons
+1. Положите DLL RenoVK в `%LocalAppData%\RHI\Custom\ReShade\` под именем `ReShade64.dll`
+2. Задайте канал RS = Custom в переопределениях
+3. Установите ReShade (Vulkan-слой — нужен администратор)
+4. Нажмите «Установить RenoDX» — скачаются все 9 аддонов Souperman9
 
-All addons are deployed simultaneously. Each reads the emulator log to detect which game is running. No swapping needed between games.
+Все аддоны разворачиваются одновременно. Каждый читает лог эмулятора, чтобы определить запущенную игру. Переключаться между играми не нужно.
 
 ---
 
-## Frame Rate Limiters
+## Ограничители частоты кадров
 
-Two mutually exclusive frame limiters. Only one can be installed per game.
+Два взаимоисключаемых ограничителя FPS. На игру может быть установлен только один.
 
 ### ReLimiter
 
-Frame pacing addon with configurable OSD hotkey and shared presets. 64-bit only (v3.0.0+). Default `relimiter.ini` is seeded on first install.
+Аддон вывода кадров с настраиваемой горячей клавишей OSD и общими пресетами. Только 64-бит (v3.0.0+). Стандартный `relimiter.ini` создаётся при первой установке.
 
 ### Display Commander
 
-Alternative limiter supporting both 32-bit and 64-bit. DLL naming overrides toggle controls both ReShade and DC filenames together.
+Альтернативный ограничитель, поддерживающий и 32-бит, и 64-бит. Переключатель имён DLL меняет сразу и имена файлов ReShade, и DC.
 
 ---
 
-## DLSS & Streamline Manager
+## Менеджер DLSS и Streamline
 
-For games with DLSS or Streamline DLLs detected, the DLSS/Streamline section appears in Nvidia Profile Overrides.
+Для игр, где найдены DLL DLSS или Streamline, секция DLSS/Streamline появляется в переопределениях профиля NVIDIA.
 
-### Version Swapping
+### Подмена версий
 
-| Component | DLL | Notes |
+| Компонент | DLL | Примечания |
 |-----------|-----|-------|
-| DLSS SR | `nvngx_dlss.dll` | Choose any version independently |
-| DLSS RR | `nvngx_dlssd.dll` | Choose any version independently |
-| DLSS FG | `nvngx_dlssg.dll` | Choose any version independently |
-| Streamline | `sl.*.dll` | All files updated as a set |
+| DLSS SR | `nvngx_dlss.dll` | Любая версия независимо |
+| DLSS RR | `nvngx_dlssd.dll` | Любая версия независимо |
+| DLSS FG | `nvngx_dlssg.dll` | Любая версия независимо |
+| Streamline | `sl.*.dll` | Все файлы обновляются комплектом |
 
-Versions are downloaded on-demand and cached at `%LocalAppData%\RHI\DLSS\{version}\`, `DLSS-D\{version}\`, `DLSS-G\{version}\`, `Streamline\{version}\`.
+Версии скачиваются по требованию и кешируются в `%LocalAppData%\RHI\DLSS\{version}\`, `DLSS-D\{version}\`, `DLSS-G\{version}\`, `Streamline\{version}\`.
 
-### Backups & Restore
+### Резервные копии и восстановление
 
-Original DLLs are backed up with `.original` extension. Select the item marked with `(D)` in the dropdown to restore. "Restore DLSS/SL" button reverts all components and resets presets.
+Исходные DLL сохраняются с расширением `.original`. Для восстановления выберите в списке элемент с пометкой `(D)`. Кнопка «Восстановить DLSS/SL» откатывает все компоненты и сбрасывает пресеты.
 
-### DLSS Presets
+### Пресеты DLSS
 
-Per-game presets written directly to NVIDIA driver profiles:
+Пресеты для каждой игры, записываемые прямо в профили драйвера NVIDIA:
 
-| Component | Presets |
+| Компонент | Пресеты |
 |-----------|---------|
 | SR | Default, J, K, L, M |
 | RR | Default, D, E |
 | FG | Default, A, B |
 
-No NVIDIA Profile Inspector needed. Changes apply instantly. Silently no-ops on AMD/Intel.
+NVIDIA Profile Inspector не нужен. Изменения применяются мгновенно. На AMD/Intel тихо ничего не делают.
 
-### DLSS Render Scale Override
+### Переопределение масштаба рендеринга DLSS
 
-Force a custom render resolution per game (33–100%) for both SR and Ray Reconstruction. Options range from 100% DLAA down to 33% Ultra Performance.
+Принудительное разрешение рендеринга для игры (33–100%) для SR и Ray Reconstruction. Варианты — от 100% DLAA до 33% Ultra Performance.
 
-### Custom Files
+### Свои файлы
 
-Place custom DLLs in `%LocalAppData%\RHI\Custom\DLSS\` and `Custom\Streamline\`. Select "Custom" from the dropdown.
+Положите свои DLL в `%LocalAppData%\RHI\Custom\DLSS\` и `Custom\Streamline\` и выберите «Custom» в списке.
 
-### Driver Override Detection
+### Определение переопределений драйвера
 
-When NVIDIA App has "Latest DLL" or "Use recommended preset" active, RHI detects it and greys out the affected dropdown with a "Driver Override Active" warning.
+Когда в NVIDIA App активны «Latest DLL» или «Use recommended preset», RHI это замечает и грейдаутит соответствующий список с предупреждением «Активно переопределение драйвера».
 
-### Batch Deploy
+### Массовое развертывание
 
-From Settings: select games via checklist, pick versions for each component, select presets, and deploy to all selected games at once. Auto-creates NVIDIA profiles for games without one. Games with v1.x DLLs are shown disabled.
+Из настроек: отметьте игры в чек-листе, выберите версии для каждого компонента, укажите пресеты и разверните всё сразу в выбранных играх. Профили NVIDIA для игр без профиля создаются автоматически. Игры с DLL v1.x показываются отключёнными.
 
 ---
 
-## DLSS & Streamline Defaults
+## DLSS и Streamline по умолчанию
 
-Configure preferred default versions, presets, and render scales from the Settings page. The "Configure Defaults" button opens a 4-column dialog (SR | RR | FG | SL) with:
+На странице настроек задаются предпочитаемые версии, пресеты и масштабы рендеринга по умолчанию. Кнопка «Настроить значения по умолчанию» открывает диалог из 4 колонок (SR | RR | FG | SL) с:
 
-- **Version** dropdown per component
-- **Preset** dropdown (SR/RR/FG)
-- **Render Scale** dropdown (SR/RR only)
+- **Версия** — список для каждого компонента
+- **Пресет** — список (SR/RR/FG)
+- **Масштаб рендеринга** — список (только SR/RR)
 
-"Default (don't change)" as the first option means that component is skipped during Quick Apply.
+Вариант «По умолчанию (не менять)» первым в списке означает, что компонент пропускается при «Быстром применении».
 
-### Quick Apply
+### Быстрое применение
 
-A button in the DLSS/Streamline section of the per-game Nvidia Profile Overrides panel. Applies all non-default settings from your configured defaults to the selected game in one click. Downloads versions on-demand. Skips components the game doesn't have and components with driver overrides active.
+Кнопка в секции DLSS/Streamline панели переопределений NVIDIA у каждой игры. Одним кликом применяет все нестандартные настройки из ваших значений по умолчанию к выбранной игре. Версии скачиваются по требованию. Пропускаются компоненты, которых у игры нет, и компоненты с активным переопределением драйвера.
 
 ---
 
-## Nvidia Profile Overrides
+## Переопределения профиля NVIDIA
 
-The Nvidia Profile Overrides section appears below Game Overrides in the detail panel. It has two rows:
+Секция переопределений профиля NVIDIA появляется ниже переопределений игры в панели подробностей. В ней две строки:
 
-### DLSS / Streamline Row
+### Строка DLSS / Streamline
 
-Horizontal columns for SR, RR, FG, and SL. Each column shows:
-- Version dropdown (managed versions + Custom + Default)
-- Preset dropdown
-- Render Scale dropdown (SR/RR only)
+Горизонтальные колонки SR, RR, FG и SL. В каждой колонке:
+- список версий (управляемые версии + Custom + Default)
+- список пресетов
+- список масштаба рендеринга (только SR/RR)
 
-Plus Quick Apply button and Restore DLSS/SL button.
+Плюс кнопка «Быстрое применение» и кнопка «Восстановить DLSS/SL».
 
-Only visible when the game has DLSS or Streamline files detected.
+Видна только когда у игры найдены файлы DLSS или Streamline.
 
-### Driver Settings Row
+### Строка настроек драйвера
 
-Per-game NVIDIA driver profile settings in 4 columns:
+Настройки профиля драйвера NVIDIA у каждой игры в 4 колонках:
 
-| Column | Settings |
+| Колонка | Настройки |
 |--------|----------|
-| VSync | VSync Mode, VSync Tear Control, Low Latency (Off/On/Ultra) |
-| Smooth Motion | Enable (Off/On), Allowed APIs, Flip Pacing FS, Flip Pacing Win |
-| Power | Power Mode, Restore Defaults button |
-| ReBAR | Enable (Off/On/Global), Mode (Standard/Optimized), Size Limit |
+| VSync | Режим VSync, управление разрывами VSync, Low Latency (Выкл/Вкл/Ultra) |
+| Smooth Motion | Включение (Выкл/Вкл), разрешённые API, Flip Pacing FS, Flip Pacing Win |
+| Питание | Режим питания, кнопка «Восстановить по умолчанию» |
+| ReBAR | Включение (Выкл/Вкл/Глобально), режим (Standard/Optimized), лимит размера |
 
-All settings are written per-game to the NVIDIA driver profile. **Requires admin privileges** — the entire row is greyed out when not running as admin. Enable Admin Mode in Settings for persistent elevation.
+Все настройки записываются в профиль драйвера NVIDIA индивидуально для игры. **Требуются права администратора** — вся строка серая, если RHI не запущен от администратора. Включите режим администратора в настройках для постоянного повышения прав.
 
 ---
 
-## Multi Frame Generation
+## Мульти-генерация кадров
 
-A "Multi Frame Gen" button in the FG column opens a per-game dialog for RTX 50 Series GPUs:
+Кнопка «Мульти-генерация кадров» в колонке FG открывает диалог для каждой игры на GPU RTX 50-й серии:
 
-| Setting | Options |
+| Параметр | Варианты |
 |---------|---------|
-| FG Mode | Default / Fixed / Dynamic |
-| Frame Count | Fixed: 2x–6x. Dynamic: Up to 2x–6x |
-| Target Frame Rate | Off / Max Refresh Rate / 60–500 FPS (Dynamic only) |
+| Режим FG | Default / Fixed / Dynamic |
+| Количество кадров | Fixed: 2x–6x. Dynamic: до 2x–6x |
+| Целевая частота кадров | Выкл / максимальная частота монитора / 60–500 FPS (только Dynamic) |
 
-A first-time warning explains the RTX 50 Series requirement (driver 572.16+ for Fixed, 595.97+ for Dynamic). Restore Defaults clears MFG settings.
+При первом открытии появляется предупреждение о требованиях RTX 50-й серии (драйвер 572.16+ для Fixed, 595.97+ для Dynamic). «Восстановить по умолчанию» сбрасывает настройки MFG.
 
 ---
 
-## Global Nvidia Settings
+## Глобальные настройки NVIDIA
 
-Located in the Settings page, these write to the global/base NVIDIA driver profile (affects all games):
+Расположены на странице настроек и записываются в глобальный/базовый профиль драйвера NVIDIA (действует на все игры):
 
-| Setting | Options |
+| Параметр | Варианты |
 |---------|---------|
-| Shader Cache Size | Off, 128MB–100GB, Unlimited |
-| Shader Pre-Compile | Disabled, Low, Medium, High |
-| G-Sync Mode | Off, Fullscreen only, Fullscreen / Windowed |
-| Preferred Refresh Rate | Application Setting, Highest Available |
-| Global ReBAR Enable | Off / On (requires admin) |
-| Global ReBAR Size | 512MB, 1GB (default), 1.5GB, 2GB, 4GB, 6GB |
-| DLSS On-Screen Indicator | Enabled / Disabled (registry-based) |
-| FPS Limit (Frame Rate Limiter V3) | VRR-optimal presets or custom value |
-| G-Sync Enable | Global on/off toggle |
-| G-Sync On-Screen Indicator | Enabled / Disabled |
-| Digital Vibrance | Per-display saturation (0–100) |
-| DMFG Defaults | Set Frame Count and Target FPS globally, apply per-game with one click |
-
-When Global ReBAR is enabled, per-game ReBAR dropdowns show "Global (On/Off)" and "Global (1GB)" as the first option — selecting it inherits from global.
+| Размер кеша шейдеров | Выкл, 128МБ–100ГБ, без ограничений |
+| Предкомпиляция шейдеров | Disabled, Low, Medium, High |
+| Режим G-Sync | Выкл, только полноэкранный, полноэкранный / оконный |
+| Предпочитаемая частота обновления | Как в приложении, максимально доступная |
+| Глобальное включение ReBAR | Выкл / Вкл (нужен администратор) |
+| Размер глобального ReBAR | 512МБ, 1ГБ (по умолчанию), 1.5ГБ, 2ГБ, 4ГБ, 6ГБ |
+| Экранный индикатор DLSS | Вкл / Выкл (через реестр) |
+| Ограничение FPS (Frame Rate Limiter V3) | Пресеты, оптимальные для VRR, или своё значение |
+| Включение G-Sync | Глобальный переключатель вкл/выкл |
+| Экранный индикатор G-Sync | Вкл / Выкл |
+| Digital Vibrance | Насыщенность по каждому дисплею (0–100) |
+| Значения DMFG по умолчанию | Задайте количество кадров и целевой FPS глобально, применяйте к играм одним кликом |
+
+Когда глобальный ReBAR включён, списки ReBAR у игр показывают «Глобально (Вкл/Выкл)» и «Глобально (1ГБ)» первым вариантом — выбор наследует глобальное значение.
 
 ---
 
 ## Digital Vibrance
 
-Per-display color saturation control available in Settings → Global NVIDIA Driver Settings. Adjust a slider (0–100) for each connected display. Saved values are automatically restored on every app startup. Uses NVAPI directly — no NVIDIA Control Panel required.
+Управление насыщенностью цвета для каждого дисплея — в Настройки → Глобальные настройки драйвера NVIDIA. Ползунок (0–100) для каждого подключённого дисплея. Сохранённые значения автоматически восстанавливаются при каждом запуске приложения. Используется NVAPI напрямую — панель управления NVIDIA не нужна.
 
 ---
 
-## Admin Mode
+## Режим администратора
 
-Task Scheduler-based persistent elevation. Located in Settings → Data & Custom Files.
+Постоянное повышение прав через планировщик заданий. Расположен в Настройки → Система и обслуживание.
 
-- **Off** — RHI runs as a normal user. ReBAR, Low Latency (ULL), Smooth Motion, and some driver settings cannot be written.
-- **On** — Creates a scheduled task named "RHI Admin Mode". On subsequent launches, RHI silently relaunches through the task with admin privileges — no UAC prompt each time.
+- **Выкл** — RHI работает от обычного пользователя. ReBAR, Low Latency (ULL), Smooth Motion и часть настроек драйвера записать нельзя.
+- **Вкл** — создаётся запланированное задание «RHI Admin Mode». При последующих запусках RHI тихо перезапускается через это задание с правами администратора — без запроса UAC каждый раз.
 
-Toggling On triggers a one-time UAC prompt to create the task. Toggling Off deletes it. Drag-and-drop continues to work when elevated (UIPI bypass). The Drop Helper enables Discord drag-and-drop when running elevated.
+Включение запускает однократный запрос UAC для создания задания. Выключение его удаляет. Drag-and-drop продолжает работать с повышенными правами (обход UIPI). Drop Helper обеспечивает drag-and-drop из Discord при повышенных правах.
 
 ---
 
-## Profile Export and Import
+## Экспорт и импорт профилей
 
-Back up all per-game NVIDIA driver profile settings to a JSON file. Located in the Settings page (Global Nvidia Settings section).
+Резервное копирование всех индивидуальных настроек профилей драйвера NVIDIA в JSON-файл. Расположено на странице настроек (секция глобальных настроек NVIDIA).
 
-### Export
+### Экспорт
 
-Scans all games in RHI, finds their matching NVIDIA profiles, and saves all settings to `%LocalAppData%\RHI\nvidia_profiles_backup.json`. Includes global profile settings (Shader Cache, G-Sync, Refresh Rate, ReBAR). Only profiles for games in RHI are exported — not all 7800+ driver profiles.
+RHI сканирует все игры, находит их профили NVIDIA и сохраняет все настройки в `%LocalAppData%\RHI\nvidia_profiles_backup.json`. Включая настройки глобального профиля (кеш шейдеров, G-Sync, частота обновления, ReBAR). Экспортируются только профили игр из RHI — не все 7800+ профилей драйвера.
 
-### Import
+### Импорт
 
-Reads the backup file, creates missing profiles, registers exe associations, and applies all saved settings. ReBAR settings are applied via elevated helper to ensure they persist. The session count of imported profiles is reported on completion.
+Читает файл резервной копии, создаёт недостающие профили, регистрирует ассоциации exe и применяет все сохранённые настройки. Настройки ReBAR применяются через повышенный хелпер, чтобы они наверняка сохранились. По завершении сообщается число импортированных профилей.
 
-Use case: restore your per-game NVIDIA settings after a driver update wipes custom profiles.
+Применение: восстановить индивидуальные настройки NVIDIA после того, как обновление драйвера стёрло пользовательские профили.
 
 ---
 
 ## OptiScaler
 
-[OptiScaler](https://github.com/optiscaler/OptiScaler) is a 64-bit middleware that redirects upscaler calls between DLSS, FSR, and XeSS, and adds/patches frame generation on any GPU.
+[OptiScaler](https://github.com/optiscaler/OptiScaler) — 64-битная прослойка, перенаправляющая вызовы апскейлеров между DLSS, FSR и XeSS, а также добавляющая/патчащая генерацию кадров на любом GPU.
 
-### Channels
+### Каналы
 
-Switch between Stable and Nightly per game in the ⚙ cog. Update All keeps each game on its selected channel.
+Переключение Stable и Nightly у каждой игры в шестерёнке ⚙. «Обновить всё» оставляет каждую игру на выбранном канале.
 
-### Install
+### Установка
 
-One-click install deploys OptiScaler.dll (renamed to a proxy DLL), companion files, DLSS DLLs, and OptiPatcher (AMD/Intel only). A first-time setup screen prompts for GPU type and DLSS input settings before the first install.
+Установка в один клик разворачивает OptiScaler.dll (переименованный в прокси-DLL), сопутствующие файлы, DLL DLSS и OptiPatcher (только AMD/Intel). Перед первой установкой открывается экран первоначальной настройки типа GPU и входов DLSS.
 
-### ReShade Coexistence
+### Сосуществование с ReShade
 
-When both are installed, ReShade is renamed to `ReShade64.dll`. OptiScaler loads it via `LoadReshade=true`. Uninstalling OptiScaler restores the original ReShade filename.
+Когда установлены оба, ReShade переименовывается в `ReShade64.dll`. OptiScaler загружает его через `LoadReshade=true`. Удаление OptiScaler возвращает ReShade исходное имя.
 
-### DLL Naming
+### Имена DLL
 
-OptiScaler.dll is renamed to: user DLL override → manifest override → `winmm.dll` (Vulkan) → `dxgi.dll` (default). Configurable per-game in Game Overrides → DLL Naming.
+OptiScaler.dll переименовывается в: своё имя из переопределений → переопределение из манифеста → `winmm.dll` (Vulkan) → `dxgi.dll` (по умолчанию). Настраивается у каждой игры в Переопределения игры → Имена DLL.
 
-### OptiScaler ⚙ Cog
+### Шестерёнка ⚙ OptiScaler
 
-The cog provides per-game settings. All settings except OptiScaler Version are greyed out when OptiScaler is not installed.
+Шестерёнка даёт настройки для каждой игры. Все настройки, кроме версии OptiScaler, серые, когда OptiScaler не установлен.
 
-**Always available:**
-- **OptiScaler Version** — Stable or Nightly
-- **Upscaler API / Upscaler** — pick which graphics API to configure (DX11, DX12, Vulkan) on the left, and the upscaler for that API on the right. Options update automatically based on the selected API. Written directly to `OptiScaler.ini`.
-- **Framerate Limit** — VRR-optimal frame cap using Reflex
+**Всегда доступны:**
+- **Версия OptiScaler** — Stable или Nightly
+- **API апскейлера / Апскейлер** — слева выберите настраиваемый графический API (DX11, DX12, Vulkan), справа — апскейлер для этого API. Варианты обновляются автоматически по выбранному API. Записывается прямо в `OptiScaler.ini`.
+- **Ограничение FPS** — потолок кадров, оптимальный для VRR, через Reflex
 
-**Nightly only — Frame Generation Settings:**
-- **Streamline/DLSS Enabler** — deploy Streamline and DLSS Enabler to the game folder (required for DLSS FG)
-- **Streamline Version** — pick which Streamline version to deploy per game
-- **FG Input** — Auto / OptiFG (Upscaler) / DLSSG via Streamline / DLSSG via Nvngx / FSR 3.1 FG / FSR 3.0 FG / XeFG
-- **HUD Fix** — enable hudless resource tracking for FG
-- **FG Output** — Auto / FSR FG / DLSSG / XeFG
-- **FG Nvngx Replacement** — None / Nukem's / Enabler / FSR 3/4 FG (shown when Output = DLSSG)
+**Только Nightly — настройки генерации кадров:**
+- **Streamline/DLSS Enabler** — развернуть Streamline и DLSS Enabler в папку игры (нужно для DLSS FG)
+- **Версия Streamline** — какую версию Streamline развернуть для игры
+- **Вход FG** — Auto / OptiFG (Upscaler) / DLSSG via Streamline / DLSSG via Nvngx / FSR 3.1 FG / FSR 3.0 FG / XeFG
+- **HUD Fix** — включить отслеживание ресурсов без HUD для FG
+- **Выход FG** — Auto / FSR FG / DLSSG / XeFG
+- **Замена FG Nvngx** — Нет / Nukem / Enabler / FSR 3/4 FG (показывается при Output = DLSSG)
 
-**Nightly only — Additional Settings:**
-- **DLSS SR Preset** — Default / J / K / L / M
-- **DLSS RR Preset** — Default / D / E
-- **Render Scale** — Off + percentage presets (33% Ultra Perf to 100% DLAA)
-- **Disable Flip Metering** — fixes thick frametime graph with NukemFG
+**Только Nightly — дополнительные настройки:**
+- **Пресет DLSS SR** — Default / J / K / L / M
+- **Пресет DLSS RR** — Default / D / E
+- **Масштаб рендеринга** — Выкл + процентные пресеты (от 33% Ultra Perf до 100% DLAA)
+- **Disable Flip Metering** — исправляет толстый график frametime с NukemFG
 
-**Nightly only — Engine.ini Settings (Unreal Engine games only):**
-- Dilated Motion Vectors, FSR Crash Fix, FSR-FG Swapchain, Upscaler Plugin
+**Только Nightly — настройки Engine.ini (только игры на Unreal Engine):**
+- Dilated Motion Vectors, FSR Crash Fix, FSR-FG Swapchain, плагин апскейлера
 
-**Presets (Nightly only):**
-4 user-configurable slots. Each has an editable name, a Save button (captures current cog settings), and an Apply button (loads preset onto any game). Presets are global — saved once, apply to any game. Stored at `%LocalAppData%\RHI\os_presets.json`.
+**Пресеты (только Nightly):**
+4 пользовательских слота. У каждого редактируемое имя, кнопка «Сохранить» (фиксирует текущие настройки шестерёнки) и кнопка «Применить» (загружает пресет в любую игру). Пресеты глобальные — сохраняются один раз, применяются к любой игре. Хранятся в `%LocalAppData%\RHI\os_presets.json`.
 
-### Deploy OptiScaler.ini
+### Развертывание OptiScaler.ini
 
-Copies your configured INI template (from `%LocalAppData%\RHI\inis\`) to the game folder, applying hotkey and plugin settings.
+Копирует ваш настроенный шаблон INI (из `%LocalAppData%\RHI\inis\`) в папку игры, применяя настройки горячих клавиш и плагинов.
 
-### OptiScaler Wiki
+### Вики OptiScaler
 
-The Info button shows compatibility data from the OptiScaler wiki (Working/Partially Working/Not Working, supported upscalers, notes).
+Кнопка «Инфо» показывает данные о совместимости из вики OptiScaler (Работает / Частично работает / Не работает, поддерживаемые апскейлеры, примечания).
 
-### DLSS Auto-Download
+### Автоскачивание DLSS
 
-Latest DLSS SR, RR, and FG DLLs are staged automatically from the RHI DLSS manifest on startup.
+Свежие DLL DLSS SR, RR и FG автоматически складываются из DLSS-манифеста RHI при запуске.
 
 ---
 
-## Shader Packs
+## Наборы шейдеров
 
-RHI maintains 46 ReShade shader packs. Shader deployment is controlled by the **Shader Management** setting in Settings → Shaders & Addons.
+RHI поддерживает 46 наборов шейдеров ReShade. Развертывание шейдеров контролируется настройкой **Управление шейдерами** в Настройки → Шейдеры и аддоны.
 
-### Global Shader Management Modes
+### Глобальные режимы управления шейдерами
 
-| Mode | Behaviour |
+| Режим | Поведение |
 |------|-----------|
-| RHI Managed | RHI deploys its built-in shader packs to all games (default) |
-| Custom | Uses your custom shader directories only |
-| Off | No shader deployment — RHI never touches the reshade-shaders folder |
+| Под управлением RHI | RHI разворачивает встроенные наборы шейдеров во все игры (по умолчанию) |
+| Свои | Используются только ваши папки шейдеров |
+| Выкл | Шейдеры не развертываются — RHI никогда не трогает папку reshade-shaders |
 
-The first-launch setup window lets you choose your preferred mode on a fresh install.
+Окно первоначальной настройки при первом запуске позволяет выбрать предпочитаемый режим на свежей установке.
 
-### Pack Categories
+### Категории наборов
 
-- **Essential** — Lilium HDR Shaders (required for HDR tone mapping). Selected by default.
-- **Recommended** — Core packs: crosire reshade-shaders, PumboAutoHDR, smolbbsoop, MaxG2D Simple HDR, clshortfuse shaders, potatoFX.
-- **Extra** — Community packs covering colour grading, film emulation, CRT simulation, VR tools, screen-space effects, and more.
+- **Основные** — Lilium HDR Shaders (нужны для HDR-тонмаппинга). Выбраны по умолчанию.
+- **Рекомендуемые** — ключевые наборы: crosire reshade-shaders, PumboAutoHDR, smolbbsoop, MaxG2D Simple HDR, clshortfuse shaders, potatoFX.
+- **Дополнительные** — наборы сообщества: цветокоррекция, эмуляция плёнки, имитация CRT, инструменты VR, эффекты экранного пространства и не только.
 
-### Per-File Selection
+### Выбор по файлам
 
-Expand any pack row in the shader picker to see its individual `.fx` files. Tick only the shaders you want — the pack checkbox shows a dash when a partial selection is active. Use **Expand All / Collapse All** to quickly browse all packs, and **Deselect All** to start fresh.
+Разверните строку набора в окне выбора шейдеров, чтобы увидеть отдельные файлы `.fx`. Отмечайте только нужные шейдеры — галочка набора показывает прочерк при частичной выборке. Кнопки **Развернуть всё / Свернуть все** помогают быстро просмотреть все наборы, **Снять всё** — начать с чистого листа.
 
-### Shader Profiles
+### Профили шейдеров
 
-The Profiles panel on the right side of the global shader picker lets you save, load, rename, and delete named shader selections. Each profile stores both pack selection and per-file exclusions.
+Панель «Профили» справа в глобальном окне выбора шейдеров позволяет сохранять, загружать, переименовывать и удалять именные подборки шейдеров. Каждый профиль хранит и выбор наборов, и исключения по файлам.
 
-- **Save** — overwrites the selected profile with the current selection, or creates a new profile if none is selected
-- **New** — creates a profile from the current selection and prompts for a name
-- **Export** — zips the currently selected shader files and copies the archive to your clipboard (paste into Discord to share)
-- **Import** — loads a profile archive exported by RHI; if the packs aren't cached locally, files are extracted from the archive automatically
+- **Сохранить** — перезаписывает выбранный профиль текущей выборкой или создаёт новый, если ничего не выбрано
+- **Новый** — создаёт профиль из текущей выборки и запрашивает имя
+- **Экспорт** — упаковывает выбранные файлы шейдеров в zip и кладёт архив в буфер обмена (вставьте в Discord, чтобы поделиться)
+- **Импорт** — загружает архив профиля, экспортированный из RHI; если наборов нет в локальном кеше, файлы автоматически извлекаются из архива
 
-Profiles can also be loaded in the per-game shader picker (read-only — apply a profile then adjust as needed).
+Профили можно загружать и в окне выбора шейдеров для отдельной игры (только для чтения — примените профиль и подправьте при необходимости).
 
-### Per-Game Shader Overrides
+### Переопределения шейдеров по игре
 
-| Mode | Behaviour |
+| Режим | Поведение |
 |------|-----------|
-| Global | Uses the global shader selection |
-| Select | Opens a picker for game-specific packs |
-| Custom | Uses shaders from your custom directories |
-| Off | No managed shaders deployed |
+| Глобальные | Используется глобальная подборка шейдеров |
+| Выбрать | Открывает окно выбора наборов для игры |
+| Свои | Используются шейдеры из ваших папок |
+| Выкл | Управляемые шейдеры не развертываются |
 
-### Custom Shaders
+### Свои шейдеры
 
-Place custom shaders in `%LocalAppData%\RHI\reshade\Custom\Shaders\` and textures in `Custom\Textures\`. Enable the Custom Shaders toggle in Settings.
+Положите свои шейдеры в `%LocalAppData%\RHI\reshade\Custom\Shaders\`, а текстуры — в `Custom\Textures\`. Включите переключатель «Свои шейдеры» в настройках.
 
-### Shader Cache
+### Кеш шейдеров
 
-Toggle in Settings. When enabled, all packs are pre-downloaded on startup. When disabled, packs fetch on-demand when needed.
+Переключатель в настройках. Когда включён, все наборы предзагружаются при запуске. Когда выключен, наборы скачиваются по требованию.
 
 ---
 
-## ReShade Addon Management
+## Управление аддонами ReShade
 
-A curated addon manager for ReShade addons from the official Addons.ini list.
+Курируемый менеджер аддонов ReShade из официального списка Addons.ini.
 
-### Global Toggles
+### Глобальные переключатели
 
-Click "ReShade Addons" in the toolbar. Toggle addons On/Off globally. Enabled addons are deployed to all games with ReShade installed.
+Нажмите «Аддоны ReShade» на панели инструментов. Включайте и выключайте аддоны глобально. Включённые аддоны разворачиваются во все игры с установленным ReShade.
 
-### Per-Game Addon Overrides
+### Переопределения аддонов по игре
 
-| Mode | Behaviour |
+| Режим | Поведение |
 |------|-----------|
-| Global | Uses the globally enabled set |
-| Select | Per-game addon picker |
-| Off | Removes all managed addons |
+| Глобальные | Используется глобально включённый набор |
+| Выбрать | Окно выбора аддонов для игры |
+| Выкл | Удаляет все управляемые аддоны |
 
-### Special Addons
+### Особые аддоны
 
-- **RenoDX DevKit** — development tool for mod authors
-- **DLSS Fix** — makes ReShade draw on native game frames. Auto-configures reshade.ini with DLSS/Streamline paths.
+- **RenoDX DevKit** — инструмент разработки для авторов модов
+- **DLSS Fix** — заставляет ReShade рисовать на нативных кадрах игры. Автонастраивает reshade.ini с путями DLSS/Streamline.
 
-### Custom Addons
+### Свои аддоны
 
-Place `.addon64` or `.addon32` files in `%LocalAppData%\RHI\Custom\Addons\`. They appear in the Addon Manager dialog and the per-game "Select Addons" picker with on/off toggles. Custom addons are deployed directly from this folder — no download needed. They participate in stale removal tracking and work alongside the built-in addon packs.
+Положите файлы `.addon64` или `.addon32` в `%LocalAppData%\RHI\Custom\Addons\`. Они появятся в диалоге менеджера аддонов и в окне «Выбрать аддоны» у каждой игры с переключателями вкл/выкл. Свои аддоны разворачиваются прямо из этой папки — скачивание не нужно. Они участвуют в отслеживании устаревших файлов и работают вместе со встроенными наборами аддонов.
 
 ---
 
-## Game Launch
+## Запуск игр
 
-Launch games from the green "▶ Launch" button or by double-clicking in the sidebar.
+Запускайте игры зелёной кнопкой «▶ Запустить» или двойным кликом в боковой панели.
 
-### Priority Chain
+### Цепочка приоритетов
 
-1. User exe override (Overrides panel)
-2. Manifest exe override
-3. Steam `-applaunch` (with overlay and playtime tracking)
-4. Epic protocol URL (when no launch args set)
-5. Direct exe (largest .exe in install path)
+1. Своё exe из переопределений (панель переопределений)
+2. Переопределение exe из манифеста
+3. Steam `-applaunch` (с оверлеем и учётом времени)
+4. Протокольная ссылка Epic (если не заданы аргументы запуска)
+5. Прямой запуск exe (наибольший .exe в пути установки)
 
-### Launch Arguments
+### Аргументы запуска
 
-Set per-game command-line arguments in the Overrides panel. Steam games pass args via `-applaunch`. Epic protocol is skipped when args are set.
+Задаются для каждой игры в панели переопределений. Игры Steam передают аргументы через `-applaunch`. Протокол Epic пропускается, когда аргументы заданы.
 
-### HDR Auto-Toggle
+### Автопереключение HDR
 
-Automatically enables Windows HDR when a game is launched through RHI and disables it when the game exits. Useful for users who keep their desktop in SDR.
+Автоматически включает Windows HDR при запуске игры через RHI и выключает при её выходе. Полезно тем, кто держит рабочий стол в SDR.
 
-- **Global setting**: Off/On in the ReShade & Display card on the Settings page.
-- **Per-game override**: "HDR" button next to Launch — purple when active, grey when inactive. Click to flip.
-- **Process monitoring**: For direct exe launches, monitors the process handle directly. For Steam/Epic protocol launches, polls for a process running from the game's install path (1-second intervals, up to 60 seconds to find it).
-- **Always enables**: The toggle always calls EnableHdr regardless of current state (detection is unreliable on some HDR-capable displays).
-- **Disables on exit**: Only when the game process exits. If the app can't find the game process (protocol launch timeout), HDR stays on.
+- **Глобальная настройка**: Выкл/Вкл в карточке «ReShade и дисплей» на странице настроек.
+- **Переопределение по игре**: кнопка «HDR» рядом с «Запустить» — фиолетовая, когда активно, серая, когда нет. Клик переключает.
+- **Мониторинг процесса**: при прямом запуске exe отслеживается сам хендл процесса. При запуске через Steam/Epic опрашиваются процессы, запущенные из пути установки игры (интервал 1 секунда, до 60 секунд на поиск).
+- **Всегда включает**: переключатель всегда вызывает EnableHdr независимо от текущего состояния (определение ненадёжно на некоторых HDR-дисплеях).
+- **Выключает при выходе**: только когда процесс игры завершается. Если приложение не смогло найти процесс игры (таймаут протокола), HDR остаётся включённым.
 
-### Running Game Indicator
+### Индикатор запущенной игры
 
-The sidebar item turns green when a game launched through RHI is currently running. Returns to normal when the game exits. Only tracks games launched via the Launch button (not externally launched games).
+Строка в боковой панели становится зелёной, пока запущенная через RHI игра работает. Возвращается в норму при выходе. Отслеживаются только игры, запущенные кнопкой «Запустить» (запущенные извне не учитываются).
 
 ---
 
-## System Tray & Jump List
+## Системный трей и список переходов
 
-RHI can minimize to the system tray when you close the window, keeping the file watcher, HDR auto-toggle, and background update checks running without a visible window.
+RHI может сворачиваться в системный трей при закрытии окна, продолжая работу наблюдателя файлов, автопереключения HDR и фоновых проверок обновлений без видимого окна.
 
-### Close to Tray
+### Закрытие в трей
 
-When enabled (Settings → System & Maintenance → System Tray), clicking the window close button hides RHI to the system tray instead of exiting. Double-click the tray icon to restore the window. Right-click the tray icon for a context menu showing your last 5 launched games, plus Open and Exit options.
+Когда включено (Настройки → Система и обслуживание → Системный трей), кнопка закрытия окна скрывает RHI в трей вместо выхода. Двойной клик по значку в трее возвращает окно. Правый клик открывает меню с последними 5 запущенными играми, плюс пункты «Открыть» и «Выход».
 
-### Jump List
+### Список переходов
 
-When "Recent Games" is enabled, right-clicking the RHI icon on the taskbar shows your recent games in the Windows jump list — the same "Recent" section you see on Steam. Click any game to launch it instantly. Works in both normal and Admin Mode.
+Когда включено «Недавние игры», правый клик по значку RHI на панели задач показывает недавние игры в списке переходов Windows — та же секция «Недавние», что и в Steam. Клик по игре запускает её мгновенно. Работает и в обычном режиме, и в режиме администратора.
 
-### Start with Windows
+### Запуск с Windows
 
-When enabled (Settings → System & Maintenance → System Tray), RHI registers itself in the Windows startup registry (`HKCU\...\Run`) with a `--minimized` flag. On boot, RHI starts directly to the system tray without showing the window. The tray icon, jump list, and background update checks all function normally. Works with Admin Mode — a signal file is used to pass the minimized state across the scheduled task elevation boundary.
+Когда включено (Настройки → Система и обслуживание → Системный трей), RHI регистрирует себя в реестре автозагрузки Windows (`HKCU\...\Run`) с флагом `--minimized`. При загрузке системы RHI стартует сразу в трее, не показывая окно. Значок в трее, список переходов и фоновые проверки обновлений работают как обычно. Совместимо с режимом администратора — флаг minimized передаётся через границу повышения прав запланированного задания файлом-сигналом.
 
-### Activate from Shortcut
+### Активация из ярлыка
 
-If RHI is hidden in the tray and you click a pinned taskbar shortcut or launch RHI again, the existing instance is brought back to the foreground instead of starting a new one.
+Если RHI скрыт в трее, а вы кликаете закреплённый ярлык на панели задач или запускаете RHI снова, существующий экземпляр выводится на передний план вместо запуска нового.
 
-### Background Update Checks
+### Фоновые проверки обновлений
 
-While running (especially useful in the tray), RHI automatically re-checks all mod updates, the manifest, and the app version every 4 hours. No manual refresh or restart needed.
+Во время работы (особенно полезно в трее) RHI автоматически перепроверяет все обновления модов, манифест и версию приложения каждые 4 часа. Ручное обновление и перезапуск не нужны.
 
-### Peak Brightness (Nits)
+### Пиковая яркость (ниты)
 
-Set your monitor's peak brightness once in the Settings page (ReShade & Display → HDR & Peak Brightness). The "Auto" button reads your monitor hardware. The value is written as `ToneMapPeakNits` to all `[renodx-preset*]` sections in reshade.ini on every deploy — installs, updates, mass deploy, and INI redeploy.
+Задайте пиковую яркость монитора один раз на странице настроек (ReShade и дисплей → HDR и пиковая яркость). Кнопка «Авто» считывает значение из аппаратуры монитора. Значение записывается как `ToneMapPeakNits` во все секции `[renodx-preset*]` в reshade.ini при каждом развертывании — установка, обновления, массовое развертывание и перенос INI.
 
 ### DOF Fix
 
-One-click install for Unreal Engine 5.0–5.6 games that have depth-of-field stepping/tiling artifacts (common on NVIDIA GPUs). Appears as a component row in the Optional section.
+Установка в один клик для игр на Unreal Engine 5.0–5.6 с артефактами ступенчатости/тайлинга глубины резкости (типично на GPU NVIDIA). Появляется строкой компонента в секции «Необязательные».
 
-- Eligibility: UE 5.0–5.6 detected (or forced via manifest `dofFixForceGames`), 64-bit only.
-- Participates in Update All.
-- Engine badge toggle: click the "Unreal Engine" badge to force UE5 eligibility on Game Pass games where version detection fails.
-- Manifest-forced games (e.g. Clair Obscur, Avowed) have the badge locked — can't be toggled off.
-- Toggling the badge OFF uninstalls the DOF Fix addon if it was installed.
+- Условия: обнаружен UE 5.0–5.6 (или форсируется через `dofFixForceGames` в манифесте), только 64-бит.
+- Участвует в «Обновить всё».
+- Переключатель значка движка: клик по значку «Unreal Engine» форсирует право на UE5 для игр Game Pass, где не срабатывает определение версии.
+- У игр, форсированных манифестом (например, Clair Obscur, Avowed), значок заблокирован — переключить нельзя.
+- Выключение значка удаляет аддон DOF Fix, если он был установлен.
 
 ---
 
-## Per-Game Overrides
+## Переопределения для каждой игры
 
-All controls save immediately when changed.
+Все элементы управления сохраняются сразу при изменении.
 
-| Override | Description |
+| Переопределение | Описание |
 |----------|-------------|
-| Game name | Editable — rename persists across restarts |
-| Wiki mod name | Match to a different wiki entry |
-| Wiki exclusion | Exclude from wiki lookups entirely |
-| DLL naming | Toggle enables both ReShade and DC filename overrides |
-| Update Inclusion | Per-component toggles (RS, RDX, UL, DC, OS, REF) |
-| Shader Mode | Global / Select / Custom / Off |
-| Addon Mode | Global / Select / Off |
-| Bitness | Auto / 32-bit / 64-bit |
-| Graphics API | Auto / DX8 / DX9 / DX10 / DX11/12 / Vulkan / OpenGL |
-| RS Channel | Global / Stable / Nightly / Custom / Legacy / No Addons |
-| DXVK | Off / Development / Stable / Lilium HDR |
-| Launch executable | Custom exe path |
-| Launch arguments | Command-line args for the game |
-| HDR auto-toggle | Per-game "HDR" button next to Launch — overrides the global HDR setting |
-| Config button | Opens the Engine.ini config folder |
-
-### Reset Overrides
-
-Clears all per-game settings back to defaults (including DXVK, RS channel, shader/addon modes, bitness, API, DLL names, launch settings).
-
-### Bitness Override Auto-Uninstall
-
-Changing the bitness override when components are installed triggers automatic uninstall of all components (ReShade, DC, RenoDX, ReLimiter, OptiScaler, DXVK, RE Framework, Luma) to provide a clean slate for the new bitness.
+| Название игры | Редактируемое — переименование сохраняется между перезапусками |
+| Имя мода в вики | Привязка к другой записи вики |
+| Исключение из вики | Полное исключение из поисков по вики |
+| Имена DLL | Переключатель включает переопределение имён файлов сразу и для ReShade, и для DC |
+| Включение в обновления | Переключатели по компонентам (RS, RDX, UL, DC, OS, REF) |
+| Режим шейдеров | Глобальные / Выбрать / Свои / Выкл |
+| Режим аддонов | Глобальные / Выбрать / Выкл |
+| Разрядность | Авто / 32-бит / 64-бит |
+| Графический API | Авто / DX8 / DX9 / DX10 / DX11/12 / Vulkan / OpenGL |
+| Канал RS | Глобальный / Stable / Nightly / Custom / Legacy / No Addons |
+| DXVK | Выкл / Development / Stable / Lilium HDR |
+| Запускаемый файл | Своё exe |
+| Аргументы запуска | Аргументы командной строки для игры |
+| Автопереключение HDR | Кнопка «HDR» рядом с «Запустить» — переопределяет глобальную настройку HDR |
+| Кнопка «Конфиг» | Открывает папку конфигурации Engine.ini |
+
+### Сброс переопределений
+
+Возвращает все настройки игры к значениям по умолчанию (включая DXVK, канал RS, режимы шейдеров/аддонов, разрядность, API, имена DLL, настройки запуска).
+
+### Автоудаление при смене разрядности
+
+Смена переопределения разрядности при установленных компонентах запускает автоматическое удаление всех компонентов (ReShade, DC, RenoDX, ReLimiter, OptiScaler, DXVK, RE Framework, Luma), чтобы начать с чистого листа под новую разрядность.
 
 ---
 
-## ReShade Presets
+## Пресеты ReShade
 
-### Preset Folder
+### Папка пресетов
 
-Place `.ini` presets in `%LocalAppData%\RHI\inis\reshade-presets\`. The "Select ReShade Preset" button in Overrides lists files with checkboxes. Click Deploy to copy selected presets to the game folder.
+Положите пресеты `.ini` в `%LocalAppData%\RHI\inis\reshade-presets\`. Кнопка «Выбрать пресет ReShade» в переопределениях показывает файлы с галочками. Нажмите «Развернуть», чтобы скопировать отмеченные пресеты в папку игры.
 
-### Drag-and-Drop
+### Drag-and-drop
 
-Drag a ReShade preset onto the window. RHI validates it, saves it, deploys to a chosen game, and offers to auto-install required shader packs.
+Перетащите пресет ReShade на окно. RHI проверит его, сохранит, развернёт в выбранную игру и предложит автоматически установить нужные наборы шейдеров.
 
-### Mass Preset Install
+### Массовая установка пресетов
 
-From Settings: select presets, choose target games, and optionally install required shader packs.
+Из настроек: выберите пресеты, укажите целевые игры и при необходимости установите нужные наборы шейдеров.
 
 ---
 
-## Nexus Mods and PCGamingWiki Links
+## Ссылки Nexus Mods и PCGamingWiki
 
 ### Nexus Mods
 
-RHI fetches the public Nexus game catalogue and matches games by name. Clickable button on the detail panel info card.
+RHI загружает публичный каталог игр Nexus и сопоставляет игры по имени. Кликабельная кнопка на карточке информации в панели подробностей.
 
-### Nexus Update Alerts
+### Оповещения об обновлениях Nexus
 
-Automatic update detection via GraphQL API (no API key). When a Nexus-hosted mod is updated, the card shows purple with "Update RenoDX" that opens the Nexus page.
+Автоматическое определение обновлений через GraphQL API (без API-ключа). Когда мод на Nexus обновляется, карточка подсвечивается фиолетовым с надписью «Обновить RenoDX», открывающей страницу Nexus.
 
 ### PCGamingWiki
 
-Resolved via Steam AppID. Falls back to OpenSearch when no AppID is available. Clickable button on the info card.
+Определяется по Steam AppID. При отсутствии AppID — запасной вариант OpenSearch. Кликабельная кнопка на карточке информации.
 
 ---
 
-## UW Fix and Ultra+ Links
+## UW Fix и ссылки Ultra+
 
 ### UW Fix
 
-Ultrawide/resolution fix links from Lyall, RoseTheFlower, and p1xel8ted. Button appears when a fix is available. Search "UW Fix" to filter.
+Ссылки на исправления ультрашироких/нестандартных разрешений от Lyall, RoseTheFlower и p1xel8ted. Кнопка появляется, когда исправление доступно. Введите «UW Fix» в поиске для фильтрации.
 
 ### Ultra+
 
-Ultra+ mod links from theultraplace.com. Button appears when available. Search "Ultra+" to filter.
+Ссылки на моды Ultra+ с theultraplace.com. Кнопка появляется, когда доступны. Введите «Ultra+» в поиске для фильтрации.
 
 ---
 
-## Vulkan ReShade Support
+## Поддержка Vulkan в ReShade
 
-### How It Works
+### Как это работает
 
-ReShade is installed as a global Vulkan implicit layer via `C:\ProgramData\ReShade\`. A per-game `reshade.ini` and `RDXC_VULKAN_FOOTPRINT` marker enable managed shader deployment.
+ReShade устанавливается как глобальный неявный слой Vulkan через `C:\ProgramData\ReShade\`. Поигровой `reshade.ini` и маркер `RDXC_VULKAN_FOOTPRINT` обеспечивают управляемое развертывание шейдеров.
 
-### Lightweight Install
+### Лёгкая установка
 
-When the layer is already registered, clicking Install on a Vulkan game is instant (INI + footprint + shaders only, no admin needed).
+Когда слой уже зарегистрирован, нажатие «Установить» у Vulkan-игры мгновенно (только INI + footprint + шейдеры, администратор не нужен).
 
-### Dual-API Games
+### Игры с двумя API
 
-Games with DirectX + Vulkan show a rendering path toggle. Switching modes reinstalls ReShade in the correct mode.
+Игры с DirectX + Vulkan показывают переключатель пути рендеринга. Смена режима переустанавливает ReShade в правильном режиме.
 
 ---
 
 ## DXVK
 
-DXVK translates DirectX 8/9/10 to Vulkan. Managed as a per-game component.
+DXVK транслирует DirectX 8/9/10 в Vulkan. Управляется как компонент каждой игры.
 
-### Enabling
+### Включение
 
-The DXVK dropdown appears in Overrides for DX8, DX9, and DX10 games. Options:
+Список DXVK появляется в переопределениях у игр DX8, DX9 и DX10. Варианты:
 
-| Option | Behaviour |
+| Вариант | Поведение |
 |--------|-----------|
-| Off | DXVK disabled / uninstalled |
-| Development | Nightly builds from doitsujin/dxvk |
-| Stable | Tagged releases |
-| Lilium HDR | EndlesslyFlowering fork with scRGB HDR output |
+| Выкл | DXVK отключён / удалён |
+| Development | Ночные сборки doitsujin/dxvk |
+| Stable | Тегированные релизы |
+| Lilium HDR | Форк EndlesslyFlowering с HDR-выводом scRGB |
 
-Selecting a variant saves the preference and shows an **Install DXVK** button — click it when ready. Switching variants while DXVK is already installed uninstalls the old version first. Selecting Off uninstalls.
+Выбор варианта сохраняет предпочтение и показывает кнопку **Установить DXVK** — нажмите её, когда будете готовы. Смена варианта при уже установленном DXVK сначала удаляет старую версию. Выбор «Выкл» удаляет DXVK.
 
-### DX8/DX9 Games
+### Игры DX8/DX9
 
-- **Development/Stable** — DXVK deployed as `dxgi_dxvk.dll` in proxy mode. ReShade stays as `d3d9.dll` with a `[PROXY]` chain.
-- **Lilium HDR** — DXVK deployed as `d3d9.dll` directly. ReShade uses the Vulkan layer. Enables SM5 HDR shaders. On uninstall, local ReShade is restored automatically.
+- **Development/Stable** — DXVK разворачивается как `dxgi_dxvk.dll` в прокси-режиме. ReShade остаётся `d3d9.dll` с цепочкой `[PROXY]`.
+- **Lilium HDR** — DXVK разворачивается прямо как `d3d9.dll`. ReShade использует Vulkan-слой. Включаются HDR-шейдеры SM5. При удалении локальный ReShade восстанавливается автоматически.
 
-### DX10/DX11 Games
+### Игры DX10/DX11
 
-Standard mode. ReShade switches to Vulkan layer. DXVK DLLs go in the game root (or OptiScaler plugins folder for coexistence).
+Стандартный режим. ReShade переключается на Vulkan-слой. DLL DXVK кладутся в корень игры (или в папку плагинов OptiScaler для сосуществования).
 
-### Anti-Cheat Blacklist
+### Чёрный список античитов
 
-Games with known anti-cheat are blacklisted via the manifest. The dropdown is disabled.
+Игры с известными античитами вносятся в чёрный список через манифест. Список отключается.
 
 ---
 
-## Foreign DLL Protection
+## Защита от чужих DLL
 
-Before overwriting an existing DLL, RHI checks if it belongs to DXVK, Special K, ENB, or another tool via binary signature scanning. A confirmation dialog appears. During Update All, foreign DLLs are silently skipped.
+Перед перезаписью существующей DLL RHI по бинарным сигнатурам проверяет, не принадлежит ли она DXVK, Special K, ENB или другому инструменту. Показывается диалог подтверждения. Во время «Обновить всё» чужие DLL тихо пропускаются.
 
 ---
 
-## Drag-and-Drop
+## Drag-and-drop
 
-RHI supports drag-and-drop for adding games and installing mods. Works even when elevated.
+RHI поддерживает drag-and-drop для добавления игр и установки модов. Работает даже с повышенными правами.
 
-| File type | What happens |
+| Тип файла | Что происходит |
 |-----------|-------------|
-| `.exe` | Opens add-game dialog with auto-detection |
-| `.addon64` / `.addon32` | Install dialog with game picker |
-| `.zip`, `.7z` (Luma archive) | Luma install with game picker |
-| `.zip`, `.7z`, `.rar` etc. | Extract and find addon files |
-| `.ini` (ReShade preset) | Validate, save, deploy, offer shader install |
-| URL (`.url` shortcut) | Download and process as addon |
+| `.exe` | Открывается диалог добавления игры с автоопределением |
+| `.addon64` / `.addon32` | Диалог установки с выбором игры |
+| `.zip`, `.7z` (архив Luma) | Установка Luma с выбором игры |
+| `.zip`, `.7z`, `.rar` и т.п. | Распаковка и поиск файлов аддонов |
+| `.ini` (пресет ReShade) | Проверка, сохранение, развертывание, предложение установки шейдеров |
+| URL (ярлык `.url`) | Скачивание и обработка как аддона |
 
 ---
 
-## Addon Auto-Detection
+## Автоопределение аддонов
 
-### File Watcher
+### Наблюдатель файлов
 
-Monitors your Downloads folder (configurable) for `renodx-*.addon64`, `renodx-*.addon32`, and archives with "renodx" or "luma" in the filename. Prompts to install when detected.
+Отслеживает вашу папку загрузок (настраивается) на файлы `renodx-*.addon64`, `renodx-*.addon32` и архивы со словами «renodx» или «luma» в имени. При обнаружении предлагает установить.
 
-### Named Pipe Forwarding
+### Пересылка через именованный канал
 
-Double-clicking an addon in Explorer opens RHI (or forwards to the running instance) and triggers install.
+Двойной клик по аддону в Проводнике открывает RHI (или пересылает в запущенный экземпляр) и запускает установку.
 
 ---
 
-## Update All
+## Обновить всё
 
-Updates ReShade, RenoDX, ReLimiter, Display Commander, OptiScaler, and RE Framework across all eligible games. Per-game exclusions are respected. Foreign DLLs are silently skipped.
+Обновляет ReShade, RenoDX, ReLimiter, Display Commander, OptiScaler и RE Framework во всех подходящих играх. Поигровые исключения соблюдаются. Чужие DLL тихо пропускаются.
 
-### Cooldown
+### Задержка
 
-4-hour cooldown between update checks. Full Refresh bypasses it.
+4-часовая задержка между проверками обновлений. «Полное обновление» её обходит.
 
-### Rate Limit Handling
+### Обработка лимитов запросов
 
-GitHub API 403 responses cancel all remaining API calls for the session.
+Ответы GitHub API 403 отменяют все оставшиеся вызовы API на сессию.
 
 ---
 
-## Auto-Update
+## Автообновление
 
-RHI checks for new versions on launch via GitHub Releases API. Beta Opt-In checks both stable and beta releases.
+RHI проверяет новые версии при запуске через GitHub Releases API. «Бета по подписке» проверяет и стабильные, и бета-релизы.
 
 ---
 
-## Remote Manifest
+## Удалённый манифест
 
-Fetched from GitHub on every launch. Provides game-specific overrides without app updates:
+Загружается с GitHub при каждом запуске. Даёт поигровые переопределения без обновления приложения:
 
-- Game blacklist, install path overrides, wiki name mapping
-- Engine/API/bitness overrides, DLL name overrides
-- Game notes (per-component), forced external links
-- DLSS skip lists, launch exe overrides, split game definitions
-- Legacy ReShade versions, install warnings
-- Shader pack and addon pack overrides
-- NVIDIA profile exe exclusions
+- Чёрный список игр, переопределения путей установки, привязка имён к вики
+- Переопределения движка/API/разрядности, переопределения имён DLL
+- Заметки по играм (по компонентам), принудительные внешние ссылки
+- Списки пропуска DLSS, переопределения запускаемого exe, определения разделения игр
+- Старые версии ReShade, предупреждения при установке
+- Переопределения наборов шейдеров и аддонов
+- Исключения exe из профилей NVIDIA
 
 ---
 
-## Message of the Day
+## Сообщение дня
 
-A message fetched from `motd.md` on GitHub. Shown as a dialog once per unique message (tracked by SHA256 hash). Empty file = no dialog.
+Сообщение, загружаемое из `motd.md` на GitHub. Показывается диалогом один раз на уникальное сообщение (отслеживается по хешу SHA256). Пустой файл = без диалога.
 
 ---
 
-## Performance
+## Производительность
 
-- **Instant launch from cache** — game list loads immediately, full scan runs in background
-- **Parallel shader pack checks and deployments**
-- **PE-level and game-level API caches**
-- **DLSS trusted path cache** — after 3 confirmations, fast `File.Exists` checks replace recursive scans
-- **DLSS skip cache** — games confirmed without DLSS are auto-exempted
-- **4-hour update cooldown** with rate-limit detection
-- **NVIDIA profile lookup cache** — expensive profile matching runs once per game per session
-- **Manifest skip lists** — skip known DLSS-free games immediately
+- **Мгновенный запуск из кеша** — список игр появляется сразу, полное сканирование идёт в фоне
+- **Параллельные проверки и развертывания наборов шейдеров**
+- **Кеши API на уровне PE и игры**
+- **Кеш доверенных путей DLSS** — после 3 подтверждений быстрые проверки `File.Exists` заменяют рекурсивное сканирование
+- **Кеш пропуска DLSS** — игры, где DLSS подтверждённо нет, автоматически исключаются
+- **4-часовая задержка обновлений** с определением лимитов запросов
+- **Кеш поиска профилей NVIDIA** — дорогое сопоставление профилей выполняется один раз на игру за сессию
+- **Списки пропуска в манифесте** — известные игры без DLSS пропускаются сразу
 
 ---
 
-## Data Storage
+## Хранение данных
 
-Everything under `%LocalAppData%\RHI\`:
+Всё внутри `%LocalAppData%\RHI\`:
 
-| Path | Contents |
+| Путь | Содержимое |
 |------|----------|
-| `game_library.json` | Detected games, hidden list, manually added games |
-| `installed.json` | RenoDX mod install records |
-| `aux_installed.json` | ReShade, ReLimiter, DC, OptiScaler install records |
-| `settings.json` | All settings and per-game overrides |
-| `downloads\` | Cached downloads (shaders, renodx, luma, misc) |
-| `DLSS\{ver}\`, `DLSS-D\{ver}\`, `DLSS-G\{ver}\`, `Streamline\{ver}\` | Versioned DLL caches |
-| `Custom\DLSS\`, `Custom\Streamline\`, `Custom\ReShade\` | User-provided DLLs |
-| `reshade\` | Staged shader packs and custom shaders |
-| `reshade-nightly\` | Nightly ReShade DLLs |
-| `dxvk-development\`, `dxvk-stable\`, `dxvk-lilium\` | DXVK staging per variant |
-| `LegacyReshade\{ver}\` | Legacy ReShade versions |
-| `optiscaler\` | OptiScaler staging |
-| `addons\` | Downloaded addon files + versions.json |
-| `logs\` | Session logs (max 10 kept) |
-| `nvidia_profiles_backup.json` | Profile Export data |
-| `nexus_baselines.json` | Nexus update tracking |
-| `dlss_trusted_paths.json` | DLSS fast-detection cache |
-| `dlss_scan_cache.json` | DLSS skip cache |
+| `game_library.json` | Найденные игры, список скрытых, вручную добавленные игры |
+| `installed.json` | Записи об установке модов RenoDX |
+| `aux_installed.json` | Записи об установке ReShade, ReLimiter, DC, OptiScaler |
+| `settings.json` | Все настройки и поигровые переопределения |
+| `downloads\` | Кешированные загрузки (шейдеры, renodx, luma, прочее) |
+| `DLSS\{ver}\`, `DLSS-D\{ver}\`, `DLSS-G\{ver}\`, `Streamline\{ver}\` | Кеши DLL по версиям |
+| `Custom\DLSS\`, `Custom\Streamline\`, `Custom\ReShade\` | Пользовательские DLL |
+| `reshade\` | Подготовленные наборы шейдеров и свои шейдеры |
+| `reshade-nightly\` | Ночные DLL ReShade |
+| `dxvk-development\`, `dxvk-stable\`, `dxvk-lilium\` | Хранилище DXVK по вариантам |
+| `LegacyReshade\{ver}\` | Старые версии ReShade |
+| `optiscaler\` | Хранилище OptiScaler |
+| `addons\` | Скачанные файлы аддонов + versions.json |
+| `logs\` | Логи сессий (хранится максимум 10) |
+| `nvidia_profiles_backup.json` | Данные экспорта профилей |
+| `nexus_baselines.json` | Отслеживание обновлений Nexus |
+| `dlss_trusted_paths.json` | Кеш быстрого обнаружения DLSS |
+| `dlss_scan_cache.json` | Кеш пропуска DLSS |
 
 ---
 
-## Troubleshooting
+## Решение проблем
 
-| Problem | Fix |
+| Проблема | Решение |
 |---------|-----|
-| Game not detected | Add Game in Settings or drag the exe onto the window |
-| Xbox games missing | Click Refresh |
-| ReShade not loading | Check install path via 📁 — DLL must be next to the game exe |
-| Black screen (Unreal) | ReShade → Add-ons → RenoDX → set R10G10B10A2_UNORM to output size |
-| UE-Extended not working | Enable HDR in the game's display settings first |
-| Downloads failing | Click Refresh, or clear cache from Settings |
-| DLSS presets not applying | Enable Admin Mode in Settings |
-| Driver settings greyed out | Enable Admin Mode — requires elevation |
-| Everything out of sync | Settings → Full Refresh |
+| Игра не найдена | «Добавить игру» в настройках или перетащите exe на окно |
+| Игры Xbox отсутствуют | Нажмите «Обновить» |
+| ReShade не загружается | Проверьте путь установки через 📁 — DLL должна лежать рядом с exe игры |
+| Чёрный экран (Unreal) | ReShade → Add-ons → RenoDX → задайте R10G10B10A2_UNORM = output size |
+| UE-Extended не работает | Сначала включите HDR в настройках экрана игры |
+| Ошибки загрузок | Нажмите «Обновить» или очистите кеш в настройках |
+| Пресеты DLSS не применяются | Включите режим администратора в настройках |
+| Настройки драйвера серые | Включите режим администратора — нужно повышение прав |
+| Всё разъехалось | Настройки → «Полное обновление» |
 
 ---
 
-## Third-Party Components
+## Сторонние компоненты
 
-| Component | Author | Licence |
+| Компонент | Автор | Лицензия |
 |-----------|--------|---------|
 | [ReShade](https://reshade.me) | Crosire | [BSD 3-Clause](https://github.com/crosire/reshade/blob/main/LICENSE.md) |
-| [RenoDX](https://github.com/clshortfuse/renodx) | clshortfuse & contributors | [MIT](https://github.com/clshortfuse/renodx/blob/main/LICENSE) |
-| [ReLimiter](https://github.com/RankFTW/ReLimiter) | RankFTW | Source-available |
+| [RenoDX](https://github.com/clshortfuse/renodx) | clshortfuse и соавторы | [MIT](https://github.com/clshortfuse/renodx/blob/main/LICENSE) |
+| [ReLimiter](https://github.com/RankFTW/ReLimiter) | RankFTW | открытый исходный код |
 | [Display Commander](https://github.com/pmnoxx/display-commander) | pmnoxx | [GPL-3](https://github.com/pmnoxx/display-commander/blob/main/LICENSE) |
 | [RE Framework](https://github.com/praydog/REFramework-nightly) | praydog | [MIT](https://github.com/praydog/REFramework/blob/master/LICENSE) |
-| [Luma Framework](https://github.com/Filoppi/Luma-Framework) | Pumbo (Filoppi) | Source-available |
-| [OptiScaler](https://github.com/optiscaler/OptiScaler) | OptiScaler contributors | Source-available |
-| [DXVK](https://github.com/doitsujin/dxvk) | doitsujin & contributors | [Zlib](https://github.com/doitsujin/dxvk/blob/master/LICENSE) |
+| [Luma Framework](https://github.com/Filoppi/Luma-Framework) | Pumbo (Filoppi) | открытый исходный код |
+| [OptiScaler](https://github.com/optiscaler/OptiScaler) | участники разработки OptiScaler | открытый исходный код |
+| [DXVK](https://github.com/doitsujin/dxvk) | doitsujin и соавторы | [Zlib](https://github.com/doitsujin/dxvk/blob/master/LICENSE) |
 | [DXVK HDR-mod](https://github.com/EndlesslyFlowering/dxvk) | EndlesslyFlowering (Lilium) | [Zlib](https://github.com/EndlesslyFlowering/dxvk/blob/HDR-mod/LICENSE) |
-| [7-Zip](https://www.7-zip.org/) | Igor Pavlov | [LGPL-2.1 / BSD-3-Clause](https://www.7-zip.org/license.txt) |
+| [7-Zip](https://www.7-zip.org/) | Игорь Павлов | [LGPL-2.1 / BSD-3-Clause](https://www.7-zip.org/license.txt) |
diff --git a/docs/DOF_FIX_IMPLEMENTATION.md b/docs/DOF_FIX_IMPLEMENTATION.md
index 8ef1ea5..9b91701 100644
--- a/docs/DOF_FIX_IMPLEMENTATION.md
+++ b/docs/DOF_FIX_IMPLEMENTATION.md
@@ -1,51 +1,51 @@
-# DOF Fix Component — Implementation Guide
+# Компонент DOF Fix — руководство по реализации
 
-## Current State
+## Текущее состояние
 
-Branch: `wip-cogs-and-dof`
+Ветка: `wip-cogs-and-dof`
 
-**Files that EXIST and are correct:**
-- `RenoDXCommander/Services/DofFixService.cs` — Full service implementation (untracked, needs `git add`)
-- `RenoDXCommander/Services/IDofFixService.cs` — Interface (untracked, needs `git add`)
-- `RenoDXCommander/ViewModels/GameCardViewModel.DofFix.cs` — ViewModel partial (untracked, needs `git add`)
+**Файлы, которые СУЩЕСТВУЮТ и корректны:**
+- `RenoDXCommander/Services/DofFixService.cs` — полная реализация сервиса (не отслеживается, нужен `git add`)
+- `RenoDXCommander/Services/IDofFixService.cs` — интерфейс (не отслеживается, нужен `git add`)
+- `RenoDXCommander/ViewModels/GameCardViewModel.DofFix.cs` — частичный ViewModel (не отслеживается, нужен `git add`)
 
-**Files that NEED modifications re-applied** (changes were lost during a git stash/branch switch):
+**Файлы, в которые НУЖНО заново внести правки** (изменения были потеряны при git stash/переключении ветки):
 
 ---
 
-## 1. `App.xaml.cs` — Register DofFixService in DI
+## 1. `App.xaml.cs` — зарегистрировать DofFixService в DI
 
-Find where other services are registered (look for `DxvkService` or `OptiScalerService` registration).
-Add:
+Найдите место регистрации других сервисов (ищите регистрацию `DxvkService` или `OptiScalerService`).
+Добавьте:
 ```csharp
 services.AddSingleton<DofFixService>();
 ```
 
 ---
 
-## 2. `ViewModels/MainViewModel.cs` — Add service field
+## 2. `ViewModels/MainViewModel.cs` — добавить поле сервиса
 
-Add field:
+Добавьте поле:
 ```csharp
 private readonly DofFixService _dofFixService;
 ```
 
-Add to constructor parameters and assign:
+Добавьте в параметры конструктора и присвойте:
 ```csharp
 _dofFixService = App.Services.GetRequiredService<DofFixService>();
 ```
 
 ---
 
-## 3. `ViewModels/MainViewModel.Init.cs` — Multiple changes
+## 3. `ViewModels/MainViewModel.Init.cs` — несколько изменений
 
-### a) Staging on startup (in `InitializeAsync`, parallel task block)
-Add alongside other staging tasks:
+### a) Подготовка хранилища при запуске (в `InitializeAsync`, блок параллельных задач)
+Добавьте рядом с остальными задачами подготовки:
 ```csharp
 Task.Run(() => _dofFixService.EnsureStagingAsync())
 ```
 
-### b) Manifest wiring (after manifest fetch, where `ApplyManifestPresets` is called — TWO places: init path and background scan path)
+### b) Привязка манифеста (после загрузки манифеста, там где вызывается `ApplyManifestPresets` — ДВА места: путь инициализации и путь фонового сканирования)
 ```csharp
 _dofFixService.SetSkipGames(_manifest?.DofFixSkipGames);
 _dofFixService.SetForceGames(_manifest?.DofFixForceGames);
@@ -53,7 +53,7 @@ if (_manifest?.ComponentUrls?.TryGetValue("ueDofFix", out var dofFixUrl) == true
     _dofFixService.ManifestUrlOverride = dofFixUrl;
 ```
 
-### c) Detection in `BuildCards` (background scan, after DXVK detection block)
+### c) Определение в `BuildCards` (фоновое сканирование, после блока DXVK)
 ```csharp
 // DOF Fix detection
 newCard.IsDofFixEligible = _dofFixService.IsGameEligible(newCard.EngineHint, newCard.Is32Bit, game.Name);
@@ -67,14 +67,14 @@ if (newCard.IsDofFixEligible && !string.IsNullOrEmpty(installPath) && Directory.
 }
 ```
 
-### d) Detection in `LoadCacheAndBuildCardsAsync` (cached startup, after DXVK block)
-Same code as above.
+### d) Определение в `LoadCacheAndBuildCardsAsync` (стартовый путь из кеша, после блока DXVK)
+Тот же код, что и выше.
 
 ---
 
-## 4. `MainWindow.xaml` — Add DOF Fix row
+## 4. `MainWindow.xaml` — добавить строку DOF Fix
 
-Insert BETWEEN the `DetailOptionalSeparator` and the OptiScaler row (`DetailOsRow`):
+Вставьте МЕЖДУ `DetailOptionalSeparator` и строкой OptiScaler (`DetailOsRow`):
 
 ```xml
 <!-- DOF Fix row -->
@@ -131,21 +131,21 @@ Insert BETWEEN the `DetailOptionalSeparator` and the OptiScaler row (`DetailOsRo
 
 ---
 
-## 5. `MainWindow.Events.cs` — Click handlers
+## 5. `MainWindow.Events.cs` — обработчики кликов
 
-Add these handlers (use existing component handlers as reference for pattern):
+Добавьте эти обработчики (образец — существующие обработчики компонентов):
 
-- `InstallDofFixButton_Click` — calls `_dofFixService.InstallAsync(card.InstallPath, progress)`, updates card status
-- `UninstallDofFixButton_Click` — calls `_dofFixService.Uninstall(card.InstallPath)`, clears card status
-- `DofFixInfoButton_Click` — shows ContentDialog with `_dofFixService.ReleaseNotes` (fetched from GitHub)
-- `DofFixCogButton_Click` — placeholder ContentDialog ("No settings available")
-- `DetailDofFixStatus_PointerPressed` — opens `_dofFixService.GetReleaseUrl(version)` in browser
+- `InstallDofFixButton_Click` — вызывает `_dofFixService.InstallAsync(card.InstallPath, progress)`, обновляет статус карточки
+- `UninstallDofFixButton_Click` — вызывает `_dofFixService.Uninstall(card.InstallPath)`, очищает статус карточки
+- `DofFixInfoButton_Click` — показывает ContentDialog с `_dofFixService.ReleaseNotes` (загружается с GitHub)
+- `DofFixCogButton_Click` — заглушка ContentDialog («Настройки недоступны»)
+- `DetailDofFixStatus_PointerPressed` — открывает `_dofFixService.GetReleaseUrl(version)` в браузере
 
 ---
 
-## 6. `DetailPanelBuilder.Components.cs` — Row population
+## 6. `DetailPanelBuilder.Components.cs` — наполнение строки
 
-In `UpdateDetailComponentRows`, add DOF Fix row logic (between Optional separator visibility and OptiScaler):
+В `UpdateDetailComponentRows` добавьте логику строки DOF Fix (между видимостью разделителя «Необязательные» и OptiScaler):
 
 ```csharp
 // DOF Fix row
@@ -184,21 +184,21 @@ _window.DetailDofFixMessage.Text = card.DofFixActionMessage;
 _window.DetailDofFixMessage.Foreground = UIFactory.GetBrush(GetMessageColor(card.DofFixActionMessage));
 ```
 
-Also update `DetailOptionalSeparator.Visibility` to show when DOF Fix OR OptiScaler is visible.
+Также обновите `DetailOptionalSeparator.Visibility` — показывать, когда видим DOF Fix ИЛИ OptiScaler.
 
 ---
 
-## 7. `Services/UpdateOrchestrationService.cs` — Update All
+## 7. `Services/UpdateOrchestrationService.cs` — «Обновить всё»
 
-Add a `UpdateAllDofFixAsync` method (same pattern as other UpdateAll methods).
-Call it from the main Update All flow.
-Add to `IUpdateOrchestrationService.cs` interface.
+Добавьте метод `UpdateAllDofFixAsync` (по образцу остальных UpdateAll-методов).
+Вызовите его из основного потока «Обновить всё».
+Добавьте в интерфейс `IUpdateOrchestrationService.cs`.
 
 ---
 
-## 8. `Models/RemoteManifest.cs` — Manifest fields
+## 8. `Models/RemoteManifest.cs` — поля манифеста
 
-Add:
+Добавьте:
 ```csharp
 [JsonPropertyName("dofFixSkipGames")]
 public List<string>? DofFixSkipGames { get; set; }
@@ -209,9 +209,9 @@ public List<string>? DofFixForceGames { get; set; }
 
 ---
 
-## 9. `Services/AddonFileWatcher.cs` — Exclusion
+## 9. `Services/AddonFileWatcher.cs` — исключение
 
-In the addon detection check (where filenames starting with `renodx-` trigger detection), add exclusion:
+В проверке обнаружения аддонов (где имена, начинающиеся с `renodx-`, запускают обнаружение), добавьте исключение:
 ```csharp
 if (fileName.StartsWith("renodx-universal_ue_dof_fix", StringComparison.OrdinalIgnoreCase))
     return;
@@ -219,17 +219,17 @@ if (fileName.StartsWith("renodx-universal_ue_dof_fix", StringComparison.OrdinalI
 
 ---
 
-## 10. `DragDropHandler.Addon.cs` + `MainViewModel.Install.cs` — Exclusion from mod replace
+## 10. `DragDropHandler.Addon.cs` + `MainViewModel.Install.cs` — исключение из замены модов
 
-Add `"renodx-universal_ue_dof_fix"` to the exclusion list alongside `renodx-dlssfix` and `renodx-devkit` in both:
-- The "existing addon" warning check
-- The removal loop
+Добавьте `"renodx-universal_ue_dof_fix"` в список исключений рядом с `renodx-dlssfix` и `renodx-devkit` в обоих файлах:
+- Проверка предупреждения «существующий аддон»
+- Цикл удаления
 
 ---
 
 ## 11. `GameCardViewModel.UI.cs` — UpdateBadgeVisibility
 
-Add DOF Fix to the update badge computation:
+Добавьте DOF Fix в вычисление значка обновления:
 ```csharp
 || (DofFixStatus == GameStatus.UpdateAvailable && !ExcludeFromUpdateAllDofFix)
 ```
@@ -238,25 +238,25 @@ Add DOF Fix to the update badge computation:
 
 ## 12. `MainViewModel.Settings.cs` — AnyUpdateAvailable
 
-Add DOF Fix to the `AnyUpdateAvailable` property:
+Добавьте DOF Fix в свойство `AnyUpdateAvailable`:
 ```csharp
 || (c.DofFixStatus == GameStatus.UpdateAvailable && !c.ExcludeFromUpdateAllDofFix)
 ```
 
 ---
 
-## Key Technical Details
+## Ключевые технические детали
 
-- **Addon filename**: `renodx-universal_ue_dof_fix.addon64`
-- **GitHub tag pattern**: `ue-dof-fix-{version}` (e.g. `ue-dof-fix-1.0.0`)
+- **Имя файла аддона**: `renodx-universal_ue_dof_fix.addon64`
+- **Схема тегов GitHub**: `ue-dof-fix-{version}` (например, `ue-dof-fix-1.0.0`)
 - **GitHub API**: `https://api.github.com/repos/RankFTW/rhi-repo/releases`
-- **Download URL pattern**: `https://github.com/RankFTW/rhi-repo/releases/download/ue-dof-fix-{version}/renodx-universal_ue_dof_fix.addon64`
-- **Staging dir**: `%LocalAppData%\RHI\ue-dof-fix\` with `version.txt`
-- **Eligibility**: UE 5.0–5.6 AND 64-bit AND not in skip list, OR in force list (still requires 64-bit)
-- **Version link URL**: `https://github.com/RankFTW/rhi-repo/releases/tag/ue-dof-fix-{version}`
-- **Manifest URL override key**: `componentUrls.ueDofFix`
+- **Схема ссылок скачивания**: `https://github.com/RankFTW/rhi-repo/releases/download/ue-dof-fix-{version}/renodx-universal_ue_dof_fix.addon64`
+- **Каталог хранилища**: `%LocalAppData%\RHI\ue-dof-fix\` с `version.txt`
+- **Условия применимости**: UE 5.0–5.6 И 64-бит И не в списке пропуска, ИЛИ в списке форсированных (64-бит всё равно требуется)
+- **Ссылка на версию**: `https://github.com/RankFTW/rhi-repo/releases/tag/ue-dof-fix-{version}`
+- **Ключ переопределения URL в манифесте**: `componentUrls.ueDofFix`
 
-## Build Verification
+## Проверка сборки
 
-After all changes: `dotnet build RenoDXCommander\RenoDXCommander.csproj --no-restore -v quiet`
-Must be 0 errors.
+После всех изменений: `dotnet build RenoDXCommander\RenoDXCommander.csproj --no-restore -v quiet`
+Ошибок быть не должно (0 errors).
diff --git a/docs/INSTALLER-RU.md b/docs/INSTALLER-RU.md
new file mode 100644
index 0000000..ea065dc
--- /dev/null
+++ b/docs/INSTALLER-RU.md
@@ -0,0 +1,48 @@
+# Сборка русифицированного инсталлятора RHI
+
+Само приложение внутри инсталлятора уже русифицировано (переведены исходники
+интерфейса). Русифицирован и мастер установки: `RHI Setup.iss` использует
+встроенный язык `Russian.isl` из Inno Setup 6.
+
+## Что нужно
+
+- Windows 10/11 x64
+- [.NET 8 SDK](https://dotnet.microsoft.com/download/dotnet/8.0)
+- [Inno Setup 6](https://jrsoftware.org/isdl.php) (ставится в `C:\Program Files (x86)\Inno Setup 6`)
+
+## Способ 1 — локально, одним скриптом
+
+```bat
+build-setup.bat
+```
+
+Скрипт: закрывает запущенный RHI → `dotnet publish` WinUI-приложения →
+собирает DropHelper → копирует сопутствующие файлы → компилирует
+`Installers\RHI-Setup.exe`.
+
+## Способ 2 — вручную
+
+```bat
+publish.bat
+"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "RHI Setup.iss"
+```
+
+`publish.bat` публикует в `C:\Users\Mark\...` (пути автора по умолчанию).
+Пути инсталлятора переопределяются через `/D`:
+
+```bat
+ISCC.exe /DPublishDir="C:\путь\к\publish\RHI" /DIconFile="C:\путь\к\icon.ico" /DInstallerOutDir="C:\путь\к\Installers" "RHI Setup.iss"
+```
+
+## Способ 3 — GitHub Actions (без своего Windows-окружения)
+
+1. Запушьте репозиторий (с русификацией и `.github/workflows/build-installer-ru.yml`) в свой форк.
+2. Вкладка **Actions** → «Сборка инсталлятора (RU)» → **Run workflow**.
+3. Скачайте артефакт **RHI-Setup-RU** — это готовый `RHI-Setup.exe`.
+
+## Примечания
+
+- Версия в заголовке установщика задаётся `#define MyAppVersion` в `RHI Setup.iss`
+  (автор обновляет её вручную при релизах).
+- Чтобы вернуть в мастере выбор языка (русский/английский), раскомментируйте
+  строку `Name: "english"` в секции `[Languages]`.
diff --git a/docs/NEW_FEATURES_GUIDE.md b/docs/NEW_FEATURES_GUIDE.md
index 9adecf8..e6cb5da 100644
--- a/docs/NEW_FEATURES_GUIDE.md
+++ b/docs/NEW_FEATURES_GUIDE.md
@@ -1,227 +1,227 @@
-# RHI v2.0.0 — New Features Guide
+# RHI v2.0.0 — руководство по новым возможностям
 
-A walkthrough of every major new feature in v2.0.0 and how to use it.
+Обзор всех крупных нововведений v2.0.0 и как ими пользоваться.
 
 ---
 
-## Nvidia Profile Overrides
+## Переопределения профиля NVIDIA
 
-The biggest addition in v2.0.0. A new section below Game Overrides in the detail panel lets you control NVIDIA driver profile settings per-game — no NVIDIA Profile Inspector needed.
+Крупнейшее пополнение в v2.0.0. Новый раздел под «Переопределениями игры» на панели игры даёт управление настройками профиля драйвера NVIDIA для каждой игры — без NVIDIA Profile Inspector.
 
-### DLSS / Streamline Row
+### Строка DLSS / Streamline
 
-Visible for any game with DLSS or Streamline DLLs detected.
+Видна для любой игры, у которой обнаружены DLL DLSS или Streamline.
 
-**Version Swapping:**
-1. Select a game with DLSS installed
-2. Click the Version dropdown for SR, RR, FG, or Streamline
-3. Pick any version — it downloads on-demand if not cached
-4. The game's DLL is swapped immediately
-5. Select the item marked `(Default)` to restore the original
+**Подмена версий:**
+1. Выберите игру с установленным DLSS
+2. Откройте список версий для SR, RR, FG или Streamline
+3. Выберите любую версию — при отсутствии в кеше она скачается по требованию
+4. DLL игры подменяется немедленно
+5. Пункт `(Default)` возвращает оригинал
 
-**Presets:**
-1. Open the Preset dropdown (below Version) for SR, RR, or FG
-2. Choose a preset (SR: J/K/L/M, RR: D/E, FG: A/B)
-3. Applied instantly to the NVIDIA driver profile — no game restart needed
+**Пресеты:**
+1. Откройте список пресетов (под версиями) для SR, RR или FG
+2. Выберите пресет (SR: J/K/L/M, RR: D/E, FG: A/B)
+3. Применяется мгновенно к профилю драйвера NVIDIA — перезапуск игры не нужен
 
-**Render Scale:**
-1. Open the Render Scale dropdown for SR or RR
-2. Pick a percentage (33%–100%) or a named preset (DLAA, Quality, etc.)
-3. Forces the DLSS internal render resolution for that game
+**Масштаб рендеринга:**
+1. Откройте список «Масштаб рендеринга» для SR или RR
+2. Выберите процент (33–100%) или именной пресет (DLAA, Quality и т.д.)
+3. Принудительно задаёт внутреннее разрешение рендеринга DLSS для этой игры
 
-**Quick Apply:**
-1. Go to Settings → DLSS & Streamline Defaults → Configure Defaults
-2. Set your preferred versions, presets, and render scales
-3. On any game, click "Quick Apply" — stamps all your defaults in one click
+**Быстрое применение:**
+1. Откройте Настройки → DLSS и Streamline по умолчанию → «Настроить значения по умолчанию»
+2. Задайте предпочитаемые версии, пресеты и масштабы рендеринга
+3. В любой игре нажмите «Быстрое применение» — все значения по умолчанию применятся одним кликом
 
-### Driver Settings Row
+### Строка настроек драйвера
 
-Controls per-game NVIDIA driver profile settings. Requires Admin Mode (see below).
+Управляет настройками профиля драйвера NVIDIA для каждой игры. Требует режима администратора (см. ниже).
 
-| Column | What you can change |
-|--------|-------------------|
-| VSync | Mode, Tear Control, Low Latency (Off/On/Ultra) |
-| Smooth Motion | Enable, Allowed APIs, Flip Pacing |
-| Power | Power Mode (Adaptive/Optimal/Max Performance) |
-| ReBAR | Enable (Off/On/Global), Mode (Standard/Optimized), Size Limit |
+| Колонка | Что можно менять |
+|---------|------------------|
+| VSync | Режим, управление разрывами, Low Latency (выкл/вкл/Ultra) |
+| Smooth Motion | Включение, разрешённые API, Flip Pacing |
+| Питание | Режим питания (Adaptive/Optimal/Max Performance) |
+| ReBAR | Включение (выкл/вкл/глобально), режим (Standard/Optimized), лимит размера |
 
-All changes are instant — written directly to the game's NVIDIA driver profile.
+Все изменения мгновенны — записываются прямо в профиль драйвера NVIDIA игры.
 
 ---
 
-## Admin Mode
+## Режим администратора
 
-Some NVIDIA settings (ReBAR, Low Latency Ultra, Smooth Motion APIs) require admin privileges.
+Некоторые настройки NVIDIA (ReBAR, Low Latency Ultra, API Smooth Motion) требуют прав администратора.
 
-**How to enable:**
-1. Go to Settings → Data & Custom Files
-2. Set Admin Mode to **On**
-3. Accept the one-time UAC prompt
-4. RHI will now auto-launch elevated on every startup — no more UAC prompts
+**Как включить:**
+1. Откройте Настройки → Данные и пользовательские файлы
+2. Переведите «Режим администратора» в положение **Вкл**
+3. Подтвердите разовый запрос UAC
+4. Теперь RHI будет сам запускаться с повышенными правами — больше никаких запросов UAC
 
-**How to disable:**
-1. Set Admin Mode back to **Off** — deletes the scheduled task
+**Как отключить:**
+1. Верните «Режим администратора» в **Выкл** — назначенное задание будет удалено
 
-When not elevated, the Driver Settings row is greyed out with a warning.
+Без повышенных прав строка настроек драйвера затемняется с предупреждением.
 
 ---
 
-## Multi Frame Generation (MFG)
+## Мульти-генерация кадров (MFG)
 
-Per-game control over NVIDIA's Multi Frame Generation. RTX 50 Series (Blackwell) only.
+Управление мульти-генерацией кадров NVIDIA для каждой игры. Только RTX 50-я серия (Blackwell).
 
-**How to use:**
-1. Select a game with Frame Generation detected
-2. Click the "Multi Frame Gen" button below the FG column
-3. Choose a mode:
-   - **Fixed** — constant frame multiplier (2x–6x)
-   - **Dynamic** — adaptive multiplier with a target frame rate
-4. For Dynamic mode, set the Target FPS from the VRR cap presets (matched to common monitor refresh rates) or pick any custom FPS value
+**Как пользоваться:**
+1. Выберите игру, у которой определена генерация кадров
+2. Нажмите кнопку «Мульти-генерация кадров» под колонкой FG
+3. Выберите режим:
+   - **Фиксированный** — постоянный множитель кадров (2x–6x)
+   - **Динамический** — адаптивный множитель с целевой частотой кадров
+4. Для динамического режима задайте целевой FPS из пресетов лимитов VRR (подогнаны под распространённые частоты мониторов) или введите любое своё значение
 
-**Requirements:** RTX 50 Series GPU, driver 572.16+ (Fixed) or 595.97+ (Dynamic).
+**Требования:** GPU RTX 50-й серии, драйвер 572.16+ (фиксированный) или 595.97+ (динамический).
 
 ---
 
-## DLSS & Streamline Defaults + Quick Apply
+## DLSS и Streamline по умолчанию + быстрое применение
 
-Configure your preferred settings once, apply to any game instantly.
+Настройте предпочитаемые параметры один раз — применяйте к любой игре мгновенно.
 
-**Setup:**
-1. Go to Settings → DLSS & Streamline Defaults → "Configure Defaults"
-2. Set your preferred Version, Preset, and Render Scale for each component
-3. Leave anything at "Default (don't change)" to skip that component
+**Настройка:**
+1. Откройте Настройки → DLSS и Streamline по умолчанию → «Настроить значения по умолчанию»
+2. Задайте предпочитаемые версию, пресет и масштаб рендеринга для каждого компонента
+3. Оставьте «По умолчанию (не менять)», чтобы пропустить компонент
 
-**Usage:**
-1. Select any game with DLSS/Streamline
-2. Click "Quick Apply" in the DLSS/Streamline row
-3. All your configured defaults are applied in one click (downloads on-demand)
+**Использование:**
+1. Выберите любую игру с DLSS/Streamline
+2. Нажмите «Быстрое применение» в строке DLSS/Streamline
+3. Все заданные значения применятся одним кликом (докачиваются по требованию)
 
 ---
 
-## Global Nvidia Settings
+## Глобальные настройки NVIDIA
 
-System-wide NVIDIA settings that affect all games. Located in Settings page.
+Общесистемные настройки NVIDIA, влияющие на все игры. Находятся на странице настроек.
 
-| Setting | What it does |
-|---------|-------------|
-| Shader Cache Size | How much disk space the driver uses for shader caching |
-| Shader Pre-Compile | Background shader compilation aggressiveness |
-| G-Sync Mode | Off / Fullscreen only / Fullscreen/Windowed |
-| Preferred Refresh Rate | App Setting / Highest available |
-| Global ReBAR | On/Off + Size Limit (applies to all games) |
-| DLSS On-Screen Indicator | The NVIDIA DLSS text overlay in game corners |
+| Настройка | За что отвечает |
+|-----------|-----------------|
+| Размер кеша шейдеров | Сколько места на диске драйвер отводит под кеш шейдеров |
+| Предкомпиляция шейдеров | Агрессивность фоновой компиляции шейдеров |
+| Режим G-Sync | Выкл / только полноэкранный / полноэкранный+оконный |
+| Предпочитаемая частота обновления | Настройка игры / наивысшая доступная |
+| Глобальный ReBAR | Вкл/выкл + лимит размера (для всех игр) |
+| Экранный индикатор DLSS | Текстовый оверлей NVIDIA DLSS в углу игр |
 
-ReBAR settings require Admin Mode.
+Настройки ReBAR требуют режима администратора.
 
 ---
 
-## Profile Export / Import
+## Экспорт / импорт профилей
 
-Back up your per-game NVIDIA profile settings and restore them after driver updates.
+Резервное копирование индивидуальных настроек профилей NVIDIA и их восстановление после обновлений драйвера.
 
-**Export:**
-1. Go to Settings → Global Nvidia Settings → "Export Profiles"
-2. All per-game settings + global settings are saved to `%LocalAppData%\RHI\nvidia_profiles_backup.json`
+**Экспорт:**
+1. Откройте Настройки → Глобальные настройки NVIDIA → «Экспортировать профили»
+2. Все индивидуальные + общие настройки сохранятся в `%LocalAppData%\RHI\nvidia_profiles_backup.json`
 
-**Import:**
-1. Click "Import Profiles"
-2. Missing profiles are created, exe associations registered, and all settings restored
+**Импорт:**
+1. Нажмите «Восстановить профили»
+2. Отсутствующие профили будут созданы, связи с exe зарегистрированы, все настройки восстановлены
 
-**Reset All:**
-1. Click "Reset All Game Profiles to Default" (red outline button)
-2. Confirm in the warning dialog
-3. ALL per-game AND global NVIDIA settings are wiped to factory defaults
+**Сбросить всё:**
+1. Нажмите «Сбросить профили всех игр» (кнопка с красной обводкой)
+2. Подтвердите в диалоге предупреждения
+3. ВСЕ индивидуальные И общие настройки NVIDIA будут сброшены до заводских
 
 ---
 
 ## Lilium HDR DXVK
 
-DXVK's HDR fork now uses Vulkan layer ReShade on DX9 games, enabling SM5 HDR shaders that don't work in DX9 mode.
+HDR-форк DXVK теперь использует слой Vulkan ReShade в DX9-играх, включая HDR-шейдеры SM5, которые не работают в DX9-режиме.
 
-**How to use:**
-1. Select a DX8/DX9/DX10 game
-2. In the DXVK dropdown (Game Overrides), select "Lilium HDR"
-3. DXVK installs with HDR swap chain upgrades enabled
-4. A "Lilium HDR Preset" dropdown appears — choose how aggressive the HDR upgrades are:
-   - **Safest** — swap chain only (default, ~100% compatible)
-   - **2nd Safest** — adds back buffer upgrade
-   - **Slightly Unsafe** → **Experimental** — progressively adds render target upgrades
+**Как пользоваться:**
+1. Выберите игру на DX8/DX9/DX10
+2. В списке DXVK (Переопределения игры) выберите «Lilium HDR»
+3. DXVK установится с включёнными апгрейдами HDR-цепочки обмена
+4. Появится список «Пресет Lilium HDR» — выберите агрессивность HDR-апгрейдов:
+   - **Safest** — только цепочка обмена (по умолчанию, ~100% совместимость)
+   - **2nd Safest** — добавляет апгрейд буферов
+   - **Slightly Unsafe** → **Experimental** — постепенно добавляет апгрейды целей рендеринга
 
-Changing the preset immediately rewrites `dxvk.conf` — no reinstall needed.
+Смена пресета сразу перезаписывает `dxvk.conf` — переустановка не нужна.
 
-**On uninstall:** ReShade is automatically restored as the local DLL.
+**При удалении:** ReShade автоматически возвращается как локальная DLL.
 
 ---
 
-## Batch DLSS & Streamline Deploy
+## Массовое развертывание DLSS и Streamline
 
-Update DLSS/Streamline versions and presets across multiple games at once.
+Обновление версий и пресетов DLSS/Streamline сразу в нескольких играх.
 
-**How to use:**
-1. Go to Settings → "Batch Deploy"
-2. Select games from the checklist (all games are selectable — v1.x components are skipped per-component)
-3. Pick versions for SR, RR, FG, and Streamline (or "None" to skip)
-4. Pick presets for SR, RR, FG
-5. Enable "Auto-create NVIDIA profiles" if desired
-6. Click "Deploy" — shows progress per game
+**Как пользоваться:**
+1. Откройте Настройки → «Массовое развертывание»
+2. Отметьте игры в чек-листе (выбираются все; компоненты v1.x пропускаются индивидуально)
+3. Выберите версии для SR, RR, FG и Streamline (или «None», чтобы пропустить)
+4. Выберите пресеты для SR, RR, FG
+5. При желании включите «Автосоздание профилей NVIDIA»
+6. Нажмите «Развернуть» — прогресс показывается по каждой игре
 
-Games already at the selected version are automatically skipped.
+Игры, уже находящиеся на выбранной версии, пропускаются автоматически.
 
 ---
 
-## Simple View (formerly Compact)
+## Простой вид (бывший компактный)
 
-The default view for fresh installs. A paged layout split across 3 pages:
+Вид по умолчанию при свежей установке. Постраничная раскладка из 3 страниц:
 
-- **Page 1** — Components (game info, install buttons)
-- **Page 2** — Game Overrides (DLL naming, shaders, addons, DXVK, RS channel)
-- **Page 3** — Nvidia Profile Overrides + Management
+- **Страница 1** — компоненты (информация об игре, кнопки установки)
+- **Страница 2** — переопределения игры (имена DLL, шейдеры, аддоны, DXVK, канал RS)
+- **Страница 3** — переопределения профиля NVIDIA + управление
 
-Use the arrow buttons on the sides to navigate between pages.
+Для перехода между страницами используйте стрелки по бокам.
 
 ---
 
-## DLL Naming Override in Luma Mode
+## Переопределение имени DLL в режиме Luma
 
-Previously locked when Luma was active. Now you can rename the ReShade DLL (`dxgi.dll`) to any custom filename even in Luma mode — useful for games that need a specific DLL name.
+Раньше блокировалось при активном Luma. Теперь можно переименовать DLL ReShade (`dxgi.dll`) в любое своё имя даже в режиме Luma — полезно для игр, требующих конкретное имя DLL.
 
 ---
 
-## Restore Profile Defaults (Per-Game)
+## Восстановление значений профиля (для каждой игры)
 
-In the Driver Settings row of any game's Nvidia Profile Overrides section:
+В строке настроек драйвера в разделе переопределений профиля NVIDIA любой игры:
 
-1. Click "Restore Defaults"
-2. Confirm the warning
-3. That game's NVIDIA profile is wiped to factory defaults (also restores DLSS/SL DLLs)
+1. Нажмите «Восстановить значения по умолчанию»
+2. Подтвердите предупреждение
+3. Профиль NVIDIA этой игры будет сброшен до заводских (также восстанавливаются DLL DLSS/SL)
 
 ---
 
-## DLSS Driver Override Detection
+## Определение переопределения драйвера DLSS
 
-When NVIDIA App has "Latest DLL" or "Use recommended preset" enabled:
-- Affected dropdowns are greyed out with "⚠ Driver Override Active" text
-- Tooltips explain how to disable in NVIDIA App
-- Quick Apply skips overridden components
+Когда в NVIDIA App включены «Latest DLL» или «Use recommended preset»:
+- Затронутые списки затемняются с текстом «⚠ Активно переопределение драйвера»
+- Подсказки объясняют, как отключить это в NVIDIA App
+- «Быстрое применение» пропускает переопределённые компоненты
 
 ---
 
-## Manifest-Driven Updates
+## Обновления через манифест
 
-Several features can now be updated server-side without an app release:
-- **Shader packs** — add, remove, or change download URLs
-- **DLSS presets** — add new presets when NVIDIA releases them
-- **Addon packs** — add, disable, or update addon entries
-- **Component URLs** — redirect downloads if repos move
+Ряд функций теперь обновляется серверно, без релиза приложения:
+- **Наборы шейдеров** — добавление, удаление, смена ссылок
+- **Пресеты DLSS** — добавление новых пресетов по мере выхода от NVIDIA
+- **Паки аддонов** — добавление, отключение, обновление записей
+- **Ссылки компонентов** — перенаправление загрузок при переезде репозиториев
 
-These happen automatically on every launch — no user action needed.
+Всё это происходит автоматически при каждом запуске — от пользователя ничего не требуется.
 
 ---
 
-## Tips
+## Советы
 
-- **Admin Mode is recommended** if you want full control over driver settings (ReBAR, Low Latency Ultra, Smooth Motion)
-- **Export your profiles** before driver updates — NVIDIA resets custom profiles on every major driver install
-- **Quick Apply** is the fastest way to configure a new game — set your defaults once, stamp everywhere
-- **VRR cap values** in the DMFG Target FPS dropdown are calculated for common monitor refresh rates — pick the one matching your display for optimal frame pacing
-- **Lilium HDR Preset "Safest"** is recommended unless you know your game benefits from render target upgrades
+- **Режим администратора рекомендуется**, если нужен полный контроль над настройками драйвера (ReBAR, Low Latency Ultra, Smooth Motion)
+- **Экспортируйте профили** перед обновлениями драйвера — NVIDIA сбрасывает пользовательские профили при каждой крупной установке драйвера
+- **Быстрое применение** — самый быстрый способ настроить новую игру: задайте значения один раз и штампуйте где угодно
+- **Значения лимитов VRR** в списке целевого FPS DMFG рассчитаны под распространённые частоты мониторов — выберите совпадающий с вашим дисплеем для оптимального вывода кадров
+- **Пресет Lilium HDR «Safest»** рекомендуется, если точно не знаете, что вашей игре нужны апгрейды целей рендеринга
diff --git a/docs/NEXUS_README.md b/docs/NEXUS_README.md
index 323bbfc..7f86d3e 100644
--- a/docs/NEXUS_README.md
+++ b/docs/NEXUS_README.md
@@ -1,165 +1,165 @@
 [center][size=6][b]RHI — ReShade HDR Installer[/b][/size]
 
-[i]Automatic HDR modding, DLSS management, and NVIDIA driver control for your entire PC game library.[/i][/center]
+[i]Автоматический HDR-моддинг, управление DLSS и настройками драйвера NVIDIA для всей вашей библиотеки ПК-игр.[/i][/center]
 
-RHI scans all your games from Steam, Epic, GOG, Xbox Game Pass, EA, Ubisoft, Battle.net, Rockstar, and itch.io — then handles everything in one click.
+RHI сканирует все ваши игры из Steam, Epic, GOG, Xbox Game Pass, EA, Ubisoft, Battle.net, Rockstar и itch.io — а затем делает всё остальное в один клик.
 
-[size=5][b]HDR for Every Game[/b][/size]
+[size=5][b]HDR для каждой игры[/b][/size]
 
-[b]Bespoke mods (RenoDX)[/b] — hundreds of game-specific HDR mods matched and installed automatically. If your game has one, RHI finds it and keeps it updated.
+[b]Именные моды (RenoDX)[/b] — сотни HDR-модов под конкретные игры, которые подбираются и устанавливаются автоматически. Если для вашей игры мод есть, RHI его найдёт и будет поддерживать актуальным.
 
-[b]Unreal Engine games[/b] — every UE game gets HDR via the UE-Extended addon. RHI writes the correct Engine.ini settings and reshade.ini configuration automatically. You don't touch any ini files.
+[b]Игры на Unreal Engine[/b] — каждая UE-игра получает HDR через аддон UE-Extended. RHI сам записывает правильные настройки Engine.ini и конфигурацию reshade.ini. Вам не придётся трогать ни один ini-файл.
 
-[b]Luma Framework[/b] — every DX11 Unreal Engine game in your library shows a Luma option automatically. Game-specific Engine.ini keys, launch arguments, and TAA settings applied from the wiki on install without any manual steps.
+[b]Luma Framework[/b] — у каждой игры на DX11 Unreal Engine в библиотеке автоматически появляется вариант Luma. Игровые ключи Engine.ini, аргументы запуска и настройки TAA применяются из вики при установке без каких-либо ручных шагов.
 
-[b]DX9 / older games[/b] — DXVK + Lilium HDR brings scRGB HDR output to games with no native HDR path.
+[b]DX9 / старые игры[/b] — DXVK + Lilium HDR добавляет вывод scRGB HDR в игры без нативного HDR-пути.
 
-[b]Everything else[/b] — RenoFX HDR Toolkit converts SDR to HDR for any remaining game.
+[b]Всё остальное[/b] — RenoFX HDR Toolkit превращает SDR в HDR для любой оставшейся игры.
 
 [line]
 
-[size=5][b]Replaces the ReShade Installer[/b][/size]
+[size=5][b]Заменяет установщик ReShade[/b][/size]
 
-RHI handles ReShade end-to-end. No manual downloads, no DLL naming guesswork, no ini editing.
+RHI ведёт ReShade от начала до конца. Без ручных загрузок, без гаданий с именами DLL, без правки ini.
 
 [list]
-[*]Detects each game's graphics API automatically and names the DLL correctly (dxgi.dll, d3d9.dll, opengl32.dll)
-[*]Latest stable and nightly builds always ready — staged on app launch
-[*]Per-game build channels: Stable, Nightly, Custom, Legacy, or No Addons — mix freely across your library
-[*]Vulkan games handled via global implicit layer — one-click install
-[*]Foreign DLL detection: warns before overwriting DXVK, Special K, or ENB
-[*]Manages reshade.ini automatically — hotkeys, screenshot path, peak brightness, DLSS paths all configured
+[*]Определяет графический API каждой игры автоматически и даёт DLL правильное имя (dxgi.dll, d3d9.dll, opengl32.dll)
+[*]Свежие стабильные и ночные сборки всегда наготове — загружаются при запуске приложения
+[*]Каналы сборки для каждой игры: Stable, Nightly, Custom, Legacy или No Addons — свободно комбинируются по библиотеке
+[*]Vulkan-игры обслуживаются через глобальный implicit-слой — установка в один клик
+[*]Обнаружение чужих DLL: предупреждает перед перезаписью DXVK, Special K или ENB
+[*]Сам ведёт reshade.ini — горячие клавиши, путь к скриншотам, пиковая яркость и пути DLSS настроены
 [/list]
 
 [line]
 
-[size=5][b]DLSS 5 and Neural Rendering[/b][/size]
+[size=5][b]DLSS 5 и нейронный рендеринг[/b][/size]
 
-[b]DLSS5 Feeder[/b] — one click deploys the full neural rendering pipeline: DLSS5 consumer addon, nvngx_dlssnr.dll, pre-configured ReShade preset, and required shader packs. Automatically selects the right method for your game (DX11, DX12, 32-bit).
+[b]DLSS5 Feeder[/b] — один клик разворачивает весь конвейер нейронного рендеринга: аддон-потребитель DLSS5, nvngx_dlssnr.dll, преднастроенный пресет ReShade и нужные наборы шейдеров. Сам выбирает подходящий метод для вашей игры (DX11, DX12, 32 бита).
 
-[b]NR Cost Scaler[/b] — runs neural rendering at 75% cost by default. Toggle before installing a method and it deploys alongside it automatically.
+[b]NR Cost Scaler[/b] — запускает нейронный рендеринг с затратами 75% по умолчанию. Включите перед установкой метода — развернётся вместе с ним автоматически.
 
-[b]ShortFuse DLSS Tool[/b] — alternative neural rendering implementation for DX12 games.
+[b]ShortFuse DLSS Tool[/b] — альтернативная реализация нейронного рендеринга для игр на DX12.
 
 [line]
 
-[size=5][b]DLSS and Streamline Manager[/b][/size]
+[size=5][b]Менеджер DLSS и Streamline[/b][/size]
 
-Every NVIDIA game gets a full DLSS management panel.
+Каждая игра с NVIDIA получает полноценную панель управления DLSS.
 
 [list]
-[*]Swap SR, RR, FG, and Streamline versions per game — any version from 2.x to current
-[*]Original DLLs backed up automatically and restorable any time
-[*]Presets (J/K/L/M for SR, D/E for RR, A/B for FG) written directly to NVIDIA driver profiles — no Profile Inspector needed
-[*]Render scale override — force any render resolution from 33% to 100% per game
-[*]NVIDIA Override — enable the driver's "Latest DLL" injection per component, directly from the version dropdown
-[*]Batch Deploy — push versions, presets, and render scales to your entire library at once
-[*]Auto-update for SR, RR, and FG — opt-in per game
+[*]Подмена версий SR, RR, FG и Streamline для каждой игры — любая версия с 2.x до актуальной
+[*]Оригинальные DLL резервируются автоматически и восстанавливаются в любой момент
+[*]Пресеты (J/K/L/M для SR, D/E для RR, A/B для FG) записываются прямо в профили драйвера NVIDIA — Profile Inspector не нужен
+[*]Переопределение масштаба рендеринга — принудительное разрешение рендеринга от 33% до 100% для каждой игры
+[*]Переопределение NVIDIA — включайте внедрение «Latest DLL» драйвером для каждого компонента прямо из списка версий
+[*]Массовое развертывание — применить версии, пресеты и масштабы рендеринга ко всей библиотеке разом
+[*]Автообновление SR, RR и FG — по желанию, для каждой игры
 [/list]
 
 [line]
 
-[size=5][b]Multi Frame Generation[/b][/size]
+[size=5][b]Мульти-генерация кадров[/b][/size]
 
-[b]RTX 40 MFG Unlock[/b] — enables MFG multipliers beyond 2x (up to 6x) on RTX 40 Series via Ultimate ASI Loader. One-click install from the Extras section.
+[b]RTX 40 MFG Unlock[/b] — включает множители MFG выше 2x (до 6x) на RTX 40-й серии через Ultimate ASI Loader. Установка в один клик из раздела «Дополнения».
 
-[b]RTX 50 Series MFG[/b] — configure Fixed or Dynamic mode, frame count (2x–6x), and dynamic target frame rate from the DLSS panel.
+[b]MFG на RTX 50-й серии[/b] — настройка фиксированного или динамического режима, количества кадров (2x–6x) и динамической целевой частоты из панели DLSS.
 
 [line]
 
 [size=5][b]OptiScaler[/b][/size]
 
 [list]
-[*]FSR4 / FSR3 / XeSS on DLSS-only games. Frame generation on AMD and Intel GPUs.
-[*]Stable and Nightly channels selectable per game
-[*]Pre-configured per-GPU INI templates (NVIDIA, AMD+DLSS, AMD no-DLSS) — user-editable, never overwritten
-[*]Streamline + DLSS Enabler deploy per game for Nightly FG setups
-[*]Full frame generation settings in the cog: FG Input, Output, Nvngx Replacement, HUD Fix
-[*]OptiPatcher auto-deployed and silently auto-updated
+[*]FSR4 / FSR3 / XeSS в играх только с DLSS. Генерация кадров на GPU AMD и Intel.
+[*]Каналы Stable и Nightly выбираются для каждой игры
+[*]Преднастроенные INI-шаблоны под GPU (NVIDIA, AMD+DLSS, AMD без DLSS) — редактируются пользователем и не перезаписываются
+[*]Развертывание Streamline + DLSS Enabler для каждой игры в связках Nightly FG
+[*]Полные настройки генерации кадров в шестерёнке: FG Input, FG Output, замена Nvngx, HUD Fix
+[*]OptiPatcher разворачивается автоматически и тихо обновляется
 [/list]
 
 [line]
 
-[size=5][b]Frame Limiters[/b][/size]
+[size=5][b]Ограничители FPS[/b][/size]
 
-[b]ReLimiter[/b] — precision frame pacing with predictive sleep, phase-locked timing, VRR awareness, and DLSS FG adaptive pacing. The most accurate per-game frame limiter available.
+[b]ReLimiter[/b] — прецизионный вывод кадров с предиктивной задержкой сна, фазо-заблокированными таймингами, учётом VRR и адаптивным pacing под DLSS FG. Самый точный из доступных ограничителей FPS.
 
-[b]Display Commander[/b] — alternative limiter with support for both 32-bit and 64-bit games.
+[b]Display Commander[/b] — альтернативный ограничитель с поддержкой и 32-, и 64-битных игр.
 
-Both install as ReShade addons and update automatically.
+Оба ставятся как аддоны ReShade и обновляются автоматически.
 
 [line]
 
-[size=5][b]NVIDIA Driver Profile Management[/b][/size]
+[size=5][b]Управление профилями драйвера NVIDIA[/b][/size]
 
-[i]Replaces NVIDIA App and NVIDIA Profile Inspector for per-game driver settings.[/i]
+[i]Замена NVIDIA App и NVIDIA Profile Inspector для настройки драйвера под каждую игру.[/i]
 
-RHI writes driver settings directly via NvAPI — no external tools needed.
+RHI записывает настройки драйвера напрямую через NvAPI — внешние инструменты не нужны.
 
-[b]Per game:[/b]
+[b]Для каждой игры:[/b]
 [list]
-[*]VSync Mode and Tear Control
-[*]Low Latency (Off / On / Ultra)
-[*]Smooth Motion (RTX 40+) — Enable, Allowed APIs, Flip Pacing
-[*]Power Management Mode
-[*]G-Sync per-game toggle
-[*]ReBAR — Off / On / Auto, Mode, Size Limit
-[*]RTX HDR — Enable with Peak Brightness, Contrast, Saturation, Middle Grey, Debanding
-[*]DLSS Presets and Render Scale Override
+[*]Режим VSync и управление разрывами
+[*]Low Latency (выкл / вкл / Ultra)
+[*]Smooth Motion (RTX 40+) — включение, разрешённые API, Flip Pacing
+[*]Режим управления питанием
+[*]Переключатель G-Sync для каждой игры
+[*]ReBAR — выкл / вкл / авто, режим, лимит размера
+[*]RTX HDR — включение с пиковой яркостью, контрастом, насыщенностью, средним серым и устранением бандинга
+[*]Пресеты DLSS и переопределение масштаба рендеринга
 [/list]
 
-[b]Global (all games):[/b]
+[b]Глобально (все игры):[/b]
 [list]
-[*]Shader Cache, G-Sync Mode, Preferred Refresh Rate, Global ReBAR, FPS Limit
-[*]Digital Vibrance per display — restored automatically on every app startup
+[*]Кеш шейдеров, режим G-Sync, предпочитаемая частота обновления, глобальный ReBAR, лимит FPS
+[*]Digital Vibrance для каждого дисплея — автоматически восстанавливается при каждом запуске приложения
 [/list]
 
-[b]Export / Import[/b] — back up all per-game NVIDIA settings to a file. Restore everything after a driver reinstall in one click.
+[b]Экспорт / импорт[/b] — резервное копирование всех индивидуальных настроек NVIDIA в файл. Восстановите всё после переустановки драйвера в один клик.
 
 [line]
 
-[size=5][b]HDR Auto-Toggle[/b][/size]
+[size=5][b]Автопереключение HDR[/b][/size]
 
-Enables Windows HDR when a game launches and disables it when the game closes. Works with Steam, Epic, Xbox Game Pass, and direct exe launches. Override per game with the HDR button next to Launch.
+Включает Windows HDR при запуске игры и выключает при её закрытии. Работает со Steam, Epic, Xbox Game Pass и прямым запуском exe. Переопределяется для каждой игры кнопкой HDR рядом с «Запустить».
 
 [line]
 
-[size=5][b]46 Shader Packs[/b][/size]
+[size=5][b]46 наборов шейдеров[/b][/size]
 
-Every shader pack you'd want, all managed from one dialog.
+Все нужные наборы шейдеров, управляемые из одного окна.
 
-[b]Essential:[/b] Lilium HDR Shaders — required for HDR tone mapping.
+[b]Необходимые:[/b] Lilium HDR Shaders — нужны для HDR-тонмаппинга.
 
-[b]Recommended:[/b] PumboAutoHDR, crosire reshade-shaders, MaxG2D Simple HDR, clshortfuse shaders, RenoFX HDR Toolkit, smolbbsoop shaders.
+[b]Рекомендуемые:[/b] PumboAutoHDR, crosire reshade-shaders, MaxG2D Simple HDR, шейдеры clshortfuse, RenoFX HDR Toolkit, smolbbsoop shaders.
 
-[b]Extra:[/b] iMMERSE, qUINT, OtisFX, SweetFX, Prod80, CRT-Royale, VRToolkit, ZenteonFX, QD-OLED APL Fixer, LumaBoost, Glamarye Fast Effects, and 28 more.
+[b]Дополнительные:[/b] iMMERSE, qUINT, OtisFX, SweetFX, Prod80, CRT-Royale, VRToolkit, ZenteonFX, QD-OLED APL Fixer, LumaBoost, Glamarye Fast Effects и ещё 28 наборов.
 
-Select packs globally or per game. Pick individual shader files within any pack. Save selections as named profiles and export them as a zip to share directly into Discord.
+Наборы выбираются глобально или для каждой игры. Внутри любого набора можно отметить отдельные файлы шейдеров. Сохраняйте подборки как именные профили и экспортируйте в zip, чтобы поделиться прямо в Discord.
 
 [line]
 
-[size=5][b]Update All[/b][/size]
+[size=5][b]Обновить всё[/b][/size]
 
-One button updates ReShade, RenoDX, ReLimiter, Display Commander, OptiScaler, and RE Framework across your entire library. Background check runs every 4 hours — works silently from the system tray.
+Одна кнопка обновляет ReShade, RenoDX, ReLimiter, Display Commander, OptiScaler и RE Framework во всей библиотеке. Фоновая проверка выполняется каждые 4 часа — тихо, из системного трея.
 
 [line]
 
-[size=5][b]Manifest-Driven[/b][/size]
+[size=5][b]Всё решает манифест[/b][/size]
 
-Game support, Engine.ini fixes, mod links, and feature flags are delivered via a server-side manifest updated on GitHub. New game support and fixes reach all users instantly without waiting for an app update.
+Поддержка игр, исправления Engine.ini, ссылки на моды и флаги функций доставляются серверным манифестом, обновляемым на GitHub. Поддержка новых игр и исправления мгновенно достаются всем пользователям без ожидания обновления приложения.
 
 [line]
 
-[size=5][b]Requirements[/b][/size]
+[size=5][b]Требования[/b][/size]
 
 [list]
-[*]Windows 10 / 11 (64-bit)
+[*]Windows 10 / 11 (64 бита)
 [*][url=https://dotnet.microsoft.com/download/dotnet/8.0].NET 8 Desktop Runtime[/url]
-[*]NVIDIA GPU recommended for driver management features (ReShade and RenoDX work on all GPUs)
+[*]Для функций управления драйвером рекомендуется GPU NVIDIA (ReShade и RenoDX работают на любых GPU)
 [/list]
 
-[b]Supported stores:[/b] Steam · Epic · GOG · Xbox Game Pass · EA App · Ubisoft Connect · Battle.net · Rockstar · itch.io
+[b]Поддерживаемые магазины:[/b] Steam · Epic · GOG · Xbox Game Pass · EA App · Ubisoft Connect · Battle.net · Rockstar · itch.io
 
 [line]
 
-[i]RHI is free. All third-party components — ReShade, RenoDX, Luma, OptiScaler, DXVK, and others — are the work of their respective authors.[/i]
+[i]RHI бесплатен. Все сторонние компоненты — ReShade, RenoDX, Luma, OptiScaler, DXVK и другие — труд их авторов.[/i]
diff --git a/docs/ROADMAP.md b/docs/ROADMAP.md
index 630bca1..99164dc 100644
--- a/docs/ROADMAP.md
+++ b/docs/ROADMAP.md
@@ -1,107 +1,107 @@
-# RHI Roadmap
+# Дорожная карта RHI
 
-Ideas, technical debt, and planned improvements. Categorised for easy reference.
+Идеи, технический долг и запланированные улучшения. Сгруппированы для удобства.
 
 ---
 
-## ✅ Completed
+## ✅ Выполнено
 
-### Phase 1 — Reduce Surface Area
-- **Remove Grid View** — deleted CardBuilder, OverridesFlyoutBuilder, ViewLayout.Grid. -5000 lines.
-- **Split mega-files** — 6 files split into 26 partials. All under 50KB.
-- **Unify dual UI builders** — OverridesFlyoutBuilder deleted (was Grid-only dead code). Only DetailPanelBuilder remains.
+### Этап 1 — уменьшение площади кода
+- **Удалён grid-режим просмотра** — удалены CardBuilder, OverridesFlyoutBuilder, ViewLayout.Grid. -5000 строк.
+- **Разделены мега-файлы** — 6 файлов разделены на 26 частичных классов. Все меньше 50 КБ.
+- **Унифицированы двойные UI-билдеры** — OverridesFlyoutBuilder удалён (был мёртвым кодом только для grid-режима). Остался только DetailPanelBuilder.
 
-### Phase 2 — Structural Improvements (Partial)
-- **NVAPI abstraction** — DlssPresetService split into 6 partials (max 36KB). ProfileMatching, DriverSettings, ReBar, Export, Reset separated.
-- **Reduce ViewModel surface** — All consumers use direct injection. 14 forwarding properties deleted. Only 3 remain (ShaderPack, AddonPack, GameName).
+### Этап 2 — структурные улучшения (частично)
+- **Абстракция NVAPI** — DlssPresetService разделён на 6 частичных классов (макс. 36 КБ). ProfileMatching, DriverSettings, ReBar, Export, Reset выделены.
+- **Уменьшена площадь ViewModel** — все потребители используют прямое внедрение. Удалено 14 транзитных свойств, осталось 3 (ShaderPack, AddonPack, GameName).
 
 ---
 
-## 🔧 Engineering Debt
+## 🔧 Инженерный долг
 
-Items that improve maintainability, performance, or reliability. No user-facing changes.
+Пункты, улучшающие сопровождаемость, производительность или надёжность. Видимых пользователю изменений нет.
 
-### Concurrency Model
-- Introduce `BackgroundTaskCoordinator` — serializes operations on shared resources (library saves, staging downloads, card updates, panel rebuilds)
-- Establish threading contract: services on background threads → marshal results to UI via single `DispatcherQueue.TryEnqueue` point
-- Replace individual guard flags (`comboInitializing`, `_suppressSelectionChanged`) with a `PanelState` enum (Building / Interactive / Rebuilding)
-- Move shader pack version tracking out of `settings.json` into dedicated `shader_pack_versions.json` (fixes startup file contention)
+### Модель конкурентности
+- Ввести `BackgroundTaskCoordinator` — сериализация операций над общими ресурсами (сохранение библиотеки, загрузки staging, обновление карточек, пересборка панелей)
+- Установить контракт потоков: сервисы работают в фоновых потоках → результаты передаются в UI через единую точку `DispatcherQueue.TryEnqueue`
+- Заменить отдельные флаги-предохранители (`comboInitializing`, `_suppressSelectionChanged`) на enum `PanelState` (Building / Interactive / Rebuilding)
+- Вынести учёт версий наборов шейдеров из `settings.json` в отдельный `shader_pack_versions.json` (устраняет конкуренцию за файл при запуске)
 
-### Settings Modernization
-- Replace flat `Dictionary<string, string>` with structured `PerGameSettings` class
-- Single serialization point instead of manual dict/hashset pattern
-- Migration system for settings format changes
-- Eliminates MigrateDict/MigrateHashSet in `RenameGame()`
+### Модернизация настроек
+- Заменить плоский `Dictionary<string, string>` на структурированный класс `PerGameSettings`
+- Единая точка сериализации вместо ручных словарей/наборов
+- Система миграций при смене формата настроек
+- Устраняет MigrateDict/MigrateHashSet в `RenameGame()`
 
-### Data-Driven Component System
-- `IGameComponent` interface: Detect, Install, Uninstall, CheckForUpdate, Update
-- Components register in DI, detail panel iterates them dynamically
-- Eliminates "add to 11 files" pattern for new components
+### Компонентная система на данных
+- Интерфейс `IGameComponent`: Detect, Install, Uninstall, CheckForUpdate, Update
+- Компоненты регистрируются в DI, панель игры динамически их обходит
+- Устраняет паттерн «добавь в 11 файлов» для новых компонентов
 
-### Structured Error Handling
-- `OperationResult` type instead of try/catch + `ActionMessage = "❌ ..."`
-- Centralized `ErrorDialogService.ShowAsync(result, retryAction?)`
-- Service methods return results, callers decide how to surface
+### Структурированная обработка ошибок
+- Тип `OperationResult` вместо try/catch + `ActionMessage = "❌ ..."`
+- Централизованный `ErrorDialogService.ShowAsync(result, retryAction?)`
+- Методы сервисов возвращают результат, вызывающий решает, как показать
 
-### Incremental Panel Updates
-- Rebuild only the changed section instead of full `BuildOverridesPanel()`
-- Bind version labels, status dots, enabled states to observable properties
-- Structural changes (add/remove rows) stay imperative
+### Инкрементальное обновление панелей
+- Перестраивать только изменённый раздел вместо полного `BuildOverridesPanel()`
+- Привязать подписи версий, статусные точки и состояния включённости к observable-свойствам
+- Структурные изменения (добавление/удаление строк) остаются императивными
 
-### Test Infrastructure
-- Baseline verification (37 tests passing): `dotnet test RenoDXCommander.Tests/RenoDXCommander.Tests.csproj -p:Platform=x64`
-- Add integration tests: install/uninstall roundtrip, manifest parse → card assertions, settings save/load
-- CI pipeline: `dotnet build && dotnet test` on push
+### Тестовая инфраструктура
+- Базовая верификация (37 проходящих тестов): `dotnet test RenoDXCommander.Tests/RenoDXCommander.Tests.csproj -p:Platform=x64`
+- Добавить интеграционные тесты: цикл установки/удаления, разбор манифеста → проверки карточек, сохранение/загрузка настроек
+- CI-конвейер: `dotnet build && dotnet test` на каждый push
 
 ---
 
-## 🚀 Feature Ideas
+## 🚀 Идеи функций
 
-User-requested features and enhancements. Not committed — just captured.
+Запрошенные пользователями функции и улучшения. Не подтверждены — просто зафиксированы.
 
-### Steam Full Library Integration
-Show the user's complete Steam catalog (owned, family shared, not just installed) to browse compatibility before downloading.
+### Полная интеграция библиотеки Steam
+Показывать весь каталог Steam пользователя (купленное, семейный доступ — не только установленное), чтобы изучать совместимость до скачивания.
 
-- Auto-read SteamID from `loginusers.vdf`
-- User provides Steam Web API key in Settings
-- `GetOwnedGames` returns all owned games with app names
-- Non-installed games show as greyed cards with compatibility badges
-- Filter chip: "Not Installed"
-- "Install" links to `steam://install/{appId}`
+- Автосчитывание SteamID из `loginusers.vdf`
+- Пользователь вводит ключ Steam Web API в настройках
+- `GetOwnedGames` возвращает все купленные игры с названиями
+- Неустановленные игры показываются затемнёнными карточками со значками совместимости
+- Фильтр: «Не установленные»
+- «Установить» ведёт на `steam://install/{appId}`
 
-**Source:** Discord user request. Similar to SteamDB/SteamDD.
+**Источник:** запрос пользователя в Discord. Похож на SteamDB/SteamDD.
 
-### Store-Qualified installPathOverrides
-Games on both Steam and Xbox with different subfolder structures (`Win64` vs `WinGDK`).
+### store-квалифицированные installPathOverrides
+Игры одновременно в Steam и Xbox с разной структурой подпапок (`Win64` против `WinGDK`).
 
-- Pipe-separated paths (try both, use whichever exists) — consistent with `engineIniPathOverrides`
-- Or store-qualified dict: `{ "Steam": "..\\Win64", "Xbox": "..\\WinGDK" }` — needs migration
+- Пути через разделитель «|» (пробовать оба, использовать существующий) — согласуется с `engineIniPathOverrides`
+- Или словарь с ключом магазина: `{ "Steam": "..\\Win64", "Xbox": "..\\WinGDK" }` — нужна миграция
 
-**Trigger:** When a game has the same name from both stores but different subfolder layouts.
+**Триггер:** когда игра с одинаковым названием приходит из двух магазинов с разной раскладкой подпапок.
 
-### NVAPI Driver Version Gating
-- Check driver version before enabling settings (MFG needs 572.16+, render scale needs 565+)
-- Currently the UI silently fails on older drivers
-- Show "Requires driver X.XX+" tooltip when disabled
+### Проверка версии драйвера через NVAPI
+- Проверять версию драйвера перед включением настроек (MFG нужен 572.16+, масштаб рендеринга — 565+)
+- Сейчас интерфейс молча не срабатывает на старых драйверах
+- Показывать подсказку «Требуется драйвер X.XX+», когда настройка недоступна
 
-### Custom ReShade Auto-Redeploy
-When a user updates a custom ReShade DLL in the Custom folder, automatically redeploy it to all games using that DLL.
+### Автоперенос пользовательского ReShade
+Когда пользователь обновляет свою DLL ReShade в папке Custom, автоматически переносить её во все игры, использующие эту DLL.
 
-- Hash each `.dll` in Custom folder, store hashes in `custom_reshade_hashes.json` alongside the DLLs (in the Custom folder itself)
-- On Refresh + 4-hour background cycle: re-hash and compare
-- If hash changed → redeploy to all games with "Custom" RS channel that use that specific DLL
-- Vulkan games: update the global layer in `%ProgramData%\ReShade\` (requires admin)
-- Update stored hashes after successful redeploy
+- Считать хэш каждого `.dll` в папке Custom и хранить хэши в `custom_reshade_hashes.json` рядом с DLL (в самой папке Custom)
+- При «Обновить» + 4-часовом фоновом цикле: пересчитывать хэши и сравнивать
+- Если хэш изменился → развернуть во все игры с каналом RS «Custom», использующие именно эту DLL
+- Vulkan-игры: обновить глобальный слой в `%ProgramData%\ReShade\` (нужен администратор)
+- Обновлять сохранённые хэши после успешного переноса
 
-**Source:** Discord user request.
+**Источник:** запрос пользователя в Discord.
 
 ---
 
-## 💡 Nice-to-Have
+## 💡 Желательно, но не срочно
 
-Low priority items with no current demand.
+Пункты низкого приоритета без текущего спроса.
 
-- **Localization** — WinUI `.resw` support. Not urgent for target audience.
-- **Accessibility audit** — Screen reader support for code-behind UI elements.
-- **Plugin system** — Third-party components register without app changes.
-- **Telemetry** — Usage analytics. Privacy-sensitive, opt-in only.
+- **Локализация** — поддержка WinUI `.resw`. Для целевой аудитории не горит.
+- **Аудит доступности** — поддержка скринридеров для UI-элементов, создаваемых в коде.
+- **Система плагинов** — регистрация сторонних компонентов без изменения приложения.
+- **Телеметрия** — аналитика использования. Чувствительна к приватности, только по согласию.
diff --git a/docs/WHAT_IS_RHI.md b/docs/WHAT_IS_RHI.md
index 9eabf9e..874d6d7 100644
--- a/docs/WHAT_IS_RHI.md
+++ b/docs/WHAT_IS_RHI.md
@@ -1,41 +1,41 @@
-# What is RHI?
+# Что такое RHI?
 
-**RHI** is a free tool that adds HDR, visual upgrades, and NVIDIA driver management to your PC games, automatically.
+**RHI** — бесплатный инструмент, который автоматически добавляет в ваши ПК-игры HDR, визуальные улучшения и управление драйвером NVIDIA.
 
-## Why RHI?
+## Зачем RHI?
 
-HDR modding involves a lot of moving parts. ReShade, shaders, RenoDX, DXVK, Luma, all great tools but spread across different sources with their own install steps. I wanted to bring it all together in one place so nobody has to juggle multiple downloads and guides.
+HDR-моддинг — это масса отдельных компонентов: ReShade, шейдеры, RenoDX, DXVK, Luma. Все инструменты отличные, но разбросаны по разным источникам, и у каждого свои шаги установки. Я хотел собрать всё в одном месте, чтобы никому не пришлось жонглировать десятком загрузок и руководств.
 
-Same with NVIDIA driver settings and DLSS. Per-game presets, ReBAR, frame gen, render scale, swapping DLSS and Streamline DLL versions, it's all possible but scattered across different apps. RHI puts it all under one roof.
+То же самое с настройками драйвера NVIDIA и DLSS: индивидуальные пресеты, ReBAR, генерация кадров, масштаб рендеринга, подмена версий DLL DLSS и Streamline — всё возможно, но размазано по разным приложениям. RHI собирает это под одной крышей.
 
-## What It Does
+## Что умеет
 
-RHI detects games across **Steam, Epic, GOG, Xbox, Ubisoft, EA, Battle.net, itch.io** then handles everything in one click.
+RHI находит игры в **Steam, Epic, GOG, Xbox, Ubisoft, EA, Battle.net, itch.io** и делает всё остальное в один клик.
 
-**HDR & Mods**
-- **ReShade** - installs, updates, manages presets and 46 shader packs
-- **RenoDX** - auto-detects the right HDR mod, installs it, keeps it updated
-- **RenoDX Upgrade + RenoFX** - HDR for games without a dedicated mod
-- **DXVK + Lilium HDR** - brings HDR to older DX9 games
-- **UE DOF Fix** - fixes DOF artifacts in Unreal Engine 5 games
-- **OptiScaler** - FSR/XeSS for DLSS-only games
-- **ReLimiter** - per-game frame limiter
-- **RE Framework** - RE Engine mod loader
-- **Luma** - Luma HDR mod install
+**HDR и моды**
+- **ReShade** — установка, обновление, управление пресетами и 46 наборами шейдеров
+- **RenoDX** — сам определяет подходящий HDR-мод, устанавливает и обновляет его
+- **RenoDX Upgrade + RenoFX** — HDR для игр без отдельного мода
+- **DXVK + Lilium HDR** — приносит HDR в старые игры на DX9
+- **UE DOF Fix** — исправляет артефакты глубины резкости в играх на Unreal Engine 5
+- **OptiScaler** — FSR/XeSS для игр только с DLSS
+- **ReLimiter** — ограничитель FPS для каждой игры
+- **RE Framework** — загрузчик модов для RE Engine
+- **Luma** — установка HDR-мода Luma
 
-**DLSS & Streamline**
-- Swap SR, RR, FG, and Streamline versions per-game from a dropdown
-- Batch deploy across your entire library, auto-update, backup/restore
+**DLSS и Streamline**
+- Подмена версий SR, RR, FG и Streamline для каждой игры из выпадающего списка
+- Массовое развертывание на всю библиотеку, автообновление, резервные копии/восстановление
 
-**NVIDIA Driver Profiles**
-- DLSS presets, render scale override, Multi Frame Generation
-- ReBAR, G-Sync, Low Latency, VSync, Power Mode, FPS Limiter
-- Export/Import all profiles for backup after driver updates
+**Профили драйвера NVIDIA**
+- Пресеты DLSS, переопределение масштаба рендеринга, мульти-генерация кадров
+- ReBAR, G-Sync, Low Latency, VSync, режим питания, ограничитель FPS
+- Экспорт/импорт всех профилей для резервного копирования после обновлений драйвера
 
-**Quality of Life**
-- One-click install, Update All across your whole library
-- Per-game overrides for everything
-- HDR auto-toggle on launch, off on exit
-- System tray with recent game launcher
+**Удобства**
+- Установка в один клик, «Обновить всё» для всей библиотеки
+- Индивидуальные переопределения для чего угодно
+- Автопереключение HDR: включение при запуске, выключение при выходе
+- Системный трей с быстрым запуском недавних игр
 
-**Download:** https://discord.com/channels/1296187754979528747/1475173660686815374
+**Загрузка:** https://discord.com/channels/1296187754979528747/1475173660686815374
diff --git a/docs/handover-session-13-sep-2026b.md b/docs/handover-session-13-sep-2026b.md
index 0d101e4..12f7b61 100644
--- a/docs/handover-session-13-sep-2026b.md
+++ b/docs/handover-session-13-sep-2026b.md
@@ -1,160 +1,160 @@
-# Handover Report — Session 13 September 2026 (Evening)
+# Отчёт о передаче — сессия 13 сентября 2026 (вечер)
 
-**Branch:** main  
-**App version:** 2.7.1 (in progress, not released)  
-**Published:** RHI.exe  
+**Ветка:** main  
+**Версия приложения:** 2.7.1 (в работе, не выпущена)  
+**Публикуется:** RHI.exe  
 **HEAD:** b744b81
 
 ---
 
-## What Was Done This Session
+## Что сделано в этой сессии
 
-### 1. `rhi_install.txt` Game-Folder Manifest
+### 1. Манифест в папке игры `rhi_install.txt`
 
-A new JSON file written to every game folder on OptiScaler install/update. Solves two persistent bugs:
+Новый JSON-файл, записываемый в каждую папку игры при установке/обновлении OptiScaler. Решает две застарелые проблемы:
 
-**Version confusion:** OptiScaler was reading the installed version from the staging `version.txt`, which changes when a new version downloads. If you installed nightly `20260912`, then a new nightly downloaded, the card showed the new version instead of what's actually installed. `rhi_install.txt` records the version at install time and is read preferentially on startup.
+**Путаница с версиями:** OptiScaler читал установленную версию из хранилищного `version.txt`, который меняется при скачивании новой версии. Если вы поставили nightly `20260912`, а затем скачался новый nightly, карточка показывала новую версию вместо реально установленной. `rhi_install.txt` фиксирует версию на момент установки и при запуске читается в приоритете.
 
-**Stale uninstall cleanup:** Uninstall previously scanned the staging folder to know which files to delete. If staging changed (new nightly downloaded between install and uninstall), wrong files got deleted. Now uninstall reads the game-folder manifest for the authoritative file list.
+**Устаревшая очистка при удалении:** удаление раньше сканировало папку хранилища, чтобы узнать, какие файлы чистить. Если хранилище менялось (новый nightly скачался между установкой и удалением), удалялись не те файлы. Теперь удаление читает манифест в папке игры как авторитетный список файлов.
 
-**Model:** `Models/RhiInstallManifest.cs`  
-Fields: `component`, `variant`, `version`, `installedAs`, `installedAt`, `files`, `folders`, `sharedFiles`, `components`, `nrMethod`  
-Static helpers: `Write`, `Read`, `Delete`, `UpdateInstalledAs`, `SetNrMethod`, `AddSharedFileOwner`, `RemoveSharedFileOwner`, `SetComponent`, `GetComponentFiles`, `RemoveComponent`
+**Модель:** `Models/RhiInstallManifest.cs`  
+Поля: `component`, `variant`, `version`, `installedAs`, `installedAt`, `files`, `folders`, `sharedFiles`, `components`, `nrMethod`  
+Статические хелперы: `Write`, `Read`, `Delete`, `UpdateInstalledAs`, `SetNrMethod`, `AddSharedFileOwner`, `RemoveSharedFileOwner`, `SetComponent`, `GetComponentFiles`, `RemoveComponent`
 
-**Files changed:** `OptiScalerService.Install.cs`, `MainViewModel.BuildCards.cs`, `MainViewModel.CacheLoad.cs`, `DetailPanelBuilder.Overrides.cs`
+**Изменённые файлы:** `OptiScalerService.Install.cs`, `MainViewModel.BuildCards.cs`, `MainViewModel.CacheLoad.cs`, `DetailPanelBuilder.Overrides.cs`
 
 ---
 
-### 2. Shared DLSS File Ownership (`sharedFiles` in manifest)
+### 2. Совместное владение файлами DLSS (`sharedFiles` в манифесте)
 
-Multiple components (OptiScaler, ShortFuse NR, DLSS5 Tool, Feeder) all deploy the same `nvngx_dlss*.dll` files to the game folder. Previously they trampled each other's sentinel files and deleted files the other component still needed.
+Несколько компонентов (OptiScaler, ShortFuse NR, DLSS5 Tool, Feeder) разворачивают одни и те же файлы `nvngx_dlss*.dll` в папку игры. Раньше они затирали sentinel-файлы друг друга и удаляли файлы, нужные другому компоненту.
 
-**Solution:** `sharedFiles` dict in `rhi_install.txt` maps each filename to a list of component owners. A file is only deleted/restored when the last owner uninstalls.
+**Решение:** словарь `sharedFiles` в `rhi_install.txt` отображает каждое имя файла на список компонентов-владельцев. Файл удаляется/восстанавливается только когда удалён последний владелец.
 
-**Components wired:**
-- OptiScaler (all variants): registers `nvngx_dlss.dll`, `nvngx_dlssd.dll`, `nvngx_dlssg.dll` always; `nvngx_dlssnr.dll` for DlssNr variant only
-- ShortFuse: registers all 4 DLLs in `DeploySfDllsAsync`; deregisters in `UninstallSf`
-- DLSS5 Tool: registers all 4 in `UpgradeDlssDllsAsync`; deregisters in `Renodx5AddonService.Uninstall`
-- Bridge: uses DLSS5 Tool's ownership
-- Feeder: registers `nvngx_dlssnr.dll` in `DeployNrDllIfAbsentAsync("Feeder")`; deregisters in `RemoveNrDll("Feeder")`
+**Подключённые компоненты:**
+- OptiScaler (все варианты): всегда регистрирует `nvngx_dlss.dll`, `nvngx_dlssd.dll`, `nvngx_dlssg.dll`; `nvngx_dlssnr.dll` — только для варианта DlssNr
+- ShortFuse: регистрирует все 4 DLL в `DeploySfDllsAsync`; снимает регистрацию в `UninstallSf`
+- DLSS5 Tool: регистрирует все 4 в `UpgradeDlssDllsAsync`; снимает в `Renodx5AddonService.Uninstall`
+- Bridge: использует владение DLSS5 Tool
+- Feeder: регистрирует `nvngx_dlssnr.dll` в `DeployNrDllIfAbsentAsync("Feeder")`; снимает в `RemoveNrDll("Feeder")`
 
-**Edge case fixed:** When DLSS5 Tool deploys to a detected plugin subfolder (e.g. `Engine\Plugins\Runtime\Nvidia\DLSS\...`) but OptiScaler deployed to the game root, `RestoreDlssDllsWithSentinel` now also cleans up the root copies if the paths differ.
+**Исправленный краевой случай:** когда DLSS5 Tool разворачивает в найденную подпапку плагина (например, `Engine\Plugins\Runtime\Nvidia\DLSS\...`), а OptiScaler — в корень игры, `RestoreDlssDllsWithSentinel` теперь чистит и корневые копии, если пути различаются.
 
-**Files changed:** `Renodx5AddonService.cs`, `DetailPanelBuilder.NeuralRendering.cs`, `OptiScalerService.Install.cs`
+**Изменённые файлы:** `Renodx5AddonService.cs`, `DetailPanelBuilder.NeuralRendering.cs`, `OptiScalerService.Install.cs`
 
 ---
 
-### 3. Per-Component File Records (`components` in manifest)
+### 3. Поигровые записи о файлах (`components` в манифесте)
 
-Each NR component now records its complete deployed file list in `rhi_install.txt` under a `components` dict. This provides redundancy alongside sentinels — if sentinels are missing or wrong, RHI still knows what to clean up.
+Каждый NR-компонент теперь записывает свой полный список развёрнутых файлов в `rhi_install.txt` в словаре `components`. Это избыточность рядом с sentinel-файлами — если sentinel'ы отсутствуют или неверны, RHI всё равно знает, что чистить.
 
-**What each component records:**
-- `ShortFuse`: `renodx-dlss.addon64` + 4 nvngx DLLs + 11 sl.*.dll files
-- `Dlss5Tool`: `renodx-dlss5.addon64` + 4 nvngx DLLs; Bridge adds `dlss5-bridge.addon64`
-- `Feeder`: feeder addon + nvngx_dlssnr + nvngx_dlss + renodx-dlss5 + shaders + `ReShadePreset.ini`; 32-bit adds `host64\*`; DX9 adds `D3D9.dll` + `dgVoodoo.conf`
+**Что записывает каждый компонент:**
+- `ShortFuse`: `renodx-dlss.addon64` + 4 DLL nvngx + 11 файлов sl.*.dll
+- `Dlss5Tool`: `renodx-dlss5.addon64` + 4 DLL nvngx; Bridge добавляет `dlss5-bridge.addon64`
+- `Feeder`: аддон feeder + nvngx_dlssnr + nvngx_dlss + renodx-dlss5 + шейдеры + `ReShadePreset.ini`; 32-бит добавляет `host64\*`; DX9 добавляет `D3D9.dll` + `dgVoodoo.conf`
 
-**Important:** OptiScaler's `InstallAsync` and `UpdateAsync` both preserve `sharedFiles`, `components`, and `nrMethod` when rewriting the manifest. Earlier bug: it was creating a fresh `RhiInstallManifest` object which wiped those fields.
+**Важно:** `InstallAsync` и `UpdateAsync` OptiScaler оба сохраняют `sharedFiles`, `components` и `nrMethod` при перезаписи манифеста. Прошлый баг: создавался свежий объект `RhiInstallManifest`, стиравший эти поля.
 
 ---
 
-### 4. GitHub API Rate Limit Fixes
+### 4. Исправления лимитов GitHub API
 
-**Token support:** `DevUnlockService.GitHubApiToken` now reads from `%LocalAppData%\RHI\github_api.txt` first (any user), falls back to `github_api=` line in `unlock.txt` (dev only). Token is applied to all `GitHubETagCache.GetWithETagAsync` calls.
+**Поддержка токена:** `DevUnlockService.GitHubApiToken` теперь сначала читает `%LocalAppData%\RHI\github_api.txt` (любой пользователь), с откатом к строке `github_api=` в `unlock.txt` (только разработчики). Токен применяется ко всем вызовам `GitHubETagCache.GetWithETagAsync`.
 
-**403 handling:** Previously any 403 set `_rateLimited = true` for the entire session. Now only sets it when `X-RateLimit-Remaining: 0` is confirmed — transient 403s no longer kill all API calls for the session. Log message now includes the URL and whether the request was authenticated.
+**Обработка 403:** раньше любой 403 ставил `_rateLimited = true` на всю сессию. Теперь только при подтверждении `X-RateLimit-Remaining: 0` — единичные 403 больше не убивают все вызовы API на сессию. В лог теперь пишется URL и то, был ли запрос аутентифицирован.
 
-**Files changed:** `DevUnlockService.cs`, `GitHubETagCache.cs`
+**Изменённые файлы:** `DevUnlockService.cs`, `GitHubETagCache.cs`
 
 ---
 
-### 5. DB Refresh Fix
+### 5. Исправление обновления базы
 
-RHI database (rhi-repo) changes were not reflecting after standard Refresh — required a full restart. Root cause: `GitHubETagCache` is in-memory and session-scoped. On Refresh, the ETag was still cached so GitHub returned 304 with the old data.
+Изменения базы RHI (rhi-repo) не подхватывались после обычного «Обновить» — требовался полный перезапуск. Причина: `GitHubETagCache` живёт в памяти и ограничен сессией. При обновлении ETag оставался закешированным, и GitHub возвращал 304 со старыми данными.
 
-Fix: `InitializeAsync` calls `_renoDxDbService.InvalidateCache()` when `forceRescan=true`, clearing the DB URLs from the ETag cache before fetching.
+Фикс: `InitializeAsync` вызывает `_renoDxDbService.InvalidateCache()` при `forceRescan=true`, очищая URL базы из ETag-кеша перед загрузкой.
 
-**Files changed:** `MainViewModel.Init.cs`, `RenoDXDbService.cs`, `IRenoDXDbService.cs`, `GitHubETagCache.cs`
+**Изменённые файлы:** `MainViewModel.Init.cs`, `RenoDXDbService.cs`, `IRenoDXDbService.cs`, `GitHubETagCache.cs`
 
 ---
 
-### 6. Detail Panel Refresh After Refresh
+### 6. Обновление панели подробностей после «Обновить»
 
-After a Refresh, `ViewModel.SelectedGame` gets set to null (new card objects don't match old references). `GameList.SelectedItem` still holds the old object so `SelectionChanged` never fires and `PopulateDetailPanel` never runs. Fixed in `MainWindow.UISync.cs`: when `IsLoading` goes false on a silent (Refresh) transition, capture the current `GameList.SelectedItem`, find the equivalent new card in `DisplayedGames`, and force `PopulateDetailPanel`.
+После «Обновить» `ViewModel.SelectedGame` становится null (новые объекты карточек не совпадают со старыми ссылками). `GameList.SelectedItem` всё ещё держит старый объект, поэтому `SelectionChanged` не срабатывает и `PopulateDetailPanel` не выполняется. Исправлено в `MainWindow.UISync.cs`: когда `IsLoading` становится false при тихом (обновление) переходе, берём текущий `GameList.SelectedItem`, находим эквивалентную новую карточку в `DisplayedGames` и форсируем `PopulateDetailPanel`.
 
-**File changed:** `MainWindow.UISync.cs`
+**Изменённый файл:** `MainWindow.UISync.cs`
 
 ---
 
-### 7. UE-Extended DB Notes in Info Dialog
+### 7. Заметки UE-Extended из базы в диалоге информации
 
-`RenoDXDbUnrealEntry.Comments` was never reaching the info dialog for NativeHDR/UE-Extended games due to three separate blocks:
-1. `MergeDbSources` only wrote Comments to `_genericNotes` in DbOnly/Hybrid mode — now always writes regardless of source mode
-2. `BuildNotes` early-returned for NativeHDR games before calling `GetGenericNote` — now includes DB comment after the HDR warning
-3. `AddonInfoResolver` never read `card.Notes` — now appends DB comment (stripped of the HDR warning line) to the fallback text
+`RenoDXDbUnrealEntry.Comments` не доходил до диалога информации для игр NativeHDR/UE-Extended из-за трёх отдельных блоков:
+1. `MergeDbSources` писал Comments в `_genericNotes` только в режиме DbOnly/Hybrid — теперь пишет всегда, независимо от режима источника
+2. `BuildNotes` рано возвращался для игр NativeHDR до вызова `GetGenericNote` — теперь включает комментарий базы после предупреждения об HDR
+3. `AddonInfoResolver` никогда не читал `card.Notes` — теперь добавляет комментарий базы (без строки предупреждения об HDR) к запасному тексту
 
 ---
 
-### 8. MFG Ada Unlock Reinstall Fix
+### 8. Исправление переустановки MFG Ada Unlock
 
-When installed via Extras section, the addon picker toggle is greyed (extrasInstalledConflict). Users couldn't deselect it in the picker. Remove button in Extras now also removes the entry from `EnabledGlobalAddons` and `PerGameAddonSelection`.
+При установке через секцию Extras переключатель в окне выбора аддонов серый (extrasInstalledConflict). Пользователи не могли его снять в окне выбора. Кнопка удаления в Extras теперь также убирает запись из `EnabledGlobalAddons` и `PerGameAddonSelection`.
 
-**File changed:** `DetailPanelBuilder.Extras.cs`
+**Изменённый файл:** `DetailPanelBuilder.Extras.cs`
 
 ---
 
-### 9. Other Fixes
+### 9. Прочие исправления
 
-- **OptiScaler Stable/Nightly uninstall removed NR-owned `nvngx_dlssnr.dll`** — step 2e now only runs for DlssNr variant
-- **DLSS files deleted twice on uninstall** — the generic step 3 loop was re-deleting files that steps 2b/2c/2d had already handled. Fixed by adding `explicitlyHandled` set
-- **Batch DLSS deploy stuck** — `MassDlssDeployDialog` now checks `Directory.Exists` before processing each game
-- **Shader pack folder renamed to `reshade-shaders-original`** — race condition in `ShaderPackService.Deploy.cs`, fixed by writing the managed marker before rename/deploy
-- **Solasta 2 Engine.ini path** — added to `engineIniPathOverrides` in manifest (`"Solasta 2": "Solasta 2"`)
-- **Stale OptiScaler sentinels** — `CleanOrphanedOptiScalerSentinels()` runs on startup, removes `.original` files whose base filename doesn't match `InstalledAs`
+- **Удаление OptiScaler Stable/Nightly удаляло принадлежащий NR `nvngx_dlssnr.dll`** — шаг 2e теперь выполняется только для варианта DlssNr
+- **Двойное удаление файлов DLSS при деинсталляции** — общий цикл шага 3 повторно удалял файлы, уже обработанные шагами 2b/2c/2d. Исправлено множеством `explicitlyHandled`
+- **Зависание массового развертывания DLSS** — `MassDlssDeployDialog` теперь проверяет `Directory.Exists` перед обработкой каждой игры
+- **Переименование папки набора шейдеров в `reshade-shaders-original`** — гонка в `ShaderPackService.Deploy.cs`, исправлена записью управляемого маркера до переименования/развертывания
+- **Путь Engine.ini для Solasta 2** — добавлен в `engineIniPathOverrides` в манифесте (`"Solasta 2": "Solasta 2"`)
+- **Устаревшие sentinel'ы OptiScaler** — `CleanOrphanedOptiScalerSentinels()` запускается при старте, удаляет файлы `.original`, чьё базовое имя не совпадает с `InstalledAs`
 
 ---
 
-## Active/Pending Items
+## Активные/ожидающие пункты
 
-### Nexus appid Registration
-Mark is in correspondence with Nexus support. Update `AppId` const in `NexusSsoService.cs` (~line 20) when they assign an official slug.
+### Регистрация appid в Nexus
+Mark переписывается с поддержкой Nexus. Обновите константу `AppId` в `NexusSsoService.cs` (~строка 20), когда выдадут официальный slug.
 
-### `rhi_install.txt` Limitations
-- Existing installs (before this session) have no manifest — uninstall falls back to staging scan. The sentinel-guard fix (only delete root files if `.original` exists in staging scan fallback) helps but isn't perfect for all edge cases.
-- The `sharedFiles` tracking only starts working once both components have been installed/reinstalled with the new code. Users who installed OptiScaler + ShortFuse before this update won't have ownership tracking until they reinstall.
+### Ограничения `rhi_install.txt`
+- Существующие установки (до этой сессии) манифеста не имеют — удаление откатывается к сканированию хранилища. Фикс с защитой sentinel'ов (удалять корневые файлы, только если `.original` есть в запасном скане хранилища) помогает, но не идеален для всех краевых случаев.
+- Отслеживание `sharedFiles` начинает работать только после того, как оба компонента установлены/переустановлены с новым кодом. У тех, кто ставил OptiScaler + ShortFuse до этого обновления, отслеживания владения не будет до переустановки.
 
 ---
 
-## Key Files Changed This Session
+## Ключевые изменённые файлы этой сессии
 
-| File | Change |
+| Файл | Изменение |
 |------|--------|
-| `Models/RhiInstallManifest.cs` | **New** — full model with all fields and static helpers |
-| `Services/OptiScalerService.Install.cs` | Manifest write/read/delete in Install/Update/Uninstall; shared file ownership; preserve fields on write |
-| `Services/Renodx5AddonService.cs` | Shared file ownership for ShortFuse/DLSS5Tool; component records; RemoveNrDll with component param |
-| `DetailPanelBuilder.NeuralRendering.cs` | Component records for Dlss5Tool/Bridge/Feeder; RestoreDlssDllsWithSentinel root cleanup; ownership calls |
-| `DetailPanelBuilder.Overrides.cs` | UpdateInstalledAs called on OS DLL rename (fixes sentinel and manifest on override change) |
-| `DetailPanelBuilder.Extras.cs` | MFG Ada Unlock remove also clears addon selection |
-| `Services/GitHubETagCache.cs` | Token auth on all requests; proper 403 handling (only rate-limit on Remaining=0) |
-| `Services/DevUnlockService.cs` | github_api.txt support for all users |
-| `Services/RenoDXDbService.cs` | InvalidateCache() method |
-| `Services/IRenoDXDbService.cs` | InvalidateCache() in interface |
-| `Services/AddonInfoResolver.cs` | DB Comments appended to NativeHDR fallback text |
-| `ViewModels/MainViewModel.cs` | MergeDbSources always writes Comments to _genericNotes |
-| `ViewModels/MainViewModel.Init.cs` | DB cache invalidate on Refresh; CleanOrphanedOptiScalerSentinels |
-| `ViewModels/MainViewModel.CacheLoad.cs` | BuildNotes includes DB comment for NativeHDR games |
-| `ViewModels/MainViewModel.BackgroundScan.cs` | CleanOrphanedOptiScalerSentinels method |
-| `ViewModels/MainViewModel.BuildCards.cs` | OsInstalledVersion from manifest first |
-| `MainWindow.UISync.cs` | Force panel rebuild for selected game after Refresh |
-| `MassDlssDeployDialog.cs` | Directory.Exists guard |
+| `Models/RhiInstallManifest.cs` | **Новый** — полная модель со всеми полями и статическими хелперами |
+| `Services/OptiScalerService.Install.cs` | Запись/чтение/удаление манифеста в Install/Update/Uninstall; совместное владение файлами; сохранение полей при записи |
+| `Services/Renodx5AddonService.cs` | Совместное владение для ShortFuse/DLSS5Tool; записи компонентов; RemoveNrDll с параметром компонента |
+| `DetailPanelBuilder.NeuralRendering.cs` | Записи компонентов для Dlss5Tool/Bridge/Feeder; чистка корня в RestoreDlssDllsWithSentinel; вызовы владения |
+| `DetailPanelBuilder.Overrides.cs` | UpdateInstalledAs вызывается при переименовании DLL OptiScaler (чинит sentinel и манифест при смене переопределения) |
+| `DetailPanelBuilder.Extras.cs` | Удаление MFG Ada Unlock также чистит выбор аддона |
+| `Services/GitHubETagCache.cs` | Токен-аутентификация во всех запросах; корректная обработка 403 (лимит только при Remaining=0) |
+| `Services/DevUnlockService.cs` | Поддержка github_api.txt для всех пользователей |
+| `Services/RenoDXDbService.cs` | Метод InvalidateCache() |
+| `Services/IRenoDXDbService.cs` | InvalidateCache() в интерфейсе |
+| `Services/AddonInfoResolver.cs` | Комментарии базы добавляются к запасному тексту NativeHDR |
+| `ViewModels/MainViewModel.cs` | MergeDbSources всегда пишет Comments в _genericNotes |
+| `ViewModels/MainViewModel.Init.cs` | Инвалидация кеша базы при «Обновить»; CleanOrphanedOptiScalerSentinels |
+| `ViewModels/MainViewModel.CacheLoad.cs` | BuildNotes включает комментарий базы для игр NativeHDR |
+| `ViewModels/MainViewModel.BackgroundScan.cs` | Метод CleanOrphanedOptiScalerSentinels |
+| `ViewModels/MainViewModel.BuildCards.cs` | OsInstalledVersion сначала из манифеста |
+| `MainWindow.UISync.cs` | Принудительная пересборка панели выбранной игры после «Обновить» |
+| `MassDlssDeployDialog.cs` | Защита Directory.Exists |
 | `manifest.json` | Solasta 2 engineIniPathOverrides |
-| `RHI_PatchNotes.md` | v2.7.1 patch notes |
+| `RHI_PatchNotes.md` | Patch notes v2.7.1 |
 
 ---
 
-## Notes for Next Agent
+## Заметки следующему агенту
 
-- Never bump version — Mark does it manually
-- Always ask before committing or pushing
-- Build: `dotnet build g:\RDXC\RenoDXCommander\RenoDXCommander.csproj --no-restore -v q -p:Platform=x64`
-- Publish: close RHI first, then `& "g:\RDXC\publish.bat"` — if RHI is running, the exe is locked and publish silently fails
-- Log access: `Copy-Item "$env:LOCALAPPDATA\RHI\Logs\*" "g:\RDXC\Logs\" -Force` then read from `g:\RDXC\Logs\`
+- Никогда не поднимайте версию — Mark делает это вручную
+- Всегда спрашивайте перед коммитом или пушем
+- Сборка: `dotnet build g:\RDXC\RenoDXCommander\RenoDXCommander.csproj --no-restore -v q -p:Platform=x64`
+- Публикация: сначала закройте RHI, затем `& "g:\RDXC\publish.bat"` — если RHI работает, exe заблокирован, и публикация тихо провалится
+- Доступ к логам: `Copy-Item "$env:LOCALAPPDATA\RHI\Logs\*" "g:\RDXC\Logs\" -Force`, затем читайте из `g:\RDXC\Logs\`
diff --git a/docs/manifest-field-reference.md b/docs/manifest-field-reference.md
index bee17d7..e0cfddf 100644
--- a/docs/manifest-field-reference.md
+++ b/docs/manifest-field-reference.md
@@ -1,349 +1,349 @@
-# RHI Manifest Field Reference
+# Справочник полей манифеста RHI
 
-Complete reference for every field in `RemoteManifest`. For each field: what it does, where in the code it's read, and what it controls.
+Полный справочник по каждому полю `RemoteManifest`. Для каждого поля: что оно делает, где в коде читается и чем управляет.
 
 ---
 
-## Game Detection Fields
+## Поля определения игр
 
 ### `blacklist` — `List<string>`
-Completely suppresses detected games from appearing in RHI (e.g. DLC launchers, anti-cheat helpers).
-- `GameInitializationService.ApplyManifest()` → populates `_manifestBlacklist`
-- `MainViewModel.Init`, `BackgroundScan`, `CacheLoad` → filters `allGames` before card build
+Полностью скрывает найденные игры из RHI (например, лаунчеры DLC, хелперы античита).
+- `GameInitializationService.ApplyManifest()` → наполняет `_manifestBlacklist`
+- `MainViewModel.Init`, `BackgroundScan`, `CacheLoad` → фильтрует `allGames` перед сборкой карточек
 
 ### `wikiNameOverrides` — `Dict<string,string>`
-Maps detected folder/store name → RenoDX wiki mod name. Fixes cases where Steam folder names don't match the wiki.
-- `GameInitializationService.ApplyManifest()` → adds to `_nameMappings` (user overrides win)
-- Marked as manifest-origin: invisible in UI, excluded from settings.json saves
+Отображение «найденная папка/магазин → имя мода в вики RenoDX». Чинит случаи, когда имена папок Steam не совпадают с вики.
+- `GameInitializationService.ApplyManifest()` → добавляет в `_nameMappings` (пользовательские переопределения сильнее)
+- Помечено как источник-манифест: невидимо в UI, исключается из сохранений settings.json
 
 ### `lumaNameOverrides` — `Dict<string,string>`
-Maps detected game names → Luma completed-mods list entry names. Separate from wiki name overrides.
-- `MainViewModel.Install.Luma.MatchLumaGame()` → highest priority, before fuzzy matching
+Отображение «имя найденной игры → имя записи в списке завершённых модов Luma». Отдельно от переопределений имён вики.
+- `MainViewModel.Install.Luma.MatchLumaGame()` → наивысший приоритет, до нечёткого сопоставления
 
 ### `installPathOverrides` — `Dict<string,string>`
-Defines the subpath within the detected install folder where mods should be deployed (e.g. `"bin\\x64"`).
-Supports pipe-separated candidates: `"Win64|WinGDK"` — tried in order, first existing wins.
-- `GameInitializationService.ApplyManifest()` → merges into shared `_installPathOverrides` dict
-- `BuildCards`, `CacheLoad`, `AddManualGame` → applies path resolution before card creation
+Задаёт подпуть внутри найденной папки установки, куда разворачиваются моды (например, `"bin\\x64"`).
+Поддерживает кандидаты через пайп: `"Win64|WinGDK"` — пробуются по порядку, побеждает первый существующий.
+- `GameInitializationService.ApplyManifest()` → вливается в общий словарь `_installPathOverrides`
+- `BuildCards`, `CacheLoad`, `AddManualGame` → применяет разрешение пути перед созданием карточки
 
 ### `splitGames` — `Dict<string,List<SplitGameEntry>>`
-Splits a single detected store entry into multiple cards, each pointing to a subfolder.
-- `MainViewModel.BuildCards` → very early, replaces one `DetectedGame` entry with N sub-entries
-- `SplitGameEntry` has `name` (card display name) and `subPath` (relative path from bundle root)
-- Entry is only created if the subPath directory actually exists on disk
+Разбивает одну найденную запись магазина на несколько карточек, каждая указывает на подпапку.
+- `MainViewModel.BuildCards` → очень рано, заменяет одну запись `DetectedGame` на N суб-записей
+- У `SplitGameEntry` есть `name` (отображаемое имя карточки) и `subPath` (относительный путь от корня бандла)
+- Запись создаётся, только если папка subPath реально существует на диске
 
 ### `engineOverrides` — `Dict<string,string>`
-Forces a specific engine type and label, overriding auto-detection.
-Special values: `"Unreal"`, `"Unreal (Legacy)"`, `"Unity"`, `"RE Engine"` → map to `EngineType` enum.
-Any other string (e.g. `"Silk"`) is stored as-is and shown in the engine badge but doesn't affect mod fallback logic.
-- `GameInitializationService.ApplyManifest()` → populates `_manifestEngineOverrides`
-- `MainViewModel.GameMatching.ResolveEngineOverride()` → called during card build
-- When present: engine badge `isClickable = false` (no user toggling)
+Форсирует конкретный тип движка и подпись, перекрывая автоопределение.
+Специальные значения: `"Unreal"`, `"Unreal (Legacy)"`, `"Unity"`, `"RE Engine"` → отображаются на перечисление `EngineType`.
+Любая другая строка (например, `"Silk"`) сохраняется как есть и показывается на значке движка, но не влияет на логику запасных модов.
+- `GameInitializationService.ApplyManifest()` → наполняет `_manifestEngineOverrides`
+- `MainViewModel.GameMatching.ResolveEngineOverride()` → вызывается при сборке карточек
+- При наличии: значок движка `isClickable = false` (без переключения пользователем)
 
 ### `engineHintOverrides` — `Dict<string,string>`
-Sets the `EngineHint` display string on a card (e.g. `"4.27.2"`, `"5.3.2"`). Unlike `engineOverrides`, display/hint only — doesn't affect `EngineType` enum.
-- `BuildCards`, `CacheLoad` → `card.EngineHint = manifestEngineHint` after card construction
-- Used by Engine.ini deployment to decide UE4 vs UE5 HDR key behaviour
+Задаёт отображаемую строку `EngineHint` на карточке (например, `"4.27.2"`, `"5.3.2"`). В отличие от `engineOverrides` — только отображение/подсказка, на перечисление `EngineType` не влияет.
+- `BuildCards`, `CacheLoad` → `card.EngineHint = manifestEngineHint` после сборки карточки
+- Используется развертыванием Engine.ini для выбора поведения ключей HDR UE4 против UE5
 
 ### `engineIniPathOverrides` — `Dict<string,string>`
-Overrides the Unreal project name used to locate `Engine.ini` in `%LocalAppData%`. Supports pipe-separated candidates and absolute paths.
+Переопределяет имя проекта Unreal для поиска `Engine.ini` в `%LocalAppData%`. Поддерживает кандидаты через пайп и абсолютные пути.
 - `BuildCards`, `CacheLoad`, `AddManualGame` → `card.EngineIniProjectOverride`
-- Consumed by all `AuxInstallService` Engine.ini methods, the 📋 INI button, and the cog dialog HDR/LUT handlers
+- Потребляется всеми методами Engine.ini в `AuxInstallService`, кнопкой 📋 INI и обработчиками HDR/LUT диалога шестерёнки
 
 ### `emulatorGames` — `Dict<string,EmulatorConfig>`
-Drives the Ryubing emulator bundle. Defines which addons to download and their fallback URLs.
-- `BuildCards`, `CacheLoad`, `AddManualGame` → sets `card.EmulatorAddonNames` and synthetic `GameMod`
-- `InstallEmulatorAddonsAsync()` → reads `AddonUrls` per wiki name (manifest priority > wiki-scraped)
-- `EmulatorConfig` fields: `addons` (wiki game name list), `addonUrls` (per-game URL overrides)
+Управляет бандлом эмулятора Ryubing. Определяет, какие аддоны скачивать, и их запасные ссылки.
+- `BuildCards`, `CacheLoad`, `AddManualGame` → задаёт `card.EmulatorAddonNames` и синтетический `GameMod`
+- `InstallEmulatorAddonsAsync()` → читает `AddonUrls` по имени в вики (манифест приоритетнее вики-скрейпа)
+- Поля `EmulatorConfig`: `addons` (список имён игр в вики), `addonUrls` (поигровые переопределения ссылок)
 
 ### `thirtyTwoBitGames` / `sixtyFourBitGames` — `List<string>`
-Forces 32-bit or 64-bit mode independent of PE header detection. Determines which DLL variant is deployed.
-- `GameInitializationService.ApplyManifest()` → populates `_manifest32BitGames` / `_manifest64BitGames`
-- `MainViewModel.GameMatching.ResolveIs32Bit()` → checked after user override, before PE-header fallback
+Форсирует 32-бит или 64-бит режим независимо от анализа PE-заголовка. Определяет, какой вариант DLL развернётся.
+- `GameInitializationService.ApplyManifest()` → наполняет `_manifest32BitGames` / `_manifest64BitGames`
+- `MainViewModel.GameMatching.ResolveIs32Bit()` → проверяется после пользовательского переопределения, до PE-заголовочного запасного варианта
 
 ### `dlssSkipGames` — `List<string>`
-Suppresses the DLSS/Streamline file scan for specific games (false positives, known irrelevant, or slow).
-- `MainViewModel.BuildCards` → inline check, skips the entire DLSS detection block when matched
+Подавляет сканирование файлов DLSS/Streamline для конкретных игр (ложные срабатывания, заведомо нерелевантные или медленные).
+- `MainViewModel.BuildCards` → инлайн-проверка, при совпадении пропускает весь блок обнаружения DLSS
 
 ### `steamAppIdOverrides` — `Dict<string,int>`
-Forces a specific Steam AppID regardless of ACF manifest or `steam_appid.txt`.
-- `SteamAppIdResolver.ResolveAsync()` → highest priority (step 1 of 5)
-- Used for PCGW lookups (HDR database, DLSS data) and Steam launch commands
+Форсирует конкретный Steam AppID независимо от ACF-манифеста или `steam_appid.txt`.
+- `SteamAppIdResolver.ResolveAsync()` → наивысший приоритет (шаг 1 из 5)
+- Используется для запросов PCGW (база HDR, данные DLSS) и команд запуска Steam
 
 ---
 
-## UE-Extended / HDR Feature Flags
+## Флаги UE-Extended / HDR
 
 ### `ueExtendedGames` — `List<string>`
-Games that should use `renodx-ue-extended.addon64` instead of the standard generic UE addon.
-- `GameInitializationService.ApplyManifest()` → adds to `gameNameService.UeExtendedGames`
-- Lowest priority UE-Extended signal — overridden by `ueExtendedCompatibility`, blocked by `noUeExtendedGames`
+Игры, которые должны использовать `renodx-ue-extended.addon64` вместо стандартного универсального UE-аддона.
+- `GameInitializationService.ApplyManifest()` → добавляет в `gameNameService.UeExtendedGames`
+- Самый низкоприоритетный сигнал UE-Extended — перекрывается `ueExtendedCompatibility`, блокируется `noUeExtendedGames`
 
 ### `nativeHdrGames` — `List<string>`
-Games that force UE-Extended on AND bypass the `hasNamedMod` block (named wiki mod won't prevent UE-Extended).
-- `GameInitializationService.ApplyManifest()` → populates `_manifestNativeHdrGames`
-- `IsNativeHdrGameMatch()` → drives `isNativeHdr = true` in BuildCards/Install
-- Effect: forces UE-Extended, hides toggle, sets `card.IsNativeHdrGame = true`
+Игры, которые форсируют включение UE-Extended и обходят блок `hasNamedMod` (именной мод из вики не помешает UE-Extended).
+- `GameInitializationService.ApplyManifest()` → наполняет `_manifestNativeHdrGames`
+- `IsNativeHdrGameMatch()` → включает `isNativeHdr = true` в BuildCards/Install
+- Эффект: форсирует UE-Extended, скрывает переключатель, ставит `card.IsNativeHdrGame = true`
 
 ### `noUeExtendedGames` — `List<string>`
-Highest-priority block on UE-Extended. Overrides everything including `nativeHdrGames` and user opt-in.
-- `GameInitializationService.ApplyManifest()` → populates `_manifestNoUeExtendedGames`
-- BuildCards/AddManualGame → `noUeExtended` gate is the first check in `useUeExt` decision tree
+Высший по приоритету блок UE-Extended. Перекрывает всё, включая `nativeHdrGames` и согласие пользователя.
+- `GameInitializationService.ApplyManifest()` → наполняет `_manifestNoUeExtendedGames`
+- BuildCards/AddManualGame → врата `noUeExtended` — первая проверка в дереве решения `useUeExt`
 
 ### `ueExtendedCompatibility` — `Dict<string,UeExtendedCompatEntry>`
-Highest-priority UE-Extended config. Replaces both `nativeHdrGames` and `ueExtendedGames` (v2+ feature).
-Presence in the dict = forced UE-Extended. Entry controls Engine.ini deployment:
-- `hdr: false` → skip HDR keys (game has its own in-engine HDR option); default: deploy for UE5, skip for UE4
-- `lut: false` → skip LUT key; default: always deploy
-- `GameInitializationService.ApplyManifest()` → populates `_manifestUeExtendedCompat` AND adds keys to `_manifestNativeHdrGames`
-- `MainViewModel.Install.InstallModAsync()`, `UpdateOrchestrationService` → read `deployHdr`/`deployLut` from entry
+Конфигурация UE-Extended с наивысшим приоритетом. Заменяет и `nativeHdrGames`, и `ueExtendedGames` (функция с v2+).
+Присутствие в словаре = форсированный UE-Extended. Запись управляет развертыванием Engine.ini:
+- `hdr: false` → пропустить ключи HDR (у игры есть собственная HDR-опция в движке); по умолчанию: разворачивать для UE5, пропускать для UE4
+- `lut: false` → пропустить ключ LUT; по умолчанию: всегда разворачивать
+- `GameInitializationService.ApplyManifest()` → наполняет `_manifestUeExtendedCompat` И добавляет ключи в `_manifestNativeHdrGames`
+- `MainViewModel.Install.InstallModAsync()`, `UpdateOrchestrationService` → читают `deployHdr`/`deployLut` из записи
 
 ### `lumaRenodxCompat` — `List<string>`
-Games where RenoDX and Luma can coexist. Normally enabling Luma removes the RenoDX mod.
+Игры, где RenoDX и Luma могут сосуществовать. Обычно включение Luma удаляет мод RenoDX.
 - `BuildCards`, `CacheLoad`, `AddManualGame` → `card.LumaRenodxCompatible = true`
-- **Important:** uses direct `Contains(game.Name)` — exact match, no normalization
-- Effect: keeps RenoDX row visible in Luma mode, skips RenoDX removal on Luma install
+- **Важно:** использует прямой `Contains(game.Name)` — точное совпадение, без нормализации
+- Эффект: строка RenoDX остаётся видимой в режиме Luma, при установке Luma удаление RenoDX пропускается
 
 ### `lumaDefaultGames` — `List<string>`
-Games that auto-enable Luma mode on first detection without user action. Respects prior toggles.
-- `MainViewModel.BuildCards` → auto-adds composite key to `_lumaEnabledGames` if no prior user toggle
-- NOT applied during CacheLoad phase (only fires in Phase 2)
+Игры, автоматически включающие режим Luma при первом обнаружении без действий пользователя. Учитывает ранее заданные переключения.
+- `MainViewModel.BuildCards` → авто-добавляет составной ключ в `_lumaEnabledGames`, если пользовательского переключения не было
+- НЕ применяется на фазе CacheLoad (срабатывает только во второй фазе)
 
 ---
 
-## Install Behaviour Fields
+## Поля поведения установки
 
 ### `forceExternalOnly` — `Dict<string,ForceExternalEntry>`
-Forces a card into redirect-only mode. Install button becomes an external link instead of a direct download.
-- `GameInitializationService.ApplyManifestCardOverrides()` → sets `card.IsExternalOnly = true`, `card.ExternalUrl`, `card.ExternalLabel`, `card.WikiStatus`
-- Entry has `url` (download link) and `label` (button text)
-- Key must match the **detected** game name, NOT the wiki-mapped name
+Форсирует карточку в режим «только перенаправление». Кнопка установки становится внешней ссылкой вместо прямого скачивания.
+- `GameInitializationService.ApplyManifestCardOverrides()` → ставит `card.IsExternalOnly = true`, `card.ExternalUrl`, `card.ExternalLabel`, `card.WikiStatus`
+- У записи есть `url` (ссылка на скачивание) и `label` (текст кнопки)
+- Ключ должен совпадать с **найденным** именем игры, а НЕ с привязанным именем вики
 
 ### `snapshotOverrides` — `Dict<string,string>`
-Injects or overrides the addon download URL for a game when wiki scraping fails or captures the wrong URL.
-- `BuildCards`, `Install`, `AddManualGame` → sets `effectiveMod.SnapshotUrl`
-- **Warning:** any non-null `SnapshotUrl` makes `hasNamedMod = true`, blocking UE-Extended. Never add NativeHDR generic UE games here.
+Внедряет или переопределяет ссылку скачивания аддона для игры, когда вики-скрейп не удался или выхватил неверный URL.
+- `BuildCards`, `Install`, `AddManualGame` → задаёт `effectiveMod.SnapshotUrl`
+- **Предупреждение:** любой непустой `SnapshotUrl` делает `hasNamedMod = true`, блокируя UE-Extended. Никогда не добавляйте сюда универсальные UE-игры с NativeHDR.
 
 ### `installWarnings` — `Dict<string,Dict<string,string>>`
-Per-game, per-component blocking confirm dialogs before install. User can cancel to abort.
-Structure: `{ "Game Name": { "reshade": "warning text", "renodx": "...", ... } }`
-- `MainViewModel.Install.Luma.CheckInstallWarningAsync(gameName, component)` → shows ContentDialog
-- Wired for all 8 components: `reshade`, `renodx`, `relimiter`, `dc`, `optiscaler`, `luma`, `reframework`, `dxvk`
+Поигровые, покомпонентные блокирующие диалоги подтверждения перед установкой. Пользователь может отменить и прервать.
+Структура: `{ "Game Name": { "reshade": "warning text", "renodx": "...", ... } }`
+- `MainViewModel.Install.Luma.CheckInstallWarningAsync(gameName, component)` → показывает ContentDialog
+- Подключено для всех 8 компонентов: `reshade`, `renodx`, `relimiter`, `dc`, `optiscaler`, `luma`, `reframework`, `dxvk`
 
 ### `dllNameOverrides` — `Dict<string,ManifestDllNames>`
-Forces specific DLL proxy filenames (ReShade and/or DC) per game. Example: `{ "reshade": "winmm.dll", "dc": "" }`
-- `GameInitializationService.ApplyManifest()` → populates `_manifestDllNameOverrides`
-- `GetManifestDllNames()` → exact → trademark-stripped → normalized lookup
-- `InstallReShadeAsync()`, `ApplyManifestDllRenames()` → use `.ReShade` filename
+Форсирует конкретные имена прокси-DLL (ReShade и/или DC) для игры. Пример: `{ "reshade": "winmm.dll", "dc": "" }`
+- `GameInitializationService.ApplyManifest()` → наполняет `_manifestDllNameOverrides`
+- `GetManifestDllNames()` → точное → без торговых знаков → нормализованное сопоставление
+- `InstallReShadeAsync()`, `ApplyManifestDllRenames()` → используют имя `.ReShade`
 
 ### `optiScalerDllOverrides` — `Dict<string,string>`
-Per-game OptiScaler DLL filename override. **Declared in the model but not currently consumed in code** — reserved for future use.
+Поигровое переопределение имени DLL OptiScaler. **Объявлено в модели, но кодом пока не потребляется** — зарезервировано на будущее.
 
 ### `gacSymlinkGames` — `Dict<string,string>`
-Routes XNA Framework games (e.g. Terraria) through a GAC symlink install instead of a normal DLL copy.
-- `GetGacSymlinkPath()` → checks this dict; if found, `InstallReShadeAsync` routes to `InstallReShadeGacAsync`
-- Dict value = absolute GAC directory path. Requires admin privileges.
+Направляет игры на XNA Framework (например, Terraria) через установку GAC-симлинком вместо обычного копирования DLL.
+- `GetGacSymlinkPath()` → проверяет этот словарь; при наличии `InstallReShadeAsync` уходит в `InstallReShadeGacAsync`
+- Значение словаря = абсолютный путь каталога GAC. Требуются права администратора.
 
 ### `legacyReShadeVersions` — `Dict<string,string>`
-Auto-assigns a locked legacy ReShade channel per game (e.g. `"Max Payne 3": "6.4.1"`). Never overwrites existing user overrides.
-- `MainViewModel.Init`, `BackgroundScan` → calls `SetReShadeChannelOverride(gameName, version)` if no existing override (checks both name-only and composite keys)
+Автоназначает игре заблокированный старый канал ReShade (например, `"Max Payne 3": "6.4.1"`). Никогда не перезаписывает существующие пользовательские переопределения.
+- `MainViewModel.Init`, `BackgroundScan` → вызывает `SetReShadeChannelOverride(gameName, version)`, если переопределения нет (проверяются и ключ по имени, и составной)
 
 ### `legacyReShadeAvailable` — `List<string>`
-List of version strings shown in the legacy ReShade version picker dialog. Server-managed.
-- `DetailPanelBuilder.Overrides.RsChannel` → RS channel picker, populates `RadioButtons` in the `"Legacy..."` selection dialog
+Список строк версий, показываемых в диалоге выбора старых версий ReShade. Управляется сервером.
+- `DetailPanelBuilder.Overrides.RsChannel` → выбор канала RS, наполняет `RadioButtons` в диалоге выбора «Legacy…»
 
 ### `launchExeOverrides` — `Dict<string,string>`
-Relative exe path from InstallPath. Used for two purposes:
-1. **Game launch** (priority 2, after user override): `MainWindow.Events.Install.LaunchGame()` → `Path.Combine(card.InstallPath, manifestExe)`
-2. **NVIDIA profile matching**: `DlssPresetService.FindProfileUncached()` → matches the exe filename against NVIDIA driver profile application entries
+Относительный путь exe от InstallPath. Используется для двух целей:
+1. **Запуск игры** (приоритет 2, после пользовательского переопределения): `MainWindow.Events.Install.LaunchGame()` → `Path.Combine(card.InstallPath, manifestExe)`
+2. **Сопоставление профилей NVIDIA**: `DlssPresetService.FindProfileUncached()` → сверяет имя exe с записями приложений в профилях драйвера NVIDIA
 
 ### `renodxIniOverrides` — `Dict<string,Dict<string,string>>`
-Per-game `[renodx]` INI keys written to `reshade.ini` on install/update. Only adds/updates — never removes user values (unless `forceOverwrite: true`).
-- Read via `AuxInstallService.GlobalManifest?.RenodxIniOverrides` in: `InstallModAsync`, `UpdateAllRenoDxAsync`, `MergeRsIni` button, `RdxCogButton_Click` redeploy
+Поигровые ключи INI секции `[renodx]`, записываемые в `reshade.ini` при установке/обновлении. Только добавляет/обновляет — никогда не удаляет пользовательские значения (если не `forceOverwrite: true`).
+- Читается через `AuxInstallService.GlobalManifest?.RenodxIniOverrides` в: `InstallModAsync`, `UpdateAllRenoDxAsync`, кнопке «Слить RS INI», повторном развертывании `RdxCogButton_Click`
 
 ### `renodxExtraSettings` — `List<RenodxExtraSetting>`
-Adds extra ComboBox rows to the RenoDX ⚙ cog Compatibility Settings grid without a client update.
-- `MainWindow.Events.Components.RdxCogButton_Click` → appends rows; `SelectionChanged` writes chosen value to `[renodx]` INI section
-- Each entry: `key`, `label`, `default`, `options` (array of `{value, name}` pairs)
+Добавляет дополнительные строки ComboBox в сетку настроек совместимости шестерёнки ⚙ RenoDX без обновления клиента.
+- `MainWindow.Events.Components.RdxCogButton_Click` → добавляет строки; `SelectionChanged` пишет выбранное значение в секцию INI `[renodx]`
+- Каждая запись: `key`, `label`, `default`, `options` (массив пар `{value, name}`)
 
 ### `pdUpscalerGames` — `Dict<string,string>`
-RE Engine games that need the PD-Upscaler REFramework build when OptiScaler is installed.
-- `InstallEventHandler.InstallOsButton_Click` → after OptiScaler install, if `dinput8.dll` exists, swaps to PD-Upscaler REFramework
-- `UninstallOsButton_Click` → restores standard REFramework on uninstall
-- Dict value = nightly.link artifact name (e.g. `"RE2"`, `"RE7"`, `"RE8"`)
+Игры RE Engine, которым нужна сборка REFramework с PD-Upscaler, когда установлен OptiScaler.
+- `InstallEventHandler.InstallOsButton_Click` → после установки OptiScaler, если существует `dinput8.dll`, подменяет на PD-Upscaler REFramework
+- `UninstallOsButton_Click` → при удалении возвращает стандартный REFramework
+- Значение словаря = имя артефакта на nightly.link (например, `"RE2"`, `"RE7"`, `"RE8"`)
 
 ---
 
-## DXVK Fields
+## Поля DXVK
 
 ### `dxvkBlacklist` — `List<string>`
-Prevents the DXVK toggle from being enabled (greyed out with anti-cheat tooltip).
+Запрещает включение переключателя DXVK (серый с подсказкой про античит).
 - `GameInitializationService.ApplyManifestCardOverrides()` → `card.IsDxvkBlacklisted = true`
-- `GameCardViewModel.Dxvk.IsDxvkToggleEnabled` → returns `false`; tooltip shows anti-cheat warning
+- `GameCardViewModel.Dxvk.IsDxvkToggleEnabled` → возвращает `false`; в подсказке предупреждение об античите
 
 ### `dxvkApiOverrides` — `Dict<string,string>`
-Intended per-game DXVK DLL selection override (`"DX8"`, `"DX9"`, etc.). **The value string is not currently consumed** — only existence is checked.
-- `ApplyManifestCardOverrides()` → `card.HasDxvkApiOverride = true` (presence only)
-- Effect: unlocks DXVK toggle for games where GraphicsApi is Unknown
+Предполагаемое поигровое переопределение выбора DLL DXVK (`"DX8"`, `"DX9"` и т.п.). **Строка-значение пока не потребляется** — проверяется только сам факт наличия.
+- `ApplyManifestCardOverrides()` → `card.HasDxvkApiOverride = true` (только наличие)
+- Эффект: разблокирует переключатель DXVK для игр с GraphicsApi = Unknown
 
 ### `dxvkGameNotes` — `Dict<string,GameNoteEntry>`
-Per-game notes shown in the DXVK Info dialog. Appended after the generic DXVK description.
-- `MainWindow.Events.Install.DxvkInfoButton_Click()` → direct inline read (bypasses `AddonInfoResolver`)
+Поигровые заметки в диалоге информации DXVK. Добавляются после общего описания DXVK.
+- `MainWindow.Events.Install.DxvkInfoButton_Click()` → прямой инлайн-чтение (мимо `AddonInfoResolver`)
 
 ---
 
-## Info Button Content Fields
+## Поля содержимого кнопок «Инфо»
 
-All `*GameInfo` and `gameNotes` fields are routed through `AddonInfoResolver.GetManifestDict(AddonType)` for the component info button dialogs. `DxvkGameNotes` is the exception — read directly in the DXVK info button handler.
+Все поля `*GameInfo` и `gameNotes` идут через `AddonInfoResolver.GetManifestDict(AddonType)` в диалогах кнопок информации компонентов. Исключение — `DxvkGameNotes`: читается напрямую в обработчике кнопки информации DXVK.
 
-| Field | Component info button | Notes |
+| Поле | Кнопка информации компонента | Примечания |
 |-------|----------------------|-------|
-| `gameNotes` | RenoDX | Also has a secondary path via `ApplyManifestCardOverrides` → `card.Notes` |
+| `gameNotes` | RenoDX | Есть и второй путь через `ApplyManifestCardOverrides` → `card.Notes` |
 | `reshadeGameInfo` | ReShade | — |
 | `relimiterGameInfo` | ReLimiter | — |
 | `displayCommanderGameInfo` | Display Commander | — |
 | `reframeworkGameInfo` | RE Framework | — |
 | `optiScalerGameInfo` | OptiScaler | — |
-| `lumaGameInfo` | Luma (primary) | — |
-| `lumaGameNotes` | Luma (supplementary) | Also has secondary path via `ApplyManifestCardOverrides` → `card.LumaNotes`; also read directly in `AddonInfoResolver.TryResolveLumaWiki()` as a supplement after LumaMod wiki notes |
-| `dxvkGameNotes` | DXVK | Read inline in `DxvkInfoButton_Click`, NOT via `AddonInfoResolver` |
+| `lumaGameInfo` | Luma (основное) | — |
+| `lumaGameNotes` | Luma (дополнительное) | Есть и второй путь через `ApplyManifestCardOverrides` → `card.LumaNotes`; также читается напрямую в `AddonInfoResolver.TryResolveLumaWiki()` как дополнение после заметок вики LumaMod |
+| `dxvkGameNotes` | DXVK | Читается инлайн в `DxvkInfoButton_Click`, НЕ через `AddonInfoResolver` |
 
-All use `GameNoteEntry` schema: `{ notes, notesUrl, notesUrlLabel }`.
+Все используют схему `GameNoteEntry`: `{ notes, notesUrl, notesUrlLabel }`.
 
 ---
 
-## Wiki Status / Author Fields
+## Поля статуса вики / авторов
 
 ### `wikiStatusOverrides` — `Dict<string,string>`
-Overrides the status emoji on wiki `GameMod` entries (e.g. `"✅"`, `"🚧"`) without requiring a wiki edit.
-- `GameInitializationService.ApplyManifestStatusOverrides()` → iterates `_allMods`, sets `mod.Status`
-- Called after wiki fetch in both Init and BackgroundScan
+Переопределяет значок статуса у записей `GameMod` вики (например, `"✅"`, `"🚧"`) без правки вики.
+- `GameInitializationService.ApplyManifestStatusOverrides()` → обходит `_allMods`, ставит `mod.Status`
+- Вызывается после загрузки вики и в Init, и в BackgroundScan
 
 ### `wikiUnlinks` — `List<string>`
-Completely severs a game from the wiki/mod system. No RenoDX row, no generic engine fallback.
-Unlike `blacklist`, the card still appears — it just has no mod options.
-- `GameInitializationService.ApplyManifest()` → populates `_manifestWikiUnlinks`
-- `BuildCards`, `InstallModAsync`, `AddManualGame` → if `_manifestWikiUnlinks.Contains(game.Name)` → `mod = null`, `fallback = null`
+Полностью отрезает игру от системы вики/модов. Ни строки RenoDX, ни универсального запасного мода движка.
+В отличие от `blacklist`, карточка остаётся видимой — просто без опций модов.
+- `GameInitializationService.ApplyManifest()` → наполняет `_manifestWikiUnlinks`
+- `BuildCards`, `InstallModAsync`, `AddManualGame` → если `_manifestWikiUnlinks.Contains(game.Name)` → `mod = null`, `fallback = null`
 
 ### `donationUrls` — `Dict<string,string>`
-Donation page URLs keyed by author display name. Merged into the hardcoded dictionary; manifest entries take priority.
+Ссылки на страницы пожертвований по отображаемым именам авторов. Вливаются в захардкоженный словарь; записи манифеста приоритетнее.
 - `MainViewModel.Init`, `BackgroundScan` → `GameCardViewModel.MergeManifestAuthorData(DonationUrls, AuthorDisplayNames)`
 
 ### `authorDisplayNames` — `Dict<string,string>`
-Display-name overrides for wiki maintainer handles (e.g. `"oopydoopy": "Jon"`). Merged into hardcoded dict.
-- `GameCardViewModel.MergeManifestAuthorData()` → same call as `donationUrls`
+Переопределения отображаемых имён мейнтейнеров из вики (например, `"oopydoopy": "Jon"`). Вливаются в захардкоженный словарь.
+- `GameCardViewModel.MergeManifestAuthorData()` → тот же вызов, что у `donationUrls`
 
 ### `authorOverrides` — `Dict<string,string>`
-Sets the mod author for games with no wiki entry (Discord/Nexus-only mods).
-- `GameInitializationService.ApplyManifestCardOverrides()` → `card.Maintainer = author` (only if `card.Maintainer` is empty)
+Задаёт автора мода для игр без записи в вики (моды только на Discord/Nexus).
+- `GameInitializationService.ApplyManifestCardOverrides()` → `card.Maintainer = author` (только если `card.Maintainer` пуст)
 
 ---
 
-## URL Override Fields
+## Поля переопределения ссылок
 
-All follow the same pattern in their respective services — checked first before any scraped/cached source.
+Все следуют одному паттерну в своих сервисах — проверяются первыми, до любых источников из скрейпа/кеша.
 
-| Field | Used in | What it overrides |
+| Поле | Где используется | Что переопределяет |
 |-------|---------|-------------------|
-| `nexusUrlOverrides` | `NexusModsService` | Game → Nexus Mods page URL |
-| `pcgwUrlOverrides` | `PcgwService` | Game → PCGamingWiki page URL |
-| `uwFixUrlOverrides` | `UltraWideFixService` | Game → ultrawide fix URL |
-| `ultraPlusUrlOverrides` | `UltraPlusService` | Game → Ultra+ URL |
-| `steamAppIdOverrides` | `SteamAppIdResolver.ResolveAsync()` | Forces Steam AppID (highest priority, before ACF/file detection) |
+| `nexusUrlOverrides` | `NexusModsService` | Игра → URL страницы Nexus Mods |
+| `pcgwUrlOverrides` | `PcgwService` | Игра → URL страницы PCGamingWiki |
+| `uwFixUrlOverrides` | `UltraWideFixService` | Игра → URL ультраширокого исправления |
+| `ultraPlusUrlOverrides` | `UltraPlusService` | Игра → URL Ultra+ |
+| `steamAppIdOverrides` | `SteamAppIdResolver.ResolveAsync()` | Форсирует Steam AppID (наивысший приоритет, до ACF/файлов) |
 
 ### `optiScalerWikiNames` — `Dict<string,string>`
-Maps RHI game names to their OptiScaler wiki compatibility list names (when they differ).
-- `AddonInfoResolver.ResolveOptiScalerWikiName()` → used before all OptiScaler wiki lookups
+Отображает имена игр RHI на имена в списке совместимости вики OptiScaler (когда они различаются).
+- `AddonInfoResolver.ResolveOptiScalerWikiName()` → используется перед всеми запросами вики OptiScaler
 
 ---
 
-## NVIDIA Profile / DLSS Fields
+## Поля профилей NVIDIA / DLSS
 
 ### `profileExeExclusions` — `List<string>`
-Additional exe names excluded from NVIDIA profile matching (extends the hardcoded defaults).
-- `DlssPresetService.ApplyManifestProfileConfig()` → merged into `_excludedProfileExeNames`
+Дополнительные имена exe, исключаемые из сопоставления профилей NVIDIA (расширяет захардкоженные значения по умолчанию).
+- `DlssPresetService.ApplyManifestProfileConfig()` → вливается в `_excludedProfileExeNames`
 - `FindProfileUncached()` → `exeNames.ExceptWith(_excludedProfileExeNames)`
 
 ### `profileNameOverrides` — `Dict<string,string>`
-Redirects a game name to a different NVIDIA driver profile name (first lookup attempted, before exe scanning).
-- `DlssPresetService.ApplyManifestProfileConfig()` → stored as `_profileNameOverrides`
-- `FindProfileUncached()` → checked before exact title match and exe scanning
+Перенаправляет имя игры на другое имя профиля драйвера NVIDIA (первая попытка сопоставления, до сканирования exe).
+- `DlssPresetService.ApplyManifestProfileConfig()` → сохраняется как `_profileNameOverrides`
+- `FindProfileUncached()` → проверяется до точного совпадения названия и сканирования exe
 
 ### `dlssPresets` — `ManifestDlssPresets`
-Injects new DLSS preset options (SR/RR/FG) into the detail panel dropdowns without a client update.
-- `DlssPresetService.ApplyManifestPresets()` → merges `.Sr`, `.Rr`, `.Fg` into static preset arrays
-- Called at Init and BackgroundScan. `disabled: true` entries remove existing presets by name.
+Внедряет новые варианты пресетов DLSS (SR/RR/FG) в выпадающие списки панели подробностей без обновления клиента.
+- `DlssPresetService.ApplyManifestPresets()` → вливает `.Sr`, `.Rr`, `.Fg` в статические массивы пресетов
+- Вызывается при Init и BackgroundScan. Записи с `disabled: true` удаляют существующие пресеты по имени.
 
 ### `rtxHdrInfoUrl` — `string`
-URL for the RTX HDR Calibration Guide hyperlink in the RenoDX info dialog. Falls back to a hardcoded Reddit post.
-- Read in `DialogService.Game` info dialog builder
+URL гиперссылки «Руководство по калибровке RTX HDR» в диалоге информации RenoDX. Запасной вариант — захардкоженный пост на Reddit.
+- Читается в конструкторе диалога информации `DialogService.Game`
 
 ---
 
-## DOF Fix Fields
+## Поля DOF Fix
 
 ### `dofFixSkipGames` — `List<string>`
-Suppresses DOF Fix eligibility for specific games (no DOF issue, or known incompatible).
+Подавляет применимость DOF Fix для конкретных игр (нет проблемы с DOF или заведомо несовместимо).
 - `MainViewModel.Init`, `BackgroundScan` → `DofFixService.SetSkipGames()`
-- `DofFixService.IsEligible()` → returns `false` if game is on the list
+- `DofFixService.IsEligible()` → возвращает `false`, если игра в списке
 
 ### `dofFixForceGames` — `List<string>`
-Force-enables DOF Fix eligibility for games where UE engine detection fails. 64-bit requirement still applies.
+Форсирует применимость DOF Fix для игр, где не сработало определение движка UE. Требование 64-бит остаётся.
 - `MainViewModel.Init`, `BackgroundScan` → `DofFixService.SetForceGames()`
-- `DofFixService.IsForceEligible()` → returns `true`
+- `DofFixService.IsForceEligible()` → возвращает `true`
 
 ---
 
-## Graphics API Fields
+## Поля графического API
 
 ### `graphicsApiOverrides` — `Dict<string,string>`
-Forces a specific graphics API badge, overriding all PE import scanning.
-Supports comma-separated multi-API: `"DX12, VLK"` marks a game as dual-API.
-Valid tokens: `DX8`, `DX9`, `DX10`, `DX11`, `DX12`, `Vulkan`/`VLK`, `OpenGL`/`OGL`.
-- `MainViewModel.GameMatching.DetectGraphicsApi()` and `_DetectAllApisForCard()` → checked after user API override, before all filesystem scanning
-- Affects: API badge on card, auto-selected ReShade DLL filename, DXVK toggle visibility, DXVK DLL deployment
+Форсирует конкретный значок графического API, перекрывая весь анализ PE-импортов.
+Поддерживает несколько API через запятую: `"DX12, VLK"` помечает игру как dual-API.
+Допустимые токены: `DX8`, `DX9`, `DX10`, `DX11`, `DX12`, `Vulkan`/`VLK`, `OpenGL`/`OGL`.
+- `MainViewModel.GameMatching.DetectGraphicsApi()` и `_DetectAllApisForCard()` → проверяется после пользовательского переопределения API, до любого сканирования файловой системы
+- Влияет на: значок API на карточке, автоматически выбранное имя DLL ReShade, видимость переключателя DXVK, развертывание DLL DXVK
 
 ---
 
-## Pack / Preset Override Fields
+## Поля переопределения наборов / пресетов
 
 ### `shaderPacks` — `Dict<string,ManifestShaderPack>`
-Add, override, or disable shader packs without a client update.
-- `ShaderPackService.ApplyManifestOverrides()` → called at Init and BackgroundScan
-- `disabled: true` removes the pack from the active list
-- New packs require at minimum a `url` and `kind` (`"GhRelease"` or `"DirectUrl"`)
+Добавление, переопределение или отключение наборов шейдеров без обновления клиента.
+- `ShaderPackService.ApplyManifestOverrides()` → вызывается при Init и BackgroundScan
+- `disabled: true` убирает набор из активного списка
+- Для новых наборов минимум: `url` и `kind` (`"GhRelease"` или `"DirectUrl"`)
 
 ### `addonPacks` — `Dict<string,ManifestAddonPack>`
-Add, override, or disable addon entries without a client update. Keyed by `SectionId`.
-- `AddonPackService.ApplyManifestOverrides()` → called at Init and BackgroundScan; also re-applied when the Addon Manager dialog opens
-- `disabled: true` removes the addon
+Добавление, переопределение или отключение записей аддонов без обновления клиента. Ключ — `SectionId`.
+- `AddonPackService.ApplyManifestOverrides()` → вызывается при Init и BackgroundScan; также повторно применяется при открытии диалога менеджера аддонов
+- `disabled: true` убирает аддон
 
 ### `componentUrls` — `Dict<string,string>`
-Override base download URLs for components. Active keys:
-- `"ueExtended"` → UE-Extended addon download URL (`MainViewModel.Install.UeExtendedUrl`)
-- `"ueDofFix"` → DOF Fix URL override (`DofFixService.ManifestUrlOverride`, set at Init/BackgroundScan)
+Переопределение базовых ссылок скачивания компонентов. Активные ключи:
+- `"ueExtended"` → ссылка скачивания аддона UE-Extended (`MainViewModel.Install.UeExtendedUrl`)
+- `"ueDofFix"` → переопределение URL DOF Fix (`DofFixService.ManifestUrlOverride`, задаётся при Init/BackgroundScan)
 
 ---
 
-## Other Fields
+## Прочие поля
 
 ### `version` — `int`
-Manifest version integer. Logged on every `ApplyManifest` call for diagnostics.
+Целочисленная версия манифеста. Пишется в лог при каждом вызове `ApplyManifest` для диагностики.
 
-### `gacSymlinkGames` — see [Install Behaviour Fields](#gacSymlinkGames)
+### `gacSymlinkGames` — см. [Поля поведения установки](#gacSymlinkGames)
 
 ---
 
-## Key Architectural Notes
+## Ключевые архитектурные заметки
 
-1. **Two apply paths**: Most fields are processed by `GameInitializationService.ApplyManifest()` (populates shared sets/dicts), then read by BuildCards/CacheLoad. Card-level fields (`forceExternalOnly`, `gameNotes`, `authorOverrides`, etc.) are applied by `ApplyManifestCardOverrides()` after cards are built.
+1. **Два пути применения**: большинство полей обрабатываются в `GameInitializationService.ApplyManifest()` (наполняет общие множества/словари), затем читаются BuildCards/CacheLoad. Поигровые поля (`forceExternalOnly`, `gameNotes`, `authorOverrides` и т.п.) применяются `ApplyManifestCardOverrides()` после сборки карточек.
 
-2. **Three init paths**: Fields that affect game display must be applied in ALL three: `InitializeAsync` (full scan), `LoadCacheAndBuildCardsAsync` (Phase 1 cache display), AND `RunBackgroundScanAndMergeAsync` (Phase 2 update). Missing one causes inconsistent state between phases.
+2. **Три пути инициализации**: поля, влияющие на отображение игр, должны применяться во ВСЕХ трёх: `InitializeAsync` (полное сканирование), `LoadCacheAndBuildCardsAsync` (фаза 1, показ из кеша) И `RunBackgroundScanAndMergeAsync` (фаза 2, обновление). Пропуск одного приводит к рассинхрону состояний между фазами.
 
-3. **Case sensitivity**: `ManifestService.Normalize()` rebuilds most dicts with `StringComparer.OrdinalIgnoreCase`. Fields NOT normalized: `dxvkApiOverrides` (lookups are case-sensitive as deserialized). `lumaRenodxCompat` uses direct `Contains(game.Name)` — exact match.
+3. **Регистр символов**: `ManifestService.Normalize()` пересобирает большинство словарей с `StringComparer.OrdinalIgnoreCase`. НЕ нормализуются: `dxvkApiOverrides` (поиск чувствителен к регистру, как десериализовано). `lumaRenodxCompat` использует прямой `Contains(game.Name)` — точное совпадение.
 
-4. **Name matching**: `GetManifestDllNames()`, `GetGacSymlinkPath()`, `ResolveEngineOverride()` all try: (1) exact match, (2) trademark-stripped (™®©), (3) fully normalized. Most other fields use exact match only.
+4. **Сопоставление имён**: `GetManifestDllNames()`, `GetGacSymlinkPath()`, `ResolveEngineOverride()` пробуют: (1) точное совпадение, (2) без торговых знаков (™®©), (3) полностью нормализованное. Большинство остальных полей — только точное совпадение.
 
-5. **User override priority**: Manifest `WikiNameOverrides` only adds if key not already in `_nameMappings` (user wins). Manifest `DllNameOverrides` are blocked by `DllOverrideService` per-game opt-outs. Manifest `LegacyReShadeVersions` doesn't overwrite existing RS channel overrides.
+5. **Приоритет пользовательских переопределений**: манифестный `WikiNameOverrides` добавляется только если ключа ещё нет в `_nameMappings` (пользователь сильнее). Манифестные `DllNameOverrides` блокируются поигровыми отказами в `DllOverrideService`. Манифестный `LegacyReShadeVersions` не перезаписывает существующие переопределения канала RS.
 
-6. **`AuxInstallService.GlobalManifest`**: A static reference to the live manifest, accessible from services that don't receive the manifest via DI. Used by `RenodxIniOverrides`, `RenodxExtraSettings`, `UeExtendedCompatibility` in Update flow.
+6. **`AuxInstallService.GlobalManifest`**: статическая ссылка на живой манифест, доступная сервисам, которые не получают манифест через DI. Используется `RenodxIniOverrides`, `RenodxExtraSettings`, `UeExtendedCompatibility` в потоке обновления.
diff --git a/docs/nexus-integration.md b/docs/nexus-integration.md
index 9cb5b52..eb63944 100644
--- a/docs/nexus-integration.md
+++ b/docs/nexus-integration.md
@@ -1,104 +1,104 @@
-# Nexus Mods Full Download Integration — Implementation Guide
+# Полноценная интеграция загрузок Nexus Mods — руководство по реализации
 
-## Purpose
+## Назначение
 
-This document is a complete implementation guide for adding one-click Nexus Mods download/update support to RHI. It covers what already exists in RHI, the Nexus API in full, and exactly what needs to be built — file by file.
+Этот документ — полное руководство по реализации поддержки скачивания/обновления Nexus Mods в один клик в RHI. Он охватывает то, что уже существует в RHI, Nexus API целиком и что именно нужно построить — файл за файлом.
 
 ---
 
-## Build & Test Commands
+## Сборка и тестирование
 
 ```powershell
-# Build
+# Сборка
 dotnet build g:\RDXC\RenoDXCommander\RenoDXCommander.csproj --no-restore -v q -p:Platform=x64
 
-# Publish (deploys to the running build location)
-# Run publish.bat from g:\RDXC\
+# Публикация (деплоит в место работающей сборки)
+# Запустите publish.bat из g:\RDXC\
 ```
 
 ---
 
-## What Already Exists in RHI
+## Что уже существует в RHI
 
-### Update Detection — `NexusUpdateService.cs`
+### Определение обновлений — `NexusUpdateService.cs`
 `RenoDXCommander/Services/NexusUpdateService.cs`
 
-Already working. Uses the **Nexus GraphQL v2 API** (`https://api.nexusmods.com/v2/graphql`) — **no API key required**.
+Уже работает. Использует **Nexus GraphQL v2 API** (`https://api.nexusmods.com/v2/graphql`) — **API-ключ не нужен**.
 
-- Queries `legacyModsByDomain` with `{gameDomain, modId}` pairs
-- `ParseNexusUrl(url)` → extracts `(Domain, ModId)` from a `nexusmods.com/domain/mods/id` URL
-- Compares `updatedAt` timestamp against persisted baselines in `%LocalAppData%\RHI\nexus_baselines.json`
-- `NexusBaseline` model: `Domain`, `ModId`, `LastKnownUpdate`, `InstalledVersion`, `HasUpdate`
-- Flags `card.Status = UpdateAvailable` when remote is newer
+- Запрашивает `legacyModsByDomain` парами `{gameDomain, modId}`
+- `ParseNexusUrl(url)` → извлекает `(Domain, ModId)` из ссылки вида `nexusmods.com/domain/mods/id`
+- Сравнивает метку времени `updatedAt` с сохранёнными базовыми значениями в `%LocalAppData%\RHI\nexus_baselines.json`
+- Модель `NexusBaseline`: `Domain`, `ModId`, `LastKnownUpdate`, `InstalledVersion`, `HasUpdate`
+- Ставит `card.Status = UpdateAvailable`, когда удалённая версия новее
 
-**Missing from `NexusBaseline`:** `FileId` (the specific file_id that was installed). Needs adding.
+**Чего не хватает в `NexusBaseline`:** `FileId` (конкретный file_id установленный). Нужно добавить.
 
-### Game Catalogue — `NexusModsService.cs`
+### Каталог игр — `NexusModsService.cs`
 `RenoDXCommander/Services/NexusModsService.cs`
 
-Fetches Nexus game catalogue (`https://data.nexusmods.com/file/nexus-data/games.json`), builds a normalised lookup of game name → Nexus URL. Used for the PCGW-style wiki link button, NOT for downloads. This service is separate from download functionality.
+Загружает каталог игр Nexus (`https://data.nexusmods.com/file/nexus-data/games.json`), строит нормализованный поиск «имя игры → ссылка Nexus». Используется для кнопки вики-ссылки в стиле PCGW, НЕ для загрузок. Этот сервис отделён от функциональности загрузки.
 
-### Card State for Nexus Mods
-A card gets `IsExternalOnly = true` when `effectiveMod.SnapshotUrl == null && effectiveMod.NexusUrl != null`. These are mods hosted on Nexus that RHI can't currently download directly.
+### Состояние карточки для Nexus Mods
+Карточка получает `IsExternalOnly = true`, когда `effectiveMod.SnapshotUrl == null && effectiveMod.NexusUrl != null`. Это моды, размещённые на Nexus, которые RHI пока не может скачать напрямую.
 
-When `IsExternalOnly`:
-- `card.ExternalUrl` = the Nexus mod page URL (e.g. `https://www.nexusmods.com/kingdomsofamalurreckoning/mods/64`)
+Когда `IsExternalOnly`:
+- `card.ExternalUrl` = URL страницы мода Nexus (например, `https://www.nexusmods.com/kingdomsofamalurreckoning/mods/64`)
 - `card.ExternalLabel` = `"Download from Nexus Mods"`
-- The install button opens the browser to `ExternalUrl` (see `MainWindow.Events.cs` → `ExternalLinkButton_Click`)
-- `card.NexusUrl` = same as `ExternalUrl` for Nexus mods (set in `MainViewModel.Install.cs` and `BuildCards.cs`)
-- `AutoUpdateService` currently **excludes** `IsExternalOnly` cards — line 113: `&& !c.IsExternalOnly`
+- Кнопка установки открывает браузер на `ExternalUrl` (см. `MainWindow.Events.cs` → `ExternalLinkButton_Click`)
+- `card.NexusUrl` = то же, что `ExternalUrl` для модов Nexus (задаётся в `MainViewModel.Install.cs` и `BuildCards.cs`)
+- `AutoUpdateService` сейчас **исключает** карточки с `IsExternalOnly` — строка 113: `&& !c.IsExternalOnly`
 
-### Existing Install Record — `InstalledModRecord.cs`
+### Существующая запись об установке — `InstalledModRecord.cs`
 `RenoDXCommander/Models/InstalledModRecord.cs`
 
-Fields: `GameName`, `Store`, `AddonFileName`, `FileHash`, `InstalledAt`, `InstallPath`, `InstalledVersion`, plus others.
+Поля: `GameName`, `Store`, `AddonFileName`, `FileHash`, `InstalledAt`, `InstallPath`, `InstalledVersion` и другие.
 
-**Missing:** `NexusFileId` (int) — the file_id from Nexus API, needed to identify which file is installed vs latest. Add this field.
+**Чего не хватает:** `NexusFileId` (int) — file_id из Nexus API, нужен, чтобы отличать установленный файл от последнего. Добавьте это поле.
 
-### Single Instance Forwarding — `SingleInstanceService.cs`
+### Пересылка в единственный экземпляр — `SingleInstanceService.cs`
 `RenoDXCommander/Services/SingleInstanceService.cs`
 
-Uses a named pipe (`RenoDXCommander_AddonPipe`) to forward file paths from a second instance to the running one. `FileReceived` event fires with the path. This exact pattern is needed for NXM protocol forwarding — the NXM URL arrives as a command-line arg to a new RHI process that needs to forward it to the running instance.
+Использует именованный канал (`RenoDXCommander_AddonPipe`), чтобы пересылать пути файлов из второго экземпляра в работающий. Срабатывает событие `FileReceived` с путём. Точно этот же паттерн нужен для пересылки протокола NXM — NXM-ссылка приходит аргументом командной строки в новый процесс RHI, который должен переслать её работающему экземпляру.
 
-### Settings Persistence — `SettingsViewModel.cs`
+### Хранение настроек — `SettingsViewModel.cs`
 `RenoDXCommander/ViewModels/SettingsViewModel.cs`
 
-Uses `[ObservableProperty]` fields with `LoadSettingsFromDict` / `SaveSettingsToDict` pattern against `%LocalAppData%\RHI\settings.json`. Add `NexusApiKey` (string) and `NexusIsPremium` (bool) here.
+Использует поля `[ObservableProperty]` с паттерном `LoadSettingsFromDict` / `SaveSettingsToDict` против `%LocalAppData%\RHI\settings.json`. Добавьте сюда `NexusApiKey` (string) и `NexusIsPremium` (bool).
 
-Pattern for a new string setting:
+Паттерн для новой строковой настройки:
 ```csharp
-// Declaration
+// Объявление
 [ObservableProperty] private string _nexusApiKey = "";
 
-// Load
+// Загрузка
 if (s.TryGetValue("NexusApiKey", out var nakVal)) NexusApiKey = nakVal ?? "";
 
-// Save
+// Сохранение
 if (!string.IsNullOrEmpty(NexusApiKey)) s["NexusApiKey"] = NexusApiKey;
 ```
 
 ---
 
-## Nexus Mods API — Complete Reference
+## Nexus Mods API — полный справочник
 
-### Base URL
+### Базовый URL
 ```
 https://api.nexusmods.com/v1/
 ```
 
-### Authentication — All v1 REST Endpoints
-Every request needs these headers:
+### Аутентификация — все REST-эндпоинты v1
+Каждый запрос требует этих заголовков:
 ```
 apikey: {USER_API_KEY}
 Application-Name: RHI
 Application-Version: 2.4.x
 ```
 
-### Validate API Key + Get Membership Status
+### Проверка API-ключа + статус подписки
 ```
 GET /v1/users/validate.json
 ```
-Returns:
+Возвращает:
 ```json
 {
   "user_id": 12345,
@@ -108,13 +108,13 @@ Returns:
   "is_supporter": false
 }
 ```
-Call this when the user enters/connects their key. Store `is_premium` as `NexusIsPremium` in settings.
+Вызывайте, когда пользователь вводит/подключает ключ. Сохраняйте `is_premium` как `NexusIsPremium` в настройках.
 
-### Get Mod Files List
+### Список файлов мода
 ```
 GET /v1/games/{game_domain}/mods/{mod_id}/files.json
 ```
-Returns all files for the mod. Each file has:
+Возвращает все файлы мода. Каждый файл:
 ```json
 {
   "file_id": 67890,
@@ -127,15 +127,15 @@ Returns all files for the mod. Each file has:
   "file_name": "modname-1.5.zip"
 }
 ```
-To find the latest file to download: filter `category_name == "MAIN"`, pick highest `uploaded_timestamp`.
+Чтобы найти свежий файл для скачивания: фильтруйте `category_name == "MAIN"`, берите максимальный `uploaded_timestamp`.
 
-### Get Download Links (PREMIUM ONLY)
+### Получение ссылок для скачивания (ТОЛЬКО ПРЕМИУМ)
 ```
 GET /v1/games/{game_domain}/mods/{mod_id}/files/{file_id}/download_links.json
 ```
-**Returns HTTP 403 for free users** with message: `"You don't have permission to get download links from the API without visiting nexusmods.com — this is for premium users only."`
+**Для бесплатных пользователей возвращает HTTP 403** с сообщением: `"You don't have permission to get download links from the API without visiting nexusmods.com — this is for premium users only."`
 
-For premium users, returns:
+Для премиум-пользователей возвращает:
 ```json
 [
   {
@@ -145,43 +145,43 @@ For premium users, returns:
   }
 ]
 ```
-The `URI` is a direct HTTPS download link that **expires in ~30 minutes**. Never cache it — generate fresh per download.
+`URI` — прямая HTTPS-ссылка на скачивание, которая **протухает примерно через 30 минут**. Никогда не кешируйте её — генерируйте свежую на каждую загрузку.
 
-### Get Download Links (FREE — NXM Key Path)
-Same endpoint but with query params from the NXM protocol URL:
+### Получение ссылок (БЕСПЛАТНО — путь через NXM-ключ)
+Тот же эндпоинт, но с параметрами запроса из ссылки протокола NXM:
 ```
 GET /v1/games/{game_domain}/mods/{mod_id}/files/{file_id}/download_links.json?key={key}&expires={expires}&user_id={user_id}
 ```
-This works for free users when the params come from an NXM link generated by the Nexus website.
+Работает для бесплатных пользователей, когда параметры приходят из NXM-ссылки, сгенерированной сайтом Nexus.
 
-### Rate Limits
-- 20,000 requests per 24 hours (resets 00:00 GMT)
-- After 20k: 500 per hour
-- Response headers: `X-RL-Hourly-Remaining`, `X-RL-Daily-Remaining`, `X-RL-Hourly-Reset`, `X-RL-Daily-Reset`
-- Always check these headers and back off gracefully
+### Лимиты запросов
+- 20 000 запросов за 24 часа (сброс в 00:00 GMT)
+- После 20k: 500 в час
+- Заголовки ответа: `X-RL-Hourly-Remaining`, `X-RL-Daily-Remaining`, `X-RL-Hourly-Reset`, `X-RL-Daily-Reset`
+- Всегда проверяйте эти заголовки и корректно снижайте активность
 
 ---
 
-## NXM Protocol — Free User Download Path
+## Протокол NXM — путь загрузки бесплатного пользователя
 
-### URL Format
+### Формат ссылки
 ```
 nxm://{game_domain}/mods/{mod_id}/files/{file_id}?key={key}&expires={timestamp}&user_id={user_id}
 ```
-Example:
+Пример:
 ```
 nxm://kingdomsofamalurreckoning/mods/64/files/67890?key=AbCdEf123&expires=1787000000&user_id=99999
 ```
 
-### How It Works
-1. User clicks "Mod Manager Download" on nexusmods.com
-2. Browser fires the `nxm://` protocol, Windows looks up the registered handler
-3. The registered handler (RHI) receives the URL as a command-line arg: `RHI.exe --nxm "nxm://..."`
-4. RHI parses the URL, calls the download_links endpoint with the key params
-5. Downloads and installs the file
+### Как это работает
+1. Пользователь нажимает «Mod Manager Download» на nexusmods.com
+2. Браузер запускает протокол `nxm://`, Windows ищет зарегистрированный обработчик
+3. Зарегистрированный обработчик (RHI) получает ссылку аргументом командной строки: `RHI.exe --nxm "nxm://..."`
+4. RHI разбирает ссылку, вызывает эндпоинт download_links с параметрами ключа
+5. Скачивает и устанавливает файл
 
-### Windows Registry — Protocol Handler
-Write at RHI first launch or in installer:
+### Реестр Windows — обработчик протокола
+Записывать при первом запуске RHI или в установщике:
 ```
 HKEY_CURRENT_USER\Software\Classes\nxm
   (Default) = "URL:NXM Protocol"
@@ -190,57 +190,57 @@ HKEY_CURRENT_USER\Software\Classes\nxm
 HKEY_CURRENT_USER\Software\Classes\nxm\shell\open\command
   (Default) = "\"C:\Users\...\RHI.exe\" --nxm \"%1\""
 ```
-Use the `Environment.ProcessPath` for the RHI exe path (not `AppContext.BaseDirectory` — wrong for single-file publish).
+Используйте `Environment.ProcessPath` для пути exe RHI (не `AppContext.BaseDirectory` — неверно для single-file публикации).
 
-**Conflict with Vortex/MO2**: These apps also register `nxm://`. Whichever registered last wins. Only overwrite if no handler exists, or show a dialog offering to claim it.
+**Конфликт с Vortex/MO2**: эти приложения тоже регистрируют `nxm://`. Побеждает тот, кто зарегистрировался последним. Перезаписывайте только если обработчика нет, или показывайте диалог с предложением перехватить.
 
-### Single Instance Forwarding for NXM
-RHI may already be running when the NXM URL arrives. The new process must forward the URL to the running instance:
+### Пересылка NXM в единственный экземпляр
+RHI может уже работать к моменту прихода NXM-ссылки. Новый процесс должен переслать её работающему экземпляру:
 
-In `App.OnLaunched`, check for `--nxm` arg. If RHI is already running, call `SingleInstanceService.SendToRunningInstance("nxm:" + url)` (use a prefix to distinguish from addon files) and exit.
+В `App.OnLaunched` проверьте аргумент `--nxm`. Если RHI уже запущен, вызовите `SingleInstanceService.SendToRunningInstance("nxm:" + url)` (префикс нужен, чтобы отличать от файлов аддонов) и выйдите.
 
-In the running instance, `SingleInstanceService.FileReceived` fires. Add a check: if the received string starts with `"nxm:"`, route to the NXM handler instead of the addon drag-drop handler.
+В работающем экземпляре срабатывает `SingleInstanceService.FileReceived`. Добавьте проверку: если принятая строка начинается с `"nxm:"`, направляйте в обработчик NXM вместо обработчика drag-drop аддонов.
 
-`SingleInstanceService` pipe name: `RenoDXCommander_AddonPipe`. The existing pipe is string-based — just prefix the NXM URL with `"nxm:"` so the receiver can distinguish.
+Имя канала `SingleInstanceService`: `RenoDXCommander_AddonPipe`. Существующий канал строковый — просто добавьте к NXM-ссылке префикс `"nxm:"`, чтобы приёмник мог различить.
 
 ---
 
-## App Registration with Nexus (Required for Public Release)
+## Регистрация приложения в Nexus (нужно для публичного релиза)
 
-Before shipping to users, email `support@nexusmods.com` with:
-- A testing build of RHI demonstrating API key input
-- App name: `RHI`
-- Short description: tool for managing ReShade, RenoDX and HDR mods across PC game libraries
-- Logo: high-res, visible on dark background
+Перед выпуском пользователям напишите на `support@nexusmods.com`:
+- Тестовую сборку RHI с вводом API-ключа
+- Название приложения: `RHI`
+- Краткое описание: инструмент управления ReShade, RenoDX и HDR-модами в библиотеках ПК-игр
+- Логотип: высокое разрешение, видимый на тёмном фоне
 
-They assign a **slug** (e.g. `"rhi"`) used for SSO. Until registered, personal API keys work for testing. Using personal keys for a public app violates their AUP.
+Они выдадут **slug** (например, `"rhi"`) для SSO. До регистрации для тестирования работают персональные API-ключи. Использование персональных ключей в публичном приложении нарушает их AUP.
 
-### SSO Flow (After Registration — Optional but Better UX)
-Instead of copy-paste, users can authorise via browser:
-1. Generate UUID v4
-2. Open WebSocket: `wss://sso.nexusmods.com`
-3. Send: `{ "id": "<uuid>", "appid": "rhi" }`
-4. Ping every 30s to keep alive
-5. Open `https://www.nexusmods.com/sso?id=<uuid>` in the user's browser
-6. User clicks Authorise on the Nexus site
-7. WebSocket receives the API key as a plain string
-8. Save key, close socket
+### Поток SSO (после регистрации — опционально, но удобнее)
+Вместо копипасты пользователи могут авторизоваться через браузер:
+1. Сгенерируйте UUID v4
+2. Откройте WebSocket: `wss://sso.nexusmods.com`
+3. Отправьте: `{ "id": "<uuid>", "appid": "rhi" }`
+4. Пингуйте каждые 30 с для поддержания
+5. Откройте `https://www.nexusmods.com/sso?id=<uuid>` в браузере пользователя
+6. Пользователь нажимает Authorise на сайте Nexus
+7. WebSocket принимает API-ключ простой строкой
+8. Сохраните ключ, закройте сокет
 
-WinUI 3 `Windows.System.Launcher.LaunchUriAsync` opens the browser. For the WebSocket, use `System.Net.WebSockets.ClientWebSocket`.
+`Windows.System.Launcher.LaunchUriAsync` в WinUI 3 открывает браузер. Для WebSocket используйте `System.Net.WebSockets.ClientWebSocket`.
 
 ---
 
-## What Needs to Be Built — File by File
+## Что нужно построить — файл за файлом
 
-### 1. `SettingsViewModel.cs` — Add API Key Fields
+### 1. `SettingsViewModel.cs` — поля API-ключа
 ```csharp
 [ObservableProperty] private string _nexusApiKey = "";
 [ObservableProperty] private bool _nexusIsPremium;
 [ObservableProperty] private string _nexusUsername = "";
 ```
-Load/save following existing pattern. Never log the API key value.
+Загрузка/сохранение по существующему паттерну. Никогда не пишите значение ключа в лог.
 
-### 2. `NexusDownloadService.cs` (new file)
+### 2. `NexusDownloadService.cs` (новый файл)
 `RenoDXCommander/Services/NexusDownloadService.cs`
 
 ```csharp
@@ -256,41 +256,41 @@ public class NexusDownloadService
 }
 ```
 
-Models:
+Модели:
 ```csharp
 public record NexusUserInfo(int UserId, string Name, bool IsPremium);
 public record NexusModFile(int FileId, string Name, string Version, string CategoryName, long UploadedTimestamp, string FileName);
 ```
 
-Inject via DI as singleton. Register in `App.xaml.cs` alongside other services.
+Внедрить через DI как синглтон. Зарегистрируйте в `App.xaml.cs` рядом с другими сервисами.
 
-### 3. `InstalledModRecord.cs` — Add NexusFileId
-Add:
+### 3. `InstalledModRecord.cs` — добавить NexusFileId
+Добавьте:
 ```csharp
 public int? NexusFileId { get; set; }
 ```
-Write this when installing a Nexus mod (drag-drop today, direct download later).
+Записывать при установке мода Nexus (сегодня drag-drop, позже прямое скачивание).
 
-### 4. `NexusBaseline` — Add FileId
-In `NexusUpdateService.cs`, add to `NexusBaseline`:
+### 4. `NexusBaseline` — добавить FileId
+В `NexusUpdateService.cs` добавьте в `NexusBaseline`:
 ```csharp
 [JsonPropertyName("fileId")]
 public int? FileId { get; set; }
 ```
 
-### 5. Settings UI
-In `MainWindow.xaml`, add a new section in the relevant settings card (or create a "Nexus Mods" card). Needs:
-- A `PasswordBox` (or `TextBox`) for API key paste
-- A "Connect" button that calls `ValidateApiKeyAsync` and shows `"Connected as {name} (Premium)"` or `"Connected as {name} (Free)"`
-- A "Disconnect" button that clears the key
-- Description text explaining premium = automatic downloads, free = one-click via browser
+### 5. UI настроек
+В `MainWindow.xaml` добавьте новую секцию в соответствующую карточку настроек (или создайте карточку «Nexus Mods»). Нужно:
+- `PasswordBox` (или `TextBox`) для вставки API-ключа
+- Кнопка «Подключить», вызывающая `ValidateApiKeyAsync` и показывающая `"Connected as {name} (Premium)"` или `"Connected as {name} (Free)"`
+- Кнопка «Отключить», очищающая ключ
+- Текст описания: премиум = автоматические загрузки, бесплатный = в один клик через браузер
 
-In `SettingsHandler.cs`, initialize the UI from `ViewModel.Settings.NexusApiKey` in `SettingsButton_Click`.
+В `SettingsHandler.cs` инициализируйте UI из `ViewModel.Settings.NexusApiKey` в `SettingsButton_Click`.
 
-### 6. `NxmProtocolHandler.cs` (new file)
+### 6. `NxmProtocolHandler.cs` (новый файл)
 `RenoDXCommander/Services/NxmProtocolHandler.cs`
 
-Handles parsing and routing of NXM URLs:
+Разбирает и маршрутизирует NXM-ссылки:
 ```csharp
 public static class NxmProtocolHandler
 {
@@ -306,11 +306,10 @@ public static class NxmProtocolHandler
 
 public record NxmLink(string Domain, int ModId, int FileId, string Key, string Expires, string UserId);
 ```
+Вызывайте `RegisterProtocolHandler()` при первом запуске (сначала проверьте `IsRegistered()`, чтобы не перезаписать Vortex).
 
-Call `RegisterProtocolHandler()` on first launch (check `IsRegistered()` first to avoid overwriting Vortex).
-
-### 7. `App.OnLaunched` — NXM Argument Handling
-In `App.xaml.cs`, `OnLaunched` already checks for `--nxm` pattern from command-line args. Add:
+### 7. `App.OnLaunched` — обработка NXM-аргумента
+В `App.xaml.cs` `OnLaunched` уже проверяет паттерн `--nxm` в аргументах командной строки. Добавьте:
 ```csharp
 string? nxmArg = null;
 if (cmdArgs.Length > 1 && cmdArgs[1].StartsWith("nxm://", StringComparison.OrdinalIgnoreCase))
@@ -321,11 +320,11 @@ if (nxmIdx >= 0 && nxmIdx < cmdArgs.Length - 1)
     nxmArg = cmdArgs[nxmIdx + 1];
 ```
 
-If RHI is already running: `SingleInstanceService.SendToRunningInstance("nxm:" + nxmArg)` and exit.
-If this is the first instance: store the NXM URL, process it after `MainWindow` is ready.
+Если RHI уже запущен: `SingleInstanceService.SendToRunningInstance("nxm:" + nxmArg)` и выход.
+Если это первый экземпляр: сохраните NXM-ссылку и обработайте её, когда `MainWindow` будет готов.
 
-### 8. `SingleInstanceService.cs` — NXM Routing
-In `MainWindow.xaml.cs`, where `FileReceived` is wired:
+### 8. `SingleInstanceService.cs` — маршрутизация NXM
+В `MainWindow.xaml.cs`, где подключается `FileReceived`:
 ```csharp
 SingleInstanceService.FileReceived += path =>
 {
@@ -336,9 +335,9 @@ SingleInstanceService.FileReceived += path =>
 };
 ```
 
-`HandleIncomingNxmUrl` parses the NXM link, calls `NexusDownloadService.GetDownloadUriWithNxmKeyAsync`, downloads, then routes to the install flow.
+`HandleIncomingNxmUrl` разбирает NXM-ссылку, вызывает `NexusDownloadService.GetDownloadUriWithNxmKeyAsync`, скачивает, затем направляет в поток установки.
 
-### 9. `MainViewModel.Install.Nexus.cs` (new partial file)
+### 9. `MainViewModel.Install.Nexus.cs` (новый partial-файл)
 `RenoDXCommander/ViewModels/MainViewModel.Install.Nexus.cs`
 
 ```csharp
@@ -355,16 +354,16 @@ public partial class MainViewModel
 }
 ```
 
-`UpdateNexusModAsync` flow:
-1. Parse `card.NexusUrl` via `NexusUpdateService.ParseNexusUrl()` → get `(domain, modId)`
-2. Call `NexusDownloadService.GetModFilesAsync(domain, modId)` → get files list
-3. Find latest MAIN file (highest `UploadedTimestamp` where `CategoryName == "MAIN"`)
-4. If `NexusIsPremium`: call `GetDownloadUriAsync` → download → extract/deploy
-5. If free: open browser to mod page (existing behaviour) — NXM path handles the rest when user clicks
-6. On success: update `InstalledModRecord.NexusFileId`, call `NexusUpdateService.ResetBaseline(card.GameName)`
+Поток `UpdateNexusModAsync`:
+1. Разберите `card.NexusUrl` через `NexusUpdateService.ParseNexusUrl()` → получите `(domain, modId)`
+2. Вызовите `NexusDownloadService.GetModFilesAsync(domain, modId)` → получите список файлов
+3. Найдите свежий MAIN-файл (максимальный `UploadedTimestamp` при `CategoryName == "MAIN"`)
+4. Если `NexusIsPremium`: вызовите `GetDownloadUriAsync` → скачайте → распакуйте/разверните
+5. Если бесплатный: откройте браузер на странице мода (существующее поведение) — NXM-путь доделает остальное, когда пользователь нажмёт
+6. При успехе: обновите `InstalledModRecord.NexusFileId`, вызовите `NexusUpdateService.ResetBaseline(card.GameName)`
 
-### 10. `AutoUpdateService.cs` — Add Nexus Cards
-In `RunUpdatePassAsync`, remove `&& !c.IsExternalOnly` filter and replace with Nexus-specific handling:
+### 10. `AutoUpdateService.cs` — добавить карточки Nexus
+В `RunUpdatePassAsync` уберите фильтр `&& !c.IsExternalOnly` и замените специфичной для Nexus обработкой:
 ```csharp
 // Nexus mods — premium only for silent update
 var nexusCards = cards.Where(c =>
@@ -384,10 +383,10 @@ foreach (var card in nexusCards)
 }
 ```
 
-`AutoUpdateService` currently doesn't have access to `NexusDownloadService` — inject it via `SetViewModel` or add it to the constructor.
+`AutoUpdateService` сейчас не имеет доступа к `NexusDownloadService` — внедрите через `SetViewModel` или добавьте в конструктор.
 
-### 11. Install/Update Button Routing
-In `MainWindow.Events.cs`, `ExternalLinkButton_Click` currently always opens the browser. When a Nexus API key is configured AND user is premium, intercept:
+### 11. Маршрутизация кнопок установки/обновления
+В `MainWindow.Events.cs` `ExternalLinkButton_Click` сейчас всегда открывает браузер. Когда API-ключ Nexus настроен И пользователь премиум, перехватывайте:
 
 ```csharp
 private async void ExternalLinkButton_Click(object sender, RoutedEventArgs e)
@@ -413,29 +412,29 @@ private async void ExternalLinkButton_Click(object sender, RoutedEventArgs e)
 
 ---
 
-## Extraction / Install After Download — Exact Pattern
+## Распаковка / установка после скачивания — точный паттерн
 
-Once the CDN URI is obtained, the download and install must go through the existing infrastructure, not a new pipeline. Here is the exact flow:
+Когда CDN-URI получен, скачивание и установка должны идти через существующую инфраструктуру, а не новый конвейер. Вот точный поток:
 
-### The Nexus download is a `.zip` containing a `.addon64` file
+### Скачанное с Nexus — `.zip`, внутри файл `.addon64`
 
-This is the standard case for Nexus-hosted RenoDX mods. The flow:
+Стандартный случай для модов RenoDX на Nexus. Поток:
 
-1. Download the zip to a temp path using `HttpClient` (stream to file, same pattern as `ModInstallService.InstallAsync` steps 3–4)
-2. Extract using 7-Zip: `App.Services.GetRequiredService<ISevenZipExtractor>().Find7ZipExe()` to get the 7z exe, then `Process.Start` with args `x "{zipPath}" -o"{tempDir}" -y`
-3. Find `.addon64`/`.addon32` files in the extracted temp dir:
+1. Скачайте zip во временный путь через `HttpClient` (поток в файл, тот же паттерн, что шаги 3–4 в `ModInstallService.InstallAsync`)
+2. Распакуйте 7-Zip'ом: `App.Services.GetRequiredService<ISevenZipExtractor>().Find7ZipExe()` даёт exe 7z, затем `Process.Start` с аргументами `x "{zipPath}" -o"{tempDir}" -y`
+3. Найдите файлы `.addon64`/`.addon32` во временной папке:
    ```csharp
    var addonFiles = Directory.GetFiles(tempDir, "*.addon64", SearchOption.AllDirectories)
        .Concat(Directory.GetFiles(tempDir, "*.addon32", SearchOption.AllDirectories))
        .Where(f => Path.GetFileName(f).StartsWith("renodx-", StringComparison.OrdinalIgnoreCase))
        .ToList();
    ```
-4. Copy the addon to the game's deploy path:
+4. Скопируйте аддон в путь развертывания игры:
    ```csharp
    var deployDir = ModInstallService.GetAddonDeployPath(card.InstallPath);
    File.Copy(addonPath, Path.Combine(deployDir, addonFileName), overwrite: true);
    ```
-5. Save an `InstalledModRecord`:
+5. Сохраните `InstalledModRecord`:
    ```csharp
    var record = new InstalledModRecord
    {
@@ -449,11 +448,11 @@ This is the standard case for Nexus-hosted RenoDX mods. The flow:
    };
    _installer.SaveRecordPublic(record);
    ```
-6. Post-install steps (mirror `UpdateOrchestrationService.UpdateAllRenoDxAsync`):
-   - Deploy Engine.ini LUT: `AuxInstallService.ApplyEngineIniLutSetting(...)` if UE game
-   - Deploy Engine.ini HDR: `AuxInstallService.ApplyEngineIniHdrSettings(...)` if UE-Extended
-   - Apply `renodxIniOverrides` from manifest: `AuxInstallService.ApplyRenodxIniOverrides(...)`
-7. Update card state on dispatcher:
+6. Пост-шаги установки (зеркало `UpdateOrchestrationService.UpdateAllRenoDxAsync`):
+   - Развернуть Engine.ini LUT: `AuxInstallService.ApplyEngineIniLutSetting(...)`, если UE-игра
+   - Развернуть Engine.ini HDR: `AuxInstallService.ApplyEngineIniHdrSettings(...)`, если UE-Extended
+   - Применить `renodxIniOverrides` из манифеста: `AuxInstallService.ApplyRenodxIniOverrides(...)`
+7. Обновите состояние карточки на диспетчере:
    ```csharp
    DispatcherQueue?.TryEnqueue(() =>
    {
@@ -466,14 +465,14 @@ This is the standard case for Nexus-hosted RenoDX mods. The flow:
        card.FadeMessage(m => card.ActionMessage = m, card.ActionMessage);
    });
    ```
-8. Reset Nexus baseline: `_nexusUpdateService.ResetBaseline(card.GameName)`
-9. Save library: `SaveLibrary()`
+8. Сбросьте базовую линию Nexus: `_nexusUpdateService.ResetBaseline(card.GameName)`
+9. Сохраните библиотеку: `SaveLibrary()`
 
-**Do NOT route through `DragDropHandler.ProcessDroppedAddon`** — that method shows a game-picker dialog asking the user which game to install to. For programmatic install where you already know the card, copy the file and save the record directly as above.
+**НЕ направляйте через `DragDropHandler.ProcessDroppedAddon`** — этот метод показывает диалог выбора игры, спрашивая пользователя, куда ставить. Для программной установки, когда карточка уже известна, копируйте файл и сохраняйте запись напрямую, как выше.
 
-`DragDropHandler.ProcessDroppedArchive` and `ProcessDroppedAddon` are for **user-initiated drag-drop only** — they show ContentDialogs, ask for confirmation, and require user interaction. The download path must be silent.
+`DragDropHandler.ProcessDroppedArchive` и `ProcessDroppedAddon` — **только для drag-drop по инициативе пользователя**: они показывают ContentDialog'ы, запрашивают подтверждение и требуют взаимодействия. Путь скачивания должен быть тихим.
 
-### Deploy path helper
+### Хелпер пути развертывания
 ```csharp
 ModInstallService.GetAddonDeployPath(card.InstallPath)
 // Returns the addon subfolder if the game uses one, otherwise the install path itself
@@ -481,72 +480,72 @@ ModInstallService.GetAddonDeployPath(card.InstallPath)
 
 ---
 
-## Card State — `InstallActionLabel`, `CanInstall`, `CardRdxInstallEnabled`
+## Состояние карточки — `InstallActionLabel`, `CanInstall`, `CardRdxInstallEnabled`
 
-These are computed properties in `GameCardViewModel.RenoDX.cs`.
+Это вычисляемые свойства в `GameCardViewModel.RenoDX.cs`.
 
-**Current state for `IsExternalOnly` cards** (Nexus-only mods today):
-- `CanInstall` returns `false` — it explicitly excludes `IsExternalOnly`: `Mod?.SnapshotUrl != null && !IsInstalling && !IsExternalOnly && ...`
-- `InstallActionLabel` falls through to the Status-based labels (Install/Update/Reinstall) but the button is disabled
-- The card row shows an external link button instead of the install button
+**Текущее состояние карточек с `IsExternalOnly`** (сегодня моды только на Nexus):
+- `CanInstall` возвращает `false` — он явно исключает `IsExternalOnly`: `Mod?.SnapshotUrl != null && !IsInstalling && !IsExternalOnly && ...`
+- `InstallActionLabel` проваливается в подписи по статусу (Install/Update/Reinstall), но кнопка отключена
+- В строке карточки вместо кнопки установки показывается кнопка внешней ссылки
 
-**What needs to change** when adding direct download support:
+**Что нужно изменить** при добавлении прямого скачивания:
 
-`CanInstall` in `GameCardViewModel.RenoDX.cs` line 30 needs to include the Nexus premium path:
+`CanInstall` в `GameCardViewModel.RenoDX.cs`, строка 30, должен включить премиум-путь Nexus:
 ```csharp
 public bool CanInstall => IsRtxHdrEnabled
     || (Mod?.SnapshotUrl != null && !IsInstalling && !IsExternalOnly && (IsRsInstalled || ExcludeFromUpdateAllReShade))
     || (IsExternalOnly && NexusUrl != null && /* nexusDownloadService.IsPremium — pass via card property */);
 ```
 
-The cleanest approach: add `[ObservableProperty] private bool _nexusDirectDownloadAvailable` to `GameCardViewModel.cs`, set it in `BuildCards`/`CacheLoad` when `card.NexusUrl != null && settings.NexusIsPremium`, and use it in the `CanInstall` expression. This avoids service dependencies in the ViewModel.
+Чище всего: добавить `[ObservableProperty] private bool _nexusDirectDownloadAvailable` в `GameCardViewModel.cs`, задавать в `BuildCards`/`CacheLoad`, когда `card.NexusUrl != null && settings.NexusIsPremium`, и использовать в выражении `CanInstall`. Это позволяет избежать зависимостей ViewModel от сервисов.
 
-`InstallActionLabel` for external-only with direct download:
-- `Status == NotInstalled` → `"Download from Nexus Mods"` (or `"Install"`)
+`InstallActionLabel` для «только внешняя» с прямым скачиванием:
+- `Status == NotInstalled` → `"Download from Nexus Mods"` (или `"Install"`)
 - `Status == UpdateAvailable` → `"⬆  Update"` 
 - `Status == Installed` → `"↺  Reinstall"`
 
-The existing `ExternalLink_Click` handler in `MainWindow.Events.cs` (line 683) is what fires for the external link button. That's the interception point for premium users — add the service check there.
+Существующий обработчик `ExternalLink_Click` в `MainWindow.Events.cs` (строка 683) срабатывает для кнопки внешней ссылки. Это точка перехвата для премиум-пользователей — добавьте проверку сервиса там.
 
 ---
 
-## `GameMod.NexusUrl` — Where It Comes From
+## `GameMod.NexusUrl` — откуда он берётся
 
-**Read-only. Do not write to it programmatically.**
+**Только чтение. Не пишите в него программно.**
 
-`WikiService.cs` scrapes the RenoDX wiki mod table. For each mod row, it scans all links — if a link contains `nexusmods.com`, it's stored as `nexusUrl`. This becomes `GameMod.NexusUrl`. Set in `WikiService.FetchAllAsync()` line 233.
+`WikiService.cs` парсит таблицу модов вики RenoDX. Для каждой строки мода он сканирует все ссылки — если ссылка содержит `nexusmods.com`, она сохраняется как `nexusUrl`. Это становится `GameMod.NexusUrl`. Задаётся в `WikiService.FetchAllAsync()`, строка 233.
 
-The manifest `nexusUrlOverrides` dict can override this per game — checked in `NexusModsService.ResolveUrl()`, but that's for the **PCGW-style info button link**, not the mod download URL.
+Словарь `nexusUrlOverrides` в манифесте может переопределить это поигрово — проверяется в `NexusModsService.ResolveUrl()`, но это для **кнопки-ссылки в стиле PCGW**, а не URL загрузки мода.
 
-For the download feature, `card.NexusUrl` (from `GameMod.NexusUrl`) is the mod page URL, from which `NexusUpdateService.ParseNexusUrl()` extracts `(Domain, ModId)`. You then call the files API to find the latest file_id. **The wiki `NexusUrl` is the mod page — not a direct download link.**
+Для функции скачивания `card.NexusUrl` (из `GameMod.NexusUrl`) — URL страницы мода, из которого `NexusUpdateService.ParseNexusUrl()` извлекает `(Domain, ModId)`. Затем вызываете files-API, чтобы найти свежий file_id. **Викишный `NexusUrl` — это страница мода, а не прямая ссылка на скачивание.**
 
 ---
 
-## `CheckInstallWarningAsync` — Call Pattern
+## `CheckInstallWarningAsync` — паттерн вызова
 
-**Defined in:** `MainViewModel.Install.Luma.cs` line 1016
+**Определён в:** `MainViewModel.Install.Luma.cs`, строка 1016
 
 ```csharp
 public async Task<bool> CheckInstallWarningAsync(string gameName, string component)
 ```
 
-- `component` is a string key matching entries in `manifest.InstallWarnings` dict (e.g. `"renodx"`, `"reshade"`, `"luma"`, `"dxvk"`, etc.)
-- Returns `true` → proceed with install
-- Returns `false` → user cancelled, abort install
-- Shows a `ContentDialog` with the manifest-defined warning message if one exists for this game+component combo
+- `component` — строковый ключ, совпадающий с записями словаря `manifest.InstallWarnings` (например, `"renodx"`, `"reshade"`, `"luma"`, `"dxvk"` и т.п.)
+- Вернул `true` → продолжать установку
+- Вернул `false` → пользователь отменил, прервать установку
+- Показывает `ContentDialog` с сообщением предупреждения из манифеста, если для этой комбинации игра+компонент оно есть
 
-**Call it in `InstallNexusModAsync` before starting the download:**
+**Вызывайте в `InstallNexusModAsync` перед началом скачивания:**
 ```csharp
 if (!await CheckInstallWarningAsync(card.GameName, "renodx")) return;
 ```
 
-Use the `"renodx"` key since Nexus mods are RenoDX addons. No new component key needed.
+Используйте ключ `"renodx"`, так как моды Nexus — аддоны RenoDX. Новый ключ компонента не нужен.
 
 ---
 
-## `ExternalLink_Click` — Current Implementation
+## `ExternalLink_Click` — текущая реализация
 
-`MainWindow.Events.cs` line 683:
+`MainWindow.Events.cs`, строка 683:
 ```csharp
 internal async void ExternalLink_Click(object sender, RoutedEventArgs e)
 {
@@ -568,7 +567,7 @@ internal async void ExternalLink_Click(object sender, RoutedEventArgs e)
 }
 ```
 
-**Intercept point for Phase 2** — before the `LaunchUriAsync` call, check:
+**Точка перехвата для фазы 2** — перед вызовом `LaunchUriAsync` проверьте:
 ```csharp
 var nexusDownload = App.Services.GetRequiredService<NexusDownloadService>();
 if (card.NexusUrl != null && nexusDownload.IsApiKeyConfigured && nexusDownload.IsPremium)
@@ -582,39 +581,39 @@ if (card.NexusUrl != null && nexusDownload.IsApiKeyConfigured && nexusDownload.I
 
 ---
 
-## Implementation Order
+## Порядок реализации
 
-| Phase | Files | User benefit |
+| Фаза | Файлы | Польза для пользователя |
 |---|---|---|
-| 1 | `SettingsViewModel` + `NexusDownloadService` (validate only) + Settings UI | Users can connect account, see premium status |
-| 2 | `GetModFilesAsync` + `GetDownloadUriAsync` + `DownloadToTempAsync` + `InstallNexusModAsync` + button routing | Premium users get one-click download/update from the card |
-| 3 | `InstalledModRecord.NexusFileId` + `NexusBaseline.FileId` + record on install | Accurate update detection — know which file is installed |
-| 4 | `UpdateNexusModAsync` + `AutoUpdateService` Nexus cards | Premium users get silent auto-update |
-| 5 | `NxmProtocolHandler` registration + `App.OnLaunched` NXM parsing + `SingleInstanceService` routing | Free users get one-click from browser |
-| 6 | SSO login flow (after Nexus app registration) | Better UX than copy-paste |
+| 1 | `SettingsViewModel` + `NexusDownloadService` (только проверка ключа) + UI настроек | Пользователи могут подключить аккаунт, увидеть статус подписки |
+| 2 | `GetModFilesAsync` + `GetDownloadUriAsync` + `DownloadToTempAsync` + `InstallNexusModAsync` + маршрутизация кнопок | Премиум-пользователи получают скачивание/обновление с карточки в один клик |
+| 3 | `InstalledModRecord.NexusFileId` + `NexusBaseline.FileId` + запись при установке | Точное определение обновлений — известно, какой файл установлен |
+| 4 | `UpdateNexusModAsync` + карточки Nexus в `AutoUpdateService` | Премиум-пользователи получают тихое автообновление |
+| 5 | Регистрация `NxmProtocolHandler` + разбор NXM в `App.OnLaunched` + маршрутизация в `SingleInstanceService` | Бесплатные пользователи получают установку в один клик из браузера |
+| 6 | Вход через SSO (после регистрации приложения в Nexus) | Удобнее, чем копипаста |
 
 ---
 
-## Known Gotchas
+## Известные подводные камни
 
-- **CDN URLs expire (~30 min)** — generate fresh per download, never cache.
-- **File categories**: always pick `category_name == "MAIN"` with highest `uploaded_timestamp`. Never auto-install `OLD_VERSION` or `OPTIONAL` files.
-- **`content_preview_link` is broken** as of June 2026 — field in `/v1/.../files.json` returns 404 for files uploaded after ~June 11 2026. Do not use this field.
-- **NXM handler conflict**: if Vortex/MO2 is installed, they're already the NXM handler. Only register if no other handler exists. Or offer a setting.
-- **Single instance + NXM**: the second RHI instance has no `DispatcherQueue` yet — use `SingleInstanceService.SendToRunningInstance("nxm:" + url)` then `Environment.Exit(0)` immediately. Don't initialise the full app.
-- **API key security**: never log the key value. Store it in `settings.json` as plaintext (same as other settings) — it's a local user file. Don't add extra encryption complexity.
-- **`IsExternalOnly` flag**: this is set for mods where `SnapshotUrl == null && NexusUrl != null`. The install button currently opens a browser. After Phase 2, this button should download directly for premium users. The card's `ExternalLabel` / `InstallActionLabel` / `CanInstall` logic may need adjustment to show "Download" vs "Update" correctly.
-- **Adult content flag**: some mods require the account's adult content setting enabled. API returns 403 for these if not enabled. Handle with a clear error message.
-- **Rate limit headers**: always read from every response. If `X-RL-Daily-Remaining < 100`, back off and log a warning.
-- **`NexusModsUrl` vs `NexusUrl`**: `card.NexusModsUrl` (set by `NexusModsService`) is the PCGW-style wiki link button — unrelated to downloads. `card.NexusUrl` (set from `GameMod.NexusUrl`) is the mod page URL used for downloading. Don't confuse them.
+- **CDN-ссылки протухают (~30 мин)** — генерируйте свежую на каждую загрузку, не кешируйте.
+- **Категории файлов**: всегда берите `category_name == "MAIN"` с максимальным `uploaded_timestamp`. Никогда не устанавливайте автоматически файлы `OLD_VERSION` или `OPTIONAL`.
+- **`content_preview_link` сломан** по состоянию на июнь 2026 — поле в `/v1/.../files.json` возвращает 404 для файлов, загруженных после ~11 июня 2026. Не используйте это поле.
+- **Конфликт NXM-обработчика**: если установлен Vortex/MO2, они уже обработчик NXM. Регистрируйтесь только если другого обработчика нет. Или предложите настройку.
+- **Единственный экземпляр + NXM**: у второго экземпляра RHI ещё нет `DispatcherQueue` — используйте `SingleInstanceService.SendToRunningInstance("nxm:" + url)` и немедленно `Environment.Exit(0)`. Не инициализируйте всё приложение.
+- **Безопасность API-ключа**: никогда не пишите значение ключа в лог. Храните в `settings.json` открытым текстом (как остальные настройки) — это локальный файл пользователя. Не добавляйте лишнее шифрование.
+- **Флаг `IsExternalOnly`**: задаётся для модов, где `SnapshotUrl == null && NexusUrl != null`. Кнопка установки сейчас открывает браузер. После фазы 2 она должна скачивать напрямую для премиум-пользователей. Возможно, придётся подправить `ExternalLabel` / `InstallActionLabel` / логику `CanInstall` карточки, чтобы корректно показывать «Скачать» против «Обновить».
+- **Флаг контента 18+**: часть модов требует включённой настройки контента 18+ в аккаунте. API возвращает для них 403, если не включено. Обработайте понятным сообщением об ошибке.
+- **Заголовки лимитов**: читайте из каждого ответа. Если `X-RL-Daily-Remaining < 100`, снижайте активность и пишите предупреждение в лог.
+- **`NexusModsUrl` vs `NexusUrl`**: `card.NexusModsUrl` (задаётся `NexusModsService`) — кнопка-ссылка в стиле PCGW, к загрузкам отношения не имеет. `card.NexusUrl` (задаётся из `GameMod.NexusUrl`) — URL страницы мода для скачивания. Не путайте.
 
 ---
 
-## Summary Table
+## Сводная таблица
 
-| Capability | Premium user with API key | Free user (NXM registered) | Free user (no NXM) |
+| Возможность | Премиум с API-ключом | Бесплатный (NXM зарегистрирован) | Бесплатный (без NXM) |
 |---|---|---|---|
-| Detect updates | ✅ already works | ✅ already works | ✅ already works |
-| One-click install/update | ✅ Phase 2 | ✅ Phase 5 (browser click) | ❌ manual |
-| Silent auto-update | ✅ Phase 4 | ❌ | ❌ |
-| No browser needed | ✅ | ❌ | ❌ |
+| Определение обновлений | ✅ уже работает | ✅ уже работает | ✅ уже работает |
+| Установка/обновление в один клик | ✅ Фаза 2 | ✅ Фаза 5 (клик в браузере) | ❌ вручную |
+| Тихое автообновление | ✅ Фаза 4 | ❌ | ❌ |
+| Без браузера | ✅ | ❌ | ❌ |
diff --git a/docs/ultraplace-rhi.md b/docs/ultraplace-rhi.md
index 46153d8..aad6a5c 100644
--- a/docs/ultraplace-rhi.md
+++ b/docs/ultraplace-rhi.md
@@ -4,48 +4,48 @@ title: RHI
 permalink: /tools/rhi/
 ---
 
-# RHI — Simplified PC Gaming
+# RHI — упрощённый ПК-гейминг
 
-RHI is a desktop app that manages HDR mods, ReShade, frame limiters, DLSS/Streamline, and more across your entire PC game library. Auto-detects games from 8 storefronts with one-click install for everything. Built by RankFTW.
+RHI — настольное приложение, управляющее HDR-модами, ReShade, ограничителями FPS, DLSS/Streamline и не только во всей вашей библиотеке ПК-игр. Само находит игры из 8 магазинов и умеет ставить всё в один клик. Разработано RankFTW.
 
-## What it does
+## Что умеет
 
-- Auto-detects games from Steam, GOG, Epic, EA, Ubisoft, Xbox/Game Pass, Battle.net, and Rockstar
-- Detects each game's engine (Unreal Engine with version, Unity, RE Engine) and graphics API automatically
-- One-click install, update, and uninstall for ReShade, RenoDX, ReLimiter, Display Commander, OptiScaler, RE Framework, Luma Framework, and DXVK
-- DLSS & Streamline version management — swap SR, RR, FG independently, set DLSS presets and render scale per-game
-- ReBAR (Resizable BAR) control per-game with mode and size limit options
-- 43 shader packs with global or per-game selection
-- Drag-and-drop mods, presets, and Luma archives
-- Automatic Engine.ini and reshade.ini configuration for UE-Extended native HDR games
-- Per-game overrides for DLL naming, shader mode, addon mode, ReShade channel, DXVK variant, and launch arguments
-- Game launch directly from RHI with Steam overlay and playtime tracking
-- Nexus Mods update detection (no API key needed)
-- Automatic app and manifest updates
+- Само находит игры в Steam, GOG, Epic, EA, Ubisoft, Xbox/Game Pass, Battle.net и Rockstar
+- Определяет движок каждой игры (Unreal Engine с версией, Unity, RE Engine) и графический API автоматически
+- Установка, обновление и удаление ReShade, RenoDX, ReLimiter, Display Commander, OptiScaler, RE Framework, Luma Framework и DXVK в один клик
+- Управление версиями DLSS и Streamline — независимая подмена SR, RR, FG; пресеты DLSS и масштаб рендеринга для каждой игры
+- Управление ReBAR (Resizable BAR) для каждой игры с выбором режима и лимита размера
+- 43 набора шейдеров с глобальным или индивидуальным выбором
+- Перетаскивание модов, пресетов и архивов Luma
+- Автоматическая настройка Engine.ini и reshade.ini для игр с нативным HDR UE-Extended
+- Переопределения для каждой игры: имена DLL, режим шейдеров, режим аддонов, канал ReShade, вариант DXVK, аргументы запуска
+- Запуск игр прямо из RHI с оверлеем Steam и учётом времени в игре
+- Определение обновлений на Nexus Mods (без API-ключа)
+- Автообновление приложения и манифеста
 
-## Download
+## Загрузка
 
-Get RHI from [GitHub](https://github.com/RankFTW/RHI/releases) or [NexusMods](https://www.nexusmods.com/site/mods/1710).
+Возьмите RHI на [GitHub](https://github.com/RankFTW/RHI/releases) или [NexusMods](https://www.nexusmods.com/site/mods/1710).
 
-For a detailed user guide, see the [RHI documentation](https://github.com/RankFTW/RHI/blob/main/docs/DETAILED_GUIDE.md).
+Подробное руководство пользователя — в [документации RHI](https://github.com/RankFTW/RHI/blob/main/docs/DETAILED_GUIDE.md).
 
-## Getting started
+## Начало работы
 
-1. Download and run RHI — your games appear automatically
-2. Pick a game and click Install on the components you want (ReShade, RenoDX, a frame limiter)
-3. Launch the game, press Home to open ReShade, go to Add-ons, and configure RenoDX
-4. Use Update All to keep everything current across your library
+1. Скачайте и запустите RHI — ваши игры появятся автоматически
+2. Выберите игру и нажмите «Установить» у нужных компонентов (ReShade, RenoDX, ограничитель FPS)
+3. Запустите игру, нажмите Home, чтобы открыть ReShade, зайдите в Add-ons и настройте RenoDX
+4. Используйте «Обновить всё», чтобы поддерживать всю библиотеку в актуальном состоянии
 
-RHI deploys a pre-configured `reshade.ini` with unnecessary addons disabled and optimal settings for HDR. UE-Extended games get Engine.ini HDR settings deployed automatically — no manual INI editing needed.
+RHI разворачивает преднастроенный `reshade.ini` с отключёнными ненужными аддонами и оптимальными настройками для HDR. Для игр UE-Extended настройки HDR в Engine.ini разворачиваются автоматически — вручную править INI не нужно.
 
-## Tips
+## Советы
 
-- If a game isn't detected, drag the .exe directly into the RHI window
-- Check the per-game Info buttons for game-specific setup notes
-- The Config button opens the game's Engine.ini folder directly
-- If installing ReShade manually (without RHI), disable the "Generic Depth" and "Effect Runtime Sync" addons — they cost performance with no benefit when using RenoDX
-- For UE-Extended native HDR games: enable the in-game HDR option if one exists
+- Если игра не определилась — перетащите её .exe прямо в окно RHI
+- Заглядывайте в индивидуальные кнопки «Инфо» — там заметки по настройке конкретных игр
+- Кнопка «Конфиг» открывает папку Engine.ini игры напрямую
+- Если ставите ReShade вручную (без RHI), отключите аддоны «Generic Depth» и «Effect Runtime Sync» — они тратят производительность без пользы при использовании RenoDX
+- Для игр с нативным HDR UE-Extended: включите HDR-опцию в самой игре, если она есть
 
-## Want to learn more about HDR?
+## Хотите узнать о HDR больше?
 
-See the [HDR guide]({{ '/hdr' | relative_url }}) for a deeper explanation of why HDR matters, what RenoDX does, and how to get the best results.
+Загляните в [руководство по HDR]({{ '/hdr' | relative_url }}) — там подробнее о том, зачем нужен HDR, что делает RenoDX и как добиться лучшего результата.
diff --git a/motd.md b/motd.md
index 6c89ea2..d0e9fb5 100644
--- a/motd.md
+++ b/motd.md
@@ -1 +1 @@
-Hi to all the new users. If you need any help with anything then we have support channels on The Ultra Place and RenoDX Discords. Thank you for all of your support, Rank
\ No newline at end of file
+Привет всем новым пользователям. Если вам нужна помощь — у нас есть каналы поддержки в Discord-сообществах The Ultra Place и RenoDX. Спасибо за вашу поддержку, Rank
diff --git a/todo/nexus-api-response.md b/todo/nexus-api-response.md
index fffe0ab..e4150e7 100644
--- a/todo/nexus-api-response.md
+++ b/todo/nexus-api-response.md
@@ -1,92 +1,92 @@
-# Nexus Mods API Approval — Response & Action Plan
+# Одобрение Nexus Mods API — ответ и план действий
 
-## Their Demands
+## Их требования
 
-> 1. Your manager appears to have credential leakage. The CrashReporter.cs:100-110 logs permanently to %LocalAppData%\RHI\logs containing a lot of information openly such as user IDs which it should not.
-> 2. Automated downloads. The AUP expressly objects to API keys being used "without the action being initiated by the user." A user opt-in would be sufficient.
-> 3. There appears to be some references to GraphQL in your code. Is that intended?
+> 1. Похоже, у вашего менеджера утечка учётных данных. CrashReporter.cs:100-110 перманентно пишет в %LocalAppData%\RHI\logs много открытой информации, например пользовательские ID, чего быть не должно.
+> 2. Автоматические скачивания. AUP прямо возражает против использования API-ключей «без действия, инициированного пользователем». Достаточно согласия пользователя (opt-in).
+> 3. В вашем коде есть упоминания GraphQL. Это намеренно?
 
 ---
 
-## Our Response to Each Point
+## Наш ответ по каждому пункту
 
-### 1. Credential Leakage in Session Logs
+### 1. Утечка учётных данных в логах сессий
 
-**Their concern:** Session logs at `%LocalAppData%\RHI\logs\session_*.txt` contain full file paths (e.g. `C:\Users\Mark\...`, Steam `userdata\{steamId}\...` paths) and Xbox AUMIDs. Logs are written by `CrashReporter.Log()` which calls `AppendSessionLog()` at lines 100–110 of `CrashReporter.cs`.
+**Их замечание:** логи сессий в `%LocalAppData%\RHI\logs\session_*.txt` содержат полные пути файлов (например, `C:\Users\Mark\...`, пути `userdata\{steamId}\...` Steam) и AUMID Xbox. Логи пишет `CrashReporter.Log()`, вызывающий `AppendSessionLog()` в строках 100–110 `CrashReporter.cs`.
 
-**Note:** These are local logs stored on the user's own machine and never transmitted anywhere. However Nexus consider logging this data at all to be a privacy concern.
+**Примечание:** это локальные логи на машине самого пользователя, никуда не передаются. Но Nexus считает, что само логирование таких данных — вопрос приватности.
 
-**Our fix (deferred to next update):** Sanitise paths before writing to the session log:
-- Replace `C:\Users\{username}\` → `%USERPROFILE%\` 
-- Replace full Steam `userdata\{numericId}` paths → `userdata\[userid]\`
-- Truncate or mask Xbox AUMIDs — log only the game name, not the full package family string
-- Implementation: add a `SanitisePath(string entry)` static helper to `CrashReporter.cs` that runs a regex substitution before `AppendSessionLog` is called
+**Наш фикс (отложен до следующего обновления):** чистить пути перед записью в лог сессии:
+- Заменять `C:\Users\{username}\` → `%USERPROFILE%\` 
+- Заменять полные пути `userdata\{numericId}` Steam → `userdata\[userid]\`
+- Обрезать или маскировать AUMID Xbox — в лог только имя игры, без полной строки пакета
+- Реализация: добавить в `CrashReporter.cs` статический хелпер `SanitisePath(string entry)`, выполняющий regex-замену перед вызовом `AppendSessionLog`
 
-**Files to change:** `g:\RDXC\RenoDXCommander\Services\CrashReporter.cs`
+**Какие файлы менять:** `g:\RDXC\RenoDXCommander\Services\CrashReporter.cs`
 
 ---
 
-### 2. Automated API Calls Without User Action
+### 2. Автоматические вызовы API без действия пользователя
 
-**Their concern:** `NexusUpdateService.CheckForUpdatesAsync` (which POSTs to `api.nexusmods.com/v2/graphql`) fires automatically:
-- On every app startup, via `RunBackgroundScanAndMergeAsync`
-- Every 4 hours via the periodic update timer in `MainViewModel.Update.cs`
+**Их замечание:** `NexusUpdateService.CheckForUpdatesAsync` (POST на `api.nexusmods.com/v2/graphql`) срабатывает автоматически:
+- При каждом запуске приложения через `RunBackgroundScanAndMergeAsync`
+- Каждые 4 часа по периодическому таймеру обновлений в `MainViewModel.Update.cs`
 
-The AUP requires API usage to be initiated by the user. A user opt-in toggle is sufficient.
+AUP требует, чтобы использование API инициировалось пользователем. Достаточно переключателя opt-in.
 
-**Our fix (deferred to next update):**
+**Наш фикс (отложен до следующего обновления):**
 
-Add a **"Background Update Checks" combo box** next to the existing Check for Updates button in Settings. Two states:
-- **On** (default): current behaviour — full update checks on startup and every 4 hours, including Nexus GraphQL calls
-- **Minimal**: manifest files only on startup/timer (RHI manifest, DLSS manifest, rhi-repo DB). All component update checks (RenoDX addons, ReShade, RE Framework, ReLimiter, DC, OptiScaler, Nexus) only fire when the user explicitly clicks Refresh or Update All
+Добавить **комбобокс «Фоновые проверки обновлений»** рядом с существующей кнопкой проверки обновлений в настройках. Два состояния:
+- **Вкл** (по умолчанию): текущее поведение — полные проверки обновлений при запуске и каждые 4 часа, включая вызовы Nexus GraphQL
+- **Минимальный**: только файлы манифестов при запуске/по таймеру (манифест RHI, манифест DLSS, база rhi-repo). Все проверки обновлений компонентов (аддоны RenoDX, ReShade, RE Framework, ReLimiter, DC, OptiScaler, Nexus) срабатывают только при явном нажатии «Обновить» или «Обновить всё»
 
-The Nexus GraphQL calls specifically are gated by the new setting. When set to Minimal, they only run on explicit user action.
+Вызовы Nexus GraphQL конкретно ограждаются новой настройкой. В режиме «Минимальный» они выполняются только по явному действию пользователя.
 
-**Files to change:**
-- `g:\RDXC\RenoDXCommander\ViewModels\SettingsViewModel.cs` — add `BackgroundUpdateChecks` property (persisted)
-- `g:\RDXC\RenoDXCommander\ViewModels\MainViewModel.Update.cs` — gate `CheckForUpdatesAsync` and Nexus check behind setting
-- `g:\RDXC\RenoDXCommander\SettingsHandler.cs` — add combo box to Settings UI next to Check for Updates button
+**Какие файлы менять:**
+- `g:\RDXC\RenoDXCommander\ViewModels\SettingsViewModel.cs` — добавить свойство `BackgroundUpdateChecks` (с сохранением)
+- `g:\RDXC\RenoDXCommander\ViewModels\MainViewModel.Update.cs` — оградить `CheckForUpdatesAsync` и проверку Nexus настройкой
+- `g:\RDXC\RenoDXCommander\SettingsHandler.cs` — добавить комбобокс в UI настроек рядом с кнопкой проверки обновлений
 
 ---
 
-### 3. GraphQL Usage
+### 3. Использование GraphQL
 
-**Their question:** Is the GraphQL usage intentional?
+**Их вопрос:** использование GraphQL намеренно?
 
-**Our answer:** Yes. RHI uses the Nexus Mods GraphQL v2 API (`api.nexusmods.com/v2/graphql`) for:
-- **Update detection**: querying `legacyModsByDomain` to check if a newer mod version is available (compares installed version string against the latest `version` field on the node)
-- **Mod summaries**: fetching the `summary` field for the Info button dialog
+**Наш ответ:** Да. RHI использует GraphQL v2 API Nexus Mods (`api.nexusmods.com/v2/graphql`) для:
+- **Определения обновлений**: запрос `legacyModsByDomain`, чтобы проверить, есть ли новая версия мода (сравнивает строку установленной версии с последним полем `version` на узле)
+- **Описаний модов**: получение поля `summary` для диалога кнопки «Инфо»
 
-No API key is used. The GraphQL endpoint is called anonymously. Nexus confirmed in earlier correspondence that anonymous GraphQL usage is permitted.
+API-ключ не используется. GraphQL-эндпоинт вызывается анонимно. Nexus ранее в переписке подтвердил, что анонимное использование GraphQL разрешено.
 
-**Confirm in reply:** State explicitly that GraphQL is intentional and used for update detection and mod summary display. No authentication, no API key.
+**Подтвердить в ответе:** прямо указать, что GraphQL намеренный и используется для определения обновлений и показа описаний модов. Без аутентификации, без API-ключа.
 
 ---
 
-## Draft Reply to Nexus
+## Черновик ответа Nexus
 
-> Hi,
+> Привет,
 >
-> Thank you for the detailed feedback. Here's our response to each point:
+> Спасибо за подробный отклик. Отвечаем по каждому пункту:
 >
-> **1. Session logs:** You're correct that our session logs write full file paths including usernames and AUMIDs to the user's local AppData folder. These logs are never transmitted outside the user's machine, but we understand your concern about logging this data at all. We will sanitise paths in the session log before the next release — replacing usernames with %USERPROFILE%, masking Steam user IDs in paths, and truncating Xbox AUMIDs to the game name only.
+> **1. Логи сессий:** Вы правы, наши логи сессий пишут полные пути файлов, включая имена пользователей и AUMID, в локальную папку AppData пользователя. Эти логи никогда не передаются за пределы машины пользователя, но мы понимаем ваше опасение насчёт самого факта логирования этих данных. Перед следующим релизом мы будем чистить пути в логе сессии — заменять имена пользователей на %USERPROFILE%, маскировать ID пользователей Steam в путях и обрезать AUMID Xbox до имени игры.
 >
-> **2. Automated API calls:** We will add a "Background Update Checks" setting that defaults to On (current behaviour) but can be set to Minimal, which restricts automatic background calls to manifest fetches only. All Nexus GraphQL calls in Minimal mode will only fire when the user explicitly initiates a Refresh or Update All action. This opt-in toggle will be prominently placed next to the existing update check controls.
+> **2. Автоматические вызовы API:** Мы добавим настройку «Фоновые проверки обновлений», по умолчанию «Вкл» (текущее поведение), которую можно поставить в «Минимальный» — тогда автоматические фоновые вызовы ограничатся только загрузкой манифестов. Все вызовы Nexus GraphQL в минимальном режиме будут срабатывать только при явном действии пользователя: «Обновить» или «Обновить всё». Этот переключатель opt-in будет заметно расположен рядом с существующими элементами проверки обновлений.
 >
-> **3. GraphQL:** Yes, this is intentional. We use the Nexus Mods GraphQL v2 API anonymously (no API key) for two purposes: checking whether a newer version of an installed mod is available, and fetching mod summaries for the in-app info dialog. We are not using any authenticated endpoints.
+> **3. GraphQL:** Да, это намеренно. Мы используем GraphQL v2 API Nexus Mods анонимно (без API-ключа) для двух целей: проверка доступности новой версии установленного мода и получение описаний модов для диалога информации в приложении. Аутентифицированные эндпоинты мы не используем вовсе.
 >
-> We will implement both fixes before our next public release and can share the updated code for review at that point.
+> Оба исправления будут реализованы до следующего публичного релиза; в этот момент мы можем предоставить обновлённый код на проверку.
 >
-> Kind regards,
+> С уважением,
 > Mark
 
 ---
 
-## Implementation Checklist (Next Update)
+## Чек-лист реализации (следующее обновление)
 
-- [ ] `CrashReporter.cs` — add `SanitisePath()` helper, apply to `Log()` before `AppendSessionLog()`
-- [ ] `SettingsViewModel.cs` — add `BackgroundUpdateChecks` string property ("On" / "Minimal"), load/save
-- [ ] `MainViewModel.Update.cs` — gate `CheckForUpdatesAsync` Nexus path behind `Settings.BackgroundUpdateChecks == "On"`
-- [ ] `SettingsHandler.cs` — add ComboBox next to Check for Updates button, wire to `BackgroundUpdateChecks`
-- [ ] Reply to Nexus with the draft above
-- [ ] Share updated code with Nexus for review before next public release
+- [ ] `CrashReporter.cs` — добавить хелпер `SanitisePath()`, применять в `Log()` перед `AppendSessionLog()`
+- [ ] `SettingsViewModel.cs` — добавить строковое свойство `BackgroundUpdateChecks` («On» / «Minimal»), загрузка/сохранение
+- [ ] `MainViewModel.Update.cs` — оградить Nexus-путь `CheckForUpdatesAsync` условием `Settings.BackgroundUpdateChecks == "On"`
+- [ ] `SettingsHandler.cs` — добавить ComboBox рядом с кнопкой проверки обновлений, привязать к `BackgroundUpdateChecks`
+- [ ] Ответить Nexus по черновику выше
+- [ ] Передать обновлённый код Nexus на проверку до следующего публичного релиза
diff --git a/todo/rhi-db-wiki-migration.md b/todo/rhi-db-wiki-migration.md
index c653d6b..252f476 100644
--- a/todo/rhi-db-wiki-migration.md
+++ b/todo/rhi-db-wiki-migration.md
@@ -1,25 +1,25 @@
-# RHI Database — Full Wiki Replacement Plan
+# База данных RHI — план полной замены вики
 
-## Goal
+## Цель
 
-Replace the RenoDX wiki (`https://github.com/clshortfuse/renodx/wiki/Mods`) as the source of truth for mod data with the RHI-maintained database (`rhi-repo`). The wiki will no longer be fetched at all once the DB is complete and trusted.
+Заменить вики RenoDX (`https://github.com/clshortfuse/renodx/wiki/Mods`) как источник истины по данным модов на базу, поддерживаемую RHI (`rhi-repo`). Вики перестанет загружаться вовсе, как только база будет полна и ей можно доверять.
 
 ---
 
-## Current State
+## Текущее состояние
 
-- `RenoDxDbSource` setting controls which source drives `_allMods`: `"WikiOnly"` (default), `"DbOnly"`, `"Hybrid"`
-- `WikiService.FetchAllAsync` always runs regardless of `RenoDxDbSource` — the wiki result is then discarded by `MergeDbSources` in DbOnly mode
-- The wiki is also used for Luma matching, game name normalization, and `SeenWikiMods` tracking
-- DB is currently dev-only (`DevUnlockService.IsUnlocked` gate in `InitializeAsync` and `RunBackgroundScanAndMergeAsync`)
+- Настройка `RenoDxDbSource` управляет тем, какой источник кормит `_allMods`: `"WikiOnly"` (по умолчанию), `"DbOnly"`, `"Hybrid"`
+- `WikiService.FetchAllAsync` выполняется всегда, независимо от `RenoDxDbSource` — в режиме DbOnly результат вики просто отбрасывается в `MergeDbSources`
+- Вики также используется для сопоставления Luma, нормализации имён игр и отслеживания `SeenWikiMods`
+- База пока доступна только разработчикам (врата `DevUnlockService.IsUnlocked` в `InitializeAsync` и `RunBackgroundScanAndMergeAsync`)
 
 ---
 
-## Phase 1 — Skip wiki fetch in DbOnly mode (quick win)
+## Фаза 1 — пропуск загрузки вики в режиме DbOnly (быстрая победа)
 
-**Files:** `MainViewModel.Init.cs`, `MainViewModel.BackgroundScan.cs`
+**Файлы:** `MainViewModel.Init.cs`, `MainViewModel.BackgroundScan.cs`
 
-Add a guard around `WikiService.FetchAllAsync` calls:
+Добавьте ограждение вокруг вызовов `WikiService.FetchAllAsync`:
 
 ```csharp
 if (!string.Equals(_settingsViewModel.RenoDxDbSource, "DbOnly", StringComparison.OrdinalIgnoreCase))
@@ -28,68 +28,68 @@ if (!string.Equals(_settingsViewModel.RenoDxDbSource, "DbOnly", StringComparison
 }
 ```
 
-Also guard `SeenWikiMods` updates — those should only run when the wiki is the active source.
+Также оградите обновления `SeenWikiMods` — они должны выполняться, только когда вики активный источник.
 
-**Result:** Saves ~300ms on startup and one unnecessary HTTP request per background scan in DbOnly mode.
+**Результат:** экономит ~300 мс при запуске и один лишний HTTP-запрос на фоновое сканирование в режиме DbOnly.
 
 ---
 
-## Phase 2 — Migrate remaining wiki dependencies to DB
+## Фаза 2 — перенос оставшихся зависимостей от вики на базу
 
-These wiki-only features must be ported before the wiki can be fully removed:
+Эти функции, доступные только через вики, нужно перенести, прежде чем вики можно будет удалить полностью:
 
-### 2a. Luma matching
-`MatchLumaGame(gameName)` searches `_allMods` for Luma entries by name. This is currently wiki-sourced. The DB has a separate `RenoDXdb-unreal.json` table for UE-Extended games. Luma named mods should be added to the DB as a `lumaUrl` or `isLuma: true` field, or kept as a separate DB endpoint.
+### 2a. Сопоставление Luma
+`MatchLumaGame(gameName)` ищет записи Luma по имени в `_allMods`. Сейчас источник — вики. В базе есть отдельная таблица `RenoDXdb-unreal.json` для игр UE-Extended. Именные моды Luma стоит добавить в базу полем `lumaUrl` или `isLuma: true`, либо держать отдельным эндпоинтом базы.
 
-### 2b. `SeenWikiMods` / "New Mods" detection
-`SeenWikiModsService` tracks which mod names the user has already seen — used to show the "New Mods" notification badge. Currently seeded from wiki mod names. Should be seeded from DB mod names instead. Low risk change.
+### 2b. `SeenWikiMods` / определение «новых модов»
+`SeenWikiModsService` отслеживает, какие имена модов пользователь уже видел — используется для значка уведомления «Новые моды». Сейчас наполняется именами модов из вики. Должно наполняться именами из базы. Изменение с низким риском.
 
-### 2c. Game name normalization for wiki matching
-`GameDetectionService.MatchGame` compares detected game names against normalized wiki names for `IsWikiExclusion`, `WikiStatusBadge`, etc. If the wiki is removed, these lookups need to be driven from DB entries. The DB `name` field already uses the same naming convention.
+### 2c. Нормализация имён игр для сопоставления с вики
+`GameDetectionService.MatchGame` сверяет найденные имена игр с нормализованными именами вики для `IsWikiExclusion`, `WikiStatusBadge` и т.п. Если вики удаляется, эти поиски должны питаться записями базы. Поле `name` в базе уже использует ту же конвенцию имён.
 
-### 2d. `ToggleWikiExclusion` and per-game wiki inclusion overrides
-`IGameNameService.WikiExclusions` / `WikiOptIns` currently reference "the wiki" in their semantics. Rename to `ModDbExclusions` / `ModDbOptIns` and update all call sites. Pure rename, no logic change.
+### 2d. `ToggleWikiExclusion` и поигровые переопределения включения в вики
+Семантика `IGameNameService.WikiExclusions` / `WikiOptIns` сейчас отсылает к «вики». Переименуйте в `ModDbExclusions` / `ModDbOptIns` и обновите все точки вызова. Чистое переименование, без смены логики.
 
-### 2e. Deprecated mods filter
-`WikiService` skips mods listed after a "Deprecated" heading. The DB should have a `"status": "Deprecated"` or just omit deprecated mods. Confirm DB has no deprecated entries before removing the filter.
+### 2e. Фильтр устаревших модов
+`WikiService` пропускает моды, перечисленные после заголовка «Deprecated». В базе должно быть `"status": "Deprecated"`, либо устаревшие моды просто опускаются. Перед снятием фильтра убедитесь, что в базе нет устаревших записей.
 
 ---
 
-## Phase 3 — Remove wiki entirely
+## Фаза 3 — полное удаление вики
 
-1. Delete `WikiService.cs` and `IWikiService.cs`
-2. Remove `WikiService` from `App.xaml.cs` DI registration
-3. Remove all `_wikiService` field references in `MainViewModel`
-4. Remove `_allMods` population from `WikiService.FetchAllAsync` — replace with DB-only fetch
-5. Change `RenoDxDbSource` default to `"DbOnly"` permanently (or remove the setting entirely)
-6. Remove `RenoDxDbSourceCard` from `MainWindow.xaml` and `SettingsHandler.InitRenoDxDbSourceCombo()`
-7. Remove `DevUnlockService.IsUnlocked` gate from `RenoDXDbService.FetchAllAsync` call sites
-8. Update `SeenWikiMods` → `SeenDbMods` references throughout
+1. Удалить `WikiService.cs` и `IWikiService.cs`
+2. Убрать `WikiService` из регистрации DI в `App.xaml.cs`
+3. Убрать все ссылки на поле `_wikiService` в `MainViewModel`
+4. Убрать наполнение `_allMods` из `WikiService.FetchAllAsync` — заменить загрузкой только из базы
+5. Поменять умолчание `RenoDxDbSource` на `"DbOnly"` навсегда (или убрать настройку целиком)
+6. Убрать `RenoDxDbSourceCard` из `MainWindow.xaml` и `SettingsHandler.InitRenoDxDbSourceCombo()`
+7. Убрать врата `DevUnlockService.IsUnlocked` из мест вызова `RenoDXDbService.FetchAllAsync`
+8. Обновить ссылки `SeenWikiMods` → `SeenDbMods` по всему коду
 
 ---
 
-## DB Completeness Requirements Before Phase 3
+## Требования к полноте базы перед фазой 3
 
-The DB must cover all mods currently on the RenoDX wiki before the wiki can be removed:
-- All named game-specific mods (currently ~954 wiki entries vs 272 DB entries)
-- All generic UE-Extended games (currently wiki-scraped; DB has ~70)
-- Status field parity: `"Done"`, `"WIP"`, `"Deprecated"` matching wiki ✅/🚧 semantics
-- `snapshotUrl` for every mod (same URLs as wiki — already the case for existing DB entries)
+База должна покрыть все моды, сейчас присутствующие на вики RenoDX, прежде чем вики можно будет убрать:
+- Все именные поигровые моды (сейчас ~954 записи в вики против 272 в базе)
+- Все универсальные игры UE-Extended (сейчас скрейпятся с вики; в базе ~70)
+- Паритет поля статуса: `"Done"`, `"WIP"`, `"Deprecated"` с семантикой ✅/🚧 вики
+- `snapshotUrl` для каждого мода (те же ссылки, что в вики — для существующих записей базы уже так)
 
-Run `docs/merge_game_db.py` against community submissions to grow the DB coverage faster.
+Запускайте `docs/merge_game_db.py` по заявкам сообщества, чтобы быстрее наращивать покрытие базы.
 
 ---
 
-## Key Files
+## Ключевые файлы
 
-| File | Role |
+| Файл | Роль |
 |------|------|
-| `RenoDXCommander/Services/WikiService.cs` | Fetches and parses RenoDX wiki — to be removed in Phase 3 |
-| `RenoDXCommander/Services/RenoDXDbService.cs` | Fetches rhi-repo DB — becomes the sole mod source |
-| `RenoDXCommander/ViewModels/MainViewModel.cs` | `MergeDbSources()` — merge logic to simplify once wiki removed |
-| `RenoDXCommander/ViewModels/MainViewModel.Init.cs` | Add Phase 1 guard here |
-| `RenoDXCommander/ViewModels/MainViewModel.BackgroundScan.cs` | Add Phase 1 guard here |
-| `RenoDXCommander/Services/SeenWikiModsService.cs` | Rename + reroute to DB in Phase 2 |
-| `game-db/game_db.json` | Community submission data |
-| `docs/RenoDXdb.json` | Named mods DB file |
-| `docs/RenoDXdb-unreal-ue-extended.json` | UE-Extended DB file |
+| `RenoDXCommander/Services/WikiService.cs` | Загружает и парсит вики RenoDX — удаляется в фазе 3 |
+| `RenoDXCommander/Services/RenoDXDbService.cs` | Загружает базу rhi-repo — станет единственным источником модов |
+| `RenoDXCommander/ViewModels/MainViewModel.cs` | `MergeDbSources()` — логику слияния упростить после удаления вики |
+| `RenoDXCommander/ViewModels/MainViewModel.Init.cs` | Сюда добавить ограждение фазы 1 |
+| `RenoDXCommander/ViewModels/MainViewModel.BackgroundScan.cs` | Сюда добавить ограждение фазы 1 |
+| `RenoDXCommander/Services/SeenWikiModsService.cs` | Переименовать + перенаправить на базу в фазе 2 |
+| `game-db/game_db.json` | Данные заявок сообщества |
+| `docs/RenoDXdb.json` | Файл базы именных модов |
+| `docs/RenoDXdb-unreal-ue-extended.json` | Файл базы UE-Extended |
